{
  "name": "@crabgic/engine-core",
  "version": "0.0.0",
  "private": true,
  "description": "EngineAdapter contract, envelope compiler, fake engine (phase 03: EngineAdapter contract + envelope compiler; fake engine lands in packages/testkit).",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "!dist/**/*.test.*",
    "!dist/**/test-support/**",
    "goldens"
  ],
  "scripts": {
    "build": "tsc -b",
    "build:goldens": "tsc -b && node scripts/write-goldens.ts"
  },
  "dependencies": {
    "@crabgic/contracts": "0.0.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "fast-check": "4.9.0"
  }
}
