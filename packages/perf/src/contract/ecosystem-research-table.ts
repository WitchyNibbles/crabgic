/**
 * Pinned "ecosystem research" budget defaults (source #2).
 *
 * Implementation moved to `@crabgic/contracts` under ledger Gap 21 — see
 * `./acceptance-criteria-parser.ts`'s header for why. Re-exported so this
 * module's own tests still prove the move changed no behavior.
 */
export {
  ECOSYSTEM_RESEARCH_BUDGETS,
  ecosystemResearchBudgets,
  isKnownResearchEcosystem,
  KNOWN_RESEARCH_ECOSYSTEMS,
} from "@crabgic/contracts";
