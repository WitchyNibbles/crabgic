/**
 * The two human-mode report shapes — RELOCATED to `@crabgic/contracts`
 * (2026-08-11) and re-exported here verbatim, in the manner of `./format.ts`.
 * Implementation: `packages/contracts/src/presentation/reports.ts`, which
 * carries the reasoning for the cardinality split and for the move.
 */
export { CLI_TEXT, pluralize, renderItemListReport, renderResultLine } from "@crabgic/contracts";
export type { ItemListReport } from "@crabgic/contracts";
