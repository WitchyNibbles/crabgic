# Phase 04 — Event journal, snapshots, idempotency, leases

| | |
|---|---|
| **Depends on** | 02 |
| **Unlocks** | 05, 07, 16 (direct); 23 transitively (via 05/07/16 — see Interfaces produced, "imported directly by 05, 07, 16 ... transitively by ... 23") |
| **Sources** | original plan "Durable execution"; adaptation §4.5 (session_id durability), §5.6 (crash recovery, plan-limit parking), §8 (journal/lease/idempotency invariants) |
| **Primary package** | `packages/journal` |

## Goal

When this phase is done, every phase that needs durable state — 05's registries, 07's freeze/quarantine records, 09's evidence reads, 13's fan-out and rate-limit parking, 16's exactly-once mutation pipeline, and 23's release-gate report — writes through one append-only, hash-chained, crash-tested journal instead of inventing its own persistence. Entries are typed against `JournalEntryType` and work-unit attempts against `WorkUnitAttemptStatus` (both 02); a kill -9 at any point in any operation this package owns always recovers to a valid, corruption-free state; identical-content operations replay byte-for-byte instead of re-executing; and exactly one supervisor process holds a project's lease at a time. Before this phase, no subsystem has a durable write path; after it, 05 (and everything behind it) can be built assuming crash-safety, replay-safety, and mutual exclusion are already solved.

## In scope

- **Journal:** append-only ndjson entry log, typed against the closed `JournalEntryType` union (02 — 13 members, listed under Interfaces consumed). This package owns the single append/query codec every phase writes through and reads from — not what triggers a given entry, or which phase decides to write which member (see Out of scope). SHA-256 hash chain (`prevHash`/`hash` per entry); append = write → `fsync(file)` → `fsync(dir)`; segment rotation at a size/age threshold; tail repair on torn writes (truncate to the last valid chained entry, report the truncation as its own entry).
- **Snapshots:** atomic `RunSnapshot` (02 schema) via temp-file + rename; recovery = load the latest snapshot + replay journal entries after its sequence number.
- **Idempotency registry:** keyed `(operationId, contentHash)`. Same id + same hash → returns the previously recorded result byte-identical, no re-execution. Same id + different hash → typed conflict failure, never a silent overwrite. Backs 16's `RemoteOperationRecord` (02) exactly-once pipeline and 08's CAS-ref rebuild loop.
- **Work-unit attempt tracking:** every `WorkUnit` (02) attempt persisted with its engine `session_id` and a status typed against `WorkUnitAttemptStatus` (02: at least `dispatched | succeeded | failed | parked:rate_limit`; full membership at 02's discretion). `parked:rate_limit` retains `session_id` so a later `resume` can continue the same engine conversation.
- **Leases:** per-project lease file, PID + process-start-time validation, 5 s heartbeat, takeover only after expiry **and** start-time mismatch — guards 05's single-supervisor-per-project invariant.
- **Evidence query surface:** a read path over `evidence_pointer` entries, filterable by run/change-set/work-unit ID, returning `EvidenceRecord` (02 schema, content decided by 14). This is the mechanism behind 09's `evidence <change-set-id>` command and 23's release-gate report: 04 durably stores and serves the records, 14 decides what qualifies as evidence.
- **Layout:** `$XDG_STATE_HOME/crabgic/<project-hash>/` — state root (existing): `journal/` (segments + snapshots), `leases/`. `$XDG_CACHE_HOME/crabgic/<project-hash>/` — cache root, pinned here as a sibling constant (Gap 14): 07's control clone nests at `.../git-control/`, 12's capability store nests at `.../capability-store/`. 04 pins the shared root; 07/12 own writing under it.
- **Retention:** segment + snapshot GC, conservative defaults — never deletes a segment newer than the latest durable snapshot.
- **Test-support:** a reusable kill/fault-injection harness — spawns the operation under test in a child process, `SIGKILL`s it at parameterized fault points, asserts recovery converges. This is "the phase-04 kill harness" 07's test plan names as reused directly.

## Out of scope

- Defining the `JournalEntryType`, `WorkUnitAttemptStatus`, run-lifecycle, `RunSnapshot`, `WorkUnit`, `EvidenceRecord`, or `RemoteOperationRecord` schemas — owned by 02; 04 types its codec and registry against them, doesn't declare them.
- Deciding *when* to emit a given entry — each owned by the phase performing that action: run/work-unit transitions and session assignment (05), adjudication decisions (05 stub; 06 real policy), remote-operation records (16), evidence pointers (06/08/14/21), git freeze/worktree quarantine (07), CAS-ref updates (08), approval-token minting (09/11), fan-out rationale (13), milestone sync (18/21), learning transitions (22). 04 only provides the durable, typed write path all of them share.
- The UDS control-plane process, worker registries, and run/work-unit routing logic — owned by 05.
- The mutation pipeline (plan → validate → apply → read-back) that calls the idempotency registry — owned by 16; 04 supplies only the `(operationId, contentHash)` primitive.
- Deciding evidence *content* or gate scoring — owned by 14; 04 stores and serves pointers, doesn't interpret them.
- The `doctor` staleness check and `evidence <change-set-id>` CLI surface — owned by 09; 04 supplies the data those commands read.

## Interfaces produced

`packages/journal` is imported directly by 05, 07, 16 (direct dependents in the phase graph) and, once built, transitively by 06, 09, 13, 21, 22, 23 (via 05/07/11/14/16). Exported surface:

- `appendEntry(entry: JournalEntryInput): Promise<JournalEntry>` — typed against `JournalEntryType` (02); assigns `seq`, `prevHash`, `hash`, `schemaVersion`; resolves only after `write → fsync(file) → fsync(dir)` completes.
- `queryEntries(filter: { type?: JournalEntryType; runId?; changeSetId?; workUnitId? }): AsyncIterable<JournalEntry>` — the evidence/traceability read path; `type: "evidence_pointer"` results deserialize as `EvidenceRecord` (02). Consumed by 09 (`evidence <change-set-id>`), 21 (traceability report), 23 (release-gate report).
- `verifyChain(segment)` / tail repair — truncates to the last valid chained entry, returns a structured report.
- `writeSnapshot(snapshot: RunSnapshot)`, `loadLatestSnapshot(runId)`, `recover(runId): { snapshot, replayed }` — consumed by 05 on supervisor restart.
- `IdempotencyRegistry.checkOrRecord(operationId, contentHash, compute)` → `{ status: "replayed" | "recorded" | "conflict", result? }` — consumed by 16 (`RemoteOperationRecord` exactly-once) and 08 (CAS-ref rebuild).
- `recordAttempt(workUnitId, sessionId, status: WorkUnitAttemptStatus)` — consumed by 05 (worker lifecycle) and 13 (limit parking, fan-out).
- `Lease.acquire(projectHash)` / `#renew()` / `#release()` — PID + start-time-validated exclusive project lease; consumed by 05 (one supervisor per project) and 09 (doctor staleness check).
- `runKillHarness(operation, faultPoints[])` — test-only export, reusable fault-injection harness; consumed directly by 07, reusable by 05/13/23's own crash-recovery tests.
- On-disk layout constants: `$XDG_STATE_HOME/crabgic/<project-hash>/journal/`, `.../leases/`; `$XDG_CACHE_HOME/crabgic/<project-hash>/` cache root (Gap 14) — single definition site for 07's `git-control/` and 12's `capability-store/` subpaths.
- Journal on-disk format: ndjson, one `JournalEntry` per line, `schemaVersion`-tagged per 02's contract-evolution convention.

## Interfaces consumed

| From | Names consumed | Why 04 needs it |
|---|---|---|
| 02 | `JournalEntryType` — closed union, 13 members (`run_transition, work_unit_transition, adjudication_decision, remote_operation_record, evidence_pointer, session_assignment, git_freeze, worktree_quarantine, cas_ref_update, approval_token_mint, fanout_rationale, milestone_sync, learning_transition`) | The codec's core discriminant — every entry carries exactly one member |
| 02 | `WorkUnitAttemptStatus` — closed union, at least `dispatched \| succeeded \| failed \| parked:rate_limit` (`cancelled` a strong candidate; full membership at 02's discretion) | Attempt records are typed against this, orthogonal to the run-lifecycle enum |
| 02 | Run-lifecycle enum (`draft → awaiting_approval → ready → running → verifying → integrating → final_verifying → published_local`, terminals `failed \| blocked \| cancelled`) | `run_transition` entries carry a `(from, to)` pair typed against this enum; 04 persists the pair — transition legality is validated by 02's own transition-table function before the caller (05) ever calls `appendEntry` |
| 02 | `RunSnapshot` schema | Snapshot writer/loader round-trips this exact shape |
| 02 | `WorkUnit` schema (incl. `session_id`) | Attempt records persist the `session_id` this schema carries |
| 02 | `EvidenceRecord` schema | Evidence query surface deserializes `evidence_pointer` entries into this shape |
| 02 | `RemoteOperationRecord` schema | Idempotency registry keys and stores against this shape for 16 |
| 02 | `ChangeSet` schema (ID field) | Evidence query surface filters by change-set ID |
| 02 | `schemaVersion` convention | Journal entries must survive contract evolution across schema versions — 02's own stated invariant, 04 is the named consumer |

## Work items

1. ndjson entry codec with chain fields, typed against `JournalEntryType` (02); schema-versioned payloads; work-unit attempts typed against `WorkUnitAttemptStatus` (02) — failing-first: a round-trip test for each of the 13 `JournalEntryType` members and each `WorkUnitAttemptStatus` member fails against a stub codec before the real encoder/decoder exists.
2. Append path + fsync discipline (`write → fsync(file) → fsync(dir)`); measure p50 append latency — failing-first: a fault-injected kill between write and fsync fails against a naive buffered writer before the fsync-ordered implementation lands.
3. Chain verifier + tail repair (truncate to last valid chained entry, report the truncation); snapshot writer/loader (temp + rename); `recover(runId)` = load snapshot + replay — failing-first: a corrupted-tail fixture (valid chain truncated mid-entry) fails verification before the repair path exists.
4. Evidence query surface (`queryEntries`, filterable by run/change-set/work-unit ID; `evidence_pointer` deserializes as `EvidenceRecord`) — failing-first: a query-by-change-set-id test fails against a stub supporting only full-segment scans.
5. Idempotency registry: `(operationId, contentHash)` keyed, journal-backed, replayable — failing-first: a same-id-same-hash replay test expects a byte-identical result and fails against a stub that re-executes instead of replaying.
6. Lease module (`O_EXCL` create, PID + start-time validation, 5 s heartbeat) — failing-first: a two-process contention test expects exactly one lease holder and fails against a naive non-atomic create. **Mechanism correction (2026-08-02, criteria-closeout pass; original wording above preserved verbatim).** "`O_EXCL` create" no longer describes how a fresh lease is published. `70d7da7` (#51) found that `open(path, "wx")` followed by a *separate* write leaves the lease path briefly observable and **empty**, which a contender read as "unparseable, take it over" without ever consulting pid liveness — a real double-acquire, measured at 9 per 11,000 two-process races. Publication is now a **staged write plus an atomic `link()`**: `publishExclusive` writes and fsyncs the complete record under a private `<leasePath>.stage-<pid>-<now>-<rand>` name, then `link()`s it into place. `link(2)` fails `EEXIST` exactly as an `O_EXCL` create does, so the single-winner property this item asks for is unchanged — but the published name now already has its bytes. The correction is a mechanism description, not a weakening: the guarantee ("exactly one lease holder") is strictly stronger than what the `O_EXCL` form actually delivered. See `docs/evidence/phase-04/README.md` §"Post-phase fix 3" and the exit-criteria closeout note on box 4.
7. Crash harness (`runKillHarness`): child killed at randomized, parameterized fault points; recovery must always converge — failing-first: the harness is run against work items 2–5 and must catch at least one seeded corruption class before it is trusted for reuse by 07/05/13/23.

## Test plan

- **Unit:** codec round-trip per `JournalEntryType` member (13) and `WorkUnitAttemptStatus` member; chain-hash computation; tail-repair truncation logic; lease-file parse/validate; idempotency key comparison.
- **Property (fast-check):** lease acquire/renew/release interleavings under simulated clocks (forged PID/start-time never wins takeover); randomized torn-write injection points always recover to a valid chain prefix; randomized `(operationId, contentHash)` sequences never produce a silent overwrite; snapshot+replay reconstructs state identical to a full replay-from-genesis across randomized snapshot points.
- **Integration:** real filesystem in tmp dirs (real fsync, not mocked); evidence query surface against a fixture journal spanning all 13 entry types; kill harness end-to-end against the append/chain/snapshot path.
- **Conformance:** not applicable as a dedicated group — 04 has no external wire protocol to conform to (that's 03/06's engine conformance and 16's provider conformance). Its cross-phase conformance obligation is the byte-identical `JournalEntryType`/`WorkUnitAttemptStatus` round-trip, covered under Unit above.
- **Security:** hash-chain tamper detection — a post-hoc single-byte modification of a historical entry (not a torn write) must fail verification, the property distinguishing integrity-checking from mere corruption-recovery; directory/file permission enforcement (`0700`/`0600`, denied to other local uids); idempotency registry resists an operationId collision from a different content hash (never silently prefers the newer write).

## Exit criteria

**Closeout pass 2026-08-02:** 10/11 ticked against recorded evidence; the one unticked box below is
an open defect — see `docs/evidence/criteria-closeout/defects/04-journal-crash-suite-ci-job-absent.md`
and that box's own note. Index: `docs/evidence/criteria-closeout/phase-04.json`.

Shared citation, reused by every ticked box. **`CI` run
[30720547145](https://github.com/WitchyNibbles/crabgic/actions/runs/30720547145)**, green at
`af46e00` — this pass's own base commit, so no ancestor-run reasoning is needed. Its
`unit-test+coverage (ubuntu-latest)` job
([91423926933](https://github.com/WitchyNibbles/crabgic/actions/jobs/91423926933)), step "test with
80% line+branch coverage gate", ran 623 test files / 6060 tests (job-log lines 1032–1033), and
job-log lines 683–726 name every `@crabgic/journal` suite cited below individually. Scoped local
re-runs of each criterion's suites, captured verbatim at `af46e00` with the command line and exit
status in each header, are committed as `docs/evidence/phase-04/closeout-c<k>-*.txt`. For context on
how recently this package moved: `git diff 70d7da7 af46e00 -- packages/journal` is **empty**, i.e.
nothing has touched it since the lease fix noted below.

**Lease note (read before trusting any date on box 4).** Box 4's guarantee was **not held by the
shipped code** until `70d7da7` (#51, 2026-08-01), which fixed a real production mutual-exclusion
defect measured at 9 double-acquires per 11,000 two-process races — the third distinct cause of the
same intermittent failure in the very test that certifies this criterion. The phase's original
2026-07-17 lease evidence (`wi6-lease-*.txt`) therefore certified something untrue at the time.
Every citation on box 4 is post-`70d7da7`.

This pass executed no `@live` test and no `engine-live` run; this phase's own Risks section records
that it asserts no unconfirmed engine fact.

- [x] 1k randomized kill-iteration run (`runKillHarness` over the append/chain/snapshot path): zero undetected corruption; recovery always converges to the last valid chained entry — evidence: `journal-crash-suite` CI job artifact. — **Evidence (2026-08-06), closed by building the named channel:** the substance was always proven; the CI job and artifact this box names as its evidence did not exist. `.github/workflows/journal-crash-suite.yml:47` carries the job name verbatim, and run [31087614384](https://github.com/WitchyNibbles/crabgic/actions/runs/31087614384) job 92570706911 shows line 235 `# iterations: 1000` and line 242 the suite passing in 189976ms, with the artifact `journal-crash-suite-report` present at 1038 bytes and uploaded `if-no-files-found: error`. The file's second test is the harness self-check asserting the scaffolding **detects** a seeded corruption class — the control that stops "zero undetected corruption" being satisfiable by a harness that detects nothing. Deliberately off the every-push path (1000 real spawn-and-kill iterations, 190s on that runner): it triggers on `packages/journal/**`, nightly, and on dispatch, while `npm test` keeps running the same suite at its 25-iteration default.
- [x] All 13 `JournalEntryType` members and every `WorkUnitAttemptStatus` member round-trip through the codec with full schema-branch coverage — evidence: codec round-trip suite + coverage report. — **Evidence (2026-08-02):** `packages/journal/src/codec/journal-entry.test.ts:81-88` round-trips one hash-consistent fixture per member in a `for (const type of JOURNAL_ENTRY_TYPES)` loop (`expect(decoded).toEqual(entry)`), with `:77-78` pinning the count at 13 so a shrunk union cannot silently loop fewer times, and `:178-186` doing the same over `WORK_UNIT_ATTEMPT_STATUSES` whose exact six-member list is pinned at `:168-175`. Negative controls, also per member: `:90-101` (payload matching no member's schema), `:104` (type outside the 13), `:117` (`.strict()` extra key), `:130` (non-hex chain fields), `:150-163` (`run_transition` with a non-lifecycle `to`). `CI` run 30720547145 green at `af46e00` (job-log lines 696, 718, 714). **Coverage report:** `docs/evidence/phase-04/closeout-c2-codec-coverage.txt` — 100% lines/branches/functions/statements across all five `codec/` production files, **enumerated by name** because v8's text reporter prints only files below 100%. Recorded correction to this phase's own mapping table: `docs/evidence/phase-04/README.md` row 2 cites coverage in `exit-criteria-package-gate.txt`, but that file's recorded command is `--coverage.enabled=false` and it carries no coverage numbers — the coverage half had no committed artifact until this pass. Also `closeout-c2-codec-roundtrip.txt` (61/61).
- [x] Idempotency semantics proven: same `(operationId, contentHash)` replays a byte-identical result; changed content fails a typed conflict, never silently overwrites — evidence: idempotency property-test suite. — **Evidence (2026-08-02):** the named property suite is `packages/journal/src/idempotency.test.ts:165-231` (fast-check, `numRuns: 40`), which runs an independent reference model beside the registry: `:199` recorded on first sight, `:205-209` `expect(outcome.status).toBe("replayed"); expect(outcome.result).toEqual(existing.result);` for a repeat hash, `:211-212` `toBe("conflict")` + `result` undefined for a changed one, and `:218-225` a final sweep proving every operationId's **original** result survives all intervening calls. Unit-level: `:45-50` `expect(compute).toHaveBeenCalledTimes(1)` across two identical calls (a replay, not an agreeing recomputation); `:100-104` the byte-identical clause at its hardest case (a `Date`/`NaN`/`undefined`-member result coerces identically on the recorded call and every replay); `:117-120` and `:134-135` the conflict clause asserted on the **stored** value, with `:149` proving three conflicting calls append exactly one `remote_operation_record`; `:53-64` proving replay is journal-backed rather than an in-memory cache. `CI` run 30720547145 green at `af46e00` (job-log line 685); `docs/evidence/phase-04/closeout-c3-idempotency.txt` (14/14); RED counterpart `wi5-idempotency-failing.txt`. Scope: proven for **sequential** calls; `idempotency.ts`'s own CONCURRENCY section and this phase's evidence-README Deviation 2 record that two truly concurrent first-time calls for one unseen `operationId` can both append.
- [x] Two-process lease contention resolves to exactly one holder; takeover fires only on expiry **and** start-time mismatch, never against a live process — evidence: lease-contention integration test. — **Evidence (2026-08-02, and see the lease note above — this became true only at `70d7da7`):** `packages/journal/src/lease.test.ts:428-437` spawns two real child processes, holds both past their decision so the contended window is enforced rather than hoped for (`:434`), and asserts `expect(outcomes.sort()).toEqual(["ACQUIRED", "DENIED"]);`; `:445-450` is the pin that keeps the denial meaningful (`expect(await readProcessStartTimeFromProc(holderPid)).toBeDefined()` — without it the children could be denied by the 15s TTL instead of by liveness, which is exactly how this test rotted twice before). The two-condition clause: `:88-97` rejects with `LeaseHeldError` 50s past a 1s TTL while the recorded process is alive, `:115-123` takes over once the same setup reports the pid dead. `packages/journal/src/lease-record.test.ts:141-155` proves the exact truth table as a 5000-run fast-check property (`expect(eligible).toBe(!recordedProcessStillAlive && expired)`) and `:128-139` that no forged record ever wins against a live process; `lease-acquire.ts:249-251` shows `recordedProcessStillAlive` is a genuine start-time **comparison**. Deterministic pins for all three historical causes: `lease-acquire.test.ts:113-134`, `:151-182`, `:185-208`, plus publish atomicity at `:257-275`. `CI` run 30720547145 green at `af46e00` (job-log lines 686, 688, 707, 719); `docs/evidence/phase-04/closeout-c4-lease-contention.txt` and `fix3-lease-publish-toctou-{failing,passing}.txt`. That transcript carries TWO captures of the same command, deliberately: a green 53/53 and, appended rather than re-rolled away, a later exit-1 run in which the separately-tracked `onLeaseLost` heartbeat arm fails (`:579` asserts `ownership_mismatch`; A's 10 ms heartbeat can fire in the window between the out-of-band unlink and B's publish, so it correctly sees `missing`). It reproduced 5/5 scoped and once in full `npm test`, on a tree whose `packages/` is byte-identical to `origin/main`, and it does not touch this criterion — the two-real-child-process contention test passes in **both** captures. Residual, stated: `isTakeoverEligible(undefined, …)` still returns `true` unconditionally (`lease-record.test.ts:96`), so "never against a live process" rests on the producer side — after `70d7da7` no correct process can leave a present-but-unparseable lease. W4's takeover-rename TOCTOU (evidence-README Deviation 3) remains open.
- [x] fsync ordering (`write → fsync(file) → fsync(dir)`) asserted on the real append path — evidence: strace-based CI assertion (Linux), or an injected fs-shim equivalent where strace is unavailable. — **Evidence (2026-08-02):** the **second** disjunct. `packages/journal/src/store/durable-io.test.ts:40-63` — `:43` wraps a **real** `createNodeFsPort()` in the recording shim, `:54-55` guard non-vacuity (`firstWriteIndex >= 0`, `fsyncIndexes.length >= 2`), `:57` `expect(firstWriteIndex).toBeLessThan(fsyncIndexes[0]!)`, `:61-62` `expect(fileFsync.path).toBe(filePath); expect(dirFsync.path).toBe(base);`. It is the **real** append path because production `appendEntry` calls the same function with the same port (`store/append-entry.ts:145`, `store/store-config.ts:46`), and it is a **CI** assertion: `CI` run 30720547145 green at `af46e00`, job-log line 697. `:82-97` and `:109-124` assert the same discipline for the snapshot and truncate paths. `docs/evidence/phase-04/closeout-c5-fsync-ordering.txt` (21/21); `wi2-fsync-append-{failing,passing}.txt`. The disjunct's precondition, honestly: `strace` appears nowhere in `.github/workflows/` and nowhere in the tree except the two lines of `store/testing/recording-fs-port.ts` restating this criterion; `which strace` on the development machine returns nothing (re-verified 2026-08-02). Whether `ubuntu-latest` ships `strace` was **not** verified by this pass — no job installs it and nothing was dispatched to find out.
- [x] Snapshot+replay recovery reconstructs state identical to a full replay-from-genesis across randomized snapshot points — evidence: snapshot-equivalence property suite. — **Evidence (2026-08-02):** `packages/journal/src/store/journal-recovery-properties.test.ts:144-208` — `:197` `expect(recoveredState).toBe(fullReplayState);`, comparing `recover(config, runId)` seeded at the snapshot's own `runState` against a genuine full `queryEntries` fold, randomized over walk length, snapshot point and pick seed (`:148-150`) with the walk itself a legal traversal of `RUN_LIFECYCLE_TRANSITIONS` (`:61-75`), `numRuns: 20`, real filesystem per run. The clause that makes "identical" strong is `:199-203`: `expect(recovered.replayed).toEqual(expectedReplayed);` where `expectedReplayed` is exactly the entries with `seq > snapshot.journalSequenceNumber` — a `recover()` that replayed from genesis every time would agree on the final state and fail here. Neighbouring torn-write property at `:122-136` (`:123` proves the injected cut genuinely corrupts before `:131-133` proves the repair). `CI` run 30720547145 green at `af46e00` (job-log lines 692, 699); `docs/evidence/phase-04/closeout-c6-snapshot-equivalence.txt` (11/11); `wi3-chain-snapshot-{failing,passing}.txt`.
- [x] Evidence query surface returns the exact `EvidenceRecord` set filtered by run/change-set/work-unit ID against a fixture journal spanning all 13 entry types — evidence: query-surface integration test. — **Evidence (2026-08-02):** `packages/journal/src/store/query-entries.test.ts:93-101` builds the 13-type fixture journal from one schema-valid representative per member and asserts `expect(all).toHaveLength(13); expect(all.map((e) => e.type)).toEqual([...JOURNAL_ENTRY_TYPES]);` — by type **and** in order; `:199-207` repeats it across 13 rotated segments. The change-set filter over an `evidence_pointer`, with a non-matching decoy present, is `:124-134` (`expect(filtered).toHaveLength(1); expect(filtered[0]!.type).toBe("evidence_pointer");`), and `:166-173` reads the 02 `EvidenceRecord` fields off the deserialized payload. runId at `:113-122` (three entries: matching, other-run, and no-runId at all), workUnitId at `:137-144`, AND-combination at `:147-163`, unreadable-segment control at `:175-197`. `CI` run 30720547145 green at `af46e00` (job-log line 693); `docs/evidence/phase-04/closeout-c7-query-surface.txt` (9/9); `wi4-query-{failing,passing}.txt`. Composition note, because the criterion reads as one sentence and the suite proves it in pieces: no single test filters the 13-type journal by run/change-set/work-unit ID. That decomposition is sound rather than convenient — `store/query-entries.ts:31-34` applies the four filters as independent **envelope-level** predicates, type-agnostic by construction — but the literal "exact `EvidenceRecord` set" phrasing is exercised only for the change-set filter, which is also the one 09's `evidence <change-set-id>` uses.
- [x] A `parked:rate_limit` attempt record retains its `session_id` across a simulated crash+recover cycle — evidence: targeted crash-recovery test (the property §0's pause-and-resume policy depends on). — **Evidence (2026-08-02):** `packages/journal/src/attempts.test.ts:159-209`, the describe block named for this exit criterion — `:206-207` `expect(recovered?.status).toBe("parked:rate_limit"); expect(recovered?.sessionId).toBe(sessionId);` read back through `getLatestAttempt` after the repair. The crash is real corruption rather than a no-op: `:188` cuts a later entry's bytes mid-line, `:191` `expect(beforeRepair.firstIssue).toBeDefined();` proves `verifyChain` sees a torn write, `:199` proves `repairJournal()` resolved it. `:114-125` proves the read-back is latest-wins (the RED capture's stub returned the **first** attempt) and `:49-53` that `sessionId` is persisted to the journal entry, not just returned in memory; `codec/journal-entry.test.ts:189-199` is the encode/decode counterpart. `CI` run 30720547145 green at `af46e00` (job-log line 690); `docs/evidence/phase-04/closeout-c8-parked-session-id.txt` (11/11); `wi5-attempts-failing.txt`. Scope: "simulated crash" is the criterion's own word and is what the test does — a hand-injected torn tail, not a real `SIGKILL`, which per this phase's evidence-README Deviation 6 would prove **less** about byte-level payload survival, not more.
- [x] Append p50 latency documented with a CI regression gate — evidence: benchmark output committed, gate wired into CI. — **Evidence (2026-08-02):** both named items exist. Committed output: `docs/evidence/phase-04/exit-criteria-append-benchmark.txt`, 1000 real appends, `"p50Ms": 2.30982199999994` (2026-07-18). Gate: `packages/journal/src/store/append-benchmark.test.ts:81-91` asserts `stats.p50Ms` under `P50_THRESHOLD_MS` over 300 **real** appends, and it genuinely executes in CI — confirmed from the job log, not inferred from the YAML: `CI` run 30720547145 green at `af46e00`, line 711 `append-benchmark.test.ts (5 tests) 695ms`. The statistic itself is pinned at `:45-67` (nearest-rank p50 on a known sample; no input mutation; `percentile([], 50)` throws `RangeError` so an empty sample cannot pass) and `:71-78` proves the measurement returns exactly `count` samples. Because the 2026-07-18 runner (`zz-bench-capture.test.ts`) was deleted after that capture, the documented number was re-derived at `af46e00` in `closeout-c9-append-p50-remeasured.txt`: `"p50Ms": 2.7037610000002132` idle and `"p50Ms": 13.335670000000391` under concurrent load, both kept verbatim. **Sensitivity, so this box is not read as more than it is:** the threshold is **100 ms**, i.e. ~37x the idle p50 and ~7.5x the loaded one. A 5x append-latency regression passes it silently. The margin is deliberate and documented (`:16-25`, citing this phase's own WSL2/9p fsync-variance risk — and the two captures above differ by 4.9x on one machine within five minutes). Read this as a catastrophic-regression smoke gate, not a tuned p50 budget; 15's `PerformanceContract` machinery is where a tuned budget belongs. Also `closeout-c9-append-benchmark-gate.txt` (5/5).
- [x] `runKillHarness` ships as a stable, independently unit-tested export (parameterized fault points → structured report) — evidence: harness unit tests plus the exported signature 07's test plan names as "the phase-04 kill harness." — **Evidence (2026-08-02):** `packages/journal/src/kill-harness.test.ts:50-87` is the discriminating pair — against `unsafe-writer.mjs`, `:63` `verdict "pass"` at one fault point but `:69` `verdict "fail"` at the next and `:71` `allConverged: false`; against the temp+rename `safe-writer.mjs`, `:87` `allConverged: true` at every point. `:57-70` pins the report's **shape** field by field (`faultPoint`/`killedAt`/`recovered`/`verdict`), `:112` and `:124` pin the two non-happy terminal states (`natural-exit` when the fault point is never signalled, `timeout-kill` when the child hangs) without which a harness could certify a child that never reached the fault point, `:101` pins the operation-factory form and `:140` that an unspawnable command rejects. Stability across the package boundary: `packages/git-engine/src/worktree-lifecycle.crash.test.ts:5` (07's own package) and `packages/supervisor/src/socket/kill-restart-recovery.test.ts:13` (05's) both `import { runKillHarness } from "@crabgic/journal"`, barrel-exported at `packages/journal/src/index.ts:144` with all seven report/option types at `:146-153`. `CI` run 30720547145 green at `af46e00` (job-log line 703); `docs/evidence/phase-04/closeout-c10-kill-harness.txt` (10/10); `wi7-kill-harness-{failing,passing}.txt`. Wording detail, recorded not corrected: 07 does not use the exact string "the phase-04 kill harness" — `roadmap/07:67` says "crash tests reusing 04's kill harness" and `07:49` "04's crash/kill-harness test utility, reused directly". The referent is unambiguous and the reuse is real.
- [x] `$XDG_STATE_HOME`/`$XDG_CACHE_HOME` crabgic roots are defined exactly once in this package — evidence: repo-wide lint/grep CI check fails if another package reimplements the root instead of importing it. — **Evidence (2026-08-02):** `packages/journal/src/layout/xdg-sole-definition.test.ts:101-126` is the scan — every production `.ts` under `packages/*/src` outside `journal`, flagged when it contains both the `"crabgic"` namespace literal and a real `process.env.XDG_STATE_HOME`/`XDG_CACHE_HOME` **read** (`:78`), asserted `.toEqual([])`. Its teeth are `:128-137` (a synthetic reimplementation fixture must match), its false-positive controls `:151` and `:158`, and `:161-173` keeps the single allowlist entry honest. `CI` run 30720547145 green at `af46e00` (job-log line 689). The sole definition site's own behaviour is pinned by `layout/xdg-layout.test.ts:37`/`:42` (state and cache root), `:48-51` (the four subpaths) and `:58` (07's `git-control` nesting under the same cache root — interface-ledger Gap 14's sibling-constant shape), job-log line 709. `docs/evidence/phase-04/closeout-c11-xdg-sole-definition.txt` (16/16); `exit-criteria-xdg-sole-definition.txt`. Verified independently of the check, because its glob is narrower than "repo-wide" (it walks `packages/*/src` only and skips `.test.ts`, so `e2e/**`, `scripts/` and `packages/*/{hooks,statusline}/*.mjs` are outside it): a whole-tree grep for an actual `process.env.XDG_STATE_HOME`/`XDG_CACHE_HOME` read across `*.ts`/`*.mjs`/`*.js` returns six files, of which exactly one is production — `packages/journal/src/layout/xdg-layout.ts` (lines 140-144). The other five are `.test.ts` fixtures: `packages/cli/src/doctor/checks/sandbox-selftest.test.ts`, `packages/gates/src/drift/cli.test.ts`, `packages/journal/src/layout/xdg-sole-definition.test.ts`, `packages/journal/src/layout/xdg-layout.test.ts`, `packages/journal/src/lease-project.test.ts`. (This list was corrected on 2026-08-02: it first named only four of the five test files, because the grep behind it was truncated by `head -30`. The count of production definition sites — one — is unchanged.) Ledger: Gap 14 quotes this criterion's current wording and is not contradicted.

## Risks & open questions

- fsync semantics differ on WSL2 9p mounts for both the state and cache roots — supported-locations doc; `doctor` (09) warns on `/mnt/c`-resident state/cache dirs.
- Unlike 03/06/16, this phase asserts no unconfirmed engine fact (adaptation §10 items 2/3/10): the only engine-adjacent field it persists is `session_id`, treated as an opaque string, never parsed against engine-specific format assumptions.
- Adaptation §10 risk 9 (subscription-auth workers share rate/usage limits) makes this phase's `parked:rate_limit` + `session_id` retention load-bearing for the confirmed pause-and-resume policy (§0) — a durability gap here silently breaks pause-and-resume, not just crash recovery.
- Retention/GC numeric thresholds (segment age/size) are left as an implementation-time tuning question, not a build blocker; the only hard invariant is never deleting a segment newer than the latest durable snapshot.
- Risk: if `runKillHarness` is too coupled to journal-specific internals to be genuinely reusable, 07/05/13/23 may each reimplement fault injection independently, fragmenting crash-test rigor — mitigate by keeping the harness parameterized over an arbitrary operation callback.
- The `$XDG_CACHE_HOME` path form used here (`<project-hash>` immediately under `crabgic/`, subpaths nested beneath it) resolves an internal inconsistency between two passages of the binding resolutions doc's own Gap-14 text (one passage describes the P07 subpath as `git-control/<project-hash>/`; the literal quoted edit given for P07 is `<project-hash>/git-control/`). This file follows the literal, twice-repeated form used in both P07's and P12's quoted edits, matching this phase's existing `$XDG_STATE_HOME` pattern — flagged for Reconcile to confirm 07 and 12 land on the same order.
