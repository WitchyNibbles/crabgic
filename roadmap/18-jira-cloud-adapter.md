# Phase 18 — Jira Cloud adapter + intake/milestone synchronization

| | |
|---|---|
| **Depends on** | 16, 17 |
| **Unlocks** | 19, 21; transitively, 23 (no direct 18→23 edge — 23's own "Depends on: all" header includes 18, but the mermaid graph's simplified inbound edges to 23 are 08/15/19/21/22 only) |
| **Sources** | original plan Jira sections (scope, intake/sync, rate limits); REST v3 + Agile APIs; adaptation §7 (Jira/Grafana connectors — unchanged, three Claude Code notes), §8 (intake/sync policy, milestone-only updates carry over unchanged) |
| **Primary package** | `packages/connectors-jira` |

## Goal

The full Jira Cloud surface behind the gateway is live and provider-neutral at the boundary: service-account OAuth, REST v3 + Agile resources operating strictly inside the plan's allow/deny matrix, ADF rendering through 17, and milestone-only synchronization with exactly-once writes — with no delete/admin/impersonation surface reachable by construction, not by convention.

## In scope

- **Auth:** service-account OAuth 2.0 client credentials (60-min tokens, refreshed by this phase's token manager, held only as a secret reference — never a literal in worker-reachable state); connection doctor validates scopes. Wrapped Atlassian MCP for discovery/reads is optional behind a capability flag, mediated by the gateway (adaptation §7: workers never see the Atlassian MCP server); REST is primary and always available.
- **Resources (plan matrix):** projects list/read; boards list/read/create/update + issue ranking; sprints incl. start/complete + move issues; epics/issues search/read/create/update/link/rank/transition; comments list/create/update; links; worklogs; attachments (streamed, size/MIME/filename/malware/secret-checked; bytes never enter prompts). **No deletes, no user/permission/workflow/security/automation admin, no impersonation or caller-supplied author/history, no raw endpoints; custom-field writes only against discovered field metadata.**
- **High-impact capabilities** — 7 of P02's 11-member `HighImpactCapabilityFlag` enum, byte-identical labels (the enum is a provider-neutral core type; only surrounding prose may gloss it): `assignment, reporter change, closing transitions` (Jira Done/Closed workflow statuses), `sprint completion, attachments, bulk mutations` (multi-issue bulk edit/transition), `issue creation`. Each requires the matching envelope flag before the operation reaches 16's mutation pipeline.
- **Capability discovery:** edition/permissions/field metadata → `CapabilitySnapshot` (P02 schema, 16-owned cache/invalidation); unknown editions/versions default read-only.
- **Intake/sync:** a referenced issue key/URL becomes the tracking item; otherwise a concise draft rendered through 17, created only post-approval. Local `IntentContract` stays authoritative. Milestone-only updates (start / material blocker / verified completion) via 17's Jira milestone-comment template; status-comment dedup by entity-property marker (edit in place, never a second comment). Workflow mapping to `JiraWorkflowStage` (`planned | in_progress | blocked | done`) is never-guess: an unrecognized remote status always resolves to `blocked`, never silently to `done`. Revision polling at every milestone feeds the revision comparator; a material remote edit triggers 11's contract-amendment stop condition (wired in 21). Jira `done` only after 21's exact-revision verification passes.
- **Rate limits:** quota/burst/per-issue write compliance, `Retry-After` honored, cross-worker throttling via 16's gateway-side serialization.
- **Reconciliation:** implements 16's marker-reconciliation interface using Jira entity properties as the exactly-once marker for every POST (issue/comment creation).

## Out of scope

- Deployment-type differences (PAT/basic auth, REST v2, wiki-markup fallback, DC version fixtures) and the deployment-parameterized conformance suite → 19.
- Generic transport security, secret-reference resolution, `CapabilitySnapshot` caching/invalidation mechanics, the mutation pipeline's persistence/replay semantics, the retry ladder, and item/result size budgets → 16 (this phase consumes those; it does not reimplement them).
- ADF safe-subset conversion mechanics, Unicode/secret/attribution scanning, and the regenerate-once enforcement loop → 17 (this phase's renders pass through that pipeline).
- `Requirement`↔`RemoteResource` contract linkage, evidence-record binding of exact final revisions, the done-transition verification gate's run-state wiring, and drift CI → 21.
- Opening or posting to a pull request or any VCS-host review surface — no such connector exists in v1; this phase is a tracker connector only.
- Live Jira Cloud sandbox provisioning/teardown and the release-gate E2E matrix → 23 (this phase produces the fixtures/cassettes 23 replays, not the harness itself).

## Interfaces produced

**Types (`packages/connectors-jira`, built on `packages/contracts`):**

- `JiraResourceClient` — deployment-type-parameterized resource-client interface (Cloud implementation lives here; 19 supplies the Data Center implementation behind the same interface) covering projects/boards/sprints/epics/issues/comments/links/worklogs/attachments; every method is typed IO, every failure mapped to exactly one of P02's 10 canonical connector errors. *(Name introduced in this phase — no upstream text names it; it is what 19's "shared resource interface" prose refers to.)*
- `JiraWorkflowStage` — closed union `planned | in_progress | blocked | done`; the Jira ticket-status projection, populated by the never-guess transition mapper. Distinct token space from P02's run-lifecycle `blocked` terminal — same spelling, unrelated enum, never to be conflated. *(Name introduced in this phase.)*
- Revision comparator — stamps each intake-tracked issue's `RemoteResource` (P02 schema) instance with its exact remote revision at every milestone poll; diffing two consecutive stamps is the material-change signal.
- The 7-member Jira subset of P02's `HighImpactCapabilityFlag` enum (see In scope), each bound to the operation(s) it gates.

**Journal entries:**

- `JournalEntryType: milestone_sync` (P02, 13-member union) — one entry per start/material-blocker/verified-completion sync, keyed by the same entity-property marker used for comment dedup.

**Fixtures (`packages/testkit`):**

- Fake-Jira: scriptable REST v3 + Agile double, fault-injectable, extending 16's fake-provider harness.
- Recorded Cloud v3/Agile cassettes.
- Fault matrix: 401/403/409/429, malformed pagination, ambiguous mid-POST timeout.
- Fake/cassette parity suite: proves the fake and a cassette replay of the same scripted scenario yield identical typed results.

**Pipelines:**

- Attachment streaming pipeline (size/MIME/filename/malware/secret-checked; bytes never enter a prompt).
- OAuth client-credentials token manager + connection-doctor scope/expiry checks. (Cloud-specific — 19 implements its own PAT/bearer manager against the same connection-doctor pattern, per adaptation's DC auth model; not a shared instance.)

**Consumed by:**

- **19** (Jira Data Center adapter): `JiraResourceClient` (DC implementation behind the same interface), the 7-member capability-flag mapping (same matrix, parameterized by deployment type), the fake-Jira + fault-matrix harness (extended with DC-specific faults), the cassette-capture tooling (reused for 10.3/11.3).
- **21** (connector evidence integration): `JiraWorkflowStage` (`done` member is the done-transition gate's hook point; the never-guess rule backs the ambiguous-op block), the revision comparator (feeds exact-revision evidence binding and remote-edit-triggered amendment review), `milestone_sync` journal entries (the milestone evidence trail), the attachment pipeline and fault matrix (security-fixture wiring into 14's framework).
- **23** (release hardening): Cloud v3/Agile cassettes + fake-Jira (non-live E2E and drift-CI baselines), the live-sandbox test suite (Jira Cloud live matrix), the attachment pipeline (data-minimization proof), the supported-edition/version statement (compatibility-matrix doc input).

## Interfaces consumed

**From 16 (`packages/gateway`):**

- `ExternalConnection` store + secret-reference resolution — the Jira service-account OAuth client credential is stored and resolved as a reference, never a literal in worker-reachable state.
- Transport security stack (TLS verification, redirect revalidation, SSRF-guarded scheme/origin/IP allowlists, ≤4 in-flight per connection, `Retry-After` + jittered backoff, per-tenant+resource write serialization, O(page) pagination) — this phase's resource clients are built on this transport, not beside it.
- `CapabilitySnapshot` (P02 schema; 16 owns the cache/15-min TTL/invalidation) — this phase populates instances from Jira discovery.
- Mutation pipeline (`RemoteMutationPlan` → `RemoteOperationRecord` persisted before network I/O → apply → read-back → verify → record; same-ID+same-content replay resolves, different content fails) — every create/update/transition/comment call is submitted through this; this phase never persists `RemoteOperationRecord` itself.
- Canonical connector errors (10-member closed union: `authentication, permission, not_found, conflict, rate_limited, validation, unsupported, transient, ambiguous_write, policy_blocked`) — every Jira REST failure maps to exactly one member; no raw Jira error body crosses the boundary.
- Retry ladder (GET free; PUT/PATCH deterministic + precondition-only; POST never blind; 409/412 → fetch, rebase-or-block) and the marker-reconciliation interface (16 declares it; this phase implements it via Jira entity properties).
- Result/item size budgets (32 KiB item / 256 KiB result, typed truncation errors).
- Fake-provider scripting harness (`packages/testkit`) — this phase's fake-Jira extends it rather than reimplementing fault injection.
- Tool-surface names `tracker.search/get/plan_create/plan_update/plan_transition/plan_comment/apply` — the contract this phase's `JiraResourceClient` backs; these tool handlers (registered by 16) dispatch to it for any Jira-typed `ExternalConnection`, and this phase registers no MCP tool of its own.

**From 17 (`packages/renderer`):**

- Markdown→ADF converter + safe-node/mark subset — every Jira Cloud comment/description render goes through this; this phase never emits raw ADF.
- Jira milestone-comment template (Outcome/Evidence/Risk/Next/Ref) — the milestone-sync engine's only comment shape.
- `CommunicationPolicy` length limits as enforced by 17's pipeline (Jira summary ≤120; Jira comment ≤800 chars/6 lines) — constants owned by 02, enforcement staged in 17.
- Regenerate-once protocol (lint failure → one regeneration with feedback → second failure blocks with typed `policy_blocked`) — every outgoing Jira write passes through this before reaching 16's mutation pipeline.

**From 02 (`packages/contracts`), transitively via 16/17's own dependency on 02 — not a direct edge in this phase's header:**

- `IntentContract` — intake's authoritative source; both a referenced-issue and a drafted-then-approved issue resolve back to it.
- `ExternalConnection`, `CapabilitySnapshot`, `RemoteMutationPlan`, `RemoteResource` — instantiated here with Jira-specific data.
- `CommunicationPolicy` constants (Jira summary/comment limits, milestone template shape).
- `HighImpactCapabilityFlag` enum (11 members; this phase uses exactly 7, byte-identical labels).
- Canonical connector errors (10-member union).
- `JournalEntryType` (13-member union) — this phase's entries carry the `milestone_sync` member.

## Work items

1. OAuth client-credentials token manager + connection doctor checks. Entry point: failing unit test — an expired/not-yet-refreshed token must be rejected before any resource call fires.
2. Resource clients on the 16 transport — typed IO + canonical-error mapping — behind the `JiraResourceClient` interface, covering projects/boards/sprints/epics/issues/comments/links/worklogs. Entry point: failing per-resource contract test against a minimal inline stub transport (no live Jira, no shared fake yet).
3. Discovery → `CapabilitySnapshot`; field-metadata-driven custom-field validation; unknown-edition read-only fallback. Entry point: failing snapshot test against a scripted "unrecognized edition" discovery response.
4. Intake + milestone-sync engine + `JiraWorkflowStage` transition mapper + revision comparator. Entry point: failing integration test — issue-key resolution, draft-then-approve, milestone-comment dedup, unmapped-status → `blocked`.
5. Attachment streaming pipeline (size/MIME/filename/malware/secret checks). Entry point: failing test with a poisoned fixture (oversized, spoofed MIME, embedded secret) that must be rejected before any byte reaches a prompt.
6. Testkit fake-Jira (scriptable, fault-injectable, extends 16's harness) + recorded Cloud v3/Agile cassettes + fault matrix + fake/cassette parity suite. Entry point: failing parity test — the same scripted scenario must produce identical typed results from the fake and from cassette replay.

## Test plan

- **Unit:** canonical-error mapping table (every Jira REST status code → exactly one of the 10 members); OAuth token-manager refresh/expiry/clock-skew edge cases; `JiraWorkflowStage` never-guess mapping (every documented Jira status name → a stage; unmapped → `blocked`).
- **Property:** fast-check over discovered field-metadata shapes (an unrecognized field type must never be silently accepted for a custom-field write); revision-comparator diffing over arbitrary field mutations between two snapshots (must be flagged material or explicitly excluded, never silently dropped).
- **Integration:** resource clients against fake-Jira: board → sprint → epic → issue → link → worklog → attachment; ADF/text conversion round-trip; transition mapper against scripted workflow schemes; milestone-sync engine across a 3-cycle scripted issue (start/blocker/completion); concurrent-edit conflict (412 → fetch-rebase-or-block).
- **Conformance:** fault-matrix replay (401/403/409/429, malformed pagination, ambiguous mid-POST timeout) — must fail with no handling before the fix, pass after; fake-Jira/cassette parity suite.
- **Security:** forged delete/admin/impersonation/raw-endpoint calls fail before any network I/O (pre-flight capability check, never a server-side 403 as the sole guard); attachment pipeline rejects oversized/spoofed-MIME/embedded-secret fixtures before bytes reach a prompt; exactly-once proof under injected POST timeout (no duplicate comment/issue on retry); custom-field writes refuse undiscovered field IDs; 17's lint runs on every outgoing payload.

## Exit criteria

**Closeout pass 2026-08-02:** 6/10 ticked against recorded evidence; the 4 unticked boxes below
are open defects — see `docs/evidence/criteria-closeout/defects/18-*.md` and each box's own note.
Machine-readable index: `docs/evidence/criteria-closeout/phase-18.json`.

Shared citation reused by every box below. **`CI` run
[30745540255](https://github.com/WitchyNibbles/crabgic/actions/runs/30745540255)**, green at this
pass's base `d60398f` — its `unit-test+coverage (ubuntu-latest)` job
([91490386248](https://github.com/WitchyNibbles/crabgic/actions/jobs/91490386248)), step "test
with 80% line+branch coverage gate", executed 625 test files / 6155 tests, and the step log names
every `@crabgic/connectors-jira` suite cited below individually (job-log lines 362–410). Scoped
local re-runs of each criterion's own suites, captured verbatim at that commit with command line
and exit status in each header, are committed as `docs/evidence/phase-18/closeout-c*.txt`.

Two findings shape four of these boxes and are worth reading before the boxes themselves. **This
phase recorded no Cloud traffic:** the single Cloud "cassette" is a serialization of the
hand-authored fake script, and the parity suite's own second test asserts the two are equal — so
the parity assertion cannot fail, and no cassette covers the mutation flow (criteria 1 and 9).
**Per-issue write serialization is keyed on `RemoteMutationPlan.canonicalTarget`,** and this
connector mints a different target per resource kind on the same issue, so an `issue.update` and
a `comment.create` on one issue run concurrently — measured, not inferred (criterion 10).

- [x] Plan's Jira flow passes on fakes + cassettes: board → sprint → epic → issue → link → worklog → attachment; ADF/text conversion; transitions; concurrent-edit conflicts. — **Evidence (2026-08-07), closed by remediation. The assessment that follows is kept VERBATIM and is superseded, not deleted; it was filed as defect:** `docs/evidence/criteria-closeout/defects/18-flow-never-replayed-on-a-cassette.md`. The fakes half is complete and strong — `packages/connectors-jira/src/testkit/jira-flow.integration.test.ts` applies every step of the named chain through the REAL `@crabgic/gateway` `executeMutationPlan` against a real temp-dir `JournalStore` (`:113` `expect(boardOutcome.status).toBe("recorded")`, `:123` sprint `"10"`, `:132` epic `"PROJ-1"`, `:141` issue `"PROJ-2"`, `:149` link, `:161` comment `"50001"`, `:171` worklog `"30001"`, `:186` attachment `"40001"`), converts markdown with 17's `toADF` at `:152-158` (this phase is the *consumer* of 17's converter — the safe-subset whitelist is pinned by 17's own closeout, and `resource-client/adf-guard.ts:80` calls 17's `validateAdfSafeSubset` rather than re-deriving it), verifies a transition at `:245` and resolves a 412 to `{status:"failed", errorKind:"conflict"}` at `:284-285`; `parameterized-conformance.integration.test.ts:53` re-runs the same chain under `describe.each(["cloud","datacenter"])`. The "+ cassettes" half has no bearer at all: the only Cloud cassette (`src/testkit/fixtures/read-scenario.cassette.json`, 7 entries) is a READ scenario — projects/boards/sprints/search/get/comments/worklogs — with no create, no link, no worklog write, no attachment, no transition and no non-2xx status, and no test replays this flow, or any mutation, from a cassette. Remedy is M-sized (a write-scenario fixture plus a `describe.each(["fake","cassette"])` parameterization of the existing suite); needs neither CI nor the live engine. **Evidence (2026-08-07), closed by remediation:** the "+ cassettes" half now has a bearer. PR #119 parameterized the flow suite over `describe.each(["fake", "cassette"] as const)` (`packages/connectors-jira/src/testkit/jira-flow.integration.test.ts:185`), so the SAME assertion set runs twice — once on the inline fake script and once replayed from bytes on disk — through the REAL `executeMutationPlan` and a real temp-dir `JournalStore` (`:161-162`), over the whole named chain (board -> sprint -> epic -> issue -> link -> ADF comment -> worklog -> attachment) plus a transition and a 412 concurrent-edit conflict. That the PREVIOUS corpus could not bear this clause is proven by execution, not by enumeration: reshaping the repository's only Cloud cassette (the 7-entry read scenario) into the write envelope fails 4 of 8 cases, including `cassette replay: section "transition" recorded 0 call(s); the connector made at least 1` and `expected 'blocked' to be 'failed'` on the 412 branch (transcript §R3). The cassette bytes genuinely drive the flow (§P2: editing one response key reddens ONLY the cassette arm while the byte-identical fake arm stays green) and the recorded REQUEST line bites (§P3: a one-byte path case change is caught by a check the fake arm does not have at all). Per-push proof, byte-compared under the one-space rule with ANSI stripped (§R7b): `jira-flow.integration.test.ts` 3 -> 8 tests and `fake-cassette-parity.test.ts` 2 -> 3 tests between `main` `6f964d7` (job 92626742663) and PR #119 at `541739f` (job 92631885308). Transcript: `docs/evidence/phase-18/cassette-flow-replay-batchJ.txt`. **SCOPE NOTE THE TICK CARRIES, verbatim and mandatory:** the cassette is HAND-AUTHORED, not captured (no licensed Jira Cloud sandbox was available or used); this closes criterion 1's cassette channel and is not evidence for criterion 9 (parity), which stays unticked and owner-gated. The fixture discloses this in its own `provenance` block (`"capture": "hand-authored"`, `"capturedFrom": null`, and a `notEvidenceFor` member naming criterion 9). Why "cassettes" here does not mean "recorded": this box's own 2026-08-02 text above states the remedy as "a write-scenario fixture plus a `describe.each(["fake","cassette"])` parameterization" needing "neither CI nor the live engine", and criterion 9 says "**recorded** cassette" where recording is meant — the contrast is in this phase's own vocabulary. The §Interfaces produced deliverable "Recorded Cloud v3/Agile cassettes" remains UNSATISFIED and is named as such in transcript §R10. Stale pointer corrected in place rather than rewritten: the sentence above cites `resource-client/adf-guard.ts:80`, which was correct when written and moved to `:137` when PR #95 inserted ahead of it; and the `:113`/`:123`/`:132`/`:141`/`:149`/`:161`/`:171`/`:186`/`:245`/`:284-285` anchors in that sentence have moved. The old->new table is section R9 of the transcript — but read it as a table of CAUSES rather than of current positions: it is pinned to PR #119's own branch tip, and measuring the same fragments at this tree shows they have drifted a further +6 to +8 lines since, because later PRs landed above them. The quoted assertions are all still present and unchanged; only the pointers moved, and the full measured classification of that drift is recorded in this phase's closeout record rather than re-enumerated here, because a second stale list is worse than a pointer to the instrument that prints the current one.
- [x] `JiraResourceClient` conformance suite green against fake-Jira for every in-scope resource. — **Evidence (2026-08-02):** reads, per resource, with typed values asserted rather than mere non-failure — `packages/connectors-jira/src/resource-client/jira-resource-client.test.ts:53` `expect(list).toEqual([{ id: "10000", key: "PROJ", name: "Project" }])`, `:70` boards, `:87` sprints, `:135` `expect(search.issues[0]?.issueType).toBe("Epic")` (epics are issues, no separate namespace), `:137` issue get, `:139-146` the exact projected `transitions` shape criterion 7 depends on, `:163` comments, `:165` worklogs; plan builders for every mutating resource at `:241-566`, each asserting the P02 capability-flag label byte-identically (`:252` `["issue creation"]`, `:262` `["assignment"]`, `:270` `["reporter change"]`, `:386` `["sprint completion"]`, `:396` `["attachments"]`) with `:253` `expect(calls).toHaveLength(0)` proving planning is local; the same surface through 16's generic dispatch adapter in `jira-provider-client.test.ts` (`:71`, `:84`, `:96`, `:106`, `:130`); and the apply half in `jira-mutation-apply-client.test.ts:130-132`. Negative controls: `:178` a malformed body rejects as `kind: "validation"` rather than coercing, `:201-202` a token-fetch failure rejects with `expect(fake.calls).toHaveLength(0)`, and `jira-provider-client.test.ts:140-143` an unsupported resource rejects as `unsupported`. `CI` run 30745540255 green at `d60398f` (job-log lines 363, 364, 366); `docs/evidence/phase-18/closeout-c1-c2-flow-and-conformance.txt`. Scope note so the tick is not read wider: "fake-Jira" is not a separate double class in this package — it is 16's `createFakeProviderTransport` scripted with Jira REST v3/Agile payloads plus `src/testkit/fault-matrix.ts`, which is what §Interfaces produced asks for ("extending 16's fake-provider harness") but not what a reader looking for a `fake-jira.ts` will find.
- [x] Exactly-once via entity-property markers proven under injected POST timeout (no duplicate comments/issues). — **Evidence (2026-08-02):** `packages/connectors-jira/src/resource-client/exactly-once.integration.test.ts:191-273` is the load-bearing case — a `midPostTimeoutFault()` on `comment.create`, recovered through the REAL `createJiraEntityPropertyMarkerReconciler` (`:251-252`, never a stubbed `findByMarker`), ending in `:272` `expect(fake.calls.filter((c) => c.method === "POST")).toHaveLength(1)` and `:268-269` `expect(outcome.status).toBe("recorded")` / `expect(outcome.appliedRevision).toBe("60002")`. `:244` `expect(plan.idempotencyKey).toBe(expectedIdempotencyKey)` pins that what `buildRequest` stamps is what reconciliation searches for, and the scripted recovery body carries an unrelated comment at `:215` that must not match, so a reconciler returning the first comment it saw would fail. Fail-closed control at `:178-179` — an unfound marker yields `{status:"blocked", errorKind:"ambiguous_write"}`, never a blind retry. The `issue.create` half at `:106-135` adds the cross-process clause: `:134-135` a second identical call `replays` from the journal without re-entering the network. Everything runs through `@crabgic/gateway`'s real `executeMutationPlan` and a real temp-dir journal. `CI` run 30745540255 green at `d60398f` (job-log line 367); `docs/evidence/phase-18/closeout-c3-c4-exactly-once-and-forged.txt`. Recorded so the reader can weigh each half: the `issue.create` case overrides `findByMarker` on a real reconciler instance (`:95-96`), so on its own it would evidence the pipeline rather than the Jira marker mechanism — the `comment.create` case is what closes that.
- [x] Forged delete/admin/impersonation calls fail before network I/O. — **Evidence (2026-08-02):** `packages/connectors-jira/src/security/preflight-capability-guard.test.ts:32` `expect(() => assertAllowedJiraOperation(action)).toThrow(ConnectorError)` with `:38` `expect((err as ConnectorError).kind).toBe("policy_blocked")`, over the 16-member forged table at `:15-30` that includes the criterion's three named families verbatim (`"issue.delete"`, `"user.impersonate"`, `"admin.settings.update"`) plus permission/workflow/security-scheme updates, `"automation-rule.create"`, `"raw.request"`, the empty string and an injection string. Negative control at `:61` — all 17 in-scope actions are allowed — so "reject everything" does not satisfy this box. The "before network I/O" clause is carried structurally rather than by the absence of a transport in that file: `jira-mutation-apply-client.test.ts:139` throws inside `buildRequest` for a plan carrying a forged `issue.delete`, and `buildRequest` is called strictly before the pipeline's single `deps.httpClient.request` call (`packages/gateway/src/mutation-pipeline/mutation-pipeline.ts:213`), so a forged action cannot reach the wire even when the typed plan builders are bypassed entirely; `assertAllowedJiraOperation` additionally gates plan *construction* in `resource-client/plan-builder.ts`. `CI` run 30745540255 green at `d60398f` (job-log lines 400, 364); `docs/evidence/phase-18/closeout-c3-c4-exactly-once-and-forged.txt`.
- [x] `JiraWorkflowStage` never-guess proven: fuzzed/unrecognized workflow-status names always resolve to `blocked`, never `done`. — **Evidence (2026-08-02), with a dated wording correction:** `packages/connectors-jira/src/workflow/workflow-stage.test.ts:131-138` is the criterion's clause exactly — a `fast-check` property that every fuzzed name absent from the known table, with no category hint, resolves positively to `"blocked"` (`:136`); `:109-120` extends "never `done`" across every non-`done` category hint; `:37-45` is the regression guard for the prototype-chain bug the property itself found (`__proto__`/`constructor`/`toString`/`hasOwnProperty`/`valueOf` each asserted to be `"blocked"` — a plain-object lookup used to return `Object.prototype` for these keys, breaking never-guess by construction; fixed by switching the table to a `Map`); and `jira-resource-client.test.ts:374-381` proves the rule at the connector level, not just on the mapper — a server-reported transition to the unrecognized status "Frobnicating" yields no `closing transitions` flag and `impactClass: "reversible"`. `CI` run 30745540255 green at `d60398f` (job-log line 374); `docs/evidence/phase-18/closeout-c5-c6-never-guess-and-comparator.txt`. **Wording correction (2026-08-02).** The box's literal words are "always resolve to `blocked`, never `done`". There is exactly one case in which an unrecognized name resolves to `done`: when Jira's own `statusCategory.key` is `done` (`workflow-stage.test.ts:122-129` asserts it explicitly rather than excluding it from the proof). That is deliberate and is the *safer* direction, not a weaker one — `statusCategory.key` is a fixed, Jira-computed three-value enum a workflow admin selects, not attacker-shaped free text, and the next box's closing-transition gate depends on it: a real closing transition to a site-custom status name ("Ready for Prod") must still be recognized as closing, or it would bypass the done-evidence gate while closing the issue for real (`workflow-stage.ts:20-39` records the rationale). Implementing the literal wording would make the system less safe. The correction restores this file's own §In scope phrasing — "an unrecognized remote status always resolves to `blocked`, never *silently* to `done`" — whose "silently" the exit criterion drops. The check is unchanged; the original wording above is preserved verbatim.
- [x] Revision comparator detects a seeded material remote edit between two milestone polls and produces the amendment-review signal (evidence: property + integration fixture). — **Evidence (2026-08-07), closed by remediation. The assessment that follows is kept VERBATIM and is superseded, not deleted; it was filed as defect:** `docs/evidence/criteria-closeout/defects/18-revision-comparator-no-integration-fixture.md`. Two of the four clauses have no bearer. The comparator itself is sound and well tested in isolation: `packages/connectors-jira/src/intake/revision-comparator.test.ts:31-36` seeds a `rev-1`→`rev-2` edit and asserts `signal.material === true`, `:57-67` is the fast-check property the criterion names, and `:69-78` is its non-vacuity partner. But **no milestone poll exists** — `git grep -in poll -- packages/connectors-jira/src` outside `.test.ts` returns only the two doc-comment lines in `revision-comparator.ts` quoting this file, and `planMilestoneSync` neither polls a revision nor stamps a `RemoteResource`; every stamp in every test is hand-built by `buildRemoteResource`. And **no integration fixture exists** — `compareRemoteResourceRevisions` has zero production callers repository-wide, appearing only in its own module, the package barrel and its own unit test. The evidence README's claim that `milestone-sync.test.ts`'s dedup tests are "the companion integration evidence for the polling/sync loop this comparator feeds" does not survive reading that file: it never imports or calls the comparator. Near miss recorded so it is not re-derived: the comparator's sibling `stampJiraRemoteResource` *is* consumed in production, by 21's `packages/gates/src/remote-resource-binding.ts:131`, whose own header notes it had zero callers before that module existed; 21 closed that gap for the stamp and not for the comparator. Remedy is M-sized and additive to `planMilestoneSync`; needs neither CI nor the live engine, but should be coordinated with 21 as the declared consumer. **Evidence (2026-08-07), closed by remediation:** PR #113 made `planMilestoneSync` the poll, so the negative fact this box rests on ("`compareRemoteResourceRevisions` has zero production callers") is superseded. Four clauses, each with a named bearer. C1 (detects a seeded material remote edit): the `rev-1 -> rev-2` edit arrives through the SECOND scripted `issues.get` and is detected by the comparator called from the milestone path — `packages/connectors-jira/src/intake/milestone-sync.ts:13` imports it and `:184` calls it. C2 (between two milestone polls): each `planMilestoneSync` call carrying `revisionPoll` stamps a `RemoteResource` (`milestone-sync.ts:169-171`, `:195`), and the fixture runs two. C3 (produces the amendment-review signal): the returned `MaterialChangeSignal` (`packages/connectors-jira/src/intake/revision-comparator.ts:37-39`), now produced by a caller rather than latent in a dead export. C4a (property evidence): the pre-existing fast-check property pair, UNTOUCHED by that branch. C4b (integration fixture): the new two-cycle fixture running over a real `createJiraResourceClient` on a real `GatewayHttpClient` over `@crabgic/gateway`'s own `createFakeProviderTransport` — which is what makes it an integration fixture rather than a second unit test. Written RED first, in both halves the bearer has: a runtime red (`expected undefined to deeply equal { material: false }`) and a typecheck red (14 x TS2353/TS2339, `'revisionPoll' does not exist`). Non-vacuity: M1 deletes the poll and reddens FOUR tests in TWO packages — including the cli seam e2e through `dist/`, which is what proves the seam consumes 18's real producer rather than a stand-in; M2 lobotomizes the comparator call to a `{ material: false }` literal and reddens THREE, with the unchanged-revision control correctly SURVIVING, and M1-vs-M2's difference IS that control. Transcript: `docs/evidence/phase-18/milestone-poll-probe-batchB.txt`. **Residuals carried forward so the tick is not read wider than it is:** (1) there is no production milestone-polling LOOP — `planMilestoneSync` still has no production caller, and this criterion's own channel ("property + integration fixture") is what is satisfied, not a shipped loop; (2) when Jira omits `fields.updated`, `toJiraIssue` stamps the literal `"unknown"` (`packages/connectors-jira/src/resource-client/reads.ts:156`), so two such polls compare equal and the coarse revision signal reports non-material even if fields did change — the `fieldSnapshot` channel is what still surfaces such an edit to 21's classifier, which is the halt decision, so the guarantee survives but the coarse signal alone does not carry it.
- [x] Milestone sync yields ≤1 status comment per milestone, edited in place; Jira `done` only with 21's verification evidence attached. — **Evidence (2026-08-02):** first clause — `packages/connectors-jira/src/intake/milestone-sync.test.ts:99-101` proves a second sync for the same (issue, kind) emits `comment.update` against the existing comment (`expect(outcome.plan.canonicalTarget).toBe("issue:PROJ-1:comment:77")`) rather than a second create, with `:67-69` as its control (no existing marker ⇒ `comment.create`, so an implementation that always updated would fail), and `:125` `expect(new Set(markers).size).toBe(3)` making "per *milestone*" mean per milestone: start / material_blocker / verified_completion cannot collide onto one slot. Second clause — `jira-resource-client.test.ts:315-317` refuses a SERVER-reported closing transition (`toStatusCategoryKey: "done"`, `:312`) that carries no verification evidence, and `planTransition`'s parameter list has no `targetStageIsDone` at all, so the gate is not caller-suppressible; `:340-342` versus `:360-362` show the gate is satisfied by 21's *exact*-revision pointer (`confirmedRevision: "rev-1"`) and refused by a stale one (`"stale-rev"`). The enforcement point is `resource-client/issue-plans.ts:30-33`. `CI` run 30745540255 green at `d60398f` (job-log lines 383, 363, 404); `docs/evidence/phase-18/closeout-c7-c8-milestone-and-attachment.txt`. Scope note: "≤1 comment" is proven at the PLAN level — `planMilestoneSync` emits one plan, never two — not by counting POSTs across two real sync cycles on a wire. `:137` additionally shows it fails closed rather than posting an over-long comment once 17's regenerate-once protocol gives up.
- [x] Attachment pipeline rejects oversized/spoofed-MIME/secret-embedded fixtures before any byte reaches a prompt. — **Evidence (2026-08-02):** `packages/connectors-jira/src/attachments/attachment-pipeline.test.ts` — oversized at `:31`/`:37-40` (`Buffer.alloc(MAX_ATTACHMENT_BYTES + 1)` ⇒ `reason` matches `/size/i`), spoofed MIME at `:51-52` (claimed `image/png`, plain-text magic bytes ⇒ `/mime|signature/i`), secret-embedded at `:99-100` (AWS-key-shaped body) and `:112-113` (PEM private-key header), plus path-traversal `:62-63`, a disguised Windows executable `:73-74`, EICAR `:86-87`, and a secret-shaped *filename* `:161-162` (which would otherwise be embedded verbatim into `redactedDiff`, journaled before any I/O). The "before any byte reaches a prompt" clause is made checkable at `:124` — `expect(JSON.stringify(result)).not.toContain(secretPayload)` — non-vacuous because the preceding cases prove this same input *is* rejected, so `result` is a populated rejection whose `reason` could have echoed it; and `jira-resource-client.test.ts:397` `expect(plan.redactedDiff).not.toContain("/")` keeps the path out of the plan. Two cases exist specifically because the scans were once windowed: `:131-141` places a secret past the old 64 KiB window and `:143-153` an EICAR marker past the old 4 KiB one, against a 10 MiB cap. Negative control at `:27` — a small, correctly-typed, clean PNG is accepted. `CI` run 30745540255 green at `d60398f` (job-log line 373); `docs/evidence/phase-18/closeout-c7-c8-milestone-and-attachment.txt`. Scope: this proves the validator's verdict carries no content and that rejection precedes staging; it is not a proof about any particular prompt-assembly path, which lives outside this package.
- [ ] Fake-Jira/cassette parity proven: the scripted scenario set replayed against both fake and recorded cassette yields identical typed results. — **Left unticked 2026-08-02, defect filed:** `docs/evidence/criteria-closeout/defects/18-cassette-parity-is-a-tautology.md`. The parity assertion cannot fail. `packages/connectors-jira/src/testkit/fake-cassette-parity.test.ts:20` asserts `expect(fromCassette).toEqual(fromFake)`, and the suite's own next test at `:26` asserts `expect(cassette.responses).toEqual(HAND_AUTHORED_READ_SCENARIO.responses)` — the two sources are the same bytes, and `runScriptedReadScenario` is a pure function of its script (fixed base URL, fixed token, no clock, no randomness in the returned `ScenarioResults`), so equal inputs give equal outputs by construction. Verified against the fixture rather than the description: every `bodyText` in `src/testkit/fixtures/read-scenario.cassette.json` is byte-identical to `JSON.stringify` of the corresponding object literal in `scripted-read-scenario.ts:34-93`, and the file carries no capture metadata of any kind — no timestamp, no request URLs, no response headers, no instance identity. `scripted-read-scenario.ts:23-31`'s doc comment claims the opposite ("two INDEPENDENT sources … only meaningful if the two sources are maintained independently"); `e2e/matrix/connector/src/exactly-once/jira-cassette-readback.test.ts:100-104` repeats the same assertion and the same wording in the release-e2e matrix, adding a second instance of the tautology rather than independent evidence. The repository already discloses this honestly for the sibling fixtures — `fake-cassette-parity-dc.test.ts:16-22` for Data Center and `e2e/attestation/src/traceabilityEvidence.ts:11-12` for Grafana — and the Cloud side is the one place the disclosure is missing. Remedy: S-sized to stop the box being false (drop `:26`, correct the doc comment), L-sized to make it tickable (capture a real cassette against the Jira Cloud sandbox this file's §Risks already says 23 needs).
- [x] Rate-limit fixture: `Retry-After` honored; per-issue write order preserved. — **Evidence (2026-08-07), closed by remediation. The assessment that follows is kept VERBATIM and is superseded, not deleted; it was filed as defect:** `docs/evidence/criteria-closeout/defects/18-per-issue-write-order-not-preserved.md`. The first clause is closed on its own merits: `packages/connectors-jira/src/testkit/rate-limit-and-write-order.test.ts:52-55` drives a 429 carrying `retry-after: 2` through the REAL `JiraResourceClient` and asserts `expect(sleepCalls).toEqual([2000])` and `expect(attempt).toBe(2)` — exactly one sleep, of exactly the header's duration. The second clause is measurably false. Its only bearers (`:60-106`, `:108-146`) construct no resource client, no plan and no `executeMutationPlan` call: they call `httpClient.request({ … resource: "issue:PROJ-1" … })` directly with the key written as a string literal, so they evidence 16's transport, not this connector's wiring — notwithstanding that file's doc comment at `:14-18`, which claims they prove the latter. Serialization is keyed on `RemoteMutationPlan.canonicalTarget` (`packages/gateway/src/mutation-pipeline/mutation-pipeline.ts:216`), and `resource-client/canonical-target.ts` mints a different target per resource kind on the same issue (`issue:PROJ-1`, `issue:PROJ-1:comment`, `issue:PROJ-1:worklog`, `issue:PROJ-1:attachment`). Measured, not inferred: a probe applying a real `issue.update` and a real `comment.create` for PROJ-1 concurrently through the real pipeline recorded `maxInFlight = 2` with both starts preceding either end, while its control — two `issue.update` plans, identical canonical target — recorded `maxInFlight = 1`. Deliberately RED transcript, probe source reproduced verbatim inside it: `docs/evidence/phase-18/closeout-c10-write-order-probe.txt`. Exactly-once is unaffected (it keys on `plan.idempotencyKey`); what this costs is 429 pressure on a busy issue and undefined arrival order between a milestone comment and a field update. `docs/evidence/phase-18/closeout-c9-c10-parity-and-rate-limit.txt` records the suite's own green run. **Fix landed 2026-08-04 (PR #84), box stays unticked:** serialization now keys on the provider `serializationTarget` hook — `issue:<key>` for all issue-scoped writes, on Cloud and Data Center alike; `canonicalTarget` is unchanged and remains the identity/audit key. Connector-level proof `packages/connectors-jira/src/testkit/write-order.integration.test.ts` (real plan builders, real `executeMutationPlan`, real temp-dir journal, Cloud and DC arms, `=== 2` different-issue controls); RED→GREEN plus six probes at `docs/evidence/phase-18/writeorder-fix-red-green.txt`. **Why the box stays unticked — this criterion is unqualified:** the fix covers SINGLE-issue writes. `issue.bulkUpdate`/`issue.bulkTransition` mint a multi-issue `bulk:<keys>` target that a single-string mutex key cannot express, so a bulk write touching PROJ-1 still races a single-issue write to PROJ-1, and two bulk plans whose key lists overlap — or differ only in ORDER, which mints a different target string — still race each other. Closing that needs multi-key lock acquisition in 16's `WriteSerializer`. The residual is asserted in `canonical-target.test.ts`, not merely described, so it cannot change silently. The two prior bearers at `:60-146` are kept and relabelled with a dated correction — they evidence 16's transport, not this connector. **Evidence (2026-08-07), closed by remediation:** the `bulk:<keys>` residual is closed by PR #125, so the unqualified criterion is now met. `packages/gateway/src/transport/write-serializer.ts:100-104`'s new `runExclusiveMulti` acquires every member mutex (and canonicalises the key set on its own side), and BOTH Jira apply clients map a bulk plan to its sorted, deduped member keys through one widened helper — `packages/connectors-jira/src/resource-client/canonical-target.ts:106-115`, consumed identically at `.../jira-mutation-apply-client.ts:371` and `.../datacenter/jira-mutation-apply-client-dc.ts:356`. Written RED first at `1bc0d51`: 9 failed | 22 passed (31), the four integration failures each reading `expected 2 to be 1` — the race itself, observed on the wire through real plan builders, the real `executeMutationPlan`, a real temp-dir `JournalStore` and a real `GatewayHttpClient`. The THREE `=== 2` controls were green in that same RED run and stay green after, which is what rules out a global-mutex "fix": a bulk plan versus a NON-member issue, two bulk plans over DISJOINT sets, and the Data Center twin of the first. Reverse probe C folds the mapping back to the pre-fix passthrough and reddens all nine, so none of the new assertions is vacuous. A finding this batch corrected in its own draft is recorded rather than smoothed: probe A shows the connector-side `.sort()` reddens only the UNIT case, because `runExclusiveMulti` re-canonicalises — the connector sort is DEFENCE IN DEPTH, not the mechanism that closes the order-permutation race. Cross-package probe D reports both numbers (3 failed src-only vs 7 after `npm run build`), the rule that a stale `dist` understates blast radius. Per-push proof, ANSI-stripped and byte-compared under the one-space rule, `main` `6f964d7` job 92626742663 vs PR #125 job 92641855998: `canonical-target.test.ts` 14 -> 19, `write-order.integration.test.ts` 5 -> 12, `write-serializer.test.ts` 5 -> 14, with Grafana's SAME-BASENAME `src/mutation/canonical-target.test.ts` as the control row at 5 -> 5. Transcript: `docs/evidence/phase-18/bulk-write-order-batchC.txt`. **Correction to this box's own text, dated 2026-08-07:** the sentence above says "The residual is asserted in `canonical-target.test.ts`" — that pin sat at `canonical-target.ts:84-96` and no longer exists; those lines now hold a doc comment declaring the residual CLOSED, and the two cases marked "was the residual" are the exact inversions of what the file previously asserted. **Residual, narrowed rather than eliminated, and asserted rather than described:** disjoint bulk/member key sets still run concurrently on purpose, pinned by the `=== 2` controls that are green BOTH before and after the fix — so over-serialization cannot creep in silently either. Nothing here evidences behaviour against a real Jira; every leg is a fake transport.

## Risks & open questions

- No unconfirmed Claude Code engine facts are load-bearing here — this phase's only engine-adjacent dependency is transitive (16's transport/sandbox posture); nothing in this file asserts a flag/setting/event the adaptation doc doesn't already settle.
- `JiraResourceClient` and `JiraWorkflowStage` are named here for the first time; if 19 has independently committed to different names for the same concepts, reconcile before both land — flagged for the reconciler.
- Jira REST v3 API drift is a provider-API risk, not an engine-version risk; 21's drift CI owns detecting it — not re-litigated here.
- ADF safe-subset gaps in 17 could block a Jira rendering need this phase surfaces late; raise against 17 rather than working around it locally — no phase may special-case the lint.
- Adaptation §10 risk 8 (OAuth'd remote MCP is interactive-only) is why Jira auth here is service-account client-credentials held by the gateway, never an interactive Claude Code MCP OAuth flow.
- Live Jira Cloud sandbox needed by 23 — provision early; cassettes refreshed by 21's drift job.
