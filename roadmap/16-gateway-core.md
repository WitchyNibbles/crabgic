# Phase 16 — Connector gateway core: transport, secrets, capability discovery, operation journal

| | |
|---|---|
| **Depends on** | 02, 04, 05 |
| **Unlocks** | 18, 20 |
| **Sources** | original plan gateway/auth/data-minimization sections; adaptation §5.5 (gateway MCP tool list), §6.1 (`gateway mcp` `.mcp.json` invocation literal), §7 (connector notes: gateway-only egress, gateway-side result-size budgets, strict-MCP-config default) |
| **Primary package** | `packages/gateway` |

## Goal

The provider-neutral policy gateway and the sole host of the single `crabgic_gateway` MCP server (name owned by 02): secure transport, secret references, capability snapshots, and a plan → validate → journal-before-network-I/O → apply → read-back → verify → record mutation pipeline with exactly-once semantics, exposed to the manager and to every worker through one extensible tool-registration API — tested throughout against scriptable fake providers. This phase natively implements `tracker.*`, `observability.*`, `evidence.get`, `evidence.attach`, and `result.submit`, and forwards `run.status`/`run.cancel` over UDS to 05's own router rather than reimplementing them; 09's `gateway mcp` command is a thin argv shim that boots this phase's server over stdio, and 11/12 later register their own already-built handlers (`project.inspect`/`contract.approve`, `capability.audit`/`capability.approve`) into the same registry, each with no new cross-phase dependency edge. Before this phase lands, 09 ships `gateway mcp` against a stub tool list and no connector has a transport to build against; after it, the registry is real, every mutating tool is exactly-once and SSRF-hardened, and 18/20 have the transport their Jira/Grafana resource clients are built on, not beside.

## In scope

- **ExternalConnection store:** provider, deployment type, exact HTTPS base URL, allowed redirect origins, tenant/org/project/folder allowlists, custom CA reference, allowed resources/actions, discovery TTL; secret **references** only (env, file 0600, exec backends; extensible) — never a literal credential in worker- or manager-reachable state.
- **Transport security:** TLS verification, redirect revalidation before credentials attach, scheme/origin/IP-range/base-path allowlists (SSRF guard incl. private ranges + DNS pinning), ≤4 in-flight per connection, Retry-After + jittered bounded backoff, write serialization per tenant+resource, O(page) pagination, N+1 detection.
- **CapabilitySnapshot:** product/edition/version/API families/resources/actions/permissions/expiry; 15-min cache; invalidation on auth/permission/unsupported errors; unknown versions read-only.
- **Mutation pipeline:** `RemoteMutationPlan` (connection+tenant, canonical target, action+capability, redacted diff, desired-state hash, idempotency key, expected remote revision, impact+rollback class, envelope ref) → persist `RemoteOperationRecord` **before network I/O**, via 04's idempotency registry → apply → read-back compare → verify → record. Same-ID+same-content replays the recorded result byte-identical; different content is a typed conflict (04's registry), never a silent overwrite.
- **Provider dispatch:** a provider-keyed extension point inside `tracker.*`/`observability.*` (keyed on `ExternalConnection.provider`), distinct from the MCP tool registry below — 18's `JiraResourceClient` and 20's `GrafanaProviderAdapter` register into it, and adding a provider never adds a new tool name; 18/19/20 register no MCP tool of their own.
- **Ambiguity:** marker-reconciliation interface declared here, adapters implement it (Jira entity properties, 18; deterministic UIDs + annotation tags, 20); ambiguous POST timeout → search marker before retry; unresolvable → canonical `ambiguous_write`, outcome treated as unknown, **block** — never a guessed duplicate.
- **Retry ladder:** GET free within budget; PUT/PATCH deterministic + precondition only; POST never blind; 409/412 → fetch, rebase-or-block.
- **Budgets:** 32 KiB item / 256 KiB result enforced here (typed truncation errors) — deliberately independent of the unconfirmed engine-side `MAX_MCP_OUTPUT_TOKENS` (adaptation §7 point 2, §10 item 10).
- **Sole MCP host & extensible tool-registration API:** this phase hosts the one `crabgic_gateway` MCP server (`GATEWAY_MCP_SERVER_NAME`, constant owned by 02) and natively registers `tracker.search/get/plan_create/plan_update/plan_transition/plan_comment/apply`, `observability.search/get/query/plan_create/plan_update/apply`, `evidence.get`, `evidence.attach`, `result.submit`, and forwards `run.status`/`run.cancel` over UDS to 05's own contract-typed router — never a second implementation of those two. The same registration API, not just these native families, is what 11 (`project.inspect`, `contract.approve`) and 12 (`capability.audit`, `capability.approve`) plug their own already-built handlers into when they land, with no new cross-phase dependency edge. There is **no** `change_set.*` family (`project.inspect`, 11, answers ChangeSet-state queries instead) and **no** `learning.*` family (promotion/review is CLI-only, 22); this registry must never grow either one (22's grep-based CI gate scans this package's registered tool names for exactly that). Every mutating tool requires run ID, idempotency key, expected state version, envelope reference, and a validated plan.
- **Upstream MCP client (optional, flag-gated):** for a provider that also exposes its own official MCP server (Atlassian, Grafana), this phase — never a worker, and never the manager directly — is the one process permitted to act as MCP *client* to it, behind a per-connection capability flag with REST remaining primary and default; adaptation §7 point 1 is unconditional on the other side of that boundary: no worker ever sees an upstream provider's MCP server, only this phase's own `tracker.*`/`observability.*` tools.
- **Fake providers (testkit):** scriptable tracker + observability doubles with fault injection (429/401/409/412, malformed pages, mid-POST timeouts) — extended, not reimplemented, by 18/19/20's own fault matrices.

## Out of scope

- New MCP tool families beyond the ones this phase natively registers — 11 (`project.inspect`, `contract.approve`) and 12 (`capability.audit`, `capability.approve`) own and register their own handlers into this phase's registry; this phase hosts the registry, not their business logic.
- The `gateway mcp` CLI-level argv shim — parser, help text, stdio process bootstrap, doctor integration — 09; this phase supplies the server 09's shim boots, never the shim itself.
- The `connection add|list|doctor|capabilities` command's parser/argv surface — 09 (ships it `NOT_IMPLEMENTED` until wired); this phase supplies the `ExternalConnection` store and capability-snapshot logic that backend calls into, never the command surface.
- Provider-specific resource semantics: discovery field mapping, workflow-transition mapping, milestone sync, OAuth/PAT token management — 18 (Jira Cloud), 19 (Jira Data Center), 20 (Grafana); this phase supplies only the generic transport, pipeline, and provider-dispatch point they plug into.
- Deciding whether a given deployment enables the optional upstream-MCP-client wrap for a provider — that per-connection flag lives in the `ExternalConnection` config 18/20 populate (or a human operator sets), never decided unilaterally by this phase.
- Rendering or linting of any communication text that travels through a connector write (ADF, PR/review-comment text, Grafana annotation templates) — 17; this phase moves already-rendered bytes, never authors or lints them.
- Approval-preview rendering of planned remote mutations, and `IntentContract`/`AuthorizationEnvelope` assembly — 11; this phase executes an already-approved plan, it never decides whether one should be approved.
- Binding confirmed remote revisions onto `EvidenceRecord`, requirement↔resource traceability views, and drift-CI replay — 21; this phase only produces the confirmed revision its own read-back step yields.
- `docs/threat-model.md` authorship — 02; this phase's transport/secret/pipeline surface is one of the document's required review passes, not the document itself.
- A `change_set.*` or `learning.*` MCP tool family, under any name, ever — deliberately absent everywhere in v1 (Gap 1 resolution; see In scope).

## Interfaces produced

- **`ExternalConnection` store** (02 schema; this phase implements the store) — CRUD + secret-reference resolution over env/file-0600/exec backends. Consumed by 09 (`connection add/list/doctor/capabilities` backend), 18, 19, 20 (populate/read connections for their own providers).
- **Connection-doctor reachability probe** — a primitive that exercises a stored connection end-to-end (incl. custom-CA validation) without performing a mutating call. Consumed by 18/19/20's own doctor-check functions (19's own words: "connection-reachability probe exercising 16's custom-CA path") and surfaced to the human through 09's `connection doctor <id>`.
- **Transport security stack** (TLS + redirect revalidation + SSRF guard + throttle/backoff + per-tenant+resource write serialization + O(page) pagination + N+1 detection) — consumed by 18 ("this phase's resource clients are built on this transport, not beside it," 18's own words), 19 (reuses 18's cross-worker throttling), 20.
- **`CapabilitySnapshot` cache** (02 schema; this phase owns the cache, 15-min TTL, and invalidation rules) — consumed by 18, 20 (populate instances from provider discovery); unknown versions default read-only.
- **Mutation pipeline** (`RemoteMutationPlan` → `RemoteOperationRecord` persisted before network I/O via 04's `IdempotencyRegistry` → apply → read-back → verify → record) — consumed by 18, 20 (every create/update/transition/comment call submits through this; neither phase persists `RemoteOperationRecord` itself), 21 (binds the confirmed revision this pipeline's read-back step yields onto `EvidenceRecord`).
- **Marker-reconciliation interface** — declared here, implemented by 18 (Jira entity properties) and 20 (deterministic UIDs + annotation tags); exercised by this phase's own ambiguous-POST retry search and by 21/23's exactly-once E2E scenarios.
- **Canonical-error redacting constructors** (02's 10-member union) — consumed by 18, 19, 20 (map every provider failure to exactly one member; no raw body crosses the boundary), 21 (`ambiguous_write`/`unsupported` block `final_verifying`).
- **Provider-dispatch point** (provider-keyed extension inside `tracker.*`/`observability.*`) — consumed by 18's `JiraResourceClient` and 20's `GrafanaProviderAdapter`; adding a provider never adds a tool name.
- **Result/item size budgets** (32 KiB item / 256 KiB result, typed truncation errors) — consumed by 18, 19, 20 (query/aggregation layers stay under this).
- **The `crabgic_gateway` MCP server + its extensible tool-registration API** (`GATEWAY_MCP_SERVER_NAME`, 02) — natively registers `tracker.*`, `observability.*`, `evidence.get`, `evidence.attach`, `result.submit`, and forwards `run.status`/`run.cancel` to 05; wire names `mcp__${GATEWAY_MCP_SERVER_NAME}__<family>.<op>` (e.g. `mcp__crabgic_gateway__tracker.search`, `mcp__crabgic_gateway__result.submit`). Consumed by 09 (its `gateway mcp` argv shim boots this server over stdio — 09 owns no tool behavior), 11 and 12 (register `project.inspect`/`contract.approve` and `capability.audit`/`capability.approve` into it at their own build time, no new dependency edge), 23 (full-registry completeness release gate).
- **Fake-provider testkit harness** (scriptable tracker + observability doubles, fault injection: 429/401/409/412, malformed pages, mid-POST timeouts) — built in work item 6 (20's own text cites this exactly: "built by 16 work item 6"), directly extended by 18 ("extends 16's harness," 18's own words) and 20; extended transitively by 19, which builds on 18's already-extended fake-Jira rather than on this phase's harness directly.
- **`docs/threat-model.md` review obligation** — this phase's transport/secret-handling/pipeline surface is a required review pass against that document (02's own citation of this phase).

## Interfaces consumed

**From 02 (`packages/contracts`):**
- Schemas this phase implements logic around, without redefining: `ExternalConnection`, `CapabilitySnapshot`, `RemoteMutationPlan`, `RemoteOperationRecord`. (`RemoteResource` is a related 02 schema, but 02's own consumer table names only 18, 20, and 21 for it, not this phase — this phase's pipeline treats a mutation's canonical target/read-back state as opaque bytes; 18/20 are the ones who construct `RemoteResource` instances from it.)
- Canonical connector-error union (10 members) — this phase's redacting constructors instantiate it; no raw provider-body field exists on the public type by construction (02's own type-level guarantee).
- `GATEWAY_MCP_SERVER_NAME` constant (value `"crabgic_gateway"`) — this phase's SDK server registration and every wire-level `mcp__${GATEWAY_MCP_SERVER_NAME}__<tool>` name derive from this; never a second hand-typed literal (02's repo-wide grep/golden-value CI check enforces this across `packages/*`).
- `JournalEntryType`'s `remote_operation_record` member — every `RemoteOperationRecord` this phase persists before network I/O is journaled against exactly this member.
- Run-lifecycle enum — canonical `ambiguous_write` routes toward the run's `blocked` terminal (02's own disambiguation: a different enum than Jira's own 4-state ticket-status `blocked`, 18, sharing only the token).
- Testkit fixture builders — this phase's fake providers are built against these rather than inventing separate fixture plumbing.

**From 04 (`packages/journal`):**
- `IdempotencyRegistry.checkOrRecord(operationId, contentHash, compute)` → `{ status: "replayed" | "recorded" | "conflict", result? }` — the exactly-once primitive the mutation pipeline calls before any network I/O; 04 supplies only the `(operationId, contentHash)` primitive, this phase owns the plan→validate→journal→apply→read-back→verify→record sequence wrapped around it.
- `appendEntry`/`queryEntries` — the durable write/read path every `RemoteOperationRecord` flows through.
- `runKillHarness` — reused directly for this phase's own crash-before/after-remote-commit exactly-once matrix (04 already names 05/13/23 as reusers; this phase is another).

**From 05 (`packages/supervisor`):**
- Contract-typed router ops `run.status`/`run.cancel` (05's own "Contract-typed router (`run.status`, `run.cancel`, internal `worker.*`)") — this phase's MCP-visible `run.status`/`run.cancel` tools are forwards over UDS to these, never a second implementation.
- UDS API (ndjson request/response, socket 0600 in 0700 dir, `SO_PEERCRED` uid check) — the transport those forwards ride over; 05's own trust-boundary description names the gateway as one of the two callers admitted to the socket, alongside the CLI.

## Work items

1. Connection store + secret-reference resolution (env, file 0600, exec backends; extensible).
2. HTTP client wrapper with the full security/throttle/serialization stack (TLS/redirect revalidation/SSRF guard/backoff/per-tenant+resource serialization/pagination/N+1 detection).
3. `CapabilitySnapshot` cache + invalidation rules.
4. Mutation pipeline (`RemoteMutationPlan` → journal-before-network-I/O via 04's idempotency registry → apply → read-back → verify → record) + reconciliation interfaces + canonical-error mapping (provider bodies never leak) + 32 KiB/256 KiB budget enforcement with typed truncation errors.
5. `crabgic_gateway` MCP server + extensible tool-registration API + provider-dispatch point: native `tracker.*`/`observability.*`/`evidence.get`/`evidence.attach`/`result.submit` handlers wired to items 1–4, forwarded `run.status`/`run.cancel` to 05, the registry surface 11/12 later populate, and the provider-keyed extension point inside `tracker.*`/`observability.*` that 18/20 register their resource clients into. Failing-first: booting against an empty native-handler set still lists a well-formed, empty registry without crashing — mirroring 09's own stub-registry convention — before any native handler is wired; a duplicate-name registration attempt is rejected before the check exists.
6. Fake providers (testkit) + fault matrix (429/401/409/412, malformed pages, mid-POST timeouts), extending 03's fake-engine fixture format — the harness 18 extends directly and 20 extends independently (19 extends 18's already-extended copy).

## Test plan

- **Unit:** secret-reference resolver per backend (env/file-0600/exec); redirect/SSRF allowlist matcher (foreign origin, private IP range, scheme mismatch); canonical-error constructors reject a raw-body field at the type level; retry-ladder decision table per verb.
- **Property (fast-check):** random redirect chains never reach a private/foreign-origin target pre-credential-attach; random `(operationId, contentHash)` sequences never silently overwrite; random paginated fixtures keep memory O(page) regardless of total item count; random ambiguous-POST timing never produces a duplicate remote object.
- **Integration:** fake-provider matrix (tracker + observability) across the full fault set (429/401/409/412, malformed pages, mid-POST timeouts); `gateway mcp` boot listing exactly the native tool set over stdio to a stub MCP client, plus a test-registered extra tool proving the registration API is genuinely extensible, not hardcoded to the native families.
- **Conformance:** exactly-once crash matrix reusing 04's `runKillHarness` — kill before/after the remote-commit point, identical-content replay, changed-content rejection, ambiguous-POST reconciled-or-blocked — none produce a duplicate or a silent overwrite; provider-body redaction proven by a live substring search across every error/log/artifact surface.
- **Security:** SSRF suite (foreign-origin redirect, private-IP redirect, scheme downgrade — all refused pre-credential-attach); custom CA honored against a disposable self-signed test server; forged delete/admin/impersonation calls fail before any network I/O; a blanket `mcp__*` deny is never required to keep this server's tools safe (Appendix B's own footgun, exercised here from the gateway side).

## Exit criteria

**Closeout pass 2026-08-02:** 11/12 ticked against recorded evidence; the one unticked box below
(criterion 12) is an open defect — see
`docs/evidence/criteria-closeout/defects/16-threat-model-signoff-not-landed.md` and its own per-box
note. Machine-readable index: `docs/evidence/criteria-closeout/phase-16.json`.

Shared citations reused by several boxes below. **`CI` run
[30743626882](https://github.com/WitchyNibbles/crabgic/actions/runs/30743626882)**, green at
`30f931e` — its `unit-test+coverage (ubuntu-latest)` job
([91485325042](https://github.com/WitchyNibbles/crabgic/actions/jobs/91485325042)), step "test with
80% line+branch coverage gate", executed 625 test files / 6155 tests (job-log lines 1032–1033) and
names all 25 `@crabgic/gateway` suites individually (job-log lines 593–644). **`learning-redteam`
run [30743626860](https://github.com/WitchyNibbles/crabgic/actions/runs/30743626860)**, green at the
same commit — its job ([91485325005](https://github.com/WitchyNibbles/crabgic/actions/jobs/91485325005))
runs 22's grep gate over this package's own source as its own named step (job-log lines 196, 203).
Scoped local re-runs, captured verbatim at the closeout commit with their command lines and exit
status, are committed as `docs/evidence/phase-16/closeout-c*.txt`; three of them are static scans
that close scope gaps the in-tree scanners do not cover (criteria 5, 7 and 8).

Citation re-resolution. Every quoted code fragment in the index was located mechanically against the
tree this branch merges into, immediately before pushing: **95 resolvable `test`/`artifact`
citations, 365 code-shaped quoted fragments, 365 exact content matches (whitespace-collapsed), all
within four lines of their cited span, and zero citations yielding no extractable fragment.** A
further 167 quoted fragments are prose rather than code and are therefore not line-anchorable; they
are counted and reported rather than silently skipped. The first resolver run surfaced 22 genuine
defects in this record's own first draft — stale line spans, three quotes of a second file smuggled
inside a first file's citation, one `?.` added to an assertion that does not have it, and one
`:30-32` that is really `:29-31` — every one of which is fixed above rather than tolerated.

- [x] Exactly-once matrix on fakes: crash before/after remote commit, identical replay, changed-payload rejection, ambiguous POST reconciled or blocked — none produce a duplicate or silent overwrite. Evidence: `runKillHarness`-driven CI suite. — **Evidence (2026-08-02):** the named harness, run for real: `packages/gateway/src/mutation-pipeline/mutation-pipeline.test.ts:600-671` drives `@crabgic/journal`'s `runKillHarness` against two child-process fixtures — the idempotent PUT (killed before *and* after the network call) and `kill-harness-fixtures/nonidempotent-post-and-crash.mjs`, a genuinely non-idempotent create (killed after the create, once with marker reconciliation and once with it disabled) — each verifying by re-running to completion and counting an irreversible on-disk side-effect line, `recovered: count === 1`. Identical replay `:143-144`; changed-payload typed conflict with no re-apply `:160-162`; ambiguous POST blocked `:377-379` or reconciled `:403`; journal-before-I/O under one `operationId` `:125`. CI job-log line 601 records the suite (26 tests). Transcript `docs/evidence/phase-16/closeout-c1-c5-transport-and-pipeline.txt`.
- [x] SSRF suite: foreign-origin/private-IP redirects refused pre-credentials; custom CA honored against a disposable self-signed server. — **Evidence (2026-08-02):** foreign-origin redirect refused with the hop never dialed (`http-client.test.ts:184-185`); private-IP redirect refused where only the second hop's resolution differs (`:188-204`); hop bound `:220`. Property coverage over random origins and private ranges at `ssrf-guard.test.ts:127-154`, plus MEDIUM #4's IPv4-mapped/NAT64-embedded cases `:37-48` against their embedded-public control `:50-55`. Custom CA honoured against a real `openssl`-generated disposable HTTPS listener at `http-transport.test.ts:117-127`, with the no-CA negative control `:129-139`. DNS-rebinding TOCTOU closed and proven at the socket, `http-client.test.ts:81-112` and `http-transport.test.ts:173-186`. CI job-log lines 594, 607, 614. Transcript `docs/evidence/phase-16/closeout-c1-c5-transport-and-pipeline.txt`.
- [x] Retry ladder proven per verb (GET free, PUT/PATCH deterministic+precondition, POST never blind, 409/412 fetch-rebase-or-block); per-resource write order preserved under concurrency. — **Evidence (2026-08-02):** `retry-ladder.test.ts:7-47` is one `it()` per clause, all at status 503 so the verb is the only variable, with the PUT pair differing only in `hasPrecondition`; 409/412 exhaustive over all five verbs `:49-65`; two fast-check properties closing the table `:89-130`; give-up proven to have three distinct causes `:67-87` so it is not the answer to everything. Order under concurrency at `write-serializer.test.ts:9-23` (durations deliberately out of submission order) with cross-key and cross-tenant concurrency controls `:25-84`, and through the real client on the write path at `http-client.test.ts:316-340`. Fault-matrix corroboration `fault-matrix.test.ts:63-93`. CI job-log lines 594, 598, 618, 622. Transcript `docs/evidence/phase-16/closeout-c1-c5-transport-and-pipeline.txt`.
- [x] Budgets enforced (32 KiB item / 256 KiB result, typed truncation errors); pagination memory stays O(page) on a 10k-item fake. — **Evidence (2026-08-02):** the two literals at `transport/budgets.ts:11-12`, measured in real UTF-8 bytes `:30-32`; boundary-exact one byte apart at `budgets.test.ts:16-21`; aggregate rule `:53-56`; the typed error's `scope`/`budgetBytes`/`actualBytes` asserted `:59-68`. Enforced at the MCP boundary — a 300 KiB provider result yields a typed error, never a truncated payload (`native-registry.test.ts:273-299`) — and on the transport's response path one byte over 256 KiB (`http-client.test.ts:298-312`). O(page) on 10 000 items at `pagination.test.ts:58-71` and `fault-matrix.test.ts:184-194`, each also asserting all 10 000 were delivered, plus the `maxPages` bound `pagination.test.ts:48-55`. CI job-log lines 594, 598, 636, 637. Transcript `docs/evidence/phase-16/closeout-c1-c5-transport-and-pipeline.txt`.
- [x] Leak hunt: no raw provider body in any error, log, or artifact (live substring search). — **Evidence (2026-08-02):** `security/leak-hunt.test.ts` plants a distinctive marker inside `rawProviderResponse` and recursively greps every observable output string for it (`:36-52`) across three surfaces — canonical-error `toData()` and its JSON round trip `:55-78`, the real native tool-registry dispatch result `:93-127`, and a failed mutation's outcome *plus every `remote_operation_record` entry it wrote to a real on-disk journal* `:143-193`. Independent second bearer with a different marker at `native-registry.test.ts:232-270`. Exhaustive by construction: every read tool's failure branch funnels through the single `errorResult` at `provider-dispatch-tool.ts:39-41`. The "log" surface is satisfied by absence, checked not assumed: no `console.` and no logger in any production source, and the only `process.stdout` occurrences are one JSDoc line about the MCP transport pipe and two kill-harness fixtures emitting a fixed fault marker — all read out in `docs/evidence/phase-16/closeout-c5-no-logging-surface.txt`. CI job-log lines 593, 600, 640. Transcript `docs/evidence/phase-16/closeout-c1-c5-transport-and-pipeline.txt`.
- [x] `gateway mcp` lists exactly this phase's native tool set (`tracker.*`, `observability.*`, `evidence.get`, `evidence.attach`, `result.submit`, forwarded `run.status`/`run.cancel`) over stdio to a stub MCP client, and accepts one externally-registered tool with no name collision — proving the registration API 11/12 depend on later is genuinely open, not hardcoded. — **Evidence (2026-08-02):** `mcp/server.test.ts:106-121` spawns this package's stdio boot fixture as a real child process, connects a real `@modelcontextprotocol/sdk` `Client`, and asserts set equality against the 18-name golden list at `:15-34`; the extensibility half `:123-140` registers 11's own `project.inspect` through the public API and asserts the native eighteen are all still present exactly once alongside it. Real `callTool` round trips both ways `:142-187`. Work item 5's failing-first note — an empty registry lists an empty set rather than `METHOD_NOT_FOUND` — at `stdio-boot.test.ts:129-134`. In-process companion `native-registry.test.ts:48-57`. The boot is this phase's server, not 09's argv shim, which §Out of scope assigns to 09. CI job-log lines 593, 609, 617. Transcript `docs/evidence/phase-16/closeout-c6-c11-mcp-dispatch-doctor.txt`.
- [x] No `change_set.*` or `learning.*` name is ever registered — repo-wide grep/golden-value check over `packages/gateway`'s registered tool names (the substrate 22's own CI gate scans). — **Evidence (2026-08-02):** the golden-value half is `native-registry.test.ts:48-57`, set equality between the live registry's whole `toolNames` and an enumerated 18-name list, so both prefixes are excluded by exhaustion; the prefix scan over the live registry is `:60-73`, non-vacuous because the same registry is proven to hold 18 names. The grep half is 22's own gate, `packages/learning/src/red-team/no-mcp-tool-family.redteam.test.ts:47-82`, scanning `packages/gateway/src` from outside with its own non-vacuity guard, wired as a named step of `.github/workflows/learning-redteam.yml:44-45` and executing at this commit (run 30743626860, job-log line 203). That gate greps `learning.` only; the `change_set.` static scan has no standing check and was run for this closeout — `docs/evidence/phase-16/closeout-c7-c8-whole-package-static-scans.txt`, production sources `exit=1`.
- [x] Every SDK server registration and wire-level tool name references `GATEWAY_MCP_SERVER_NAME` — zero hand-typed `"crabgic_gateway"` literals anywhere in `packages/gateway` (02's repo-wide grep gate). — **Evidence (2026-08-02):** the registration claim is carried by the name read back off a real MCP handshake, `mcp/stdio-boot.test.ts:76-82`, against the single SDK construction site `mcp/server.ts:31-33`; the other two constant-consuming sites are `upstream-mcp-client-policy.ts:54` and `uds-forward-client.ts:78`. 02's repo-wide gate `packages/contracts/src/gateway/server-name.test.ts:73-80` allowlists exactly two contracts paths, so this package carries no exemption, and this package's own scanner `mcp/gateway-name-reference.test.ts:38-58` never spells the literal it searches for. The criterion's "anywhere in `packages/gateway`" clause exceeds both scanners' `src/**` globs, so this closeout grepped every one of the package's 78 tracked files — `exit=1`, no match (`docs/evidence/phase-16/closeout-c7-c8-whole-package-static-scans.txt`). The known scan-scope gap in that gate concerns `packages/plugin/.mcp.json` and is already filed as `docs/evidence/criteria-closeout/defects/02-gateway-literal-scan-scope.md`; it does not reach this package. CI job-log lines 434, 617, 624.
- [x] Provider-dispatch point resolves to the correct provider-specific client for a fixture `ExternalConnection` per registered provider; an unrecognized provider value is rejected before any network call. — **Evidence (2026-08-02):** two providers registered simultaneously and each resolved by reference identity, `provider-registry.test.ts:13-21`; unrecognized value rejected with a typed `UnknownProviderError` against a non-empty registry `:24-28`; duplicate registration refused `:31-35`. "Before any network call" is structural: `provider-registry.ts:55-58` is a pure Map lookup with no I/O, and in `native-tools/provider-dispatch-tool.ts` the provider client is not even looked up (`:83`) or invoked (`:95`) until `resolve` has returned at `:69` — an unknown provider returns at `:71-79`. Through the real MCP tool against a fixture `ExternalConnection`: rejection `native-registry.test.ts:183-203`, unsupported-operation edge `:206-229`, and the positive control that a registered client's own payload comes back `:75-104`. CI job-log lines 593, 641. Transcript `docs/evidence/phase-16/closeout-c6-c11-mcp-dispatch-doctor.txt`.
- [x] Connection-doctor reachability probe (incl. custom-CA validation) succeeds against a disposable fixture connection and fails informatively against an unreachable one, exercised directly against this package, ahead of 18/19/20's own doctor-check wiring. — **Evidence (2026-08-02):** `connection-doctor/reachability-probe.test.ts:103-114` succeeds against a real `node:https` listener with a freshly generated self-signed cert and a 0600 CA file, against its no-CA negative control `:117-124` that differs in nothing else. Unreachable target `:127-139`. "Informatively" proven as discrimination, not as a non-empty string — secret-resolution `:147`, custom-CA-read `:171`, SSRF refusal `:154` and reachable-but-5xx `:201-202` each produce a distinct result an operator can act on. The default (non-injected) client builder is exercised at `:150-165`. CI job-log line 597 (8 tests, 536ms of real TLS). Transcript `docs/evidence/phase-16/closeout-c6-c11-mcp-dispatch-doctor.txt`.
- [x] When the optional upstream-MCP-client wrap is enabled for a fixture connection, no additional MCP server ever appears in a simulated worker's `mcpServers` config — only this phase's own native tools are worker-visible. — **Evidence (2026-08-02):** the criterion's own sentence executed at `mcp/upstream-mcp-client-policy.test.ts:31-40` — the flag is set for a fixture connection, the resulting key set is unchanged and is exactly `[GATEWAY_MCP_SERVER_NAME]`, and `store.isEnabled(...)` is asserted `true` so the antecedent genuinely holds. Reconciled with what merged in `bddac4c`: the containment is not the disabled default but structural unenableability, pinned as five numbered facts by `mcp/upstream-mcp-client-unenableable.test.ts` — zero-arity worker-config builder `:149-158`, no MCP client and no enablement call in any production source `:114-119`, `.strict()` schema rejecting three plausible flag names against a passing control `:122-141` — with the scanner's own failing-first proof against seeded violations `:96-112` and its non-vacuity floor `:91-93`. The real worker builder emits the same single key (`packages/engine-core/src/compiler/worker-settings.ts:55`). See §Risks' PRECONDITION RECORDED 2026-08-01. CI job-log lines 599, 644. Transcript `docs/evidence/phase-16/closeout-c6-c11-mcp-dispatch-doctor.txt`.
- [x] Security review sign-off recorded against `docs/threat-model.md`. — **Evidence (2026-08-06):** the record now exists in the file the criterion names. `docs/threat-model.md:356` opens `### Implementation re-review — gateway (§5), recorded 2026-08-06`, whose `:365` reads "Sign-off status for §5 (Gateway): reviewed, twice, and recorded — the records were simply not in this" and `:366` "file until now." It cross-references the two pre-existing sign-offs rather than inventing a reviewer: `docs/security-posture.md:33` (`## Sign-off`, whose `:35` states "**No unresolved CRITICAL or HIGH security finding blocks this release.**") with `:151` `### 5. Gateway`, and `docs/evidence/phase-16/README.md:87` §Security review with its explicit `**Sign-off:**` paragraph at `:134` — the same session that recorded at `:136` that editing the threat model was "**Not done in this session:** editing `docs/threat-model.md` itself". That carry-forward is what this entry lands. Two §5 corrections land with it, made **in place and line-count-neutral**: the Spoofing residual `docs/threat-model.md:163` and the information-disclosure residual `docs/threat-model.md:166` each keep their original "flag-gated" sentence byte-identical and gain a dated correction naming the structural unenableability `bddac4c` established, pinned by `packages/gateway/src/mcp/upstream-mcp-client-unenableable.test.ts:115` (`expect(scanProductionSources(ENABLEMENT_PATTERN)).toEqual([]);`) and `:154` (`expect(buildSimulatedWorkerMcpServers.length).toBe(0);`); Open item 3 at `docs/threat-model.md:338` is restated as still open but a **recorded precondition**, per §Risks' PRECONDITION RECORDED 2026-08-01 bullet. The stale `docs/threat-model.md:27` "unimplemented" list is corrected at `docs/threat-model.md:34`, stamping **only 16**, because only 16 was counted. One edit beyond the defect's four steps, disclosed rather than smuggled: Open item 7 at `docs/threat-model.md:352` gains a dated partial-discharge note. **No line shifts:** merged phase-02 evidence pins this document's nine section headings at `docs/threat-model.md:70`, `:91`, `:113`, `:133`, `:153`, `:172`, `:191`, `:211` and `:230`, and all nine still resolve — measured in the transcript, not asserted. Transcript `docs/evidence/phase-16/closeout-c12-signoff-landed.txt`. What is recorded is the two pre-existing reviews plus the dated corrections — **not** a fresh review.

## Risks & open questions

- **`Unlocks` resolved as `18, 20` (unchanged) — 09 deliberately not added:** 09's own file states plainly that "16 has no dependency edge to this phase in either direction in the README graph," which matches the README's mermaid graph (`P16 --> P18 & P20` only; `P05 --> P06 & P09 & P16` — 09 and 16 are siblings under 05, not sequential). 09 (`Depends on: 05` only) lands first and ships `gateway mcp` as a real CLI/argv entry point tested against a stub/fake tool registry (09's own exit criterion: it "lists exactly the resolved tool set... to a stub MCP client"); this phase, landing later, is what turns that stub into a real server. 09 itself flags the composition mechanism as unresolved ("whoever lands 16, or 23's final wiring pass, must settle the actual composition point"); this phase settles it by exporting its server/registry as a plain importable module that 09's argv shim imports at its own entry point — an ordinary npm-workspace import inside the finished monorepo, not a new phase-sequencing dependency, since 09's exit criteria were already satisfied earlier against a stub and the real import only needs to exist by 23's full-registry release gate. Adding 09 to `Unlocks` would fabricate a graph edge neither file claims.
- **06's worker-wiring question, confirmed from this side:** 06 flags that its workers reach the gateway only as an external MCP client of the `gateway mcp` process (never an in-process import of `packages/gateway`) and asks this phase to confirm the registry is reachable that way "for worker connections, not solely the manager's `.mcp.json` connection." Confirmed here: the `crabgic_gateway` server this phase hosts is one stdio process, addressed identically by the manager's `.mcp.json` entry (10) and by every worker's `WorkerSdkOptions.mcpServers` entry (06) — one registry, two categories of client, never a separate in-process surface.
- **13's artifact-store question, left open from this side too:** 13 flags that the relationship between its own supervisor-side Artifact store and this phase's `evidence.attach`/`result.submit` tools is unspecified — plausibly one store with two write paths (supervisor-direct, worker-via-MCP-tool), plausibly two stores with a sync step. 13 is outside this phase's Depends-on just as this phase is outside 13's, so it stays flagged for the reconciler rather than silently assumed either way; this phase's own exit criteria don't require an answer — `evidence.attach`/`result.submit` only need a durable destination for a worker-submitted reference, which 04's journal already supplies.
- **Verify-at-build-time:** this phase deliberately never depends on `MAX_MCP_OUTPUT_TOKENS` (unconfirmed per adaptation §10 item 10; §7 point 2 names the gateway explicitly as where result-size budgets belong instead) — the 32 KiB/256 KiB budgets are enforced independently of any engine-side MCP output limit, the same self-sufficiency 19 and 20 both cite when explaining why they need no additional spike of their own.
- With 03, this is a security keystone — full security review + threat-model update required before this phase closes. **Resolved:** 23's text now consistently phrases the security-keystone set as "03/16 security keystones + 17's blocking-lint surface" (17 is the lint surface under review, not a third keystone) across its In-scope/Work-items/Exit-criteria prose. No further reconciler action needed.
- **"8-family" registry-completeness count (02, 09, 11, 23) — corrected from an earlier "9-family" miscount:** Gap 1's ruling counts by top-level dot-prefix, applied uniformly: `tracker.*`, `observability.*`, `evidence.*` (`evidence.get`+`evidence.attach` group into one family, exactly as `run.status`+`run.cancel` group into one `run.*` family and `capability.audit`+`capability.approve` into one `capability.*` family), `result.submit`, `run.*` (forwarded), `project.inspect`, `contract.approve` (11), and `capability.*` (12) — **eight** families total (`docs/interface-ledger.md`, Gap 1). An earlier draft here special-cased `evidence.*` into two families to reach nine; that hybrid is arithmetically inconsistent with the same prefix-grouping rule this file applies to `run.*` and `capability.*` (a consistent leaf-count would instead give eleven), and it is retired. This is not adaptation §5.5's pre-resolution 9-item list (`project.inspect, contract.*, capability.*, run.*, change_set.*, evidence.get, learning.*, tracker.*, observability.*`), which still carried the since-deleted `change_set.*`/`learning.*` families; counted the ledger's way the surface is eight, and 09/11/23's release-gate phrasing is corrected to match.
- **Naming clarification, not a new mechanism:** the corpus already distinguishes an MCP tool-*family* registry (Gap 1's resolution, hosted here, extended by 11/12) from a provider-*dispatch* point inside `tracker.*`/`observability.*` (20 names "16's provider-dispatch point" when describing `GrafanaProviderAdapter`'s registration). This file is the first to name and place both side by side; nothing new is introduced beyond making an existing two-tier extension design explicit.
- **Open: does the optional upstream-MCP-client wrap need 12's quarantine?** 12's own scope says every MCP server reaching "a callable gateway tool" passes its fetch → pin → scan → sandboxed-test → human-approval pipeline first. If a deployment enables the wrapped-Atlassian/Grafana-MCP flag (In scope, above), whether that upstream server counts as a quarantine-worthy capability — as opposed to gateway-internal transport plumbing already outside 12's remit, like the REST path — is addressed by neither file. Not resolved here: 16 and 12 share no dependency edge to settle it directly, and this phase's own exit criteria don't exercise the flag enabled against a real upstream MCP server (only against REST-path fakes); flagged for the reconciler alongside 12's own already-flagged capability-quarantine STRIDE gap.
  - **PRECONDITION RECORDED 2026-08-01 (settled on paper while it is free).** The question stays open, but it is no longer allowed to *stay* open past the moment it matters. Answering it is a **precondition on any work that makes the wrap enableable** — not a parallel task, and not something the enabling PR may defer to a follow-up. Concretely: before an upstream MCP client, an `ExternalConnection` field for the flag, or any production caller of `UpstreamMcpClientPolicyStore.setEnabled` lands, this bullet must be replaced with a ruling that says either (a) an upstream provider MCP server is a quarantine-worthy capability and MUST pass 12's fetch → pin → scan → sandboxed-test → human-approval pipeline before any tool it exposes is reachable, or (b) it is gateway-internal transport plumbing outside 12's remit, with the reasoning that distinguishes it from the REST path spelled out. Recording the precondition costs nothing today because nothing can enable the wrap: the disabled default is not what contains it — there is no MCP client in shipped source, `ExternalConnectionSchema` is `.strict()` with no field for the flag (so a config declaring one is rejected, not ignored), no environment variable reaches it, and `buildSimulatedWorkerMcpServers()` takes no policy argument. All five facts are pinned by `packages/gateway/src/mcp/upstream-mcp-client-unenableable.test.ts`, so the enabling change fails CI here first and this bullet is what the author is sent to. See also `docs/security-posture.md`'s residual entry and `docs/threat-model.md` §5/§6.
