# Phase 01 — Monorepo bootstrap, toolchain & CI

| | |
|---|---|
| **Depends on** | — (none; runs in parallel with phase 00 — no shared artifact either direction) |
| **Unlocks** | 02 (direct — README's dependency graph draws only `P01 --> P02`); transitively, every phase through 23 via 02. 23's own header additionally lists 01 among its direct "Depends on" set (all phases) even though the simplified mermaid graph only draws 23's inbound edges from 15/19/21/22 — 23 re-verifies this phase's CI/workspace/licensing artifacts directly (see Interfaces produced). |
| **Sources** | original plan (monorepo layout, strict TS, Node 24 LTS, Apache-2.0 licensing, npm package name `crabgic`); adaptation §0 (confirmed decision row 1: published OSS product from v1 — Apache-2.0 npm package + plugin marketplace, full installer/doctor/upgrade/uninstall polish); §1 Verdict (this phase sits entirely within the ~70–75% engine-agnostic carryover — no Claude Code engine fact is asserted here; see Risks) |
| **Primary package** | none — this phase scaffolds the workspace root and all 18 packages (Gap 3) as empty stubs; no phase-01 business logic lives inside any single package |

## Goal

Before this phase, the repository has no git history and no code — only `docs/` and `roadmap/`. When this phase is done, a git repository exists with a green CI pipeline enforcing lint, typecheck, an 80%-line+branch coverage gate, and conventional-commit hygiene, over an 18-package (Gap 3) strict-TypeScript npm workspace in which every package compiles empty; the Apache-2.0 licensing, contribution, and Changesets scaffolding a public npm package requires is in place; and the `crabgic` npm name's availability is recorded. Every later phase adds real code inside an already-disciplined, already-enforced repository instead of retrofitting toolchain rigor after the fact.

## In scope

- npm workspaces layout — empty packages (`package.json` + `tsconfig.json` only, no source logic) for all **18** workspace members (Gap 3): `packages/contracts`, `packages/testkit`, `packages/engine-core`, `packages/journal`, `packages/supervisor`, `packages/engine-claude`, `packages/git-engine`, `packages/gateway`, `packages/renderer`, `packages/connectors-jira`, `packages/connectors-grafana`, `packages/detect`, `packages/scheduler`, `packages/gates`, `packages/perf`, `packages/learning`, `packages/cli`, `packages/plugin`. `packages/engine-core` is scaffolded empty here exactly like every other package; phase 03 implements it, it does not originate it (Gap 3).
- Root `tsconfig.base.json` (strict, `NodeNext`, `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`), per-package `tsconfig.json` with project references, root + per-package `engines: { node: ">=24" }`.
- Vitest workspace config + `@vitest/coverage-v8`; **80% line+branch coverage gate enforced in CI** (greenfield rule, README ground rules).
- ESLint + Prettier configs; commitlint with conventional-commit types `feat|fix|refactor|docs|test|chore|perf|ci`; lint-staged optional pre-commit wiring.
- Apache-2.0 `LICENSE`, `NOTICE`, `SECURITY.md` (private disclosure route), `CONTRIBUTING.md` (states the phase workflow: failing tests first, review, exit-criteria evidence), `CODE_OF_CONDUCT.md`, issue/PR templates.
- Changesets skeleton (config + version/publish scripts, empty `.changeset/`) for versioning and `CHANGELOG.md` generation; npm publish config (`access: public`; provenance flag noted, exercised at phase 23).
- CI (GitHub Actions): `lint`, `typecheck`, unit-test+coverage-gate jobs on Linux x86-64 + ARM64; a manually-triggered `engine-live` job placeholder that phase 06 wires to run the `@live`-tagged conformance suite (needs a host with `claude`) (Gap 15).
- **Engine-version pinning policy** (adaptation §0, Worker transport decision: "also pins a reproducible bundled engine version per release"): `@anthropic-ai/claude-agent-sdk` — and any future engine-bundling dependency — is declared with an exact version, never a `^`/`~` range; `package-lock.json` is committed; an `engine-pin-lint` CI check fails on any range specifier for the engine dependency. Policy and check are active from bootstrap; the first real target lands when 06 adds the SDK dependency. This build-time exact pin (which SDK version is bundled) is complementary to, not competing with, 06's runtime version-range gate (which host engine versions may run, per `docs/engine-baseline.md`'s accepted range).
- `docs/release-notes-prep.md` recording the `npm view crabgic` package-name availability result (re-checked verbatim at phase 23's publication step).

## Out of scope

- Domain logic inside any of the 18 scaffolded packages — each owning phase implements its own (e.g., phase 02 implements `packages/contracts`; phase 03 implements `packages/engine-core`).
- `renderer-core` — a module living inside `packages/contracts`, not a 19th workspace package (Gap 3); its logic (length/line counters, attribution-token scanner) is phase 02 work item 6's job, not this phase's.
- Engine verification spikes, permission-rule probes, and `docs/engine-baseline.md` — owned by phase 00, which runs in parallel with no shared artifact either direction; this phase asserts zero Claude Code engine facts (see Risks).
- Plugin/marketplace packaging format, `.mcp.json`, `.claude/*` scaffolding, `CLAUDE.md` — owned by phase 10.
- Progressive coverage-ratchet logic and seeded-fault quality/security gate matrices — owned by phase 14 (`packages/gates`), built on top of the flat 80% CI threshold this phase establishes.
- Release automation beyond the Changesets skeleton, npm provenance execution, and the real `v1.0.0` publish — owned by phase 23, which also re-checks the npm name recorded here and closes any ARM64 deferral from this phase.
- Threat modeling and the first enforced security review gate — owned by phases 02 and 14/23 respectively.

## Interfaces produced

The workspace root and toolchain every later phase (starting with 02) builds inside. Every name below is the literal, stable identifier downstream phases reference by name.

- **18-package npm workspace** (Gap 3), each an empty compiling stub (`package.json` + `tsconfig.json`, project references, no source logic): `packages/contracts`, `packages/testkit`, `packages/engine-core`, `packages/journal`, `packages/supervisor`, `packages/engine-claude`, `packages/git-engine`, `packages/gateway`, `packages/renderer`, `packages/connectors-jira`, `packages/connectors-grafana`, `packages/detect`, `packages/scheduler`, `packages/gates`, `packages/perf`, `packages/learning`, `packages/cli`, `packages/plugin`. Every later phase's own "Primary package" header field names one of these paths byte-for-byte — none may be renamed without a coordinated cross-phase edit.
- **`tsconfig.base.json`** (root strict config: `NodeNext`, `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`) — every package's `tsconfig.json` extends it; structurally consumed by every phase that writes TypeScript (all of them).
- **Vitest workspace config + `@vitest/coverage-v8`**, 80%-line+branch CI gate — the test/coverage harness every phase's own test suite runs inside.
- **ESLint/Prettier/commitlint configs** (conventional-commit types `feat|fix|refactor|docs|test|chore|perf|ci`) — enforced on every later phase's commits.
- **CI skeleton** (GitHub Actions): `lint`, `typecheck`, unit-test+coverage-gate jobs on Linux x86-64 + ARM64, plus a manually-triggered **`engine-live`** job placeholder — the exact job phase 06 wires to run the `@live`-tagged conformance suite (Gap 15); inert here, no engine invoked. The ARM64 leg additionally uploads an **`arm64-run-record`** artifact, which is the one thing this skeleton produces for a later phase to *read*: 23 consumes it at `docs/evidence/phase-23/arm64-run-record.json`, or — the path CI actually takes, since committing it would advance `HEAD` past the object ID it names — through `$CRABGIC_ARM64_RUN_RECORD`, an absolute path to the downloaded record; that path convention and `CRABGIC_<SUBJECT>` override naming are `docs/interface-ledger.md`'s Gap 16, binding on this phase as a producer.
- **`LICENSE`** (Apache-2.0) and **`NOTICE`** — consumed by phase 23's reproducible-build/provenance/publish step. **`SECURITY.md`**, **`CONTRIBUTING.md`**, **`CODE_OF_CONDUCT.md`**, issue/PR templates — repo-hygiene artifacts for human contributors; no phase consumes these programmatically.
- **Changesets skeleton** (`.changeset/config.json` + version/publish scripts) — the tool phase 23 executes to produce the real `v1.0.0` `CHANGELOG.md` entry.
- **`docs/release-notes-prep.md`** — the recorded `npm view crabgic` name-availability verdict; phase 23 re-checks the same name against this record at publication.
- The initialized git repository itself (first commit) — the substrate every subsequent phase commits into. Not a schema/API-style interface; stated for completeness.

## Interfaces consumed

None. This phase's own header states **Depends on: —**. Per the README's dependency graph, `P01 --> P02` is the only edge touching phase 01, and it is outbound: phase 01 is the repository's origin point. It reads only the adaptation doc and the original plan — no other phase's schema, contract, tool name, or artifact. Phase 00's engine spikes run in parallel and produce no artifact this phase needs, and this phase produces no artifact phase 00 needs — the two are mutually independent.

## Work items

1. Initialize the git repository; author root `package.json` (npm `workspaces` glob spanning all 18 package paths, Gap 3's `packages/engine-core` included; `engines.node: ">=24"`), `.gitignore`, `.npmrc` (`access=public`) — failing-test-first: a smoke script asserting the workspace enumeration (`npm ls --workspaces --json` or equivalent) returns exactly 18 entries is written first and fails (zero workspaces declared) before the manifests exist.
2. Author `tsconfig.base.json` (strict, `NodeNext`, `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`) and, per package, a stub `package.json` + `tsconfig.json` (project references) + empty `src/index.ts`, for all 18 packages — failing-test-first: a deliberately circular two-package reference fixture is committed first and fails `tsc -b` with a cycle error; the real, acyclic reference graph replaces it and builds clean, both cold and incremental.
3. Wire Vitest workspaces + `@vitest/coverage-v8`, with an 80% line+branch threshold that fails the run below it — failing-test-first: a temporary fixture package exercised at 60% coverage is committed and the coverage step fails in CI; removing the fixture (or raising coverage) restores green.
4. Author ESLint/Prettier configs and commitlint (types `feat|fix|refactor|docs|test|chore|perf|ci`) + GitHub Actions workflows (`lint`, `typecheck`, unit-test+coverage-gate jobs on Linux x86-64 and ARM64; a manually-triggered `engine-live` placeholder job that phase 06 wires to run the `@live`-tagged conformance suite, Gap 15) — failing-test-first: a fixture commit message with no type prefix is asserted rejected by commitlint; the assertion itself fails (nothing rejects it yet) until the commitlint config lands.
5. Author `LICENSE` (Apache-2.0), `NOTICE`, `SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, issue/PR templates, and the Changesets skeleton (`.changeset/config.json` + version/publish scripts) — failing-test-first: a repo-hygiene check asserting all required top-level files exist and are non-empty is written first and fails against the empty repo, then passes.
6. Run `npm view crabgic` and record the availability verdict, timestamped, in `docs/release-notes-prep.md` — failing-test-first: a check asserting the doc exists with a recorded verdict fails before the doc is written.
7. `engine-pin-lint` CI check (adaptation §0's reproducible-bundled-engine decision): fails on any `^`/`~`/range specifier for `@anthropic-ai/claude-agent-sdk` (or any declared engine-bundling dependency) in any workspace `package.json` — failing-test-first: a fixture manifest declaring `"@anthropic-ai/claude-agent-sdk": "^2.1.0"` is asserted rejected, and the assertion fails (no check exists yet) until the check lands; the check then passes green on the real workspace, which declares no engine dependency at this phase (06 adds the first real target).

## Test plan

- **Unit** (meta-tests — this phase has no runtime business logic, so its "units" are the toolchain's own enforcement points): coverage-gate failure fixture (temporary 60%-covered package must fail CI; work item 3); commitlint fixture (a commit message with no type prefix must be rejected; work item 4); `tsc -b` reference-cycle canary (a deliberately circular two-package reference must fail cold build; work item 2); repo-hygiene check (required top-level files present and non-empty; work item 5); release-notes-prep presence check (work item 6); `engine-pin-lint` fixture (a range-specified `@anthropic-ai/claude-agent-sdk` manifest must be rejected; work item 7).
- **Property:** not applicable — this phase contains no algorithmic logic to fuzz. The first property-tested surfaces are phase 02's config-precedence resolver and canonical-error redaction.
- **Integration:** `npm ci && npm run build && npm test` against a clean checkout, executed in CI on both Linux x86-64 and ARM64 runners.
- **Conformance:** all 18 packages (Gap 3) independently pass `tsc -b`, cold and incremental, with zero circular project references; workspace enumeration returns exactly 18 entries.
- **Security:** not applicable at this phase — no runtime code, network access, or credential handling exists yet to attack; `SECURITY.md`'s disclosure route and `LICENSE`/`NOTICE` correctness are covered by the Unit repo-hygiene check above, not a separate security test. `docs/threat-model.md` is authored in phase 02; the first enforced security gate is phase 14, re-verified live at phase 23.

## Exit criteria

**CLOSED 2026-08-01, evidenced.** All ten criteria walked individually against recorded
evidence (criteria-closeout pass; machine-readable index:
`docs/evidence/criteria-closeout/phase-01.json`). Three boxes carry a dated wording
correction, all of the same one cause: this phase ran on 2026-07-15 against a repository
with **no git remote at all**, a deviation `docs/evidence/phase-01/README.md` recorded at the
time, so the three boxes whose named channel is a *failing* CI run are evidenced by verbatim
recorded local runs — the phase's originals plus fresh re-captures at this commit — rather
than by GitHub Actions runs. No check was changed and no criterion text was rewritten.

Shared citation reused by most boxes below. **`CI` run
[30718972881](https://github.com/WitchyNibbles/crabgic/actions/runs/30718972881)**, green at
`65ff0da` on all nine jobs: `lint`, `typecheck` and `unit-test+coverage` each on **both**
`ubuntu-latest` and `ubuntu-24.04-arm`, plus `commitlint`, `engine-pin-lint` and
`meta-checks`. Closeout transcripts captured verbatim at that commit are committed as
`docs/evidence/phase-01/closeout-c<k>-*.txt`; the phase's original 2026-07-15 `wi<N>-*.txt`
transcripts are untouched. No `engine-live` run and no `@live` suite was triggered by this
pass.

- [x] `npm ci && npm run build && npm test` exits 0 on a clean checkout, and the CI `lint` and `typecheck` jobs are separately green — evidenced by a passing required CI run, on Linux x86-64. — **Evidence (2026-08-01):** `CI` run 30718972881 green at `65ff0da`. Job `unit-test+coverage (ubuntu-latest)` ([91419417946](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417946)) runs exactly those three commands, in that order, against a fresh `actions/checkout@v4` — steps "Run npm ci", "Run npm run build", "test with 80% line+branch coverage gate" — finishing `Test Files 623 passed (623)` / `Tests 6060 passed | 1 skipped (6061)`. `lint (ubuntu-latest)` ([91419417929](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417929), steps `npm run lint` + `npm run format`) and `typecheck (ubuntu-latest)` ([91419417975](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417975)) are separate jobs, each green. Scope note recorded rather than glossed: `main` carries **no GitHub branch-protection rule** (`gh api repos/WitchyNibbles/crabgic/branches/main/protection` → 404 "Branch not protected"), so "required" is not enforced as a merge gate today; `CI` is nonetheless triggered automatically on every push to `main` and every pull request, and the run cited is the automatic push-triggered one at head.
- [x] The same (build/test/lint/typecheck) is green on ARM64, or an explicit deferral is recorded in this file's Risks section and closed by phase 23's real-hardware (or documented-substitute) verification. — **Evidence (2026-08-01):** the **first** disjunct, directly — `CI` run 30718972881 ran the whole matrix natively on `ubuntu-24.04-arm` at `65ff0da`: `unit-test+coverage (ubuntu-24.04-arm)` ([91419417937](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417937), `npm ci` → `npm run build` → `npm test`, `Test Files 623 passed (623)` / `Tests 6060 passed | 1 skipped (6061)`), `typecheck (ubuntu-24.04-arm)` ([91419417938](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417938)) and `lint (ubuntu-24.04-arm)` ([91419417951](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417951)). The hardware is observed, not assumed: that job's own `arm64-run-record.json` step emitted `"arch": "aarch64"`, `"kernel": "Linux 6.17.0-1020-azure"`, `"commitSha": "65ff0da0fec2c6c3f3526758ee765eeb8cbe6d6e"`, `"conclusion": "success"` (job log lines 1912–1919), with `conclusion` interpolated from `${{ job.status }}` under `if: always()` so a red leg would say so. The second disjunct is independently discharged by phase 23's ticked box "ARM64 build+test verified on real hardware/CI, or an explicitly documented substitute recorded — closes 01's deferred ARM64 gate", `release-e2e` run [30250453824](https://github.com/WitchyNibbles/crabgic/actions/runs/30250453824) at RC `2435cb9`.
- [x] All 18 packages (Gap 3) — the 17 originally enumerated plus `packages/engine-core` — compile empty via `tsc -b`, cold and incremental, zero circular project references — evidenced by the CI typecheck job log. — **Evidence (2026-08-01):** the named channel at head — `CI` run 30718972881 job `typecheck (ubuntu-latest)` ([91419417975](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417975)), whose two steps are literally "tsc -b (cold)" and "tsc -b (incremental, no-op expected)", both green, and the identical pair on `typecheck (ubuntu-24.04-arm)` ([91419417938](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417938)). `tsc -b` *is* the circular-reference detector (TS6202): `docs/evidence/phase-01/wi2-tsc-cycle-failing.txt` records it firing on a deliberately circular two-package fixture, and `docs/evidence/phase-01/closeout-c3-tsc-18-packages.txt` adds the manifest-level guard `check-package-graph-acyclic: PASS — 18 workspace packages, dependency graph is a DAG` plus `check-workspace-count: PASS — exactly 18 workspaces declared` (also a green `meta-checks` step in the same run). `wi2-tsc-cold-build-passing.txt` / `wi2-tsc-incremental-build-passing.txt` enumerate all 18 project paths for the bootstrap state. The word *empty* is the one clause CI cannot re-evidence, since the stubs stopped being empty in phase 02, so it is checked against git rather than asserted: every one of the 18 `packages/*/src/index.ts` at the bootstrap commit `e6aa458` is the single line `export {};` (same closeout transcript).
- [x] Coverage gate fails CI against the work-item-3 fixture below 80% line+branch, and passes once corrected — evidenced by the recorded before/after CI run. — **Evidence (2026-08-01):** *passes-once-corrected*, in CI — `CI` run 30718972881 job `unit-test+coverage (ubuntu-latest)` ([91419417946](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417946)) step "test with 80% line+branch coverage gate", green at `65ff0da`, measuring `All files | 94.54 | 87.38 | 96.05 | 95.48` (stmts/branch/funcs/lines) over 623 test files. *Fails-below-80%* — `docs/evidence/phase-01/wi3-coverage-gate-failing.txt`, the original work-item-3 fixture at 53.33% lines / 50% branches with `ERROR: Coverage for lines (53.33%) does not meet global threshold (80%)` and `exit=1`; re-captured at this commit in `docs/evidence/phase-01/closeout-c4-coverage-gate.txt`, which reproduces all four `ERROR: Coverage for <metric> … does not meet global threshold (80%)` lines out of today's unchanged `vitest.config.ts:138-143` (`lines/branches/functions/statements: 80`).
      **Wording corrected 2026-08-01.** The clause reads "evidenced by the recorded before/after CI run". The *after* half is exactly that. The *before* half never was and now cannot be: this phase ran before the repository had any git remote (`docs/evidence/phase-01/README.md`, "Deviations from a literal CI-evidenced phase"), and the work-item-3 fixture package was deleted as soon as its transcript was captured — reinstating it would break the `meta-checks` workspace-count step, which requires exactly 18 packages. The corrected claim is: the red half is a **verbatim recorded local run** of the same gate, captured twice (2026-07-15 against the fixture, and again at this commit against the live thresholds), not a GitHub Actions run. What is weaker is the channel of the red half; the guarantee — the configured 80% line+branch gate fails the run below threshold, and the run that CI executes on every push is above it — is fully evidenced. The gate is unchanged; the claim now matches what was recorded.
- [x] Commitlint rejects the work-item-4 malformed-message fixture in CI — evidenced by the recorded failing CI run. — **Evidence (2026-08-01):** `docs/evidence/phase-01/closeout-c5-commitlint-fixture.txt`, captured at this commit against today's `commitlint.config.js`, reproduces `wi4-commitlint-rejecting-passing.txt` case for case: the malformed fixture → `✖ subject may not be empty [subject-empty]` / `✖ type may not be empty [type-empty]`, `exit=1`; `build: bump deps` → `✖ type must be one of [feat, fix, refactor, docs, test, chore, perf, ci] [type-enum]`, `exit=1` (the narrowed enum this phase specifies, not stock config-conventional); and the control `feat: add commitlint config` → `exit=0`, so the rejections are not a check that rejects everything. The job that carries this into CI is `commitlint` ([91419417974](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417974)) in `CI` run 30718972881, green at `65ff0da`, whose step "lint pushed commit" ran `npx commitlint --from 70d7da7… --to 65ff0da… --verbose` → `✔ found 0 problems, 0 warnings`.
      **Wording corrected 2026-08-01.** The clause reads "in CI — evidenced by the recorded failing CI run". No GitHub Actions run has ever recorded commitlint *rejecting* a message, and none realistically can on this repository: the `commitlint` job lints the commits actually pushed, and a red run would mean a malformed commit had reached `main`. The phase's original evidence was captured before any remote existed at all. The corrected claim is: the rejection is a **verbatim recorded local run** of the repository's own `commitlint.config.js`, captured twice, and the CI job that enforces the same config on every push and pull request is green at head. What is weaker is the channel of the red half, not the guarantee that the malformed fixture is rejected. The config and the job are unchanged.
- [x] `LICENSE` (Apache-2.0), `NOTICE`, `SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, and issue/PR templates exist at repo root, non-empty — evidenced by the work-item-5 repo-hygiene check passing in CI. — **Evidence (2026-08-01):** `CI` run 30718972881 job `meta-checks (workspace-count, repo-hygiene, release-notes, criteria-closeout)` ([91419417950](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417950)), step "repo-hygiene (required top-level files present, non-empty)", green at `65ff0da`; the step log enumerates every file with its byte count — `LICENSE (11357 bytes)`, `NOTICE (767)`, `SECURITY.md (2338)`, `CONTRIBUTING.md (3424)`, `CODE_OF_CONDUCT.md (2952)`, `README.md (15636)`, `.github/PULL_REQUEST_TEMPLATE.md (448)`, `.github/ISSUE_TEMPLATE/bug_report.md (454)`, `.github/ISSUE_TEMPLATE/feature_request.md (431)` — and ends `check-repo-hygiene: PASS`. The check is not a bare existence glob: `scripts/check-repo-hygiene.mjs:11-20` is the required roster, `:35-38` rejects a non-file, `:40-43` rejects `stat.size === 0`. `docs/evidence/phase-01/wi5-repo-hygiene-failing.txt` is the negative control (`MISSING — NOTICE/SECURITY.md/CONTRIBUTING.md/CODE_OF_CONDUCT.md`, `exit=1`). The Apache-2.0 clause is the one thing the check does not itself assert; `LICENSE:1-3` carries the verbatim `Apache License` / `Version 2.0, January 2004` / `http://www.apache.org/licenses/` preamble.
- [x] Changesets skeleton produces a valid (if empty) `CHANGELOG.md` entry from a trial `changeset add` + `changeset version` dry run — evidenced by the committed dry-run output. — **Evidence (2026-08-01):** the named channel — `docs/evidence/phase-01/wi5-changesets-trial.txt`, the committed transcript of `changeset add --empty` → `changeset status --verbose` → `changeset version`, carrying the generated `packages/contracts/CHANGELOG.md` ("## 0.0.1 / ### Patch Changes") and the `"version": "0.0.0"` → `"0.0.1"` diff, then reverted; its own footer records why this is the dry-run equivalent (the changesets CLI has no `version --dry-run`). `docs/evidence/phase-01/closeout-c7-changesets-live.txt`, captured at this commit, shows the skeleton is live rather than a one-off: `changeset status --verbose` resolves three pending entries to `crabgic 1.6.0`, and `packages/cli/CHANGELOG.md` holds the real generated release notes phase 23 produced with the same two commands.
- [x] `docs/release-notes-prep.md` records a timestamped `npm view crabgic` name-availability verdict — evidenced by the committed doc. — **Evidence (2026-08-01):** `docs/release-notes-prep.md` §"npm package name availability — `crabgic`" — "**Verdict: available (unclaimed) as of 2026-07-26T18:11:17Z.**", followed by the verbatim `npm view crabgic` → `npm error code E404` transcript, and by the note that the verdict was **re-probed** rather than search-and-replaced when the project was renamed from `engineering-orchestrator` (the superseded 2026-07-15 verdict is preserved in git history). Enforced in CI: `CI` run 30718972881 job `meta-checks` ([91419417950](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417950)) step "release-notes-prep records the npm-name verdict" → `check-release-notes: PASS — docs/release-notes-prep.md records a timestamped npm-name verdict.` That check asserts all three parts separately (`scripts/check-release-notes.mjs:28-30`: the package name, an ISO-8601 UTC timestamp, and an `available|taken` verdict word), and `docs/evidence/phase-01/wi6-release-notes-failing.txt` is its negative control (`MISSING`, `exit=1`).
- [x] `engine-pin-lint` CI check rejects the work-item-7 fixture manifest declaring a `^`-ranged `@anthropic-ai/claude-agent-sdk` and passes on the real workspace (which declares no engine dependency at this phase) — evidenced by the recorded failing/passing CI runs; 06's SDK dependency is the first real enforcement target. — **Evidence (2026-08-01):** *passes on the real workspace*, in CI — `CI` run 30718972881 job `engine-pin-lint` ([91419417923](https://github.com/WitchyNibbles/crabgic/actions/jobs/91419417923)) step "engine-pin-lint (fails on any ^/~/range specifier for the pinned engine dependency)", green at `65ff0da`, log line 165: `engine-pin-lint: PASS — checked 18 manifest(s), no range-specified engine-bundling dependency found (watched: @anthropic-ai/claude-agent-sdk).` *Rejects the fixture* — `docs/evidence/phase-01/wi7-engine-pin-lint-fixture-and-real.txt` and, re-captured at this commit, `docs/evidence/phase-01/closeout-c9-engine-pin-fixture.txt`: `engine-pin-lint: FAIL — … dependencies["@anthropic-ai/claude-agent-sdk"] = "^2.1.0" is not an exact pin`, `exit=1`, against the fixture manifest quoted in the same file. `scripts/check-engine-pin.mjs:39` is the sole accepting pattern (`^\d+\.\d+\.\d+…$`).
      **Wording corrected 2026-08-01.** Two clauses. (1) "evidenced by the recorded failing/passing CI runs": the *passing* run is a real CI run of the named job; the *failing* one is not and cannot be, because the fixture manifest lives outside the workspace by design, so `engine-pin-lint` in CI never sees it — the red half is a verbatim recorded local run of the same script, captured twice. What is weaker is that half's channel, not the guarantee that a `^`-ranged engine dependency is rejected. (2) "(which declares no engine dependency at this phase)" was true on 2026-07-15 and is no longer: phase 06 added `@anthropic-ai/claude-agent-sdk` to `packages/engine-claude` and `packages/cli`, both at the exact pin `0.3.218`. That is the criterion's own predicted outcome ("06's SDK dependency is the first real enforcement target"), and it makes the green CI half *stronger* than when it was written — the check now passes against real targets rather than vacuously. The check is unchanged.
- [x] `engine-live` CI job exists, manually-triggered, and its description names the `@live`-tagged suite phase 06 will wire it to (Gap 15) — evidenced by the committed workflow YAML; the job itself remains inert (no engine invoked) at this phase. — **Evidence (2026-08-01):** the named channel — `.github/workflows/engine-live.yml`, committed at `65ff0da`: the job `engine-live` exists (`jobs: engine-live:`), its only trigger is `on: workflow_dispatch: {}` with no `push`/`pull_request`, and its header comment names the suite by path — "this job runs the `@live`-tagged conformance suite — `packages/engine-claude/src/live/*.live.test.ts` via `vitest.live.config.ts`" — plus "Trigger stays manual (`workflow_dispatch`): live runs consume the owner's subscription and are started deliberately, never as a push side effect." This matches `docs/interface-ledger.md` Gap 15 ("Phase 01 states Phase 06 wires it; Phase 06 does"). The trailing "remains inert at this phase" clause is checked against git rather than assumed, in `docs/evidence/phase-01/closeout-c10-engine-live-yaml.txt`: at the bootstrap commit `e6aa458` the file was headed "INERT PLACEHOLDER (Phase 01, Gap 15)" with a single `echo`-only step, and it was phase 06 (`55874d8`) that replaced it with the real suite invocation — exactly as this criterion predicted. `.github/workflows/ci.yml` and this file were both schema-validated at authoring time (`docs/evidence/phase-01/wi4-ci-workflows-schema-valid.txt`). **No `engine-live` run was triggered by this pass**; `gh run list --workflow engine-live.yml` is empty and that job is owner-gated.

## Risks & open questions

- **ARM64 CI flakiness.** ARM64 GitHub-hosted runners are newer and can be unreliable. Mitigation: if unavailable/flaky here, record the gap explicitly (this file's Exit criteria) rather than block; phase 23's own exit criteria require closing it with real-hardware (or documented-substitute) verification before the `v1.0.0` tag — not a silent waiver.
- **This phase asserts zero Claude Code engine facts.** The `engine-live` job is an inert placeholder; nothing in this file should be read as a claim about `claude` CLI flags, settings keys, hook events, or version behavior. Adaptation §10 risk 1 (weekly release velocity) is why real engine-version pinning and the conformance suite live in phases 00/06, not here. No verify-at-build-time engine items apply to this phase.
- **npm package-name collision.** If work item 6's `npm view crabgic` shows the name already taken, this phase only records that verdict — a rename is a product decision escalated to the owner before phase 23's real publish, not resolved here. This is not one of resolutions.md's 15 adjudicated gaps; its "OWNER DECISIONS REQUIRED: None" refers to those 15, not to this distinct, forward-looking operational risk.
- **Package-list stability.** All 18 workspace paths are fixed here and referenced by literal path in every downstream phase's own "Primary package" header field; a later rename would require a coordinated edit across every phase file. Names are taken directly from the cross-phase ledger's own verified cross-check of actual phase-file usage (ledger §1), not guessed, to minimize this risk.
- **Dependency weight compounds downstream.** Phase 05's supervisor daemon owns an idle-budget perf harness (idle <100 MiB RSS, <1% of one core, 5 s heartbeat — its own exit criterion, re-measured at 23). This phase's 18 packages are empty stubs with no runtime dependencies, so no exit criterion here is affected; the note is a bias for later phases choosing dependencies (avoid heavyweight frameworks) that this phase's own toolchain choices (bundler-free TS project references, lean lint/test stack) are already consistent with.
