# Phase 15 — PerformanceContract & benchmarking harness

| | |
|---|---|
| **Depends on** | 13, 14 |
| **Unlocks** | 23 |
| **Sources** | original plan performance sections (budgets, thresholds, noise rules); adaptation §8 (performance contracts unchanged as planned), §4.2 (sandbox network defaults), §5.7 (dispatch budget/turn semantics) |
| **Primary package** | `packages/perf` |

## Goal

Before this phase, `final_verifying` has no performance-regression gate and 11's approval render has nothing real behind its "provisional perf budgets" line. After this phase, every ChangeSet's integrated candidate is scored by a reproducible, interleaved A/B benchmark against its frozen base revision, and 14's gate framework blocks, passes, or inconclusively-blocks it on a statistically defensible, hash-tamper-evident PerformanceContract verdict.

## In scope

- **Risk detection** — heuristics over diff paths + StackEvidence (12, reaching this phase via 14's direct dependency on 12):
  - Categories: CPU, allocation, copying, I/O, networking, database, serialization, concurrency, caching, dataset-size, user-visible hot paths.
  - Runs at each work unit's `verifying` stage as a lightweight risk tag, not a full benchmark — the full A/B run happens once, later (see Gate registration).
- **Budget sourcing** — in order, first source that resolves wins:
  1. The ChangeSet's IntentContract `performance` section / Requirement acceptance criteria (11, resolved via the requirement IDs a benchmark TaskPacket carries, 13).
  2. Else ecosystem research.
  3. Else the base-revision benchmark run sets the budget itself.
  - The enforced figure must hash-match the provisional one 11's approval render already committed to (via ChangeSet, 02); a mismatch fails closed rather than silently re-sourcing. Since 2026-08-06 (ledger Gap 22) the envelope's own signed content hash covers that derived provisional `budgetHash`, so the enforced figure must ALSO match the envelope-covered value — `envelope_hash_mismatch` fails closed, and an unresolvable or unbound envelope fails closed as `no_envelope_budget_binding`.
  - **Where the rule lives, and who runs it (ledger Gap 21, 2026-08-01):** the sourcing order is implemented in `@crabgic/contracts` (`budget-sourcing.ts`, `acceptance-criteria-parser.ts`, `ecosystem-research-table.ts`; `packages/perf` re-exports them verbatim) and is EXECUTED by 11's intake, which derives `(budgetSource, budgets)` from the performance-section requirements it has just built. `IntakeRequest` carries neither field, so provenance cannot be misdeclared. This phase consumes that result; it does not source it.
- **Methodology:**
  - Framework-appropriate warmup; ≥10 interleaved repetitions (A/B alternating base/candidate, never concurrent).
  - Two worktrees — the integrated candidate's own worktree plus a base-revision worktree, both provisioned through 13's executor (13's own dependency on 07; this phase never calls `packages/git-engine` directly).
  - Measured where applicable: latency percentiles, throughput, error rate, CPU time, peak RSS/heap, allocations, fs/network ops+bytes, query counts, capacity.
- **Decision rules:**
  - Absolute-budget breach blocks.
  - Without an absolute SLO: block statistically supported regressions beyond max(noise bound, 5% for critical-path / 10% for sensitive-path changes).
  - Critical-path noise >15% = inconclusive **and blocking** — a hard stop requiring re-invocation on a quieter host, deliberately *not* routed through 14's flake-quarantine mechanism (a noisy perf verdict must never silently start passing).
- **Resource capture:** `/proc` + `getrusage` wrappers around the benchmarked base/candidate processes only (not the supervisor's own idle budget — see Out of scope); raw samples archived; summaries recorded into EvidenceRecord.
- **Adapters:** generic command benchmark (any `ProjectProfile`-declared benchmark command, mirroring 14's own stack-native test-execution pattern) + a purpose-built Node harness; documented extension point for further stacks.
- **Gate registration:** the full A/B decision runs once, at `final_verifying`, against the exact integrated object ID (mirroring 14's own final-candidate orchestration) — registered into 14's gate registry under the IntentContract's existing `performance` section tag; never a per-work-unit full benchmark.

## Out of scope

- **Supervisor idle-resource budget** (idle RSS/CPU heartbeat SLO) — owned end-to-end by 05 (its own perf-harness work item + exit criterion) and re-measured directly by 23; not a PerformanceContract, never routed through `packages/perf`.
- **Benchmark dispatch, worktree creation, worker sandboxing** — 13 (DAG executor, worktree/session provisioning via its own 07/06 dependencies); this phase supplies methodology and decision logic only, and never spawns a worker or calls `packages/git-engine` itself.
- **The generic gate framework** (risk-tag selection, EvidenceRecord schema, `verifying`/`final_verifying` stage wiring) — 14 (`packages/gates`); this phase is one registered gate, not the framework.
- **IntentContract/Requirement authoring, approval-envelope rendering** — 11; this phase reads the approved `performance` section and never authors requirements or renders the approval UI.
- **StackEvidence/ProjectProfile detection** — 12; this phase only consumes.
- **TDD, coverage, and security checks** (SAST, secrets, dependency/license, flake policy) — 14's own risk tags, not performance's.
- **Disposable live environments and release-candidate orchestration** — 23; this phase supplies the fixture matrix and the contract-satisfaction check 23 gates on.

## Interfaces produced

- **`packages/perf`** — sole implementation host for performance-contract logic; nothing else in the roadmap implements it. Exports:
  - Risk detector + PerformanceContract builder.
  - Twin-worktree A/B runner.
  - Stats/decision engine.
  - `/proc` + `getrusage` measurement wrappers.
  - Adapters (generic command, Node harness) behind a documented extension point.
- **Performance gate handler** — registered into 14's risk-tag-keyed gate registry under the IntentContract's `performance` tag, firing at `final_verifying`.
  - Emits standard EvidenceRecord instances (02 schema: command, exit status, env/toolchain fingerprint, timestamp, artifact digests, exact object ID).
  - Outcome is one of `pass` / `block` / `inconclusive-blocking`.
- **PerformanceContract instances, enforced variant** (02 schema) — this phase builds the measurement-backed, hash-linked instance at gate time and attaches it to the ChangeSet (02 schema; 11 creates) alongside the provisional figure 11 already populated at approval time; this phase never edits or re-derives 11's provisional figure, only hash-checks against it (see Budget sourcing).
- **`perf-conformance` fixture matrix** — a named, standalone-runnable CI job:
  - Covers 20%-CPU-regression-blocks, 3%-noise-passes, noisy-critical-inconclusive-blocking, and methodology-violation-refusal (too-few-reps, no-interleave).
  - This is the exact artifact 23's "Quality/security/perf/learning: seeded-fault matrices from 14/15/22" bullet re-invokes.
- **Archived raw resource-capture samples** — EvidenceRecord artifact digests; the sole substrate for the "verdicts reproducible from archived samples alone" exit criterion (23 can re-derive a verdict without re-running the benchmark).
- **Release re-run record** — the A/B runner's output when 23 re-runs it on a quiet host for the release candidate's real verdicts (23's own exit criterion) is archived as a phase-23 evidence record at `docs/evidence/phase-23/perf-contract-rerun.json`, or handed to the consuming check out-of-checkout through `$CRABGIC_PERF_CONTRACT_RERUN_RECORD`, an absolute path to that file; the path convention, the `CRABGIC_<SUBJECT>` override naming, and the requirement that a missing or malformed record FAIL that one release-gate item rather than throw are `docs/interface-ledger.md`'s Gap 16, not this phase's invention.

## Interfaces consumed

- **02 (ambient schema import — same convention every phase uses):** `PerformanceContract`, `EvidenceRecord`, `ChangeSet`, `Requirement` (`performance`-section acceptance criteria) schemas.
- **13 (`packages/scheduler`):**
  - TaskPacket dispatch (exact base object ID, requirement IDs, resource limits, gates) for the benchmark run.
  - The DAG executor's worktree/session provisioning (13's own 07/06 dependencies) — this phase never reaches past 13 to call `packages/git-engine` or spawn a worker directly.
  - Model routing — benchmark dispatch fits the existing "haiku chores" default (13, adaptation §0); no new role introduced.
  - The content-hash cache's `verified results` category — base-revision measurements are cache candidates keyed on base object ID + toolchain fingerprint (optional; avoids re-measuring an unchanged baseline across change sets).
- **14 (`packages/gates`):**
  - The risk-tag-keyed gate framework as an extensible registry — the same aggregation pattern as the gateway's tool registry (resolved Gap 1): 14 exposes the registry, and this phase — already a dependent of 14 — registers into it, no new dependency edge required.
  - `verifying`/`final_verifying` stage hooks and the EvidenceRecord emission shape.
  - Explicitly **not** consumed: 14's flake-quarantine path (see Decision rules) — a noisy perf verdict blocks, it is never quarantined-as-passing.
- **12 (`packages/detect`, transitively — 14 depends on 12 directly, this phase depends on 14):** StackEvidence — risk-detection heuristics and adapter selection key off it.
- **11 (`packages/supervisor`/`plugin`/`cli`, transitively — 13 depends on 11 directly, this phase depends on 13):** the approved PerformanceContract's provisional figure and envelope hash, embedded in ChangeSet (11 creates); the enforced budget must hash-match it or fail closed. 11 itself is unaffected by this phase's build order — it ships and renders its "provisional perf budgets" line independently of when this phase lands.

## Work items

1. Risk detector + PerformanceContract builder — budget-sourcing order (Requirement/IntentContract `performance` section → ecosystem research → base-revision fallback).
   - Failing-first: synthetic diffs tagged with expected risk categories against StackEvidence fixtures.
2. Measurement wrappers (`/proc` + `getrusage`) + artifact schema.
   - Failing-first: wrapper unit tests against a synthetic workload with known resource consumption, plus a secret-leakage negative test (no env/argv capture).
3. Adapters: generic command benchmark (`ProjectProfile`-declared) + Node harness; documented extension point.
   - Failing-first: one conformance fixture per adapter type.
4. Twin-worktree A/B runner — dispatches through 13's executor into per-attempt worktrees/sandbox profiles; warmup + ≥10 interleaved repetitions.
   - Failing-first: fake-engine E2E with a scripted, deterministic benchmark command proving exact interleaving order before any real stack adapter exists.
5. Stats module (bootstrap-CI noise bound, documented method) + decision engine (absolute-budget / 5%–10% statistical / >15% critical-noise inconclusive-blocking).
   - Failing-first: synthetic distributions with known injected regression/noise.
6. Gate registration into 14's registry at `final_verifying`; envelope/ChangeSet hash-link check; EvidenceRecord emission wired for the `perf-conformance` fixture matrix.
   - Failing-first: a tampered post-approval budget fixture must fail the hash-link check and block.

## Test plan

All of the below start red: no risk detector, decision engine, wrappers, adapters, or hash-link check exist yet.

- **Unit:** stats module against synthetic distributions with known regression/noise percentages; decision-engine boundary tests at exactly 5%, 10%, 15%; measurement-wrapper parsing of `/proc`/`getrusage` fixtures.
- **Property:** fast-check over randomized interleaved sample sequences — verdict classification is order-independent (base/candidate interleaving order never changes the outcome) and monotonic in regression magnitude.
- **Integration:** fake-engine E2E dispatching a scripted deterministic benchmark TaskPacket through 13's executor; twin-worktree runner produces a byte-identical interleaved schedule across two runs (determinism); gate fires at `final_verifying` and emits a schema-valid EvidenceRecord 14's framework can read.
- **Conformance:** the `perf-conformance` fixture matrix itself — 20% CPU regression blocked, 3% noise-level change passes, noisy-critical fixture inconclusive-blocking, methodology violations (too few reps, no interleave) refuse a verdict.
- **Security:** resource-capture artifacts contain no process environment/argv content (secret-leakage vector into evidence); a post-approval budget edit fails the envelope/ChangeSet hash-link check closed rather than silently enforcing the new figure.

## Exit criteria

**Closeout pass 2026-08-02:** 6/7 ticked against recorded evidence; the one unticked box below
(criterion 3) is an open defect — see
`docs/evidence/criteria-closeout/defects/15-enforced-budget-not-bound-to-approved-envelope.md` and
its own per-box note. Machine-readable index:
`docs/evidence/criteria-closeout/phase-15.json`.

Shared citations reused by several boxes below. **`perf-conformance` run
[30741826024](https://github.com/WitchyNibbles/crabgic/actions/runs/30741826024)**, green at
`eabb65a` — its `perf-conformance (ubuntu-latest)` job
([91480517302](https://github.com/WitchyNibbles/crabgic/actions/jobs/91480517302)) ran the four
named steps and the job log records each one's suite and count individually (lines 201, 215,
229–231, 247–248). **`CI` run
[30741826008](https://github.com/WitchyNibbles/crabgic/actions/runs/30741826008)**, green at the
same commit — its `unit-test+coverage (ubuntu-latest)` job
([91480519773](https://github.com/WitchyNibbles/crabgic/actions/jobs/91480519773)), step "test with
80% line+branch coverage gate", executed 625 test files / 6155 tests and names all 24
`@crabgic/perf` suites individually (job-log lines 750–790). Scoped local re-runs of each
criterion's own suites, captured verbatim at the closeout commit with their command lines and exit
status, are committed as `docs/evidence/phase-15/closeout-c<k>-*.txt`.

Citation re-resolution. Every quoted code fragment in the index was located mechanically against
the tree this branch merges into, immediately before pushing: **69 code-shaped quoted assertions,
69 exact content matches (whitespace-collapsed), all within four lines of their cited span.** Four
further citations quote prose rather than code and are therefore not line-anchorable — the
`.strict()` member lists in `authorization-envelope.ts` and `performance-contract.ts`, the
`criteria-seal.ts` sentence (which contains a backtick and so is not extractable as a quoted span),
and the `risk-tags.ts` tag reservation. All four were byte-verified by hand at the closeout commit
and are named as such in the index, rather than being skipped silently.

Scope caveat that dates every box below, recorded rather than left implicit: **this phase's gate
handler has no production caller.** `createPerformanceGateHandler` is referenced only by
`packages/perf/src/index.ts`'s barrel and by this package's own tests — `docs/interface-ledger.md`
Gap 21 says so in as many words ("`createPerformanceGateHandler` still has no production caller"),
and a repo-wide search for `@crabgic/perf` at `eabb65a` finds no dependent package. Derivation is
live (11's intake executes the budget-sourcing rule); *enforcement* is not wired. So criteria 1, 3,
4 and 5 below are evidenced at the level of the registered handler exercised through 14's real
registry and 04's real journal in tests and in the `perf-conformance` job — never as a gate that
fires in a shipped run.

Second dated note. Ledger **Gap 21** (2026-08-01, `4f2b33b`) relocated this phase's budget-sourcing
rule — `budget-sourcing.ts`, `acceptance-criteria-parser.ts`, `ecosystem-research-table.ts`, and
(in `bddac4c`) `higherIsWorse` — into `@crabgic/contracts`, leaving verbatim re-export shims under
`packages/perf/src/`. No exit criterion below names any of those modules, so no box moved with
them; the §In scope "Budget sourcing" bullet already carries the reconciliation. One consequence is
worth a reader's attention because it is invisible in a coverage table: those relocated modules are
exercised **only** cross-package, through `packages/contracts/dist`, which the coverage config
excludes — so `acceptance-criteria-parser.ts`, `budget-sourcing.ts` and `metric-direction.ts` sit at
0% source coverage in `packages/contracts` while their behaviour is fully tested from
`packages/perf`. Measured independently for this pass (`--coverage.reporter=json-summary` over both
packages, enumerating the files by name); already reported to the closeout integrator by phase 02's
own record, so it is corroborated here rather than re-filed.

- [x] `perf-conformance` fixture matrix green: 20% CPU regression blocked; 3% noise-level change passes; noisy-critical fixture → inconclusive-blocking (CI job). — **Evidence (2026-08-02):** `packages/perf/src/conformance/perf-conformance.test.ts` carries one `it()` per named outcome, each with its own fresh journal and its own fresh `@crabgic/gates` registry, all three firing the real handler through `registry.fireByTag("performance", { stage: "final_verifying", … })`: `:104-113` a zero-variance base of 12×100 against 12×120 on a `sensitive` path (10% threshold) — `expect(results[0]?.verdict.passed).toBe(false)` plus `expect(EvidenceRecordSchema.safeParse(results[0]?.evidence).success).toBe(true)`; `:115-123` the anti-vacuity control, 12×100 against 12×103 on a `critical` path (5% threshold) — `expect(results[0]?.verdict.passed).toBe(true)`, which a block-everything decision engine fails; `:125-135` a deliberately high-variance base sample set fired against itself on a `critical` path, asserting not merely a block but the specific outcome — `const detail = JSON.parse(results[0]!.verdict.detail) as { outcome: string }; expect(detail.outcome).toBe("inconclusive_blocking")` — so a noisy verdict can never be recorded as an ordinary pass or an ordinary block. The zero-variance fixtures make the boundary exact rather than approximate (a zero-variance base gives a bootstrap noise bound of exactly 0, pinned by `packages/perf/src/stats/bootstrap-ci.test.ts:6-9`), and the 5%/10%/15% boundaries themselves are pinned to the 0.0001% either side in `packages/perf/src/stats/decision-engine.test.ts:19-104`. The criterion's named CI channel: `perf-conformance` run 30741826024 job 91480517302, step "perf-conformance fixture matrix (4 independent fixtures) decides correctly" — job-log line 195 records the verbatim command and line 201 `✓  @crabgic/perf  src/conformance/perf-conformance.test.ts (5 tests) 65ms`. `docs/evidence/phase-15/closeout-c1-perf-conformance-matrix.txt` is the supplementary scoped re-run at the closeout commit. Stale label, noted not fixed: the step's own name says "4 independent fixtures" while the suite has five `it()` blocks — fixture 4 was split into 4a/4b — a workflow-step caption, not an assertion.
- [x] Methodology violations (too few reps, no interleave) refuse to produce a verdict (unit test). — **Evidence (2026-08-02):** the refusal is asserted at all three layers it can be bypassed at, each with a positive control. Pure check — `packages/perf/src/runner/methodology.test.ts:35-46` `expect(caught).toBeInstanceOf(MethodologyViolationError)` with `.reason` `"too_few_repetitions"` at exactly `MIN_INTERLEAVED_REPETITIONS - 1`, `:48-69` the same for a block design with `.reason` `"not_interleaved"`, `:71-76` a *single* broken alternation inside an otherwise-valid schedule, and `:78-88` the discriminant guard (`expect((caught).reason).toBe("too_few_repetitions")` on an empty schedule, never a false `"not_interleaved"` pass); the two positive controls at `:19-23` and `:25-33` accept an exactly-minimal alternating schedule and one preceded by warmup steps, so a refuse-everything implementation fails. Runner — `packages/perf/src/runner/twin-worktree-runner.test.ts:11-27` proves the refusal happens BEFORE any work: the `dispatchWorktree`/`measure` injection points increment counters and the test asserts they are still zero after the rejection, so a partially-dispatched benchmark cannot be silently discarded. Gate boundary — `packages/perf/src/gate/performance-gate.test.ts:238-259` (the MINOR-1 defense-in-depth regression) feeds five samples per side to the *registered handler*, which consumes pre-computed samples and would otherwise never see the schedule at all, and asserts it REJECTS rather than returning a pass/block verdict; `packages/perf/src/gate/performance-gate.ts:71-86`'s `assertGateInputMeetsMethodologyFloor` runs before any `decide()` call. Note the deliberate asymmetry this phase documents: a methodology violation rejects with no verdict and no evidence, whereas a hash-link failure is converted into a recordable blocking verdict — `performance-gate.ts:97-115` states both and why. CI: `perf-conformance` run 30741826024 job 91480517302, step "methodology-violation unit tests (too-few-reps, no-interleave — typed refusal)", job-log line 215; the runner and gate suites in `CI` run 30741826008 job 91480519773, job-log lines 757 and 751. `docs/evidence/phase-15/closeout-c2-methodology-refusal.txt`.
- [x] Enforced budgets are hash-linked to the approved envelope; a tampered post-approval edit fails closed (integration test + journal entry). — **Evidence (2026-08-07), closed by remediation. The UNMET assessment that follows is kept VERBATIM and is superseded, not deleted.** Defect: `docs/evidence/criteria-closeout/defects/15-enforced-budget-not-bound-to-approved-envelope.md`. The second clause is fully evidenced and the first is not. What exists: `EnforcedPerformanceContract.provisionalBudgetHash` carries the *provisional* contract's `budgetHash` forward, and `packages/perf/src/contract/hash-link.ts:71-90` runs three ordered checks against 04's append-only hash-chained journal, so a post-approval edit fails closed three ways — `self_consistency_mismatch` (`contract-builder.test.ts:67-91`), `no_journal_anchor` (`:134-147`, fail-closed, never "no anchor means trust the record") and the real vector `journal_anchor_mismatch` (`:93-132`, a widening from 200 to 2000ms that ALSO recomputes its own hash, with `:113-115`'s inline sanity assertion proving the self-consistency check alone would have passed the same fixture). It holds through the full registry path with the block journaled as evidence (`performance-gate.e2e.test.ts:105-157`, `expect(results[0]?.verdict.detail).toMatch(/hash-link check failed \(journal_anchor_mismatch\)/)`). What does not exist is the binding the words name: **nothing links an enforced budget to the approved `AuthorizationEnvelope`.** `AuthorizationEnvelopeSchema` (`packages/contracts/src/contracts/authorization-envelope.ts:73-104`) carries no budget and no budget hash; `EnforcedPerformanceContractSchema` carries no envelope hash; the approval token is minted over the envelope hash alone (`ApprovalTokenSubjectKind: "envelope_hash"`), and `packages/contracts/src/contracts/criteria-seal.ts:6-22` states the same fact from roadmap/24's side — "the `AuthorizationEnvelope` content hash covers authority fields only (never criteria), the approval token signs that envelope hash". The substituted anchor proves "this is what intake committed", not "this is what a human approved", so a budget already wrong when 11 journaled it is undetectable — this phase's own `docs/evidence/phase-15/README.md` names exactly that as a HIGH-PRIORITY carry-forward needing a coordinated 02/11 change. That is a lost guarantee rather than an imprecise word, so it is UNMET rather than a wording correction. **Evidence (2026-08-07), closed by remediation:** both conjuncts now have bearers, and the UNMET reasoning above is superseded rather than deleted — its central negative fact ("carries no budget and no budget hash") stopped being true when PR #116 landed interface-ledger Gap 22. Conjunct 1 (hash-linked to the APPROVED envelope): `packages/contracts/src/contracts/authorization-envelope.ts:134` declares `provisionalBudgetHash`, and `packages/supervisor/src/intake/envelope-builder.ts:94` puts it inside the object `hashEnvelopeContent` canonicalises (`:77-78`), so the approval token — which signs the envelope hash — now covers the budget set; `packages/contracts/src/contracts/performance-contract.ts:177` carries the matching `approvedEnvelopeHash`. The binding is DERIVED, never declared: `packages/supervisor/src/intake/intake-pipeline.ts:73` types the caller's field as `RequestedEnvelopeContent = Omit<AuthorizationEnvelopeContent, "provisionalBudgetHash">` and `:84` is where `IntakeRequest` consumes it. Conjunct 2 (a tampered post-approval edit fails closed): already evidenced via `journal_anchor_mismatch`, now joined by `no_envelope_budget_binding` and `envelope_hash_mismatch`, which are checks 4 and 5 and are ordered LAST on purpose — first-failure-wins, so moving them above check 1 reddens the three PRE-EXISTING tests this box's own merged evidence cites (measured, P2b). Channels: "integration test" is `packages/perf/src/gate/performance-gate.e2e.test.ts` through 14's real `createGateRegistry()` and 04's real `JournalStore`; "journal entry" is the journaled blocking evidence the 2026-08-02 text already cites. Non-vacuity: deleting checks 4+5 reddens exactly the six new binding tests and no pre-existing one (P1, 6 of 1160); re-labelling check 5's reason to a colliding existing member reddens exactly the three whose subject IS that reason (P2a); deleting the derivation reddens three cross-package assertions plus the regenerated golden (P3, both stale-dist and rebuilt numbers recorded); and the compile-time claim was FALSIFIED rather than asserted (P4 — excess-property checking binds object literals only, so a widened intermediate smuggles the field past `tsc` at exit 0, which is why the runtime override is pinned by its own reverse-probed test). Transcript: `docs/evidence/phase-15/envelope-binding-probe-batchE.txt`. **Residuals, so this tick is not read wider than it is:** ledger Gap 22 is appended AWAITING OWNER RATIFICATION on two points (its omission from the ledger's intro table, and the OPTIONAL-in-schema posture) — that gates the ledger ENTRY's ratification, not the enforcement; `createPerformanceGateHandler` still has no production caller (Gap 21, unchanged — this phase's own scope caveat stands, and `packages/cli/src/daemon/compose-gate-registry.ts:75` records the absence deliberately); legacy envelopes without the member still parse but cannot pass enforcement, so drain in-flight runs before upgrading; and remedy step 2's disclosed deviation (the approval-time check is discharged by signature coverage) is recorded at `hash-link.ts`.
- [x] Verdicts reproducible from archived samples alone, byte-identical on re-derivation (determinism test). — **Evidence (2026-08-02):** re-derivation needs nothing but the sample arrays, and is byte-identical because the whole stats layer is a pure function of the sample MULTISET. Mechanism: `packages/perf/src/stats/bootstrap-ci.ts:54` sorts the input before resampling and `:57` seeds a fixed deterministic PRNG (`stats/deterministic-rng.ts`, never `Math.random()`), and `stats/mean.ts` is the one sorted summation all three call sites share — floating-point addition is not associative, so an unsorted reduce over a permutation can differ by ULPs. Assertions: `packages/perf/src/stats/bootstrap-ci.test.ts:22-25` `expect(computeNoiseBoundPct(samples)).toBe(computeNoiseBoundPct(samples))` and `:27-31` the same figure from a shuffled array, with `:6-9` (`toBe(0)` on a constant set) and `:11-15` (strictly larger bound for strictly higher variance) as the controls that stop a constant-returning implementation from satisfying determinism trivially. Verdict level: `packages/perf/src/stats/decision-engine.property.test.ts:40-65` re-derives the whole `DecisionResult` from a GENUINELY RANDOM full-length permutation of both sample arrays — `expect(b).toEqual(a)` over 150 fast-check runs, covering `outcome`, `regressionPct` and `noiseBoundPct` together — which is strictly stronger than re-running on the identical array; `:17-35` records why the earlier `.reverse()`-only form was replaced (a middle-element bug survived it) and builds the permutation at `:33`. Benchmark level: `packages/perf/src/runner/twin-worktree-runner.e2e.test.ts:108-131` runs the identical twin-worktree benchmark twice through `@crabgic/scheduler`'s real `dispatchAttempt` with a pinned clock and asserts `expect(second).toEqual(first)` on the schedule and both sample sets. CI: `perf-conformance` run 30741826024 job 91480517302, step "decision-engine boundary + property tests", job-log lines 229–233; the runner E2E in `CI` run 30741826008 job 91480519773, job-log line 759. `docs/evidence/phase-15/closeout-c4-determinism.txt`. Two scope facts so the tick is not read wider than it is. (a) "Archived" is exercised as "the same sample values, handed to `decide()` with no benchmark, no worktree and no journal in the loop" — no test reads samples back from a persisted archive, because `packages/perf` writes none: `PerformanceGateMeasurements.artifactDigests` are caller-supplied strings (literally `"sha256:conformance-fixture"` in the matrix), and archival into an `EvidenceRecord` is 14's seam, which this phase's own §Out of scope assigns away. (b) "Verdict" is the decision — `outcome`/`regressionPct`/`noiseBoundPct` — not the enforced-contract record, whose `id` and `createdAt` default to `randomUUID()`/`new Date()`; a caller wanting a byte-identical enforced record must inject `contractIdFactory` and `now`, which `CreatePerformanceGateHandlerOptions` exposes for exactly that reason (`gate/performance-gate.ts:41-42`).
- [x] Performance gate fires through 14's risk-tag-keyed registry at `final_verifying` and emits a schema-valid EvidenceRecord (integration test against 14's gate harness). — **Evidence (2026-08-02):** `packages/perf/src/gate/performance-gate.e2e.test.ts:49-103` is the integration test the criterion names, against 14's real harness and 04's real journal — no fakes on either side. It registers `createPerformanceGateHandler(...)` into a FRESH `createGateRegistry()` (`@crabgic/gates`' public API; `packages/gates/src/risk-tags.ts:23` reserves the `performance` tag for this registrant, and `packages/gates` is not edited by this phase) and fires `registry.fireByTag("performance", { stage: "final_verifying", changeSetId, objectId: "integrated-candidate-obj", journal: tj.store })`. Assertions: `:84-86` `expect(results).toHaveLength(1)` and `expect(results[0]?.tag).toBe("performance")` — the tag-keyed dispatch really resolved this handler; `:90` `expect(EvidenceRecordSchema.safeParse(evidence).success).toBe(true)` against 02's own schema; `:91-93` the record carries `gateTag: "performance"`, the exact `objectId` and the `changeSetId`; and `:98-102` reads the entry back out of the real journal — `for await (const entry of tj.store.queryEntries({ type: "evidence_pointer" }))` then `expect(entries).toHaveLength(1)` — so emission is journaled, not merely returned. The record is minted by 14's own single emission path (`packages/gates/src/evidence.ts:22-48`), not hand-built by this phase. `perf-conformance` run 30741826024 job 91480517302, step "hash-link tamper fixture (fail-closed) + gate registration E2E", job-log line 248; `CI` run 30741826008 job 91480519773, job-log line 754. `docs/evidence/phase-15/closeout-c5-gate-registry-final-verifying.txt`. One precision a reader should not have to reconstruct: `final_verifying` is a caller-supplied `GateContext` field on this path, not an enforced or recorded property — `registry.fireByTag` (`packages/gates/src/registry.ts:72-82`) never inspects `context.stage`, this phase's handler never reads it, and `EvidenceRecordSchema` has no stage field, so the same test would pass with `stage: "verifying"`. What is genuinely evidenced is that the handler dispatches through the tag-keyed registry and emits a schema-valid, journaled `EvidenceRecord` when fired at that stage; that it is only ever fired at that stage is 14's `fireFinalCandidateVerification` guard (`packages/gates/src/final-candidate.ts:45-47`), which this phase's handler is not exercised through, and which nothing invokes for it in production (see the scope caveat above).
- [x] Resource-capture artifacts contain no environment/argv content (security test). — **Evidence (2026-08-02):** `packages/perf/src/measurement/secret-leakage.test.ts` closes the vector at both ends. Schema — `:12-23` and `:25-36` feed `ResourceCaptureArtifactSchema` an otherwise-valid artifact carrying an extra `env: { SECRET_TOKEN: … }` and an extra `argv: [...]` respectively and assert `.safeParse(...).success` is `false`, so `.strict()` makes an env/argv field unrepresentable rather than merely absent by convention. Behaviour — `:38-48` runs a REAL child process with a secret injected into its environment and asserts the serialized artifact contains neither the secret's value nor its key name (`expect(serialized).not.toContain(secret)`, `expect(serialized).not.toContain("CRABGIC_PERF_TEST_SECRET")`); `:50-61` then sweeps the whole ambient `process.env` — whatever CI happens to populate — asserting `expect(serialized.includes(value)).toBe(false)` and `expect(serialized.includes(key)).toBe(false)` for every entry, so the guarantee is not limited to one planted name. Shape — `packages/perf/src/measurement/rusage.test.ts:29-32` pins the capture's key set exactly, `expect(Object.keys(sample).sort()).toEqual(["cpuSystemMs", "cpuUserMs", "maxRssKb"])`, so an env/argv field cannot be added to `captureSelfRusage`'s return without a test failing. `CI` run 30741826008 job 91480519773, job-log lines 767 (`src/measurement/secret-leakage.test.ts (4 tests)`) and 774 (`src/measurement/rusage.test.ts (3 tests)`). `docs/evidence/phase-15/closeout-c6-secret-leakage.txt`. Narrow scope note: the ambient sweep at `:57` skips env values shorter than six characters to avoid coincidental substring matches, and skips that entry's key check with them.
- [x] `perf-conformance` runs as a standalone, named CI job invocable without the full release harness — the exact entry point 23 re-runs. — **Evidence (2026-08-02):** the job exists, is named, and demonstrably ran standalone: `.github/workflows/perf-conformance.yml` is its own workflow (`name: perf-conformance`, job `perf-conformance`, `on: push`/`pull_request`, matrix `ubuntu-latest` + `ubuntu-24.04-arm`) whose four steps invoke the suites directly, and run [30741826024](https://github.com/WitchyNibbles/crabgic/actions/runs/30741826024) is a push-triggered execution of it at `eabb65a` with no release harness anywhere in the run — job-log line 195 records the verbatim first command, `npx vitest run packages/perf/src/conformance/perf-conformance.test.ts --coverage.enabled=false`. That it is the EXACT entry point 23 re-runs is not left as prose: phase 23's producer names the same path as a constant and re-invokes it identically — `e2e/attestation/src/perfContractRerun.ts:59` `export const PERF_CONFORMANCE_SUITE = "packages/perf/src/conformance/perf-conformance.test.ts"` and `:129-146` `realPerfConformanceRunner` spawns `npx vitest run PERF_CONFORMANCE_SUITE --coverage.enabled=false`, byte-for-byte the workflow's own command — and a drift guard binds the declared fixture set to the real suite text: `e2e/attestation/src/perfContractRerun.test.ts:184-205` reads the suite file and asserts `expect(suite).toContain(contract.fixtureTitle)` (`:193`) for each of the three declared contract ids, with `:202-203` `expect(PERF_CONFORMANCE_FIXTURES.length).toBeGreaterThan(0)` as the empty-set floor, so a renamed or deleted fixture reddens instead of quietly shrinking what the release re-runs. `:237-242` pins the developer-and-CI-run-the-same-thing path (`expect(pkg.scripts["probe:perf-contract-rerun"]).toContain("perfContractRerunCli.ts")`) and `:213-243` pins `release-e2e.yml`'s invocation of it plus its Gap-16 `$CRABGIC_PERF_CONTRACT_RERUN_RECORD` override. `docs/evidence/phase-15/closeout-c7-standalone-entry-point.txt` (24 tests) is the execution evidence for that file, which lives under `e2e/attestation/` and is therefore NOT in the default `npm test` fan-out — it runs only under `npm run test:e2e` and `release-e2e.yml`, so no green `CI` run covers it and the local transcript is cited in its place. Recorded, not claimed away: `perf-conformance` is fixture-based by its own header, so it produces no release-candidate verdicts — `e2e/attestation/src/performanceContracts.ts:823-828` emits that as a standing blocking reason on 23's `performance-contracts` item. That is 23's obligation (23:75), not this criterion's, which asks only that the entry point exist, be named, and be invocable standalone.

## Risks & open questions

- Shared CI runners are noisy — product release perf runs happen on quiet hosts or are marked inconclusive per policy; a parked-then-resumed benchmark (13's `parked:rate_limit`) spans two host-load regimes and must restart the full interleaved sequence from scratch, never resume mid-sequence.
- The reference worker sandbox profile (adaptation §4.2/Appendix B, implemented by 03's envelope compiler) sets `allowLocalBinding: false`; benchmarking a networking-risk hot path that binds a local port needs an explicit, approval-visible AuthorizationEnvelope grant (11) — never a silent default change. Verify at build time whether this phase's Node-harness adapter ever needs such a grant for realistic fixtures.
- Phase 20 and Phase 21 have already reconciled the connector latency/throughput counters that reach 14's shared EvidenceRecord stream: both explicitly confirm no 20→15 or 21→15 edge exists in the README graph (15 and 21 are sibling dependents of 14; 20 depends on 16/17 only) and that the counters are available to, but not contractually consumed by, 15 — surfacing solely via the shared journal (04)/EvidenceRecord stream. No reconciliation is outstanding here; this phase consumes no such feed.
- No new unconfirmed Claude Code engine facts: dispatch, sandbox, and session routing are entirely inherited from already-resolved 06/13 spikes; the only environment-specific constraint this phase introduces is the confirmed (not unconfirmed) `allowLocalBinding` default above.
