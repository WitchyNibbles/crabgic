# Phase 14 — Quality & security verification gates

| | |
|---|---|
| **Depends on** | 12, 13 |
| **Unlocks** | 15, 21, 22 |
| **Sources** | original plan quality/security sections (folds into original-plan phase 9's cross-cutting evidence integration per README's mapping table); adaptation §8 (quality gates carried over unchanged: TDD, coverage ratchet, NIST SSDF-selected checks, exact-revision evidence, flake quarantine) |
| **Primary package** | `packages/gates` |

## Goal

Before this phase, `verifying`/`final_verifying` are named run-lifecycle stages (02) with no enforcement behind them, and nothing binds a `Requirement` to proof it was met. After this phase, every work-unit attempt and every final-integrated candidate passes through a risk-tag-keyed, externally-extensible gate registry — TDD evidence, a monotonic coverage ratchet, stack-native test execution, SSDF-selected security scanning, and flake policy — each firing producing an `EvidenceRecord` (02) hash-bound to the exact object ID under test, so `Requirement → EvidenceRecord → exact object ID` resolves in both directions for any `ChangeSet`.

## In scope

- **Gate framework & registry:** an extensible, risk-tag-keyed gate registry — tags are `IntentContract`'s named sections (11: `scope/non-goals/audience/compatibility/security/performance/observability/rollout/acceptance`) plus this phase's own always-on default tags `tdd`, `coverage`, `flake`, `engine-conformance` (no matching `IntentContract` section exists for these; they apply to every `ChangeSet` regardless of which sections are populated). `security` is shared: this phase's own SAST/secret/dependency scanners register under it alongside 21's connector-security fixtures; `performance` is used exclusively by 15's registered gate, nothing here contributes to it. Fires at the `verifying` (per-work-unit) and `final_verifying` (final-candidate) run-lifecycle stages (02). External phases register handlers with no new phase-dependency edge (same aggregation pattern as the gateway tool registry, Gap 1). Every firing — this phase's own or an external registrant's — emits one `EvidenceRecord` (command, exit status, env/toolchain fingerprint, timestamp, artifact digests, exact object ID). The `engine-conformance` default tag (adaptation §9 delta row 9: "Add permission/hook/sandbox conformance gates to the exact-revision evidence set") is a *binding* gate: at `verifying`/`final_verifying` it resolves the attempt's dispatching engine version (recorded per attempt by 13's dispatch from 06's `EngineCapabilities` — a journal read, ambient, the same no-new-edge pattern as the `Requirement`-ID consumption under Interfaces consumed), requires a journaled green `engine-live` run record for that exact engine version (engine version + run ID + suite digest, emitted by 06), and binds that reference into the `ChangeSet`'s `EvidenceRecord`, failing closed when no matching green record exists — it never re-runs permission/hook/sandbox probes per `ChangeSet`; it binds the version-grain conformance verdict into the exact-revision evidence chain.
- **TDD gate:** failing-test-first evidence — red at base journaled before implementation dispatch, green at candidate; a missing red-baseline `EvidenceRecord` for the same `Requirement` fails the gate.
- **Gate verdicts feed repair attempts, never review rounds (2026-07-28, amended 2026-07-29 — ledger Gap 19).** A verdict from this phase is one of the two admissible sources of "new diagnostic evidence" for 13's repair path, which stays bounded at initial + 2. An **adversarial review round** — over the design, the test suite, or the diff — is a different loop: read-only, spending no repair attempt, and bounded by progress rather than by round count (loop while a round closes a blocking finding; ceiling five; then escalate). This phase neither bounds nor scores review rounds; a review of the *test suite* in particular is not a gate verdict and never substitutes for one. **The converse is now also binding:** anything a deterministic gate in this phase can decide **is** decided by that gate, and a reviewer may not raise findings in gate territory — coverage percentages, lint, types and conformance are `GateVerdict`s, not opinions to be re-litigated in prose. This is the tool-grounded half of the loop and it takes precedence over the judged half.

  **AMENDED 2026-08-15 (owner ruling R4, ledger Gap 19).** The progress bound above is superseded. A stage
  now closes on a round that raises **no admissible novel finding**, with severity playing no part — a new
  `advisory` holds it open exactly as a blocker does. Admissible means: concerns a path this change set
  writes, and not raised before. The ceiling of five becomes a runaway guard at `REVIEW_RUNAWAY_GUARD` (20),
  which reports a **stall**, never a close. Everything else in Gap 19 — read-only rounds, no repair attempt
  spent, dispositions at every severity, a fresh reviewer per round — stands unchanged.

  **AMENDED 2026-08-16 (ledger Gap 23, parts 1-3 — the review surface this phase owns grew, and stopped
  being the only consumer).** `PIPELINE_STAGES` carries **nine** members (`design-gate`, `audit` and
  `document` added), with criterion ids unique across the whole roster, and `exitCriteriaFor` covers every
  one. `review.submit` derives criteria for the `research` and `documentation` artifacts on the same terms as
  `design` and `plan` — parsed and refused rather than ignored, derivations server-side, and an attestation
  claiming a criterion the artifact contradicts is voided. And this phase's handler is **no longer the
  roster's only consumer**: `pipeline.plan` reads the same table to decide what runs next, so the roster is
  now sequenced as well as judged. Neither the gate registry nor any `GateVerdict` is changed by this, and
  the precedence rule above is untouched — a reviewer still may not re-litigate what a gate decides.
  **Coordinated with:** `roadmap/{10,11,13,25}` in the same change.
- **Coverage:** ≥80% line+branch on greenfield projects; existing projects never regress below their recorded floor; changed instrumentable code reaches 80%; ratchet state journaled and monotonic.
- **Test execution:** unit/integration/E2E via stack-native commands declared on `ProjectProfile` (02), run inside the dispatching work unit's own worker-grade sandbox (03/06, provisioned by 13). Which adapter/scanner *categories* apply at all is decided from `StackEvidence` (02, populated by 12) — e.g. no JS-specific SAST ruleset fires without Node evidence; IaC adapters fire only when Terraform/CloudFormation files are detected. (`ProjectProfile` says *how* to run a stack's own tests; `StackEvidence` says *whether* a given gate category applies at all — the two schemas serve distinct roles here, not made explicit anywhere else in the roadmap.)
- **Security checks (SSDF-selected):** SAST (semgrep), secret scanning (gitleaks), dependency/license analysis (osv-scanner + policy); negative tests; fuzzing/DAST/container/IaC adapters where `StackEvidence` applies; CRITICAL/HIGH findings block. Scanner binaries resolve as digest-pinned entries from 12's content-addressed capability store — never fetched or executed ad hoc.
- **Root-cause policy detectors:** flag disabled checks, broad exception swallowing, hidden fallbacks appearing in a diff; configurable advisory→blocking; same normalized-finding shape as the security adapters.
- **Flake policy:** a rerun-then-pass result is marked `unstable` until fixed or explicitly quarantined; quarantine entries are journaled and expire on a recorded schedule, reverting to blocking.

## Out of scope

- **Stack detection, the capability-quarantine pipeline, and digest-pinning the scanner binaries themselves** (12) — this phase only consumes already-produced `StackEvidence` and already-pinned tool entries; it never runs a detector or a quarantine pass.
- **DAG dispatch, `TaskPacket` construction, worktree/sandbox provisioning, and model routing** (13) — this phase supplies gate definitions/thresholds that ride inside 13's `TaskPacket.gates` field and reads back results; it never spawns a worker or picks a model role.
- **`IntentContract`/`Requirement` authoring and section structure** (11) — this phase reads already-approved section names as its risk-tag vocabulary; it never assembles a contract.
- **Computing or freezing the base/integrated object ID, merge preflight, CAS ref updates** (07/08) — this phase gates against object IDs 07/08 already compute and freeze; it never touches Git plumbing.
- **Compiling worker permission/sandbox profiles or spawning the engine** (03/06) — gate commands execute inside whatever sandboxed worker session 13 already dispatches into.
- **`PerformanceContract` budget sourcing, the twin-worktree A/B methodology, its decision rules** (15) — this phase exposes the registry 15's gate registers into; it implements no performance-specific logic itself.
- **Authoring connector-specific security fixtures** — forged admin/delete, tenant boundary, redaction (21) — this phase only runs whatever 21 registers, inside the same framework.
- **Eval grading, shadow-run comparison, contamination detection** (22) — this phase supplies `EvidenceRecord`s/gate outcomes as grading ground truth; it never grades anything itself.
- **CLI command shape, argument parsing, output formatting for `evidence <change-set-id>`** (09) — this phase supplies the `EvidenceRecord` data that command surfaces; the command itself is 09's.
- **Provisioning disposable/live/containerized environments or release-scale orchestration** (23) — this phase builds gate logic once; 23 re-invokes the same `gates-conformance` job against the frozen release-candidate object ID.

## Interfaces produced

- **`packages/gates` extensible gate registry** — risk-tag-keyed (see In scope for the tag vocabulary); external registration requires no new phase-dependency edge. 15 registers its performance gate under `performance`; 21 registers connector-security fixtures under `security`. Fires at `verifying` and `final_verifying` (02).
- **`EvidenceRecord` (02 schema) emission** — this phase is the primary emitter: every gate firing produces one instance (command, exit status, env/toolchain fingerprint, timestamp, artifact digests, exact object ID, **and the handler's own `gateVerdict`** — `emitEvidence` is the sole setter, and a record without one is by construction not a firing; interface-ledger Gap 20, "Amended 2026-07-29"), journaled as a `JournalEntryType.evidence_pointer` entry (02/Gap 5). This exact field shape is what 15 cites verbatim ("mirroring 14's own..."), and what 09's `evidence <change-set-id>` command, 21's traceability report, and 23's release-gate report all read.
- **Coverage ratchet store** — persists the monotonic coverage floor per project; consumed by 23 (re-verified, never re-designed, at release scale).
- **Flake-quarantine registry** — active/expired quarantine entries with expiry timestamps, built from journaled `evidence_pointer` history; consumed by 23 (re-run against the release candidate).
- **`gates-conformance` CI job** — the named, standalone-runnable seeded-fault matrix (mirrors 15's `perf-conformance` naming precedent). This is the exact artifact 23's "seeded-fault matrices from 14/15/22" bullet re-invokes for this phase.
- **Grading ground truth for 22** — this phase's `EvidenceRecord`/gate-outcome data is available for 22's dev/held-out eval grading. This confirms, from this phase's side, the architectural link 22's own text flags as an unconfirmed inference: `EvidenceRecord`'s existing command/exit-status/object-ID shape (above) is sufficient as eval ground truth; no interface beyond the `EvidenceRecord` emission already described here is needed.

## Interfaces consumed

- **From 12** (`packages/detect`, direct dependency — 12's own header: "Unlocks: 14 (gate selection)"): `StackEvidence` (02 schema, populated by 12) — drives adapter/gate-category selection. Content-addressed capability store (`$XDG_CACHE_HOME/crabgic/<project-hash>/capability-store/`, pinned in 04) — this phase's scanner binaries (semgrep, gitleaks, osv-scanner, Syft where SBOM applies) resolve as digest-pinned entries from it ("tools digest-pinned via 12" — this phase's own In-scope text); a tool whose resolved digest no longer matches fails the gate closed rather than running a stale/tampered binary.
- **From 13** (`packages/scheduler`, direct dependency): `TaskPacket` dispatch — per-work-unit gate execution (TDD/coverage/stack-native tests) rides inside the same worker session/`TaskPacket` as the work unit's own implementation, via 13's `TaskPacket.gates` field (13 §In scope) carrying this phase's definitions/thresholds; final-candidate re-verification (work item 6) dispatches as its own `TaskPacket` through 13's executor — 13's own model-routing policy applies, this phase introduces no new role. Artifact store — raw tool output (coverage lcov files, SAST/secret-scan JSON reports) stored as bounded artifacts; this phase's `EvidenceRecord.artifactDigests` reference them, never inlining raw output. Attempt policy — a gate failure is the "new diagnostic evidence" 13's repair-attempt policy (one initial + two evidence-driven repairs) requires before a repair dispatch; this is a journal-checked evidentiary requirement only, never a claim that repair evidence must be rendered/lint-passed text (17's renderer is out of scope for this relationship entirely — see Gap 6 rationale).
- **From 11** (transitively via 13, through `Requirement` IDs riding on `ChangeSet`/`WorkUnit` — no new dependency edge, since 13 already depends on 11; 11's own text already names this: "`Requirement` IDs consumed by 14 (requirement → evidence resolution)"): stable `Requirement` IDs — this phase's `EvidenceRecord`s resolve bidirectionally against them.
- **Ambient, via `packages/contracts` (02)** (same convention every phase uses, not a direct Depends-on edge): `EvidenceRecord`, `ProjectProfile`, `WorkUnitAttemptStatus` (Gap 4 — this phase's gate verdicts are the evidentiary basis 05/13 use to drive an attempt's `succeeded`/`failed` status; this phase never transitions the enum itself), `JournalEntryType` member `evidence_pointer` (Gap 5), run-lifecycle states `verifying`/`final_verifying`; `packages/testkit` fixture builders (fake engine, fake `TaskPacket`s) for gate-framework fixtures.

## Work items

1. Gate framework core: registry (register/list/fire-by-tag), the risk-tag vocabulary above, `EvidenceRecord` emission, journaled as `evidence_pointer`. Failing-first: a stub gate registers and fires through the registry, before any real check (TDD/coverage/security) exists.
2. TDD-evidence gate, cooperating with 13's scheduler dispatch and repair-attempt policy. Failing-first: a fixture attempt missing a red-baseline `EvidenceRecord` for its `Requirement` is rejected.
3. Coverage adapters (lcov/istanbul, go cover, pytest-cov) + ratchet store, with `StackEvidence`-driven adapter selection. Failing-first: one parser fixture per adapter at a known percentage; a ratchet-regression fixture (recorded floor 82% → new run 79%) blocks.
4. Security adapters (semgrep, gitleaks, osv-scanner) with normalized findings + CRITICAL/HIGH-blocks policy, plus root-cause policy detectors; tool binaries resolved via 12's capability store. Failing-first: one seeded-finding fixture per adapter, red before the adapter exists.
5. Flake detector + quarantine registry with expiry, built on the registry/`EvidenceRecord` primitives from item 1. Failing-first: a scripted flaky-then-passing fixture is marked `unstable`, never silently green.
6. Final-candidate orchestration: re-fire the full registered gate set (this phase's own plus any external registrants) against the exact integrated object ID (08), dispatched as its own `TaskPacket` through 13's executor. Failing-first: fake-engine E2E where a per-work-unit gate already passed, but only the rerun against the truly-integrated object ID is asserted as the actual pass condition.
7. `engine-conformance` binding gate (adaptation §9 delta row 9): resolve the attempt's dispatching engine version, look up the journaled green `engine-live` run record for that exact version (engine version + run ID + suite digest, 06), bind the reference into the `ChangeSet`'s `EvidenceRecord`, and fail closed when no matching green record exists — never a per-`ChangeSet` re-run of the permission/hook/sandbox probes themselves. Failing-first: a fixture attempt whose engine version has no green `engine-live` record fails the gate before the pass path exists.

## Test plan

- **Unit:** per-adapter coverage-parser fixtures (lcov/istanbul, go cover, pytest-cov) against known-percentage fixtures; `EvidenceRecord` schema round-trip per gate type; ratchet-regression fixture blocks.
- **Property:** fast-check over randomized coverage-history sequences — the ratchet floor is monotonic non-decreasing regardless of insertion order; fast-check over randomized gate-registration order — dispatch is tag-keyed, never order-dependent.
- **Integration:** scheduler dispatch of a gate-bearing `TaskPacket` on the fake engine (`packages/testkit`); final-candidate re-verification fixture — a per-work-unit gate that already passed is re-fired against the truly-integrated object ID and asserted as the actual pass condition (proves final-candidate re-verification never trusts a cached per-work-unit result); `StackEvidence`-driven selection fixture — a pure-Go fixture repo never invokes a JS-specific SAST ruleset.
- **Conformance:** registry-extensibility test — a stub external gate registers and fires at `final_verifying` with zero code change inside `packages/gates` (the mechanism 15/21 rely on); every emitted `EvidenceRecord` round-trips through 02's `JournalEntryType` discriminated-union exhaustiveness check (shared CI job with 02/04, mirroring 22's identical exit criterion).
- **Security:** planted secret (AWS-shaped test key in a fixture file) → gitleaks fixture blocks; known-CVE test double in a fixture manifest → osv-scanner fixture blocks; an intentionally vulnerable pattern in a fixture source file → semgrep fixture blocks; a disabled-check diff (fixture commenting out an assertion / widening to a bare except) → root-cause detector flags; a scanner binary whose digest no longer matches 12's pinned entry → resolution fails closed rather than running a stale/tampered binary (mirrors 12's own unsigned-digest-swap vector).

## Exit criteria

**Closeout pass 2026-08-02:** 8/9 ticked against recorded evidence; the 1 unticked box below is an
open defect — see `docs/evidence/criteria-closeout/defects/14-ratchet-property-report-not-ci-archived.md`
and that box's own note. Machine-readable index: `docs/evidence/criteria-closeout/phase-14.json`.
No box carries a wording correction: every criterion here matches what the code does, and the one
that does not match its own evidence *channel* is left unticked rather than reworded.

Shared citations reused by several boxes below. **`gates-conformance` run
[30741826035](https://github.com/WitchyNibbles/crabgic/actions/runs/30741826035)**, green at
`eabb65a` — its `gates-conformance (ubuntu-latest)` job
([91480518492](https://github.com/WitchyNibbles/crabgic/actions/jobs/91480518492)) runs the two
suites this phase's own workflow names, as two separately named steps. **`CI` run
[30741826008](https://github.com/WitchyNibbles/crabgic/actions/runs/30741826008)**, green at the
same commit — its `unit-test+coverage (ubuntu-latest)` job
([91480519773](https://github.com/WitchyNibbles/crabgic/actions/jobs/91480519773)), step "test with
80% line+branch coverage gate", executed 625 test files / 6155 tests, and the step log names every
`@crabgic/gates` suite cited below individually (job-log lines 539–589). Scoped local re-runs of the
cited suites, captured verbatim at the closeout commit, are committed as
`docs/evidence/phase-14/closeout-c*.txt`.

- [x] `gates-conformance` CI job blocks correctly on all seven seeded fixtures independently: coverage regression, flaky-then-passing test, planted secret, vulnerable dependency, SAST finding, disabled-check diff, missing green `engine-live` record. — **Evidence (2026-08-02):** `.github/workflows/gates-conformance.yml:43-44` is the named job's own step, run on every push and PR; `packages/gates/src/gates-conformance.test.ts` carries one `it()` per named fixture, in the criterion's own order, each asserting the BLOCK — `:79` coverage 82%→79% `expect(result?.verdict.passed).toBe(false);`, `:94-95` flake `unstable` + blocked, `:121` a planted 20-char `AKIA`-prefixed key (the literal is not reproduced here — this repository's own pre-commit secret scanner rejects it even inside prose), `:157` `CVE-2024-22222` CRITICAL, `:187` semgrep `ERROR`, `:201` commented-out `assert(...)`, `:212` engine-conformance with no green record — and `:35-42`'s fresh journal plus a per-test `createGateRegistry()` is the "independently" clause. Anti-vacuity controls live in the sibling suites, because every fixture here asserts a failure: `security-gate.test.ts:323` (all three scanners pass on clean reports), `:168` (the same disabled-check diff passes when advisory), `coverage-gate.test.ts:38`/`:133`, `flake-gate.test.ts:34-35`, `engine-conformance-binding.test.ts:94`. `gates-conformance` run 30741826035 green at `eabb65a`, job-log line 201; `docs/evidence/phase-14/closeout-c1-c9-gates-conformance-matrix.txt`. Scope note: all seven are parser/policy fixtures — no scanner binary is executed anywhere in this phase (`docs/evidence/phase-14/README.md` §"What is fixture-modeled vs live") — though the digest-pin resolution runs against a real `@crabgic/detect` `CapabilityStore`.
- [x] Coverage ratchet floor is monotonic non-decreasing across a simulated multi-`ChangeSet` history (fast-check property report, CI-archived). — **Evidence (2026-08-06):** split criterion; only the archival half was ever in doubt. The property runs in the default `npm test` fan-out and is non-vacuous — `packages/gates/src/coverage/ratchet.property.test.ts:82` closes the fast-check assertion that the floor never decreases, checked incrementally after every observation on a real on-disk journal, with the order-independence property above it as its anti-vacuity partner. "CI-archived" was simply untrue: no workflow uploaded a report. `gates-conformance.yml` now produces one per matrix leg — run [31087616366](https://github.com/WitchyNibbles/crabgic/actions/runs/31087616366) line 266 shows the property executing, and the artifacts `ratchet-property-report-ubuntu-latest` (964 bytes) and `ratchet-property-report-ubuntu-24.04-arm` (966 bytes) exist, uploaded `if-no-files-found: error`.
- [x] `Requirement → EvidenceRecord → exact object ID` resolves in both directions for a fixture `ChangeSet` (named integration test). — **Evidence (2026-08-02):** `packages/gates/src/requirement-resolution.test.ts` — `:65-66` `expect(evidence!.objectId).toBe("exact-object-under-test"); expect(evidence!.requirementId).toBe(requirement.id);` is the record→requirement→object-id half, and `:70-73` is the other, resolved from the requirement id ALONE (`findEvidenceForRequirement(tj.store, requirement.id)`, `expect(foundViaJournal).toHaveLength(1)`, `expect(foundViaJournal[0]?.objectId).toBe("exact-object-under-test")`). `packages/gates/src/evidence.ts:76-80` shows the reverse leg is a real journal scan on a real field, not a lookup table the test populated. `CI` run 30741826008 green at `eabb65a`, job-log line 575; `docs/evidence/phase-14/closeout-c3-c8-core-suites.txt`. Honesty note recorded in the index: `:63-64` of that test is VACUOUS (it asserts a literal it wrote one line earlier) and is deliberately not cited — nothing in `packages/gates` populates `Requirement.evidenceRecordIds`, which is 11's field per this phase's own §Out of scope.
- [x] A rerun-until-pass result is marked `unstable`; an explicit quarantine suppresses blocking until its recorded expiry, after which it reverts to blocking (flake-registry expiry test). — **Evidence (2026-08-02):** three clauses, three cases in `packages/gates/src/flake-gate.test.ts`, each with an injected clock rather than a wait — `:54-55` rerun-then-pass is `unstable` AND blocks; `:80-81` an active quarantine (`expiresAt: "2099-01-01T00:00:00Z"`) suppresses the block while LEAVING the `unstable` mark, so it is not laundered into a clean pass; `:106` an expired quarantine (`expiresAt: "2020-01-01T00:00:00Z"`, otherwise the identical fixture and clock) reverts to blocking. The named flake-registry expiry test is `packages/gates/src/flake/quarantine-registry.test.ts:43` (`expect(afterExpiry).toBeUndefined();`) with its unexpired control at `:29` and a latest-entry case at `:58`. `unstable` is discriminating, not constant: `flake-gate.test.ts:34-35` (clean pass) and `:125-126` (genuine repeated failure) both assert it is undefined. `CI` run 30741826008 green at `eabb65a`, job-log lines 563 and 582; `docs/evidence/phase-14/closeout-c3-c8-core-suites.txt`.
- [x] TDD gate rejects a fixture attempt lacking a red-before-green `EvidenceRecord` pair for the same `Requirement`. — **Evidence (2026-08-02):** `packages/gates/src/tdd-gate.test.ts:47-48` rejects a GREEN candidate (`exitStatus: 0`) purely for the missing red half (`expect(result?.verdict.detail).toMatch(/no red-baseline/i);`); `:347` proves the requirement scoping (a genuine red baseline for a different `Requirement` does not satisfy this one); `:270` proves the ordering — the gate's own earlier failing verdict, journaled indistinguishably from a real baseline, does NOT retroactively satisfy its own precondition, because `hasRedBaseline` is cut off at the dispatch boundary (`:211` false before it, `:212` true after). The gate can pass (`:84-85` `red-baseline confirmed`; `:311` with the baseline strictly before the boundary), the green half is genuinely required (`:121` `still failing`), and the red half cannot be forged (`:134` `RedBaselineNotFailingError`). `CI` run 30741826008 green at `eabb65a`, job-log line 541; `docs/evidence/phase-14/closeout-c3-c8-core-suites.txt`.
- [x] A stub external gate registers into the registry and fires at `final_verifying` with zero code change inside `packages/gates` (registry-extensibility conformance test). — **Evidence (2026-08-02):** `packages/gates/src/registry.test.ts:115-142` — a locally-defined `registerExternalGate` calls the same public `register()` under the `performance` tag (15's, which nothing in this package populates), fires at `stage: "final_verifying"`, and `:135-137` assert exactly one result, named `15-external-perf-gate`, with `evidence.gateTag === "performance"`; `:140-141` shows it is also reachable through `fireAll`, the final-candidate path. `registry.ts:60-63` has no allowlist and no branch on `gate.name` — a registrant is a `(tag, name, handler)` triple. **Amended 2026-08-18:** that signature is now `register(tag, name, handler, options?)` at `registry.ts:96`, the optional argument being `GateRegistrationOptions` — how a gate declares itself `perWorkUnit` so final-candidate verification skips it. The criterion is unaffected and was re-verified: the argument is optional, a registrant declares it about itself, and nothing still keys off a registrant's identity, so an external gate registers and fires with zero code change inside `packages/gates`. `docs/evidence/criteria-closeout/phase-14.json` carries the re-anchored fragments. Real-world corroboration: phase 24's `criteria-seal-gate.ts:47` registers under the existing `acceptance` tag, and `git show --stat 663dd08 -- packages/gates/src/registry.ts packages/gates/src/risk-tags.ts packages/gates/src/types.ts` is EMPTY. `CI` run 30741826008 green at `eabb65a`, job-log line 556; `docs/evidence/phase-14/closeout-c3-c8-core-suites.txt`. Two boundaries recorded in the index: "external" is simulated (no package outside `packages/gates` imports `createGateRegistry` at HEAD — 21's material landed inside this package), and "zero code change" holds for the 13 existing tags; a NEW tag would require `risk-tags.ts`.
- [x] Every emitted `EvidenceRecord` round-trips as a `JournalEntryType.evidence_pointer` entry through 02's discriminated-union exhaustiveness check. — **Evidence (2026-08-02):** `packages/gates/src/conformance.test.ts:85-91` fires a gate, reads the journal back, re-parses the payload through 02's own `EvidenceRecordSchema` and asserts `expect(reparsed).toEqual(result?.evidence);` — a round trip, not two constructions; `:21` is the `Record<JournalEntryType, boolean>` exhaustiveness literal `tsc -b` enforces, with `:49` pinning it to `JOURNAL_ENTRY_TYPES` and `:56` pinning the members this package actually writes. "Every" is structural, not a sample: `evidence.ts:51-56` is the single append and `registry.ts:53-56`'s `fireOne` its sole caller, and the package's only other `EvidenceRecord` writers — `tdd-gate.ts:74-76` and 21's `remote-evidence-pointer.ts:132-134` — both use `evidence_pointer` too. `CI` run 30741826008 green at `eabb65a`, job-log lines 572 and 562; `docs/evidence/phase-14/closeout-c3-c8-core-suites.txt`. Checked against interface-ledger Gap 5's Resolution (2026-08-01): `coverage/ratchet-store.ts:138-144` and `flake/quarantine-registry.ts:54-57` do write `adjudication_decision`, exactly as that Resolution and the §Risks bullet below record — but their payloads are ratchet observations and quarantine entries, not `EvidenceRecord`s, so this criterion's subject is untouched by that correction.
- [x] Final-candidate re-verification fires against the truly-integrated object ID rather than trusting a cached per-work-unit result (integration test with a deliberately stale per-work-unit pass). — **Evidence (2026-08-02):** `packages/gates/src/final-candidate.test.ts:45-99` — a stateful handler that passes ONLY for the stale per-work-unit object id; the `verifying` firing against it passes (`:67`), then `fireFinalCandidateVerification` against the regressed integrated id fails, and `:84` `expect(finalResults[0]?.evidence.objectId).toBe(REGRESSED_INTEGRATED_OBJECT_ID);` is what pins "fires against the truly-integrated object ID" rather than merely "reports failure". `:89-98` asserts the earlier passing `EvidenceRecord` is STILL in the journal, so the failure comes from a genuine re-fire and not from erasing history. Two structural guards: `:110` an empty registry throws rather than passing vacuously (`[].every(...)` is `true`), and `:126` the full gate set across four tags fires, never a subset. `final-candidate.e2e.test.ts:80-87` carries the same scenario through `@crabgic/scheduler`'s real `dispatchAttempt` + `FakeEngineAdapter`. `CI` run 30741826008 green at `eabb65a`, job-log lines 560 and 557; `docs/evidence/phase-14/closeout-c3-c8-core-suites.txt`. Recorded in the index: the E2E's own `expect(outcome.kind).toBe("failed")` follows from the fake engine's script and evidences propagation, not the regression — the unit test is the bearer.
- [x] `engine-conformance` gate fails closed on the missing-green-record fixture, and a matching green `engine-live` record's run ID round-trips into the fixture `ChangeSet`'s `EvidenceRecord` — named test `engine-conformance-binding.test`, run in the `gates-conformance` CI job. — **Evidence (2026-08-02):** `packages/gates/src/engine-conformance-binding.test.ts` is that named file; `:70-71` fails closed against an empty journal with `expect(result?.verdict.detail).toMatch(/failing closed/i);`, and `:96-98` is the round trip — `expect(result?.evidence.artifactDigests).toContain(...)` for both `engine-live-run-id:<runId>` and `engine-live-suite-digest:<digest>`, on the fired gate's OWN emitted `EvidenceRecord`, bound to the ChangeSet's object id. Two negative controls at `:115` (green record, wrong engine version) and `:137` (right version, `exitStatus: 1`) make "matching" mean something. The channel clause is wired as its own named step, `.github/workflows/gates-conformance.yml:45-46`, and executed by `gates-conformance` run 30741826035 job 91480518492, job-log line 215. Drift check for this closeout: `engine-conformance-gate.ts:31`'s locally re-declared `ENGINE_LIVE_COMMAND` is still byte-identical to its producer, `packages/engine-claude/src/live/live-harness.ts:874` — the coupling the evidence README records as Deviation 2 has not drifted. `docs/evidence/phase-14/closeout-c1-c9-gates-conformance-matrix.txt`. Fixture-modeled by design (no `@live` run happened here and none was run for this closeout); the gate binds a version-grain verdict, per §Risks below.

## Risks & open questions

- **`StackEvidence` vs `ProjectProfile` (resolves 12's flagged gap):** 12's own Risks section flags that neither 14 nor 15 names `StackEvidence` verbatim, leaving the `ProjectProfile`↔`StackEvidence` relationship unstated. This file's In-scope "Test execution" bullet is the first to make it explicit: `ProjectProfile` supplies *how* to run a stack's own commands; `StackEvidence` supplies *whether* a gate category applies at all. Flagging so 15's author and the reconciler can adopt the same split rather than re-deriving it.
- **Risk-tag vocabulary is a design inference, not stated verbatim upstream:** tying registry tags to `IntentContract`'s named sections (plus this phase's own `tdd`/`coverage`/`flake` defaults) is this phase's own read of 11's section list and 15's "under the IntentContract's existing `performance` section tag" phrasing — internally consistent with both, but not itself asserted anywhere before this file.
- **No dedicated `JournalEntryType` member for gate/flake evidence, by design:** this phase's journal writes map onto two existing members — `evidence_pointer` for gate `EvidenceRecord`s (tdd, engine-conformance, remote evidence), and `adjudication_decision` for the coverage ratchet and the flake-quarantine registry, whose records carry a discriminator in `payload.decision` and their structured detail JSON-encoded in `payload.rationale`. 02/Gap 5's union is closed at exactly 13 members and Gap 5's own rationale rejected a 14th. **Mirror citation corrected 2026-08-01:** this bullet used to say "mirrors 12's identical treatment of `capability.audit` decisions," which was true of the conclusion and false of the effect — 12 reached the same *ruling* but journaled **nothing** for its verdicts until interface-ledger Gap 5's Resolution (2026-08-01). 12 now writes `adjudication_decision` entries under a `capability_audit:` discriminator, following this phase's shape verbatim; the mirror is real from that date, not before it.
- **Engine conformance stays version-grain by design (adaptation §9 delta row 9):** permission/hook/sandbox conformance *execution* remains version-grain only — 06's `engine-live` CI job plus 23's release re-run; this phase's `engine-conformance` gate adds per-`ChangeSet` *binding* of that version-grain verdict into the exact-revision evidence set, never per-`ChangeSet` re-execution of the probes. Its journal writes map onto the existing `evidence_pointer` member, consistent with the closed-union posture above — no 14th `JournalEntryType` member.
- **Adaptation §10 risk 9 (subscription-auth, token-based accounting):** final-candidate re-verification is itself a worker dispatch consuming turns under the owner's subscription, not a metered budget — track token/turn usage from the result `usage` block, never `--max-budget-usd`, mirroring 15's and 22's identical treatment.
- **No new unconfirmed Claude Code engine facts:** gate execution inherits its worker sandbox, dispatch, and session routing entirely from already-resolved 03/06/13 work; nothing here needs a `docs/engine-baseline.md` citation (same posture as 15/22's own Risk notes).
- **Gap 6, confirmed no-op for this phase:** the binding resolutions explicitly rejected tying this phase's repair-dispatch "new diagnostic evidence" requirement to 17's rendered/lint-passed review-comment template — that evidence is journal-checked only (see Interfaces consumed → 13). This file introduces no relationship to `packages/renderer` (17).
