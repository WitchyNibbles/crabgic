# Phase 06 — Claude Code worker runtime (SDK transport)

| | |
|---|---|
| **Depends on** | 03, 05 |
| **Unlocks** | 10, 11, 13 |
| **Sources** | adaptation §0 (transport/auth/model-routing/plan-limit decisions), §3.1–§3.3, §4.3–§4.6, §5.1–§5.3, §5.6–§5.7, §9 (`EngineAdapter` capability delta, new test-matrix items), §10 risks 1–6 & 9–10, Appendix A, Appendix B; `docs/engine-baseline.md` (phase 00 output) |
| **Primary package** | `packages/engine-claude` |

## Goal

Before this phase, `packages/supervisor` (05) spawns only 03's fake engine, and 03's `AdjudicationCallback` slot, `resume`, and version gate have no real policy behind them. When this phase is done, `packages/engine-claude` is the first real implementation of 03's `EngineAdapter`: workers run via `@anthropic-ai/claude-agent-sdk` under `compileEnvelope`'s output, with subscription OAuth, a journal-first `AdjudicationCallback`, schema-validated `WorkerResult`, `SessionRef`-based crash/repair recovery, and `limitSignal` detection — proven against the real, pinned Claude Code engine, not only the fake.

## In scope

- **Spawn path (§5.3, confirmed v1 transport):** implements `EngineAdapter.spawn(packet, profile, adjudicate) → WorkerHandle` over Agent SDK `query()`: `cwd` = supervisor-provisioned worktree (never `.claude/worktrees/` or `isolation: "worktree"` — those stay manager-side only, §3.2), `env` sanitized + per-worker `CLAUDE_CONFIG_DIR` (05-provisioned) + injected `CLAUDE_CODE_OAUTH_TOKEN` (mechanism per phase-00's auth-spike verdict), `settingSources: []` always passed explicitly (never the default, §10 risk 3), `WorkerSdkOptions` (03's compiled output) passed through verbatim, `systemPrompt` preset + role preamble, `maxTurns` from `packet`, model routed per role (balanced defaults, overrides only via approved envelope), a pre-generated `SessionRef`/session UUID journaled (`session_assignment`) before the process starts. This phase declares the `@anthropic-ai/claude-agent-sdk` dependency with an exact version per 01's `engine-pin-lint` policy (01 §In scope — build-time exact pin, complementary to this phase's runtime engine-baseline version-range gate).
- **Gateway wiring (Gap 11, Gap 2):** `WorkerSdkOptions.mcpServers` is keyed `GATEWAY_MCP_SERVER_NAME` with `strictMcpConfig: true` (already set by 03's compiler off the same constant). This phase's spawn path configures that entry as a connection to the `crabgic gateway mcp` process (Gap 2's fixed CLI-invocation convention — the identical external-process shape 10's `.mcp.json` entry already uses), never a hand-typed `"crabgic_gateway"` literal. See Risks for why this is external-process, not an in-process import of `packages/gateway`.
- **`AdjudicationCallback` — real implementation** (03 defines the call shape; 05 stubs it; this phase answers it): every tool call is routed to 05's journal-teed bus for a decision **before** it takes effect (`adjudication_decision` journal entry written first, then allow/deny/`updatedInput` returned); a bridge failure (crash, timeout) resolves to **deny**, never a silent allow. PostToolUse audit hook; SessionEnd hook captures the transcript as an evidence pointer.
- **Results:** `WorkerResult` schema enforced via `--json-schema`/`structured_output`; gateway `result.submit` retained as belt-and-suspenders (dotted form, Gap 8 — unchanged); schema violation → typed failure entering the repair-attempt path; usage/turn accounting captured (dollar budgets stay informational under subscription auth, §5.7).
- **Recovery:** implements `EngineAdapter.resume(sessionRef, adjudicate) → WorkerHandle` — reconnects the same `SessionRef` for crash recovery and rate-limit re-dispatch; `forkSession` starts an isolated session for repair attempts that must not touch the original transcript (§4.5).
- **Limit signals:** detect the rate/usage-limit shape phase 00 recorded; emit a typed `limitSignal` `EngineEvent`. Parking policy itself is 13's, not built here.
- **Version gate:** `EngineCapabilities.engineVersion` checked against `docs/engine-baseline.md`'s accepted range; `spawn`/`resume` refuse outside it.
- **`@live` conformance:** wire the `engine-live` CI job (inert placeholder from 01, Gap 15) to run the `@live`-tagged suite — 03's envelope-conformance fixture set replayed against the real, pinned engine; fake-vs-live parity re-asserted.

## Out of scope

- `compileEnvelope`, the `EngineAdapter`/`EngineEvent`/`EngineCapabilities`/`WorkerHandle`/`SessionRef`/`AdjudicationCallback` type definitions, the fake engine, and golden settings artifacts — all owned by 03 (`packages/engine-core`, `packages/testkit`); this phase consumes them, it does not define or regenerate them.
- Supervisor lifecycle, UDS protocol, registries, ring buffer, per-worker `HOME`/`TMP`/`CLAUDE_CONFIG_DIR` provisioning, and the `AdjudicationCallback` *stub* — owned by 05; this phase supplies the real answering policy behind that stub, it is not a replacement for 05's own code.
- Rate-limit parking policy (backoff timers, the `WorkUnitAttemptStatus: parked:rate_limit` transition itself, restart-safe re-dispatch scheduling) — owned by 13; this phase only emits the raw `limitSignal` event.
- Gateway MCP tool *implementations* and the `crabgic_gateway` tool registry itself (`tracker.*`, `observability.*`, `evidence.get`, `evidence.attach`, `result.submit`'s server-side handler, forwarded `run.status`/`run.cancel`) — owned by 16 (`packages/gateway`); workers reach these only as an MCP client of the external `gateway mcp` process, never by importing `packages/gateway`.
- Manager-side session, subagents (`eo-explore`, `eo-reviewer`), advisory hooks, slash commands — owned by 10; the manager session runs under ordinary interactive permissions, never a compiled worker profile.
- The CLI command surface (`resume`, `cancel`, `gateway mcp`, doctor's own version/hermeticity/sandbox self-tests) — owned by 09; per 03's own note, 09's doctor checks are direct engine probes, not imports of this phase's package. This phase supplies no CLI surface itself.
- Journal encoding/schema (ndjson codec, chain fields, segment rotation) — owned by 04; this phase only writes entries through 05's already journal-teed bus.
- Contract assembly, approval-token minting, `CapabilityManifest` assembly — owned by 11.

## Interfaces produced

- **`packages/engine-claude`** — the first real implementation of `EngineAdapter` (`spawn`, `resume`, `cancel`, `capabilities() → EngineCapabilities`) over the Agent SDK. Per 03's own accounting, this package — not `packages/engine-core` directly — is "the surface 10/11/13/23 rely on downstream." The specific piece each named consumer relies on:
  - **10** — `EngineCapabilities.engineVersion` plus the version-range gate, reused for the plugin's doctor/install compatibility checks and its own `CapabilityManifest` version pin (10's own stated reuse); the `engine-live` CI job wiring below is also reused directly by 10's `@live` plugin-load smoke test.
  - **13** — the `limitSignal` `EngineEvent` ("on `limitSignal` (06): park → backoff → re-dispatch via `resume`," 13's own text); `resume`/`forkSession` for re-dispatch and repair-attempt isolation; the `WorkerResult` pass/fail outcome feeding its attempt policy.
  - **23** — `@live`-tagged conformance suite, version-range gate, `resume`/`forkSession`, `session_id` (all four already named on 23's own Interfaces-consumed row for this phase).
  - **11** — `EngineAdapter.capabilities()`'s `engineVersion`, `supportsJsonSchema`, `supportsSessionResume` (field names per Gap 7), read at approval-preview time to populate `CapabilityManifest`'s pinned-engine entry and caption `PerformanceContract` budget previews (per 11's own Interfaces-consumed section).
- **`limitSignal` `EngineEvent`** — emitted when the rate/usage-limit shape phase 00 recorded is detected; carries the reset-window hint and scope (work-unit vs. account-wide) 13's scheduler needs.
- **`session_id`/`SessionRef` assignment + `resume`/`forkSession` wiring** — a supervisor-chosen UUID journaled (`session_assignment`) before spawn; `resume` reconnects the same `SessionRef` for crash recovery and rate-limit re-dispatch; `forkSession` isolates repair attempts from the original transcript.
- **`WorkerResult` validation outcome** — schema-validated `structured_output`, or a typed schema-violation failure — feeds 13's "one initial + two evidence-driven repairs" attempt policy.
- **Real `AdjudicationCallback` + evidence bridge** — journal-first `adjudication_decision` entries; SessionEnd transcript capture as an `evidence_pointer` — transcripts land in 13's artifact store once that phase lands; re-run live by 23 ("Hook enforcement | 03/06").
- **`engine-live` CI job wiring + `@live`-tagged suite** (Gap 15) — 10's own `@live` plugin-load smoke test runs in this same CI job; reused wholesale by 23 at release. Each green `engine-live` run is additionally journaled as a queryable record — `{engineVersion, runId, suiteDigest}` — consumed by 14's `engine-conformance` binding gate, which requires a journaled green record for the attempt's exact engine version and fails closed without one ("engine version + run ID + suite digest, emitted by 06," 14's own In-scope gate framework and work item 7).

## Interfaces consumed

From **03** (`packages/engine-core`, `packages/testkit`):
- `EngineAdapter` interface this phase implements: `spawn(packet, profile, adjudicate)`, `resume(sessionRef, adjudicate)`, `cancel(handle, deadline)`, `capabilities()`.
- `EngineCapabilities`, `WorkerHandle`, `SessionRef`, `AdjudicationCallback` — the call-shape types.
- `compileEnvelope`'s output — `CompiledWorkerProfile`, materialized as `WorkerSettingsJson` and `WorkerSdkOptions` — mirrored into this phase's concrete `query()` call, not regenerated.
- Golden settings artifacts (three canonical envelopes: read-only, standard implementation, network-granted) — this phase's worker-profile assembler is tested against these byte-for-byte for real-engine drift.
- The fake engine + envelope-conformance fixture format (per-envelope trace + expected per-layer verdict) — reused byte-identical as this phase's `@live` suite input, and for fake-vs-live parity.

From **05** (`packages/supervisor`):
- The journal-teed UDS event bus — this phase's `AdjudicationCallback` implementation and audit/evidence hooks call into it; every decision is journaled before it takes effect.
- Worker registries (the `session_id` slot on the worker/run record) and per-worker `HOME`/`TMP`/`CLAUDE_CONFIG_DIR` provisioning — this phase points the SDK's `env`/`cwd` at what 05 already provisioned.
- The stubbed `AdjudicationCallback`, the crash-detection → journaled-attempt-record → recovery-hook slot 05 already built ("resume/fork policy lands in 06/13," per 05's own text), and the exact integration point 05 spawns workers "via EngineAdapter (fake engine until 06)" — this phase supplies the real answering policy and replaces the fake at that point; 05's own code does not change.

From **02** (`packages/contracts` — ambiently available to every phase, the same convention 10 and 03 apply to `GATEWAY_MCP_SERVER_NAME` citing Gap 11; no new dependency edge):
- `GATEWAY_MCP_SERVER_NAME = "crabgic_gateway"` — this phase's `mcpServers` key references it directly (Gap 11); `WorkerSdkOptions.mcpServers`'s key is already set off the same constant by 03's compiler.
- `TaskPacket` schema — the type of `spawn()`'s first parameter (02 owns the schema; 13 builds instances; matching 03's own framing, this phase's implementation of `spawn` only consumes the type — 13 depends on 06, so no cycle).
- `WorkerResult` schema — the shape `structured_output` is validated against.
- `JournalEntryType` members `session_assignment`, `adjudication_decision`, `work_unit_transition`, `evidence_pointer` — the categories this phase's journaled events fall under (Gap 5); the enum and journal codec stay owned by 02/04.

`docs/engine-baseline.md` (phase 00's output — cited per README's own ground rule, not a phase dependency; 00 is not in this phase's Depends-on, matching 03's identical citation pattern) supplies the tested version + accepted range, the hermeticity/permission/sandbox/session/structured-output probe verdicts, the auth decision record, the rate-limit signal shape, and `spikes/fixtures/` transcripts this phase's parser and `@live` suite are built and tested against — every such fact is a citation, per Hard Rule 1, never an independently asserted claim.

## Work items

1. `EngineAdapter` real implementation skeleton: `spawn`/`resume`/`cancel` over Agent SDK `query()`, translating `WorkerSdkOptions` (03) into concrete call options; wire `mcpServers`/`strictMcpConfig` off `GATEWAY_MCP_SERVER_NAME` (Gap 11) as a connection to the external `gateway mcp` process (Gap 2); inject auth per phase-00's recorded mechanism. First failing test: assemble options for each of 03's three golden canonical envelopes and diff against a golden SDK-call fixture that does not exist yet.
2. `EngineEvent` normalizer: parse real (and fake-engine, for this test) messages into 03's typed stream (`init | assistant | toolUse | result | retry | limitSignal`); build `limitSignal` detection from phase-00's captured rate-limit shape. First failing test: feed phase-00's rate-limit fixture transcript through the parser and assert a typed `limitSignal` event — fails, no parser exists.
3. Real `AdjudicationCallback` over 05's bus: every call adjudicated and journaled (`adjudication_decision`) before it takes effect; bridge failure denies, never allows; PostToolUse audit hook; SessionEnd evidence-capture hook. First failing test: a forged tool call outside the envelope must be denied and the denial journaled before the engine sees a response — fails, no callback implementation exists.
4. Result validation + usage accounting: enforce `WorkerResult` via `structured_output`; malformed output → typed schema-violation failure. First failing test: a deliberately malformed `structured_output` payload must produce a typed failure, not a silent pass — fails, no validator exists.
5. `resume`/`forkSession` + crash-recovery integration test: pre-assign and journal `SessionRef`/`session_id` before spawn; `resume` reconnects the same worktree/session; `forkSession` isolates repair attempts. First failing test: kill -9 a running worker mid-turn, then `resume` — context and worktree state must be intact — fails, no resume wiring exists.
6. Version gate + `@live` wiring: `spawn`/`resume` refuse outside `EngineCapabilities.engineVersion`'s accepted range; wire the `engine-live` CI job (01) to run the `@live`-tagged suite against 03's fixtures on the pinned engine; assert fake-vs-live parity. First failing test: a spawn attempt against a baseline-mismatched version string must be refused before any engine invocation — fails, no gate exists.

## Test plan

**Unit:** worker-profile assembler (golden `WorkerSdkOptions`-to-`query()`-call diff, 3 canonical envelopes); `GATEWAY_MCP_SERVER_NAME` reference check (no hand-typed `"crabgic_gateway"` literal anywhere in `packages/engine-claude`); `EngineEvent` parser against each of phase-00's captured fixtures (clean success, retry/backoff, rate-limit signal, schema-violating result, crash) — each must normalize to its correct typed event.

**Property:** `SessionRef`/worktree round-trip — for any generated UUID + worktree pair (fast-check), `resume(sessionRef, adjudicate)` reconnects to that exact pair, never a substituted one.

**Integration (vs. 03's fake engine):** `AdjudicationCallback` denies a forged call and journals the denial before returning; PostToolUse/SessionEnd hooks fire and an evidence pointer is written; schema-violating result triggers the typed failure path; kill -9 → `resume` continuity; `forkSession` leaves the original transcript untouched; two concurrent same-project-dir sessions with distinct `SessionRef`s never interleave.

**Conformance (`@live`, tagged, run in the `engine-live` CI job):** 03's full envelope-conformance fixture set replayed against the real pinned engine — compound-command/process-wrapper smuggling denied, path escape denied, deny-wins-over-allow holds, sandbox probes (egress denied, UDS reachable, `denyRead ~/.ssh` enforced, masked secret shows only the placeholder); hermeticity (planted rogue settings/hook/`CLAUDE.md`/`.mcp.json` under `settingSources: []` all ignored); structured-output round-trip; sessions (kill-9/resume, fork-session, no-interleave); version-drift gate (refuses an untested `claude --version`). Fake-vs-live parity: identical verdicts across both engines. This phase owns the live half of adaptation §9's Envelope conformance, Hook enforcement, Sandbox, Hermeticity, Structured output, Sessions, and Version drift test-matrix categories (03 owns the unit/property half of the first two; 09 owns the doctor-side half of Version drift/Hermeticity independently, per 03's own note that its checks are direct probes, not imports of this package).

**Security:** masked secret never appears in worker env or transcript (live substring search); `failIfUnavailable` aborts closed when `bwrap` is unavailable; an `AdjudicationCallback` failure (crash/timeout) denies the pending call rather than allowing it by default — the live counterpart of 03's own fake-engine test ("a fake engine attempts a tool call with no `AdjudicationCallback` supplied, or one that throws, must fail closed"); a blanket `mcp__*` deny is never present alongside the gateway allow (Appendix B's footgun, reconfirmed live); the injected `CLAUDE_CODE_OAUTH_TOKEN`/`.credentials.json` fallback never appears outside its own worker's `CLAUDE_CONFIG_DIR`.

## Exit criteria

**Closeout pass 2026-08-05:** 8/10 ticked against recorded evidence. The two unticked boxes
(criteria 1 and 10) are blocked on the `engine-live` CI channel their own text names — see
`docs/evidence/criteria-closeout/defects/06-engine-live-plugin-cli-unresolvable-on-ci.md` and each
box's own note. Machine-readable index: `docs/evidence/criteria-closeout/phase-06.json`.

A scoped local live batch closed four criteria that had never been green. `docs/evidence/phase-06/wi5-live.md`
records that the phase-time attempt made exactly one live invocation (the canary) and aborted at
rate-limit utilization 0.87 — correctly, and it was ruled correct, but it left c2, c3, c4 and c7
unevidenced. This pass ran **exactly four named files** under `--bail 1`, `setsid` and a
process-group watchdog: `Test Files 4 passed (4)` / `Tests 12 passed (12)` / `EXIT=0`. Measured cost,
from a 0.25 s poller recording every distinct engine-process pid rather than from an estimate: **11
engine processes, 10 model-serving invocations, haiku 11/11, zero opus, zero sonnet, 110 s wall
clock, watchdog did not fire.** Predicted beforehand: 10. What made it boundable at all is that none
of the four files can spawn a subagent — every profile they use comes from `compileEnvelope`, which
puts `Agent` in `MANDATORY_FIXED_DENY` (`packages/engine-core/src/compiler/permission-profile.ts:40`),
and the engine enforces that by catalog-removal of the live `Task` tool, asserted live at
`packages/engine-claude/src/live/envelope-conformance.live.test.ts:259-260`. `npm run test:live` was
never invoked deliberately: it runs all 17 files. Transcript:
`docs/evidence/phase-06/closeout/closeout-live-batch.txt`.

c7 was VACUOUS before that run, and this is the pass's most substantive finding. `fake-live-parity.test`
was green in CI for weeks while comparing the fake engine against `live-verdicts.json` — a file the
fake engine had derived. Two names, one source. The green run regenerated that file from real-engine
observations (`envelope-conformance.live.test.ts:147`, guarded at `:144`), its mtime moved into the
run's window proving it was genuinely rewritten, and `git status --porcelain` on it came back
**empty**. So the parity claim now rests on two independent observations. Had the diff been
non-empty, that would have been a real fake/live divergence: c7 would have gone UNMET and the new
bytes would not have been committed to force the offline test green.

Two criteria name suites that do not carry them, found by mutation probe rather than by reading.
Deleting the version gate from `ClaudeEngineAdapter.resume()` reddened one test — in
`adapter.test.ts`, while all 20 of `version-gate.test.ts`'s stayed green. Perturbing phase-00's
recorded 0.96 utilization reddened one test — in `rate-limit-fixtures.test.ts`, while
`limit-signal.test.ts` stayed green. Both criteria's substance is fully pinned per push, so both
boxes are ticked; the pointer drift is filed as
`docs/evidence/criteria-closeout/defects/06-criteria-name-suites-that-do-not-carry-them.md`.
Probe transcript, with baseline → mutation → restore at one tree and constant test counts throughout
(314/314, 724/724, 314/314, 961/961): `docs/evidence/phase-06/closeout/closeout-bearer-probes.txt`.

Shared CI citation reused by the boxes that rest on the default gate. **`CI` run
[31037500615](https://github.com/WitchyNibbles/crabgic/actions/runs/31037500615)**, green at
`9abc3fd` — its `unit-test+coverage (ubuntu-latest)` job
([92413322812](https://github.com/WitchyNibbles/crabgic/actions/jobs/92413322812)), step "test with
80% line+branch coverage gate", executed 634 test files / 6360 tests (job-log lines 1052–1053) and
names each `@crabgic/engine-claude` suite individually (job-log lines 520–525). Every quoted log line
was byte-compared against the downloaded raw log under the one-space rule. Note that
`gateway-name-reference.test.ts` exists in FOUR packages in that same log (lines 353, 403, 524, 634),
so each citation is anchored on the package label rather than the filename.

Citation re-resolution, and the numbers that make it worth trusting. Every quoted fragment in
`docs/evidence/criteria-closeout/phase-00.json` and `phase-06.json` was located mechanically by a
purpose-built **four-rule** resolver — content, line-anchoring, group consecutiveness, and
repeat-text detection — run against the tree this branch lands as: **86 citations, 80 of them
resolvable paths, 174 marker groups, 329 quoted fragments, 329 content-verified, 230 line-anchored,
27 ranged groups verified consecutive, 41 fragments flagged as repeat-text and therefore
position-verified rather than merely content-verified, 4 resolved through a cross-file marker, 7
matched only after joining wrapped lines, and 0 problems.**

Its first run found **65 problems in this record's own first draft.** Of those, 5 were resolver
defects (English possessives mis-read as opening quotes; same-line multi-fragment groups; a marker
lookahead that stopped at a colon and silently charged a group's fragments to the *previous*
marker; no cross-file `file.ts:NN` handling; no wrapped-line fallback) and **34 were genuine
citation defects** — including three CROSS-FILE quotes attributed to the wrong file, two TEMPLATE
strings (`<path>`) quoted as if verbatim, ten quotes falling outside their own declared span, three
off-by-one comment-block ranges, and a two-line YAML comment quoted as one unmatchable fragment.
All are corrected above. The resolver was then **mutation-tested eight ways** — falsified quote,
moved span, wrong range head with every fragment real, right content in the wrong file, a real
quote pushed outside its span, a marker past EOF, an unattributed cross-file quote, and a
prose-only reword that must NOT fire — **8/8 behaved as required, control included.**

The prose is checked separately, because nothing else reads it: a sweeper resolved every `path:NN`
reference in these annotations and this pass's four defect records — **80 references, 80 resolved,
19 of them bare basenames resolved to a unique tracked file, 0 problems** — after flagging four
genuinely AMBIGUOUS bare basenames (`adapter.test.ts`, `adapter.ts`, each present in two packages),
now qualified with their repository paths. It was mutation-tested four ways including a
section-containment check and its negative control, 4/4.

Job-log quotes were byte-compared against the downloaded raw log under the one-space rule:
**18 fragments, 18 EXACT, 0 mismatched.** That comparer was itself mutation-tested four ways,
including the one nobody thinks of — right count, wrong file. It matters here: `(3 tests)` occurs at
**70 different lines** of this job's log, and `gateway-name-reference.test.ts` exists in four
packages, so a comparer keyed on a count or a bare filename would have matched the wrong suite and
reported success.

Every transcript pins the UPSTREAM BASE `9abc3fd` rather than a branch tip, so its sha resolves for
any reader after this branch merges. `git diff --name-only 9abc3fd -- packages/ e2e/ scripts/
.github/ spikes/` is **empty**, so every code, workflow and fixture file cited above is
byte-identical to the pinned commit and a grep in this tree is a grep at that sha.


- [ ] 03's full envelope-conformance fixture set passes on the pinned live engine — suite `envelope-conformance.live.test`, run in the `engine-live` CI job. — **Left unticked 2026-08-05, defect filed:** `docs/evidence/criteria-closeout/defects/06-engine-live-plugin-cli-unresolvable-on-ci.md`. Split criterion; the first clause is fully evidenced and the second cannot be satisfied by anything local. *Clause 1* — the fixture set passes on the pinned live engine: proved by this pass's green scoped run (`Test Files 4 passed (4)`, `EXIT=0`, engine 2.1.218), with `envelope-conformance` costing exactly two engine spawns of the batch's eleven. *Clause 2* — "run in the `engine-live` CI job": that workflow has **zero runs in its entire history**, and a dispatch today reds on ENOENT before any assertion. Measured chain: `packages/plugin/src/live/plugin-load.live.test.ts:32` and `:65` pass the bare string `"claude"` to `execFile`; both SDK packages report `bin: undefined` and `ls node_modules/.bin | grep -x claude` exits 1; and `.github/workflows/engine-live.yml`'s whole install surface is checkout, setup-node, `apt-get install bubblewrap socat` (`:39`), `npm ci` (`:46`), `npx tsc -b` (`:47`) — nothing puts a `claude` on PATH. Reading clause 2 down to "it passed somewhere" would drop a guarantee, which is `UNMET`, not a wording fix. **Disclosure carried forward from wi5 §5.1**, because it is the honest meaning of "passes" here: 5 of the 7 fixtures are denied at the adapter's own `assertNoFootguns` gate before any engine call and only 2 reach the engine's permission layer, so a reader should not imagine 7 live spawns. Both classes resolve to the overall `deny` every fixture's `expected` names. Transcripts: `docs/evidence/phase-06/closeout/closeout-live-batch.txt`, `docs/evidence/phase-06/closeout/closeout-engine-live-enoent.txt`.
- [x] Masked secret never appears in worker env or transcript — `secret-mask.live.test`. — **Evidence (2026-08-05):** closed by a green run this pass produced — one invocation, haiku, `maxTurns: 3`. The criterion names a suite and **no CI job**, so a green run at the pinned engine is the strongest evidence its own text asks for. Both conjuncts are asserted separately: *transcript* at `packages/engine-claude/src/live/secret-mask.live.test.ts:110` (`expect(transcriptText(result.messages)).not.toContain(REAL_SECRET)`), and *env/filesystem spill* at `:115-118`, which scans the worker's whole worktree and HOME trees for every registered secret and asserts `[]` for each — deliberately EXCLUDING the worker's own `CLAUDE_CONFIG_DIR`, named at `:112-114` as the material's one sanctioned home, so the assertion is about leakage rather than about the credential existing. **The absence assertions are non-vacuous** because `:103-107`'s executed-call guard turns a refused or idle session into a red; without it a session that never ran the probe would satisfy both `not.toContain` calls for free. That is not hypothetical — `docs/engine-baseline.md:41` records the original hermeticity probe being vacuous for exactly this reason. Transcript `docs/evidence/phase-06/closeout/closeout-live-batch.txt`.
- [x] `failIfUnavailable` aborts (fails closed) when `bwrap` is unavailable — `sandbox-unavailable.live.test`. — **Evidence (2026-08-05):** closed by a green run this pass produced, and it is nearly free — **one engine process, zero model invocations**, because the whole point is that `query()` throws at sandbox init before any turn. The criterion names a suite and no CI job. Mechanism and assertion: `packages/engine-claude/src/live/sandbox-unavailable.live.test.ts:63` starves `PATH` to Node's own directory so `bwrap`/`socat` become unresolvable while the SDK can still launch the bundled engine, and `:66` asserts `await expect(collectEngineEvents(handle.events)).rejects.toThrow()`. **Why it is a real probe and not a tautology**, both checked rather than assumed: the host genuinely HAS bwrap (`/usr/bin/bwrap`, 0.9.0, recorded in the transcript's pre-run baseline), so the throw comes from the deliberate starving and not from an absent dependency; and the profile genuinely carries `failIfUnavailable: true`, visible both in the committed `read-only.sdk-call.json` golden the file's header names and, live, in the captured engine command line. Without either, the test would pass for the wrong reason. `rejects.toThrow()` is loose about the error *type*; what it pins precisely is the *direction* — refuse rather than silently degrade to an unsandboxed run — which is the guarantee the criterion names. `PATH` is restored twice over, in `afterEach` (`:46`) and in the test's own `finally` (`:68`), so a throw cannot leave later files starved. Transcript `docs/evidence/phase-06/closeout/closeout-live-batch.txt`.
- [x] kill -9 → `resume` continues in the same worktree with context intact; `forkSession` leaves the original transcript untouched — `crash-recovery.live.test`. — **Evidence (2026-08-05):** closed by a green run this pass produced; six of the batch's eleven engine processes belong to this file, making it the batch's dominant cost. Conjunctive, cited per conjunct. *kill -9 → resume, same worktree, context intact*: the kill is genuinely mid-turn, driven off the observed `sleep` tool_use at `packages/engine-claude/src/live/crash-recovery.live.test.ts:249-252` rather than a wall-clock hold, with `:255` and `:271` guarding that a no-op kill cannot pass as a crash; the three post-resume identity assertions are at `:278-280` (the middle one is the "same worktree" half) and context integrity is `:282`. *forkSession leaves the original transcript untouched*: asserted as literal BYTE identity at `:322`, paired with `:319`'s proof that the fork's own transcript is non-empty so "the fork did nothing" cannot satisfy it. *Concurrent no-interleave* (beyond the criterion's text, and what rules out interleaving being mistaken for context integrity): `:401-403` and `:425-428`, where every absence is paired with a positive control on the same transcript. **The PID-walk kill mechanism had never executed live before this run** (wi5 §8 records it as unexercised) — it behaved. Independent engine-side corroboration that the resume and fork paths really ran: the captured command lines carry `--resume=` and `--fork-session`. `:348-362` records a real earlier vacuity in the concurrent arm, where the engine REFUSED the original token-echo framing and the test failed on its design rather than on the product; that is why the current framing reads a file that genuinely exists on disk. Transcript `docs/evidence/phase-06/closeout/closeout-live-batch.txt`.
- [x] Schema-violating `structured_output` triggers a typed failure entering the repair-attempt path, never a silent pass — `structured-output-violation.test`. — **Evidence (2026-08-05):** clean, and **measured**. The criterion names a specific typed outcome, so the probe is to change the kind and see whether anything reddens — a bare `expect(...).toThrow(SomeError)` passes for every kind and would not be a bearer. Probe C downgraded the typed reason `"absent"` to `"invalid"` in `result-validation.ts` and reddened exactly the two cases that assert it, both in the named suite (`Tests 2 failed | 312 passed (314)` against a `314 passed (314)` baseline; `tsc -b` exit 0, so the measurement is of the new code and not a stale `dist`). All three reasons are asserted by kind AND by reason (`packages/engine-claude/src/structured-output-violation.test.ts:39`, `:62`, `:106`), and "never a silent pass" is asserted as a **sole-valid** property at `:127-138` — the does-not-fail control that rules out "everything is a violation" satisfying the suite equally well. On *entering the repair-attempt path*: the typed union member is the entry token, and it is shown both reached and consumed — `packages/engine-claude/src/adapter.test.ts:486` proves the real adapter's normalized output lands on it, and `packages/scheduler/src/attempt-policy.ts:111` with `packages/scheduler/src/executor.ts:214` show phase 13 routing it into a retry (corroboration; the repair loop is 13's scope). **Carried forward rather than glossed:** `docs/engine-baseline.md:268` records the `error_max_structured_output_retries` path as never observed live, and `packages/engine-claude/src/result-validation.ts:63-66` says so at the constant that names it. The criterion is still fully satisfied because its subject is the OBSERVED shape — `subtype: success` with `structured_output` absent — which `:34`'s describe names and which §5 did observe. CI job-log line 521. Probe transcript `docs/evidence/phase-06/closeout/closeout-bearer-probes.txt`.
- [x] `limitSignal` fires against phase-00's captured (or an equivalently live-triggered) rate-limit shape — `limit-signal.test`. — **Evidence (2026-08-05):** ticked on measured substance, **with a bearer finding disclosed**. Both conjuncts hold and both run per push, but not both in the suite the criterion names — and this pass measured that rather than assuming it. *"limitSignal fires"* is fully carried by `limit-signal.test`, including the error-string fallback channel `docs/engine-baseline.md:378` requires, asserted against the baseline's verbatim sample at `packages/engine-claude/src/limit-signal.test.ts:220-225` with **four** negative controls at `:232-252` that stop it degrading into a substring match on the word "limit". *"Against phase-00's captured shape"* is **not** carried there: probe B changed the recorded 0.96 utilization to 0.97 and `limit-signal.test.ts` stayed **green**, because its assertions are `toEqual({ ...payload })` spreads — tautologies over whatever the constant holds. What carries the conjunct is `packages/engine-claude/src/event-normalizer.test.ts:90-113`, which reads the committed `spikes/fixtures/` transcripts off disk (`:18`) and asserts all 16 recorded samples normalize to a `limitSignal`. **That suite staying green under probe B is the proof of independence, not a gap** — the fixture-backed suite and the hand-typed constants are two genuinely separate sources, so perturbing one does not move the other. `docs/engine-baseline.md:364` independently names `event-normalizer.test.ts` as the intended consumer of this fixture set, so this is the wiring phase 00 asked for. Second, smaller finding: `packages/testkit/src/fake-engine/rate-limit-fixtures.test.ts:11-19`'s first case compares `RECORDED_RATE_LIMIT_PAYLOADS` against its own four members — a tautology over a literal, in a file whose doc comment calls itself a "§8 verbatim schema check"; its value-pinning cases at `:29` and `:39` are the non-vacuous ones and are what probe B reddened. Both filed as `docs/evidence/criteria-closeout/defects/06-criteria-name-suites-that-do-not-carry-them.md`. CI job-log line 520.
- [x] Fake-vs-live parity: identical fixture verdicts across `packages/testkit`'s fake engine and the real engine — `fake-live-parity.test`. — **Evidence (2026-08-05):** **this was the trap criterion, and it was vacuous until this run.** `fake-live-parity.test` was green in CI for weeks while comparing the fake engine's verdicts against `packages/engine-claude/src/live/fixtures/live-verdicts.json` — a file the fake engine had derived. Two names, one source; `docs/evidence/phase-06/wi5-live.md` §6 admits it, and `git log` over that path shows two commits, neither from a live run. The green batch closed it: `packages/engine-claude/src/live/envelope-conformance.live.test.ts:147` rewrites that same committed path from real-engine observations, guarded at `:144` so only a genuinely green run in which every expected verdict was recorded persists anything. The file's mtime moved to 19:21:08Z, **inside** the run's 19:19:32Z–19:21:22Z window, proving it was actually rewritten rather than left alone; and `git status --porcelain` on it came back **empty**, digest still `ada6ddd1cc98cf10146a8c9629b6c95c`. So a fake-derived file and a real-engine-derived file are byte-identical, and the parity claim now rests on two independent observations. wi5 §6 predicted exactly this proof and it held. **The counterfactual was pre-committed:** a non-empty diff would have been a genuine fake/live divergence — c7 → `UNMET`, diff filed as a defect, and the new bytes NOT committed to force the offline test green. **One residual, because it reads as provenance and is not:** the file's `source` field says `"live"` both before and after. It was flipped from an honest `"offline-baseline"` to `"live"` by `29b3c46`, a commit about wiring `learn-*` promotion — unrelated to the live suite, and at a time when no green run existed. Nothing checks that field, and `deriveOfflineBaselineVerdicts` (the function the harness names as the honest branch's source) has **zero call sites**. The mtime, the green run and the clean diff are the provenance; the string is not. Filed as `docs/evidence/criteria-closeout/defects/06-live-verdicts-source-label-not-provenance.md`. CI job-log line 525; transcript `docs/evidence/phase-06/closeout/closeout-live-batch.txt` §4.
- [x] `WorkerSdkOptions.mcpServers` key and `strictMcpConfig` reference `GATEWAY_MCP_SERVER_NAME` byte-for-byte; zero hand-typed `"crabgic_gateway"` literals anywhere in `packages/engine-claude` — `gateway-name-reference.test`. — **Evidence (2026-08-05):** clean on both conjuncts, and the emptiness proof was **measured** rather than trusted. *Both named `Options` members* in one case: `packages/engine-claude/src/gateway-name-reference.test.ts:92` asserts the whole `mcpServers` key array equals `[GATEWAY_MCP_SERVER_NAME]` — `toEqual` rather than `toContain`, so a second stray key also fails — and `:93` asserts `strictMcpConfig` is `true`, over options built by the real assembler from a real compiled envelope (`:72-73`), not a hand-written literal. *Zero hand-typed literals*: `:57` asserts `expect(violations).toEqual([])` over a recursive walk whose root, `SRC_DIR` (`:27`), is the test file's own directory — i.e. `packages/engine-claude/src`, the criterion's exact scope, and not a written-out path that could drift as files move. Emptiness proofs rot; this one **cannot**, because it re-derives its own scope and runs in the default gate on every push. Probe D planted one hand-typed literal and reddened **two** tests in two packages — this bearer and phase 02's independent sole-definition-site scanner in `packages/contracts` (`Tests 2 failed | 959 passed (961)` against `961 passed (961)`). The design detail worth noting: the scan needle is derived from the imported constant (`:26`), so the scanner is not exempt from its own rule and needs no allowlist entry in 02's scanner — a hand-typed needle would have been the very violation the criterion prohibits. Non-vacuity is additionally asserted in-file at `:60-63`. On the wording: "`strictMcpConfig` reference `GATEWAY_MCP_SERVER_NAME` byte-for-byte" reads oddly for a boolean member; both members are asserted, which is what interface-ledger Gap 11 governs, and no wording correction is claimed. CI job-log line 524 — anchored on the `@crabgic/engine-claude` package label because this filename appears in four packages in the same log.
- [x] `spawn`/`resume` refuse to start outside `docs/engine-baseline.md`'s accepted version range — `version-gate.test`. — **Evidence (2026-08-05):** ticked on measured substance, **with the bearer-name finding this pass considers most worth reading**. The substance is fully evidenced, both conjuncts, in the default gate: `spawn` refuses at `packages/engine-claude/src/adapter.test.ts:217` — and additionally proves `expect(calls).toHaveLength(0)` and `expect(orderLog).toHaveLength(0)`, i.e. zero `sdkQuery` invocations and zero journal appends, so the refusal precedes every side effect — and `resume` refuses at `:233`, cited separately because the criterion names both verbs and a spawn-only bearer would have dropped half of it. Production wires the gate at three entry points (`packages/engine-claude/src/adapter.ts:429`, `:453`, `:481`), so `fork` is covered too. The range the refusal is measured against is pinned to the baseline **document** per push: `packages/engine-claude/src/version-gate.test.ts:109` reads the file off disk and `:113` asserts the constants match its headline range. **But the suite the criterion names does not test `spawn` or `resume`.** Probe A settled it by measurement: deleting the gate from `resume()` reddened exactly one test — in `adapter.test.ts` — while all 20 of `version-gate.test.ts`'s stayed green (`Tests 1 failed | 313 passed (314)` against `314 passed (314)`). `version-gate.test` tests the pure helper plus the document-sync check, both genuinely load-bearing and together carrying the criterion's "outside the baseline's accepted range" half; a reader following the criterion's own pointer would simply find no spawn/resume assertion there. That is a **pointer** defect, not a missing guarantee — filed as `docs/evidence/criteria-closeout/defects/06-criteria-name-suites-that-do-not-carry-them.md`. Not pedantry: this repository's own de-grandfathering finding is that coverage migrates between rules with no test failing, and a criterion aimed at the wrong suite is how the next reader fails to notice when the real bearer is deleted. CI job-log line 523.
- [ ] `engine-live` CI job (01) runs the `@live`-tagged suite end to end against the pinned version — evidenced by a green CI run. — **Left unticked 2026-08-05, defect filed:** `docs/evidence/criteria-closeout/defects/06-engine-live-plugin-cli-unresolvable-on-ci.md`. Cannot be closed by anything local, **and cannot be closed by a red run either**, because the criterion's own text says "evidenced by a green CI run". `gh run list --workflow=engine-live.yml` returns exit 0 with empty output: **zero runs in the workflow's entire history**, which is why the fault below has never surfaced. A dispatch today is guaranteed red on ENOENT — the plugin lane sharing this job resolves `claude` from PATH (`packages/plugin/src/live/plugin-load.live.test.ts:32`, `:65`, `packages/plugin/src/live/plugin-inventory-probe.ts:63`), the SDK packages expose no `bin`, and `.github/workflows/engine-live.yml` installs no CLI. **And the red does not spare the spend:** `npm run test:live` carries no `--bail`, so all 17 files run regardless of order — measured, in a zero-spend gate-refused run, as `Test Files 17 failed (17)`. Roughly 60–90 haiku invocations would be spent to reach a millisecond failure, and spent again on the eventual real dispatch. *(This corrects an earlier belief that the engine-claude files run first: measured, `plugin-load.live.test.ts` ran **first** of the 17. The conclusion survives; the reasoning for it does not.)* Two preconditions, both ordinary defect-fix work: a pinned CLI install step in `engine-live.yml` — pinned **inside** the accepted 2.1.207–2.1.220 range, never `@latest`, since the registry is already at 2.1.221+ — and the spawn case's three already-filed test defects. When a green run exists, cite the run URL plus the per-file job-log lines and the uploaded `engine-live-run-record` artifact (`engine-live.yml:54-60`), never the run conclusion. Transcript `docs/evidence/phase-06/closeout/closeout-engine-live-enoent.txt`.

## Risks & open questions

- **Release velocity (§10 risk 1):** Claude Code ships weekly; a version bump restarts the `@live` conformance clock — this phase's version gate blocks adopting an unverified version until `docs/engine-baseline.md` is revised (deliberate "00/06 policy," matching 00's and 23's framing), rather than degrading silently.
- **`--permission-prompt-tool` (§10 risk 2):** not used; the real `AdjudicationCallback` mirrors the documented SDK `canUseTool` callback shape 03 already designed against, not the undocumented CLI flag.
- **SDK `settingSources` default ambiguity (§10 risk 3):** mitigated because `WorkerSdkOptions` (03) already shows `settingSources: []` explicitly; this phase's hermeticity conformance test is what earns the assumption, not the assumption itself.
- **Sandbox domain-fronting / no default TLS termination (§10 risk 4):** workers get zero egress by envelope default; only the gateway (outside the worker sandbox) talks to providers.
- **Sandbox default read-open for credential paths (§10 risk 5):** the compiled sandbox block this phase mirrors always carries `denyRead`/`credentials` (03's responsibility to emit); covered here by the live sandbox-probe conformance tests, not re-derived independently.
- **Native worktree/subagent isolation is young (§10 risk 6; §3.2):** this phase never spawns workers into `.claude/worktrees/` or with `isolation: "worktree"`; write-capable workers always run at a `cwd` 05 provisions inside the supervisor-owned worktree (07); native isolation stays manager-side only (10).
- **Subscription-auth workers share plan rate limits; `--bare` skips OAuth sources (§10 risk 9):** this phase uses the SDK transport exclusively for v1 (§5.2's CLI form is an escape hatch only); auth injection follows whichever mechanism phase 00's blocking auth spike recorded as adopted — this phase does not independently choose between direct `CLAUDE_CODE_OAUTH_TOKEN` resolution and the `.credentials.json` fallback.
- **Verify-at-build-time (§10 risk 10):** the exact stream-json event taxonomy, `MAX_MCP_OUTPUT_TOKENS`, and hook-input field details are unconfirmed. This phase's `EngineEvent` normalizer parses whatever phase-00 empirically captured into 03's stable typed shape; a taxonomy shift on a version bump requires updating this parser before that version can pass `@live` (23 does not re-derive the taxonomy itself). Budget enforcement never depends on `MAX_MCP_OUTPUT_TOKENS`; the gateway (16) enforces its own budgets independently.
- **Worker-side gateway MCP wiring is external-process, not in-process:** adaptation §5.3's code sample shows workers reaching the gateway via an in-process `createSdkMcpServer` object built from `supervisorHandlers.*`. Building that literally would require this phase to import `packages/gateway` (16) at the point of spawn — a dependency edge the README graph does not have (16 depends on 02/04/05; nothing depends on 06↔16 either direction). This phase instead treats that part of the code sample as illustrative, the same way Gap 8 already treats that sample's `result_submit` spelling as illustrative rather than literal: workers connect to the gateway as an MCP client of the external `crabgic gateway mcp` process (Gap 2), the identical shape 10's `.mcp.json` entry uses, never an in-process import. Flagged for the reconciler to confirm 16's and 09's own files agree the registry is reachable this way for worker connections, not solely the manager's `.mcp.json` connection.
- **11's dependency on this phase, now resolved:** 03's own Interfaces-produced section states "06 ... is itself the surface 10/11/13/23 rely on downstream," and the README graph lists 06 in 11's `Depends on`; 11's own Interfaces-consumed section now names the exact fields — `EngineAdapter.capabilities()`'s `engineVersion`, `supportsJsonSchema`, `supportsSessionResume` (field names per Gap 7) — read at approval-preview time to populate `CapabilityManifest`'s pinned-engine entry and caption `PerformanceContract` budget previews. No longer an open question.
- **Cross-phase gap inherited from 03, now resolved:** 03 previously flagged that 05's "spawn via EngineAdapter (fake engine until 06)" implied a hard 05→03 dependency neither 05's header nor the README graph reflected. This is now formalized: 05's header lists `03, 04` under Depends on, and the README graph carries a `P03 --> P05` edge. This phase's own header (`Depends on: 03, 05`) was already correctly graphed.
