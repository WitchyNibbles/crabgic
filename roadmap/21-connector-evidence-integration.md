# Phase 21 — Connector evidence integration & drift CI

| | |
|---|---|
| **Depends on** | 14, 18, 20 |
| **Unlocks** | 23 |
| **Sources** | original plan phase 9 (connector evidence into contracts/verification/perf/security/learning); adaptation §7 (connector notes: gateway-only egress, result-size budgets), §4.4 (evidence-carrying results) |
| **Primary packages** | `packages/gates` (gate registration, traceability view, drift-CI job), `packages/connectors-jira`, `packages/connectors-grafana` (revision comparators, done-transition guards) |

## Goal

Remote state becomes first-class evidence instead of a side effect: every requirement bound to a Jira issue or Grafana resource carries a confirmed remote revision before its run may leave `final_verifying` (02); the requirement→work-unit→object-ID→remote-revision chain resolves in both directions on demand; an unmapped or ambiguous remote operation blocks rather than silently passing; a material remote-side edit mid-run reliably reaches phase 11's approval gate instead of being overwritten; and provider drift is caught on a schedule without ever auto-changing what's pinned.

## In scope

- **Evidence-pointer linkage:** every `RemoteResource` (02) that a Jira/Grafana intake or mutation resolves for a requirement (18/20) is recorded as an `evidence_pointer`-typed `JournalEntryType` (02, 13-member union) entry keyed by `Requirement.id`, tagged `tracking-issue | dashboard | alert`; bidirectional requirement↔resource lookup built on top, in `packages/gates`. Pre-approval mutation *previews* stay 11's concern (11 calls gateway tools directly, before 21 exists in a run's timeline) — this bullet is strictly post-execution, durable, evidence-only.
- **Evidence binding:** the read-back-verified remote revision that 16's pipeline confirms (surfaced through 18/20) is bound onto the same `EvidenceRecord` (02) instances 14 already emits for that requirement, alongside 14's existing exact-object-ID field — an extension of 14's evidence channel, not a new one.
- **Verification gate:** a `remote_verification` gate registered into 14's contract-risk-tag gate framework blocks the Run lifecycle's `final_verifying`→`published_local` transition (02) for any requirement with an unbound evidence pointer, or whose remote operation resolved to canonical `unsupported` or `ambiguous_write` (02's 10-member connector-error union) — never a silent pass, and never an informal 11th error code. (Jira's own `done` workflow state (18, its own 4-state `planned/in_progress/blocked/done` mapping) is a distinct enum from the Run lifecycle's `final_verifying`/`published_local`; the gate blocks the latter on verification of the former — the two are never conflated.)
- **Remote-edit reconciliation:** 18's milestone revision polling feeds a conservative field-level materiality classifier (summary/description/acceptance-criteria fields only, first pass); a material diff raises 11's `material amendment` stop condition (reached transitively via 14→13→11) — 21 supplies the trigger signal, 11 owns the amendment/re-approval mechanics.
- **Drift CI:** a scheduled job replays 18's and 20's pinned cassettes/fixtures against live sandboxes; on divergence it emits a `DriftProposal` artifact (connector, pinned vs. observed version, redacted diff reusing 16's provider-body redaction discipline, recommended fixture update) for human review — never an automatic routing or version-pin change.
- **Cross-gate wiring:** 16/18/20's already-built security fixtures (forged admin/delete, tenant-boundary, redaction) are registered as standing, blocking entries in 14's gate manifest rather than one-off phase-exit tests; connector operation latency is captured in the same `EvidenceRecord` stream 14 emits — available to any performance analysis, but 21 does not implement 15's benchmarking harness and there is no direct 21→15 dependency edge (15 and 21 are sibling dependents of 14, per the README graph).

## Out of scope

- Pre-approval remote-mutation **preview** rendering — 11 (calls 16's gateway tools directly; nothing there waits on 21).
- Jira/Grafana auth, resource CRUD, rate limiting, capability discovery, and the plan→validate→journal→apply→read-back pipeline itself — 16 (transport/pipeline), 18 (Cloud), 19 (Data Center), 20 (Grafana).
- PerformanceContract methodology and the benchmarking harness itself — 15; 21 only ensures connector timing lands in evidence 15 could read.
- Amendment-diff computation, envelope re-versioning, re-approval token minting — 11; 21 only supplies the materiality signal.
- Capability-quarantine review UX (`trust review`) — 12; the drift-proposal review is its own self-contained artifact/flow, not a reuse of 12's command.
- Jira Data Center-specific evidence handling — 19, which extends 18's Cloud evidence contract; 21 does not special-case DC.
- Any VCS-host (GitHub/GitLab/Bitbucket) delivery of evidence — confirmed absent from v1 scope entirely (adaptation §8: local-branch publication without checkout/push; no such connector phase exists anywhere in 00–23); not reintroduced here despite the topical adjacency.
- Security/coverage adapter selection and implementation (SAST, secret scanning, dependency/license analysis, coverage, flake policy) — 14; 21 only adds connector-specific fixtures into that already-built framework.

## Interfaces produced

- **`evidence_pointer` journal entries** (02's `JournalEntryType`) — payload `{requirementId, remoteResourceId, relation: "tracking-issue"|"dashboard"|"alert"}`, queryable bidirectionally. Consumed internally by this phase's traceability view and verification gate; no other phase reads them directly.
- **Bound `EvidenceRecord` instances** carrying a confirmed remote revision alongside 14's existing object-ID/digest fields. Consumed by: 23 (release-gate check "every requirement linked to evidence from the exact final object ID and remote revisions").
- **`remote_verification` gate** — a new entry in 14's contract-risk-tag gate registry. Runtime-consumed by 14's own gate-execution engine; this is a registration into an already-extensible framework, the same pattern 12/18/19/20 already use for their own SSDF-selected security adapters — it does not require 14 to depend on 21.
- **Materiality trigger signal**, feeding 11's `material amendment` stop condition. Runtime-consumed by 11's stop-condition detector (reached transitively: 21→14→13→11; no new build-order edge — 11 is already-built infrastructure by the time 21's trigger fires during a live run).
- **Traceability view** (requirement → work unit → exact object ID → RemoteResource → confirmed revision, bidirectional) — backs phase 09's already-stubbed `evidence <change-set-id>` command (09 ships it `NOT_IMPLEMENTED` per its own convention; 21 wires the connector-evidence portion here, mirroring the `gateway mcp` stub-then-wire pattern already established between phases 09 and 16 — no new 21→09 dependency edge required). Consumed by: 23 (E2E traceability check); the CLI's `evidence` command once wired.
- **Release traceability record** — the `RemoteResource` + `RemoteEvidencePointer` pair this phase's `bindRemoteResourceEvidence` writer produces, serialised by 23's containerized binding into `docs/evidence/phase-23/requirement-traceability.json`. Because that record is produced *outside* the checkout being scored, it follows `docs/interface-ledger.md`'s **Gap 16** convention: the `docs/evidence/phase-23/<record-name>.json` path, the single absolute-path override `$CRABGIC_REQUIREMENT_TRACEABILITY_RECORD` (unset or blank falls back to the in-repo path), and a `.strict()` schema read through `safeParse` so an absent, unparseable or schema-violating record FAILs that one checklist item with the path and issues quoted, rather than throwing out of the release-evidence run. Consumed by: 23 (`requirement-traceability` release-gate check).
- **`DriftProposal` artifact** (connector, pinned-fixture version, observed-live version, redacted diff, recommended fixture update) + the scheduled CI job producing it. Consumed by: a human reviewer, out-of-band (no CLI dependency introduced); keeps 18/20's cassette corpus current, which 18's own Risks/notes already forward-reference ("cassettes refreshed by 21's drift job").
- **Connector security-fixture gate registrations** (importing 16/18/20's forged-admin/delete, tenant-boundary, and redaction fixtures into 14's manifest as blocking, not advisory) + **connector operation latency** captured in 14's `EvidenceRecord` stream. Consumed by: 14's gate-execution engine (registrations); available to, but not contractually consumed by, 15 (no 21→15 edge exists in the dependency graph).

## Interfaces consumed

From **14** (`packages/gates`):
- The contract-risk-tag-keyed gate registry/framework — 21 registers into it, does not reimplement it.
- `EvidenceRecord` emission (02 schema; 14 populates command, exit status, env/toolchain fingerprint, timestamp, artifact digests, exact object ID) — 21 extends specific instances with remote-revision data.
- The stop-condition/amendment surface, reached transitively through 14's own dependency chain (14→13→11) — 21 relies on it existing and firing on the signal supplied; 21 does not re-implement 11's `material amendment` handling.

From **18** (`packages/connectors-jira`):
- `RemoteResource` (02 schema) records for Jira issues/epics/boards/sprints, and their read-back-verified revisions (16's pipeline, surfaced by 18).
- The `planned/in_progress/blocked/done` workflow-state mapping and `milestone_sync`-typed `JournalEntryType` (02) events from 18's milestone-sync engine.
- 18's recorded Cloud v3/Agile cassettes (its work item 6) — replayed by the drift job.
- Canonical connector errors (02's 10-member union) as typed by 18's resource clients, including `unsupported` for gaps the adapter never guesses at.

From **20** (`packages/connectors-grafana`):
- `RemoteResource` (02 schema) records for folders/dashboards/alert rules/contact points/mute timings/notification templates, their read-back-verified revisions, and rollback-snapshot state (20's mutation-safety mechanism).
- 20's cassette/version fixtures per Grafana release (its work item 6) — replayed by the drift job.
- The dashboard-version/`resourceVersion`/ETag concurrency token (20) used as the "confirmed remote revision" value for Grafana resources.

## Work items

1. Evidence-pointer population: write `evidence_pointer` `JournalEntryType` entries linking `Requirement.id`↔`RemoteResource.id` as 18/20 resolve tracking issues/dashboards; bidirectional lookup in `packages/gates`. Failing-first: a lookup against an empty journal must return empty, not throw — written before the writer exists.
2. Evidence binding: extend 14's `EvidenceRecord` emission with the confirmed remote revision from 18/20's read-back verification, keyed by item 1's pointers. Failing-first: an `EvidenceRecord` for a tracked requirement missing the revision field must fail a completeness assertion before the populating code exists.
3. `remote_verification` gate: register into 14's framework; block `final_verifying`→`published_local` (02) on unresolved pointers or `unsupported`/`ambiguous_write` outcomes (02). Failing-first: a requirement with an unbound pointer must fail the gate before the gate's pass path is implemented.
4. Materiality classifier + amendment trigger: conservative field allow-list (summary/description/acceptance-criteria) over 18's milestone revision diffs; fires 11's `material amendment` stop condition. Failing-first: a diff touching only a non-tracked field (e.g. Jira watchers) must **not** trigger, asserted before the tracked-field-triggers case is implemented.
5. Drift-CI job: scheduled replay of 18/20 cassettes against live/sandbox endpoints; diff → `DriftProposal`. Failing-first: a run against an intentionally bumped fixture must produce a red check before the job's no-drift green path is implemented.
6. Cross-gate wiring: register 16/18/20's security fixtures into 14's manifest as blocking entries; capture connector latency in the `EvidenceRecord` stream. Failing-first: a manifest-completeness test (asserting all named fixtures present) is written before the registrations exist, so it fails first.

## Test plan

**Unit** — `evidence_pointer` round-trip: seed 3 requirements (2 Jira-tracked, 1 Grafana-tracked); bidirectional requirement↔`RemoteResource` lookup resolves both directions; an untracked requirement returns empty, not an error. Canonical-error mapping: a `RemoteMutationPlan` whose action type is absent from the adapter's table must resolve to `unsupported` (02), never a bespoke "unknown" value. Enum-disjointness: Jira's `done` (18) and the Run lifecycle's `final_verifying`/`published_local` (02) share no member string.

**Property** — materiality classifier: fast-check property over randomized field-level diffs — any diff touching summary/description/acceptance-criteria fields classifies `material = true`; diffs touching only non-tracked fields (watchers, labels, etc.) classify `material = false`, holding across the generated space.

**Integration** — E2E on 16/18/20's fakes: a run completes only when every requirement's `EvidenceRecord` carries a confirmed remote revision; a seeded mid-run edit to a tracked Jira issue's description halts the run via 11's `material amendment` stop condition before `final_verifying` completes. Unknown-operation fixture: a seeded `RemoteMutationPlan` with an unmapped action type blocks `final_verifying` with an actionable `EvidenceRecord` entry, not a silent pass.

**Conformance** — drift-CI job run against an intentionally bumped fake Jira/Grafana schema fixture (renamed field or withdrawn capability): must produce exactly one `DriftProposal` artifact and a red check, with zero pinned-fixture/config changes applied by the job itself (diffed before/after).

**Security** — gate-manifest completeness: 14's loaded gate set must include 16/18/20's forged-admin/delete, tenant-boundary, and redaction fixtures as blocking (not advisory) entries; removing any one fails the completeness test — proof that these fixtures graduated from one-off phase-exit checks to standing, continuously-run gates.

## Exit criteria

**Closeout pass 2026-08-02:** 4/6 ticked against recorded evidence; the two unticked boxes below
(criteria 1 and 5) are open defects — see
`docs/evidence/criteria-closeout/defects/21-material-amendment-halt-not-wired-to-phase-11.md`,
`docs/evidence/criteria-closeout/defects/21-tenant-boundary-manifest-entries-tautological.md`, and
each box's own per-box note. Machine-readable index:
`docs/evidence/criteria-closeout/phase-21.json`.

Shared citations reused by several boxes below. **`CI` run
[30743626882](https://github.com/WitchyNibbles/crabgic/actions/runs/30743626882)**, green at
`30f931e` (this pass's base) — its `unit-test+coverage (ubuntu-latest)` job
([91485325042](https://github.com/WitchyNibbles/crabgic/actions/jobs/91485325042)), step "test with
80% line+branch coverage gate", executed 625 test files / 6155 tests and names all sixteen
phase-21 `@crabgic/gates` suites individually (job-log lines 536, 537, 548, 549, 551, 555, 558,
561, 568, 569, 571, 573, 578, 581, 584, 590). Scoped local re-runs of each criterion's own suites,
captured verbatim at that commit with their command lines and exit status, are committed as
`docs/evidence/phase-21/closeout-c<k>-*.txt`.

**The red drift-CI check was produced by this pass, not reinterpreted.** Before it,
`drift-ci.yml` had exactly one recorded run — 30256034367, scheduled, green — so criterion 3's
"red CI check" had no evidence and a local transcript would have been a channel downgrade. The
red half was producible with no code change, through the workflow's own documented
`workflow_dispatch` drift-simulation inputs, so it was produced: three dispatches at main
`30f931ea`, no scratch branch, no repository change. Run
[30743854535](https://github.com/WitchyNibbles/crabgic/actions/runs/30743854535) primes the
threshold-2 debounce (green); run
[30743998864](https://github.com/WitchyNibbles/crabgic/actions/runs/30743998864) is the red one,
carrying exactly one `DriftProposal`. The intervening run
[30743929773](https://github.com/WitchyNibbles/crabgic/actions/runs/30743929773) emitted **two**,
which is a real defect in the job rather than in the criterion — filed as
`docs/evidence/criteria-closeout/defects/21-drift-ci-blank-dispatch-input-false-positive.md` and
summarised in criterion 3's own note.

Provenance of the shared `CI` citation, phrased so it does not rot. The run above is at `30f931e`,
this branch's base and — **as of main @ `d0a6520`** — its merge base. The proof is scoped to the
**25 paths this record actually cites**, not to whole packages, because `d0a6520` does touch
`packages/cli` (`real-handlers.ts`, `run-doctor.ts`) and a package-level claim would be false while
a path-level one is true: `git diff 30f931e d0a6520 --` over those 25 refs is **empty**. One
adjacent file did move — `d0a6520`'s annotation sweep added a dated pre-rename header to
`docs/evidence/phase-21/README.md`. That file is referenced in prose below but is not a citation
ref, and the two things this record says about it are unchanged at `d0a6520`: its "Journal excerpt
(MAJOR-1's named evidence artifact)" heading is still at line 237, and its §Deviations item 4 still
reads as before. The integrator should re-derive both diffs at merge time rather than trust these
sentences.

Citation re-resolution. Every quoted code fragment in the index was located mechanically against
the tree this branch merges into, immediately before pushing: **140 code-shaped quoted assertions
across 38 resolvable citations, 140 exact content matches (whitespace-collapsed), all within four
lines of their cited span, zero citations yielding no snippet.** Twelve further fragments are prose
rather than code — package names, `file:line` pointers, bare filenames, bare zero-arg calls and
single state words — and are named and hand-checked rather than skipped silently:
`.github/workflows/drift-ci.yml`, `@crabgic/journal`, `createGateRegistry()`, `drift/cli.ts`,
`exit=1`, `packages/gates/src/drift/pinned-fixtures.ts`, `packages/gates/src/index.ts:151`, `pass`,
`run()`, `run-drift-ci.ts`, `running`, `with:`. Separately, the **21** quoted strings on this record's eight `ci-run`
citations were byte-compared against the downloaded job logs and the downloaded `drift-proposals`
artifact: **21 exact, 0 mismatched.**

Scope caveat that dates criteria 1 and 2, recorded rather than left implicit: **this phase's gate
has no production caller, and no code fires it at the transition it names.**
`createRemoteVerificationGate` is referenced only by `packages/gates/src/index.ts`'s barrel and by
this package's own tests; `fireFinalCandidateVerification` — the one function that enforces
`stage: "final_verifying"` (`packages/gates/src/final-candidate.ts:45-47`) — is likewise test-only;
and `registry.fireByTag` never inspects `context.stage` at all. The same narrowing is already on
record for the sibling perf gate (`docs/evidence/criteria-closeout/phase-15.json` criterion 5;
`docs/interface-ledger.md` Gap 21). So the boxes below are evidenced at the level of the gate's
verdict, exercised through 14's real registry and 04's real journal — never as a transition that a
shipped run is blocked at.

- [x] E2E on fakes: a run completes only when every requirement's `EvidenceRecord` carries a confirmed remote revision; a seeded mid-run tracked-field edit halts the run via 11's `material amendment` stop condition before `final_verifying`. Evidence: integration suite in `packages/gates` + the halted run's journal excerpt. — **Evidence (2026-08-07), closed by remediation. The UNMET assessment that follows is kept VERBATIM and is superseded, not deleted.** Defect: `docs/evidence/criteria-closeout/defects/21-material-amendment-halt-not-wired-to-phase-11.md`. The first clause is fully evidenced; the second is not. What exists: `packages/gates/src/remote-verification-e2e.test.ts:94-146` fires the `remote_verification` gate through a real `createGateRegistry()` against a real `@crabgic/journal` store for a three-requirement fixture `ChangeSet`, asserting `expect(allGatesPassed(firstResults)).toBe(false)` while req2 is unbound and `expect(allGatesPassed(secondResults)).toBe(true)` once it is bound, with `expect(req2FinalEvidence?.evidence.artifactDigests).toContain(` / `` `confirmed-revision:${remote2}:3`, `` — the emitted evidence literally carries the revision — and `:158-165` reading the entry back out of the journal. The untracked third requirement passes trivially, so a block-everything gate would not satisfy it. What does not exist is the halt the words name: the criterion says the edit "halts the run via 11's `material amendment` stop condition", and what the suite proves is that the test's own three-line helper calls `throwIfMaterialAmendment(signal)` — a phase-21 function (`material-amendment-guard.ts:26`) — two lines before the gate call, then asserts `expect(finalVerifyingGateFired).toBe(false)`. 11's actual stop condition is `haltOnStopCondition` (`packages/supervisor/src/intake/stop-conditions.ts:73`, kind `material_amendment`, transitions the run to `blocked` and journals an `adjudication_decision`); it has **no production caller** — its only four call sites are its own unit test and `packages/cli/src/intake/intake.e2e.test.ts:232`, and none is in or reachable from `packages/gates` — `MaterialAmendmentSignal` has no consumers outside `packages/gates`' own barrel and tests, and `@crabgic/gates` does not depend on `@crabgic/supervisor`, so the "reached transitively via 14→13→11" path this phase's own §In scope predicts does not exist. (Correction, 2026-08-02, review of PR #76: this annotation first said "no callers anywhere in the repository". That was false — the grep behind it excluded `*.test.*`. The verdict is unchanged, and the narrower fact is the more useful one: 11's halt is exercised end-to-end for all seven stop-condition kinds, `material_amendment` among them, so what is missing is the wiring from 21's signal, not the mechanism.) Second, independent gap on this criterion's own named evidence: it asks for "the halted run's journal excerpt", and the halt path journals nothing — it throws before `registry.fireByTag`, and the suite's only journal assertions belong to the run that *completes*. (`docs/evidence/phase-21/README.md`'s "Journal excerpt" section presents those completing-run entries under a heading that reads as the halted run's; that is a doc correction, listed in the defect's remedy.) Substituting a phase-21-local throw for phase 11's stop condition loses a guarantee rather than describing the same one more precisely, so this is UNMET and not a wording correction. `docs/evidence/phase-21/closeout-c1-remote-verification-e2e.txt`. **Evidence (2026-08-07), closed by remediation:** PR #113 wired 21's signal to 11's stop condition, so the negative fact above (`haltOnStopCondition` has no production caller) is superseded. Clause 1 (a run completes only when every requirement's `EvidenceRecord` carries a confirmed remote revision) is borne, as before, by `packages/gates/src/remote-verification-e2e.test.ts` — which IS the "integration suite in `packages/gates`" this criterion's evidence channel names, and which PR #113 did NOT modify (`git log` on the path shows two commits, both ancestors of `b3bf737`). Clause 2 (a seeded mid-run tracked-field edit halts the run via 11's stop condition before `final_verifying`) is borne by `packages/supervisor/src/intake/material-amendment-halt.ts:102` — the FIRST and only production caller of `haltOnStopCondition` — plus the seam e2e `packages/cli/src/intake/material-amendment-halt.e2e.test.ts`. The criterion's second named channel, "the halted run's journal excerpt", is now a committed artifact: `docs/evidence/phase-21/halt-wiring-journal-excerpt-batchB.txt` shows seq 4 `run_transition {from: "running", to: "blocked"}` PRECEDING seq 5's `adjudication_decision`, whose `rationale` names the kind verbatim (`stop condition "material_amendment"`), over a real temp-dir journal, a real `RunsRegistry`, 02's real run-lifecycle state machine and 21's REAL classifier — and a mandatory does-not-halt CONTROL on a SECOND run, where only a non-tracked custom field moved, which stays `running`, journals ZERO `adjudication_decision` entries and walks on cleanly to `final_verifying`. "Before `final_verifying`" is proven against the STATE MACHINE rather than a flag: resuming the halted run is refused with `IllegalTransitionError` (`RunLifecycle: illegal transition from "blocked" to "verifying"`, captured verbatim in that excerpt) and `blocked` is absorbing. Precision worth stating rather than leaving to be discovered: the seam e2e's own version of that step asserts `.rejects.toThrow()` with NO error class, so the TYPED proof is the committed excerpt and `packages/supervisor/src/intake/material-amendment-halt.test.ts:132`'s `.rejects.toThrow(IllegalTransitionError)` on the no-resurrect case, not the e2e line. Probes M1-M4 in `docs/evidence/phase-21/halt-wiring-probes-batchB.txt`. **POINTER NOTE, mandatory and verbatim:** the halt-wiring seam e2e lives in `packages/cli`, not `packages/gates`, because a `gates -> supervisor` dependency would invert the 21 -> 14 -> 13 -> 11 phase path; naming the instrument precisely, `scripts/check-package-graph-acyclic.mjs` would in fact ACCEPT that edge today (measured — nothing in gates' transitive closure depends on supervisor), so the BINDING constraint is the roadmap phase graph, not manifest acyclicity. Clause 1's named suite is in gates; the criterion's channel wording predates the wiring seam. If the reviewing owner reads the channel as requiring the halt proof ITSELF in `packages/gates`, untick and escalate — never move code across the phase boundary to satisfy a pointer. **Residual:** the composed daemon does not yet invoke `haltRunOnMaterialAmendment` (future work, sized L, named in that module's own doc comment), and no production milestone-polling loop exists; the criterion says "E2E on fakes", which the seam e2e satisfies, so this is disclosure rather than a lost conjunct. The doc correction this box asked for is done: `docs/evidence/phase-21/README.md`'s "Journal excerpt" heading no longer stands alone as the halted run's.
- [x] `unsupported`-mapped and `ambiguous_write` remote operations block the `final_verifying`→`published_local` transition (02) with an actionable `EvidenceRecord` entry, never a silent pass. Evidence: fault-matrix test. — **Evidence (2026-08-02):** the fault matrix the criterion names is `packages/gates/src/remote-verification-gate.test.ts:88-110` — `it.each(["unsupported", "ambiguous_write"] as const)(` with `expect(result.passed).toBe(false);` and `expect(result.detail).toContain(outcome);`, run against a fixture that records a bound, confirmed pointer first (`:94-101`), so the block is attributable to the canonical outcome and not to a missing pointer. The anti-vacuity control is the second `it.each` at `:112-128`, over the remaining eight members of 02's ten-member connector-error union — `"does NOT block on the other 8 canonical outcomes by itself (%s)",` / `expect(result.passed).toBe(true);` — without which a gate that blocked on everything would satisfy the positive half. The blocking set is typed from 02's union rather than restated (`remote-verification-gate.ts:48-51`, `const BLOCKING_CONNECTOR_OUTCOMES: ReadonlySet<ConnectorErrorKind> = new Set([` / `"unsupported",` / `"ambiguous_write",`), so no informal eleventh code can enter it. The same matrix fired through the real registry, per requirement, is `remote-verification-e2e.test.ts:194-199`, and that is the path that journals: `packages/gates/src/evidence.ts:48` `gateVerdict: verdict.passed ? "passed" : "failed",` with `:51-55` `await journal.appendEntry({` / `type: "evidence_pointer",` / `payload: record,`. CI: run 30743626882 job 91485325042, job-log lines 555 and 536. `docs/evidence/phase-21/closeout-c2-fault-matrix.txt`. Two precisions so the tick is not read wider than it is. (a) The transition itself is not enforced — see the scope caveat above; what is evidenced is that these two outcomes produce `passed: false`, which is what makes `allGatesPassed` false. (b) "Actionable" reaches the verdict, not the record: the canonical kind lives in `GateVerdict.detail`, and `EvidenceRecordSchema` is `.strict()` with no `detail` member, so the journaled entry carries `command: "remote_verification"`, `exitStatus: 1`, `gateVerdict: "failed"` and `gateTag` — enough to identify the failing gate and rule out a silent pass, not enough to name which of the two kinds fired.
- [x] Drift-CI job run against an intentionally bumped fixture produces exactly one `DriftProposal` artifact and a red CI check, with zero pinned-fixture/config changes applied by the job itself. Evidence: CI job log + before/after repo-state diff. — **Evidence (2026-08-02):** both named artifacts, both produced by this pass. **CI job log** — `drift-ci` run [30743998864](https://github.com/WitchyNibbles/crabgic/actions/runs/30743998864), conclusion `failure`, `workflow_dispatch` at main `30f931ea` with `jira_observed_version=1001.0.0` (pinned is `1000.0.0`) and `grafana_observed_version=13.1.0` (its pinned value, i.e. deliberately no drift): the job log reads `drift-ci: 1 DriftProposal(s) written to /home/runner/.local/state/crabgic/drift-ci/drift-proposals.json — human review required.` then `##[error]Process completed with exit code 1.`, and the uploaded `drift-proposals` artifact (ID 8832254797), downloaded, is a one-element array — `"connector": "jira"`, `"pinnedVersion": "1000.0.0"`, `"observedVersion": "1001.0.0"`, `"recommendedFixtureUpdate": "update pinned jira fixture from 1000.0.0 to 1001.0.0 (version bump only, no shape change)"`. Exactly one proposal, red check. Run [30743854535](https://github.com/WitchyNibbles/crabgic/actions/runs/30743854535) is the green first sample that primes the threshold-2 debounce, so the redness is attributable to the repeated-failure rule §Risks demands rather than to one sample. **Before/after repo-state diff** — `docs/evidence/phase-21/closeout-c3-repo-state-diff.txt` runs the workflow's exact command (`node packages/gates/dist/drift/cli.js`) with the same overrides, twice, and records `diff exit=0  (0 == identical)` between the `git status --porcelain` captures, byte-identical `sha256sum` for `.github/workflows/drift-ci.yml` and `packages/gates/src/drift/pinned-fixtures.ts` on both sides, no output from `git diff --stat HEAD -- packages/ .github/`, and a `find` showing the only files written are `drift-proposals.json` and `debounce-state.json` under the XDG state root, outside the tree. It independently reproduces the single proposal (`drift-ci: 1 DriftProposal(s) …` / `exit=1`). Unit-level: `run-drift-ci.test.ts:78-80` `expect(secondRun.proposals).toHaveLength(1);` / `expect(secondRun.proposals[0]?.connector).toBe("jira");` / `expect(secondRun.redCheck).toBe(true);` against the shape-drift bump (a withdrawn `description` field, which the version-only CI override does not exercise), with `:58-59` `expect(result.proposals).toEqual([]);` / `expect(result.redCheck).toBe(false);` on the un-bumped snapshot as the control; `no-pinned-write.test.ts:29-31` scans `run-drift-ci.ts` — and only that file — for write primitives. `docs/evidence/phase-21/closeout-c3-drift-suite.txt` (28 tests). Two things a reader must know. **A defect was found producing this.** The first red attempt (run [30743929773](https://github.com/WitchyNibbles/crabgic/actions/runs/30743929773)) emitted **two** proposals: `cli.ts:143` tests `process.env["GRAFANA_OBSERVED_VERSION"] !== undefined`, and an unsupplied `workflow_dispatch` input — like every input on the `schedule` trigger — arrives as `""`, not absent, so the un-bumped connector was compared against an observed version of `""`. The single historical scheduled run's own log shows both variables blank and stayed green only as a first sample. Filed as `docs/evidence/criteria-closeout/defects/21-drift-ci-blank-dispatch-input-false-positive.md`; not fixed here, because verifying one's own same-pass fix is self-review. **Live scope is unchanged:** the "observed" half is fixture/env-modeled, never a live Jira or Grafana probe, exactly as this phase's evidence README and §Risks already disclose.
- [x] Traceability view resolves requirement → work unit → exact object ID → RemoteResource → confirmed revision, both directions, on a seeded multi-requirement `ChangeSet`. Evidence: golden traceability-view fixture, snapshot-tested. — **Evidence (2026-08-02):** `packages/gates/src/traceability-view.test.ts:112-128` seeds three requirements (two Jira tracking-issue, one Grafana dashboard) and asserts the full forward chain in one structural equality — `expect(entry1).toEqual({` / `requirementId: REQ1,` / `workUnitIds: [WU1],` / `objectIds: ["object-id-req1"],` / `remoteResources: [` / `{ remoteResourceId: REMOTE1, relation: "tracking-issue", confirmedRevision: "7" },` — then the reverse index at `:124-125` (`expect(view.byRemoteResourceId[REMOTE3]).toEqual([REQ3]);`) and the snapshot at `:128` (`expect(view).toEqual(golden);`) against the committed `packages/gates/goldens/phase-21/traceability-view.golden.json`. The golden was read directly rather than assumed: 63 lines carrying `workUnitIds`, `objectIds` and a structured `remoteResources` binding for each requirement, so the comparison is full-shape and cannot pass on a view that dropped a leg. "Both directions" is proven on non-degenerate topology, because the golden's own is 1:1 — fan-IN at `:165` `expect(view.byRemoteResourceId[sharedRemote]).toEqual([reqA, reqB]);`, fan-OUT at `:214-215`, duplicate collapse at `:246`, and revision-source precedence at `:270-272` (the pointer's `"fresh-10"` wins over a stale `RemoteResource.revision` of `"stale-9"`). The fail-open control is `:319-323`: a pointer whose RemoteResource is absent still yields a binding with `confirmedRevision` undefined rather than being silently dropped, so the golden's completeness is not an artefact of discarding awkward rows. CI: run 30743626882 job 91485325042, job-log line 537. `docs/evidence/phase-21/closeout-c4-traceability-golden.txt`. Scope note, cited but not executed by this pass: the view's one real consumer, 23's release-gate check, supplies **empty** `workUnitIds` (`e2e/attestation/src/requirementTraceability.ts:222-224`), and `e2e/attestation` is not in the default `npm test` fan-out — so the work-unit leg of the chain is evidenced by the golden above and by nothing downstream of it.
- [x] 16/18/20's security fixtures (forged admin/delete, tenant boundary, redaction) are present as blocking entries in 14's gate manifest; removing one fails the manifest-completeness test. Evidence: manifest-completeness test. — **Evidence (2026-08-07), closed by remediation. The UNMET assessment that follows is kept VERBATIM and is superseded, not deleted.** Defect: `docs/evidence/criteria-closeout/defects/21-tenant-boundary-manifest-entries-tautological.md`. The second clause holds and five of the seven entries hold; the tenant-boundary pair does not. What exists: `SECURITY_FIXTURE_MANIFEST` has seven entries, all `blocking: true`, and `security-fixture-manifest.test.ts:50-52` removes each required id in turn — `const withOneRemoved = SECURITY_FIXTURE_MANIFEST.filter((e) => e.id !== idToRemove);` / `expect(() => assertManifestComplete(withOneRemoved)).toThrow(idToRemove);`. Five entries genuinely re-exercise 16/18/20's own exported primitives (`assertAllowedJiraOperation`, `createGrafanaProviderAdapter`, `containsSecretShapedContent`, `redactSecretBearingObject`, `mapHttpStatusToConnectorError`). What does not hold: both tenant-boundary entries are the byte-identical call `() => assertTenantBoundary("tenant-a", "tenant-b"),` (`security-fixture-manifest.ts:172` and `:184`) into a guard this phase wrote itself — `export function assertTenantBoundary(planTenant: string, callerTenant: string): void {` / `  if (planTenant !== callerTenant) {`. Two unequal literals make the throw, and therefore the `pass` verdict, a compile-time constant: **deleting every tenant-boundary enforcement from both connector packages would leave both standing, blocking gates green**, and neither entry touches Jira or Grafana at all. Meanwhile 20's real fixture exists and was not used: `packages/connectors-grafana/src/fixtures/fault-injection-matrix.ts:61-67`'s `tenantBoundaryBreachScenario` drives `checkGrafanaConnectionDoctor` with an out-of-allowlist org, is on 20's public barrel (`connectors-grafana/src/index.ts:185`), and its own file comment says it is "consumed by 21 work item 6" and self-verifying "so 21/23 can drive this matrix directly". So for that category the roadmap's own purpose — these fixtures "graduated from one-off phase-exit checks to standing, continuously-run gates" — is defeated: 20's fixture is still a one-off, and the standing gate replacing it is inert. This phase's evidence README (§Deviations item 4) discloses the substitution and justifies it as "neither connector package exported a dedicated tenant-boundary assertion function of its own"; that is true of an assertion *function* and beside the point, since 20 exported a self-verifying *scenario* addressed to this phase by name. Ticking would assert something false for two of seven entries and lose a real guarantee. `docs/evidence/phase-21/closeout-c5-security-fixture-manifest.txt`. **Evidence (2026-08-07), closed by remediation:** the tautology is gone. the two-string-literal guard `assertTenantBoundary` was deleted by PR #94 — measured at this tree, `git grep assertTenantBoundary -- packages` returns exactly one hit, a doc comment in the NEW Jira fixture recording the history, and no definition or call anywhere — and PR #122 added the seventh entry, so `packages/gates/src/security-fixture-manifest.ts` now carries all seven required ids — `jira-forged-admin-delete`, `grafana-forged-admin-delete`, `jira-tenant-boundary`, `grafana-tenant-boundary`, `jira-redaction`, `grafana-redaction`, `gateway-redaction` — every one of them blocking, and unrepresentably so: `:216` declares `readonly blocking: true;` as the literal type. The manifest-completeness clause is borne by `packages/gates/src/security-fixture-manifest.test.ts:53-58`, which removes EACH required id in turn and asserts `assertManifestComplete` throws naming it. Both tenant-boundary entries now drive real enforcement: the Grafana half runs 20's real `tenantBoundaryBreachScenario` over `checkGrafanaConnectionDoctor` (PR #94, oracles pinned by PR #112, `docs/evidence/phase-21/fix-c5-tenant-boundary-probe.txt`), and the Jira half runs a real `RemoteMutationPlan` through the real `executeMutationPlan`, which consults `refuseOutOfAllowlistTenant`. That it is not another tautology is measured, not argued: deleting the real enforcement reddens the new gate — **5 failures src-only, 13 after `npm run build`** — and the 8 newly-red rows under rebuild are exactly the ones PR #122 adds (`docs/evidence/phase-21/fix-21c5-jira-tenant-boundary-probe-batchH.txt` §2). Both verifiers refuse a vacuous selection rather than passing on it, and probe B mutates the SCENARIO's own verdict in both conjuncts, which is the one-layer-down attack that defeated the previous entries. Per-push proof, measured by this pass from the job logs rather than inherited — same workflow, same job name, same runner label, ANSI escapes stripped and byte-compared under the one-space rule: at `main` `b9a935b` (run 31133749171, job 92728405712, log line 557) ` ✓  @crabgic/gates  src/security-fixture-manifest.test.ts (21 tests) 18ms`, and at `main` `5bc7206` (run 31134399812, job 92730458909, log line 535) ` ✓  @crabgic/gates  src/security-fixture-manifest.test.ts (29 tests) 31ms`, with the new ` ✓  @crabgic/connectors-jira  src/fixtures/tenant-boundary-scenario.test.ts (11 tests) 30ms` at log line 366 of the same job. **The T2 pin's flip is deliberate and annotated in-file** — the in-file ruling's satisfiable condition ("returns only together with real Jira-side enforcement") was met by PR #100 (enforcement) plus PR #122 (fixture); the enforcement stays gateway-owned and provider-agnostic, so phase 18 owns the fixture, not the check. **This tick implies NOTHING about the multi-tenant question:** the enforcement it exercises binds the tenant a plan DECLARES, on the mutation path only — reads are not tenant-checked and the remote's actual identity is not verified, and the pinned wording stands, "It is not a guarantee that cross-tenant access is refused" (`packages/contracts/src/contracts/external-connection.ts:117-122`).
- [x] Jira's `done` (18) and the Run lifecycle's `final_verifying`/`published_local` (02) are proven to share no member string. Evidence: enum-disjointness test. — **Evidence (2026-08-02):** `packages/gates/src/remote-verification-gate.test.ts:131-145`, the enum-disjointness test the criterion names, asserting membership in both directions against the live imported constants — `expect(RUN_LIFECYCLE_STATES).not.toContain("done");` / `expect(JIRA_WORKFLOW_STAGES).not.toContain("final_verifying");` / `expect(JIRA_WORKFLOW_STAGES).not.toContain("published_local");` — then computing the actual intersection and re-checking it: `const shared = JIRA_WORKFLOW_STAGES.filter((s) =>` / `(RUN_LIFECYCLE_STATES as readonly string[]).includes(s),` / `expect(shared).not.toContain("final_verifying");` / `expect(shared).not.toContain("published_local");` / `expect(shared).not.toContain("done");`. The anti-vacuity check was done by reading both operands, not by trusting the matcher: `packages/connectors-jira/src/workflow/workflow-stage.ts:42` is `export const JIRA_WORKFLOW_STAGES = ["planned", "in_progress", "blocked", "done"] as const;` and `packages/contracts/src/state-machines/run-lifecycle.ts:12-23` lists `"final_verifying",` / `"published_local",` / `"blocked",` among eleven states — two string arrays over an overlapping vocabulary that **do** intersect, on `blocked`, so `shared` is non-empty and these `not.toContain` assertions are not a category error that cannot fail. 18's own `workflow-stage.ts` documents the `blocked` coincidence separately as an unrelated enum with the same spelling; the criterion names three strings and the test disproves exactly those three. Because both unions are imported `as const` arrays rather than restated locally, adding `done` to the run lifecycle or `published_local` to the Jira stage set would redden this test without anyone touching it. CI: run 30743626882 job 91485325042, job-log line 555. `docs/evidence/phase-21/closeout-c2-fault-matrix.txt` names this case individually under the verbose reporter.

## Risks & open questions

- Materiality-rule false negatives/positives: the field allow-list stays conservative (summary/description/acceptance only) until 22's learning loop proposes tuned versions — never silently widened. 
- Drift job hits live systems on a schedule: transient provider flakiness could masquerade as drift. Mitigation: require repeated failing runs before emitting a `DriftProposal` (debounce), not a single sample.
- Live sandbox availability for the drift job depends on the disposable environments 18/20 provision for phase 23 (P18: "Live Jira Cloud sandbox needed by 23 — provision early"; P23: "Provision live sandboxes during phases 18–20, not here") — the drift job schedules against those same environments rather than provisioning its own.
- Open implementation detail (non-blocking): exact storage location for `DriftProposal` artifacts (CI-artifact store vs. a repo path) is left to the implementing PR.
- No adaptation §10 engine-fact risk directly governs this phase — 21 does not touch the Claude Code engine boundary; its only engine-adjacent exposure (worker-grade sandbox profiles used to execute security fixtures) is already covered by 03/06's own verify-at-build-time items.
