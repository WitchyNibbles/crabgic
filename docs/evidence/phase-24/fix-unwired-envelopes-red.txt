roadmap/24 defect fix — RED capture for the SEVERABLE `envelopes` requiredness step
captured (UTC): 2026-08-04T16:14:10Z
HEAD:           74951481d302f098efe3b6c241f5ed6a1696c09a  — SEE THE CORRECTION BELOW
cwd:            repository root (git worktree of WitchyNibbles/crabgic, branch fix/daemon-requirements-registry)
command:        npm run typecheck
exit status:    2

CORRECTION (2026-08-04, adversarial review of PR #85). The sha in the header is
NOT a commit on this branch. It was the tip when this measurement was taken, and
was then amended away by a commitlint fix (the subject exceeded 100 characters);
it survives only as a loose object and will not exist in a fresh clone. The
measurement itself is unaffected and is left exactly as captured rather than
rewritten — but do not try to check it out.

The same claim is re-measured at a reachable sha as **M1b** in
`fix-unwired-falsification.txt` (HEAD `f7dacd5e`, a real commit on this branch):
restoring `?` on both fields reports `TS2578: Unused '@ts-expect-error'
directive.` at fixture lines 61 AND 76, so the `envelopes` fixture is shown to
bite there too. Cite that one.

DELIBERATELY RED. Taken at the tip of the requirements-registry commit, BEFORE
the envelopes change — so it measures the second field in isolation, on a tree
where `requirements` is already required.

`SupervisorDependencies.envelopes` is the identical latent trap `requirements`
was: declared optional for the same stated reason ("the pre-existing construction
sites ... keep compiling"), and non-firing only because `composeSupervisor`
happens to build one. Nothing in the type system says it must. The
`@ts-expect-error` guarding an `envelopes`-less literal is therefore unused
today:

    > crabgic-monorepo@0.0.0 typecheck
    > tsc -b

    packages/supervisor/src/router/supervisor-dependencies-required.type.test.ts(76,5): error TS2578: Unused '@ts-expect-error' directive.

The blast radius is the same set of construction sites as the requirements
change, and production already satisfies it, so this is a narrowing with no
behavioural component — which is exactly why it is a SEPARATE commit and
severable if a reviewer judges it out of scope.

Difference in kind, stated so the two are not conflated: omitting `envelopes`
would have been LOUD (`run.dispatch` refuses every change set with "authorization
envelope ... is not available"), whereas omitting `requirements` was SILENT (the
acceptance bar degraded to empty and every completion was accepted). The
requirements fix closes a real, measured defect; this step closes the class it
belongs to.
