Closeout supplementary — phase 01 exit criterion 9 (`engine-pin-lint` rejects the
work-item-7 `^`-ranged fixture manifest, and passes on the real workspace).

Captured 2026-08-01 by the criteria-closeout pass, in a worktree at
65ff0da0fec2c6c3f3526758ee765eeb8cbe6d6e. Every `$ ` line below is the exact command
that produced the output beneath it, and the output is unedited.

BOTH HALVES OF THIS CRITERION NOW HAVE A REAL CI RUN.
  * FAILING: `CI` run 30720815771, job `engine-pin-lint` 91424161909, **failed** on
    scratch PR #59 at commit `a4f88bd`, which seeds `"@anthropic-ai/claude-agent-sdk":
    "^0.3.218"` into `packages/engine-claude/package.json`.
  * PASSING: `CI` run 30718972881, job `engine-pin-lint` 91419417923, green at
    65ff0da on the real workspace.
This file is supplementary. It records the standalone fixture-manifest form the phase's
own work item 7 used (a manifest passed as an argument, outside the workspace), and the
real exact pins the green half covers.

One thing changed since `wi7-engine-pin-lint-fixture-and-real.txt` (2026-07-15) and it
is recorded rather than assumed: at phase 01 the workspace declared no engine dependency
at all, so the green half was vacuous. Phase 06 added
`@anthropic-ai/claude-agent-sdk`, so the check now passes against real targets —
exactly what the criterion predicted ("06's SDK dependency is the first real
enforcement target").

$ cat /tmp/claude-1000/-home-eimi-projects-crabgic/ad1b1803-e069-487d-929f-1343a7cbee31/scratchpad/p01fix/wi7-fixture-manifest.json
{
  "name": "wi7-fixture",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^2.1.0"
  }
}

$ node scripts/check-engine-pin.mjs /tmp/claude-1000/-home-eimi-projects-crabgic/ad1b1803-e069-487d-929f-1343a7cbee31/scratchpad/p01fix/wi7-fixture-manifest.json
engine-pin-lint: FAIL
  - /tmp/claude-1000/-home-eimi-projects-crabgic/ad1b1803-e069-487d-929f-1343a7cbee31/scratchpad/p01fix/wi7-fixture-manifest.json: dependencies["@anthropic-ai/claude-agent-sdk"] = "^2.1.0" is not an exact pin (no ^/~/range/wildcard/tag allowed — engine-bundling dependencies must be pinned exactly)
exit=1

$ node scripts/check-engine-pin.mjs   # the real workspace, packages/*/package.json
engine-pin-lint: PASS — checked 18 manifest(s), no range-specified engine-bundling dependency found (watched: @anthropic-ai/claude-agent-sdk).
exit=0

$ grep -rn "claude-agent-sdk" packages/cli/package.json packages/engine-claude/package.json   # the real, exact-pinned targets the PASS above covers
packages/cli/package.json:49:    "@anthropic-ai/claude-agent-sdk": "0.3.218",
packages/engine-claude/package.json:28:    "@anthropic-ai/claude-agent-sdk": "0.3.218",
exit=0
