Closeout supplementary — phase 01 exit criterion 7 (Changesets skeleton produces a
valid CHANGELOG.md entry from a trial `changeset add` + `changeset version`).

Captured 2026-08-01 by the criteria-closeout pass, in a worktree at
65ff0da0fec2c6c3f3526758ee765eeb8cbe6d6e. Verbatim output; nothing edited.

The criterion's own named channel is "the committed dry-run output", i.e.
`docs/evidence/phase-01/wi5-changesets-trial.txt`, which is on disk and unchanged.
This file is SUPPLEMENTARY: it records that the skeleton is not merely a committed
transcript from 2026-07-15 but a live, load-bearing tool at head — the trial's
`add` + `version` shape is the same one phase 23 executed for the real v1.0.0 entry.
No `changeset version` was run here; that would mutate package versions.

$ ls .changeset/
README.md
config.json
drain-and-restart-honesty.md
lease-publish-toctou.md
upgrade-boundary-and-intake-validation.md
exit=0

$ npx changeset status --verbose
🦋  info Running release would release NO packages as a patch
🦋  ---
🦋  info Packages to be bumped at minor
🦋  - crabgic 1.6.0
🦋    - .changeset/drain-and-restart-honesty.md
🦋    - .changeset/lease-publish-toctou.md
🦋    - .changeset/upgrade-boundary-and-intake-validation.md
🦋  ---
🦋  info Running release would release NO packages as a major
exit=0

$ head -12 packages/cli/CHANGELOG.md   # generated by `changeset version`, not hand-written
# crabgic

## 1.5.0

### Minor Changes

- bfba6b8: Actively resume a rate-limit-parked unit once its reset window passes (same-daemon).

  When a worker hit an account/rate limit it was parked with its session
  retained, but nothing ever continued it: `driveRun` seeded the unit
  `parked:rate_limit`, `computeReadyUnits` (which only advances `pending`
  units) skipped it, and the run sat `parked` forever. The unit could not even
exit=0
