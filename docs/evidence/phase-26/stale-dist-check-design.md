# Design — a build-output staleness check for this repository

**Change set:** `cbd21c01-0588-41e4-b297-a34794b3a8b6`
**Stage:** `design` · **Inputs:** `./stale-dist-research-record.md` (research closed
round 15), the owner's `clarify` rulings of 2026-08-20 (full per-unit walk; wired
into `check:all`).

Every count below states the command that produced it, per the research record's
third standing rule. All measured 2026-08-20 at a clean working tree.

⚠️ **NO RULE IN THIS DOCUMENT IS CITED BY LINE NUMBER INTO THIS DOCUMENT (round 11).**
Intra-document references name the section and the rule's own name — "§3.3's **SKIP RULE —
ALL FOUR cli-SCOPED COMPARISONS**", "§4's **SKIPPED UNITS ARE NAMED ON BOTH PATHS**" — because a file every round
rewrites reflows underneath its own anchors, and a named rule survives the reflow.
Re-derived 2026-08-20 at a clean tree: `grep -on '§[0-9][0-9.]*:[0-9]'` over this file
returned **six** such anchors and **all six resolved to the wrong text**. `§4:667` (twice)
lands inside §3.3 — round 9 moved §4's skipped-units rule (renamed **SKIPPED UNITS ARE NAMED ON BOTH PATHS** in round 13, OP13-1) 548 lines and the anchor was carried
forward unchanged — and round 10's three `§3.3:71x` anchors were wrong at the commit that
wrote them (`:710` is the chunk-collision bound, `:724` is "when there is one — answers
freshness"). All six are repointed by NAME in this round. The check is that command run over the
document **below this guard** — `awk '/^## 0\./,0' <file> | grep -on '§[0-9][0-9.]*:[0-9]'`
— which returns **nothing**, measured 2026-08-20. Run over the WHOLE file it returns
**two**: the anchors quoted above as counterexamples. Round 11 wrote the unscoped form
first and it failed on its own illustration — a check a counterexample falsifies is not a
check, and the scope is part of the check. ⚠️ **THAT COMMAND SEES THE SECTION-SIGN-PLUS-COLON
FORM ONLY, and a BARE colon-plus-digits is invisible to it — a residual STATED rather than
hidden (round 26, `CF26-2`).** FOURTEEN intra-document line references stood inside §9's
round-25 entry — THIRTEEN of them bare colon-plus-digits, TEN of those in the very bullet
whose own sentence says its sites are "stated as SECTIONS, never line numbers", and a
fourteenth written as a section plus the word `line` and no colon at all — and NOT ONE of
them resolved at the commit that published them. They are DELETED in round 26, and the
command is NOT widened. Two widenings were MEASURED before being refused (standing rule 5,
and round 11's test for whether a check is a check). The bare three-or-four-digit form
returns **128** hits below the `## 0.` heading, of which **13** are these anchors and
**115** are LEGITIMATE line citations INTO OTHER FILES in this document's file-named-once
shorthand — §1's `scripts/bundle-cli.mjs` cell alone carries `:55`, `:121`, `:176`, `:178`
and `:181` — and the next paragraph rules that those stay and stay exact, so that form can
never return nothing. The four-digit-only form returns exactly the thirteen TODAY, and is
refused because it keys on digit COUNT as a stand-in for "into this document": it is blind
to a bare three-digit intra-document anchor — `:710` and `:724`, quoted as counterexamples
above, are both — and it also goes blind to every other-file citation past 9999. The fourteenth
reference carries no colon at all, so no colon-keyed form sees it either. §9's round-26
entry carries both counts with the command and the sha. The rule is enforced by READING;
this command bounds it, and this sentence states the bound.

⚠️ **A NAME THAT RESOLVES TO NOTHING IS INVISIBLE TO THAT GREP, so the check has TWO MORE
PASSES (round 12), AND A THIRD (round 13, CF13-3).** The anchor grep guards WHERE a
citation points; these guard WHAT it
names. **All three are scoped below the `## 0.` heading for the reason the anchor grep is** —
this guard must quote the failing forms to teach them, and an unscoped pass would then
report its own illustrations forever, which is the trap round 11 recorded one paragraph
above. Measured 2026-08-20 before this round, both failed:

```
# (1) every finding ID cited anywhere must have a §9 entry
awk '/^## 0\./,0' <file> | grep -oE '\b(C|CF|CR|O|OP)[0-9]*-?R?[0-9]*-([0-9]+|[A-Z])\b' | sort -u | while read -r id; do
  awk '/^## 9\./,0' <file> | grep -qF -- "$id" || echo "DANGLING $id"; done

# (2) every `§N's **NAME**` citation must occur inside that section
awk '/^## 0\./,0' <file> | grep -oE "§[0-9](\.[0-9])?'s \*\*[^*]+\*\*" | sort -u | while IFS= read -r cite; do
  sec=$(printf '%s' "$cite" | sed -E "s/^§([0-9](\.[0-9])?)'s.*/\1/")
  name=$(printf '%s' "$cite" | sed -E 's/^[^*]*\*\*(.*)\*\*$/\1/')
    awk -v s="$sec" 'BEGIN{re="^#+ " s "[ .]"} $0 ~ re {inS=1;next} /^#+ [0-9]/{if(inS)exit} inS' <file> \
    | grep -qF -- "$name" || echo "UNRESOLVED $cite"; done

# (3) every UNBOLDED §N's ALL-CAPS RULE NAME citation must occur inside that section
awk '/^## 0\./,0' <file> | grep -oE "§[0-9](\.[0-9])?'s [A-Z][A-Z-]+( [A-Z][A-Z-]+)+" | sort -u | while IFS= read -r cite; do
  sec=$(printf '%s' "$cite" | sed -E "s/^§([0-9](\.[0-9])?)'s.*/\1/")
  name=$(printf '%s' "$cite" | sed -E "s/^§[0-9](\.[0-9])?'s //")
  awk -v s="$sec" 'BEGIN{re="^#+ " s "[ .]"} $0 ~ re {inS=1;next} /^#+ [0-9]/{if(inS)exit} inS' <file> \
    | grep -qF -- "$name" || echo "UNRESOLVED $cite"; done
```

Pass 1 returned **six** IDs — `C10-1`, `C10-2`, `C10-3`, `C10-4`, `O9-1`, `OP11-2` —
carrying **37** citations in the body (12, 9, 7, 4, 4 and 1), two of them inside §0's
requirement rows, against a §9 that stopped at round 9. Pass 2 returned **one**: the name
for §4's PASS-line rule that this guard offered above as its own model and that §4 does
not contain. Both are discharged in this round — §9 gains round 10, round 11 and round 12
entries, and the four citations are renamed — and both must return **nothing**
thereafter. **A round that files a new finding ID adds its §9 entry in the SAME round**,
or pass 1 fails on that round's own body text. ⚠️ **Pass 1's suffix alternation is
`([0-9]+|[A-Z])`, and the numeric-only form it was written with could not see round 2's
entire slate (round 13 — CF13-2, C13-3 and OP13-4, filed independently by all three
lenses).** ⚠️ **Every figure below is measured against the document ROUND 12 LEFT, named
by its sha — `git show c18ebff:<file>` — and not against this one, because this round's
own apply would make any bare count stale in the commit that writes it, which is verbatim
the defect `CF13-6` files two paragraphs down.** `C-A`, `C-B`, `C-C`, `C-D`, `CF-A`,
`CF-B`, `O-A`, `O-B` and `O-C` carry no digit, so the first form extracted **82** IDs with
not one of them letter-suffixed, while **19** such citations were live below `## 0.` —
**five** of them in the body above §9, at §0's requirement 2 row, §2's `exitCodeFor`
comment, §5.1's `pretest` paragraph, §8(l) and §8(m), and the other **14** inside §9,
where they resolve trivially. The pass returned nothing because all nine happen to resolve,
not because it looked, and a later round filing a tenth letter-suffixed ID and citing it in
§0 would have passed forever — standing rule 1, on the check itself, in the sibling pass to
the one this paragraph already records it for. Falsified by mutant rather than by reading:
strip `C-C`, `CF-A`, `CF-B` and `O-A` from §9 alone on a copy — 0 occurrences left in §9,
5 citations left in the body — and the numeric-only pass printed **nothing**. The widened
form extracts **91** IDs there, returns nothing, and prints four `DANGLING` lines on that
mutant. The population is stated so a later NARROWING is as visible as this one was not.
⚠️ **The alternation is `[A-Z]`, ONE letter, and deliberately not `[0-9A-Z]+`:** the wider
form matches exactly the same 91 IDs on that document — re-derived, the two sets are
identical — and it would also match any hyphenated ALL-CAPS token following `C`, `CF`,
`CR`, `O` or `OP`, which would `DANGLE` forever. A pass that cannot return nothing is not a
pass — round 11's own lesson, one screen above, applied to the widening rather than to the
narrowing. ⚠️ **Pass 2's section matcher is `[ .]`,
not an escaped dot, and that is not cosmetic**: §3.3's heading is `### 3.3 Declaration
cache` with no dot, so a dot-anchored form extracts an empty section and reports
`§3.3's **SKIP RULE — ALL FOUR cli-SCOPED COMPARISONS**` as UNRESOLVED when it resolves —
standing rule 1, on the check itself. ⚠️ **Pass 2 is NARROWER THAN ITS TITLE IN TWO WAYS,
and pass 3 is what makes the convention this guard STATES the one it TESTS (round 13,
CF13-3/OP13-4).** The figures here are measured against the document round 12 left —
`git show c18ebff:<file>` — for the reason the paragraph above gives. First, pass 2 matches
the BOLD form only: **5** occurrences below `## 0.` and **8** in the whole file, against
**272** unbolded `§N`-apostrophe-s references below `## 0.` (278 total, 6 of them bold).
Falsified by mutant: rename `IMPORT RULE` to `LOAD-TIME RULE` throughout §1 alone and
pass 2 prints **nothing**, while five §1 citations dangle. Second — found by the
consolidation step rather than by a lens — **`grep` is LINE-ORIENTED, so a bold citation
that WRAPS across a newline is invisible to it, and one is live below `## 0.`**: §2's
`ComparisonCoverage` comment carries the bold `SKIP RULE — ALL FOUR cli-SCOPED
COMPARISONS` citation of §3.3 split over two comment lines. **The reproducible form of
that gap is the DIFFERENCE, not a count**, because a count moves with every citation any
round adds: the `§N`-apostrophe-s-star-star PREFIX must occur below `## 0.` exactly as
often as the full pattern matches, and today it occurs **one** time more. Re-derive both
and subtract; a pass reporting nothing cannot show it. ⚠️ **The obvious widening is not a
check.** Extracting the noun phrase up to the first comma, semicolon, full stop or
close-paren — `grep -oE "§[0-9](\.[0-9])?'s [^,;.)]+"` — yields **237** distinct citations
below `## 0.` and **193** UNRESOLVED lines on that UNMODIFIED document, because the greedy
form swallows the rest of the sentence and a descriptive citation is then looked up as a
literal. (The finding that proposed it reported 184; re-derived at 193, which is standing
rule 2 applied to a reviewer's own figure — the conclusion is unchanged and the number is
now reproducible from the regex beside it.) A pass that cannot return nothing is not a
pass. So the convention is narrowed to what a machine can resolve, and pass 3 enforces
exactly that: **a rule NAME cited across sections is written BOLD or in ALL CAPS**; a
descriptive phrase is not a name and is out of scope by construction. Pass 3 inspects one
citation on that document, §1's IMPORT RULE, and returns nothing; on the rename mutant
above it prints `UNRESOLVED`. ⚠️ **THREE residuals are stated rather than hidden:** no pass
sees a citation that names no rule at all, which is how a pointer to §3.2 "precedence" — a
word §3.2 contains only in order to DENY it — survived twelve rounds in §4 (round 13,
CF13-4); no pass sees a bold citation that wraps, until the prefix-versus-full-match
difference above is re-derived; and neither pass sees a citation that puts a WORD between
`'s ` and the name, both keying on the name starting there (round 31, `CF31-3`). ⚠️ **The
EIGHT live such citations are DELETED down to the conformant form rather than the passes
widened:** measured at `git show 55666e8:<file>`, inserting `([a-z]+ )?` after `'s ` takes
pass 2 from **66** matches to **71** and pass 3 from **22** to **28** and both still return
nothing, but THREE of pass 2's five additions — `§0's requirement **1**`, `§3's own
**Units**` and `§3.2's mandated **minimum**` — name no rule and resolve on a bare literal,
so the widening buys three vacuous checks for two real ones. The convention this guard
states is that a cross-section rule name is BOLD or ALL CAPS; it is now also that the name
starts immediately after `'s `. Line citations into OTHER
files stay and stay exact — those files are not rewritten by these rounds. §6 already
guards its row numbers this way ("cited from OUTSIDE this section, so nothing here is
renumbered casually"); this is the same guard for everything else.

⚠️ **A SECOND REPRODUCIBLE CHECK, ON TABLE ROWS (round 12).** GFM discards every cell
beyond its header's column count and does NOT exempt a pipe inside a code span, so one
unescaped `|` in a cell silently TRUNCATES the row there and throws the rest away — the
sentence is in the file and not on the page. Every `|` inside a cell is written escaped,
the shape `docs/presentation-policy.md:234` already uses. The check compares each row's
pipe count against its own table's header, **after stripping the escaped ones — and that
stripping is the whole of it**: the naive form still reports a CORRECTLY escaped row as
malformed, so it can never return nothing and is not a check (standing rule 1, on the
check itself):

```
awk '/^\|/{gsub(/\\\|/,""); n=gsub(/\|/,"|"); if(p==0)h=n; if(n!=h) print NR": "n" vs "h; p=1; next}{p=0}' <file>
```

Run 2026-08-20 before this round it printed exactly **one** line — §0's requirement 9 row,
**6** pipes against a **4**-pipe header — where a `git ls-files … wc -l` command cut the
cell at its first pipe and dropped the whole of the bounded-residual statement round 11
had just added to it. After escaping it returns **nothing**, verified against a simulated
fixed copy. The anchor grep above is the guard on WHERE a fix points; this is the guard on
WHAT it inserts, at the one place a correct sentence can be inserted and still not render.

⚠️ **THIS FILE IS NEVER EDITED WITH A STRING REPLACEMENT (round 12).** Round 11's first
apply passed replacement text containing a dollar followed by an apostrophe to
JavaScript's `String.replace`, which reads that pair as "everything after the match": it
spliced the rest of the file back in and doubled the document to **4499** lines. It was
caught, reverted, and re-applied with a function replacement plus a line-delta guard.
Both are STANDING requirements of the apply step, not that round's remedy, because this
document carries live `String.replace` special patterns as legitimate content, in blocks
re-derived and rewritten most rounds, so an edit whose replacement text spans one is likely
rather than exotic. The check is a shell-neutral census — `grep -on` for a
dollar followed by any character, over the whole file, discounting the forms that are not
special. ⚠️ **NEITHER THE COUNT NOR THE SITE LIST IS RESTATED HERE, and both were,
and both had drifted (round 35, `C35-2`, `C11-4`'s and `CF14-4`'s classes at the guard
that protects every apply).** This paragraph read "**four** … in §0's requirement-9 row,
twice in §3's reproducible-command block, and in §3.3's chunk-collision bound", measured
2026-08-20 and re-derived unchanged by rounds 13 and 20. Re-derived read-only 2026-08-22
across the shas the rounds that moved it are pinned at, the census being
`grep -oE` for a dollar followed by an apostrophe or by a backtick: **4** at `9c247eb` and
`3095116`, **6** at `e99b066`, **8** at `2ef2ce5`, and **10** at `5e4918f` and `a5605e9` —
seven dollar-apostrophes and three dollar-backticks — so SIX sites were live and unlisted,
in §5 twice, in §6 row 25's cell (rewritten by round 33 for `C33-1`) and in §9 three times.
The count and the enumeration are therefore DELETED rather than corrected, per `C11-4` and
`CF14-4`: **re-run the census before any apply and never restate a subset of its output**,
and if a count is ever restored it travels with the sha it was run at. The RULE above is
unconditional and was never a whitelist — a replacement text is checked against the census
this apply re-runs, never against a list a later round invalidates. It is written unquoted, and the two
sequences are named in words rather than quoted here, for two measured reasons: quoting
them would ADD TWO HITS to the census this apply re-runs — no ordinal replaces the one that stood here, "the fifth and sixth", which was the deleted four's own arithmetic and drifted with it (round 35, `C35-2`, at the fan-out of its own fix), and the quoted form of the
census returns NOTHING under `fish`, which is how a first scan nearly recorded the
document as free of them — standing rule 1, at the verification of an absence claim.
Re-derived 2026-08-20 for residual damage of the round-11 class: every `#`-heading
occurring once, no duplicated line over 120 characters, no TABLE ROW ORPHANED BY A BLANK
LINE — `awk '{L[NR]=$0} END{for(i=2;i<=NR;i++) if(L[i]~/^\|/ && L[i-1]=="" && L[i+1]!~/^\|[ -]*-/) print i}'`,
which is RUN AS PART OF EVERY APPLY — beside the formatter check below, not as a post-hoc
re-derivation — and whose result is reported as the command WITH the sha it was run at,
never as a bare absence claim a later round cannot date. ⚠️ **"Returns nothing thereafter"
stood here for one round and was FALSE in the commit that published it (round 16,
`CF16-1`/`OP16-1`/`CF16-2`/`OP16-2`).** Re-derived: at the round-13 commit it returns
nothing; at the round-14 commit (`fe892ae`, byte-identical for this file to the `dbe9850`
§9 cites elsewhere) it returns **one** line, §7's `C14-5` row; and at `db857c3` it returns
**two**, **2541** and **2838** — that same §7 row still untouched, the diff across round
15's commit carrying no change to it, plus a SECOND orphan round 15's own apply shipped at
§6's battery, rows 40-43. The guard's PUBLISHED OUTPUT was the false completion claim this
time, which is standing rule 2 applied to a check rather than to a finding, and it is why
the sha travels with the result: a stale figure is then distinguishable from damage,
exactly as `CF13-6` forced on the line-count clause. ⚠️ **APPEND RULE, standing (round 16):
a table row is appended BELOW THE LAST ROW of its table and NEVER below a blank line, and
this sweep is re-run until it returns nothing.** Round 14 rejected that anchor as a
proposal, round 15 shipped it at §7, and round 15 shipped it again at §6 in the round that
filed it — round 10's "guard WHAT an apply inserts, not only WHERE" at its third site, and
stated as document text rather than as one round's remedy per `OP12-6` —
and no editor directive,
placeholder or "see above"/"see below" standing in as document text, and — the sixth clause,
round 21, `CF21-2` — **NO SENTENCE SPLIT BY A PARAGRAPH**: a line ending in a comma whose
successor is blank and which is not a table row, a list item, a heading or a blockquote; and
— the seventh clause, round 36, `CF36-3` — **NO CODE SPAN RUN TOGETHER WITH ITS PROSE**: a
code span whose immediately preceding character is alphanumeric or a comma, full stop,
semicolon or colon, or whose immediately following character is alphanumeric, unless what
follows is one of the English suffixes `ed`, `ing` or `s` this document writes on purpose.

```
awk '/^## 0\./,0' <file> \
  | awk '{L[NR]=$0} END{for(i=1;i<=NR;i++) if(L[i]~/,$/ && L[i+1]=="" && L[i]!~/^\|/ && L[i]!~/^[[:space:]]*([-*+] |[0-9]+\. |#|>)/) print i": "L[i]}'
```

RUN AS PART OF EVERY APPLY beside the orphan-row sweep, never as a post-hoc re-derivation,
and its result reported as the command WITH the sha it was run at. At `3095116` it returned
exactly **one** line — §6's live smoke test, whose sentence round 9 split with three
paragraphs and four blank lines, so the rendered page carried a paragraph ending in a comma
part-way through a parenthesised list and, 33 lines later, an orphan paragraph opening with
a bare backticked name and a close-paren, while THREE sites cite that paragraph for the
**FOUR** `stale-unit` it predicts — §0's requirement 1 row, §3's input rule 1 and §3's
output-set bullet — and on the page that citation resolved to a list of ONE unit. Five
rounds re-derived past it, because this is the one damage class the checks above provably
cannot see: the anchor grep and passes 1-3 read citations, the escape-aware row census and
the orphan-row sweep read tables, the formatter check reads prettier's diff, and the
placeholder clause reads tokens. It returns nothing once that sentence is closed and flips
on a paragraph inserted into any sentence — verified in both directions on scratchpad
copies. ⚠️ **Earlier §9 entries say "the preamble's five structural clauses"; that figure is
history, pinned to the sha each entry names, and the clauses are run as the SET stated here
rather than to a count (`C11-4`).** ⚠️ **The LINE COUNT
that stood here is DELETED rather than corrected (round 13, CF13-6/OP13-5), which is
C11-4's own remedy applied to the baseline of the doubling detector itself.** It read
**2574** while the very commit publishing it as a re-derivation of "the CURRENT document"
was **518** lines longer: `git show 46a1d5c:<file> | wc -l` → 2574,
`git show c18ebff:<file> | wc -l` → 3092. That was the one clause of the four that would
have caught round 11's own failure mode — a doubling from 2574 to 4499 — and a figure
every round invalidates cannot be the baseline of the detector for it. The other three
clauses are scale-free, survive a reflow and carry it alone. If a count is ever restored
it is written as `git show <sha>:<file> | wc -l` WITH the sha, so a later round can tell a
stale figure from damage. ⚠️ **And the placeholder clause was FALSE when it was written**:
§3.3's unit-state reason table gave an existing-but-empty `dist/` the whole reason
`indistinguishable — see below`, an editor's cross-reference under a §4 rule that prints
that field to a terminal (round 13, OP13-2). Fixed in this round; the clause now holds,
and it scans for "see below" as well as "see above" because the surviving instance was the
one the wording did not name. None survives.

⚠️ **THE SEVENTH CLAUSE IS A SWEEP, AND ITS SCOPE IS PART OF IT (round 36, `CF36-3`).**
TWO applies have shipped a cell whose code spans lost the spaces around them. Round 31
repaired one at §6 row 26 — `CF31-2`, whose hand-derived population was **66**, fixed in
`944fc9d`, a commit titled "forty-two elided spaces around code spans" — and round 35 shipped
**61** more into §6 row 47's ROUND-25 TWIN, the one cell §0's requirement 9 names for two of
its clauses' failing sides. No guard already here sees them, all re-derived read-only at
`d0b706e`: the escape-aware row census counts pipes and an elision moves none, the orphan-row
sweep reads blank lines, the anchor grep and passes 1-3 read citations, and `prettier` plus
`norm()` plus `diff` print nothing on the damaged file and are idempotent on it — the
mangled-form-is-the-stable-form mechanism `CF31-1` records one clause down. The sweep pairs
backtick RUNS as CommonMark does, skips fenced blocks and any block carrying a line that
opens with a double slash — §6's wiring assertions, which are one single-backtick run
crossing blank lines and therefore not code spans on the page at all, the mechanism the
FOURTH check below records — and skips a span holding no letter, which is what keeps a
quantifier written beside a BACKSLASH-spelled escape legal:

```
python3 - <<'PY'
import re, sys
SUF = {"ed", "ing", "s"}
FENCE = chr(96) + chr(96) + chr(96)
lines = open(sys.argv[1]).read().split("\n")
blocks, cur, fenced = [], [], False
for i, l in enumerate(lines, 1):
    if l.strip().startswith(FENCE):
        fenced = not fenced
        if cur: blocks.append(cur); cur = []
    elif fenced: pass
    elif l.strip() == "":
        if cur: blocks.append(cur); cur = []
    else: cur.append((i, l))
if cur: blocks.append(cur)
for blk in blocks:
    if any(l.lstrip().startswith("//") for _, l in blk): continue
    text = "\n".join(l for _, l in blk)
    runs = [(m.start(), m.end() - m.start()) for m in re.finditer("`+", text)]
    k = 0
    while k < len(runs):
        s, n = runs[k]; j = k + 1
        while j < len(runs) and runs[j][1] != n: j += 1
        if j >= len(runs): k += 1; continue
        e = runs[j][0]; k = j + 1
        body = text[s + n:e]
        if not re.search("[A-Za-z]", body): continue
        ln = blk[0][0] + text.count("\n", 0, s)
        b = text[s - 1] if s else " "
        if b.isalnum() or b in ",.;:": print(ln, "LEFT", body[:40])
        w = re.match("[A-Za-z0-9]+", text[e + n:e + n + 40])
        if w and w.group(0) not in SUF: print(ln, "RIGHT", body[:40], w.group(0))
PY
```

It returns **0** at `a5605e9`, **61** at `d0b706e` — every one of them on the single line §6
row 47 occupies — and **0** on the fixed candidate both before and after `prettier`, so it
flips in both directions and is a check rather than a pinned baseline, which is round 11's
own test applied to a seventh clause. Of the 61, **57** abut a letter and **4** abut a comma,
a full stop or a colon; a letter-only predicate leaves those four standing — standing rule 5
at the guard being written rather than at the code it guards. ⚠️ **ROUND 31 REFUSED A CLAUSE
OF THIS CLASS, AND THAT RULING IS ENGAGED RATHER THAN STEPPED AROUND (`CF31-2`).** What it
refused was a clause re-deriving the letter-backtick-letter census, "a population narrower
than the damage it guards — 40 of 66 here, measured". This predicate is a different one and
it was measured against THAT round's own damage rather than against this one only: **0** at
`187eb2d`, **60** at `55666e8` and **0** at `944fc9d`, so it flips on the round-31 event as
well as on the round-35 one and would have caught both. ⚠️ **It is still NARROWER than the
damage — 60 of that round's 66 — so this clause is a FLOOR and not a certificate, and that
bound is STATED rather than hidden**, which is the shape the anchor grep above already uses
for its own residual and what makes this an addition to `CF31-2`'s refusal rather than a
re-run of it. RUN AS PART OF EVERY APPLY beside the orphan-row sweep, and its result reported
as the command WITH the sha it was run at.

⚠️ **A FOURTH REPRODUCIBLE CHECK, ON WHAT THE FORMATTER REWRITES (round 15, CF15-1).**
An apply is not finished until `prettier --parser markdown` has been RUN over this file,
and the only lines its output may change are TABLE PADDING — cell widths and the dash runs
in delimiter rows. The check normalises exactly that and nothing else:

```
norm() { awk '{ if ($0 ~ /^\|/) { gsub(/-{2,}/,"-"); gsub(/ +/," ") } print }' "$1"; }
node_modules/.bin/prettier --parser markdown <file> > <scratch>
diff <(norm <file>) <(norm <scratch>)
```

must print nothing, and a second prettier run over `<scratch>` must be byte-identical to
it. ⚠️ **`diff -w` is NOT the check and was measured failing as one (standing rule 1, on
the check itself, at its first writing):** re-padding a table also changes the DASH COUNT of
its delimiter row, and a dash is not whitespace, so `-w` reports three hunks on a
correctly-formatted candidate — a check that cannot return nothing is not a check, which is
round 11's own lesson at a fourth site. The normalised form was verified in both directions:
silent on this document and on a fully-edited candidate, and printing exactly the damaged
line on a control copy with the stars restored. This is the guard on the one class of damage
the three checks above provably cannot see: none of them reads a regex, and the row census
and the anchor grep are both blind to a change inside a backticked span. Re-derived —
`git show 934c547` is a commit whose message says "whitespace only, in 47 table hunks", and
its diff carries exactly TWO non-whitespace changes, both of them prettier normalising
emphasis from the star form to the underscore form: one in prose (`*precisely*` in §3.4,
harmless) and one inside §6's census-import regex, where `[^;]` STAR `from` BACKSLASH-s STAR
became the same text with underscores in place of both stars. **The count is two and the
mechanism is one**, which is the point: a formatter that normalises emphasis cannot tell
prose from a regex when the regex is not in a code block. Cause: the wiring block is one
single-backtick span crossing blank lines, so it is not code to a markdown parser, the star
pair is left- and right-flanking emphasis, and this repository's prettier 3.9.5 normalises
emphasis to underscores. The mangled form then passed `prettier --check`, so it was the
STABLE form and every later format run reproduced it. The predicate it left could not match
a conforming import statement at all, so its own mutant assertion passed for every input —
vacuous, the class §6 exists to prevent. Two consequences are standing: every regex this
design asks an implementer to copy is written with quantifiers that form no emphasis pair
(`+` rather than the star — measured a no-op over the whole file, while restoring the stars
is measured to be RE-MANGLED on the next run), and the command above is part of the apply.
Round 10's "guard WHAT an apply inserts, not only WHERE" has a second mechanism beside
`String.replace`, and unlike that one it fires on a formatting run nobody calls an edit.

## 0. Requirement traceability

| #   | requirement                                                                          | discharged by                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | `dist` side scoped to compiler outputs, excluding the `bundle:cli` asset copy        | §3 output set; `dist/plugin/**` (covered by §3.4) + `dist/index.d.ts` (covered by §3.5) excluded from the **mtime** side for `packages/cli` — their PRESENCE is required by §3.3's artifact set, so "excluded" never means "the check ignores them". ⚠️ **TWO bounds on that sentence, both written in round 15.** (1) **§8(p), C15-1** — the presence test is over the six TOP-LEVEL paths and never their contents, so an entry whose recursive `cp` was interrupted part-way ships present-and-PARTIAL with clause 1 satisfied, §3.4's mtime side carrying copy time, and clause 2 reading a comparand `dist/plugin/**` is excluded from: `compared` plus one advisory at exit **0 even under `--strict`**. (2) **CF15-4** — the guarantee holds only while that set is READ from the `export`ed `PLUGIN_ASSET_ENTRIES` rather than duplicated as a literal, the clause-1 mandate §6 first asserted in round 15; a six-element copy leaves `dist/plugin` covered at six after a seventh member lands, and this row would then be narrower than it reads. Fixture rows 7, 8, 22, 44 and **row 7's round-18 TWIN**, §6 — ⚠️ **FOUR fixtures were cited for TWO `packages/cli` exclusions and only ONE exclusion was reached (round 18, `C18-6`)**: row 7 mutated `dist/plugin/.mcp.json` until round 35 (`C35-1`, which re-spells that mutation as a WHITELISTED extension under `dist/plugin`, round 20's whitelist having rejected the `.json` on its own), row 8 a plugin SOURCE, row 44 a non-cli scratch fixture and row 22 the shipped copy's BYTES at `mtime UNCHANGED or NEWER`, so all four asserted the plugin exclusion or a byte rule and not one moved `packages/cli/dist/index.d.ts`'s mtime. Row 7's twin moves exactly that artifact and asserts `stale-unit packages/cli`, explicitly NOT `unbuilt` and NOT clean; the masked window is bounded at a re-derived **11.10 ms** on a completed build — row 44 being the MTIME half of §3's `dist/eo-*-fixture-*/**` exclusion, the third artifact this row's "scoped to compiler outputs" covers and the only one no row asserted until round 16 (`C16-1`); row 14's amended fixture carries its stem and unit-state halves. ⚠️ **AND the walk this row quantifies over is RECURSIVE, which no §6 element required until round 18 (`C18-1`)** — §6 row 1's amended fixture is the mtime side's row and row 4's the stem side's, both failing a `readdirSync` that omits `recursive: true` on their own fixtures. ⚠️ **AND the set this row scopes is a four-extension WHITELIST before it is a list of exclusions, and that half had an element in NEITHER column until round 20 (`C20-1`, standing rule 9).** Every fixture this row cites asserts an EXCLUSION — row 7 `dist/plugin/**`, row 7's twin `dist/index.d.ts`, row 44 `dist/eo-*-fixture-*/**`, row 9 `.tsbuildinfo`, row 22 the byte rule — so an `isCompilerOutput` whitelisting `.js`/`.d.ts` alone, carrying all four exclusions and the `**` correctly, discharged this row while reporting **SEVEN** `stale-unit` on this working copy against the **FOUR** §6's live paragraph predicts (re-derived read-only 2026-08-21 at `9c247eb`: on `packages/supervisor`, `packages/scheduler` and `packages/learning` the newest member of §3's filtered set is a `.d.ts.map` and the newest `src` input falls STRICTLY BETWEEN it and the newest `.js`/`.d.ts`), and the reciprocal blacklist-only reading manufactures `orphan-output` on a `dist/**.json` that the next `tsc -b` re-emits. §6 row 44's round-20 TWIN is the element for both readings. ⚠️ **AND this filter has a THIRD consumer, named by §3 only since round 23 (`C23-2`, standing rule 9, `C16-1`'s site list stopping one walked side short)** — §3.2's FIRST `stale-bundle` input member, the newest output across the 16 INLINED units, where `.tsbuildinfo` and `dist/eo-*-fixture-*/**` are the two minus-bullets that reach it and the two `packages/cli`-only ones cannot. NO fixture in §6 places either excluded file under the `dist` of a unit its own marker names as inlined — rows 5, 40, 41 and 45 give their inlined units a bare `dist/index.js`; rows 14 and 44, the two that DO carry scratch, are not cli-scoped, so `checkBundleFreshness` never runs there; and rows 32, 36 and 46, the cli-scoped rows holding a `.tsbuildinfo`, hold it under `packages/cli/dist`, which is not inlined, on trees where `packages/cli` is `unbuilt` or chunkless and §3.2's rank-1 or `bundleAt === undefined` branch suppresses the comparison outright. So a member-(i) walk with no accept predicate discharged this row while reporting `stale-bundle packages/cli` on every tree whose `npm test` ran after its last `bundle:cli`: `packages/journal` is one of the 16 AND the one unit this suite writes scratch into, re-derived read-only at `53996f5` at mtimeMs **1787308024588.285** against the newest FILTERED output across the 16 at **1787156422460.689**, a **1.75-day** forward shift, printed there at `check:all` member 0 and at line 5 of §5.1's measured 204 under flagless `pretest` — at the START of the very `npm test` whose interrupted predecessor left the residue. **Row 5's round-23 TWIN** is that consumer's element, and it needs no mutation: its two excluded files sit at the baseline                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2   | orphan check                                                                         | §3.1 `checkOrphans`. ⚠️ **Bounded residual — §8(m)**: a `PLUGIN_ASSET_ENTRIES` source DELETED without a rebuild leaves its shipped copy under `packages/cli/dist/plugin`, which §3.1 exempts from orphan detection by construction, §3.3's clause 1 checks only for top-level presence, and §3.4 compares mtimes only. Round 2 dispositioned that as `C-C`, "accepted as a stated limit"; the limit reached no section until round 11. ⚠️ **And the STEM set's OTHER exclusion — `dist/eo-*-fixture-*/**`, mandated by §3's output set and by §3.1 since rounds 5 and 6 — was asserted by nothing in §6 until round 16 (`C16-1`).** Re-derived at `db857c3`, the document round 16 READ — the sha travels with the figure, this file's own convention for a figure a later round must be able to tell from damage: `grep -n stemsUnder` returned exactly TWO lines, both in §2 (the declaration and the purity sentence), and NOTHING in §6. ⚠️ **That reading was already false in `badf45a`, the commit that first printed it — rows 14 and 44 carry the name there — and today §6 returns rows 4, 14 and 44, all three landed at or after round 16, which is what the `until round 16` above is about (round 29, `C29-1`).** So a `stemsUnder` accept-predicate carrying no exclusion discharged this row while reporting **20** `orphan-output packages/journal` findings on the live tree, exit **1** under `--strict` at `check:all` member 0, with the remedy `rm -rf packages/journal/dist` aimed at a directory the concurrent `npm test` is writing into. Fixture rows 14, 44 **and 4**, §6 — row **4**'s amended fixture being the STEM walk's RECURSION, required by nothing until round 18 (`C18-1`): a `stemsUnder` omitting `recursive: true` reports clean on a nested orphan and passes rows 14 and 44, whose scratch exclusions a shallow walk also satisfies — **and row 4's round-23 TWIN**, which pins this requirement's SCOPE rather than its walk. ⚠️ **`Unit.kind` decides whether this check runs for a unit AT ALL and no section derived it for twenty-two rounds (round 23, `C23-1`, standing rules 7 and 9, `C21-1`'s shape at the sibling field of the same struct).** A `kind` read off `packages/cli`'s 16-member reference closure — the only other thing `Unit.references` could be for, that field and `Unit.configs` being read by nothing in §3-§6 — marks **17 of 19** `"bundled"`, runs `checkOrphans` on `packages/perf` and `e2e/report` ALONE, and discharged this row while requirement 2 was inert for `packages/contracts`, `packages/gates`, `packages/journal`, `packages/plugin` and twelve more: PASS at exit **0 even under `--strict`** on a tree carrying an orphan, at `check:all` member 0 and under flagless `pretest`. Every element in §6 passed it, and the reason is exact: NO fixture in this design places a `packages/cli` beside a unit its own `packages/cli/tsconfig.json` references AND carries an orphan there — rows 4, 14, 44, 48 and 49 are the rows whose fixtures reach the stem side at all and not one of them builds a `packages/cli`, rows 40, 45 and 46 call comparisons DIRECTLY so `checkOrphans` never runs, row 42's scope twin asserts NO `stale-declarations` anywhere with `declaration-cache` coverage `full` and reads no orphan, row 34's fixture already carries two findings, row 39's is zero findings by construction, and the live smoke test asserts the unit SET and the coverage SHAPE and explicitly not the finding set. That twin is the first element anywhere to assert the `packages/cli` exemption POSITIVELY — ZERO `orphan-output` for it beside a non-cli orphan it must still report                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 3   | `packages/cli/dist/bin.js` vs newest `dist` of every inlined `@crabgic/*`            | §3.2 `checkBundleFreshness`, over the **metafile-derived** inlined set (16 units, equal to the reference closure today but never derived from it — ⚠️ **ASSERTED only since round 16 (`C16-2`); for fifteen rounds the bolded word in this cell was discharged by nothing, and a `checkBundleFreshness` deriving that set from the tsconfig reference closure passed every assertion in this design**, because every cli-scoped fixture made the two derivations coincide by construction; §6 row 45 is the one fixture in which they deliberately differ, and it asserts BOTH directions so an implementation that never fires cannot satisfy it) — **inert unless ALL SIX of §1's `bundle-cli.mjs` edits land, and the clear, `mkdir` and write in the right order** (round 12, CF12-4; raised to six in round 21, `C21-2`: the `writeFile` import, the clear, the `mkdir` of `.bundle-meta/` — without which the write throws `ENOENT` on the first build of every tree and `npm run build` fails at its last step forever, every remedy §4 prints then terminating in a failing command — the write, the `export` of `PLUGIN_ASSET_ENTRIES` and the entry-point guard — §1 says of the last two that "nothing above is reachable without them", because without the `export` at `:89` the check's own import fails to link and §4's internal-error row fires `ERROR`, exit **2**, at `check:all` member 0; THREE of the six edits are ORDER-sensitive, as ONE CHAIN — the clear before `build()`, the `mkdir` AFTER the clear, the write after the `mkdir` and after the plugin copy — because a `mkdir` hoisted above the wipe-step clear is deleted by it and `writeFile` throws the very `ENOENT` `C21-2` measured, on a tree carrying all six edits (round 22, `CF22-1`: round 21 raised this cell's count to six and left "only the clear/write pair" standing one clause away, a claim its own same-round insertion falsified)), of which §6's mutant-proved wiring assertion establishes FIVE SOURCE PROPERTIES and no more — §1's own count, the `writeFile` import being implied by the `WRITE` anchor and separately asserted by nothing (§1, §8(k)); **"mutant-proved" is true of ALL FIVE only since round 27 (`CF27-1`), and of the FIRST of the five — the clear before `build()` — only on a source whose `mkdir` FOLLOWS the plugin-copy loop, which is §1's prescribed placement and not the only conforming one (round 28, `CF28-1`)**, the clear-before-`build()` conjunct having been flipped by none of the six mutants standing before it and so deletable with every one of them still flipping — at the one conjunct §6's own mutant-coverage note never paired off — and, on any conforming source whose `mkdir` sits EARLIER, still deletable with all SEVEN still flipping, round 27's clear-sink landing after that `mkdir` too and so flipping `CLEAR<MKDIR` beside `CLEAR<BUILD`; §8(k) licenses exactly such a placement in terms and now records the residual where the licence is granted, the other FOUR properties being placement-independent and the conjunct remaining ASSERTED — and still failing a bundler whose clear sits after `build()` — on every form — ⚠️ **and until round 20 (`CF20-1`) that assertion FAILED a correct implementation.** Its two placement anchors were contiguous source substrings; the conforming one-line write at the wipe block's 2-space indent is exactly **100** characters against this repository's own `printWidth: 100`, with `scripts/` absent from `.prettierignore` and `npm run format` being `prettier --check .`, so it sits AT the width with zero margin and one further argument makes the formatter wrap the call across four lines — on which `includes` is FALSE while every one of §1's edits to that file is landed and correctly ordered. Re-derived 2026-08-21 at `9c247eb` with this repository's prettier 3.9.5, in both directions. Re-keyed to callee-plus-literal regexes whose `[^;]+` spans newlines, so requirement 3 is established for the wrapped and the `metaDir`-factored write as well as the one-line one, and **conditional on the marker**: absent OR present-but-unparseable — two states `readMetafile` collapses and §3.3's clause 2 does not (round 14, C14-1), and neither do the two surfaces that must SPELL them: the advisory's reason and the `bundle-freshness` coverage entry's, both read off the marker PATH rather than off that return (round 22, `C22-1`), before which the coverage side was mandated in §3.2 and in §4's verbatim reason list and asserted by nothing, so an implementation calling that file absent while it sat on disk discharged this row — the comparison does not run and the advisory `bundle-provenance-missing` is reported instead — unconditionally, never as a migration window (§3.3's cli reason table, §6 battery row 10), which is the state of every tree until the first build after this lands. **Discharged only when it RUNS**: §4's coverage line prints the `not-run` entry on both paths, so the row is never silently undischarged — and since round 14 (OP14-1) a non-`full` entry also makes §4's REMEDY PLAN print on a run holding ZERO findings, so an undischarged requirement 3 reaches the operator with recipe step 5 beside it rather than as a bare `PASS` — **including the not-run case in which NOTHING is absent (round 10, C10-1)**: when `unitState("packages/cli").state === "unbuilt"` all four cli-scoped comparisons are suppressed, and all four then report `not-run` with the second mandated reason `packages/cli unbuilt` (§3.2, §3.3, §3.4, §3.5, and §4's verbatim reason list). Without it the most routine tree there is — §7's `npm run typecheck` row, a bare `tsc -b` after a completed bundle — prints one `unbuilt` finding and no coverage line at all, which §4's recipe step 5 reads as "all six ran". There is no `reduced` status — round 10 deleted it as unreachable: a `packages/cli` with no esbuild chunk is `unbuilt` through §3.3's clause 1, and no cli-scoped comparison runs on an `unbuilt` unit (§3.2). ⚠️ **That argument holds only INSIDE the `compared` branch, and round 17 (`C17-1`) states the other half rather than reinstating `reduced`:** §3.3's cli conjunction is "one further step INSIDE the `compared` branch — never before it", so a `packages/cli/dist` that is present and outputless is `skipped`, still reaches this comparison since round 15's `C15-4`, and answers coverage `not-run` with the reason `packages/cli/dist holds no esbuild chunk` — never `reduced`, never a comparison against `undefined`, and never a finding (§3.2's Rule; §6 row 46) |
| 4   | STATE the bootstrap limit                                                            | **residual** — §8(a). Partly sidestepped: the check lives in `scripts/`, which no tsconfig compiles                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 5   | enumerate from root `references` (19), never `workspaces` (18)                       | §3, via `enumerateRootReferences`. ⚠️ **ASSERTED only since round 14 (CF14-2); until then this row was an unqualified discharge over the exact substitution it exists to forbid.** `enumerateWorkspaces` is exported at `repo-census.mjs:270`, beside `enumerateRootReferences` at `:286`, and returns a STRICT SUBSET — re-derived 2026-08-21 by calling both on the live tree: 18 dirs against 19, set difference exactly `e2e/report`, and nothing in `workspaces` that `references` lacks. A `units.mjs` importing it discharged requirement 11's letter, passed §6's live smoke test (a MEMBERSHIP check, true of any subset) and every synthetic battery row, and never walked `e2e/report`, whose `dist` holds **41** files emitted by `tsc -b` from **10** `.ts` sources today — blind spot 5, shipped, every assertion green. §6 now pins the SET by equality against the live tree and the CALL by a mutant-proved read of `units.mjs`'s `repo-census.mjs` import statement — ANCHORED to the line start only since round 29 (`CF29-2`), the unanchored regex having matched the whole semicolon-free span before the import and so returned FALSE on a conforming `units.mjs` carrying the very comment §6 predicts, failing a correct implementation at this row's own discharge — which pins BOTH imported names since round 15 (CF15-2), `enumerateRootReferences` for this row and `enumerateTsconfigs` for requirement 11's second call, each with its own mutant — and, from round 29 (`CF29-3`), THIS row's own prohibition too, by a THIRD mutant importing all three names: the predicate's negated conjunct `!includes("enumerateWorkspaces")` was DELETABLE with both name mutants still flipping, so the substitution this row exists to forbid was unseen at the source-level element that names it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 6   | each unit's `tsconfig.json` + its `extends` chain on the input side                  | §3 input rule 3 — TWO members, `<unit>/tsconfig.json` itself AND its upward chain, and this row's own subject line names both. Fixture row 2 **and its round-18 twin**, §6. ⚠️ **The FIRST member was discharged by nothing for seventeen rounds (round 18, `CF18-1`)**: row 2's mutation is the shared ancestor `tsconfig.base.json` and NO row in the battery moved a unit's own `tsconfig.json`, so an input set built from the resolved ancestors alone passed every element here while `utimesSync packages/gates/tsconfig.json` on the live tree printed PASS, exit 0 even under `--strict`, on a `packages/gates/dist` compiled under the old `compilerOptions` — the founding failure's own shape at the member this row names FIRST, and requirement 9's 21-of-30 arithmetic falls to 2 of 30 with it. The twin moves ONE fixture unit's own `tsconfig.json` and asserts the reported unit set EQUALS that unit ALONE. Row 2's CHAIN half since round 16 (`CF16-3`) asserts that the reported `stale-unit` unit set EQUALS a TWO-unit fixture's `Unit.dir` values, both units extending the fixture's own `tsconfig.base.json`. ⚠️ **Until then this row's SOLE non-vacuity element read `stale-unit` ×N, which any N≥1 satisfies:** a shared `extends`-chain resolver with a module-level visited set attributes `tsconfig.base.json` to the FIRST unit walked and to no other, so requirement 6 was discharged for one unit of nineteen with every assertion in this design green — and §7 row 1's owner-accepted cost is stated at this row's SCALE, so what the owner was asked to accept rested on a row that could not tell ×1 from ×19                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 7   | STATE the build-program/toolchain limit                                              | **residual** — §8(b). Partly closed: each producer script is an input to ONE artifact it produces (§3.2, §3.3); `bundle-cli.mjs`'s other two outputs are compared against their sources (§3.4, §3.5). ⚠️ **ASSERTED only since round 15 (C15-3); until then that partial closure was STATED and NOTHING exercised it.** Re-derived 2026-08-21 over §6's then-39 rows, its false-negative battery, its wiring assertions and its live smoke test: `bundle-types.mjs` appeared in §6 in one citation and no mutation, and every `bundle-cli.mjs` hit was a citation or a fixture module copy — never a mutation of either as a comparand member. So an implementation whose `stale-bundle` input side was the 16 dists plus `packages/cli/src`, and whose declaration-cache comparand was `packages/*/src` plus `tsconfig.dts.json`, passed every assertion in this design and shipped BOTH members hardcoded never to fire — the vacuity class this change set exists for, at the two members requirement 7 is partly discharged by. §6 rows 41 and 42 close it — ⚠️ **and until round 25 they closed DIFFERENT halves of it (`CF25-2`): row 42 asserts `stale-declarations` as the run's SOLE kind and so discharges `an input to ONE artifact` for `bundle-types.mjs`, while row 41 asserted only `stale-bundle` and NOT `stale-unit`, permitting any number of further kinds, so the same clause was discharged for `bundle-cli.mjs` — the producer with THREE outputs and therefore the only one the clause can be false of — by nothing at all.** Row 41 now carries the SOLE-kind half and the second mutant that puts `bundle-cli.mjs` on §3.4's source side. Row 42 also doubles as the sole-`stale-declarations` fixture §4 step 2's carve-out never had                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 8   | account for `packages/cli/.dts-cache/`                                               | §3.3 `checkDeclarationCache` (cache vs sources) + §3.5 `checkShippedDeclarations` (cache vs the shipped copy) — **both** returning `{ findings, coverage }`, and both reporting coverage `not-run` (§3, printed by §4) when their side of the comparison is absent — or, for §3.5, when BOTH of its sides are absent, which is a fresh clone — and NOT a CI job, §5's **THE CHECK HAS ONE CI OCCURRENCE** rule putting this member's only CI run at `pretest` on `ci.yml:129`, after `ci.yml:86`'s build, where it can only report clean (round 25, `CF25-3`) — where §3.5's own precedence line (round 17, C17-3) picks `packages/cli/dist/index.d.ts absent` as the one spelling — or, whenever `packages/cli` is `unbuilt` and §3.2's suppression rule fires, reason `packages/cli unbuilt` (round 10, C10-1) — not confined to the tree where nothing is absent, that suppression being evaluated before any comparison inspects its own comparand, so it outranks BOTH spellings above (§3.2's REASON PRECEDENCE rank 1; §6 row 26's round-19 twin is the element, round 19, `C19-1`, row 26's own fixture leaving both of this requirement's files present) — for §3.3 that side is `.dts-cache/index.d.ts`, which §3.3's own skip rule makes reachable on a fresh clone: `npm ci` never creates the cache, so that is the state a fresh clone runs in, `meta-checks` running no step of this check at all (§5's **THE CHECK HAS ONE CI OCCURRENCE**, round 25, `CF25-3`). Fixture rows 6, 17, 22, 23, 29, §6 — row **17** carrying, since round 18 (`C18-3`), the SECOND of §3.5's two mandated spellings (`packages/cli/.dts-cache/index.d.ts absent`), which nothing asserted for seventeen rounds while rows 23, 26 and row 23's twin assert the first or `packages/cli unbuilt`, so an implementation naming `packages/cli/dist/index.d.ts absent` whichever side was missing discharged this row and sent §4's recipe step 5 after a file that exists. ⚠️ **AND the BYTE half of this discharge — §3.5's `cache vs the shipped copy` — held at SIZE granularity only until round 34 (`C34-1`), the shape this table's requirement rows 3, 5, 6, 7, 10 and 11 already carry.** §3.5's Rule spells it `size first, then content`, and every §6 fixture in which the two files DIFFER also gave them DIFFERENT sizes, so a body doing the size step alone discharged this row while the published `types` entry could still ship the pre-force copy after any edit whose regenerated declarations keep the byte COUNT. Row **22**'s TWIN is the element: both declaration files pinned byte-identical at 28 bytes, rewritten to a barrel of the SAME length                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 9   | every `tsconfig*.json` a build program hands to a compiler, incl. downward `extends` | §3 input rule 4 + §3.3. Fixture rows 3 **and 47**, §6 — row **47** carries rule 4's TWO elements: its MAIN half, the POSITIVE side (a config on which ALL THREE clauses hold MUST be a member) and, from round 27 (`C27-1`), clause (ii)'s ACCEPTING side against the EQUALITY reading, its `outDir` being a STRICT subdirectory of `packages/A`'s `distDir`, and, from round 33 (`CF33-1`), clause (i)'s ACCEPTING side against the PATH-CONTAINMENT reading, that config sitting at its fixture's ROOT — OUTSIDE `packages/A` — and reaching the unit by its `extends` chain ALONE; and its ROUND-25 TWIN — ONE element carrying TWO clause-sides from round 34 (`CF34-1`), as the MAIN half above carries three: clause (ii)'s FAILING side (a config passing (i) and (iii) and FAILING (ii) must be in NO input set) AND clause (i)'s FAILING side, a SECOND non-member config declaring NO `extends`, so its chain reaches nothing inside the unit and (i) FAILS while (ii) and (iii) BOTH hold — while row **3** is clause (iii)'s failing side. ⚠️ **ALL THREE clauses name an EXCLUDING element only from round 34 (`CF34-1`).** Until then clause (i) had an ACCEPTING element ALONE and EVERY rule-4 config in this design SATISFIED it, so a `descendantConfigs` testing (ii) and (iii) ALONE — no chain test at all — passed both halves of both rows and every mutant-based assertion either half carries, and clause (i) was DELETABLE with every element still passing: `CF25-1`'s formula at the last of the three clauses still in that state. ⚠️ **This cell read `row 47 is rule 4's ONLY element` until round 25, naming ONE half of ONE row for a THREE-clause rule while clause (ii), a MANDATED conjunct, was SATISFIED by BOTH of the fixtures that turn on this rule and tested by NEITHER (round 25, `CF25-1`).** Every rule-4 config in §6 put its `outDir` INSIDE the unit's compared output set — row 47's `outDir`, `"./dist"` at that round, `"./dist/emit"` from round 27 (`C27-1`) and `"./packages/A/dist/emit"` from round 33 (`CF33-1`), which resolves from that fixture's ROOT to the same `packages/A/dist/emit`, and row 3's INHERITED `./dist` — so a `descendantConfigs` implementing clauses (i) and (iii) ALONE, with no `outDir` test at all, attached row 47's config, declined row 3's, passed BOTH halves of both rows, and could not be seen on the live tree, rule 4 having 0 members there under BOTH readings. The TWIN ADDS NO ROW, so §1's battery-row count and every count in this cell — the `21 of the 30`, the `29 PATHS` and §8(l)'s `NINE` below — are unchanged, BOTH of its configs being SYNTHETIC (round 34, `CF34-1`). Row 47 is also round 17's (`CF17-1`: for sixteen rounds the DOWNWARD half was mandated by this row, by §2's `descendantConfigs` and by §3's rule 4 while NOTHING in §6 exercised it, so an implementation building the input set from rules 1-3 alone discharged this requirement with every assertion green; the arithmetic below is unchanged, row 47's fixture being SYNTHETIC and rule 4 still having 0 members on the live tree); row 3 asserts `stale-declarations` and NOT `stale-unit` since round 14 (C14-3), and its round-18 TWIN (`C18-4`) — mutating the fixture's `tsconfig.base.json` rather than the leaf — asserts BOTH: `stale-declarations` for §3.3's `and its chain` member and `stale-unit` on every unit the fixture holds, rule 3's upward chain carrying the base for all of them, which is why the twin does not re-open C14-3. The 21-of-30 arithmetic is unchanged: `tsconfig.base.json` was already counted as covered through rule 3's chain, so this widens ATTRIBUTION and not the count (§8(l)'s `read by some comparison` wording already admits it): `packages/cli/tsconfig.dts.json` sets `"composite": false` EXPLICITLY, so it fails §3 rule 4's clause (iii) — the ONE clause that excludes it, its chain resolving INTO `packages/cli` and its INHERITED `outDir` resolving to `packages/cli/dist`, which EQUALS that unit's `distDir` (re-derived read-only at `8fdf3ed`) — so rule 4 does not attach it and §3.3's comparand is what discharges it. ⚠️ **The earlier wording here — `governs .dts-cache/ and nothing in packages/cli's COMPARED output set` — named a build-SCRIPT fact that no walk over `allConfigs` can evaluate, and this row and §6 row 47 read it to OPPOSITE verdicts (round 24, `C24-1`).** ⚠️ **Bounded residuals — §8(l) and §8(f)**: discharged for **21 of the 30** tracked `tsconfig*.json` (re-derived 2026-08-20: `git ls-files \| grep -E 'tsconfig.*\.json$' \| wc -l` → 30 — the pipes are ESCAPED, and that is not cosmetic: GFM discards every cell past its header's column count and does not exempt a pipe inside a code span, so until round 12 this row truncated at `git ls-files` and everything after it was in the file and not on the page). `enumerateTsconfigs` returns 27 project dirs + 2 variants = **29 PATHS**, which is the CENSUS POPULATION and never the covered set. Re-derived 2026-08-20 by running §3's rules 3 and 4 over all 30: **21** are covered — the 19 unit `tsconfig.json` (rule 3's FIRST member, exercised by nothing until round 18's twin on §6 row 2 — `CF18-1`: under an ancestors-only walk all 19 leave every input set and this arithmetic reads **2 of 30**, `tsconfig.base.json` and `packages/cli/tsconfig.dts.json` being the only survivors, with every §6 element green) and `tsconfig.base.json` (rule 3's upward chain, length 2 for all 19) in some unit's INPUT SET, plus `packages/cli/tsconfig.dts.json` in §3.3's declaration-cache COMPARAND (round 14, C14-3; rule 4 has no member today, that config failing rule 4's clause (iii) and NOTHING else — round 24, `C24-1`) — and **NINE do not**. The nine are the root `tsconfig.json`, which bare `tsc -b` reads (`build` is `tsc -b && …`, `package.json:15`) and which carries no `compilerOptions` — residual **§8(l)** — plus the **8** non-`report` `e2e/*` configs, each declaring `"extends": "../../tsconfig.base.json"` (or `../../../`), which resolves to the root-level base and never INTO a unit, so neither rule reaches them, while `scripts/check-e2e-types.mjs:40` hands each to `npx tsc -p … --noEmit` for exactly `E2E_TYPECHECK_PROJECTS` (`:24-32`, wired as `check:e2e-types`, `package.json:24`) — residual **§8(f)**, bounded because all 8 set `noEmit: true` and emit nothing that can go stale                                                                                                                                                                                                            |
| 10  | wire into `check:all`                                                                | §5 — 15th member, FIRST in the chain, verbatim `npm run check:stale-dist -- --strict`; §6 pins `members[0]` by equality, so neither the position nor the flag can drift. ⚠️ **The OTHER half of this requirement — §5.1's `pretest` amendment — was asserted by nothing for eighteen rounds (round 19, `CF19-1`).** §6 pins `pretest`'s SCRIPT TEXT by equality and §6 row 21 pins `exitCodeFor` under both `strict` values, but no element ran the CLI as a PROCESS on a tree carrying a non-advisory finding: rows 27 and 31 are the only §6 elements that run it at all, and neither does — row 27's fixture is a malformed config, every exit code row 31 asserts is its LOAD-TIME mutant's, and row 31's clean baseline carries ZERO findings, where exit **0** is what a correct implementation and a hardcoded one BOTH return. A `check-stale-dist.mjs` whose success path calls `exitCodeFor(result, { strict: true })` — the argument hardcoded rather than threaded from the flag it parsed — therefore passed every element in this design while `pretest`, which carries no flag, exited **1** on the four `stale-unit` findings §6 predicts for this working copy, which §5.1's measured table makes PRETEST FAILED with `test` never running. §6 row **43**'s round-19 CLI twin is the element. ⚠️ **Bounded residual — §5.1's Honest bound**: npm's pre-hook is NAME-EXACT, so `pretest` reaches `npm test` and nothing else. Re-derived 2026-08-20 (npm 11.16.0): four sibling scripts bypass it — `test:watch` (`package.json:20`, `vitest`), `test:live` (`:21`), `test:e2e` (`:22`) and `test:e2e:release-evidence` (`:23`) — plus `npx vitest`, so the bypass set is **five**, not the one §5.1 named for twelve rounds. ⚠️ **`measured` covered TWO of those five, and the third KIND no npm measurement can reach in either direction (round 30, `CF30-3`):** §5.1's scratchpad ran `npm run test:watch` and `npm run test:e2e`, and what it MEASURED is npm's pre-hook NAME-EXACTNESS; `test:live` and `test:e2e:release-evidence` are members BY that property rather than by a run of their own, and `npx vitest` invokes no npm script at all. The set is unchanged and the property under it is measured; and `test:watch` is the vitest WATCH loop, the founding incident's own context. "Trigger's real reach stated" is the claim; §5.1 is where it is now measured                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 11  | call `scripts/repo-census.mjs` rather than re-deriving                               | §2 — `units.mjs` imports `enumerateRootReferences` + `enumerateTsconfigs` from `scripts/repo-census.mjs`. ⚠️ **That is §1's SECOND repo-internal import, and §1 counted "exactly one" for thirteen rounds (round 13, CF13-1)**: an implementer who honours that count discharges this row only by re-deriving the unit list inside `units.mjs`, which is verbatim what this requirement forbids, and passed every §6 row, because nothing in §6 READ the import list of `units.mjs` — until round 14 (CF14-2), when §6's wiring block gained a mutant-proved read of that file's `repo-census.mjs` import STATEMENT — which it did not become until round 29 (`CF29-2`) anchored the regex to the line start, the unanchored form having matched from the EARLIEST `import` substring in the semicolon-free span and so returned FALSE on a conforming commented source. ⚠️ **That read pinned ONE of the two calls this row is discharged by, for a round (round 15, CF15-2):** the predicate tested `enumerateRootReferences` alone, so a `units.mjs` importing that name and hand-rolling a `readdirSync` config walk beside it — verbatim what this requirement forbids, at the call feeding §3's rule 4 and requirement 9's arithmetic — returned true, and it is indistinguishable from a conforming one by the live smoke test (the unit SET is identical) and by every synthetic row, since rule 4 has no member on today's tree. Both names are now in the predicate, with `enumerateWorkspaces`, a renamed `enumerateTsconfigs` and — from round 29 (`CF29-3`) — an import of BOTH mandated names PLUS `enumerateWorkspaces` as the THREE mutants that must each FLIP it, ONE PER CONJUNCT: the first two left the NEGATED conjunct deletable, mutant 1 removing `enumerateRootReferences` in the same edit and mutant 2 never naming the forbidden one, so requirement 5's prohibition at this call was carried by a conjunct nothing flipped. That is what closes the gap this cell had only been able to STATE, and requirement 5's row carries the measurement of what the substitution costs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## 1. Shape and placement

`scripts/`, not the doctor registry. Three consequences, all verified:

- **It dissolves blind spot 4 for itself.** `scripts/` is compiled by no
  `tsconfig.json` (root `tsconfig.json:2` is `"files": []`), so this check is not
  a compiled artifact and cannot be disabled by the staleness it detects. The
  limit is still STATED in §8(a) because it remains true of `crabgic doctor`.
- It is covered by `packages/testkit/src/git-spawn-hygiene.test.ts` —
  `SCANNED_ROOTS` includes `"scripts"` and `SCANNED_EXTENSIONS` includes `".mjs"`
  (`:114,116`).
- Its colocated tests run under `npm test` via the `scripts` vitest project
  (`vitest.config.ts:84`, `test: { root: "scripts", name: "scripts" }`).

Node builtins, plus **exactly TWO** repo-internal imports — and the second one IS
requirement 11 (round 13, CF13-1). `units.mjs` imports `enumerateRootReferences` and
`enumerateTsconfigs` from `scripts/repo-census.mjs` (§0's requirement 11 row, §2), and
`walk.mjs` imports `PLUGIN_ASSET_ENTRIES` from `scripts/bundle-cli.mjs` (§3.3's clause 1,
§3.4's source side) — asserted since round 15 (CF15-4) by a mutant-proved read of THAT
file's import statement, the shape round 14 built for the first import; until then the
mandate had no surface anywhere and the duplicated literal it forbids passed every row.
⚠️ **BOTH reads match an import STATEMENT only from round 29 (`CF29-2`).** Until it
anchored them to the line start, each matched the whole semicolon-free span before its
import, which FAILED a conforming commented `units.mjs` and PASSED a `walk.mjs` naming
`PLUGIN_ASSET_ENTRIES` in a comment while importing some other binding — so "import
statement" was this design's word for fifteen rounds and not the predicate's. ⚠️ **Only the second one costs anything, and that is a MEASURED FACT
about `repo-census.mjs` rather than a property of it:** re-derived 2026-08-20,
`grep -n "^import" scripts/repo-census.mjs` returns `node:fs`, `node:path`,
`node:child_process` and `node:url` — builtins only — so the requirement-11 import
resolves nothing outside node and §8(n)'s bound covers `bundle-cli.mjs` alone. Should
`repo-census.mjs` ever take a dependency, §8(n) is the residual that widens, and this
sentence is the only thing in the design pointing at it. The design said "exactly one"
for thirteen rounds; an implementer who followed it literally satisfied it by re-deriving
the unit list in `units.mjs` — verbatim what requirement 11 forbids — and passed every §6
row, because no battery row or wiring assertion READ the import list of `units.mjs` — the
gap §6 closed in round 14 (CF14-2) with a mutant-proved read of that file's
`repo-census.mjs` import statement — ANCHORED to one only from round 29 (`CF29-2`),
above — widened in round 15 (CF15-2) to pin BOTH names, since
the one-name form left a hand-rolled `readdirSync` config walk beside
`enumerateRootReferences` passing — in the shape it already used for `bundle-cli.mjs`,
once `enumerateWorkspaces` was measured to be an exported STRICT SUBSET (18 of the 19,
missing `e2e/report`) that passed every assertion this design then had. ⚠️ **That import is
not free, and the flat "dependency-free" claim does not survive it (round 10, C10-2):**
`bundle-cli.mjs:59` is `import { build } from "esbuild"`, so importing the module resolves
`esbuild` at load time. Re-derived 2026-08-20: `esbuild` is a root **devDependency**
(`package.json:61`, pinned `0.28.1`), which `npm ci` installs — so `ci.yml:129`'s `test`
job, the check's ONE CI occurrence (§5, round 25, `CF25-3`), and every local invocation
resolve it, as does any `npm ci` tree with no build step (the constraint
`scripts/citation-content/file-index.mjs` documents in its header and
`scripts/repo-census.mjs` restates at `:39-41`). An `--omit=dev` install does
not — and what happens there depends on HOW the graph is entered, which this design now
rules on rather than leaving implicit. ⚠️ **IMPORT RULE (round 12, OP12-1):
`scripts/check-stale-dist.mjs` reaches `stale-dist/compare.mjs` and `stale-dist/report.mjs`
through `await import(...)` INSIDE its try/catch, never by a static `import` statement.**
Everything below those two keeps ordinary static imports — `walk.mjs`'s
`import { PLUGIN_ASSET_ENTRIES } from "../bundle-cli.mjs"` included, so no signature in §2
becomes async — because the whole static subgraph is resolved AT that `await import`, which
is inside the try. A static ESM graph, by contrast, is resolved and linked BEFORE the first
statement of the entry module runs, so the catch this section's file table and §4's
internal-error row assign as owner is not yet on the stack and cannot fire. Measured
2026-08-20 (node v24.18.0, npm 11.16.0, nothing else running but the shell) on a probe with
this design's exact shape — `cli.mjs` statically importing `sub/walk.mjs` importing
`sub/dep.mjs` importing an absent `esbuild`, with the call wrapped in the try/catch — BOTH
invocations printed no `check-stale-dist: ` line at all, node printed
`ERR_MODULE_NOT_FOUND` plus a ten-frame internal stack, and the process exited **1** with
`--strict` AND **1** without. By §5.1's own measured table (`pretest THROWS -> npm exits 1,
and test NEVER RUNS`) the non-strict path is then a total block on `npm test` — verbatim
round 1's `O-2`, re-opened by omission at the site round 10's C10-2 fix created. Under the
dynamic entry the throw lands in the catch and §4's row holds: `WARN`/exit 0 by default,
`ERROR`/exit **2** under `--strict`. Stated as residual §8(n), pinned by §6 row 31, and its
reachability is wider than `--omit=dev`: any incomplete or pruned `node_modules`, and any
load-time throw anywhere in the six-module graph, lands in the same uncatchable window. ℹ️ The alternative that
keeps `esbuild` out of the graph — never "a builtins-only import graph", which was
already false of the requirement-11 import above (round 13, CF13-1) — is to move the
const into a builtins-only
`scripts/plugin-assets.mjs` that BOTH files import; it costs one more file and one more §6
assertion (that `bundle-cli.mjs` still imports it, so the bundler cannot fork its own
list) and needs no entry-point guard.

| file                                | lines (est.) | purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `scripts/check-stale-dist.mjs`      | ~110         | `#!/usr/bin/env node`, the WHY header, arg parsing, CLI — **and the `try`/`catch` that owns §4's internal-error row**: it reaches `checkStaleDist` and `formatFindings` through `await import("./stale-dist/compare.mjs")` and `await import("./stale-dist/report.mjs")` **inside** the try — §1's IMPORT RULE, because a static `import` puts the whole six-module graph outside the catch and a load-time throw then exits **1** with an unprefixed node stack and no `check-stale-dist: ` line at all (measured, round 12, OP12-1) — then prints `formatInternalError(err, { strict, json })` (§2), passing the `--json` it parsed so a throw under `--json` emits the JSON error object and never the human line (round 12, C11-5), and exits **2** under `--strict`, **0** without — ⚠️ **and that PAIR is read on the `--json` path only since round 28 (`OP28-1`); for twenty-seven rounds this cell and §4 both mandated it there and NO element in §6 asserted an exit code on any `--json` invocation.** §6 row **27**'s third invocation now asserts exit **0** beside the parsed object, and its `C18-5` twin's third runs `--json --strict` and asserts exit **2** — so a catch spelled `const out = formatInternalError(err, { strict, json }); if (json) { console.log(JSON.stringify(out)); process.exit(0) }`, the ordinary "machine consumers get a clean stream and a clean code" branch and `CF19-1`'s own shape at the CATCH path rather than the success path, FAILS there instead of passing every element, where before it exited **0** on `npm run check:stale-dist -- --strict --json` against a malformed `tsconfig.json` and handed a consumer `ran: false` on a stream that had reported success. Exit 2 comes from here and never from `exitCodeFor`, which takes a `result` a throw never produces. ⚠️ **AND the SUCCESS path is `process.exit(exitCodeFor(result, { strict }))` with the `strict` THIS FILE PARSED, never a literal (round 19, `CF19-1`)** — this cell specified the catch path's two exit codes for eighteen rounds and said nothing at all about the other one, so an implementation that parsed `--strict` for `formatInternalError` (rows 27 and 31 pin that) and then hardcoded `{ strict: true }` at the success-path call — equivalently `process.exit(result.findings.length ? 1 : 0)` — satisfied every word here. §2 owns the FUNCTION and §6 row 21 asserts it under both values; what this cell owes is the ARGUMENT, and §6 row **43**'s CLI twin is what makes it falsifiable — ⚠️ **on FOUR flag settings since round 29 (`OP29-2`), the `--json` ones included, because this clause says `never a literal` and says nothing at all about `--json`:** a success path that emits the JSON object and exits **0** whenever `json` is set threads the parsed `strict` exactly as written here and still never REACHES `exitCodeFor`, handing a consumer a populated `findings` array on a stream that reported success — `CF19-1`'s own shape at the SIBLING of the path `OP28-1` closed in the catch two sentences above, and §4's "`--json` is unaffected on EVERY path" read at the path that had no element       |
| `scripts/stale-dist/units.mjs`      | ~150         | build-unit enumeration and per-unit input/output sets                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `scripts/stale-dist/walk.mjs`       | ~95          | mtime walk primitives, `sameBytes`, and the output/input classifiers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `scripts/stale-dist/compare.mjs`    | ~250         | the SIX comparisons — the FOUR cli-scoped ones (§3.2, §3.3, §3.4, §3.5) return `{ findings, coverage }`, the other two (`checkUnitFreshness`, `checkOrphans`) `Finding[]` — plus `unitState` (§3.3's three-way reason tables **and, for `packages/cli`, that section's three-clause conjunction plus its own read of the marker PATH** — naming one table as sole authority is what let C13-1 stand for eleven rounds and C14-1 for fourteen — **and `checkBundleFreshness`'s own EXISTENCE test on that SAME path, which is where its finding's and its coverage entry's `absent`/`unreadable` spellings both come from, the `metafile` argument being unable to separate the two (round 22, `C22-1`)**) and the `checkStaleDist` driver                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `scripts/stale-dist/report.mjs`     | ~115         | findings, **`states`** and coverage → text/JSON, and the exit code — ⚠️ **all THREE members §2's `checkStaleDist` returns, this cell having named two of them for twenty-three rounds (round 24, `OP24-2`): the two-of-three spelling is what invites a serialiser that drops the per-unit verdict array, and §6 row 43's CLI twin's `--json` invocation is the element that fails it**; §4's coverage line prints on BOTH paths **and on NEITHER when every entry is `full`**, and its units-compared/skipped summary prints in EXACTLY ONE of its two slots per run — the `PASS` line's own slot, or a line of its own where there is no `PASS` line, never both (round 21, `OP21-2`) — suppressed entirely, label included, on a fully covered run (round 16, `OP16-4`: stated in one §4 bullet and asserted by no row for sixteen rounds, so an unconditional emitter printed a bare `check-stale-dist: ⚠️ coverage: ` on every green build and passed every assertion; §6 row 38 is the row) — **and so does the units-compared/skipped summary** — the `PASS` line's own slot where there is one, its own line after the grouped findings where there is not (round 13, OP13-1); **the grouped `×N` collapse covers the FOUR delta-carrying kinds only**, while the four kinds whose §4 line carries per-finding text print one line each (round 13, OP13-3), **and the units-compared/skipped summary groups by REASON rather than by kind** — one clause per distinct `unitState()` reason, written `<units>: <reason>` (§4's **THE SKIPPED SUMMARY GROUPS BY REASON**, round 14, OP14-2: the per-unit form is 2,433 characters and 31 rows at 80 columns after `npm run build:clean`, against 512 and 7); **plus §4's REMEDY PLAN** — the block `formatFindings` computes from the finding SET **and from the COVERAGE entries** (the kinds, plus the UNIT for step 1's fourth carve-out, round 14, C14-5, plus each non-`full` entry's REASON, from which step 5 names its producer on a run holding no findings at all — round 18, `OP18-4`) with §4's FOUR carve-outs applied, printed whenever the run holds ≥1 finding OR ≥1 non-`full` coverage entry, withheld where NEITHER holds — the gate's FALSE side, asserted by §6 row 38's CLEAN side only since round 36 (`OP36-3`), without which a healthy tree is told to re-run the check on every green run — and withheld among DEGRADED runs ONLY on the `nothing was checked` PASS (§4's **A DEGRADED RUN CARRIES A NEXT ACTION**, round 14, OP14-1 — gated on findings alone the plan is absent from exactly the page §4's own recipe step 1 produces, which is the one page on which recipe step 5 is the only action stated anywhere), which had no owner, no shape and no assertion for twelve rounds (round 12, OP12-4), so an implementer printing the per-kind remedies beside each grouped block passed every row and handed the operator exactly the unordered list §4 measures as harmful; plus `formatInternalError` — the `WARN`/`ERROR` line §4's internal-error row prints, or the JSON error object under `--json` (round 12, C11-5), the one output this check writes that no `result` produces |
| `scripts/check-stale-dist.test.mjs` | ~680         | colocated suite (§6) — **49** battery rows, the false-negative battery, the mutant-proved wiring assertions and the live smoke test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

⚠️ **THREE EXISTING FILES MUST ALSO CHANGE. The first two were a round-8 fix; `package.json` is round 9's (CF9-3).** The first two were specified only in §3.2's prose for two rounds, and `package.json`'s three edits have lived only in §5/§5.1 for nine — so an implementer could build all six new files, pass every battery row and the wiring test, and still leave requirement 3's comparison **permanently muted**, or chain the check last and non-strict so it blocks nothing — the vacuity class this whole change set exists for.
Round 7 filed that as `CF-3` and the disposition did not reach this table; caught by
pre-checking before round 8 reported, which is the eighth consecutive partly-true
completion claim in this record and the first caught by its author.

| existing file            | change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | why it is load-bearing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `scripts/bundle-cli.mjs` | add `writeFile` to the `node:fs/promises` import (`:55` — absent today, measured; ⚠️ **`mkdir` is ALREADY in that same import, so the `mkdir` edit below widens nothing** — re-derived read-only 2026-08-21 at `3095116`, `:55` is `import { copyFile, cp, mkdir, readdir, rm } from "node:fs/promises";`); **clear** with `rm(join(CLI_ROOT, ".bundle-meta")…` in the wipe step, before `build()` at `:121`; **mkdir** with `mkdir(join(CLI_ROOT, ".bundle-meta"), { recursive: true })` and then **write** with `writeFile(join(CLI_ROOT, ".bundle-meta", "metafile.json")…`, the pair placed together after the plugin-copy loop, beside `:181`. ⚠️ **THE `mkdir` IS UNCONDITIONAL AND IS AN EDIT IN ITS OWN RIGHT, never a consequence of any factoring (round 21, `C21-2`).** `packages/cli/.bundle-meta/` is gitignored by this change set's own `.gitignore` edit and is created by nothing today — re-derived read-only 2026-08-21 at `3095116`: `grep -c bundle-meta scripts/bundle-cli.mjs` → **0**, and the directory does not exist — so it is absent on the FIRST build of every tree, and the wipe-step clear removes it again on every build thereafter, whatever an implementer factors. Land exactly the other five edits and the write throws `ENOENT: no such file or directory, open '<cli>/.bundle-meta/metafile.json'`. `build` is `tsc -b && npm run bundle:types && npm run bundle:cli` (`package.json:15`), so `npm run build` then fails at its last step **on every tree, forever** — and `npm run build` is the remedy §4's table prints for `stale-bundle`, `stale-plugin-assets`, `unbuilt` and `bundle-provenance-missing` and the command its recipe steps 2 and 5 name, so every printed remedy in this design would terminate in a failing command: round 1's `O-2` class ("following the design would have left an operator unable to") at the build program rather than at the test hook. The in-file precedent is `:176`'s own `await mkdir(pluginOut, { recursive: true });` immediately before the `PLUGIN_ASSET_ENTRIES` loop — the exact shape and the exact neighbourhood, copied WHOLE per standing rule 7. It carries **a source property of its own since round 22 (`CF22-1`), because the reason round 21 gave for withholding one is true of an EXISTENCE assertion and false of an ORDER one**: a `mkdir` mutant need not be a deletion. Hoist it above the wipe-step clear and the clear deletes the directory it just made, so `writeFile` throws the very `ENOENT` `C21-2` measured — on a tree where all six edits are PRESENT, reached through ORDER instead of absence, and non-tautological in exactly the way §6's other placement mutants are. §6 therefore carries a `MKDIR` anchor beside `CLEAR` and `WRITE` and asserts two order conjuncts, `search(CLEAR) < search(MKDIR)` and `search(MKDIR) < search(WRITE)` — the FUNCTIONAL order only, never this cell's stronger "the pair placed together", which an implementer may violate without breaking a build; §8(k) records that as the bound. §6 anchors on those THREE CALLS — on the callee plus one distinct literal, never on the path alone: all three edits name `.bundle-meta`, so a bare path anchor cannot tell them apart (`CF9-4`). ⚠️ **They are NOT contiguous substrings, and were until round 20 (`CF20-1`).** The conforming one-line write at this file's 2-space indent is exactly **100** characters, `.prettierrc.json` sets `printWidth: 100`, `.prettierignore` does not cover `scripts/` and `npm run format` is `prettier --check .` — so the anchored line sits AT the width with ZERO margin, and one further argument makes the formatter wrap the call across four lines. Measured 2026-08-21 at `9c247eb` with this repository's `node_modules/.bin/prettier` 3.9.5: the 100-char form survives `--check`, the one-argument-longer form is rewritten across four lines, and on the rewritten form the old `includes` anchor is FALSE while every edit is landed and ordered — an assertion failing a correct implementation, `OP16-3`'s class. §6's anchors are therefore `/rm\([^;]+"\.bundle-meta"/` and `/writeFile\([^;]+"metafile\.json"/`, written here in the SAME spelling §6's consts carry — escaped dots included, because two spellings for one anchor is the defect this design files elsewhere in terms — in the newline-spanning shape §6's two import predicates already used, with the quantifier a plus and never a star per `CF15-1`. **TWO MANDATES follow (the second is round 22's, `CF22-1`), recorded here because §1 is where §6's anchors are prescribed:** the clear names `".bundle-meta"` INSIDE the `rm(` call, and the `mkdir` names it INSIDE the `mkdir(` call, never through a module-level `META_DIR` const. The write survives the `const metaDir = join(CLI_ROOT, ".bundle-meta")` factoring an implementer may reach for once the `mkdir` and the `writeFile` name one directory between them, because its own literal is the FILENAME; the clear and the `mkdir` have no second literal and do not survive it, so an unmandated `MKDIR` anchor would FAIL a factored-but-correct implementation — `OP16-3`'s class, which is why the mandate is stated here rather than left for §6 to guess. ⚠️ **This clause CONDITIONED the `mkdir` on the factoring, and the conditioning is false in both directions (round 21, `C21-2`).** The directory is absent on the first build of every tree, factored or not, so the `mkdir` is mandated above unconditionally and this clause keeps only its actual subject: which of the two ANCHORS survives a `metaDir` const, and why §1 therefore mandates the literal INSIDE the `rm(` call. §8(k) records that as the bound. ⚠️ **TWO MORE EDITS TO THIS FILE, and nothing above is reachable without them (round 10, C10-2; renumbered (5)/(6) in round 21, `C21-2`, the `mkdir` entering this cell's prose as edit 3 and these ordinals being POSITIONAL, which is what makes the tail enumeration below checkable against this cell's own body — the two sites that cited these ordinals from outside, §2's `formatInternalError` comment and §6's `EXPORTED` const, are repointed onto the edit's NAME rather than onto a number, per `C11-4`):** (5) `export` the `PLUGIN_ASSET_ENTRIES` const at `:89` — re-derived 2026-08-20, `grep -n "^export" scripts/bundle-cli.mjs` returns `:71` `EXTERNAL_DEPENDENCIES` and nothing else, so §3.3's clause 1 and §3.4's source side both cite a binding no importer can obtain, and a literal `import { PLUGIN_ASSET_ENTRIES } from "../bundle-cli.mjs"` fails to link ("does not provide an export named") — and that reaches §4's internal-error row (`ERROR`, exit **2**, from `check:all -- --strict` at member 0) **only under §1's IMPORT RULE**. Measured 2026-08-20 (node v24.18.0): with the entry module importing statically, the same link failure is a `SyntaxError` raised before its first statement runs, so no catch sees it — exit **1**, unprefixed stack, no `check-stale-dist: ` line, and under `pretest` no test suite at all (round 12, OP12-1); (6) put the file's last statement `await main()` behind the entry-point guard `scripts/repo-census.mjs:382` already uses in this repo (`process.argv[1] && statSync(process.argv[1]).ino === statSync(fileURLToPath(import.meta.url)).ino`) — without it a namespace import executes `main()` and REBUILDS `packages/cli/dist` from inside the check that observes it, the side effect round 3 disclosed, and throws at `:147` on a fresh clone. Nothing imports this module today (re-derived 2026-08-20: its only consumer is `package.json:48`'s `node scripts/bundle-cli.mjs`), so the guard changes no existing behaviour. **SIX edits to this file** — the `writeFile` import, the clear, the **`mkdir`**, the write, the `export`, the entry-point guard, in this cell's own order of prescription — **of which §6 asserts FIVE SOURCE PROPERTIES** (four until round 22, `CF22-1`): the clear precedes `build()`, the `mkdir` follows the clear and precedes the write, the write follows the plugin copy, `PLUGIN_ASSET_ENTRIES` is `export`ed, `await main()` sits behind the ino guard. ONE of the six edits is NOT separately asserted — the `writeFile` import — §6's `WRITE` anchor pins the CALL, not the import. **Edits and asserted properties are different counts, stated apart** (round 12, CF12-4: this cell read "FOUR edits … all four asserted" inside its own "TWO MORE EDITS TO THIS FILE … (4) … (5)") | it is requirement 3's provenance oracle — the only record of what entered this bundle (§3.2) — and after round 9 its ABSENCE reports `bundle-provenance-missing` and never `unbuilt`; `unbuilt` keys on C9-1's freshness rule, which reads artifacts in the tree and needs no history. **The write's POSITION is what the rest rests on**: `:181` runs after every byte `bundle-cli.mjs` puts under `dist`, so a present marker implies every earlier step ran and a completed bundle always leaves mtime(marker) >= the newest qualifying output under `dist` — §3.3's marker-freshness clause — and `bundle-provenance-missing` is reachable only in the `:178`→`:181` window. Placed earlier it proves only that esbuild ran. ⚠️ **The wipe-step clear is retained as defence in depth and is NO LONGER the invalidation mechanism (round 9).** `build` is `tsc -b && npm run bundle:types && npm run bundle:cli` (`package.json:15`), so an interrupt during the ~5-minute `bundle:types` step means `bundle-cli.mjs` never runs and the clear never fires; the clear covers the classes where the bundler IS executing, the freshness comparison covers the classes where it is not. **Not cleared first the marker describes some OTHER run's bundle** — `OUT_DIR` is `dist` only (`:63`), so neither the wipe (`:113`) nor the design's own `rm -rf packages/cli/dist` touches the sibling `.bundle-meta/`, and a surviving metafile would name a stale inlined unit set |
| `.gitignore`             | append `packages/cli/.bundle-meta/` after the `packages/cli/.dts-cache/` entry at `:47`. Measured 2026-08-20: `git check-ignore -v packages/cli/.bundle-meta/` reports nothing and `grep -n bundle-meta .gitignore` returns no hit, so the directory is untracked-and-unignored today                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `.dts-cache/` is ignored at `:47`; without the matching entry every build leaves an untracked file that `repo-census.mjs` reports in its "on disk, neither tracked nor ignored" bucket and that sits in `git status` forever. §6 asserts it by reading `.gitignore`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `package.json`           | add `"check:stale-dist": "node scripts/check-stale-dist.mjs"` and `"pretest": "node scripts/check-stale-dist.mjs"` (neither carries `--strict`), and insert `npm run check:stale-dist -- --strict` as **`check:all`'s member 0**, before the 14 that exist today                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | position and flag ARE requirement 10's value. Measured: `check:tarball` is member 12 and `check:install-smoke` member 13, and both read local `dist` state (defect `25-install-smoke-depends-on-local-dist-state.md`), so a member appended last runs AFTER the two §5 orders it before; and without `--strict` `check:all` exits 0 on a stale tree. §6 asserts `members[0]` by equality                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

§6 asserts all three **by reading them**, in the source-reading shape
`check-support-window-freshness.test.mjs:543-544` and
`check-marketplace-pin-digest.test.mjs:353-358` use — `:530`/`:339`, cited here until
round 9, assert `package.json`, not a file read — and for ORDER the from-0 comparison
`run-e2e-suites.test.mjs:31` uses: `indexOf` for the two literal anchors and `search()`
for the three regex ones — `CLEAR`, `MKDIR` and `WRITE`, the second of them since round 22
(`CF22-1`) — which is the same comparison for a pattern (round 20, `CF20-1`).
SIX source properties, of which **FIVE** are proved non-vacuous by a mutated copy of the
source: the clear precedes `build()` — proved non-vacuous only from round 27 (`CF27-1`), no
mutant before it flipping that conjunct, and only on a source whose `mkdir` FOLLOWS the
plugin-copy loop, this cell's own prescribed placement (round 28, `CF28-1`): on a conforming
source whose `mkdir` sits earlier — §8(k) licenses one in terms, and one between `build()` and
the loop behaves identically — round 27's clear-sink flips `search(CLEAR) < search(MKDIR)` as
well, and that conjunct is deletable again with all seven mutants still flipping; ASSERTED on
every conforming placement, PROVED on one — the `mkdir` sits between the clear and the write,
the write follows the plugin copy, `PLUGIN_ASSET_ENTRIES` is `export`ed, and
`await main()` sits behind the entry-point guard. ⚠️ **The sixth is not, and "each
proved non-vacuous by a mutated copy" stood here
for eleven rounds and was false of it (round 20, `CF20-1`).** That `.gitignore` carries the
entry is a bare `toContain` on the file's text — re-derived 2026-08-21, §6's line is
`expect(readFileSync(".gitignore", "utf8")).toContain("packages/cli/.bundle-meta/")` and
no mutant stands beside it — whose only failure mode IS the absence it asserts against, so
none was ever written. Stated apart rather than papered over, since a mutant there could
only restate the assertion. The last two are round 10's (C10-2): without
them §3.3's clause 1 and §3.4's source side name a const no importer can reach, and the
only import that reaches it runs the bundler.

⚠️ **The third file, `package.json`, is deliberately NOT asserted in that citations' shape (CF9-3).** `:530` and `:339` are both `expect(root.scripts["check:all"]).toContain("check:…")` — membership only, blind to position and to flags. That is all those two checks need; here it is exactly the assertion a last-position, non-strict member passes. §6 pins this one by index and equality instead — `expect(members[0]).toBe("npm run check:stale-dist -- --strict")` (§5) — because position and flag ARE requirement 10's value.

The subdirectory follows `scripts/check-citation-content.mjs` +
`scripts/citation-content/*.mjs`; the colocated `*.test.mjs` follows
`scripts/repo-census.test.mjs`.

## 2. Elements

```
// units.mjs
export function buildUnits(cwd)                      -> Unit[]
// ⚠️ **`srcDir` and `distDir` are DERIVED per unit, never `join(cwd, dir, "src")` /
//   `join(cwd, dir, "dist")` (round 21, `C21-1`).** `buildUnits` resolves each unit's
//   `compilerOptions.rootDir` and `compilerOptions.outDir` from `tsconfigPath`'s own
//   resolved `extends` chain — the chain `configChain` already returns — and defaults to
//   `./src` and `./dist` when neither the unit config nor its chain sets them. §3's Units
//   step mandates that reading in terms, and this section DECLARED both fields while
//   saying nothing about where they come from, so the hardcoded body was the reading §3's
//   own rule text endorsed. It is indistinguishable on the live tree — re-derived
//   read-only 2026-08-21 at `3095116`, `rootDir === "./src" && outDir === "./dist"` for
//   **19 of 19**, exception set EMPTY — and §6 row 49 is the SYNTHETIC row that fails it
//   on its own fixture, needing no mutant swap.
// ⚠️ **`kind` IS DERIVED TOO, and its derivation was stated in NO section for twenty-two
//   rounds (round 23, `C23-1`, standing rules 7 and 9).** `C21-1` closed `srcDir` and
//   `distDir` in THIS comment block and stopped at two fields of the three, the third being
//   declared four lines into the same struct: `kind` is `"bundled"` for the ONE unit
//   `scripts/bundle-cli.mjs` bundles — `packages/cli`, whose `dist` that program's
//   `CLI_ROOT`/`OUT_DIR` write (`bundle-cli.mjs:63`), read as a DESIGN-TIME fact and NEVER
//   imported, so §1's exactly-two repo-internal-import count and §8(n)'s bound are both
//   untouched — and `"tsc"` for EVERY other root reference. **Never off `Unit.references`,
//   never off `packages/cli`'s reference closure, and never off `dist/` (`buildUnits` never
//   reads it, below).** §3.1's scope predicate is this field's ONLY consumer, and
//   `Unit.references` and `Unit.configs` are read by NOTHING anywhere in §3-§6 — re-derived
//   read-only 2026-08-21 at `53996f5`, zero occurrences of either outside this struct — so
//   the closure is the one other thing `references` could be for, and that reading marks
//   **17 of 19** `"bundled"`, leaves `checkOrphans` running on `packages/perf` and
//   `e2e/report` ALONE, and makes requirement 2 INERT for the other 16 at PASS, exit **0
//   even under `--strict`**, on a tree carrying an orphan. Re-derived read-only at
//   `53996f5` by parsing the live tsconfigs (`git status --porcelain` empty before and
//   after; no import of `scripts/bundle-cli.mjs`): root references 19, `packages/cli`
//   direct references 16, transitive closure 16, complement exactly those two units. §6
//   row 4's round-23 TWIN is the element, and it pins the `packages/cli` exemption
//   POSITIVELY for the first time — §3.1 has asserted it in prose since round 1 and no
//   element read it.
export function configChain(tsconfigPath, cwd)       -> string[]   // upward `extends`, cycle-guarded
// ⚠️ **THE RETURN INCLUDES `tsconfigPath` ITSELF, as its first member (round 18,
// `CF18-1`).** §3's input rule 3 has TWO members, and a return of the resolved ANCESTORS
// alone satisfied everything in §6: row 2's mutation is the shared `tsconfig.base.json`
// and no battery row moved a unit's own `tsconfig.json`, so all 19 unit configs left every
// input set with every assertion green and `utimesSync packages/gates/tsconfig.json`
// printed PASS on a `dist` compiled under the old `compilerOptions`. §6 row 2's twin is
// the non-vacuity row and it fails that body on its own fixture, needing no mutant swap.
export function descendantConfigs(unitDir, allConfigs) -> string[] // configs extending DOWNWARD into the unit
//   ⚠️ **THE PREDICATE IS §3 INPUT RULE 4'S THREE CLAUSES, NOT THE DOWNWARD HALF ALONE
//   (round 24, `C24-1`).** A member's chain must resolve INTO `unitDir`, its RESOLVED
//   `outDir` must land in that unit's COMPARED output set — CONTAINMENT at or below that
//   unit's `distDir` past §3's minus-bullets, and never EQUALITY with `distDir`, which
//   `distDir` itself satisfies (§6 row 3) and so does any subdirectory no minus-bullet
//   reaches (§6 row 47, round 27, `C27-1`) — AND its RESOLVED `composite`
//   must be `true` — all three read off the config's own `extends` chain, which is the
//   whole of what `allConfigs` supplies AS CRITERION: the argument is a list of config
//   PATHS, and NOT ONE of the three clauses is a test on a member's own path (round 33,
//   `CF33-1`), and all three parsed with the raw `JSON.parse`
//   §3 already mandates for rules 3 and 4. This comment declared the FIRST clause only, so
//   a body implementing it alone attaches `packages/cli/tsconfig.dts.json` to
//   `packages/cli` — the one config of the 30 whose chain resolves into a unit — and fires
//   `stale-unit packages/cli` on any `touch` of it (a `git checkout`, a branch switch,
//   format-on-save) at `check:all` member 0, exit **1** under `--strict`, printing
//   `rm -rf packages/cli/dist` plus a 19-unit rebuild for a change whose whole remedy is
//   §4's steps 3-4, and manufacturing the SECOND kind that defeats §4 step 2's
//   sole-`stale-declarations` carve-out: verbatim the harm `C14-3` filed. §6 row 3's
//   round-24 MUTANT is that body's element and fails it on row 3's own fixture.
//   ⚠️ **AND A BODY SPELLING (i) AND (iii) ALONE — clause (ii) DROPPED — is a THIRD wrong
//   body, killed only by §6 row 47's ROUND-25 TWIN (round 25, `CF25-1`).** Rows 3 and 47
//   BOTH SATISFY (i) and (ii) and differ on (iii) alone, so clause (ii) was a mandated
//   conjunct no element in this design could see, and §8(l)'s "the price of making clause
//   (ii) decidable" reads as licence to drop it. That body — the path of least resistance,
//   (i) and (iii) both being reads of the chain this function already walks while (ii)
//   additionally needs the unit's filtered output set — attaches row 47's config, declines
//   row 3's, and passes BOTH rows; the live tree cannot see it either, rule 4 having 0
//   members there under both readings. The twin's `packages/A/tsconfig.emit-elsewhere.json`
//   passes (i) and (iii) — `composite` INHERITED-TRUE — and fails (ii) with
//   `"outDir": "./dist/eo-elsewhere-fixture-1"`, INSIDE `packages/A/dist` RAW and OUTSIDE
//   the COMPARED set through the output set's `dist/eo-*-fixture-*/**` minus-bullet, and
//   must be in NO input set — which that body denies on the twin's own fixture, and so
//   does a FOURTH wrong body, clause (ii) evaluated as RAW path containment under
//   `<unit>/dist` (round 26, `CF26-1`): the cheaper reading, an `outDir` being a DIRECTORY
//   while the compared set is a set of FILES that need not exist yet, and the reading
//   `C14-3` refused. ONE twin, BOTH bodies, needing no mutant swap. ⚠️ **And a FIFTH wrong body reads (ii) as EQUALITY
//   with the unit's `distDir` (round 27, `C27-1`)**, which declines the twin's config too and
//   is killed by row 47's MAIN half ALONE, whose `outDir` is `"./packages/A/dist/emit"`,
//   resolving from that fixture's ROOT to a STRICT subdirectory a correct body ACCEPTS. So
//   each of the THREE clauses now has its own EXCLUDING
//   element, clause (i)'s from round 34 (`CF34-1`) — (iii) at §6 row 3's round-24 mutant, and
//   (ii) and (i) at §6 row 47's round-25 twin, on its FIRST and its SECOND config
//   respectively — and
//   row 47's main half is the positive side, clause (ii)'s ACCEPTING element AND, from round
//   33, clause (i)'s.
//   ⚠️ **A SIXTH WRONG BODY SPELLS CLAUSE (i) AS PATH CONTAINMENT —
//   `allConfigs.filter((c) => c.startsWith(unitDir + "/"))`, `unitDir` being a PATH and this
//   function's FIRST argument — and it is killed by row 47's MAIN half ALONE (round 33,
//   `CF33-1`).** Clause (i) tests the RESOLVED `extends` chain and NEVER where the config
//   file itself sits, and until that round every rule-4 config in this design sat INSIDE its
//   unit's directory — row 47's under `packages/A`, row 3's under `packages/cli` — so the
//   two readings agreed on all of them. The live tree cannot separate them either: re-derived
//   read-only 2026-08-22 at `2ef2ce5`, `packages/cli/tsconfig.dts.json` is the ONE config of
//   the 30 whose chain resolves into a unit AND it sits under that same unit, so rule 4 has 0
//   members under BOTH readings. Row 47's config now sits at its fixture's ROOT and extends
//   `./packages/A/tsconfig.json`, so that body attaches nothing there and FAILS the row on
//   the row's own fixture, needing no mutant swap.
//   ⚠️ **A SEVENTH WRONG BODY DROPS CLAUSE (i) ENTIRELY — `allConfigs` filtered on (ii)
//   AND (iii) ALONE, with NO chain test at all — and it is killed by row 47's ROUND-25 TWIN's
//   SECOND config (round 34, `CF34-1`).** It is the cheapest reading of the sentence above:
//   the argument is a list of PATHS, and clause (ii) already ties a config to ONE unit, so the
//   chain walk buys nothing its author can see. EVERY rule-4 config in this design SATISFIES
//   (i) — row 47's main half, that row's twin's `packages/A/tsconfig.emit-elsewhere.json` and
//   row 3's `packages/cli/tsconfig.dts.json` — so until that round clause (i) was DELETABLE
//   with every element still passing, and the live tree cannot see the difference either:
//   re-derived read-only 2026-08-22 at `5e4918f`, the ONLY configs of the 30 passing (ii) and
//   (iii) there are the 19 units' own `tsconfig.json`, each attaching to the SAME unit it is
//   already a rule-3 member of, so NO unit's input set gains a member and this body is
//   invisible on this tree exactly as the correct one is. Its wrong result is §3's own worked
//   example one clause over — `packages/gates/tsconfig.standalone.json`, with
//   NO `extends`, `"composite": true` and `"outDir": "./dist"` — which fails (i) correctly and
//   which this body ATTACHES, so a `touch` of it prints `stale-unit packages/gates` with
//   `rm -rf packages/gates/dist` plus a 19-unit rebuild for a config whose output side provably
//   cannot move for it. The twin's SECOND config is that shape inside the fixture and NEWER
//   than `packages/A/dist`, so this body renders `stale-unit packages/A` at the twin's BASELINE.
//   §6 row 47 is its non-vacuity row, and round 17 is the first round in which it has one
//   (`CF17-1`): until then this element, §3's input rule 4 and §0's requirement 9 row all
//   mandated the downward half while nothing in §6 exercised it, so a body returning the
//   empty array passed the whole suite and requirement 9's downward half shipped
//   unimplemented. Row 47 fails that body on its own fixture, needing no mutant swap.
export const PRODUCER_INPUTS                                       // build programs, per artifact
//   TWO members, each one comparand member of ONE comparison: `scripts/bundle-cli.mjs` on
//   §3.2's input side and `scripts/bundle-types.mjs` in §3.3's declaration-cache comparand.
//   ⚠️ **Both are REPO-RELATIVE and are resolved against the run's `cwd` — the one
//   `checkStaleDist` was called with and `buildUnits(cwd)` has already joined into every
//   `Unit`'s `srcDir` and `distDir` — never against `process.cwd()` and never against this
//   module's own `import.meta.url` (round 15, C15-3).** Neither
//   `checkBundleFreshness(units, metafile)` nor `checkDeclarationCache(units)` takes a
//   `cwd`, so each recovers it from the `packages/cli` `Unit` it already holds — the same
//   root that unit's `.dts-cache/index.d.ts` comparand is resolved from — and no signature
//   in this section changes arity. Stated because a check that stats its OWN repository's
//   copies cannot be exercised by a synthetic fixture at all, which is how these two
//   members reached round 15 with no assertion of any kind touching them. §6 rows 41 and 42
//   are the non-vacuity rows and both mutate the fixture's own copies. ⚠️ **And they cover
//   DIFFERENT halves of `each one comparand member of ONE comparison` (round 25,
//   `CF25-2`):** row 42's `SOLE kind` is the ONE-comparison half for `bundle-types.mjs`;
//   row 41 asserted only `stale-bundle` and NOT `stale-unit` until round 25, so for
//   `bundle-cli.mjs` — the producer with three outputs, and the only one this clause can
//   be false of — the half was mandated here and asserted nowhere, and a wiring adding it
//   to §3.4's source side passed the whole battery. Row 41 now carries the SOLE-kind half
//   and that wiring as its second mutant.

// Unit = { dir, srcDir, distDir, tsconfigPath, configs: string[],
//          references: string[], kind: "tsc" | "bundled" }
// A Unit carries NO state flag of any kind. `buildUnits` never reads `dist/`, so it
// cannot know one; the verdict is `unitState(unit)`'s, computed once by `checkStaleDist`.
// The corollary, stated because leaving it implicit is what made round 8's rule
// unimplementable at BOTH ends (round 9, C9-1): if no field on Unit carries a `dist`
// fact, then `unitState` is the function that reads them, and it is NOT pure over its
// argument. It stats `<unit>/dist` through `walk.mjs` and, for `packages/cli` only,
// tests `packages/cli/.bundle-meta/metafile.json` for EXISTENCE and reads its MTIME off
// that path directly for clause 2 — never off `readMetafile`'s return, which is
// `undefined` for an absent marker AND for a present-but-unparseable one and therefore
// cannot separate them (round 14, C14-1). Existence is tested BEFORE the mtime read, so
// this adds no error path and C10-3's "never throws" rule is untouched. It also
// calls `readMetafile(cwd)` and then `cliArtifactGaps(cwd, metafile)` — the TWO-argument
// form declared below, which round 12's C11-6 introduced without reaching this caller
// (round 13, C13-1). ⚠️ **NOT presence facts alone, and this line said so for four
// rounds.** Round 9 moved completeness off the marker onto §3.3's artifact set, so
// clauses 1 and 3 need no timestamp — but clause 2 compares TWO mtimes, the marker's
// against the newest member of §3's filtered output set, and `unitState` is what
// evaluates it. So the outcome list below has THREE branches, not two, and §3.3's cli
// reason table has a row for the third. `readMetafile` is called HERE rather than passed
// in, so that no signature in this section changes arity — `unitState(unit)` is cited in
// that exact form by §3's Skip rule and by §6 row 15. Stated cost: the marker is parsed
// twice per run, once here and once by `checkStaleDist` for `checkBundleFreshness`;
// `readMetafile` never throws (below), so the second read adds no error path.

// walk.mjs
// ⚠️ EVERY ONE OF THESE PRIMITIVES RECURSES, and §6 required no member below depth 0
//   until round 18 (`C18-1`). `newestUnder` and `stemsUnder` walk `<dir>` recursively —
//   §3's input rule 1 and its `<unit>/dist/**` output glob both say so — and §6 row 1's
//   amended fixture (mtime side) plus row 4's (stem side) are the two rows a `readdirSync`
//   without `recursive: true` now fails on their own fixtures, needing no mutant swap.
export function newestUnder(dir, accept)             -> { path, mtimeMs } | undefined
export function newestOfEntries(paths, accept)       -> { path, mtimeMs } | undefined
//   MIXED-TYPED roots: `statSync`s a regular-file member, walks a directory member.
//   §3.4's SOURCE side needs it and `newestUnder(dir, …)` cannot do it — an argument
//   about the PER-ENTRY application ALONE, narrowed in round 28 (`C28-1`) because a
//   `newestUnder` applied to the PACKAGE ROOT is not reached by it: of the six
//   `PLUGIN_ASSET_ENTRIES` — obtained as
//   `import { PLUGIN_ASSET_ENTRIES } from "../bundle-cli.mjs"`, which requires §1's
//   `export` and entry-point-guard edits and is NOT satisfiable today: re-derived
//   2026-08-20, `:89` is a bare `const` and the file's last statement is `await main()`
//   (round 10, C10-2) — five are directories and
//   `packages/plugin/.mcp.json` is a regular FILE. Re-derived 2026-08-20 against the
//   design's own six-entry list: `readdirSync` on that member throws
//   `ENOTDIR: not a directory, scandir '…/packages/plugin/.mcp.json'` — thrown on a
//   fully built, CLEAN tree, which `check-stale-dist.mjs`'s try/catch (§1) turns into §4's
//   internal-error row — `WARN` and exit 0 by default, `ERROR` and exit **2**,
//   from every `npm run check:all -- --strict` (round 10).
//   ⚠️ **That is the MIXED-TYPE half of §3.4's mandate and NOT its SCOPE half (round 28,
//   `C28-1`).** The source side is equally the six ENTRIES and never the package that
//   CONTAINS them, and `newestUnder(join(cwd, "packages/plugin"))` — one call, and `cwd`
//   is the only argument `checkPluginAssets(cwd)` offers — never hands `.mcp.json` to
//   `readdirSync`, so it never throws and neither this declaration nor §6 row 24 can see
//   it. §6 row 8's SECOND MUTANT is the SCOPE half's element, failing on that row's own
//   fixture with no injection point.
export function stemsUnder(dir, accept)              -> string[]   // extension-stripped, sorted
export function isCompilerOutput(unit, relPath)      -> boolean    // requirement 1
//   TWO halves: a four-extension WHITELIST (`.js`, `.d.ts`, `.js.map`, `.d.ts.map`) and
//   §3's four minus-bullets. Only the minus-bullets had elements until round 20
//   (`C20-1`), and the two wrong readings are two verdicts apart on this repository's own
//   tree. ⚠️ **AND THE WHITELIST ROUND 20 ADDED THEN VACATED TWO OF THOSE
//   MINUS-BULLET ELEMENTS, WHICH THAT ROUND DID NOT RE-CHECK (round 35, `C35-1`, standing
//   rule 8).** `.tsbuildinfo` is rejected by EXTENSION, so its bullet is subsumed
//   UNCONDITIONALLY and NO body can discriminate it — round 23's own note at §6 row 5's
//   ROUND-23 twin (qualified in round 36, `C36-1`: row 5 carries a SECOND twin from that
//   round, so `C20-2`'s must-say-WHICH rule reaches this citation). `dist/plugin/**` is subsumed only CONTINGENTLY, and §6 row 7's mutation was
//   `dist/plugin/.mcp.json`, whose `.json` the whitelist alone rejects, so the
//   `packages/cli` plugin bullet was mandated here, in §0's requirement 1 row and in §3
//   and exercised by NOTHING once round 20 landed. Row 7's mutation is a WHITELISTED
//   extension under `dist/plugin` from round 35, and its SECOND MUTANT is the
//   whitelist-carrying, plugin-bullet-omitting body. §6 row 44's round-20 TWIN fails BOTH on its own fixture, needing no mutant swap:
//   a whitelist of `.js`/`.d.ts` alone, which reads the newest output as the newest
//   `.js`/`.d.ts` and reports `stale-unit` wherever a `.d.ts.map` is newer — the state
//   THREE units of this working copy are in, re-derived read-only 2026-08-21 — and a
//   reading with NO whitelist at all, which takes a `dist/**.json` as an output and names
//   it `orphan-output`.
//   ⚠️ **The whitelist governs BOTH walked sides — the mtime set §3 defines and the stem
//   set §3.1 compares — which are `newestUnder`'s and `stemsUnder`'s accept predicates and
//   are "two places one filter has to land" (§6 row 44's own words for the EXCLUSION side,
//   read here at the INCLUSION side). This declaration does NOT merge them:** row 44's twin
//   kills the too-narrow whitelist at the MTIME side and the absent whitelist at the STEM
//   side on ONE fixture, so neither site can be left unfiltered, and rows 44 and 14's
//   existing division of the scratch exclusion between the two is untouched (`C16-1`).
export function isCompilerInput(relPath)             -> boolean
export function sameBytes(pathA, pathB)              -> boolean    // size, then content — §3.5
//   ⚠️ **BOTH steps, and NO element read the second until round 34 (`C34-1`).** The size
//   step is an optimisation; §3.5's rule is `their bytes differ`. A body returning
//   `statSync(a).size === statSync(b).size` — this comment's first step and not its second —
//   passed every row in §6, because every fixture there in which the two files DIFFER also
//   gave them DIFFERENT sizes. §6 row 22's TWIN is what separates them: both declaration
//   files pinned byte-identical at 28 bytes, rewritten to a barrel of the SAME length. Do
//   not delete that pin without replacing the element.

// compare.mjs
export function checkUnitFreshness(unit)             -> Finding[]  // reqs 1, 5, 6, 9
export function checkOrphans(unit)                   -> Finding[]  // req 2
export function readMetafile(cwd)                    -> object | undefined
//   absent OR UNPARSEABLE ⇒ `undefined`, and `undefined` ⇒ a `bundle-provenance-missing`
//   finding with the reason its case mandates (§3.2) — and, since round 22 (`C22-1`), the
//   `bundle-freshness` coverage entry's reason with it, one spelling per state across both
//   surfaces. ⚠️ **THOSE TWO HALVES WERE FIVE LINES APART, round 14's inserted block
//   standing between them, from `badf45a` through `5fef126` (round 22, `C22-1`)** — the
//   orphaned half read as a fragment below "whether the bundle COMPARISON can run", and it
//   is the clause that says the reason is per-CASE, which is the clause an implementer
//   needs here.
//   ⚠️ **THAT RETURN IS NOT AN EXISTENCE TEST, and round 10 wrote it without saying so
//   (round 14, C14-1).** It collapses two states §3.3 partitions on, so nothing may read
//   it to decide whether the marker FILE exists — clause 2 tests the path itself
//   (`unitState` above), and so does the reason above: `.bundle-meta/metafile.json absent`
//   only where that path test is FALSE and `.bundle-meta/metafile.json unreadable`
//   otherwise, for the FINDING and for the coverage entry alike (round 22, `C22-1`). This
//   return decides only whether the bundle COMPARISON can run, and never how a state is
//   SPELLED. It NEVER throws (round 10, C10-3):
//   the marker is 404,198 bytes over 758 inputs written by a plain `writeFile`, so a
//   truncated one is reachable on a tree where every artifact is complete — and the
//   `object | undefined` signature read as an invitation to catch, versus read as a bare
//   `JSON.parse`, is two defensible implementations two exit codes apart: advisory at
//   **0**, or §4's internal error at **2** from `check:all -- --strict` at member 0.
export function checkBundleFreshness(units, metafile) -> { findings, coverage }  // req 3;
//   `bundleAt` is the MINIMUM over the chunk set (§3.2's Rule) and this section declares NO
//   minimum helper: `newestUnder` is a MAXIMUM, so the reachable wrong implementation is
//   `newestUnder(join(cwd, "packages/cli/dist"), isChunk)`, and §6 row 5's two-chunk fixture
//   is the only element that fails it (round 18, `C18-7`).
//   `metafile === undefined` ⇒ exactly ONE `bundle-provenance-missing` finding plus
//   coverage `not-run`, never a comparison over an empty inlined set —
//   ⚠️ **but ONLY on a `compared` `packages/cli`, and this line was unconditional
//   (round 17, C17-1),** which §4's per-kind line list and §6's false-negative battery
//   both already state and this signature did not. The branch order is therefore:
//   `unitState("packages/cli").state !== "compared"` ⇒ coverage `not-run` with §3.2's
//   precedence-ranked reason and NO finding; then `bundleAt === undefined` ⇒ coverage
//   `not-run`, reason `packages/cli/dist holds no esbuild chunk`, and NO finding — never
//   `.mtimeMs` off `undefined`, which is §4's internal-error row at exit 2 from
//   `check:all` member 0; then this line. §6 row 46 asserts both twins.
//   ⚠️ **AND THE REASON THAT LAST BRANCH CARRIES — the `metafile === undefined` one — IS
//   READ OFF THE MARKER PATH AND NEVER OFF THE `metafile` ARGUMENT (round 22, `C22-1`).**
//   That argument is `readMetafile`'s return, which collapses two states §3.3 partitions on
//   (above), so it cannot separate an absent marker from an unparseable one — while BOTH the
//   `bundle-provenance-missing` finding and the `bundle-freshness` coverage entry must spell
//   them apart (§3.2, §4). This body
//   therefore tests `packages/cli/.bundle-meta/metafile.json` for EXISTENCE on the `cwd` it
//   recovers from the `packages/cli` `Unit` — the same path `unitState` tests and the same
//   recovery `PRODUCER_INPUTS` already prescribes, so no signature in this section changes
//   arity — and ONE such test feeds BOTH spellings, so the two surfaces cannot drift. Stated
//   cost, in the shape `unitState`'s double parse is stated in: the marker's existence is
//   tested twice per run, and existence is tested before any read, so this adds no error
//   path and C10-3's never-throws rule is untouched. Without the rule the coverage owner has
//   no way to tell the two states apart from its arguments, and the implementation that
//   spells the coverage reason `absent` whenever `metafile` is `undefined` while
//   distinguishing them for the finding passed every element in this design until round 22;
//   §6's false-negative battery's truncated-marker bullet is the element, and it fails that
//   implementation on its own fixture.
export function checkDeclarationCache(units)         -> { findings, coverage }  // req 8
//   ⚠️ **Its SOURCE side is `packages/*/src` under the RECOVERED `cwd`, never
//   `units.map(u => u.srcDir)` (round 20, `C20-2`).** This signature takes no `cwd` and
//   hands the body a 19-entry `Unit[]`, so the units' own `srcDir`s are the only
//   source-side collection it offers — and §3's Units step states in terms that those 19
//   exceed `workspaces` (18) by exactly `e2e/report`, whose `src` holds 10 `.ts`
//   (re-derived read-only 2026-08-21: `find e2e/report/src -name '*.ts' | wc -l` → 10,
//   against `ls -d packages/*/ | wc -l` → 18). That reading is a FALSE POSITIVE with no
//   remedy: `bundle-types.mjs`'s `newestSourceMtime()` (`:43-61`, re-read 2026-08-21:
//   `for (const pkg of readdirSync(PACKAGES_DIR, …))`) never leaves `packages/`, so `:70`
//   skips regeneration and §4's `npm run build` branch cannot clear the
//   `stale-declarations packages/cli` it reports for a `touch e2e/report/src/<any>.ts` —
//   a printed step that cannot clear the finding it is printed for, `CF14-1`'s class at
//   the comparand rather than at a table row, leaving only `bundle:types -- --force`
//   (~5 min). `e2e/report` is outside `packages/cli`'s reference closure (§3.2), so no
//   byte of it can reach `.dts-cache/index.d.ts`. §6 row 42's round-20 SCOPE TWIN is the
//   element, and it is the only fixture in this design holding a unit outside
//   `packages/`.
//   ⚠️ **THAT POINTER BACKS THE DIRECTORY HALF ALONE, AND THIS MEMBER HAS TWO (round 30,
//   `C30-1`).** §3.3 states it as `every *.ts under every packages/*/src` — a DIRECTORY set
//   AND an EXTENSION filter — and `C20-2`'s twin discriminates `packages/*` from the 19
//   units and nothing about the filter. Two scope-CORRECT bodies get the filter wrong: this
//   section's own `newestUnder(dir, accept)` called with NO accept (§3.4's dist side is
//   specified as "a plain recursive walk", so the argument-less form is an idiom this design
//   already uses), and the same call carrying `isCompilerInput` above, which §3's input
//   rule 2 makes TRUE of a `src/**.json`. The producer this member is keyed on takes `.ts`
//   ALONE — `bundle-types.mjs:43-61` reaches `statSync(full).mtimeMs` only inside
//   `entry.name.endsWith(".ts")`, re-read as TEXT read-only 2026-08-22 — so either body
//   reports `stale-declarations packages/cli` for a touch of any of the **18** non-`.ts`
//   files §3 counts under the 19 `src/` trees, ALL 18 of them under a `packages/*/src`, on a
//   cache `newestSourceMtime()` cannot see: `:70` skips, §4's `npm run build` branch clears
//   nothing and the check fires again identically — `CF14-1`'s class at the OTHER half of
//   the member `C20-2` half-closed. That same twin now carries the EXTENSION half's own
//   fixture clause and a SECOND MUTANT.
export function checkPluginAssets(cwd)               -> { findings, coverage }  // §3.4
export function cliArtifactGaps(cwd, metafile)       -> string[]   // §3.3 artifact set
//   takes the metafile (from `readMetafile`, `undefined` when absent or unparseable)
//   because clause 1's chunk member is a SET when there is one and "at least one" when
//   there is not (§3.3, round 12, C11-6). It never throws on it — `readMetafile` already
//   returns `undefined` for both degraded cases — so this adds no new error path.
// checkUnitFreshness/checkOrphans return no coverage — theirs IS the unit table (§3, §3.1)
export function checkShippedDeclarations(cwd)        -> { findings, coverage }  // §3.5,
//   requirement 1's 2nd excluded artifact. ⚠️ **`{ findings, coverage }`, not `Finding[]`
//   (round 10).** §3.3's **SKIP RULE — ALL FOUR cli-SCOPED COMPARISONS** binds them to report a coverage
//   entry of `not-run` rather than the early `return []` it calls "the founding silence in
//   a smaller box", and §0's requirement 8 promises one for this comparison — but four
//   other sections said three, so this signature structurally forced the silence §3.3
//   bans. Under `Finding[]` a `skipped` run degenerates to `toEqual([])`, indistinguishable
//   from "ran and found nothing", and §6 row 23 had no assertable surface at all (its twin,
//   row 12, already records why a bare "skipped" is unassertable). Reachable today:
//   `rm -rf packages/cli/.dts-cache` on a complete `dist` leaves `packages/cli` `compared`,
//   so this comparison declines with nothing of its own to report.
export function inlinedUnitsFromMetafile(metafile)   -> string[]   // req 3's ground truth
export function checkStaleDist(cwd)                  -> { findings, states, coverage }
//   states: { unit, state, reason }[] — ONE entry per enumerated unit, from unitState().
//   ONE container for the three-way verdict: a `units` array beside a `skipped` array
//   is the same collapse in a different shape, and neither carried the `reason` §4's
//   PASS line prints (`1 skipped (packages/cli: no dist/)`, §4) — asserted since round 21
//   by §6 row 39's MAIN half, which pins that line by equality off its own fixture's counts
//   (`OP21-2`; before it, no element in §6 rendered a `PASS` line holding a skipped unit at
//   all, so the field this array exists to carry reached the human path unasserted). Both counts are filters
//   over this one array, and `units` is recoverable as states.map(s => s.unit). The SAME
//   two filters feed the findings path's units-compared/skipped summary (§4, round 13,
//   OP13-1), so this array has THREE human consumers and not two — `--json` is not the
//   only surface on which a skipped unit survives a run that reports findings.
//   ⚠️ **And the `--json` surface itself was read by NO element until round 24
//   (`OP24-2`):** §6 row 43's CLI twin's `--json` invocation now deep-equals this array
//   against the same run's return, on the one fixture in §6 carrying a `skipped` unit
//   BESIDE a finding — the exact state this comment is written about. Rounds 18 and 22
//   closed `coverage` and `findings` on that surface and BOTH wrote a mutant that keeps
//   this key (`{ findings, states }` at §6 row 39, `{ states, coverage }` at row 43's
//   twin), so the third member of the object above was carried by every mutant and
//   asserted by none.
//   `coverage` is separate and stays: it is about the FOUR cli-scoped COMPARISONS
//   (§3.2, §3.3, §3.4, §3.5), never about unit state.
export function unitState(unit)                      -> { state, reason }  // §3.3's reason tables AND, for
//   `packages/cli`, that section's three-clause conjunction — the cli table alone admitted
//   no clause-2 state until round 13 (C13-1), so naming one table as the sole authority is
//   what let the missing row stand for eleven rounds

// `cliArtifactGaps` returns the required `packages/cli` artifacts that are ABSENT —
// the esbuild chunk SET, dist/bin.js, dist/index.js, dist/bin/supervisord.js,
// dist/index.d.ts, and the six dist/plugin entries. The METAFILE IS NOT A MEMBER — it is
// READ, for the chunk set only (§3.3 clause 1, round 12, C11-6), and never REQUIRED,
// which is what keeps `unbuilt` and `bundle-provenance-missing` disjoint. Round 12
// changed the reading, not the membership: a marker-less tree is still `compared` +
// `bundle-provenance-missing`, never `unbuilt` for want of a marker (CF9-2).
// The two cli kinds are disjoint by construction, and collapsing them re-opens CF9-2:
//   gaps.length > 0                   -> `unbuilt`, gaps named in the line, with the
//                                        reason §3.3's cli reason table gives ON THIS
//                                        BRANCH — TWO of its THREE cli `unbuilt`
//                                        reasons since round 12 (C11-3), the third
//                                        being clause 2's, which the THIRD branch below
//                                        reaches with an EMPTY gap list, so the gap
//                                        list is a property of THIS branch and not of
//                                        the kind (round 29, OP29-1): `bundle:cli did
//                                        not finish`, and the `--clean` signature's own,
//                                        which is why `unitState` and never the reporter
//                                        picks it (§4; §6 row 30 forbids the reporter
//                                        inventing a sentence, §6 row 32 pins the
//                                        `--clean` spelling by equality, and §6 row 20's
//                                        RENDERED TWIN pins `bundle:cli did not finish`
//                                        — round 19, OP19-1, before which row 32 was the
//                                        ONLY cli-`unbuilt` row pinning any reason
//                                        sentence at all, so a `unitState` collapsing the
//                                        two onto the `--clean` sentence passed every
//                                        one of them and blamed a command the operator
//                                        never ran. Row 30's fixtures are rows 9, 13 and
//                                        14's end states, whose one `unbuilt` unit is NOT
//                                        `packages/cli`, so that pair proved the
//                                        `--clean` sentence alone)
//   ⚠️ **EVERY BRANCH BELOW PARTITIONS ON THE MARKER FILE, NEVER ON `readMetafile`'s
//   RETURN (round 14, C14-1).** `readMetafile` is `undefined` for absent AND for
//   unparseable, so a branch reading "no metafile" SWALLOWS a truncated marker and
//   clause 2 — which keys on the FILE (§3.3) — never evaluates for it: §2 then says
//   `compared` + advisory at exit 0 while §3.3 says `unbuilt` at exit 1, on one tree.
//   Standing rule 8: C10-3's fix (unparseable ⇒ `undefined`) re-opening the clause
//   C9-1 added, and the founding failure's TENTH appearance.
//   gaps.length === 0 && the marker FILE does not exist
//                                     -> `bundle-provenance-missing`, unit stays
//                                        `compared`; a FINDING, never a skip.
//   gaps.length === 0 && the marker FILE exists, WHATEVER `readMetafile` returned, and
//                        its mtime is OLDER than the newest §3-filtered output
//                                     -> `unbuilt`, the offending file named, carrying
//                                        §3.3's clause-2 reason. ⚠️ **THIS THIRD BRANCH
//                                        WAS MISSING (round 13, C13-1).** With only the
//                                        two above, an implementer returns `compared` on
//                                        §6 row 19's own fixture, every comparison then
//                                        runs clean, and the check prints PASS, exit 0
//                                        even under `--strict`, on a `packages/cli` whose
//                                        `bin.js` is tsc per-file output importing
//                                        `@crabgic/*`. It does NOT collapse the two cli
//                                        kinds and CF9-2 stays closed: the marker is
//                                        still not a member of the artifact set, so a
//                                        marker-LESS tree is `compared` plus the
//                                        advisory, never `unbuilt` for want of a marker.
//                                        A marker that EXISTS and is stale is a different
//                                        fact from one that is absent.
//   gaps.length === 0 && the marker FILE exists and is NOT older
//                                     -> `compared`. The bundle COMPARISON then runs only
//                                        if `readMetafile` parsed it; an unparseable
//                                        marker leaves the unit `compared` with the
//                                        advisory `bundle-provenance-missing`, reason
//                                        `.bundle-meta/metafile.json unreadable`, and
//                                        coverage `not-run` (§3.2) — the FOURTH branch,
//                                        missing until round 14 (C14-1). Three branches
//                                        for four inputs is what let the contradiction
//                                        stand. It is why §3.3's cli reason table splits
//                                        on parseability at that row and at NO other:
//                                        everywhere else the FILE decides, and the parse
//                                        decides only whether the comparison can run.

// unitState().state ∈ "compared" | "unbuilt" | "skipped" — three values, never a
// boolean, and never a field on Unit. A `built: boolean` cannot separate the three
// outcomes §3.3's reason table produces, and the separation IS the fix: an implementer
// collapsing them to `built ? compare : skip` reproduces round 3's CR-2 exactly.
// Round 8 wrote this enum and left `built: boolean` in the struct 18 lines above;
// round 9 removed it (CF9-1). What each state DOES is §3's skip-rule table.
// Battery rows 9, 13 and 14 keep the three apart — one fixture per state, or the enum
// is untested decoration that any boolean implementation satisfies.

// ComparisonCoverage = { comparison, status, reason }
// comparison ∈ "bundle-freshness" | "declaration-cache" | "plugin-assets"
//              | "shipped-declarations"
//   FOUR members, one per cli-scoped comparison, matching §3.3's **SKIP RULE — ALL FOUR
//   cli-SCOPED COMPARISONS**. A
//   three-member union contradicted that rule, §0's requirement 8 and §6's live smoke
//   test in one stroke (round 10): an implementer who obeyed §3.3 and added a fourth
//   entry emitted a value no type admitted and FAILED the smoke assertion, and one who
//   obeyed this line shipped the sixth comparison with no coverage channel.
// status     ∈ "full" | "not-run"   — TWO values, and NOT a count. `not-run` has
// exactly TWO causes and BOTH are mandated (round 10, C10-1): a side of the comparison
// is ABSENT, or `unitState("packages/cli").state === "unbuilt"`, which suppresses all
// four (§3.2) — INCLUDING on a tree where a side is also absent, the suppression being
// evaluated before any comparison inspects its own comparand, so `packages/cli unbuilt` is
// the reason there too (§3.2's REASON PRECEDENCE rank 1; §6 row 26 AND its round-19 twin,
// round 19, `C19-1`). The tree where every artifact is present is merely the case that has
// no other spelling available, not the case this cause is confined to. Keying the entry on absence
// alone leaves the second cause with no reason this design admits — four comparisons
// suppressed and nothing printed. Do NOT answer it with a third status: round 10 deleted
// `reduced` to keep this enum two-valued, and re-widening it re-opens CF9-2.
// ⚠️ **`reduced` was REMOVED in round 10 as UNREACHABLE.** Its only producer was §3.2's
// fallback to the three entry outputs when no esbuild chunk exists. Round 9 made one file
// matching the chunk pattern a REQUIRED member of §3.3's clause-1 artifact set —
// `cliArtifactGaps` below lists "an esbuild-named chunk" first — so a chunkless
// `packages/cli` is `unbuilt` with `missing: chunk-*.js`, and §3.2 states that no
// cli-scoped comparison runs on an `unbuilt` `packages/cli`. Both predicates read the
// same `/-[A-Z0-9]{8}\.js$/` for PRESENCE, so they cannot disagree that a chunkless tree
// is `unbuilt` — §3.2 carries that argument, the bound the metafile branch puts on it and
// the element round 32 named for it, and this comment does not restate them — and
// `checkBundleFreshness` never reaches the fallback to report anything.
// ⚠️ **"Chunkless ⇒ `unbuilt`" holds only where clause 1 is REACHED (round 17, C17-1):**
// §3.3's cli conjunction is one further step INSIDE the `compared` branch, so a present
// but outputless `dist` is `skipped` before clause 1 evaluates, and since `C15-4` a
// `skipped` `packages/cli` no longer suppresses the four — so `bundleAt` CAN be
// `undefined` here. The enum stays two-valued and `reduced` stays deleted: that state is
// `not-run`, reason `packages/cli/dist holds no esbuild chunk` (§3.2's Rule, §6 row 46),
// which is the answer this enum already admits. ⚠️ **Standing rule 8: do NOT resolve
// that the other way.** Letting a chunkless tree report `reduced` at exit 0 is verbatim
// round 6's `CR6-3` — `PASS` on a `tsc -b`-only tree — the founding failure's eighth
// appearance, produced by the coverage element round 9 added. A member no reachable state
// produces is a value hardcoded never to appear: CF9-2's shape, relocated into this enum
// by round 9's own fix, with §6 row 16 mandating it.
// `comparisonsSkipped` was a count, appeared exactly ONCE in this design (§2's
// signature), had no line shape in §4 and no battery row in §6 — so the check computed
// a degraded coverage and never printed it. A count cannot say WHICH comparison did not
// run, and it cannot carry the REASON §4's coverage line prints — which is the whole of
// the argument now that `reduced` is gone and the enum is two-valued (round 10).
// Round 9, O9-1.
// `skipped` stays UNIT-scoped: it is a `states` entry above and §4's
// units-compared/skipped summary — the `PASS` line's own slot where there is a `PASS`
// line, a line of its own after the grouped findings where there is not (round 13,
// OP13-1) — never a coverage entry. Naming skipped UNITS and not skipped COMPARISONS is
// the same defect one level up; naming them on ONE of the two paths is that same defect
// again, at the surface round 5 built to close it.

// Finding = { kind, unit, remedy, newerInput?, olderOutput?, deltaMs?, reason? }
//   ⚠️ **`remedy` IS `--json`-ONLY TOO, AND IS RENDERED ON NO HUMAN PATH (round 21,
//   `OP21-1`):** `OP15-3`'s ruling applied to the fourth field it did not reach. §4's
//   remedy table is the ordered plan's SOURCE, and the plan is computed from the finding
//   KINDS, from the non-`full` coverage entries' REASONS and from step 1's exempt UNIT —
//   never from this field — so an implementer must NOT infer a human line from its being
//   REQUIRED here. That inference produced the page §4 measures as harmful while every
//   element in §6 stayed green; §6 row 34's second mutant is the element.
//   the three comparison fields are OPTIONAL AND `--json`-ONLY (round 15, OP15-3): no
//   human line in §4 renders `newerInput`, `olderOutput` or `deltaMs` — the four
//   delta-carrying kinds collapse to `<kind> ×N — <units>` at EVERY N, and the other
//   four have no delta at all — so an implementer must NOT infer a human shape from
//   their presence here. They are carried for `--json` consumers and for §6 row 29's
//   mutant, which is the only place the generic shape appears at all. ⚠️ **And a
//   CONSUMER-shaped element reads them only since round 22 (`OP22-1`):** §6 row 43's
//   CLI twin's THIRD invocation parses `--json` on a findings-carrying tree and requires
//   the parsed `findings` to DEEP-EQUAL the run's own, so a serialiser projecting each
//   finding to `{ kind, unit }` — which this OPTIONAL marking permits outright — no
//   longer passes. `remedy` above rides that same element for its PRESENCE ALONE — a
//   deep-equality against the run's OWN return pins no VALUE, which is what left SEVEN of
//   §4's EIGHT remedy-table rows asserted by nothing (round 34, `OP34-2`). Its VALUE is
//   §4's remedy-table command for the finding's KIND and NEVER the first ordered-plan step
//   that kind selects: that backwards reading agrees with a correct implementation on five
//   kinds of the eight and emits `rm -rf <unit>/dist` with NO rebuild for `stale-unit`,
//   `orphan-output` and `unbuilt`. §6 row 28's PLAN TWIN's RETURN-VALUE conjunct is that
//   element. Before both, all four fields
//   were declared for ONE surface and read on none.
//   §4 names FOUR kinds with no delta
//   to print — `unbuilt`, `bundle-provenance-missing`, `orphan-output` and
//   `stale-shipped-declarations`, whose §4 line is byte-based (`(<a> vs <b> bytes)`) and
//   carries no mtime delta. NAMED, not counted, because this count has now drifted once:
//   an implementer marking "three" kinds delta-less guesses round 7's set and leaves
//   `stale-shipped-declarations` on the generic comparison shape, rendering
//   `stale-shipped-declarations packages/cli undefined undefined NaN` — round 4's
//   `OP-R4-2` and round 7's `OP-1` verbatim, and nothing in §6 fails (round 11).
//   `reason` carries unitState()'s reason for `unbuilt`, so §4's line states
//   WHICH reason-table row fired instead of one sentence that is false for two of them
//   (round 7's OP-2, recurring at the sites round 8's rule created).
// kind ∈ "stale-unit" | "orphan-output" | "stale-bundle" | "stale-declarations"
//       | "stale-shipped-declarations" | "stale-plugin-assets" | "unbuilt"
//       | "bundle-provenance-missing"

// report.mjs
export const ADVISORY_KINDS = new Set(["bundle-provenance-missing"])  // §4 exit table
export function formatFindings(result, { json })     -> string
//   Renders the PASS line, the grouped finding lines, the units-compared/skipped
//   summary, **§4's REMEDY PLAN** and the coverage line — the last only where
//   some entry is not `full`, never a bare label on a fully covered run (round 16,
//   `OP16-4`) — in that order, which
//   §4's prefix rule fixes. ⚠️ **The FIRST adjacency of that order — the `PASS` line ABOVE
//   the finding lines — has an element only since round 24 (`OP24-1`):** §6 row 18, whose
//   advisory-only fixture is the one state §4's exit table renders both lines on, pins the
//   `PASS` line's rendered index as strictly LESS THAN the finding line's, so that the
//   `PASS` line prints FIRST. Before it, rows 35 and 39 covered the other three adjacencies
//   and neither could reach this one — row 35's fixture prints no `PASS` line and row 39's
//   holds zero findings — so a formatter emitting the finding lines first and this
//   function's PASS/summary branch at the standalone summary's ONE slot passed every
//   element in §6. ⚠️ **The skipped summary prints on BOTH paths (round 13, OP13-1), in EXACTLY ONE of the
//   two slots per run and never both (round 21, `OP21-2`):** §4's prefix rule owns that
//   half, and its elements are §6 row 39's MAIN half for the `PASS` slot and row 35 for the
//   findings slot. ⚠️ **At `M = 0` the parenthetical is OMITTED, never rendered EMPTY
//   (round 25, `OP25-2`)** — `<N> units compared, 0 skipped`, in EITHER slot. That is the
//   steady state after the first post-adoption build and therefore this function's entire
//   human output on every green run, so a `${clauses.join(", ")}` wrapped in unconditional
//   parentheses emits `check-stale-dist: PASS — <N> units compared, 0 skipped ()` at
//   `check:all` member 0 and in both `test` legs of `ci.yml:129` — `OP16-4`'s
//   bare-container class at the sibling line. §6 row 18 is the element for the `PASS`
//   slot, its fixture holding ZERO skipped units, and §6 row 38 for the STANDALONE slot,
//   which had no element until round 26 (`OP26-1`); row 39's MAIN half runs M=1 and row
//   35 M≥2, so neither could reach either. Row 18's fixture is ADVISORY-ONLY and so
//   always renders a `PASS` line: before row 38 carried this, a formatter written as TWO
//   templates — the omission on the `PASS` line, the parenthetical unconditional on the
//   standalone one — passed every element in §6 and printed `0 skipped ()` on every run
//   holding a finding against a fully built tree. The `PASS` line carries it in its slot, and where there is no `PASS` line
//   — every run holding a non-advisory finding — it is a line of its own, with
//   `unitState()`'s reasons verbatim. ⚠️ **And the ABSENCE of a `PASS` line on that path is asserted only since round 36 (`OP36-2`):** §6 row **38**'s rendered half now requires that a two-kind non-advisory fixture's output carry NO line beginning `check-stale-dist: PASS`. Every rendered findings-path element in §6 asserted the PRESENCE of a line, so a `formatFindings` whose `PASS` line is gated on the run not having THROWN — `check-citation-runs.mjs`'s shape copied WHOLE, standing rule 7 — printed a green verdict beside the findings on every flagless run and passed every one of them. ⚠️ **And GROUPED BY REASON in BOTH slots
//   (round 14, OP14-2)** — §4's **THE SKIPPED SUMMARY GROUPS BY REASON**: one clause per
//   distinct reason, written `<units>: <reason>`, never one such pair per unit. With one
//   unit that is `packages/cli: no dist/`, identical to the `PASS` line's own literal
//   above, so this generalises that shape rather than respelling it. The per-unit form
//   on §4's `npm run build:clean` example is eighteen repetitions of one 111-character
//   sentence on a single unwrappable line — re-derived at 2,433 characters, 31 rows at
//   80 columns, against 512 and 7 grouped — which is round 1's `O-3` one level up,
//   reinstated by the fix that put this summary on the findings path.
//   ⚠️ **Grouping is per KIND and NOT uniform
//   (round 13, OP13-3):** `stale-unit`, `stale-bundle`, `stale-declarations` and
//   `stale-plugin-assets` collapse to `<kind> ×N — <units>` at EVERY N — and three of
//   those four are structurally N=1, each being ONE `packages/cli`-scoped comparison
//   (§3.2, §3.3, §3.4), so the collapse is their ONLY rendering and a formatter that
//   collapses at N≥2 alone is wrong for them everywhere (round 18, `OP18-2`; the
//   closure is the SET of grouped literals §6 asserts by EQUALITY and never one row, each
//   carrying this section's `check-stale-dist: ` prefix — row 28 and its N=1 twin for
//   `stale-unit`, row 38 for `stale-bundle`, row 43's CLI twin for `stale-declarations`
//   (round 19, `CF19-1`) and row 8's RENDERED TWIN for `stale-plugin-assets`, the fourth
//   kind, which no element rendered until round 20, `OP20-1`); `unbuilt`, `orphan-output`,
//   `bundle-provenance-missing` and `stale-shipped-declarations` carry per-finding text
//   in their §4 line and print one line EACH, whatever N is — the same four kinds §2
//   names delta-less, for the same reason. The plan is a pure
//   function of the finding SET **and of the COVERAGE entries** (round 18, `OP18-4`: with
//   zero findings the plan still names a producer per non-`full` entry, which the finding
//   set alone cannot supply) — the KINDS, plus each non-`full` entry's REASON, plus the
//   UNIT for step 1's fourth carve-out
//   (round 14, C14-5) — with §4's FOUR carve-outs applied, NOT a per-kind
//   remedy printed beside each grouped block, which is the unordered list §4 measures as
//   harmful. ⚠️ **That NOT clause had no element until round 21 (`OP21-1`):** §6 row 34's
//   single mutation was a CONJUNCTION — per-kind text AND no plan — so a formatter
//   rendering BOTH passed it, and rows 21, 38, 39 and 42 — and, since round 25, row 28's
//   PLAN TWIN (`OP25-1`), since round 34 row 43's CLI twin's step-3 PLAN conjunct
//   (`OP34-1`), and since round 35 row **36**'s plan conjunct and row **39**'s ALL-NOT-RUN
//   and CACHE-ONLY twins (`OP35-1`) — assert their plans' presence and steps and never the absence
//   of other remedy text. Row 34's second mutant fails it, and is the only element that
//   does; every OTHER plan element named here passes it — stated as the SET rather than as
//   a count, an appending round re-invalidating a count and not a set (`C11-4`). ⚠️ **It prints whenever the run reports ≥1 finding OR ≥1 non-`full`
//   coverage entry (round 14, OP14-1)** — §4's **A DEGRADED RUN CARRIES A NEXT ACTION**. ⚠️ **And NOT otherwise — the gate's FALSE side, which had no element for twenty-two rounds (round 36, `OP36-3`).** §6 row **38**'s CLEAN side, zero findings with all four entries `full`, must render NO line beginning `check-stale-dist: remedy plan:`, its mutant being the plan gated on the negation of the `nothing was checked` carve-out alone.
//   Gating it on findings alone leaves §4's own recipe step 1 —
//   `rm -rf packages/cli/dist` on a complete tree — printing a `PASS` line, three
//   `not-run` entries and NO stated action, because the plan is the only element that
//   prints step 5 and the coverage line carries no remedy of its own. Multi-kind is not
//   the trigger either, because §4's carve-outs make single-kind runs multi-step (a lone
//   `stale-declarations` needs step 3 AND step 4, and the remedy table's row names only
//   step 3). ONE carve-out for the plan, and the SAME tree is the summary's one carve-out too — the SAME TREE and never the same ELEMENT SET (round 36, `OP36-1`), the summary's carve-out having TWO conjuncts to the plan's THREE and its own EXCLUDING elements being §6 row **39**'s CACHE-ONLY twin, where a `declaration-cache` running `full` falsifies **no comparison ran** alone, and row **39**'s ALL-NOT-RUN twin, where the units still comparing falsify **no unit was compared** alone, each asserting the ORDINARY `PASS` line where round 35 had them assert the plan alone — the clause list is suppressed on the `nothing was checked` PASS (§4, round 15, OP15-5): no plan on the `nothing was checked` PASS — no unit
//   compared, no comparison run and no finding — which research Q5 requires to stay
//   quiet (§4). ⚠️ **THREE CONJUNCTS, AND EACH WAS DELETABLE UNTIL ROUND 35 (`OP35-1`):**
//   the only element was §6 row 39's NEGATIVE twin, where all three HOLD, so a body
//   withholding on any TWO of them passed every element — including the body that shares
//   ONE predicate with §4's `PASS`-line carve-out, which has TWO conjuncts because that
//   line is absent from the findings path. Each conjunct now has an EXCLUDING element
//   too, that conjunct ALONE failing: row **36** for "no finding fired" (its unit set
//   pinned to two `unbuilt` units, nothing compared, all four comparisons suppressed),
//   row **39**'s ALL-NOT-RUN TWIN for "NO unit was compared" and row **39**'s CACHE-ONLY
//   TWIN for "no comparison ran", each asserting the plan PRINTS.
//   It had no owner in this section for twelve rounds (round 12, OP12-4);
//   §6 rows 21, **28**, 34, 38, 39 and 42 are what make it non-vacuous — one row per §4
//   carve-out plus the multi-kind and zero-finding cases (round 18, `OP18-3`), plus the NOT-DEGRADED case at row **38**'s CLEAN side, where both disjuncts are false and the plan must NOT print (round 36, `OP36-3`), plus step 1's
//   own PRESENCE at row 28's PLAN TWIN (round 25, `OP25-1`), plus step 1's THREE-KIND
//   SELECTOR at that same twin, whose fixture carries one unit per kind step 1 names
//   since round 33 (`OP33-1`), plus **row 43's CLI twin**, whose FLAGLESS process run must
//   render step 3's command on a SOLE `stale-declarations` triggered by a
//   `packages/*/src/**.ts` — §4's UNCONDITIONAL-step-3 rule, which had no element for
//   twenty rounds because the only fixture asserting that step is PRODUCER-triggered
//   (round 34, `OP34-1`), plus **row 36** and **row 39's ALL-NOT-RUN and CACHE-ONLY
//   twins**, the three EXCLUDING elements of §4's THREE-conjunct `nothing was checked`
//   carve-out, whose one prior element held all three conjuncts at once, and plus step 4's
//   COMMAND at **row 34's SOLE-KIND TWIN**, selected by step NUMBER alone at both elements
//   that reach it (round 35, `OP35-1`, `OP35-2`): rows 21 and 42 are the
//   two carve-outs that had a fixture and no assertion, and row 38 has asserted step 1's
//   fourth carve-out since round 14 while this list named only 34 and 39. ⚠️ **Step 1 was
//   the one step of the five with no element for twenty-four rounds (round 25, `OP25-1`):**
//   the other five rows — that SET AS IT STOOD AT ROUND 25'S APPEND, this clause being
//   scoped to that round rather than left as a claim about the set's members today
//   (`C11-4`; round 34, `OP34-1`) — each assert a step-1 NEGATIVE or name no step, so a
//   plan assembler
//   with NO step-1 branch — and one emitting step 1 for the FIRST selected unit only,
//   byte-identical at N=1 — passed every one of them, and row 28's ≥2-unit fixture is the
//   only one in that battery where this step selects more than one unit. ⚠️ **The SET
//   above closes the plan's PRESENCE, its steps and its order, and NOT the clause above
//   (round 21, `OP21-1`) — the SET and never its size, which is why the two counts that
//   stood here are gone: round 34's append made both wrong (`OP34-1`, `C11-4`):** what
//   makes the NOT clause non-vacuous is row 34's SECOND
//   mutant, which renders the per-kind remedy beside each grouped block AND the correct
//   plan, and every other member of that SET passes it.
export function formatInternalError(err, { strict, json }) -> string   // §4's internal-error row
//   The ONLY line this check prints that no `result` produces, and the reason §4's
//   internal-error row had no owner for eleven rounds (round 11). ⚠️ **It takes `json`
//   for the same reason `formatFindings` does (round 12, C11-5): `--json` PLUS a throw
//   was an input pair no section named.** Every reachable throw §4's own row admits — the
//   `ENOTDIR` on `packages/plugin/.mcp.json` (§2, §3.4), row 27's malformed unit
//   tsconfig, an `--omit=dev` install with no `esbuild` (§8(n)), a missing
//   `PLUGIN_ASSET_ENTRIES` export (§1's `export` edit — NAMED, never numbered, since round 21's `C21-2` made it the fifth) — would otherwise emit
//   `check-stale-dist: WARN internal error — …` on **stdout** under
//   `node scripts/check-stale-dist.mjs --json`, so the JSON stream is not JSON. **This
//   repository has already SHIPPED that failure:** `scripts/bundle-types.mjs:63-68`
//   records `check-published-tarball.mjs` dying with
//   `Unexpected token 'b', "bundle-typ"... is not valid JSON` because a producer wrote
//   human chatter to stdout. Latent today — nothing consumes `--json` yet — which is why
//   it is closed BEFORE a consumer exists rather than after. `check-stale-dist.mjs`'s
//   try/catch is its sole caller (§1); exit **2** comes from that catch, never from
//   `exitCodeFor`. ⚠️ **The BODY this returns is §4's, verbatim, and this declaration named
//   an element for the IMPORT RULE and for both exit codes and NONE for its printed text
//   until round 30 (`OP30-2`):** both lines carry
//   `; the staleness check did NOT run, so this is not a verdict on your tree`, and the
//   `ERROR` line additionally carries the re-run sentence whose literal §6 row 27's strict
//   half now names — so a formatter returning the head alone fails §6 rows 27 and 31 and
//   row 27's `C18-5` twin instead of passing every element in this design. ⚠️ **And the code that catch returns UNDER `json` was mandated at §1
//   and §4 and read by NOTHING until round 28 (`OP28-1`):** row 27's `--json` invocation
//   now pins exit **0** and its `C18-5` twin's `--json --strict` one pins exit **2**, so a
//   branch that emits this object and then swallows the code — the ordinary
//   machine-consumer shape, `CF19-1`'s own defect moved from the success path to the
//   catch — fails there instead of passing every element in §6.
//   ⚠️ **A catch owns only what is LOADED inside it (round 12, OP12-1):**
//   the entry module reaches `compare.mjs` and `report.mjs` through `await import(...)`
//   inside the try, because a static import resolves the whole graph before the try
//   exists and turns every load-time throw into an uncatchable exit **1** with no
//   prefixed line at all — §1's IMPORT RULE, §6 row 31. Without a catch the operator gets an unprefixed node stack — and under
//   `pretest` no test suite at all: re-derived 2026-08-20 in a scratchpad package (npm
//   11.16.0, node v24.18.0, nothing else running but the shell), an UNCAUGHT throw in
//   `pretest` exits npm **1** and `npm test` never runs, verbatim round 1's `O-2`.
export function exitCodeFor(result, { strict })      -> 0 | 1       // exit 2 is the CLI
//   catch's (`formatInternalError` above, §1) — a throw yields no `result` to pass here.
//   Round 2's `CF-A` raised this range because §4 required exit 2 and no element produced
//   it; round 11 supplies the element instead of widening the range. Partitions by kind,
//   never by count: `--strict` exits 1 only when some finding's kind is NOT advisory — and
//   WITHOUT `--strict` the answer is **0** whatever the finding set is, which is §4's
//   exit-table DEFAULT column and the only reason `pretest` can carry findings at all
//   (§5.1). ⚠️ **Both columns are asserted only since round 18 (`OP18-1`):** every
//   `exitCodeFor` call in §6 passed `strict: true` and the literal `strict: false` occurred
//   nowhere in this design, so an implementation that ignored the second argument entirely
//   passed every element in §6. §6 row 21 now calls it BOTH ways on BOTH of its fixtures.
//   ⚠️ **That is the FUNCTION, and the CLI's ARGUMENT is a SECOND obligation nothing read
//   until round 19 (`CF19-1`):** row 21's four calls are DIRECT, so the value
//   `check-stale-dist.mjs` actually passes here was observed by no element, and the only
//   two §6 elements that run the CLI as a process neither run it on a findings-carrying
//   tree — row 27's fixture is a malformed config, every exit code row 31 asserts is its
//   LOAD-TIME mutant's, and row 31's clean baseline is a ZERO-finding tree where 0 is
//   correct under either `strict`. §1 therefore mandates
//   `process.exit(exitCodeFor(result, { strict }))` with the parsed flag, and §6 row 43's
//   CLI twin runs the flagless process on a findings-carrying fixture — and, since round
//   29 (`OP29-2`), the SAME process under `--json` and under `--json --strict` on that
//   same fixture, at **0** and **1**. ⚠️ Threading the parsed flag into this call is not
//   enough if the CLI never REACHES it: a success path emitting the JSON object and
//   exiting **0** whenever `json` is set satisfies every word of §1's clause and still
//   reports success on a stale tree — `CF19-1`'s shape at the sibling of the path
//   `OP28-1` closed in the catch. This function is
//   unchanged by that; what changes is that its caller's argument is now pinned too.
//   ⚠️ **And BOTH of those closed the partition by `strict` and left the partition by KIND
//   open, the MIXED finding set being a FREE VARIABLE in every fixture either reaches
//   (round 23, `OP23-1`):** neither of row 21's two fixtures is required to carry an
//   advisory BESIDE a non-advisory finding, so
//   `some((f) => ADVISORY_KINDS.has(f.kind))` written where this rule needs
//   `every((f) => ADVISORY_KINDS.has(f.kind))` as the exit-**0** test under `--strict`
//   returned **0** on every tree holding both kinds — which is every tree built before this
//   change set lands (§3.4, §7's day-one row, §6's live paragraph) — while agreeing with a
//   correct implementation on advisory-only and on non-advisory-only, and so passing every
//   element in §6. Row 21's EXIT-CODE MIX TWIN is the element. ⚠️ **It differs on the EMPTY
//   set too, returning 1 where a correct implementation returns 0, and no element read this
//   function's exit code on an empty finding set until round 27 (`OP27-1`):** every
//   `exitCodeFor` assertion in §6 carried at least one finding, so the guarded spelling
//   `findings.length > 0 && findings.every(isAdvisory)` — §4's exit-table rows 1 and 2
//   merged — agreed everywhere §6 asserted while exiting **1** from `check:all` member 0 on
//   every correctly built tree. §6 row 39's exit-code conjunct is that element.
```

Everything is pure over its arguments except `newestUnder`/`newestOfEntries`/`stemsUnder`/`sameBytes`,
`cliArtifactGaps` and `unitState`, which read the filesystem — the split
`repo-census.mjs` uses between `computeDisagreements` (pure, unit-tested) and
`enumerate*` (I/O). `sameBytes` is the only one that reads file CONTENT rather than
metadata; §3.5 states why that single artifact needs it, and why it is not the content
oracle §7 row 1 rejects.

**No `git` spawn — and this is the design's FIRST repo-internal import, which §1 did not
count for thirteen rounds (round 13, CF13-1).** `units.mjs` imports only
`enumerateRootReferences` and `enumerateTsconfigs` from `scripts/repo-census.mjs`, whose
own import list is `node:fs`, `node:path`, `node:child_process` and `node:url` — builtins
only, re-derived 2026-08-20 with `grep -n "^import" scripts/repo-census.mjs`, which is
what keeps this import out of §8(n) and is a fact about today's file rather than a
property of it; `enumerateGit` is the one that
spawns and is not imported. A future revision that needs git must go through the same
module's `censusGitEnv()`, which already scrubs `GIT_LOCATION_ENV_VARS`.

## 3. The enumeration algorithm

**Units.** `enumerateRootReferences(cwd)` → **19** entries. Requirement 5 and 11 in
one call — the census already owns this set and disagrees with `workspaces` (18) by
exactly `e2e/report`.

```
node -e "const j=JSON.parse(require('fs').readFileSync('tsconfig.json','utf8').replace(/^\s*\/\/.*$/gm,''));console.log(j.references.length)"   # 19
node scripts/repo-census.mjs | grep -A2 'NOT an npm workspace'                                                                                  # 1 — e2e/report
```

All 19 are uniform today — `extends: ../../tsconfig.base.json`, `rootDir: ./src`,
`outDir: ./dist`, `include: ["src"]`, `composite: true` — but the design READS these
per unit rather than assuming them: `<unit>`'s `srcDir` and `distDir` (§2) are resolved
from its own `compilerOptions` through `tsconfigPath`'s resolved `extends` chain,
defaulting to `./src` and `./dist`, and the input- and output-set rules below are spelled
against those two FIELDS and never against the literals.

⚠️ **§6 row 49 is the non-vacuity row, and this sentence was exercised by NOTHING for
twenty rounds (round 21, `C21-1`, standing rule 9).** Re-derived read-only 2026-08-21 at
`3095116` (`git status --porcelain` empty before and after; no import of
`scripts/bundle-cli.mjs`) by parsing all 19 root references:
`compilerOptions.rootDir === "./src" && outDir === "./dist"` for **19 of 19**, exception
set EMPTY — and every fixture in §6 is a `src`/`dist` tree at the UNIT level, which is the
only level `buildUnits` reads: row 47 carries THREE variant configs from round 34 (`CF34-1`) and NOT ONE is a unit's
own `tsconfig.json` — `tsconfig.emit-into-A.json` AT THE FIXTURE ROOT declaring `"outDir": "./packages/A/dist/emit"` from round 27 (`C27-1`) as relocated in round 33 (`CF33-1`); from round
25 (`CF25-1`) as amended in round 26 (`CF26-1`), its twin's `tsconfig.emit-elsewhere.json`
declaring `"outDir": "./dist/eo-elsewhere-fixture-1"`, a directory that fixture never
creates; and, from round 34 (`CF34-1`), that same twin's `packages/A/tsconfig.standalone.json`
declaring NO `extends` and `"outDir": "./dist"`, which resolves to `packages/A/dist`, the
unit's OWN `distDir` — and is still not where that layout comes from, `buildUnits` reading
`packages/A/tsconfig.json` and NO OTHER file. From none of the three is any unit's layout
derived. ⚠️ **Naming
the first alone was a SUBSET of the set this sentence quantifies over — `CF14-4`'s class —
and it would have read as FALSE at the commit the twin landed in.** So a `buildUnits` setting
`srcDir = join(cwd, dir, "src")` and `distDir = join(cwd, dir, "dist")` passed all 48 rows
and every twin, the false-negative battery, the four `bundle-cli.mjs` source properties,
the `units.mjs` and `walk.mjs` import reads, the `package.json`/`.gitignore` reads and the
live smoke test (which asserts the unit SET and the coverage SHAPE, neither affected), and
is indistinguishable from the mandated reading everywhere this design can currently look.
Add one unit declaring `"outDir": "./build"` — or `"rootDir": "./source"` — and that body
stats an absent `<unit>/dist`, §3.3's unit-state table returns `skipped` with reason
`no dist/`, `checkUnitFreshness` and `checkOrphans` are suppressed for that unit FOREVER,
its whole `build/` tree is in no output set, and the run prints **PASS, exit 0 even under
`--strict`** at `check:all` member 0 and under flagless `pretest`, with the unit named only
in the skipped summary — the founding failure's shape at the two primitives every
comparison is built on. This is verbatim input rule 4's state until round 17 (`CF17-1`:
mandated, 0 members on the live tree, exercised by nothing), which that round closed with a
SYNTHETIC row rather than accepting, on the identical argument at the identical distance
from today's tree.

⚠️ **`kind` is the THIRD derived field of the same struct and this step derived it nowhere
(round 23, `C23-1`, standing rule 9).** It is `"bundled"` for `packages/cli` ALONE — the
unit whose `dist` `scripts/bundle-cli.mjs` writes (`CLI_ROOT`/`OUT_DIR`, `:63`), a
design-time fact this design READS and never imports, so §1's exactly-two
repo-internal-import count and §8(n)'s bound are unchanged — and `"tsc"` for the other
**18**, which is where §3.1's `18` comes from rather than its being a bare count of today's
tree. Never off `Unit.references`, and never off `packages/cli`'s reference closure:
re-derived read-only 2026-08-21 at `53996f5` by parsing the live tsconfigs (`git status
--porcelain` empty before and after; no import of `scripts/bundle-cli.mjs`), that closure
holds **16** units, so the closure reading marks 17 of the 19 `"bundled"` and runs
`checkOrphans` on `packages/perf` and `e2e/report` alone. That figure is deliberately NOT
read as §3.2's 16-member inlined set: the two are equal today, which §3.2's own ℹ️ records
as a fact about the import graph and not a property of it. §6 row 4's round-23 TWIN is the
element.

**Skip rule.** A unit's state is `unitState(unit)`'s — `compared`, `unbuilt` or `skipped`, from the reason tables in §3.3 **together with, for `packages/cli`, that section's three-clause conjunction, which its cli reason table's rows must cover between them** (round 13, C13-1: for eleven rounds they did not, and this bullet and §2 both cited one table as sole authority, so the state clause 2 exists for had no verdict anywhere an implementer reads) — **never from directory existence alone, and never a boolean (§2)**. What each state DOES is stated here, because 'which comparisons run' is the thing `built ? compare : skip` gets wrong:

| state      | per-unit comparisons run              | reported as                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------- | ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `compared` | `checkUnitFreshness` + `checkOrphans` | findings, if any                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `unbuilt`  | neither — the absence IS the finding  | one `unbuilt` finding carrying its `reason`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `skipped`  | neither                               | no finding **from the two this column names** — `checkUnitFreshness` and `checkOrphans`. ⚠️ **The four cli-scoped comparisons are NOT governed by this row and MAY still report a finding against `packages/cli` while it is `skipped` (round 15, C15-4; §6 row 43).** §3.3's **SKIP RULE — ALL FOUR cli-SCOPED COMPARISONS** governs them, and `declaration-cache` is the reachable case, because `.dts-cache/` is a sibling of `dist` and §4's own recipe step 5 states it still RUNS, `full`, after `rm -rf packages/cli/dist`. Named with its `reason` in §4's skipped summary, on BOTH paths and GROUPED BY REASON there — §4's **THE SKIPPED SUMMARY GROUPS BY REASON** (round 14, OP14-2), because after `build:clean` this row fires for 18 of 19 units carrying one identical 111-character reason |

This bullet read "a unit whose `dist/` does not exist is `skipped`, never stale" for seven rounds while the rule that consumes it moved four times, 220 lines away in a section headed _Declaration cache_ (round 8). The research rationale it cited still holds for the genuinely-absent case (research
Q5: a fresh clone must not be noisy).

**Per-unit input set** — newest mtime over:

1. every `*.ts` under `<unit>`'s `srcDir` (§2 — resolved from that unit's own `rootDir`,
   `./src` for all 19 today; round 21, `C21-1`), **recursively** — **§6 row 1's amended fixture is its
   non-vacuity row, from round 18 (`C18-1`, standing rule 9).** Re-derived read-only at
   `896dd84`: no row in that battery required either walked side to hold a member below
   depth 0 whose depth changes a verdict, so a walk whose `readdirSync` omits
   `recursive: true` passed every row, the false-negative battery, the wiring assertions
   and the live smoke test — while `packages/contracts/src` alone holds **141** nested
   `.ts` against **1** flat, and the shallow walk silently deletes findings from the four
   `stale-unit` this section's live paragraph calls independent evidence the check is not
   vacuous against real inputs;
2. every `*.json` under `<unit>`'s `srcDir` (§2; `resolveJsonModule: true`, `tsconfig.base.json`) —
   **§6 row 48 is its non-vacuity row, from round 17 (`CF17-1`, standing rule 9).** Every
   src-triggered row in that battery moves a `*.ts`, so an input walk filtered to `*.ts`
   shipped this rule hardcoded never to fire against the **3** members it has on the live
   tree (re-derived read-only 2026-08-21 at `badf45a`:
   `packages/connectors-jira/src/testkit/fixtures/read-scenario.cassette.json`, its
   `write-scenario` sibling, and
   `packages/engine-claude/src/live/fixtures/live-verdicts.json`), while the
   false-negative battery's `.info`/`.snap`/`.mjs` bullet is the EXCLUSION side, which
   that same walk also passes;
3. `<unit>/tsconfig.json` **and its full upward `extends` chain** — requirement 6.
   Chain length is 2 for all 19 today (`<unit>/tsconfig.json` → `tsconfig.base.json`).
   ⚠️ **BOTH members are inputs — the unit's OWN config as well as the chain above it —
   and only the SECOND had a non-vacuity element until round 18 (`CF18-1`).** §6 row 2's
   mutation is `tsconfig.base.json`, a shared ANCESTOR of both its fixture units, so an
   input set assembled from the RESOLVED ANCESTORS alone — equivalently `configChain` read
   as excluding its own argument (§2, whose declaration says only `upward extends,
cycle-guarded`), or a config set built from `enumerateTsconfigs`' variants plus rule 4,
   which is a natural reading while `tsconfigDirs` is a list of DIRS — satisfied that row
   and every other element in §6. Re-derived read-only at `896dd84` over §6's whole mutation
   column: NO row's mutation is a unit's own `tsconfig.json`. On the live tree that
   implementation leaves all **19** of them outside every input set, so
   `utimesSync packages/gates/tsconfig.json` printed PASS, exit 0 even under `--strict`, on
   a `packages/gates/dist` compiled under the old `compilerOptions` — the founding failure's
   own shape at the member this rule names first. §6 row 2's TWIN is the element: it moves
   one fixture unit's own `tsconfig.json` and asserts the reported unit set EQUALS that unit
   ALONE;
4. every `tsconfig*.json` whose own `extends` chain resolves **into** this unit —
   requirement 9. Computed from `enumerateTsconfigs(cwd)`, which returns 27 project
   dirs plus **2** variants (`packages/cli/tsconfig.dts.json`, `tsconfig.base.json`).
   ⚠️ **NARROWED IN ROUND 14 (C14-3) AND MADE EVALUABLE IN ROUND 24 (`C24-1`): a
   descendant config enters `<unit>`'s input set only when ALL THREE of these hold, each
   resolved from the config's own `extends` chain and from NOTHING ELSE, that chain being
   the whole of what `descendantConfigs(unitDir, allConfigs)` may DECIDE ON — its argument
   list also hands it the config PATHS, and NO clause here is a test on a member's own path
   (round 33, `CF33-1`), and NO body may DROP (i) and test (ii) and (iii) ALONE — the cheapest
   reading of that sentence, clause (ii) already tying a config to ONE unit, and killed by §6
   row 47's ROUND-25 TWIN's SECOND config from round 34 (`CF34-1`) — (i) the chain
   resolves INTO `<unit>`; (ii) the config's RESOLVED `outDir` lands in `<unit>`'s COMPARED
   output set — §3's filtered set below, NOT `<unit>/dist` raw, and CONTAINMENT at or
   below `<unit>`'s `distDir` past that set's minus-bullets rather than EQUALITY with
   `distDir`, which `distDir` itself satisfies and so does any subdirectory no minus-bullet
   reaches (round 27, `C27-1`); and (iii) the config's
   RESOLVED `composite` is `true`.** ⚠️ **CLAUSE (ii)'s FAILING-SIDE ELEMENT IS §6 ROW 47's ROUND-25
   TWIN and its ACCEPTING-SIDE element is that row's MAIN half from round 27 (`C27-1`), and
   NOTHING exercised clause (ii) at all until round 25 (`CF25-1`).** Row 47 and §6 row
   3 BOTH SATISFY clauses (i) and (ii) and differ on (iii) alone — which is what the
   two paired fixture pins below buy — so the pair pins (iii) and leaves a body that DROPS
   (ii) ENTIRELY unconstrained: a `descendantConfigs` implementing (i) and (iii) ALONE, with no `outDir` test
   at all, passes row 47 (its config satisfies both) and passes row 3's NOT-`stale-unit`
   half (its config fails (iii)), and the live tree cannot discriminate, rule 4 having 0
   members there under BOTH readings. That body is the path of least resistance, (i) and
   (iii) both being reads of the `extends` chain the function already walks while (ii)
   additionally needs `<unit>`'s filtered output set. Its harm is `C14-3`'s own harm one
   clause over: a future `<unit>/tsconfig.*.json` inheriting `composite: true` and
   emitting OUTSIDE the compared set — `"outDir": "./dist-test"`, or `"outDir":
"./dist/eo-elsewhere-fixture-1"`, which is inside `<unit>/dist` RAW and outside the set
   this clause is worded against through §3's `dist/eo-*-fixture-*/**` minus-bullet, which
   is in BOTH families and NOT cli-scoped, so it reaches every unit — enters that unit's input set, and a `touch` of it reports
   `stale-unit <unit>` at `check:all` member 0, exit **1** under `--strict`, printing
   `rm -rf <unit>/dist` plus a full rebuild for an edit whose output side provably cannot
   move. Row 47's twin holds exactly that config with (i) and (iii) satisfied, so (ii) is
   the ONLY property in play there and the twin fails that body on its own fixture.
   ⚠️ **Clause (iii) is what makes (ii) DECIDABLE, and
   without it this rule mandated OPPOSITE verdicts for two configs it cannot otherwise
   separate (round 24, `C24-1`).** Re-derived read-only at `8fdf3ed` (`git status
--porcelain` empty before and after; no import of `scripts/bundle-cli.mjs`):
   `packages/cli/tsconfig.dts.json` PASSES (i) and (ii) — it declares no `outDir` of its
   own and INHERITS `"outDir": "./dist"` from `packages/cli/tsconfig.json`, resolving to
   `packages/cli/dist`, which EQUALS that unit's `distDir`, along with `include: ["src"]`
   and `declaration: true`, so a compiler handed this file emits `dist/**.js` and
   `dist/**.d.ts`, both inside §3's four-extension whitelist. **It is excluded by (iii)
   ALONE**: it sets `"composite": false` EXPLICITLY, which takes it out of the `tsc -b`
   project graph that writes the compared outputs — a non-composite variant can only be
   handed to a tool directly, and where that tool puts its output is the tool's decision,
   not the config's. §6 row 47's `tsconfig.emit-into-A.json` SATISFIES (i) and
   (ii) as this one does — with `"outDir": "./packages/A/dist/emit"` from round 27 (`C27-1`)
   as relocated in round 33 (`CF33-1`), resolving from that fixture's ROOT to a STRICT
   subdirectory of `packages/A`'s `distDir` rather than the same literal — and differs on
   (iii) alone, its `composite` being INHERITED-TRUE, which is why row
   47's fixture cell pins that inheritance and row 3's pins the explicit `false` — without
   both pins the discriminating property is a free variable in the two fixtures that turn
   on it. `packages/cli/tsconfig.dts.json` declares
   `"extends": "./tsconfig.json"` — the config an upward walk provably never reaches —
   with `"references": []`, and its only consumer is
   `scripts/bundle-types.mjs:84`, which hands it to `dts-bundle-generator --project`
   writing `packages/cli/.dts-cache/` (re-derived 2026-08-21: `grep` over tracked
   non-doc files returns that line plus `repo-census.mjs:64`'s comment and
   `repo-census.test.mjs` fixtures; `tsc -b` reads `packages/cli/tsconfig.json` and never
   this file). ⚠️ **Its only artifact under `dist` TODAY is `dist/index.d.ts` via
   `bundle-cli.mjs:153` — the ONE artifact §3 excludes from `packages/cli`'s mtime side —
   and that is a fact about two build SCRIPTS, never this rule's criterion (round 24,
   `C24-1`).** A walk over `allConfigs` cannot derive which script invokes which config, so
   no implementation can resolve rule 4 from it; it is recorded because it is why the
   raw-`dist` wording would ALSO have left the defect standing, and because it is what
   clause (ii) alone would contradict — compiled, this config emits `.js` and `.d.ts`
   throughout `dist`, and only `dts-bundle-generator --project`, which READS the program
   and emits nothing under `dist`, keeps that hypothetical off the disk. Clause (iii) is
   what an implementation actually evaluates. Unnarrowed,
   rule 4 attached an input to a comparison whose output side provably cannot move for
   it: `touch` it and the check reports `stale-unit packages/cli`, sending the operator
   to `rm -rf packages/cli/dist` plus a 19-unit rebuild for a change whose whole remedy
   is §4's steps 3-4, and manufacturing a SECOND kind out of one edit, which is exactly
   what stops §4 step 2's sole-`stale-declarations` carve-out from applying. **No false
   negative is created: nothing in `packages/cli`'s compared output set can go stale for
   this config, and §3.3's declaration-cache comparand already holds it — with §6 row 3
   as its non-vacuity row.** **Requirement 9 is therefore discharged for this config by
   §3.3, and rule 4 has NO MEMBER on today's tree — by clause (iii), the ONLY clause that
   excludes it (round 24, `C24-1`)** — stated rather than hidden, because
   a future config satisfying ALL THREE of this rule's conditions must
   still be covered. ⚠️ **Clause (iii)'s cost is a STATED bound, not a silent one:** a
   future NON-composite variant that a build program hands to a compiler AND that emits
   into a unit's compared output set would be in no input set. It has no member on today's
   tree — re-derived read-only at `8fdf3ed`, `packages/cli/tsconfig.dts.json` is the ONLY
   config of the 30 whose chain resolves into a unit at all, and §3.3's comparand holds it
   — so the 21-of-30 arithmetic below and §8(l)'s NINE are unmoved, and the bound is
   recorded at **§8(l)** beside requirement 9's other two. ⚠️ **AND §6 row 47 is what makes that sentence testable, from round
   17 (`CF17-1`).** For sixteen rounds this rule, §2's `descendantConfigs` and §0's
   requirement 9 row all mandated the downward half and nothing in §6 exercised it, so an
   input set built from rules 1-3 alone passed every battery row, the false-negative
   battery, the wiring assertions and the live smoke test — the same shape as `C16-1`,
   `C16-2`, `C15-3` and `CF15-4`, at the one rule whose own text says it is kept for a
   tree that does not exist yet. Row 47's fixture is SYNTHETIC and carries a config
   satisfying ALL THREE of this rule's conditions — clause (iii) included, its `composite`
   being INHERITED-TRUE from the fixture's own `packages/A/tsconfig.json`, which that row's
   fixture cell pins from round 24 (`C24-1`) — so the arithmetic below is unaffected: rule
   4 still has no member on the live tree and the covered set is still 21 of 30.
   ⚠️ **Row 47's MAIN half is clause (i)'s and clause (iii)'s POSITIVE element, and clause
   (ii)'s ACCEPTING element from round 27 (`C27-1`) — until then its config's `outDir` WAS
   `packages/A`'s `distDir` itself, which a body carrying NO `outDir` test and a body
   spelling (ii) as EQUALITY with `distDir` both accept, and A MANDATED CONJUNCT THAT EVERY
   ELEMENT AGREES ON IS A CONJUNCT NOTHING TESTS (round 25, `CF25-1`).** It is now
   `"./packages/A/dist/emit"` — the config having moved to its fixture's ROOT in round 33
   (`CF33-1`), so the literal resolves against THAT directory to the same
   `packages/A/dist/emit` — a STRICT subdirectory reached by no minus-bullet and written on no disk,
   so the EQUALITY body declines it and FAILS this row on the row's own fixture while a
   correct body accepts it, the compared set being a set of FILES that need not exist yet.
   That body's harm runs the OPPOSITE way to the neighbours' — a FALSE NEGATIVE: a future
   `<unit>/tsconfig.esm.json` inheriting `composite: true` with `"outDir": "./dist/esm"`,
   the ordinary dual-emit layout, is in NO input set, so a `touch` of it prints PASS, exit
   **0 even under `--strict`** at `check:all` member 0 and under flagless `pretest`, on a
   `dist` compiled under the old `compilerOptions`. The body carrying NO `outDir` test still
   passes this half, which is what round 25's twin is for. ALL THREE clauses now name their own EXCLUDING element (round 34,
   `CF34-1`), and no element pins another's clause — clause (i)'s being the SECOND config that
   same twin gains, enumerated in this rule's closing clause-(i) paragraph below: §6 row 3's `packages/cli/tsconfig.dts.json`
   passes (i) and (ii) and fails (iii), so that row's NOT-`stale-unit` half kills a body
   spelling (i)+(ii) ALONE; §6 row 47's ROUND-25 TWIN carries
   `packages/A/tsconfig.emit-elsewhere.json`, which passes (i) and (iii) — its `composite`
   INHERITED-TRUE from `packages/A/tsconfig.json` exactly as `tsconfig.emit-into-A.json`'s is —
   and FAILS (ii) with `"outDir": "./dist/eo-elsewhere-fixture-1"`, a directory that
   fixture never creates, INSIDE `packages/A/dist` RAW and OUTSIDE the COMPARED set through
   the output set's `dist/eo-*-fixture-*/**` minus-bullet — which is in BOTH families and
   is NOT cli-scoped, rows 14 and 44 being its non-cli rows — so its CLEAN-in-BOTH-states
   verdict kills BOTH wrong bodies with ONE element: one spelling (i)+(iii) ALONE, and one
   spelling clause (ii) as RAW path containment under `<unit>/dist`, the cheaper reading and
   the one `C14-3` refused, an `outDir` being a DIRECTORY while the compared set is a set of
   FILES that need not exist yet (round 26, `CF26-1`). Written with `"./lib"` the twin was
   declined under BOTH readings and discriminated NEITHER. Before that twin
   BOTH fixtures SATISFIED clause (ii) — row 47's own cell says so in terms — and the
   (i)+(iii) body, the natural reading of §8(l)'s "the price of making clause (ii)
   decidable", passed every battery row, both halves of rows 3 and 47, the false-negative
   battery, the wiring assertions and the live smoke test, which cannot discriminate at
   all: rule 4's live member count is ZERO under the correct reading AND under that body.
   Its wrong result is on the tree this rule is kept for — a
   `packages/gates/tsconfig.bench.json` declaring `"extends": "./tsconfig.json"`, so
   `composite` is INHERITED-TRUE and (i) and (iii) hold, with `"outDir": "./bench-out"`, so
   (ii) fails: correct, it is in no input set and the check stays silent; under that body,
   `touch` it and the check prints `stale-unit packages/gates`, `rm -rf packages/gates/dist`
   and a 19-unit rebuild for a config whose output side provably cannot move for it. The
   twin's config is SYNTHETIC too, emits into a directory the fixture never creates, and is
   in no input set under the three-clause rule — so it adds no member here either and the
   21-of-30 arithmetic, the 20/9 split and §8(l)'s NINE stay as they are.
   ⚠️ **CLAUSE (i) HAD NO DISCRIMINATING ELEMENT UNTIL ROUND 33 (`CF33-1`), AND THE CHEAPEST
   WRONG READING OF IT IS PATH CONTAINMENT:** `allConfigs.filter((c) => c.startsWith(unitDir + "/"))`,
   `unitDir` being a PATH and `descendantConfigs`' first argument. Every rule-4 config in this
   design sat INSIDE its unit's directory — row 47's under `packages/A`, row 3's under
   `packages/cli` — so chain resolution and path containment agreed on all of them, and that
   body passed both halves of rows 3 and 47, round 27's two mutants, the false-negative
   battery, the wiring assertions and the live smoke test. Re-derived read-only 2026-08-22 at
   `2ef2ce5` (`git status --porcelain` empty before and after; no import of
   `scripts/bundle-cli.mjs`), the live tree cannot discriminate either: of the 30 tracked
   configs exactly ONE has a chain resolving into a unit, `packages/cli/tsconfig.dts.json`,
   and it sits under that same unit, so rule 4 has 0 members under BOTH readings — the
   argument this rule already makes for clause (ii), one clause over. Its harm runs BOTH ways.
   FALSE NEGATIVE, on the tree this rule is kept for: a ROOT-LEVEL `tsconfig.gates-emit.json`
   declaring `"extends": "./packages/gates/tsconfig.json"`, so `composite` is INHERITED-TRUE
   and (i) holds, with `"outDir": "./packages/gates/dist/emit"`, so (ii) holds, is in NO input
   set under that body — `touch` it and the check prints PASS, exit **0 even under
   `--strict`** at `check:all` member 0 and under flagless `pretest`, on a
   `packages/gates/dist` compiled under the old `compilerOptions`, which is the founding
   failure's shape. Such a config IS enumerated: `isVariantTsconfig` admits any
   `tsconfig.*.json` basename (`repo-census.mjs:75-79`) and the walk pushes a ROOT-LEVEL
   variant through its `rel === ""` branch (`:316-318`), which is why `tsconfig.base.json` is
   in `variantConfigFiles` today. FALSE POSITIVE the other way: a
   `packages/gates/tsconfig.standalone.json` with NO `extends`, `"composite": true` and
   `"outDir": "./dist"` fails (i) correctly and is in no input set, while that body attaches
   it and a `touch` prints `stale-unit packages/gates` with `rm -rf packages/gates/dist` plus
   a 19-unit rebuild — the false positive this rule's clause-(ii) passages already refuse. §6
   row 47's MAIN half is the element from round 33: its config sits at the FIXTURE ROOT,
   OUTSIDE `packages/A`, and only its chain reaches the unit, so that body attaches nothing
   and fails the row on the row's own fixture. ⚠️ **THAT IS CLAUSE (i)'s ACCEPTING SIDE, AND
   ITS FAILING SIDE WAS UNCLAIMED UNTIL ROUND 34 (`CF34-1`).** EVERY rule-4 config in this design
   SATISFIED (i), so a body DROPPING it — `allConfigs` filtered on (ii) AND (iii) ALONE, with NO
   chain test at all — passed both halves of rows 3 and 47, every mutant either half carries,
   the false-negative battery, the wiring assertions and the live smoke test. That body is
   cheaper than the PATH-CONTAINMENT one, the argument list being PATHS and clause (ii) already
   tying a config to ONE unit, and the live tree cannot discriminate it either: re-derived
   read-only 2026-08-22 at `5e4918f` by parsing all 30 tracked configs as text (no import of
   `scripts/repo-census.mjs`, none of `scripts/bundle-cli.mjs`), the only configs passing (ii)
   and (iii) are the 19 units' OWN `tsconfig.json`, each landing in the one input set it is
   already a rule-3 member of, so NO unit's input set gains a member and the check behaves
   IDENTICALLY under that body and under the correct one — rule 4 contributing ZERO members
   under the correct reading and NINETEEN REDUNDANT ones under that body. Its wrong result is
   this paragraph's own
   `packages/gates/tsconfig.standalone.json`, which that body ATTACHES: a `touch` prints
   `stale-unit packages/gates` with `rm -rf packages/gates/dist` plus a 19-unit rebuild for a
   config whose output side provably cannot move for it. §6 row 47's ROUND-25 TWIN carries the
   element, a SECOND non-member config `packages/A/tsconfig.standalone.json` in exactly that
   shape — NO `extends`, so (i) FAILS on the CHAIN while the FILE sits INSIDE `packages/A`;
   `"composite": true` declared directly, so (iii) holds; and `"outDir": "./dist"`, resolving to
   `packages/A/dist`, the unit's `distDir` ITSELF, so (ii) holds by the same at-or-below
   containment row 3's config satisfies — set NEWER than the newest output under
   `packages/A/dist`, so that body renders `stale-unit packages/A` at the twin's BASELINE where
   a correct body renders nothing. NO UNIVERSAL coverage claim is made for clause
   (i) even now — its two elements kill the PATH-CONTAINMENT body and the NO-CHAIN-TEST body,
   this paragraph enumerates no other wrong reading of it, and asserting more would be the
   universal `C11-4`'s class refuses.
   ⚠️ **27 + 2 = 29 is the size of `enumerateTsconfigs`' RETURN, not the size of the
   covered set — and the covered set is 21 of 30 (round 12, C11-1/CF12-3, correcting
   round 11).** The two are different questions and round 11 answered the wrong one: 8 of
   those 27 project dirs are NOT units. Units come from `enumerateRootReferences` (**19**,
   re-derived 2026-08-20); the other 8 are `e2e/{attestation,live,provisioning,release}`
   and `e2e/matrix/{connector,git,installation,orchestration}` — exactly the set
   difference. Re-derived 2026-08-20 by running rules 3 and 4 over the 30 tracked configs:
   **21 land in some unit's input set, 9 do not.**

   The 21 are the 19 unit `tsconfig.json` (rule 3's FIRST member — whose only non-vacuity
   element is §6 row 2's round-18 twin, `CF18-1`; under the ancestors-only walk that twin
   excludes, all 19 leave every input set and this arithmetic reads **2 of 30**, only
   `tsconfig.base.json` and — through §3.3's comparand — `packages/cli/tsconfig.dts.json`
   surviving, with every §6 element green) and `tsconfig.base.json` (rule 3's upward
   chain, length 2 for all 19) — **20 in some unit's INPUT SET** — plus
   `packages/cli/tsconfig.dts.json`, covered by **§3.3's declaration-cache comparand** and
   not by rule 4 since round 14 (C14-3). The COUNT is unchanged at 21 and the ATTRIBUTION is
   not, which is why §8(l)'s definition of discharge is widened in the same round from
   "membership of some unit's input set" to "read by some comparison": under the old wording
   this reads 20 of 30 for identical coverage, and an arithmetic that moves to follow an
   attribution change is not a measurement. The 9 are:

   - the **ROOT `tsconfig.json`**. Rule 3 reaches it only by an upward `extends` walk and
     it declares none; rule 4 reaches only configs whose own chain resolves INTO a unit,
     and this one has `"files": []` plus 19 `references` and nothing else — re-derived
     2026-08-20, `Object.keys` on the parsed file returns exactly `files, references`.
     `build` is `tsc -b && …` (`package.json:15`), so a build program DOES hand it to a
     compiler. Residual **§8(l)**, bounded because it carries no `compilerOptions` and
     because §3's own **Units** step re-reads it on every run (`enumerateRootReferences`);
   - the **8 `e2e/*` configs**, each declaring `"extends": "../../tsconfig.base.json"` (or
     `../../../`), which resolves to the root-level base — so rule 4's chain never
     resolves into a unit — and none of which an upward walk from the 19 reaches.
     `scripts/check-e2e-types.mjs:40` runs `npx tsc -p <config> --noEmit` for exactly
     `E2E_TYPECHECK_PROJECTS` (`:24-32`), wired as `check:e2e-types` (`package.json:24`),
     so requirement 9's own wording covers them. Residual **§8(f)**, bounded by that
     bullet's already-measured reason: all 8 set `noEmit: true`, so they emit no output
     that can go stale.

     Requirement 9 is therefore discharged for **21 of the 30** — **20 through a unit input set
     (rule 3; rule 4's member count on today's tree is 0, `packages/cli/tsconfig.dts.json`
     being excluded by rule 4's clause (iii) ALONE and never by a build-script fact — round
     24, `C24-1`) plus `packages/cli/tsconfig.dts.json`
     through §3.3's comparand** (round 14, C14-3). ⚠️ **Standing rule 4 at the
     site of round 11's own fix**: 29 was a measurement of the enumeration, and this rule is
     about the input SETS.

⚠️ **EVERY CONFIG READ NAMES ITS FILE (round 12, OP12-5).** Rules 3 and 4 parse configs
with raw `JSON.parse` — all 30 do so today — and a `JSON.parse` failure carries no path:
measured 2026-08-20 on node v24.18.0, a malformed config yields `Expected double-quoted
property name in JSON at position 7 (line 1 column 8)` and nothing else, while an `fs`
failure yields `ENOENT: no such file or directory, open 'nope.json'` WITH the path. So
`units.mjs` wraps each read and re-throws as `<path>: <err.message>` — the shape `ENOENT`
gives for free — and wraps the one census call that parses, `enumerateRootReferences`
(`repo-census.mjs:290`), whose file is the root `tsconfig.json`. Without it §6 row 27's
own fixture hands a 3am operator one line, exit **2** at `check:all` member 0, with **30**
candidate configs and no locus. §4's internal-error line is where that path is printed,
and §6 row 27 is where the path is asserted — ⚠️ **row 27's own fixture reaching only the `units.mjs` wrap, and the CENSUS wrap being asserted by its round-18 TWIN and by nothing before it (`C18-5`, standing rule 9).** Row 27 mutates ANY unit's `tsconfig.json`, which rules 3 and 4 read; `enumerateRootReferences` parses the ROOT `tsconfig.json`, intact on that fixture, so for six rounds this paragraph's second clause was mandated here and exercised nowhere, and an unwrapped census call passed rows 27 and 31 alike.

Everything else under `src` is excluded: **18** non-`.ts` files exist across all 19
`src/` trees — 12 `.mjs`, 3 `.json`, 2 `.snap`, 1 `.info` — so the 3 `.json` are inputs
and the other **15** are fixtures no compiler reads.

```
for d in packages/*/src e2e/report/src; do find $d -type f; done | grep -v '\.ts$' | sed 's/.*\.//' | sort | uniq -c   # 12 mjs, 3 json, 2 snap, 1 info
git ls-files | grep -E 'tsconfig.*\.json$' | wc -l                                            # 30
```

**Per-unit output set** — newest mtime over `<distDir>/**`, where `distDir` is §2's
per-unit field resolved from that unit's own `outDir` and never the literal `dist`
(round 21, `C21-1`; `./dist` for all 19 today), **recursively — the `**` is
the whole of that claim and until round 18 nothing read it (`C18-1`)**: re-derived read-only
over this repository's 19 root references, **933** nested `.js` against **332** flat outside
`dist/plugin/` and the scratch dirs, and §6 row 1's amended fixture is the one element a
shallow DIST walk fails, reading `dist/a.js` as the newest output and reporting `stale-unit`
at BASELINE. Filtered to `.js`,
`.d.ts`, `.js.map`, `.d.ts.map` — ⚠️ **a WHITELIST of FOUR, and until round 20 nothing in
§6 read it (`C20-1`, standing rule 9 beside the `**` above)**: row 48's cell CITES this
list and its fixture holds no `.map` and no `dist/**.json` at all, and row 22's twin writes
`dist/index.d.ts.map` in the SAME emit as `dist/index.js`, so it moves no verdict under
either reading. §6 row 44's round-20 TWIN is the element, in that row's own
newest-file-under-`dist` shape, and it kills both reachable wrong readings. (1) A whitelist
of `.js`/`.d.ts` alone — carrying all four minus-bullets and the `**` correctly — is a live
FALSE POSITIVE: re-derived read-only 2026-08-21 at `9c247eb` (`git status --porcelain`
empty before and after; no import of `scripts/bundle-cli.mjs`), on `packages/supervisor`,
`packages/scheduler` and `packages/learning` the newest member of this filtered set is a
`.d.ts.map` (1787133004.80 / 1787133015.08 / 1787133016.63) and the newest `src` input
falls STRICTLY BETWEEN it and the newest `.js`/`.d.ts` (1787058900.27 < 1787065543.56;
1787072756.96 < 1787078266.57; 1786862792.88 < 1786871043.74) — so that implementation
reports **SEVEN** `stale-unit` against the **FOUR** this section's live paragraph calls
independent evidence, at `check:all` member 0, exit **1** under `--strict`, and at line 5 of
every developer's `npm test` under `pretest`. The design's own verdict on those three trees
is CLEAN, which §7's `Stale .d.ts.map lingering after declarationMap: false` row names as an
accepted false NEGATIVE. (2) A reading with NO whitelist — the four minus-bullets and
nothing else — is the reciprocal FALSE POSITIVE at the STEM side: `resolveJsonModule: true`
(`tsconfig.base.json:10`) makes `dist/**.json` reachable the moment a compiled `.ts` imports
one of the **3** `src/**.json` rule 2 counts as inputs, after which §3.1's stem walk names
an `orphan-output` the next `tsc -b` re-emits, so §4's `rm -rf <unit>/dist` +
`npm run build` cannot clear it. §8(g) is amended in round 20, having mandated that second
reading in terms — minus:

- `.tsbuildinfo` — incremental state, not emitted output; `bundle-cli.mjs:113`
  preserves it across the wipe. ⚠️ **This exclusion is load-bearing a SECOND time, in
  §3.3's marker-freshness clause (round 9).** Measured on a composite fixture with the
  pinned `typescript@6.0.3`: `touch src/a.ts && tsc -b` moved `dist/.tsbuildinfo` and left
  `a.js`/`a.d.ts` untouched. A freshness rule reading raw `dist/**` would therefore report
  **`unbuilt packages/cli`** after any `tsc -b` that re-checks a touched-but-unchanged
  source — §7 row 1's scenario (format-on-save, `git checkout`), promoted from `stale-unit`
  to the loudest kind there is, with a `rm -rf` + ~5-minute-rebuild remedy;

**This filtered set is also the comparand for §3.3's `packages/cli` completion-marker
freshness clause AND for §3.2's FIRST `stale-bundle` input member — the newest output
across the 16 inlined units** — the same `isCompilerOutput` predicate in both, never a raw
`dist/**` walk. ⚠️ **TWO further consumers, and this sentence named ONE for seventeen
rounds (round 23, `C23-2`, standing rule 9).** The second is evaluated against an INLINED
unit, so `.tsbuildinfo` and `dist/eo-*-fixture-*/**` are the two minus-bullets that reach
it — and the scratch one is LIVE, `packages/journal` being one of the 16 AND the one unit
this repository's own suite writes `dist/eo-*-fixture-*/**` into: re-derived read-only
2026-08-21 at `53996f5`, that unit's newest scratch `.js` sits at mtimeMs
**1787308024588.285** against the newest FILTERED output across the 16 at
**1787156422460.689**, a **1.75-day** forward shift an unfiltered member-(i) walk takes and
the correct one drops. Stated here, in the section that DEFINES the output set, and not
only where the rule is written: that exact split is what round 6's `CF6-1` filed, and
naming one consumer out of two is that same split one consumer along. §6 row 5's round-23
TWIN is the second consumer's element.

- for `packages/cli` only, `dist/plugin/**` — **requirement 1**. `bundle-cli.mjs:178`
  copies the six `PLUGIN_ASSET_ENTRIES` with `cp(..., { recursive: true })` and no
  `preserveTimestamps`, so those mtimes are copy time and say nothing about a compile.
  ⚠️ **Excluded from the MTIME comparison only (round 9, CF9-2)**: all six entries are
  members of §3.3's artifact set and must EXIST, and a missing one is `unbuilt` with the
  gap named. The presence check is load-bearing since round 9 moved completeness off the
  marker onto the artifact set — read as a plain exclusion, two of that set's members
  ship unchecked;
- for `packages/cli` only, `dist/index.d.ts` — a `copyFile` of the declaration cache
  (`bundle-cli.mjs:153`). Measured: `copyFile` does NOT preserve timestamps, so the
  destination always carries copy time, and leaving it in would make `packages/cli`
  look fresh after any bundle whether or not `tsc` recompiled — the masking fixture
  row 7's round-18 TWIN exists for — ⚠️ **row 7 ITSELF mutated `dist/plugin/.mcp.json` until round 35 (`C35-1`) and its own verdict cell says the plugin exclusion is what it tests, so this exclusion was cited to a row asserting the NEIGHBOURING one (`C18-6`; `CF13-4`'s and `CF17-1`'s shape at the second excluded artifact).** THREE of this list's four minus-bullets have an ELEMENT that DISCRIMINATES them — `dist/plugin/**` (row 7 AS AMENDED in round 35, whose `.json` mutation round 20's whitelist alone rejected, so the cell calling itself the proof that the comparand is the filtered set proved the WHITELIST and not this bullet), `dist/index.d.ts` (row 7's round-18 TWIN, `C18-6` — THIS bullet's own element, named at the head of the very bullet this sentence sits in, and one round 20 never subsumed: `.d.ts` is INSIDE the four-extension whitelist, so a body carrying the whitelist and dropping this bullet still reads `bundle-cli.mjs:153`'s copy time as an output and the twin still fails it) and `dist/eo-*-fixture-*/**` (rows 14 and 44, `C16-1`) — while `.tsbuildinfo` ALONE has NONE and needs none. ⚠️ **Round 35's first apply wrote `TWO` here, dropping the bullet this sentence SITS IN four clauses after naming its element — standing rule 9 at the fan-out of a fix for standing rule 8.** Read at `TWO` this list asserts that `dist/index.d.ts` has no discriminating element, which is the premise the round-23 ruling in the NEXT sentence licenses DELETING an element on; apply it and row 7's round-18 twin goes, re-opening `C18-6` verbatim — the masked window this bullet re-derives below, at PASS and exit **0 even under `--strict`**. §0's requirement-1 row and §9's own round-35 coverage matrix both name that twin as this bullet's element, so the contradiction was internal and checkable without leaving the document. ⚠️ **The two subsumptions are DIFFERENT claims, and carrying them as one is what let round 20's whitelist vacate row 7 unnoticed for fifteen rounds (round 35, `C35-1`, standing rule 8):** the four-extension whitelist rejects `.tsbuildinfo` by EXTENSION, so dropping THAT bullet flips nothing anywhere and no wrong body exists to catch — round 23's own ruling at §6 row 5's ROUND-23 twin (qualified in round 36, `C36-1`: row 5 carries a SECOND twin from that round, so `C20-2`'s must-say-WHICH rule reaches this citation), "a cell claiming one mutant per excluded file would be asserting a mutation that cannot fail", read here at the list that ATTRIBUTES the elements — while `dist/plugin/**` is subsumed only while no file under the six `PLUGIN_ASSET_ENTRIES` carries one of the whitelist's four extensions, which is a fact about `packages/plugin` and never a property of it: **ZERO** of the **26** files under `packages/cli/dist/plugin` do today, over the four extensions `.json`, `.md`, `.mjs` and `.mts`, and `packages/plugin/hooks/` already holds **five** `.mjs` RECURSIVELY — four at its top level and one under `lib/`, re-derived read-only 2026-08-22 with `find packages/plugin/hooks -type f -name '*.mjs'`, the recursive form because a `.js` ANYWHERE beneath an entry is copied by `bundle-cli.mjs:178` (standing rule 1), so a `.js` sibling is one commit away — both re-derived read-only 2026-08-22 at `a5605e9` (`git status --porcelain` empty before and after; no import of `scripts/bundle-cli.mjs`), `CF15-4`'s own drift argument at the neighbouring set. And this one had none until round 18. Re-derived read-only at `896dd84` over §6's whole mutation column: ZERO rows moved `packages/cli/dist/index.d.ts`'s mtime, so an `isCompilerOutput` carrying the other three and not this one passed every battery row, the false-negative battery, the wiring assertions and the live smoke test, while masking any `packages/cli/src` edit whose mtime falls between the esbuild write (`bundle-cli.mjs:121`) and this `copyFile` (`:153`) — a window re-derived at **11.10 ms** on this working copy, which bounds the exposure. **Excluded from the mtime comparison only**, for the same reason:
  its presence is a member of §3.3's artifact set. Its CONTENT is **covered by §3.5's
  sixth comparison, NOT by §3.3** — §3.3 reads `.dts-cache/index.d.ts` only, and this
  pointer named it for seven rounds while nothing in the design read the shipped copy
  at all (round 9);
- **`dist/eo-*-fixture-*/**` — test-written scratch, in BOTH families.**
  `packages/journal/src/crash-fixtures/prepare-runtime.ts:25,:110` and
  `packages/journal/src/lease-fixtures/prepare-runtime.ts:60,:91` both set
  `SCRATCH_ROOT` to `packages/journal/dist` and `mkdtemp` into it, transpiling `.ts`
  sources to `.js` there. Round 5 named only the crash family and put the exclusion in
  §3.1's prose rather than here, where the output set is actually defined — so an
  implementer building `walk.mjs` from §2 and this list would have shipped no exclusion
  at all. The glob covers both families and any third that follows the convention.
  **§6 row 44 is the non-vacuity row for the mtime side, and row 14's amended fixture for
  the stem and unit-state sides — both round 16's (`C16-1`), and both the FIRST:** for
  fifteen rounds this exclusion was mandated in TWO sets by two sections and asserted by
  nothing in §6, so an implementation carrying no exclusion at all passed every battery
  row, the false-negative battery, the wiring assertions and the live smoke test, which
  asserts unit-set equality and coverage shape and explicitly not the finding set. Measured
  read-only on this working copy 2026-08-21: that implementation reports **20**
  `orphan-output packages/journal` findings — this kind is one of the four §4 does NOT
  collapse, so that is twenty printed lines — exit **1** under `--strict` at `check:all`
  member 0, and it reads that unit's newest output as **1787308024** rather than
  **1787133002**, a **2.03-day** forward shift that masks any `stale-unit packages/journal`
  in the window.

**Verdict:** `newest(inputs) > newest(outputs)`, strictly. Equal timestamps are clean —
the safe direction under second-granularity filesystems. ⚠️ **This convention governs FIVE
mtime comparisons and, until round 36 (`C36-1`), TWO of them had an element at the BOUNDARY
— standing rule 9 at the sentence that states it once for all five.** This Verdict's own is
§6's false-negative bullet `input mtime exactly equal to output mtime`; §3.3's clause-2
MIRROR (`>=`) is §6 row 19's EQUAL-mtime twin (round 19, `C19-2`); and §3.2's, §3.3's
declaration-cache and §3.4's thresholds are §6 rows 5, 6 and 24's EQUAL-MTIME TWINS. That
bullet reaches none of the other four: its subject is `checkUnitFreshness`'s comparand, and
§2 declares the four comparisons as four functions with no shared staleness predicate to
carry it to them.

**Comparison coverage.** That verdict answers each comparison that RAN. Four of the
six can fail to run on a tree that is otherwise clean, so `checkBundleFreshness`,
`checkDeclarationCache`, `checkPluginAssets` and `checkShippedDeclarations` each return
`{ findings, coverage }` with `status ∈ "full" | "not-run"` — two values, since round 10
deleted `reduced` as unreachable (§2, §3.2) — and a reason
naming the absent artifact — or, where the state names no absence at all, the spelling its
own section mandates: `packages/cli/dist holds no esbuild chunk` for a `dist` that is
PRESENT and matches no chunk file (§3.2's Rule; round 17, C17-1), and
`.bundle-meta/metafile.json unreadable` for a marker that exists and does not parse (§3.2;
round 10, C10-3) — the second read off the marker PATH rather than off `readMetafile`'s
return, which cannot separate it from an absent marker, and asserted on this side only
since round 22 (`C22-1`) — or, when `unitState("packages/cli").state === "unbuilt"`
suppresses all four, the mandated `packages/cli unbuilt`
(§3.2, §4; round 10, C10-1) — ⚠️ **which is the reason WHENEVER that suppression fires and
not only on the tree where nothing is absent (round 19, `C19-1`)**: it is evaluated before
any comparison inspects its own comparand (§3.2's **REASON PRECEDENCE**, rank 1), so an
implementation that resolves each entry's reason from its own comparand and falls back to
this one only when nothing is absent is the reading this bullet forbids — the reading the
phrase "with nothing absent to name" licensed here for nine rounds, and the one §6 row 26's
round-19 twin fails on its own fixture — and **§4 prints every non-`full` entry on BOTH
the PASS path and the findings path**. A comparison that did not run is never absorbed
into `clean`, exactly as `unbuilt` is never folded into `clean` (§3.3). ⚠️ **And a
non-`full` entry is also what makes §4's REMEDY PLAN print on a run holding ZERO
findings (round 14, OP14-1)** — §4's **A DEGRADED RUN CARRIES A NEXT ACTION**. Reporting
that a comparison did not run, and stating how to make it run again, are two different
silences; until round 14 this bullet closed only the first, so on the tree §4's recipe
step 1 creates three entries printed and the check's only re-run instruction did not.
`checkUnitFreshness` and `checkOrphans` emit no coverage entry: theirs is the unit
table, already printed in the units-compared/skipped summary §4 renders on BOTH paths —
the `PASS` line's own slot where there is a `PASS` line, a line of its own after the
grouped findings where there is not (§4's **SKIPPED UNITS ARE NAMED ON BOTH PATHS**) —
and printing that fact twice is the failure mode on the other side of this rule.
⚠️ **Round 13 (OP13-1): this bullet justified the missing coverage entry by a slot the
findings path did not print**, so on every run holding a non-advisory finding the unit
table reached the operator nowhere at all — the O9-1 defect one level over, produced by
O9-1's own fix.

### 3.1 Orphan outputs — requirement 2

For the 18 `kind: "tsc"` units **that `unitState` reports `compared`** — `kind` derived per §2's `buildUnits` and §3's Units step, `"bundled"` for `packages/cli` ALONE and never off that unit's reference closure, with §6 row 4's round-23 TWIN the element pinning BOTH halves (round 23, `C23-1`) — compare extension-stripped stems **RECURSIVELY on both sides**: `<distDir>/X.js` must have `<srcDir>/X.ts`, and `<distDir>/sub/y.js` must have `<srcDir>/sub/y.ts` — the two §2 fields, resolved per unit from that unit's own `outDir`/`rootDir` and never the literals (round 21, `C21-1`) — **§6 row 4's amended fixture is the recursion's non-vacuity row, from round 18 (`C18-1`)**, this section's own citation (row 14's BASELINE) and row 44 both being SCRATCH-EXCLUSION rows a shallow `stemsUnder` satisfies unchanged. ⚠️ **AND the stem set is drawn from §3's FILTERED output set, so a `dist/**.json` contributes NO stem at all — mandated here for the first time in round 20 (`C20-1`), and asserted by nothing before it.** §3's four-extension whitelist is what excludes it; row 48's cell cites that list from the INPUT side only (a `src/**.json` that is an input and never an output stem) and its fixture holds no `dist/**.json`, so a stem walk whose accept predicate carries the four minus-bullets and NO extension whitelist passed every element here while reporting one `orphan-output` per emitted `.json`. `resolveJsonModule: true` (`tsconfig.base.json:10`) makes that reachable the moment a compiled `.ts` imports one of the **3** `src/**.json` §3's rule 2 counts as inputs, and the finding it manufactures is one this section's own printed remedy cannot clear: `rm -rf <unit>/dist` then `npm run build` re-emits the file, so the check fires again identically. §6 row **44**'s round-20 twin carries a `dist/**.json` whose stem has NO `src` counterpart under any extension and asserts ZERO `orphan-output` at its baseline; §8(g) is amended in round 20, having claimed the orphan check WOULD report exactly this input. A `skipped` or `unbuilt` unit is never walked for stems — see §3's mapping. Measured today across all 18: **0** orphaned outputs and **0**
un-emitted sources, a clean bijection — so the rule is exact, not approximate.

⚠️ **THAT COUNT IS UNSTABLE, BECAUSE THE TEST SUITE WRITES INTO A `dist` (round 5).**
`packages/journal/src/crash-fixtures/prepare-runtime.ts:25` sets
`SCRATCH_ROOT = <journal>/dist` and `:110` `mkdtemp`s `eo-crash-fixture-*` there,
transpiling `.ts` sources to `.js` inside it. Those `.js` have no `src` counterpart, so
they ARE orphans by this rule — round 5 measured **40** of them, and observed two
fixture directories being reaped mid-review. The suite's job is to SIGKILL its children,
so `cleanup()` cannot run when the harness dies.

Three ways this bites precisely this design:

- `pretest` is the primary trigger, so the check runs at the START of the very
  `npm test` whose interrupted predecessor left the residue;
- the printed remedy would be `rm -rf packages/journal/dist`, telling the operator to
  delete a directory a concurrent run may be writing into;
- §6's live smoke test calls `checkStaleDist(REPO_ROOT)` from inside vitest, so it can
  observe the orphans or `ENOENT` on a directory reaped between `readdirSync` and
  `statSync`.

**So the output and stem sets exclude test-written scratch**: `dist/eo-*-fixture-*/**`
by name, stated rather than inferred, **and §6 row 14's BASELINE is the assertion of the
STEM half (round 16, `C16-1`)** — until then nothing in §6 read the stem side at any point
(re-derived at `db857c3`, the document round 16 READ: `grep -n stemsUnder` returned exactly
TWO lines, both in §2, and NOTHING in §6 — the sha travels with the figure because the bare
present-tense form was already false in `badf45a`, the commit that printed it, where rows 14
and 44 carry the name, and today §6 returns rows 4, 14 and 44, all three at or after round 16;
round 29, `C29-1`), so a `stemsUnder` accept-predicate omitting this exclusion discharged
requirement 2
while reporting one `orphan-output` per scratch `.js` on the live tree — 20 of them — with
the `rm -rf packages/journal/dist` remedy this section names as the hazard three bullets
above. Row 44's baseline asserts the same half a second time, on a fixture whose scratch
file is also the newest under `dist`. And §3.1's bijection is restated as _0 orphans on a
tree with no test run in flight_ — a measurement whose subject a test run mutates.

```
# per unit: comm -23 <(dist .js stems) <(src .ts stems)  -> 0 for all 18
```

`packages/cli` is exempt, structurally rather than by fudge: `bundle-cli.mjs:113-119`
wipes `dist` except `.tsbuildinfo` before every bundle, so a completed `bundle:cli`
cannot leave an orphan; and esbuild's outputs are content-hashed chunks
(`chunk-DVV3SNQ3.js`, `run-dispatcher-POLZZ2DH.js` on disk now) that map to no single
source. Its 10 non-plugin `dist` files are covered by §3.2 instead.

⚠️ **That argument covers COMPLETED bundles only, and the gap is stated rather than closed
(round 11).** Delete a `PLUGIN_ASSET_ENTRIES` source and do NOT rebuild: the shipped copy
under `packages/cli/dist/plugin` survives as an orphan by this section's own rule, and
this exemption is precisely why nothing here reports it. Residual §8(m), cleared by the
next completed `bundle:cli`, whose wipe at `bundle-cli.mjs:113-119` removes `dist/plugin`
entirely before re-copying — re-derived 2026-08-20 at `:113`, where `KEEP` is
`.tsbuildinfo` alone.

**The orphan comparison therefore emits no coverage entry of its own** (§3).
`packages/cli` is exempt by construction, and any other unit it could not check is
`skipped` or `unbuilt` — already named as a finding, or in the units-compared/skipped
summary §4 prints on BOTH paths (§4's **SKIPPED UNITS ARE NAMED ON BOTH PATHS**).
⚠️ **Round 13 (OP13-1): this read "the PASS line's skipped slot", and the run that
carries a `skipped` unit BESIDE a finding prints no `PASS` line at all** — §3's coverage
bullet carried the same half-truth, and standing rule 9 is why both are fixed in one
round rather than one.
An implementer who adds a FIFTH entry here prints the same fact twice; one who reads
§3's "six comparisons" and expects six coverage entries prints a blank. Four entries,
never six: `checkUnitFreshness` and `checkOrphans` are the two comparisons without one,
and `checkShippedDeclarations` is the fourth that has one (§2, §3.5).

### 3.2 Bundle freshness — requirement 3

The comparison the record says a design omitting it "does not address the incident it
is named for".

**Consumer set:** the transitive closure of `packages/cli/tsconfig.json`'s
`references` — **16** units. `packages/perf` and `e2e/report` are unreachable and
correctly excluded.

ℹ️ **Round 4 measured the two sets and they are EQUAL today** — the metafile filter
yields 16 units, member for member identical to the reference closure, and `cli` never
matches `^packages/([^/]+)/dist/` because it enters only via `src/`. So "superset"
overstates it and the "minus `cli`" step is a no-op. The metafile remains the rule
anyway: the closure's agreement is a fact about today's import graph, not a property of
it, and the reasoning below is why the closure cannot be trusted to keep agreeing.

⚠️ **Calling the closure "the inlined set" was a round-1 finding.** `@crabgic/testkit` is in the closure but is not inlined: it is a
`devDependency` of `packages/cli`, not a dependency, and **zero** testkit runtime
symbols appear anywhere in `packages/cli/dist/*.js`. So editing `packages/testkit/src`
and running `npm run typecheck` fires `stale-bundle` against a bundle containing none
of that code — and under `--strict` that halts the other 14 `check:all` members, while
under `pretest` it reprints on every `npm test`. ⚠️ **Never a blocked push, and this
sentence said one for sixteen rounds (round 17, OP17-1):** §5 measures `check:all` as
invoked by no workflow and no hook, and `core.hooksPath`'s `pre-push` rebuilds before it
tests.

⚠️ **THE ROUND-1 FIX FOR THAT WAS REFUTED IN ROUND 2.** The "zero testkit symbols"
premise was wrong: it grepped `packages/cli/dist/*.js` for the specifier
`@crabgic/testkit`, and **esbuild ERASES specifiers when it inlines**, so that search
could only ever return nothing. It was narrower than the claim it evidenced — standing
rule 1, on this design's own fix. Re-measured by symbol DEFINITION,
`packages/cli/dist/chunk-I6JBP7DT.js` carries **11** occurrences of testkit identifiers
(`GIT_FIXTURE_IDENTITY`, `ALL_FIXTURES`, `buildIntentContract`, …). Testkit **is**
inlined, reached through `@crabgic/engine-claude`'s re-export of
`./adjudication-policy.js`.

Worse, the rule generalises catastrophically. `packages/cli/package.json` declares
**zero** `@crabgic/*` `dependencies`; its only two `@crabgic` edges are
`devDependencies` — `renderer` and `testkit` — and **renderer is inlined too**. So
"minus dev-only edges" either drops both inlined units, or read transitively drops all
16 and makes the comparison **vacuous**.

**The inlined set is therefore derived from esbuild's own metafile, never from the
package.json graph.** `result.metafile.inputs` is exactly the set of files that entered the
bundle.

**The derivation is stated rather than left to the implementer**, because `inputs` is
not a unit list: measured with esbuild 0.28.1 under `bundle-cli.mjs`'s own options it
holds **758** entries spanning **17** `packages/*` plus a large `node_modules/**` tail.
`inlinedUnitsFromMetafile` therefore keeps keys matching `^packages/([^/]+)/dist/` — ⚠️ **the ONE `dist` this design leaves as a LITERAL after round 21's per-unit `distDir` rule (`C21-1`), and deliberately:** these keys are esbuild's own resolved input paths, so the pattern reads what the BUNDLE consumed rather than what a tsconfig declares, and re-spelling it against §2's `distDir` would key it on a config the metafile never saw. A unit emitting anywhere other than `dist` therefore falls OUT of the inlined set rather than being mis-compared — a bounded false negative with **no member on today's tree** (19 of 19 uniform, re-derived read-only at `3095116`), written here so §3's per-unit rule and this filter are not read as contradicting each other (`CF13-4`'s shape)**,**
takes the capture, and drops `cli` itself — `packages/cli` appears via its own `src/`
and is the consumer, not an inlined dependency. The declared dependency graph is not that set and never was — the bundle is
built from the ESM import graph. **§6 row 45 is the non-vacuity row, added in round 16
(`C16-2`).** Until then this rule — the one round 1's `CF-1`/`C-3` and round 2's `C-A` cost
two rounds to establish, `C-A` calling the alternative "a **vacuous** comparison, in the one
place requirement 3 exists for" — was exercised by NOTHING: `inlinedUnitsFromMetafile` is
ASSERTED in §6 at row 45 ALONE — the ASSERTION and never the occurrence set, because the SET
moved with the next append exactly as the count did (round 29, `C29-1`): re-derived read-only
at `c672565` the name occurs in §6 at rows 40, 42 and 45, row 40 inside its twin's
fixture-construction clause about the marker's `inputs` ORDER (`C26-2`) and row 42 inside a
fixture-conformance argument about `C15-2`'s inlined-set rule, and neither of those asserts
this derivation — that the inlined set is `inlinedUnitsFromMetafile(metafile)` and never the
package.json graph or the tsconfig closure — which is row 45's own stated subject. The
historical figures keep their shas, and the SET form round 17 wrote (`CF17-2`) was true only
in the commit that wrote it. At
`git show db857c3:<file>`, the document round 16 READ, it occurred **three** times with none
inside §6, exactly right; at `git show badf45a:<file>`, the commit that PRINTS the figure,
it occurs **six** times with **one** inside §6 — row 45's own new cell, written by round
16's own apply. `C11-4`'s class, one bullet away from `CF16-2`, which had just forced the
sha onto the preamble's fifth structural clause for the identical reason.
A `checkBundleFreshness` deriving its inlined set from the transitive closure of
`packages/cli/tsconfig.json`'s references passed every battery row, the false-negative
battery, the five `bundle-cli.mjs` source properties, the `units.mjs` and `walk.mjs` import
reads, the `package.json` and `.gitignore` reads and the live smoke test — because every
cli-scoped fixture makes the two derivations COINCIDE by construction, and neither the
marker-`outputs` rule nor the inlined-`dist` rule forces them apart at any row. Row 45 is
the one fixture in which they are deliberately made to differ, which is precisely what the
ℹ️ above requires: the closure's agreement is a fact about today's import graph, so no
assertion may rest on a fixture in which they agree.

⚠️ **BUT THAT RULE WAS UNIMPLEMENTABLE AS FIRST WRITTEN, AND THE DESIGN CAUGHT IT
BEFORE THE NEXT ROUND DID.** `bundle-cli.mjs:140` sets `metafile: true` and `:181`
reads `result.metafile.outputs` **in memory**, then discards it. Measured:
`find packages/cli/dist -name "*meta*"` returns nothing. The check runs at a different
time from the bundle, so there is no metafile for it to read.

**So the design requires EDITS TO THE BUILD PROGRAM, and §1's table is the
authoritative scope list — this sentence states no count of its own** (round 12, CF12-4:
round 10's C10-2 raised the count from three to five and it reached §1, §2, §3.3, §3.4,
§6 and §8 but not here, so this line still read TWO). The two THIS section is about are: `bundle-cli.mjs` **writes** `result.metafile` to
**`packages/cli/.bundle-meta/metafile.json`** — gitignored, and deliberately OUTSIDE
`dist` — **after the plugin-copy loop**, and **clears** it in the wipe step before
`build()` so the marker names the build it belongs to (§3.3, round 8's C8-2). ⚠️ **The write is prescribed as a PAIR with the `mkdir` of `packages/cli/.bundle-meta/` that precedes it (round 21, `C21-2`) and that FOLLOWS the wipe-step clear (round 22, `CF22-1` — a `mkdir` above the clear is deleted by it, so §6 asserts BOTH halves of the order) — a companion of the write and NOT a third subject of this section, so the count one sentence above is untouched and §1's table stays the authoritative scope list.** That directory is gitignored by this change set's own `.gitignore` entry and is created by nothing today — re-derived read-only 2026-08-21 at `3095116`, `grep -c bundle-meta scripts/bundle-cli.mjs` → **0** and the directory does not exist — so `writeFile` alone throws `ENOENT` on the first build of every tree and takes `npm run build` down with it. All THREE
placements are pinned by §6 — the clear before `build()`, the `mkdir` after the clear and
the write after the plugin-copy loop (round 22, `CF22-1`); none is visible from the tree.

⚠️ **`packages/cli/dist/.bundle-meta.json` was the first proposal and it would have
SHIPPED.** `packages/cli/package.json`'s `files` is
`["dist", "!dist/**/*.test.*", "!dist/**/test-support/**", "!dist/.tsbuildinfo"]`, so
anything new under `dist` is published by default — and the metafile is **404,198 bytes
over 758 inputs**, including a `node_modules/**` tail. This repository already fought
exactly this battle: `check-published-tarball.mjs:41-45` bans `.tsbuildinfo` because it
is "the one file that differs between two builds of identical sources in different
environments, so shipping it makes the published artifact non-reproducible — directly
undermining roadmap/23's reproducible-build criterion. Shipped in 1.0.0 through 1.1.1."
The metafile is twice that size with the same defect, and **no `files` negation and no
`FORBIDDEN_PATTERNS` rule would have caught it** — `check:tarball` would have passed it
straight into the published package.

Beside `.dts-cache` is the shape this repository already uses for a build artifact that
must not ship.

⚠️ **And the entry has to be copied, not just the shape.** `.dts-cache/` is gitignored at
`.gitignore:47`; `packages/cli/.bundle-meta/` is **not** — measured with
`git check-ignore -v`, which reports nothing for it. Without that line the design creates
an untracked file on every build, which `repo-census.mjs` reports in its "on disk,
neither tracked nor ignored" bucket and which sits in `git status` forever. So the change
to the build program comes with a second, one-line change: add
`packages/cli/.bundle-meta/` to `.gitignore`.

This was caught by pre-checking the fix rather than by a review round — the fourth
consecutive round in which a fix in this design carried a defect, and the third caught
before a reviewer saw it. The pattern is worth more than any single instance: **a fix
that copies an existing shape must copy the whole of it**, and the only reliable way to
find out is to run the check the shape implies. Three properties then make it the right oracle rather than a workaround:

- it is the **only** ground truth for "what went into this bundle" — every alternative
  (the package.json graph, the tsconfig closure, grepping for specifiers) has already
  been measured wrong in this design;
- when it is absent the bundle comparison does not throw — but **absence is reported,
  not silently skipped**. "No bundle at all" and "a bundle exists whose provenance is
  missing" are different states, and the second is reachable on a fully built tree:
  every tree built by today's `bundle-cli.mjs`, and any build interrupted between
  esbuild and the metafile write. Silently skipping there would mute requirement 3's
  comparison on exactly the founding incident, so it reports
  `bundle-provenance-missing` **and sets this comparison's coverage to `not-run`,
  reason `.bundle-meta/metafile.json absent`** — or, for a marker that EXISTS and does not
  `JSON.parse`, the same advisory kind AND the same coverage entry with the second mandated
  reason **`.bundle-meta/metafile.json unreadable`**, since `absent` would be false (round
  10, C10-3). ⚠️ **BOTH SPELLINGS ARE READ OFF THE MARKER PATH AND NEVER OFF
  `readMetafile`'s RETURN, and they govern the COVERAGE entry exactly as they govern the
  FINDING (round 22, `C22-1`).** That return is `undefined` for absent AND for unparseable
  (§2), so a `checkBundleFreshness` resolving the coverage reason from its `metafile`
  argument can only ever spell `absent` — printing, on a tree where the marker sits on disk,
  the advisory's `unreadable` line beside a coverage entry calling that same file absent:
  one state in two spellings on one page, which is the wrong diagnosis §4's verbatim reason
  rule forbids and `OP18-5`'s own diagnosis at the sibling surface. §6's false-negative
  battery pins the pair on the truncated-marker fixture; for twelve rounds it pinned the
  finding alone. `readMetafile` returns `undefined` in both cases and never throws: on that tree
  every artifact is complete, clause 1 passes, and clause 2 is STILL EVALUATED — on the marker
  FILE's own mtime, never on this function's return (round 14, C14-1) — so the unit is
  `compared` only while that mtime dominates, and an unparseable marker OLDER than the
  newest §3-filtered output is `unbuilt` with the advisory suppressed by this section's own
  `unbuilt` rule, not `compared` with an advisory; and
  a throw reaches §4's internal-error row — `ERROR`, exit **2** from
  `check:all -- --strict` at member 0, halting the other 14 — while the only deletion this
  design ever prints, `rm -rf packages/cli/dist`, does NOT clear it, because `.bundle-meta/`
  is a SIBLING of `dist` (§3.3's opening sentence, C8-2's own fact) — after round 10 the only non-`full`
  status this design produces anywhere, the unreachable `reduced` having been deleted
  (§2, §3.2's Rule), so the "two states must not share one word" caution is moot. ⚠️ **DISJOINTNESS RULE — NO precedence
  rule, because the two kinds are DISJOINT (round 9, C9-3/O9-2 and CF9-2, replacing
  round 8's; NAMED in round 13, CF13-4, so that a citation to it resolves under the
  preamble's pass 3 rather than resolving to the one word this rule exists to deny):** the marker is not a member of §3.3's artifact set, so a `dist` with a
  gap is `unbuilt` whatever the marker says, and a `dist` with no gaps and no marker is
  `bundle-provenance-missing` on a unit that stays `compared` — a **finding**, never a
  skip, so a marker-less tree is never a bare PASS and requirement 3 is not muted while
  the marker is missing. Round 8 tied both kinds to one fact, which made `unbuilt` win
  in every reachable case and left this kind **unable to fire at all** — a kind
  hardcoded never to fire, the defect class this change set exists for.
  ⚠️ **And it fires unconditionally IN TIME.** Round 8 excepted "the first build after
  adoption", but **the check holds no state**, so that is not a predicate it can
  evaluate. Measured 2026-08-20: `ls -d packages/cli/.bundle-meta` → no such file, while
  `dist` holds all three entry outputs, `index.d.ts`, the six `PLUGIN_ASSET_ENTRIES` and
  five chunks — so this tree is ALREADY in that state, permanently indistinguishable
  from a build interrupted between `bundle-cli.mjs:178` and `:181`. The one surviving
  condition is structural, not temporal, and it is a condition on the UNIT'S STATE rather
  than on one directory's existence (round 17, C17-1): **no `bundle-provenance-missing` is
  emitted on a `packages/cli` that is not `compared`** — an absent `dist`, and equally a
  `dist` that is PRESENT and yields no qualifying compiler output, which `C15-4` left
  `skipped` and left reaching this comparison. That is the gate §4's per-kind line list
  ("it fires only on a unit that is otherwise `compared`") and §6's false-negative battery
  already state, and the one this bullet did not. So a fresh clone (`npm ci`, no build)
  stays silent — CI reaches this check only through `pretest` on `ci.yml:129`, after
  `ci.yml:86`'s build (§5's **THE CHECK HAS ONE CI OCCURRENCE**, round 25, `CF25-3`) — and
  so does the outputless tree — where a bullet
  keyed on directory existence would fire requirement 3's advisory on a `packages/cli`
  holding zero build output, and, a finding having fired, would drag §4's ordered REMEDY
  PLAN onto that page with it, its `nothing was checked` carve-out requiring "AND no
  finding fired" — ⚠️ **the conjunct this bullet leans on, and one no element could
  discriminate until round 35 (`OP35-1`): §6 row **36** is its EXCLUDING element, a tree
  where nothing is compared and every comparison is suppressed yet findings fire and the
  plan must PRINT, so a body reading the carve-out with two conjuncts fails it.** §6 row **46**'s marker-absent twin asserts it at the state that bullet's
  own dist-ABSENT fixture cannot reach. **The coverage entry is
  set in every not-run case**, including the ones the skip rule silences — silencing a
  finding must never silence the report that requirement 3's comparison did not run, or
  the operator reads `skipped`, runs the remedy, and never learns the bundle was never
  compared (standing rule 8). And no cli-scoped comparison runs on an `unbuilt`
  `packages/cli` (§3.3, §3.5), so one fact never prints two findings — **and all four then
  report coverage `not-run` with the reason `packages/cli unbuilt`** (round 10, C10-1).
  That is the one not-run case in which no artifact is absent, so no absence string can
  spell it, and suppression is a not-run case like every other: this bullet's rule governs
  it, and exempting it is exactly the silence the bullet exists to forbid. The suppression
  is evaluated BEFORE any comparison inspects its own comparand, so on a tree that is both
  `unbuilt` and missing a side, `packages/cli unbuilt` is the reason and the absence string
  is not — one spelling per state, as §4's verbatim rule requires. ⚠️ **That second clause was
  asserted by NOTHING until round 19 (`C19-1`).** Row 26's own fixture has nothing absent, so
  rank 1 was pinned only where ranks 2-5 are all unreachable — verbatim the argument this
  section already makes about row 16 for rank 2 ("evidence for the spelling and never for the
  ordering") and did not make about row 26 — and an implementation resolving each reason from
  its own comparand, falling back here only when nothing is absent, passed every element in
  §6, printing `.bundle-meta/metafile.json absent` at `check:all` member 0 on EVERY `unbuilt`
  `packages/cli` today, `.bundle-meta/` being absent on every tree until the first
  post-adoption build. Row 26's round-19 twin makes ranks 1, 3 and 4 live at once. §6 row 26's end state
  suppresses four comparisons: requirement 3's, and `stale-declarations`, whose comparand
  touches no `packages/cli/dist` artifact at all and whose remedy costs ~5 minutes; the wipe-step
  clear plus the after-the-plugin-copy write mean a marker that IS present always names
  the bundle whose chunks are on disk, so `inlinedUnitsFromMetafile` is never fed a
  metafile for a bundle the tree no longer holds (C9-1, answered by the write ordering
  rather than by a marker-mtime rule). §3.3 owns the state rule; §6 row 10 exercises it;
- it is **excluded from the output set** for the same reason `.tsbuildinfo` is — it is
  bundler bookkeeping, not compiler output, and including it would let the bundle
  comparison clear itself.

This is the third round in a row where a fix in this design carried the defect it was
fixing. It is recorded rather than smoothed over because the pattern is the finding:
**every one was caught by running the fix instead of reasoning about it**, and the two
caught before a review round were caught by pre-checking rather than by a reviewer.

**Why `dist`, not `src`, is the inlined side:** every workspace package resolves to
its build output (`exports["."].default === "./dist/index.js"`; `main` is
`./dist/index.js` for all 18) and `bundle-cli.mjs:129` sets `bundle: true`.

⚠️ **The three entry outputs are NOT an esbuild-only oracle, which was a round-1
finding and a false NEGATIVE in the one comparison requirement 3 exists for.**
`packages/cli/src/{bin.ts,index.ts,bin/supervisord.ts}` all exist, and with
`rootDir: ./src` / `outDir: ./dist` a plain `tsc -b` writes exactly those same three
paths — the repo documents this itself at `bundle-cli.mjs:108-112`. So
`npm run typecheck` alone refreshes them, `bundleAt` resets, and the check reports
clean although `bundle:cli` never ran.

**Rule:** let `bundleAt` be the mtime of an artifact **only esbuild produces** — a
hashed chunk under `packages/cli/dist/` (esbuild emits chunk files that `tsc` has no
notion of), taking the **minimum** across the whole matching set — all five hashed chunks
on this tree, not the first one found — so a partial refresh cannot mask a stale sibling. **§6 row 5's amended fixture is the minimum's non-vacuity row, from round 18 (`C18-7`)**: no element gave a fixture two chunks at different mtimes for seventeen rounds, so `newestUnder(packages/cli/dist, isChunk)` — a MAXIMUM, and the only helper §2 declares — passed every battery row, the false-negative battery, the wiring assertions and the live smoke test, at the one comparison requirement 3 exists for.
⚠️ **`bundleAt` CAN BE `undefined`, AND THIS SENTENCE SAID IT COULD NOT (round 17, C17-1).**
It read "always defined here: this comparison runs only on a `compared` `packages/cli`, and
§3.3's clause 1 requires a chunk to exist". Both halves were true under the PRE-round-15
gating and are false since `C15-4`: `unitState` reaching `skipped` suppresses
`checkUnitFreshness` and `checkOrphans` and NOTHING ELSE, so a `skipped` `packages/cli`
still reaches this comparison; and §3.3's three-clause conjunction is "one further step
INSIDE the `compared` branch — never before it", which a present-but-outputless
`packages/cli/dist` never enters. Reachable with two published root scripts and no manual
`rm`: a fresh clone, then `npm run typecheck` (`tsc -b`, re-derived 2026-08-21 from
`package.json`), then `npm run build:clean` (`tsc -b --clean`). Re-derived read-only at
`badf45a` with `./node_modules/.bin/tsc -b packages/cli --clean --dry` under the pinned
typescript 6.0.3, the command lists **4,781** file paths, **five** of them under
`packages/cli/` — `dist/bin.js`, `dist/index.js`, `dist/index.d.ts`,
`dist/bin/supervisord.js` and `dist/.tsbuildinfo` — and **zero** directory entries, so
`--clean` deletes the incremental state too and leaves `dist` standing and EMPTY, which
§3.3's reason table calls `skipped` and not `unbuilt`. **Rule: `bundleAt === undefined` ⇒
`bundle-freshness` reports coverage `not-run`, reason
`packages/cli/dist holds no esbuild chunk`, and NO finding of any kind** — never a
comparison against `undefined`, and never a `reduced` status, which round 10 deleted and
which nothing here reinstates. Dropping the guard and rendering `full` is one of the two
readings this forbids — a comparison that compared nothing, which §4's recipe step 5
licenses an operator to read as "requirement 3's comparison covered its inputs" — and
reading `.mtimeMs` off `undefined` is the other, reaching §4's internal-error row at exit
**2** from `check:all` member 0, halting the other 14: verbatim `C10-3`'s and `C15-2`'s
framing, at the one comparison requirement 3 exists for. §6 row **46** is the non-vacuity
row, and §3.3's **SKIP RULE — ALL FOUR cli-SCOPED COMPARISONS** carries the general form.

⚠️ **THERE IS NO FALLBACK TO THE THREE ENTRY OUTPUTS — round 10 deleted the one this rule
carried, as UNREACHABLE.** It read: fall back to `min(mtime)` over the three entry outputs
"only when no such artifact exists", setting this comparison's coverage to `reduced`,
reason `no esbuild chunk`, printed by §4. Measured against this design's own rules, that
state cannot occur. Round 9 made one file matching the chunk pattern a REQUIRED member of
§3.3's clause-1 artifact set — `cliArtifactGaps` (§2) lists "an esbuild-named chunk" first
— so a chunkless `packages/cli` is `unbuilt` with `missing: chunk-*.js`, and §3.2 above
states that **no cli-scoped comparison runs on an `unbuilt` `packages/cli`** — ⚠️ **true
only where clause 1 is REACHED, which is inside §3.3's `compared` branch (round 17,
C17-1).** A `packages/cli/dist` that is present and outputless is `skipped` by §3.3's
reason table before clause 1 evaluates, and since round 15's `C15-4` a `skipped` unit no
longer suppresses this comparison — so the chunkless state IS reachable here, and this
paragraph's deletion argument covered only half of it. The conclusion is unchanged and
`reduced` stays deleted: that state answers coverage `not-run`, reason
`packages/cli/dist holds no esbuild chunk` (the Rule below), a status this two-valued enum
already has. Both
predicates read the same `/-[A-Z0-9]{8}\.js$/` for PRESENCE, so they can never disagree
that a chunkless tree is `unbuilt` — round 12 (C11-6) made clause 1's chunk MEMBERSHIP
metafile-derived when a marker exists, which only NARROWS clause 1, and §6's false-negative
battery's marker-absent gap bullet is what makes that agreement falsifiable on the
no-metafile branch (round 32, `C32-1`) — and
`checkBundleFreshness` never reaches the fallback to report anything. §6 row 16 mandated a
rendered line — `bundle freshness reduced (no esbuild chunk)`, still exit 0 — that no
correct implementation can print; what actually prints on that tree is §4's own worked
example, `unbuilt packages/cli — bundle:cli did not finish; missing: chunk-*.js`, exit
**1** under `--strict`.

⚠️ **Standing rule 8, at the site of the fix: do NOT close this the other way.** Dropping
the chunk from clause 1 so the fallback becomes reachable lowers the newest output mtime
until clause 2 passes, `bundleAt` falls back to round 4's tsc-writable proxy, and
`dist/bin.js` opens with `import … from "./chunk-FRJGAF5Y.js"` (verified 2026-08-20) — a
CLI that cannot start, reported PASS for the eighth time. That is verbatim round 6's
`CR6-3`, and §3.3's four-proxy table forbids it. So: **no chunk ⇒ `unbuilt`, never a
degraded comparison.** Report `stale-bundle` when any of

- newest member of §3's FILTERED output set across the 16 units — the same
  `isCompilerOutput` predicate the definedness paragraph below already names for these very
  members, evaluated against the INLINED unit and never against `packages/cli`, so §3's
  `.tsbuildinfo` and `dist/eo-*-fixture-*/**` minus-bullets are the two that reach this
  member and the two `packages/cli`-only ones cannot, `cli` being dropped from the inlined
  set (round 23, `C23-2`). ⚠️ **A CITATION, not a fourth member — this list stays at THREE,
  the count §3.3's cli reason table states in terms.** This was the one comparand member
  here with no pointer to the rule defining its set while its sibling one line down carried
  `(§3 rules 1-2)`, and §3's own "also the comparand for" sentence named the marker clause
  as its only further consumer: `CF6-1`'s shape — one exclusion landing in one section — at
  the filter's third reader, and `C16-1`'s site list stopping one walked side short
  (standing rule 9). §6 row 5's round-23 TWIN is the element,
- newest compiler input under `packages/cli/src` (§3 rules 1-2),
- `scripts/bundle-cli.mjs`

is newer than `bundleAt`. ⚠️ **STRICTLY newer, and that THRESHOLD is asserted only
since round 36 (`C36-1`) by §6 row 5's EQUAL-MTIME TWIN.** §7's `Second-granularity mtimes`
row states ONE mitigation for this comparison and four others, and named an element for one
of them. Re-derived read-only 2026-08-22 at `d0b706e` over §6's whole fixture column
(`awk '/^## 6\./,/^## 7\./' <file> | grep -oin 'equal'`, then classified by hand): the ONLY
mtime-EQUALITY clauses in that section are row 19's twin, which is §3.3's clause-2 mirror,
and the false-negative bullet's `input mtime exactly equal to output mtime`, which is §3's
input-versus-output Verdict — every fixture reaching THIS comparison writes its two sides
strictly apart. So a `checkBundleFreshness` spelling it `>=` passed every element in this
design and reported `stale-bundle packages/cli` at `check:all` member 0, exit **1** under
`--strict`, with a full rebuild as the printed remedy, on a build whose newest input and
whose OLDEST chunk land in ONE SECOND — the state §8(e) and §7's own row name, and which
`./mtime-propagation-probe.mjs:74-75` does NOT cover, its `ordering:` rows being
`first write` and `second write, >1s later`.

⚠️ **AN INLINED UNIT WITH NO QUALIFYING OUTPUT MAKES THIS COMPARISON `not-run`, and this
section stated definedness for the OUTPUT side only (round 15, C15-2).** ⚠️ **And that
definedness was itself false — the Rule above now carries `bundleAt === undefined` as its
own `not-run` case (round 17, C17-1), so this paragraph no longer rests on it, and the two
causes are ranked against each other in the precedence line below.** The SIXTEEN members of
the first bullet are unguarded,
and `newestUnder` returns `undefined` for a `dist` that is absent or holds nothing
`isCompilerOutput` accepts (§2). The state is reached from §4's own recipe: step 1 deletes
`<unit>/dist` for every unit reported `stale-unit`, and its FOURTH carve-out exempts
`packages/cli` when its only kinds are `stale-unit` and `stale-bundle` — so `packages/cli`
stays complete with a fresh marker while an inlined unit's `dist` is gone, and re-running the
check (or `npm test`, `pretest` being the primary trigger) reaches this comparison with
`unitState("packages/cli").state === "compared"`. `npx tsc -b <unit> --clean` reaches it a
second way, directory present and empty. The two defensible readings are two exit codes
apart — verbatim round 10's `C10-3` framing — so the rule is written rather than left to the
implementer: **any inlined unit whose `dist` yields no qualifying compiler output makes
`bundle-freshness` report coverage `not-run`, never a comparison over the remaining
fifteen**, with the reason naming those units' `dist` paths comma-joined **in `buildUnits`'
own order — the order `enumerateRootReferences` returns, sorted (§3's Units step; the
`.sort()` is `scripts/repo-census.mjs:293`, re-derived read-only at `e99b066` on a clean
tree as 19 path-form dirs from `e2e/report` to `packages/testkit`), never
`inlinedUnitsFromMetafile`'s key order, stated because a join order left implicit is a
second spelling of one state under the precedence line below, and PINNED since round 26
(`C26-2`) by §6 row 40's round-18 TWIN, whose marker names its two offenders in the
OPPOSITE order — without which the mandated order and the forbidden one COINCIDE on the
one fixture in this design that reaches N=2, leaving this sentence's ORDER half mandated
and unexercised while its RENDERING half is pinned** and suffixed
` absent` — `packages/contracts/dist absent`, or
`packages/contracts/dist, packages/gates/dist absent` (§4's verbatim reason list). Dropping
the `undefined` member and rendering `full` is the reading this forbids: §4's recipe step 5
licenses an operator to read `full` as "requirement 3's comparison covered its inputs", so a
comparison over 15 of 16 reported `full` is the founding silence at the one comparison
requirement 3 exists for; reading `.mtimeMs` off `undefined` is the other, and reaches §4's
internal-error row at exit **2** from `check:all` member 0. ⚠️ **REASON PRECEDENCE, written
because two of these causes co-occur on the commonest tree there is:** `packages/cli unbuilt`
first (this section's suppression, evaluated before any comparison inspects its comparand),
then `packages/cli/dist absent` (§3.3's skip rule — on a fresh clone EVERY unit is bare, so
without this line that tree has two admissible
spellings), then `packages/cli/dist holds no esbuild chunk` (the Rule above; round 17,
C17-1 — on a `dist` that is PRESENT and outputless the marker is reachable both
present-and-parseable and absent, so without this rank that tree would take a marker
spelling and requirement 3's own missing comparand would go unnamed), then
`.bundle-meta/metafile.json absent`/`unreadable`, then this one. One state
keeps one spelling, as §4's verbatim rule requires.

⚠️ **The REASON and its RANK are different obligations, and each was short one element (round 18, `CF18-3` and `C18-2`).** §6 row **40** pins rank 5's REASON — its ORIGINAL half at ONE offender and its round-18 TWIN at TWO, because at N=1 this comma-join, a per-path ` absent` suffix and a first-offender-only rendering are BYTE-IDENTICAL, and §6's own `C15-2` sibling-fixture rule forbids any other row from reaching a second offender, so for three rounds three different renderings satisfied the one element this reason had. The RANK is pinned pairwise and only where a row makes two causes CO-OCCUR, which is what this line was written for: row **46**'s two twins give rank 3 over rank 4 — a `packages/cli/dist` PRESENT and outputless with `.bundle-meta/` present on one twin and removed on the other, asserted IDENTICAL — and row **39**'s NEGATIVE twin gives rank 2 over rank 4 since round 18, on the very tree this line's own rationale names, where `packages/cli/dist` and `.bundle-meta/metafile.json` are BOTH absent and an implementation reading the marker first renders `.bundle-meta/metafile.json absent` at `check:all` member 0 on every fresh clone (never in CI — §5's **THE CHECK HAS ONE CI OCCURRENCE**, round 25, `CF25-3`). Row **16** pins rank 2's reason against no competitor at all — its mutation is `rm -rf packages/cli/dist` while `.bundle-meta/` survives beside it, so ranks 3, 4 and 5 are all unreachable there — and is therefore evidence for the spelling and never for the ordering. Row **26**'s round-19 twin gives rank **1** over ranks 3 and 4 (round 19, `C19-1`) — row 19's end state with `.bundle-meta/`, every chunk-pattern file, `dist/plugin/`, `dist/index.d.ts` and `.dts-cache/index.d.ts` all removed, where each of the four entries has a competing absence spelling live and all four must STILL read `packages/cli unbuilt` — and ranks 1 and 2 cannot CO-OCCUR at all, an absent `packages/cli/dist` being `skipped` and never `unbuilt` (§3.3's unit-state table), so that pair needs no row. ⚠️ **Rank 5 against ranks 1-4 is still pinned by nothing, and is recorded as a residual rather than claimed:** it needs a marker that PARSES (or the inlined set is not derivable at all) beside an absent `packages/cli/dist` for ranks 2-4, or beside an `unbuilt` `packages/cli` for rank 1, and no fixture in §6 builds either. ⚠️ **Until round 19 this sentence read `ranks 2-4` and rank 1 appeared in neither the pinned list nor the residual (`C19-1`)** — the five-rank order was pinned for two pairs, conceded for one, and silent for the rank it puts FIRST, which is the rank live on every `unbuilt` `packages/cli` this design has.

Including the build program here is precise rather than noisy:
of the three artifacts `bundle-cli.mjs` produces, this is the only one compared against
its PRODUCER — `dist/plugin/**` (§3.4) and `dist/index.d.ts` (§3.5) are compared against
their own sources, so editing the producer fires `stale-bundle` once, not three times. **§6 row 41 is the
non-vacuity row, added in round 15 (C15-3).** Until then no assertion anywhere touched this
member: an implementation whose input side was the 16 dists plus `packages/cli/src` passed
every battery row, the false-negative battery, the wiring assertions and the live smoke test,
and left an esbuild-options or `EXTERNAL_DEPENDENCIES` edit shipping a stale bundle with
requirement 3's comparison silent.

⚠️ **The SOURCE side, by contrast, DELIBERATELY DUPLICATES `checkUnitFreshness`, and no
section said so for fourteen rounds (round 14, C14-5).** §3's skip-rule table runs
`checkUnitFreshness` on every `compared` unit with no `packages/cli` exemption — only §3.1's
orphan check exempts it — so `touch packages/cli/src/index.ts` reports BOTH
`stale-unit packages/cli` and `stale-bundle packages/cli` from one fact, with two different
remedy-table rows. §8(b) states the opposite principle at the neighbouring site and reasons
about it explicitly; the same reasoning was never applied here. ⚠️ **Standing rule 8: the
overlap is KEPT, not removed.** The two comparisons differ on the OUTPUT side — `bundleAt`
is the **minimum** over the chunk set — asserted by §6 row 5's two-chunk fixture only since round 18 (`C18-7`), row 38 NAMING the word while its own mutation makes min and max agree — while `checkUnitFreshness` takes the **maximum** over
the whole filtered output set — so this comparison is strictly tighter, and dropping
`packages/cli/src` from the list above would trade one duplicate for a FALSE NEGATIVE on a
partial chunk refresh. What changes is that the duplicate is stated where it is created:
§7 carries the row, §4's step 1 carries the carve-out it forces, and §6 row 38 is the row.

### 3.3 Declaration cache — requirement 8

`packages/cli/.dts-cache/index.d.ts` is gitignored (`.gitignore:47`), is neither
`src` nor `dist`, and survives the `rm -rf packages/cli/dist` a reader performs when
the check fires.

⚠️ **SKIP RULE, RE-KEYED IN ROUND 3 — directory existence is the wrong predicate.**
`tsc -b --clean` leaves `dist` **existing and empty**, measured, and `build:clean` is a
published root script. A rule keyed on absence does not fire there, so the comparison
runs with nothing to compare and its verdict is undefined. Reporting `clean` would print
PASS on a tree with **zero build output** — the worst available answer for the incident
this check is named for.

**The rule is keyed on qualifying COMPILER OUTPUTS, not on the directory**, and the
empty state gets its own name:

| unit state                                     | verdict                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| no `dist/` at all                              | `skipped`, and the `reason` §4 prints VERBATIM is the LITERAL `no dist/`. ⚠️ **Written here in round 15 (OP15-2); until then NO section of §3 spelled it** — `grep -n 'no dist/'` returned §2, §4 and §9 only, while §2 and §3 both name §3.3 as `unitState`'s authority and §6 row 30 forbids the reporter inventing one. So the reason with the widest printed reach in the design — `packages/cli` after §4 recipe step 1, and every `dist`-less unit in the findings-path summary — was the one an implementer had to guess. ⚠️ **AND THAT REACH IS NARROWER THAN THIS CELL CLAIMED (round 21, `OP21-2`):** on a fresh clone all 19 units carry this reason and NOT ONE of them prints it, because `OP15-5` suppresses the clause list on the `nothing was checked` `PASS` line — the same round that wrote this literal wrote that carve-out, and this sentence went on naming the suppressed tree as its most frequent printing site. The argument is restated rather than withdrawn: the reason is still printed, and the `PASS` line after recipe step 1 is where. ⚠️ **That printing site had no element either:** §6 row 30's fourth fixture asserts this literal on the FINDINGS path, and §6 row 39's MAIN half now asserts it inside the `PASS` line by equality (`OP21-2`). The reason table below owns the other two `skipped` strings; this row owns this one, and adding a row there instead would duplicate this verdict and renumber a table §6 rows 13-14 and §7 cite (round 13, OP13-2) |
| `dist/` exists, no qualifying compiler outputs | **decided by the reason table below, never by this row** — `unbuilt` only when something proves a build ran (`.tsbuildinfo`); `skipped` when the directory is empty or holds only `eo-*-fixture-*` scratch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `dist/` exists with outputs                    | `compared`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

⚠️ **AND FOR `packages/cli`, PRESENCE OF OUTPUTS IS NOT ENOUGH — round 4.** `tsc -b --clean` removes only tsc's own outputs; the **five hashed esbuild chunks survive**, measured with `tsc -b packages/cli --clean --dry`. So after `npm run build:clean` the unit holds fresh qualifying outputs, is not `unbuilt`, compares **clean**, and `bundleAt` reads a chunk that is still there — while `packages/cli/dist/bin.js`, the published `bin.crabgic` entry point, **does not exist**. The check would print PASS on a tree with no CLI.

⚠️ **AND THE ENTRY-OUTPUT RULE IS ITSELF A tsc-WRITABLE PROXY — the FOURTH appearance
of this failure (round 6).** The **`packages/cli/` subset** of
`tsc -b packages/cli --clean --dry` is exactly `dist/bin.js`, `dist/index.js`,
`dist/index.d.ts`, `dist/bin/supervisord.js` and `.tsbuildinfo` (round 12: the whole
command lists 4,781 across 17 packages), so `tsc -b` alone writes every entry output the
rule checks. Interrupt
`npm run build` during the **~5 minute** `bundle:types` step — Ctrl-C, CI timeout, 3am —
and the tree has all three entries present, no chunks, no `dist/plugin`, and a stale
`.bundle-meta/` that `rm -rf dist` did not touch. The check reports **PASS, exit 0**, on
a tree with no `crabgic` binary.

⚠️ **AND CLEARING THE MARKER IN THE WIPE STEP DOES NOT CLOSE THAT — the SEVENTH appearance
(round 9).** Round 8 answered this with a per-build clear inside `bundle-cli.mjs`. But
`build` is `tsc -b && npm run bundle:types && npm run bundle:cli` (`package.json:15`), so an
interrupt during the ~5-minute `bundle:types` step means **`bundle-cli.mjs` never executes
and the clear never fires**. Reached from this design's own printed remedy:
`rm -rf packages/cli/dist`, then `npm run build`, then Ctrl-C. Measured — the
**`packages/cli/` subset** of `tsc -b packages/cli --clean --dry` is `dist/bin.js`,
`dist/index.js`, `dist/index.d.ts`, `dist/bin/supervisord.js` and `.tsbuildinfo` (round
12: the whole command lists 4,781 across 17 packages), so the tree is left with
three entries present, no chunks, no `dist/plugin`, and the PREVIOUS build's marker beside
`dist`. Marker present, entries present, every comparison passing or skipping: **PASS,
exit 0.** Round 8's fix could not fire in the failure class it was written for, because the
tool that carries the fix is the tool that did not run.

**So the rule is a CONJUNCTION OF THREE CLAUSES, none of which replaces another —
`packages/cli` is `unbuilt` unless all three hold:**

1. **Completeness: every artifact `bundle-cli.mjs` writes EXISTS** — the esbuild chunk
   SET (rule below, round 12, C11-6), the three entry outputs (`:121`), `dist/index.d.ts`
   (`:153`), and all six `PLUGIN_ASSET_ENTRIES` under `dist/plugin` (`:178`), **tested for the EXISTENCE
   of those six PATHS and never walked for their CONTENTS — residual §8(p) (round 15,
   C15-1)** — read from
   the `export`ed const §1 adds, **never a literal duplicated into `walk.mjs`** (round 10,
   C10-2): a copy leaves the check at six after a seventh member is added to
   `bundle-cli.mjs:89`, that asset then ships in `dist/plugin` with its absence not a gap
   and its edits never firing `stale-plugin-assets`, and §6's fixtures are synthetic trees
   so no battery row can observe the drift. ⚠️ **No battery row can, and since round 15
   (CF15-4) §6's wiring block does:** it reads `walk.mjs`'s `bundle-cli.mjs` import
   statement for the name — ANCHORED to the line start only since round 29 (`CF29-2`);
   until then the regex matched the whole semicolon-free span, so a `walk.mjs` naming this
   const in a COMMENT above an import of some other binding satisfied the read and the
   duplicated literal this clause forbids shipped unseen — with the duplicated literal as
   the mutant that must flip the predicate — the shape round 14 built for `units.mjs`, applied here, because until then
   this clause was a mandate with no assertion anywhere and every green suite was
   compatible with the literal it forbids. §8(k) states what a source read still does not
   establish. `packages/plugin/workflows/` already exists in
   the source tree today and is not a member (re-derived 2026-08-20), so that drift is one
   commit away. That set is
   the **§3.3 artifact set**; it is read off the tree, needs no history, and §4 prints the
   gaps. ⚠️ **The chunk member is a SET, not "at least one" — round 12, C11-6.** Measured
   import graph on this tree 2026-08-20: `dist/bin.js` imports `chunk-FRJGAF5Y.js`, which
   imports `chunk-DVV3SNQ3.js`, `chunk-I6JBP7DT.js` and `chunk-UF6GI6PE.js`. Delete any
   three of those four on an otherwise complete tree WITH a marker and the check reports
   **PASS, exit 0 even under `--strict`** — clause 1 passes because one file still matches
   the pattern, clause 3 passes because the three entries survive, and clause 2 passes
   because deleting an output LOWERS the newest output mtime, which is this design's own
   argument for retaining clause 3 — on a `packages/cli` whose `bin.crabgic` dies with
   `ERR_MODULE_NOT_FOUND`. **So when the marker exists, chunk membership is taken from the
   chunk-pattern-matching entries of `metafile.outputs`**: `result.metafile` already names
   every file esbuild emitted, it is the artifact §1 writes, and §3.2 already reads it — so
   clause 1 checks the exact chunk set the bundle produced, and `cliArtifactGaps` takes the
   metafile as an argument (§2). **§6 row 33 is the non-vacuity row**, in the shape §4
   already uses for rows 32 and 34 — added round 13 (CF13-5), because §6's own registry
   asserted this citation existed while `grep -n "row 33"` returned nothing anywhere in
   the file. ⚠️ **This is a READ, never a MEMBERSHIP, and the
   distinction is what keeps CF9-2 closed:** a marker-less tree is still `compared` plus
   the advisory `bundle-provenance-missing`, never `unbuilt` for want of a marker, and the
   rule can only ever NARROW clause 1. When there is no PARSED metafile — the marker absent OR
   present and unparseable, the two states `readMetafile` collapses (round 14, C14-1; this
   sentence said "ABSENT" alone, which is the wrong key for the one clause that legitimately
   reads the parse) — the clause keeps "at
   least one file matching the pattern", and **the completeness of the chunk set is then
   unverifiable** — stated in §8(i) beside the other marker-absent costs, never hidden
   here. ⚠️ **THAT FALLBACK IS THE BRANCH EVERY TREE IN THIS REPOSITORY TAKES until its
   first post-adoption build, and its chunk member was exercised by NOTHING until round 32
   (`C32-1`) — standing rule 9, beside the marker-PRESENT reading this same sentence
   pins.** Every element that reaches clause 1 with no marker holds a chunk-pattern file
   PRESENT, so a `cliArtifactGaps` resolving that member as the metafile's outputs when
   there is a metafile and as NOTHING when there is not — an EMPTY requirement, which
   nothing can be missing from — passed every one of them while reporting `compared` plus
   one advisory at exit 0 even under `--strict` on a `packages/cli/dist` holding no chunk
   at all and a `dist/bin.js` whose first statement imports one. §6's false-negative
   battery's marker-absent gap bullet is the element, and it binds the unparseable
   sub-state with it: both sub-states reach this branch through the one value
   `readMetafile` returns for each, `undefined` (§2), so no body takes the fallback on one
   and not on the other. ⚠️ **Standing rule 8, at the site of the fix: do NOT close it by hardcoding
   five.** The chunk count is a function of the import graph and moves with any
   re-chunking under `splitting: true`, so a literal would be a rule keyed on today's tree
   — §3.3's own proxy lesson at a new site. Reachability is thin and said so: no repo
   script produces this state, and the realistic paths are an interrupted esbuild write
   phase or a manual or partial `rm`. ⚠️ **The metafile is deliberately NOT a member (round 9).** Keying `unbuilt` on its
   presence made every correctly built tree report `unbuilt` on day one — measured
   2026-08-20, this tree holds all three entry outputs, `dist/index.d.ts`, the six plugin
   entries and five chunks, and no `.bundle-meta/` at all — and it left
   `bundle-provenance-missing` with no reachable state. An absent marker on a complete
   `dist` is `bundle-provenance-missing`, advisory, unconditionally (§3.2), and never
   `unbuilt`; the two kinds are **disjoint by construction** and need no precedence rule.
2. **Freshness — evaluated ONLY when the marker FILE exists, which is a stated residual and
   not a bound this clause may hide (§8(i), round 10 C10-4); EXISTENCE AND MTIME ARE BOTH
   READ OFF THAT PATH, never off `readMetafile`'s return, which is `undefined`
   for an absent marker AND for an unparseable one (round 14, C14-1 — a clause keyed on
   that return skips a truncated marker entirely, §2's branch list then reads `compared`
   plus one advisory, and the check prints PASS, exit 0 EVEN UNDER `--strict` on a
   `packages/cli` this section's own stated-cost paragraph calls one that "cannot publish
   and cannot run its own `bin.crabgic`": the founding failure's TENTH appearance,
   introduced by C10-3's fix for its ninth): if
   `packages/cli/.bundle-meta/metafile.json` exists,
   `mtime(marker) >=` the newest mtime over `packages/cli`'s §3 output set** — the same
   `isCompilerOutput` filter, so `.tsbuildinfo`, `dist/plugin/**`, `dist/index.d.ts` and
   `dist/eo-*-fixture-*/**` are all excluded (§3 defines that comparand; §7 row 1 is why it
   is filtered and not a raw `dist/**` walk). `>=`, not `>`: equal is clean, matching §3's
   Verdict convention, and it is what makes the microsecond-apart `:178` → `:181` pair safe
   where the filesystem is coarse (§8(e)). ⚠️ **§6 row 19's round-19 twin is the element, and
   until round 19 this sentence was discharged by NOTHING (`C19-2`):** every specification of
   the marker's mtime in §6 reads `at or after` or `newest` — the complete-cli-build rule, the
   truncated-marker bullet, and rows 5, 16, 19, 26, 32, 33, 37, 38, 39, 40, 41, 42, 45 and 46 —
   and the battery's one equal-mtime element AT THAT ROUND was the false-negative bullet's
   `input mtime exactly equal to output mtime`, which is §3's INPUT-versus-OUTPUT Verdict and
   never reaches this mirror. ⚠️ **Read at round 19 and never as a live census (round 36,
   `C36-1`):** §6 now takes the boundary at every mtime comparison this design has — that
   bullet for §3's Verdict, this clause's own twin on row 19, and rows 5, 6 and 24's
   EQUAL-MTIME TWINS for §3.2's, §3.3's cache and §3.4's thresholds — so a count of equality
   elements written here in the present tense is one every later append re-invalidates,
   `C11-4`'s class. So a fixture author writing the marker strictly after the newest output, the
   natural choice since `:181` runs last, let a clause 2 spelled `>` pass every element in
   this design while reporting `unbuilt packages/cli` — `rm -rf packages/cli/dist` plus a
   ~5-minute rebuild, exit **1** under `--strict` at `check:all` member 0 — on a correctly
   COMPLETED build under any second-granularity filesystem. An absent marker is not a failure of this
   clause — that case is clause 1's ⚠️ above.
3. **Existence of the three entry outputs — round 4's clause, RETAINED and not replaced** —
   `dist/bin.js`, `dist/index.js`, `dist/bin/supervisord.js`. They are members of clause 1's
   set and are named again because clause 2 **cannot subsume them**: deleting an output
   LOWERS the newest output mtime, which makes the marker look MORE current, not less.

**The design still requires that write to be placed AFTER the plugin-copy loop** (beside
`bundle-cli.mjs:181`) and the clear to be placed BEFORE `build()`. Clause 2 is worth exactly
what those two placements are worth, so both are pinned by §6's mutant-proved source-order
assertion — the assertion that stood there until round 9 passed on a bundler with no write
at all, and whose CLEAR half was flipped by no mutant until round 27 (`CF27-1`) and is proved
NON-DELETABLE only on a source whose `mkdir` follows the plugin-copy loop (round 28, `CF28-1`)
— the PIN holds on every conforming placement, the PROOF on §1's prescribed one, and §8(k)
records the difference at the clause that licenses the other. `:181` runs after every byte `bundle-cli.mjs` writes under `dist`, which is what
makes clause 2 TRUE after every completed bundle and FALSE after anything that writes into
`dist` later — **including the failure classes where `bundle-cli.mjs` never runs at all and
the wipe-step clear therefore cannot fire.**

⚠️ **Standing rule 8, stated at the site of the fix.** Dropping the marker from the `unbuilt`
predicate re-opens round 7's `CR-2` unless clause 1 keys on the artifacts written AFTER
esbuild: the `:147` throw on a fresh clone leaves five chunks and three entries with no
`dist/index.d.ts` (`:153`) and no `dist/plugin/**` (`:178`), and the marker was the only
oracle that saw it. Both are present on every completed build, including every pre-adoption
one — verified 2026-08-20 on this tree — so keying on them costs no day-one false positive.
Clause 1 is also what catches the class round 8's clear could not: an interrupt during the
~5-minute `bundle:types` step leaves three entries with no chunk, no declarations and no
plugin assets.

**Stated cost, measured rather than assumed.** A bare `npm run typecheck` (`tsc -b`) that
actually re-emits then flags `packages/cli` as `unbuilt` through clause 2. That is a TRUE
positive, not a tax: `tsc -b` overwrites `dist/bin.js` and `dist/index.d.ts` with per-file
output that still imports `@crabgic/*` — `bundle-cli.mjs:105-112` states it, and `ci.yml`'s
`packaging` job comments on the same fact as the cause of defect
`25-install-smoke-depends-on-local-dist-state`. Verified on this tree: `dist/bin.js` is
esbuild's, importing `./chunk-FRJGAF5Y.js`. A tree in that state cannot publish and cannot
run its own `bin.crabgic`. A `tsc -b` that emits NOTHING (touched-but-unchanged source)
moves only `dist/.tsbuildinfo`, which clause 2's comparand excludes, so it stays clean
(§7 row 1).

⚠️ **An esbuild-only chunk was round 6's answer and it is not sufficient, because esbuild
is step 2 of 5 (round 7).** Verified order in `bundle-cli.mjs`:

| line         | step                                                                                          | absent if it never runs                                              |
| ------------ | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| `:113-119`   | wipe `dist`, keeping only `.tsbuildinfo` — **plus the `.bundle-meta` clear this design adds** | —                                                                    |
| `:121`       | `build()` — writes 5 chunks + 3 entries                                                       | ← a chunk oracle is satisfied HERE                                   |
| `:147-151`   | throw if `.dts-cache/index.d.ts` is absent                                                    | `dist/index.d.ts`                                                    |
| `:153`       | `copyFile` the declarations                                                                   | `dist/index.d.ts`                                                    |
| `:177-179`   | `cp` the six `PLUGIN_ASSET_ENTRIES`                                                           | `dist/plugin/**`                                                     |
| after `:179` | **write the marker (this design, §1)** — the LAST artifact                                    | `.bundle-meta/metafile.json` ← **the oracle is satisfied only HERE** |

A fresh clone has no `.dts-cache` (gitignored, `npm ci` does not create it), so
`npm run bundle:cli` throws at `:147` — leaving five chunks, three entries, **no
declarations and no plugin assets**. Every comparison then passes or skips, and the check
reports **PASS** on a `packages/cli` that would publish incomplete.

**Proving the whole sequence ran, not just its second step, is what the oracle must do** —
and round 9 split WHICH oracle does it. The marker answers provenance; the artifacts the
sequence's last two steps write (`dist/index.d.ts` at `:153`, `dist/plugin/**` at `:178`)
answer completion, and they answer it statelessly. That is the sixth form of this rule,
and the reason the previous five failed splits in two: four proved something _upstream_ of
the thing that matters, and the fifth failed at both ends of history — it proved completion
only for trees whose history the check can see, and its PRESENCE proved that A build once
finished, never that THIS tree came out of one, because the clear that would have
invalidated it lives inside the tool the failing path never runs. That second half is why
the marker is now read for FRESHNESS (clause 2) rather than for presence. ⚠️ No form is
called final in this record again.

**Pre-checked before the next round, per the rule that a fix must be run rather than
reasoned about.** The pattern matches all five live artifacts — `chunk-DVV3SNQ3.js`,
`chunk-FRJGAF5Y.js`, `chunk-I6JBP7DT.js`, `chunk-UF6GI6PE.js` and
`run-dispatcher-POLZZ2DH.js`, i.e. **both** naming families esbuild produces under
`splitting: true` — and matches **none** of the three tsc entry outputs.
⚠️ **THE SECOND FAMILY WAS EXERCISED BY NOTHING UNTIL ROUND 33 (`C33-1`), and this breadth
is what decides clause 1's required set:** every element in §6 deleted both families
together or neither, so an `isChunk` keyed on the `chunk-` PREFIX matched the same set as
the pattern on every one of them, and on a marker-PRESENT tree short one esbuild output it
reported `compared` at exit 0 even under `--strict` — `C11-6`'s own harm at the family
`C11-6` did not name. Re-derived read-only at `2ef2ce5`, the document round 33 READ, the
sha travelling with the figure because this very apply moves it:
`awk '/^## 6\./,/^## 7\./' <file> | grep -c 'A-Z0-9'` returned **0**, and the same slice
piped to `grep -on 'run-dispatcher'` returned **2** hits, BOTH on ONE line, §6 row 25's
mutation cell. §6 row 25 is the element — the second-family file deleted ALONE on a fixture
whose marker's `outputs` name all five — and the state it needs is the one clause 1's own
reachability note above already accepts.

⚠️ **Its one bound, stated rather than left implicit.** `tsc` names outputs after their
sources, so a source called `x-ABCD1234.ts` WOULD emit a matching `.js` and satisfy the
rule without esbuild running. Measured: **zero** tracked `.ts` files match
`-[A-Z0-9]{8}\.ts$`, and no `dist` outside `packages/cli` holds a matching `.js`. The
collision is possible in principle and absent in fact; the honest form of the rule is
"an artifact esbuild produces and no source in this repository would name". The entry-output condition
is additional, never the whole one.

The five superseded proxies, in order, are the record of how this was learned: directory
existence → output presence → entry-output presence → an esbuild-only chunk → the marker's
presence → **the artifacts the bundle's last two steps leave in `dist`, plus the marker's
FRESHNESS against the outputs it must dominate**.
Each of the first three is writable by `tsc -b` alone; the fourth proves only that esbuild
ran, which is step 2 of 5; and the fifth failed twice over — it proved all five steps and
nothing weaker, so a tree built before this check existed read as `unbuilt` (round 9,
CF9-2), and it survived a build that never happened, because the clear that would have
invalidated it runs only when `bundle-cli.mjs` runs (round 9, C9-1). The sixth form is a
conjunction of the two: the artifacts answer completeness statelessly, and the marker —
when there is one — answers freshness.

**Clause 3 above — the three entry outputs must EXIST — is round 4's rule, RETAINED and not replaced** — `dist/bin.js`, `dist/index.js`, `dist/bin/supervisord.js`, which §3.2 already names for `bundleAt`. Any one missing is `unbuilt`, whatever the chunks OR the marker say, and §4 names the gaps. The membership it belongs to is clause 1's **§3.3 artifact set** — one esbuild-named chunk and these three entries (`:121`), `dist/index.d.ts` (`:153`), the six `PLUGIN_ASSET_ENTRIES` under `dist/plugin` (`:178`) — of which `.bundle-meta/metafile.json` is deliberately not a member, which is what makes `unbuilt` and `bundle-provenance-missing` disjoint. ⚠️ **Freshness cannot subsume this clause (standing rule 8, on this fix):** deleting an output LOWERS the newest output mtime, which makes the marker look MORE current, not less — so `rm dist/bin.js` on an otherwise complete tree PASSES clause 2 and is caught only here. Dropping it would re-open `C-R4-1` and make §6 row 11 vacuous. This is the third appearance of one failure — a verdict of `clean` on a tree with no usable build output — and each time it survived because the previous fix keyed on a proxy (directory existence, then output presence) rather than on the artifact anyone actually runs.

`unbuilt` is never folded into `clean`.

⚠️ **`unbuilt` is decided by WHY `dist` is empty, not by how many entries it has
(round 7 — round 6's count-based rule reverted round 3's fix).** Counting non-excluded
entries made an empty `dist` report **clean**: `tsc -b --clean` deletes files and leaves
the directory, so after `npm run build:clean` all 19 units hold zero entries, zero
non-excluded entries, and compare clean against `undefined`. That is verbatim the state
round 3 filed `CR-2` for. The rule is therefore:

| what `dist` holds                          | verdict                                                                | the `reason` §4 prints VERBATIM where there is one, then why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------ | ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| nothing at all                             | **`skipped`**                                                          | `dist/ exists but is empty — a --clean, a test's mkdir, or a manual delete; nothing here proves a build ever ran`. ⚠️ **Round 13 (OP13-2): this cell read `indistinguishable — see below`** — an editor's cross-reference standing in as the operator-facing string, under a §4 rule that prints this field verbatim and a §6 row that forbids the reporter inventing one, so the one `skipped` state the design could not spell was the one it sent to a terminal. The indistinguishability argument is §8(h)'s and lives in the prose beneath this table, never in the cell |
| only `.tsbuildinfo`                        | `unbuilt`                                                              | `dist/ holds only .tsbuildinfo — outputs were deleted without it` — §4's generic `unbuilt` reason, stated here because §4 prints it verbatim and `.tsbuildinfo` is positive proof the unit WAS built                                                                                                                                                                                                                                                                                                                                                                          |
| only `eo-*-fixture-*` scratch              | `skipped`                                                              | a test `mkdir`ed it; nothing was ever built here                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| any real output, unit ≠ `packages/cli`     | compared                                                               |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| any real output, unit **= `packages/cli`** | `compared` only if clauses 1-3 above all hold; otherwise **`unbuilt`** | round 9: 'any real output → compared' is the row that let a `tsc -b`-only tree compare clean. The three entry outputs ARE real output, and they are exactly what an interrupted build leaves behind                                                                                                                                                                                                                                                                                                                                                                           |

For `packages/cli` only, one further step **inside** the `compared` branch — never before
it, so an absent or empty `dist` is still `skipped` and the fresh-clone and `build:clean`
cases above are untouched:

| cli `dist`, given `compared`                                                                                                                           | verdict                                                                                                                                                                                      | why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| every tsc-written member absent — the three entry outputs, `dist/index.d.ts` AND `.tsbuildinfo` — while ≥1 esbuild chunk survives                      | `unbuilt`, gaps named, reason `tsc -b --clean removed the compiler outputs from a completed bundle`                                                                                          | the `--clean` SIGNATURE, and it is not `bundle:cli` failing (round 12, C11-3). ⚠️ **Standing rule 8, at the site of this fix:** keying on `.tsbuildinfo` absence ALONE would misfire on a standalone `npm run bundle:cli` against a fresh clone, which also has none — so the predicate is the conjunction. No `bundle-cli.mjs` failure class produces it: the wipe at `:113-119` removes the chunks FIRST, and `build()` at `:121` writes the entries before anything downstream can throw, so a surviving chunk with no entries can only be `tsc -b --clean`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| any other member of the §3.3 artifact set missing                                                                                                      | `unbuilt`, gaps named, reason `bundle:cli did not finish`                                                                                                                                    | a step of `bundle-cli.mjs` did not run. ⚠️ **Nothing in §6 rendered this sentence until round 19 (`OP19-1`)** — the row above's `--clean` spelling is pinned by §6 row 32's equality and this one by no element at all, so an implementation collapsing both rows onto the `--clean` sentence satisfied every cli-`unbuilt` row in §6 and sent an operator after a command they never ran. §6 row **20**'s RENDERED TWIN is the element, on that row's own `rm -rf packages/cli/dist/plugin/skills` end state, and the two rows' mutants are mirrors. Both rows share ONE remedy (§4's `unbuilt` row), so the diagnosis is the only thing that distinguishes them — which is why `unitState`, never the reporter, chooses between them                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| every member present, `.bundle-meta/metafile.json` absent                                                                                              | `compared` **plus** `bundle-provenance-missing`                                                                                                                                              | the bundle is fine, the check is degraded — §3.2, and day one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| every member present, marker FILE present, PARSES, and NOT OLDER than the newest §3-filtered output                                                    | `compared`, bundle comparison runs                                                                                                                                                           | the whole sequence ran and named its own inputs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| every member present, marker FILE present and NOT OLDER, but it does not `JSON.parse`                                                                  | `compared` **plus** `bundle-provenance-missing`, reason `.bundle-meta/metafile.json unreadable`; the bundle comparison does NOT run                                                          | round 14, C14-1. The row above was FALSE for this input — `readMetafile` returned `undefined`, so the comparison it promises cannot be performed — and no row admitted the state at all. This is the ONLY row in this table that splits on parseability: every other row keys on the marker FILE, and only whether the COMPARISON runs keys on the parse (§3.2, §2's fourth branch)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| every member present, marker FILE present but OLDER than the newest §3-filtered output — **whatever `readMetafile` returned for it** (round 14, C14-1) | `unbuilt`, the offending file named, reason `dist/<file> (<mtime>) is NEWER than the last completed bundle (.bundle-meta/metafile.json, <mtime>); tsc -b has overwritten the bundled output` | **clause 2** — `tsc -b` re-emitted over a completed bundle. ⚠️ **This row is round 13's (C13-1), and its absence was the founding failure's NINTH appearance, at the clause added to close its seventh.** The table partitioned on (members present) × (marker PRESENT) only, so §6 row 19's own end state landed on the row above and read `compared`; every comparison then ran clean — `bundleAt` is the min chunk mtime and is unchanged, §3.2's three input sides are unchanged, `checkUnitFreshness` sees a FUTURE output, §3.4 and §3.5 are untouched — and the check printed **PASS, exit 0 even under `--strict`** on the tree this section's own stated-cost paragraph calls one that "cannot publish and cannot run its own `bin.crabgic`" (§7 row 2's). An implementer following the three-clause conjunction FAILED §6 rows 19 and 26; one following this table PASSED them holding a state it does not admit — round 7's `CF-1` shape, at the clause round 9 introduced. ⚠️ **The elements for this reason's TEXT, and what each of them covers (round 30, `OP30-1`):** §6 rows 19 and 37 assert the RETURN value's reason by REFERENCE — the offending FILE in row 19, "the clause-2 reason" in row 37 — and §6 row 26's MAIN half is the only RENDERED assertion on it, anchored over the WHOLE line from round 30 with only the two mtime slots free, having pinned its tail alone since round 29 (`OP29-1`); until then a reason naming the file and ending with `tsc -b has overwritten the bundled output` but carrying neither the marker path nor either mtime was SPELLED nowhere in §6, that literal returning ZERO hits inside that section |

⚠️ **The empty case is `skipped`, not `unbuilt`, and that is a residual limit rather than a preference (round 8).** Round 7 made it `unbuilt` on the reasoning "it was built once and the outputs are gone". But both journal fixture families call `mkdir(SCRATCH_ROOT, { recursive: true })` and then `rm(dir, …)` — **the mkdtemp directory, not the root** (`crash-fixtures/prepare-runtime.ts:131`, `lease-fixtures/prepare-runtime.ts:110`). So on a fresh clone one `npm test` leaves `packages/journal/dist` **empty**, and the next `pretest` would report `unbuilt` on a tree nobody has ever built — CR6-2, re-opened by the fix that replaced CR6-2's fix.

**Nothing at the unit level distinguishes the two states**: `tsc -b --clean` deletes `.tsbuildinfo` too, so a `--clean`ed unit and a never-built one are byte-identical. `build:clean` is therefore **accepted as indistinguishable at unit granularity** and stated here rather than papered over. It is not undetectable overall — the **`packages/cli/` SUBSET** of `npx tsc -b packages/cli --clean --dry` is exactly five deletions (`dist/bin.js`, `dist/index.js`, `dist/index.d.ts`, `dist/bin/supervisord.js`, `.tsbuildinfo`; re-measured 2026-08-20, nothing under `.bundle-meta/`). ⚠️ **The WHOLE command lists 4,781 deletions across 17 packages (round 12, C11-2), because `--clean` cleans the project's entire REFERENCE GRAPH, not the project.** Quoting the `grep 'packages/cli/'` subset as the command's output was standing rule 1 — a search narrower than its claim — used as the bound for a residual, and the root `build:clean` (`tsc -b --clean`, `package.json:17`) is wider still: `npx tsc -b --clean --dry` lists **5,051** deletions across all **19** units, including 19 `.tsbuildinfo`. So a `--clean`ed tree loses its entry outputs and still reports `unbuilt packages/cli` — **through clauses 1 AND 3**: `dist/index.d.ts` (`:153`) is one of that five and is a clause-1 member, so clause 1 fires independently of clause 3 and the gap list §4 prints is `dist/bin.js, dist/index.js, dist/bin/supervisord.js, dist/index.d.ts`. ⚠️ **Clause 2 does NOT catch this state (round 9):** `build:clean` is `tsc -b --clean` and runs no bundle, so the wipe-step clear never fires; the five hashed chunks and `dist/plugin/**` survive as well, and having been written before the marker they are all OLDER than it, so the freshness comparison passes. ⚠️ **What clause 3 buys is therefore stated rather than overstated (round 12, C11-2).** The three entry outputs are clause-1 MEMBERS, so clause 1 alone already catches every state clause 3 does, `build:clean` included — "the second reason clause 3 can never be dropped" rested on the "through nothing else" claim corrected above. Clause 3 is retained as redundancy on purpose: an explicit guard against clause 2, which can never subsume it (a deletion LOWERS the newest output mtime and makes the marker look MORE current), and against a later edit dropping the entries from clause 1's set — the class of edit standing rule 8 keeps catching. It is not a second detector, and it is not deleted: dropping it is one edit away from re-opening `C-R4-1`. The marker is NOT what carries this: `--clean` never touches it, so it survives the very state it was cited for.

**The distinguishing fact is the REASON the directory is empty, and the count cannot
carry it** — a never-built tree and a `--clean`ed tree both hold zero entries.

⚠️ **The original round-6 wording — "requires at least one NON-EXCLUDED entry" — is what
reverted the earlier fix.** Both fixture families call
`mkdir(SCRATCH_ROOT, { recursive: true })` and their `cleanup()` removes only the
mkdtemp directory — so `packages/journal/dist` **persists, empty**, after the first
`npm test` on a tree that was never built. With every remaining entry excluded as
scratch, the next `pretest` would report `unbuilt packages/journal` and print
`rm -rf packages/journal/dist`. The round-5 fix would have converted a silent wrong
answer into a loud one. Round 2's exclusion of `.tsbuildinfo` makes this
state reachable a second way: `bundle-cli.mjs:113-119` deletes outputs while keeping
`.tsbuildinfo`, and a following `tsc -b` re-emits nothing.

⚠️ **SKIP RULE — ALL FOUR cli-SCOPED COMPARISONS, extended again in round 9.**
`checkPluginAssets` was outside it: on a fresh clone, or after the design's own
`rm -rf packages/cli/dist`, `newestUnder("packages/cli/dist/plugin")` is `undefined`, so
it would throw or emit `stale-plugin-assets` against a tree that was never built — round
1's C-1 recurring on the comparison round 1's own fix introduced. `checkShippedDeclarations`
joined them in round 9 — same failure, second excluded artifact — and its asymmetric case is
the sharp one: `rm -rf packages/cli/dist` deletes the copy and LEAVES the cache, so a rule
keyed on 'the cache is newer' would fire on a tree with no CLI. All four report `skipped`
when either side of their comparison YIELDS NOTHING — ⚠️ **re-keyed off directory
existence in round 17 (C17-1), because `C15-4` made `skipped` stop suppressing these four
and a rule keyed on `packages/cli/dist` being ABSENT does not fire on a `packages/cli/dist`
that is PRESENT and outputless.** Stated PER COMPARISON, because a rule given only in the
aggregate is what let two of the four stand with no admissible reason:
`checkBundleFreshness` declines when `bundleAt` is `undefined`, no file under
`packages/cli/dist` matching the chunk pattern (§3.2); `checkPluginAssets` when
`packages/cli/dist/plugin` yields nothing (§3.4, whose spelling for it round 17 restores);
`checkDeclarationCache` when `packages/cli/.dts-cache/index.d.ts` is absent; and
`checkShippedDeclarations` when EITHER of its two files is (§3.5). The SKIP TARGETS are
therefore artifacts under `packages/cli/dist`, `packages/cli/dist/plugin` and
`packages/cli/.dts-cache`, **never**
`packages/cli/.bundle-meta/metafile.json`. ⚠️ **Those targets are NOT the whole of any
comparison's comparand, and this sentence read as though they were for fifteen rounds
(round 15, C15-2).** `checkBundleFreshness`'s INPUT side is the sixteen inlined units' `dist`
trees (§3.2), every one of which can be absent or emptied on a tree whose `packages/cli` is
complete — reached from §4's recipe step 1, whose fourth carve-out leaves `packages/cli`
alone while deleting another unit's `dist`. So a not-run cause for the one comparison
requirement 3 exists for lives OUTSIDE `packages/cli` entirely, and an enumeration that
scopes every side to `packages/cli` is what let §3.2's input list stand with no absence rule
at all. §3.2 now states that rule beside the list; its reason (`<unit>/dist absent`,
comma-joined over the offenders) joins §4's verbatim list below the two that outrank it, and
§6 row 40 pins it — its original half at one offender and its round-18 twin at two, the JOIN itself being unexercised at N=1, where the mandated spelling and two wrong ones are byte-identical (round 18, `CF18-3`). With `dist` present and the marker absent,
`checkBundleFreshness` reports `bundle-provenance-missing` (§3.2, round 9); reading "either
side" as the metafile there would mute the only signal a missing marker now has, and make
the kind vacuous. And `skipped` here means a coverage entry of `not-run` naming that side
(§3), **printed** by §4 — never an early `return []`. ⚠️ **This rule says when the four
DECLINE; it has never said they are SILENT on a `skipped` `packages/cli`, and §3's skip-rule
table read as though it did for fifteen rounds (round 15, C15-4).** `unitState` reaching
`skipped` suppresses `checkUnitFreshness` and `checkOrphans` — the two PER-UNIT comparisons
that table's column names — and nothing else; only `unbuilt` suppresses these four (§3.2).
`checkDeclarationCache` is the reachable case: `.dts-cache/` is a sibling of `dist`, so
`rm -rf packages/cli/dist` leaves both of its own sides intact and it RUNS, `full`, exactly
as §4's recipe step 5 states — and against a cache older than `packages/*/src` it reports
`stale-declarations` on a `packages/cli` the same run names as `skipped`. One unit in the
skipped summary and as the subject of a finding is correct output that no section admitted;
an implementer who suppresses every finding for a `skipped` unit ships a false negative for
requirement 8's kind on the tree this design's own remedy creates. §6 row 43 is the row. ⚠️ **Absence is not the only
not-run cause (round 10, C10-1).** All four are ALSO suppressed when
`unitState("packages/cli").state === "unbuilt"` (§3.2), where every artifact is present
and nothing can be named absent — so those four entries carry the second mandated reason,
`packages/cli unbuilt`, verbatim (§4's list), and that suppression is evaluated before any
comparison inspects its own comparand, so it is the reason even when a side is also
absent — ⚠️ **which no element asserted until round 19 (`C19-1`): row 26's fixture has
NOTHING absent, so it proves the entry is EMITTED and never that this reason OUTRANKS an
available absence string; row 26's round-19 twin is what proves the order.** This section's
rule is therefore "the coverage entry is set whenever the comparison did not run", never
"whenever a side is absent"; the narrower reading prints no coverage line on §6 row 26's or
§7's `npm run typecheck` tree, which §4's recipe step 5 then reads as "all six ran", and on
row 26's TWIN prints four absent artifacts' names where §3.2's rank 1 mandates
`packages/cli unbuilt` — one state, two spellings, which §4's verbatim rule calls a defect
in terms. A comparison that quietly declines to
run and says nothing is the founding silence in a smaller box.

⚠️ **SKIP RULE, EXTENDED IN ROUND 1.** §3's skip rule was per-unit and covered only
`checkUnitFreshness`; `checkBundleFreshness` and `checkDeclarationCache` stat their
targets unconditionally. On a fresh clone neither exists — `dist` is unbuilt and
`.dts-cache` is gitignored, so `npm ci` does not create it — and the check would throw.
**This design's own documented remedy reaches that state**: `rm -rf packages/cli/dist`,
then `npm test`, and the operator cannot run the suite at all (see §4's internal-error
row). Both comparisons now report `skipped` when their own comparand yields nothing, exactly as a
unit with no `dist` does — ⚠️ **"when their TARGET DIRECTORY is absent" is what this
sentence said until round 17 (C17-1), and a `packages/cli/dist` that is present and
outputless satisfies neither that absence test nor §3.2's `unbuilt` suppression, so the
bundle comparison ran with `bundleAt` `undefined`** — the comparand being the chunk set
`bundleAt` minimises over for the bundle comparison and
`packages/cli/.dts-cache/index.d.ts` for the declaration one, **never**
`packages/cli/.bundle-meta/metafile.json`: an absent OR unreadable metafile on a present, gap-free
`packages/cli/dist` is a finding (`bundle-provenance-missing`, §3.2, round 9; round 10,
C10-3), not a skip and not a throw,
or the kind can never fire — and with the same consequence: coverage `not-run`, reason
`packages/cli/.dts-cache/index.d.ts absent` or `packages/cli/dist absent`, printed by
§4. "Exactly as a unit with no `dist` does" only becomes true once comparisons are
named in the output the way skipped units already are on BOTH paths (§4's **SKIPPED UNITS ARE NAMED ON BOTH PATHS**; the unit side became true on the findings path only in round 13, OP13-1). Report
`stale-declarations` when the newest of

- every `*.ts` under every `packages/*/src` (the set `bundle-types.mjs:43-61` walks) —
  ⚠️ **`packages/*`, and NOT the 19 enumerated units: this member's SCOPE was asserted by
  nothing for nineteen rounds (round 20, `C20-2`, standing rule 9 beside members 2 and 3,
  whose rows are 3's twin and 42).** §2 hands `checkDeclarationCache(units)` no `cwd`, so
  `units.map(u => u.srcDir)` is the only source-side collection the signature offers, and
  §3's Units step says in terms that those 19 exceed `workspaces` (18) by exactly
  `e2e/report` — whose `src` holds **10** `.ts` (re-derived read-only 2026-08-21,
  `find e2e/report/src -name '*.ts' | wc -l`, against `ls -d packages/*/ | wc -l` = 18).
  That reading fires `stale-declarations packages/cli` on a
  `touch e2e/report/src/<any>.ts`, at `check:all` member 0 and exit **1** under
  `--strict`, on a cache no byte of that unit can reach: `e2e/report` is outside
  `packages/cli`'s reference closure (§3.2's own "`packages/perf` and `e2e/report` are
  unreachable and correctly excluded"). Sharper, §4's remedy row cannot clear it —
  `newestSourceMtime()` (`:43-61`, re-read 2026-08-21:
  `for (const pkg of readdirSync(PACKAGES_DIR, …))`) never leaves `packages/`, so
  `bundle-types.mjs:70` skips regeneration, the build runs, the cache is untouched and
  the check fires again identically, leaving only `bundle:types -- --force` (~5 min): a
  printed step that cannot clear the finding it is printed for, `CF14-1`'s class at the
  comparand. Nothing discriminated the two readings — every fixture unit in §6 is under
  `packages/`, row 6's mutation is `touch any packages/*/src/*.ts`, rows 3 and 3's twin
  move configs, row 42's main half moves the producer, and the live smoke test asserts
  the unit SET and the coverage shape and explicitly not the finding set. **§6 row 42's
  round-20 SCOPE TWIN is the element**, and it is the only fixture in this design
  carrying a unit outside `packages/`. ⚠️ **THAT ELEMENT BACKS THE DIRECTORY HALF ALONE,
  AND THIS MEMBER HAS TWO — the EXTENSION half was asserted by nothing for twenty-nine
  rounds (round 30, `C30-1`, standing rule 9 beside `C20-2` above).** `every *.ts under
every packages/*/src` is a DIRECTORY set AND an EXTENSION filter; `C20-2`'s twin
  discriminates `packages/*` from the 19 enumerated units and nothing about the filter.
  Two scope-CORRECT bodies get the filter wrong and neither is carelessness:
  `newestUnder(join(cwd, "packages", pkg, "src"))` with NO accept — §2 declares
  `newestUnder(dir, accept)` and §3.4's own dist side is specified as "a plain recursive
  walk", so the argument-less form is an idiom this design already uses — and the same
  call carrying §2's `isCompilerInput`, which §3's input rule 2 makes TRUE of a
  `src/**.json`. Both are WRONG: the producer this member is keyed on takes `.ts` ALONE —
  re-read as TEXT read-only 2026-08-22 at `187eb2d`, `bundle-types.mjs:43-61`'s `walk()`
  recurses, skips `node_modules`/`dist`, and reaches `statSync(full).mtimeMs` only inside
  `entry.name.endsWith(".ts")`. ⚠️ **THE MEASUREMENT AND ITS SCOPE TOGETHER, because
  stating one without the other is the defect this closes:** §3's census of **18**
  non-`.ts` files across the 19 `src/` trees (12 `.mjs`, 3 `.json`, 2 `.snap`, 1 `.info`)
  re-derives unmoved under its own printed command, and ALL 18 sit under a
  `packages/*/src` — `e2e/report/src` holds **0**, same command split by directory,
  read-only 2026-08-22, `git status --porcelain` empty before and after. So either body
  reports `stale-declarations packages/cli` for a
  `touch packages/gates/src/coverage/test-support/real-vitest-lcov.info`, at `check:all`
  member 0, exit **1** under `--strict` and at line 5 of §5.1's measured 204 under
  `pretest`, on a cache `dts-bundle-generator --project` never reads that file into — and
  §4's remedy row cannot clear it either: `newestSourceMtime()` does not see the file, so
  `bundle-types.mjs:70` skips regeneration, the cache does not move and the check fires
  again identically, leaving only `bundle:types -- --force` (~5 min). `CF14-1`'s class at
  the OTHER half of the member `C20-2` half-closed. ⚠️ **Not live on this working copy,
  and that is the point:** measured read-only 2026-08-22, the newest non-`.ts` under
  `packages/*/src` is **1786978107051.44** against `packages/cli/.dts-cache/index.d.ts` at
  **1787172303152.90**, so no live assertion can see it. That same twin now carries the
  extension half — a `packages/cli/src/fixture.json` newer than the cache — and a SECOND
  MUTANT,
- `packages/cli/tsconfig.dts.json` **and its chain** — **§6 row 3's twin is the chain's
  non-vacuity row, from round 18 (`C18-4`, standing rule 9); row 3 itself mutates the LEAF
  only**. The chain is `packages/cli/tsconfig.json` then `tsconfig.base.json`, whose
  `compilerOptions` (`declaration`, `declarationMap`, `strict`, `target`) govern the
  generated `.d.ts` because `bundle-types.mjs:84` hands the file to
  `dts-bundle-generator --project`, whose config parser resolves `extends`,
- `scripts/bundle-types.mjs`

exceeds the cache's mtime. ⚠️ **STRICTLY exceeds, and that THRESHOLD is asserted only
since round 36 (`C36-1`) by §6 row 6's EQUAL-MTIME TWIN.** The cheapest wrong transcription
is the PRODUCER's own guard read back: `bundle-types.mjs:70`, re-read as TEXT read-only
2026-08-22, is
`if (!force && existsSync(OUTPUT) && statSync(OUTPUT).mtimeMs >= newestSourceMtime())`, so
`newest(comparand) >= mtime(cache)` is its reciprocal and the reading an implementer arrives
at by copying the file this member is keyed on. Nothing discriminated the two until that
twin: every cache fixture here writes its two sides strictly apart — row 6 `cache newest`,
rows 3 and 3's twin moving configs, row 42 the producer — and §6 holds no mtime-equality
clause outside row 19's clause-2 mirror and the false-negative bullet's input-versus-output
Verdict (re-derived read-only 2026-08-22 at `d0b706e`). That body reports
`stale-declarations packages/cli` for a source edit and a `bundle:types` run landing in ONE
SECOND, at `check:all` member 0, exit **1** under `--strict` and at line 5 of §5.1's measured
204 under `pretest`, on a cache the producer calls current — and §4's `npm run build` clause
cannot clear it, `:70` skipping regeneration at exactly this boundary, so the check fires
again identically and only the ~5-minute `bundle:types -- --force` remains: `CF14-1`'s class
at the THRESHOLD rather than at the comparand. The last two are the hole the record names in blind spot 9:
`newestSourceMtime()` (`bundle-types.mjs:43-61`) stats no `.json` at all, so touching
`tsconfig.dts.json` — which `bundle-types.mjs:84` hands to the generator — does not
invalidate its own cache. This closes that from outside, without editing that file. **§6 row 42 is the non-vacuity
row, added in round 15 (C15-3)**, and until then nothing in this design exercised either of
these two members: `newestSourceMtime()` (`bundle-types.mjs:43-61`) stats neither, so an
implementation whose comparand was `packages/*/src` plus `tsconfig.dts.json` alone passed
rows 3 and 6 and every other assertion while shipping the blind-spot-9 closure inert. ⚠️ **The CHAIN member stayed in exactly that state one round longer (round 18, `C18-4`):** row 3's mutation is the LEAF `tsconfig.dts.json`, so a comparand holding the leaf alone still passed rows 3, 6 and 42, and `tsconfig.base.json` — which `bundle-types.mjs:43-61` also stats not at all — could move forward with the cache invalidated from neither side. §6 row 3's twin is that member's row. Row 42
also supplies the run §4 step 2's sole-`stale-declarations` carve-out describes in terms — a
PRODUCER edit makes the cache stale with no `dist` stale — which no fixture in this design
produced before it.

### 3.4 Plugin assets — the hole excluding `dist/plugin/**` opened (round 1)

Excluding `dist/plugin/**` from the output set is what closes blind spot 1: those files
are refreshed by `bundle-cli.mjs:178`'s `cp` on every build regardless of whether `tsc`
recompiled anything, so leaving them in makes `packages/cli` look permanently fresh.
⚠️ **§6 row 7's AMENDED mutation is that exclusion's element, from round 35
(`C35-1`), and §3.5's opening has carried the same pointer for its own bullet since round
18 (`C18-6`).** Until round 35 row 7 moved `dist/plugin/.mcp.json`, a `.json`, which §3's
four-extension whitelist rejects on its own (round 20, `C20-1`), so this exclusion was
mandated in §0, §2 and §3 and exercised by no element a whitelist-carrying implementation
fails.

⚠️ **But nothing then covers them.** `packages/plugin/{agents,hooks,skills,statusline,.mcp.json,.claude-plugin}`
are in no input set, and `check:marketplace-pin` digests `packages/plugin` (the source),
never the shipped copy. Edit a skill file, do not rebuild, and the copy inside
`packages/cli/dist/plugin` is stale with every check silent.

**Rule (fifth comparison):** the remedy is `npm run build` — §4's table is
authoritative, and `npm run bundle:cli` is banned there for the reason the
`stale-bundle` row gives. Report `stale-plugin-assets` when the newest mtime **over the six
`PLUGIN_ASSET_ENTRIES`** exceeds the newest beneath `packages/cli/dist/plugin`.
⚠️ **STRICTLY exceeds, and that THRESHOLD is asserted only since round 36 (`C36-1`) by §6
row 24's EQUAL-MTIME TWIN.** Both sides are written by ONE `cp` at `bundle-cli.mjs:178` over
sources an editor may have written in the same second, so equality is a state a COMPLETED
build reaches wherever the filesystem is coarse (§7's `Second-granularity mtimes` row,
§8(e)). Until that twin both plugin rows specified `plugin assets fresh` with the two sides
strictly apart, §6's complete-cli-build rule quantifies its plugin-SOURCE clause as
`OLDER than`, and §6 held no mtime-equality clause outside row 19's clause-2 mirror and the
false-negative bullet's input-versus-output Verdict (re-derived read-only 2026-08-22 at
`d0b706e`) — so a `checkPluginAssets` spelling this comparison `>=` passed every element
here and reported `stale-plugin-assets packages/cli`, with `npm run build` as its printed
remedy, on a build that had just run it.
⚠️ **The source side is `newestOfEntries(PLUGIN_ASSET_ENTRIES)` (§2) — the SIX ENTRIES and NEVER the package that CONTAINS them (round 28, `C28-1`), and never
`newestUnder` (round 10):** the const is IMPORTED from `scripts/bundle-cli.mjs`, which
only §1's fourth and fifth edits make possible — `export`ing `:89` and putting
`await main()` behind the entry-point guard — because today `:89` is a bare `const` and the
only import that reaches it executes the bundler and rebuilds `packages/cli/dist`
(round 10, C10-2). `bundle-cli.mjs:89` lists six entries and
`packages/plugin/.mcp.json` is a **regular file** while the other five are directories,
so a directory-typed walk throws `ENOTDIR` on a fully built, clean tree — §4's
internal-error row — `WARN` and exit **0** by default, `ERROR` and exit **2** under
`--strict`, both printed by `formatInternalError` from `check-stale-dist.mjs`'s try/catch
(§1, §2) and never propagated as a bare node stack. Re-derived 2026-08-20 on this machine
(node v24.18.0): `readdirSync` on that member throws
`ENOTDIR: not a directory, scandir 'packages/plugin/.mcp.json'`. The
dist side is safe with a plain recursive walk, because `.mcp.json` arrives under
`dist/plugin` as a leaf; only the SOURCE side has mixed-typed roots. ⚠️ **That mandate
is TWO obligations and only the MIXED-TYPE one had an element until round 28 (`C28-1`).**
The `ENOTDIR` measurement above and §6 row 24 discharge it, and the wrong body they kill
is a `newestUnder` applied PER ENTRY; a `newestUnder` applied to the PACKAGE ROOT —
`newestUnder(join(cwd, "packages/plugin"))`, one call, and `cwd` is the only argument
§2's `checkPluginAssets(cwd)` offers — never hands `.mcp.json` to `readdirSync`, never
throws, and is reached by NEITHER. It is not idle on this repository: re-derived
read-only at `e70c9bb`, `newestUnder(packages/plugin)` is carried by
`packages/plugin/dist/.tsbuildinfo`, the one file §3's `.tsbuildinfo` minus-bullet
measures as moving on a `touch src/a.ts && tsc -b` that emits nothing — §7 row 1's own
scenario — and that minus-bullet cannot mitigate it, being scoped to a unit's OUTPUT set
while this source side is not one. So an ordinary `npm run typecheck` would print
`stale-plugin-assets packages/cli` with a full rebuild as its remedy at `check:all`
member 0 and under flagless `pretest`, on a tree where not one of the six sources moved.
§6 row 8's SECOND MUTANT is the SCOPE half's element.

**Coverage:** `full` when the **mtime comparison** ran; `not-run` **whenever it did not
run, for either reason** (round 10, C10-1) — §3.3's skip rule firing, reason naming the
absent target, **`packages/cli/dist` only** — ⚠️ **NO LONGER: the `packages/cli/dist/plugin` spelling is RESTORED, because round 14's `C14-4` deleted it on a premise round 15's `C15-4` falsified one round later (round 17, C17-1).** `C14-4` argued that this comparison runs only on a `compared` `packages/cli`; that `compared` requires §3.3's clause 1, which requires all six `PLUGIN_ASSET_ENTRIES` to EXIST under `dist/plugin`; and that whenever `dist/plugin` is absent the unit is `unbuilt` with §3.2's suppression, evaluated first, spelling the reason `packages/cli unbuilt`. `C15-4` then ruled that `skipped` suppresses `checkUnitFreshness` and `checkOrphans` and NOTHING ELSE, and §3.3's conjunction is one further step INSIDE the `compared` branch — so on a `packages/cli/dist` that is present and holds no qualifying compiler output (a fresh clone, `tsc -b`, then `tsc -b --clean`) the unit is `skipped`, this comparison IS reached, `newestOfEntries` over `packages/cli/dist/plugin` is `undefined`, and every reason this cell then admitted was false: `packages/cli/dist absent` is false, `packages/cli unbuilt` is false, and the one true spelling had been deleted. Standing rule 8 on `C14-4`'s own fix, in the round after the one that created the state. **PRECEDENCE, in §3.2's REASON PRECEDENCE shape and for its reason:** `packages/cli unbuilt` first, then `packages/cli/dist absent`, then `packages/cli/dist/plugin absent` — so a bare tree keeps ONE spelling, §6 rows 12, 20 and 26 are untouched, and the outputless tree gets the spelling that names what is actually missing. §6 row 12 pins the second, §6 row **46** the third, and §6 row 26's round-19 twin the FIRST — which nothing pinned until round 19 (`C19-1`), row 26's own fixture leaving `dist/plugin` present, so no row made this rank compete with the spelling below it and an implementation naming the absent directory first passed every element in §6; or `packages/cli` being
`unbuilt` (§3.2), reason `packages/cli unbuilt`, where nothing is absent to name — §4
prints it either way. The status is keyed on that comparison and not
on a six-entry presence check, which this comparison no longer performs — presence is
§3.3's clause 1 (round 10). `full` and `not-run` are the only two statuses anywhere (§2):
round 10 deleted `reduced` as unreachable, so "there is no `reduced` state HERE" no longer
distinguishes this comparison from any other, and every comparison either ran or did not.

⚠️ This paragraph carried a trailing `Remedy: npm run bundle:cli` for three rounds after
that command was banned four lines above it, and it survived two dispositions claiming
otherwise — the second time because a verification grep was single-line and the sentence
wrapped. §4's table is the only remedy source.

⚠️ **And mtime alone cannot see a PARTIAL copy (round 7).** `bundle-cli.mjs:177-179`
copies the six `PLUGIN_ASSET_ENTRIES` in a loop; interrupt it after `agents/` and before
`skills/` and `dist/plugin` exists with a copy-time mtime, so `newest(sources)` is not
greater and the comparison reports clean while the shipped CLI has no skills. So the
comparison does **NOT** check the six entries for presence: §3.3's clause 1 owns entry
presence and §3.4 owns **mtime alone** (round 10). Clause 1 is stateless and
marker-independent by construction — §2's `cliArtifactGaps` comment says "The METAFILE IS
NOT A MEMBER" — so `rm -rf packages/cli/dist/plugin/skills` with the marker absent yields
`unbuilt` with the gaps named, exactly as §6 row 20 asserts, and `checkPluginAssets` never
runs at all (§3.2). ⚠️ **The "equivalently, subsumed by the completion marker" reading was
REMOVED in round 9.** A partial copy does mean `bundle-cli.mjs` never reached the marker
write that follows the copy loop (§1's
`writeFile(join(CLI_ROOT, ".bundle-meta", "metafile.json")…` fragment, beside `:181`) —
an ordering §6 now proves rather than assumes — but an absent marker is now an
**advisory** `bundle-provenance-missing` that does not fail `--strict` (§4), never
`unbuilt`. ⚠️ **And the "ONLY thing standing between round 7's CR-3 and a CLI shipped with
no skills" sentence that closed this paragraph was DELETED in round 10.** It was refuted
two sentences above itself: an implementer taking it at its word and not gating on
`unitState` makes one fact print two findings, which §3.2 promises never happens; one
gating it correctly ships a clause with no reachable state and no battery row — the
"hardcoded never to fire" class §3.2 names as the defect class this change set exists for.
Round 9's fix vacated the very clause it simultaneously promoted to load-bearing (standing
rule 8, on that round's own fix). Round 7's CR-3 is answered by clause 1 BETWEEN entries — an interrupt
after `agents/` leaves `skills/` absent from `dist/plugin` altogether and clause 1 names the
gap, marker or no marker (§6 row 20) — and that is what makes clause 1 and not this
comparison the load-bearing one. ⚠️ **It is NOT answered WITHIN one entry, and this sentence
claimed "on every tree" for five rounds (round 15, C15-1).** `bundle-cli.mjs:177-179` is
`for (const entry of PLUGIN_ASSET_ENTRIES) await cp(join(pluginRoot, entry), join(pluginOut, entry), { recursive: true })` — re-derived 2026-08-21 — so an interrupt INSIDE one `cp`
(Ctrl-C, OOM, ENOSPC) leaves that entry's TOP-LEVEL path present and its contents partial,
which clause 1 provably cannot see: it tests the six paths for existence and never walks
them. This comparison cannot see it either — `cp` writes the destination at copy time, so
`newest(dist/plugin)` is `now` and the source side can never exceed it. Nor can clause 2:
`dist/plugin/**` is outside §3's filtered comparand, so no plugin write moves anything
clause 2 reads, and on the reachable form of this tree the wipe-step clear has already
removed the marker and the write never ran, so clause 2 does not evaluate at all. The verdict
is `compared` plus exactly one advisory `bundle-provenance-missing`, **exit 0 even under
`--strict`**, at `check:all` member 0 — so `check:tarball` (member 12) and
`check:install-smoke` (member 13) then run against the precondition §5 says this ordering
names, on a `packages/cli` whose tarball ships an empty `skills/`. Stated as residual
**§8(p)** rather than closed, and pinned by §6's false-negative battery, which asserts that
accepted verdict rather than a better one.

This and §3.5 are the two places the design deliberately compares a copied artifact
rather than compiler output. It is safe HERE precisely because the comparison runs the
other way round: the asset copy refreshes the OUTPUT side, so it can only ever mask
staleness in the direction blind spot 1 names — never manufacture a false positive.
⚠️ **One masked case is NAMED rather than left implicit: a DELETED source (round 11).**
Removing a file under a `PLUGIN_ASSET_ENTRIES` directory raises nothing on the source
side, so this comparison stays clean while the shipped copy keeps a file no source
produces; §3.3's clause 1 does not catch it either, checking the six entries for top-level
presence and not their contents. And `check:marketplace-pin` digests the SOURCE, so it
stays green too — although `bundle-cli.mjs:171-173` records that the copy is kept
byte-identical to the source _precisely_ so the trust pin's digest over the installed copy
matches. Residual §8(m), bounded by the wipe at `bundle-cli.mjs:113-119`.
§3.5's copy is keyed on bytes rather than mtime instead, for the reason stated there.

### 3.5 Shipped declarations — the hole excluding `dist/index.d.ts` opened (round 9)

Excluding `dist/index.d.ts` from `packages/cli`'s output set is what keeps requirement 1
honest: `bundle-cli.mjs:153` `copyFile`s it from `.dts-cache/` on every bundle, and
`copyFile` does not preserve timestamps (measured), so leaving it in makes `packages/cli`
look fresh after any bundle whether or not `tsc` recompiled — the mtime-side exclusion §6 row 7's round-18 twin asserts (`C18-6`), row 7 itself testing the neighbouring `dist/plugin/**` exclusion and this one having had no row in §6 for seventeen rounds.

⚠️ **But nothing then covered it, and §3 pointed at a section that never read it.** §3's
bullet said "handled as its own artifact in §3.3", while §3.3 compares sources against
`.dts-cache/index.d.ts` **only**. No comparison in this design read
`packages/cli/dist/index.d.ts` — the published `types` entry
(`packages/cli/package.json` `exports["."].types`, inside `files`). CF-2's shape exactly,
at the second excluded artifact, eight rounds later.

**The counterexample is §4's own recipe.** Run step 3 (`npm run bundle:types -- --force`)
and stop — step 4 exists precisely because "step 3 alone leaves the pre-force copy
shipped". `bundle-types.mjs` writes `packages/cli/.dts-cache/index.d.ts` and nothing else
(`:39`, `:87`, `:92`), so the cache is fresh, `stale-declarations` clears, the completion
marker and `bundleAt` are untouched, every unit compares clean — **PASS, exit 0**, with
the published declarations stale.

**Rule (sixth comparison), `checkShippedDeclarations(cwd)`:** report
`stale-shipped-declarations` when `packages/cli/.dts-cache/index.d.ts` and
`packages/cli/dist/index.d.ts` both exist and their **bytes differ** — size first, then
content. §4's table is the only remedy source.

⚠️ **mtime is the wrong predicate here, and this repository already shipped the bug that
proves it.** `bundle-types.mjs:32-38` records why the cache is not written straight into
`dist`: `tsc -b` emits its own `dist/index.d.ts` — "a barrel of `export * from
\"./errors.js\"` relative re-exports" — which "would clobber the bundled file AND refresh
its mtime", and `check-install-smoke.mjs` caught the result as `Cannot find module
'./exit-codes.js'` from an installed consumer. An mtime oracle here would therefore be
`tsc -b`-writable: the fifth appearance of §3.3's four-proxy lesson, and it would have
been introduced by the fix for the sixth. ⚠️ **That state is the ORIGIN of the byte rule,
not a state §3.5 can observe (round 10):** the same `tsc -b` emit also writes
`dist/index.js` and `dist/index.d.ts.map` — `tsconfig.base.json:22-23` sets `declaration`
and `declarationMap`, and no config sets `emitDeclarationOnly` or `noEmit` for
`packages/cli` (measured 2026-08-20) — and §3's filtered output set INCLUDES both, so
clause 2 makes `packages/cli` `unbuilt` and this comparison does not run; §7 row 4 is
row 2's scenario — ⚠️ **but ONLY while the marker EXISTS, and this sentence said it
unconditionally (round 13, C13-2).** Clause 2's predicate is "**if** the marker exists", so
on every tree until the first build after this lands — measured 2026-08-20, `.bundle-meta/`
absent on a complete `dist` — the barrel emit leaves `packages/cli` **`compared`** and THIS
comparison is what fires, NON-advisory, at exit **1** under `--strict`, with
`.dts-cache/index.d.ts` present (**230,199** bytes, measured) and the shipped copy now
tsc's. That is §8(i)'s already-stated "the common case IS caught — but by §3.5, whose
stated purpose is different"; §4's line for this kind carries the cause-neutral wording
that branch forced, and §6 row 22's twin pins it.
What keys the rule on bytes on a tree that is actually `compared` is the
pair §3.5 still has to separate: the recipe's **step-3-only** state (`bundle:types
--force` then stop, which writes only `.dts-cache/index.d.ts` and moves no output mtime),
where the cache is newer and the shipped copy is stale, and a `--force` regeneration
producing IDENTICAL bytes with a newer cache mtime, which §6's false-negative battery
requires to stay clean. An mtime disjunct passes the first and FAILS the second. Byte equality is not writable by any compiler
and is exact in both directions — `copyFile` makes the two files identical by
construction (measured 2026-08-20: `cmp` reports identical, **230,199** bytes each), so
equal bytes means the shipped file IS the current cache, and unequal bytes means it is
not.

**Cost, measured rather than assumed:** six warm runs of `readFileSync` on both files
plus `Buffer.equals` took **0.30-1.43 ms** (spread reported per standing rule 3) against
the walk's 0.23-0.24 s. On the LIVE pair the `tsc` barrel is three orders of magnitude
shorter than the cache's **230,199** bytes, so the size pre-filter decides that tree
without a read. ⚠️ **That is a fact about today's two files and NEVER a property of the
barrel case, and this sentence generalised it for twenty-five rounds (round 34, `C34-1`).**
A barrel of the SAME length as its cache is the ordinary result of any source edit whose
regenerated declarations keep the byte COUNT — an exported identifier renamed to another of
the same length — so a body performing the size step and NOT the content step is a FALSE
NEGATIVE in exactly the comparison this section exists for. The pre-filter is an
optimisation and never the rule: the CONTENT step is mandatory, and §6 row 22's TWIN is the
one element that reads it, its two declaration files pinned byte-identical at 28 bytes
against a same-length rewrite.

**This is not the content oracle §7 row 1 rejects.** That rejection is about needing a
_persisted baseline_ — a twentieth build artifact with the same staleness problem. Here
the baseline already exists and is already checked: `.dts-cache/index.d.ts` is §3.3's
subject. The two comparisons chain — sources → cache (§3.3, mtime) → shipped copy (§3.5,
bytes) — and neither introduces a new artifact.

**Skip, coverage and SUPPRESSION** — not "precedence", which §3.2 and §3.3 both abolished
in round 9, so every surviving occurrence of that word in §0-§8 must be a NEGATION (round
13, CF13-4). Absent EITHER file → `skipped`; never a throw, never a
finding — and, as §3.3's **SKIP RULE — ALL FOUR cli-SCOPED COMPARISONS** requires of all four, never an early
`return []`. `skipped` here IS a coverage entry: this comparison returns
`{ findings, coverage }` (§2), `full` when both files existed and were compared, and
otherwise `shipped-declarations` at `not-run` with the reason naming the absent side
verbatim — **each spelling with its own row since round 18 (`C18-3`): §6 rows 23 and 26 and row 23's twin pin the first, §6 row 17 the second, which until then was produced by no assertion in this design, so an implementation hardcoding the first satisfied every one of them** — `packages/cli/dist/index.d.ts absent` or
`packages/cli/.dts-cache/index.d.ts absent`, and ⚠️ **when BOTH are absent the FIRST wins
(round 17, C17-3): `packages/cli/dist/index.d.ts absent` OUTRANKS
`packages/cli/.dts-cache/index.d.ts absent`, in §3.2's REASON PRECEDENCE shape and for
§3.2's reason.** "The absent side", singular, admitted no rule for the both-absent case,
and that case is a fresh clone (§5's **THE CHECK HAS ONE CI OCCURRENCE** rule keeps it out of CI, round 25, `CF25-3`) — nothing is built and
`.dts-cache/` is gitignored at `.gitignore:47`, which `npm ci` never creates — the
commonest and quietest tree this design has. Two implementations that both followed this
sentence rendered two different spellings for one state, which §4's own verbatim-reason
rule calls a defect in terms ("A battery row asserting any other spelling fails an
implementation that follows the sections"), and nothing discriminated them: §6 row 23 pins
the cache-PRESENT case alone and §5's fresh-clone output prediction — its `meta-checks` bullet until round 25 (`CF25-3`) — named none of the four reasons until round 18 (`C18-2`, which put them in that bullet and pinned them in §6 row 39's negative twin), asserting four `not-run` entries
while naming no reason. This is the gap `C15-2`'s consolidation widened §3.2's precedence
line for — applied at §3.2 and never carried here, standing rule 9. §6 row 23's
both-absent twin pins it — or, when BOTH files exist and this
comparison is nevertheless suppressed because `packages/cli` is `unbuilt` (§3.2), the
reason `packages/cli unbuilt` (round 10, C10-1) — which §4's coverage line prints on both
paths, and which OUTRANKS both spellings above whenever the suppression fires, pinned by §6
row 26's round-19 twin (round 19, `C19-1`; until then this clause named no element and row
26's own fixture leaves BOTH files present, so nothing discriminated an implementation that
names whichever side it finds absent and reaches this reason only when neither is). ⚠️ **The asymmetric case is why this is load-bearing rather than symmetry**:
`rm -rf packages/cli/dist`, step 1 of §4's own recipe, deletes the shipped copy and leaves
the cache beside `dist` (measured — `.dts-cache` is a SIBLING, §3.3's opening sentence),
so the declaration-cache comparison still runs `full` while this one skips. Without an
entry, the operator reads a coverage line naming the comparisons that did not run, applies
§4's recipe step 5 rule — "a comparison the coverage line reported `not run` was **not
performed**" — and concludes the published `types` entry was compared and is clean. It was
never compared.
`rm -rf packages/cli/dist` — this design's own remedy — leaves the cache and deletes the
copy, and a fresh clone has neither (`.gitignore:47`). And on those trees this comparison declines because ITS OWN SIDE is absent, not because of
the unit's state — ⚠️ **this clause read "the comparison runs only on a `packages/cli`
whose state is `compared`: `unbuilt` and `skipped` win, for §3.3's stated reason", which is
the one rule §3.3 exists to DENY (round 17, C17-2).** §3.3's **SKIP RULE — ALL FOUR
cli-SCOPED COMPARISONS** and §3's skip-rule table both state the negation in terms — only
`unbuilt` suppresses these four; a `skipped` `packages/cli` does not — so §3.5 cited §3.3
as the authority for its own contradiction, `CF13-4`'s shape at the section round 15's
`C15-4` did not walk. An implementer building the four from this sentence gates them all on
`state === "compared"`, which is verbatim the mutant §6 row 43 exists to kill, and fails
row 43 on row 43's own fixture at exit 1 under `--strict`; one building from §3.3 passes
it, and both sentences were live. `unbuilt` DOES win, for the reason that always held — it
names the tree's actual state, while a declarations finding would send an operator to
`bundle:cli` when the whole bundle is missing (§3.2's suppression). ⚠️ **Why no row caught
it, stated so a later round does not re-file it as an inconsistency:** on every reachable
`skipped` tree this comparison's own `dist/index.d.ts` side is absent too, so its coverage
entry is byte-identical under both readings — coincidence, not design.

**No day-one migration**, unlike round 8's marker: both files exist on a built tree today
and are byte-identical (measured), so the comparison is clean on adoption.

## 4. Output and exit code

Human text on stdout by default; `--json` for machine use (the `repo-census.mjs`
convention). One line per finding naming the unit, the newest input, the older output
and the delta. ⚠️ **THAT SENTENCE IS FALSE OF ALL EIGHT KINDS AND IS SUPERSEDED BY THE PER-KIND SHAPES BELOW (round 15, OP15-3).** FOUR kinds are delta-less by this section's own rule, and the other four collapse to `<kind> ×N — <units>`, which has nowhere to put a newest input, an older output or a delta — so `newerInput`, `olderOutput` and `deltaMs` (§2's `Finding`) are computed on every delta-carrying finding and rendered on NO human path. Stated as the SET the command returns rather than a list every append re-invalidates (`C11-4`, which this sentence had already fallen to by three rows before round 22 added a fourth): re-derived read-only 2026-08-21 at `5fef126`, outside §9's round log `grep -n 'deltaMs'` returns §2's `Finding` type and its own `--json`-only note, §4's own TWO blocks — this paragraph and the FOUR-kinds-have-no-delta one below — and the four §6 rows whose MUTANT falls through to the generic comparison shape, 8, 28, 29 and 38; since round 22 (`OP22-1`) it also returns row 43's CLI twin's `--json` assertion. **Not one of them PRINTS one on a human path.** They are **`--json`-only** — ⚠️ **and READ on that surface only since round 22 (`OP22-1`): `--json` carries `findings` verbatim from `checkStaleDist`'s return exactly as it carries `coverage`, each member keeping its `remedy` and, for a delta-carrying kind, its `newerInput`, `olderOutput` and NUMERIC `deltaMs`; §6 row 43's CLI twin's THIRD invocation is the element, and before it there was none, so a serialiser projecting every finding to `{ kind, unit }` passed every row, the false-negative battery, the wiring assertions and the live smoke test while these four fields reached NEITHER path** — stated rather than promised: the operator's next action is the same either way (§7 row 1's accepted false positive and a genuine stale build share the `stale-unit` row's remedy), and the collapse is what buys round 1's `O-3`. The cost is named where it falls — §7 row 1 asks for up to 19 `rm -rf` plus a full rebuild, and the human line says only which units, never which input moved or by how much. ⚠️ **AND
`states` — the THIRD member `checkStaleDist` returns (§2) — was read on NEITHER path until
round 24 (`OP24-2`), so this object had two of its three members pinned and the per-unit
verdict array none.** `--json` carries `states` verbatim from that same return exactly as it
carries `findings` and `coverage`, and §6 row 43's CLI twin's `--json` invocation deep-equals
it there, on the one fixture in that section carrying a `skipped` unit BESIDE a finding —
the state §2's `states` comment exists for, and one row 39's zero-finding fixture cannot
reach. Both rounds that closed a sibling key wrote a mutant that KEEPS this one (round 18's
`{ findings, states }` at §6 row 39, round 22's `{ states, coverage }` at row 43's twin), and
§1's `report.mjs` cell spelled this path "findings and coverage → text/JSON" until the same
round — two of three, at the site an implementer builds it from. Under a serialiser dropping
it the `npm run build:clean` example above loses all **18** `skipped` units on the machine
path while the human page still names them, and the internal-error object below, which is
defined BY CONTRAST as carrying no `findings`, `states` or `coverage` key, would be
contrasting on a key no completing run emits either.

⚠️ **AND ONE COVERAGE LINE, ON BOTH PATHS — for nine rounds the check computed a
degraded coverage and never printed it (round 9, O9-1).** `comparisonsSkipped` appeared
exactly once in this design, in §2's signature; "reduced-confidence" appeared three
times as a property of a result with no line shape anywhere. So on a tree whose bundle
provenance is missing the operator read `PASS — 19 units compared, 0 skipped` while
requirement 3's comparison — one of the six — had not run at all. ⚠️ **The example this
paragraph carried until round 10 was itself UNREACHABLE**, which is why it is replaced
rather than reworded: "no esbuild chunk and no `packages/cli/dist/plugin`" is TWO gaps in
§3.3's clause-1 artifact set, so that tree is `unbuilt packages/cli` — a finding, exit 1
under `--strict` — and never the `PASS` the sentence needs; and `reduced` is a status no
run can produce (§2, §3.2). The state below is reachable because neither the marker nor
the cache is a member of the artifact set. Rendered against THIS working copy (measured
2026-08-20: `ls -d packages/cli/.bundle-meta` → no such file, while `dist` holds four
`chunk-*.js` plus `run-dispatcher-POLZZ2DH.js`, the three entry outputs, `index.d.ts` and
the six plugin entries), the line is:

```
check-stale-dist: ⚠️ coverage: bundle freshness not run (.bundle-meta/metafile.json absent)
```

- it prints **LAST — after the PASS line, the findings list, the units-compared/skipped summary and the remedy plan** — both paths, never one; the summary carries this bullet's own rule as of round 13 (OP13-1) ⚠️ **with one difference this bullet let collapse for eight rounds (round 21, `OP21-2`): for the COVERAGE line "both paths" means one LINE on either path, and for the SUMMARY it means one SLOT on either path and never both slots on one run** — which is the prefix rule below, pinned by §6 row 39's MAIN half; and the prefix rule below stays authoritative for the full order;
- it lists only entries whose status is not `full`, so a fully covered run prints nothing extra;
- `--json` carries `coverage` verbatim from `checkStaleDist`'s return, so the machine path cannot drift from the text path — ⚠️ **asserted only since round 18 (`OP18-7`): `--json` was read in §6 at row 27 alone, on a THROWING fixture whose object carries no `coverage` key at all, so nothing read it on a run that COMPLETES and a serialiser emitting `{ findings, states }` alone passed everything; §6 row 39's fourth invocation is the row**;
- it carries **no remedy of its own**: the table below is keyed by finding KIND and a coverage entry is not a finding. It names what the check could not see. ⚠️ **Asserted only since round 21 (`OP21-1`), by §6 row 18, which now reads that this line carries no command from the table below.** Rows 16, 17, 26 and 18 pin the prefix, the label and one entry as a CONTIGUOUS SUBSTRING, which a formatter appending a producer command AFTER the entry still satisfies, and row 38 pins the label's ABSENCE on an all-`full` fixture — so this sentence was a rule with no element. Step 5 of the ordered recipe below hands an implementer exactly the entry-to-producer mapping that wrong rendering needs, so it is this design's own recipe that invites it: the same class as the per-kind remedy §6 row 34's second mutant closes, at the sibling surface. ⚠️ **That is not the same as carrying no NEXT ACTION, and for thirteen rounds the design let the two collapse (round 14, OP14-1):** a non-`full` entry now makes the ordered REMEDY PLAN print even on a run with ZERO findings, so recipe step 5 — the only re-run instruction this check ever writes — reaches the operator on the tree this section's own recipe step 1 creates. The gate and its single `nothing was checked` carve-out are **A DEGRADED RUN CARRIES A NEXT ACTION**, below.
- **the renderer is stated here, once, because the design implied three incompatible ones (round 10).** Each entry renders as `${comparison.replace(/-/g, " ")} ${status.replace("-", " ")} (${reason})`; entries are joined by `·` in §2's union order (`bundle-freshness`, `declaration-cache`, `plugin-assets`, `shipped-declarations`), so the line is deterministic and a row may assert a whole line rather than a fragment; the whole is preceded by this section's `check-stale-dist: ` prefix and the `⚠️ coverage: ` label — ⚠️ **whose glyph is pinned as a LITERAL and cannot degrade, residual §8(o) (round 14, OP14-5)**: re-derived 2026-08-21 across all 14 `check:all` members, every glyph in them sits inside a COMMENT and none reaches stdout, so this line introduces the first, while `docs/presentation-policy.md` resolves a non-TTY to `text` and `CRABGIC_ASCII=1` to `ascii` and `scripts/` cannot import `PresentationPolicy` (§8(a)). The label stays as written — `⚠️` is that policy's own warn glyph and the entry names the comparison, the status and the reason without it — and the missing degradation is stated in §8(o) rather than closed here. So `declaration-cache` + `not-run` renders **`declaration cache not run (…)`** — never `declarations`, which no rule in this design produces, and never the raw `declaration-cache: not-run (…)`, which is what `result.coverage` HOLDS and what §6's direct-call rows 12 and 23 assert. And the `reason` is the one its own section mandates, **verbatim**: `.bundle-meta/metafile.json absent` or `packages/cli/dist absent` (§3.2, §3.3); `packages/cli/dist holds no esbuild chunk`, for a `packages/cli/dist` that is PRESENT and matches no chunk file, so `bundleAt` is `undefined` (§3.2's Rule; round 17, C17-1), ranked BELOW `packages/cli/dist absent` and ABOVE both marker spellings by §3.2's REASON PRECEDENCE, because on that tree the marker is reachable present-and-parseable AND absent; `packages/cli/.dts-cache/index.d.ts absent` (§3.3); `packages/cli/dist` absent, then `packages/cli/dist/plugin absent` (§3.4 — the second spelling was DELETED in round 14 (C14-4) as producible by no reachable tree, and is RESTORED in round 17 (C17-1) because its unreachability argument was "this comparison runs only on a `compared` `packages/cli`", which round 15's `C15-4` falsified in the very next round; §3.4 now RANKS the two, so this section's own "A battery row asserting any other spelling fails an implementation that follows the sections" still yields exactly one spelling per state and §6 rows 12, 20 and 26 keep theirs); `packages/cli/dist/index.d.ts absent` or `packages/cli/.dts-cache/index.d.ts absent` (§3.5 — the first pinned by §6 rows 23 and 26 and row 23's twin, the SECOND by §6 row 17 only since round 18, `C18-3`); the offending inlined units' `dist` paths comma-joined and suffixed ` absent` — `packages/contracts/dist absent`, or `packages/contracts/dist, packages/gates/dist absent` — for an inlined unit yielding no qualifying compiler output (§3.2; round 15, C15-2), the paths joined in the order §3.2 states and pinned by EQUALITY on the WHOLE string at BOTH N=1 and N=2 (§6 row 40 and its round-18 twin; round 18, `CF18-3`: at N=1 this spelling, a per-path ` absent` suffix and a first-offender-only rendering are byte-identical, so this section's own "any other spelling fails" rule had no element that could tell them apart, while at N=2 the first-offender rendering also leaves the ordered REMEDY PLAN naming one absent artifact of two), which is the one not-run cause whose artifact lies OUTSIDE `packages/cli` and therefore the one §3.3's skip-rule enumeration could not spell, and which §3.2's own precedence line ranks BELOW `packages/cli unbuilt` and `packages/cli/dist absent` so that a bare tree keeps one spelling — **the REASON asserted by §6 row 40 and its round-18 twin, and the RANKS pairwise by §6 row 46's twins (3 over 4), row 39's negative twin (2 over 4) since round 18 (`C18-2`) and row 26's round-19 twin (1 over 3 and 4, and over §3.4's and §3.5's own second ranks) since round 19 (`C19-1`), with ranks 1 and 2 unable to co-occur — an absent `packages/cli/dist` is `skipped`, never `unbuilt` — and rank 5 against ranks 1-4 recorded at §3.2 as a residual no fixture reaches**; `packages/cli unbuilt`, carried by ALL FOUR entries whenever `unitState("packages/cli").state === "unbuilt"` suppresses them (§3.2, §3.3, §3.4, §3.5; round 10, C10-1) — the one not-run case in which no artifact is absent, and therefore the one no absence string can spell, and the one that WINS when a side happens to be absent too, because the suppression is evaluated first — **pinned by §6 row 26's round-19 twin (round 19, `C19-1`); row 26's own fixture has nothing absent, so for nine rounds the WINS half of this clause was asserted by nothing and an implementation resolving each reason from its own comparand first passed every element in §6, this section's "A battery row asserting any other spelling fails an implementation that follows the sections" notwithstanding**; and `.bundle-meta/metafile.json unreadable`, for a marker that exists and does not parse (§3.2; round 10, C10-3) — **pinned on the COVERAGE side only since round 22 (`C22-1`), by §6's false-negative battery's truncated-marker bullet, and for twelve rounds the ONE spelling in this list carrying no element at all**: `unreadable` reached §6 at row 29's rendered FINDING line and at that same bullet's finding reason, and nowhere on `result.coverage`, so a `checkBundleFreshness` spelling the coverage reason `.bundle-meta/metafile.json absent` off its `metafile` argument — which cannot separate an absent marker from an unparseable one (§2) — while distinguishing the two states for the finding passed every element in this design and printed both spellings for one state on one page, telling the operator a file was absent while it sat on disk. A battery row asserting any other spelling fails an implementation that follows the sections; an implementation that satisfies such a row special-cases one label and leaves a mandated reason unused.

The shape is `check-citation-runs.mjs:290`'s: a PASS line that names its own unresolved
fraction (`, N unresolvable (tolerated)`) rather than printing a green line it has not
earned (`:269-273`).

⚠️ **AND THE FIRST THING THAT LINE PRINTS IS ITS OWN NAME — which this design copied none
of until round 10.** `check-citation-runs.mjs:290` reads `check-citation-runs: PASS — …`,
and copying that shape without its name is standing rule 7 at the site of this design's
own cited precedent, and the third time this change set has copied part of a shape
(`CF8-1`, `CF9-4`). ⚠️ **A UNIVERSAL over `check:all`'s 14 members stood here from round 10
until round 31 (`OP31-1`) — "every one prefixes its top-level output lines with its own
script name, zero exceptions", with all fourteen names enumerated — and it is measured
FALSE of FOUR of them.** DELETED rather than scoped: no requirement in §0 traces to another
member's output stream, the precedent cited one clause above already establishes that the
convention exists, and the RULE below is warranted by the attribution argument two
paragraphs down, measured on THIS check's own two wirings. Nothing in this section
quantifies over the other members any more.

**RULE: every top-level line this check writes to stdout begins
`check-stale-dist: `** — the `PASS` line, every finding line, every grouped `×N` line, **the
units-compared/skipped summary on the findings path** (round 13, OP13-1), the
**the ordered REMEDY PLAN's first line** (its steps are indented
continuation lines, which the sentence below exempts), the
coverage line **where there is one — it prints only where some entry is not `full`, so a fully covered run writes no coverage line at all, label included (round 16, `OP16-4`)**, and the `WARN`/`ERROR` internal-error lines — every one of them on **stdout**. ⚠️ **This rule read "stdout or stderr" until round 31 (`OP31-2`), and `or stderr` is DELETED rather than covered:** it was the only mention of that stream anywhere in this design, no producer in it writes there, §4's opening puts human text on stdout, §2's `formatInternalError` comment mandates stdout in terms, and §6 rows 27 and 31 assert `stdout contains` while row 43's CLI twin asserts a line RENDERED on stdout — so the licence could only ever be taken up by an implementation copying the `check-citation-runs.mjs` shape WHOLE (standing rule 7: that script's `WARN` line goes to `console.warn` and its `FAIL` lines to `console.error`, only its `PASS` line to `console.log`), which then FAILS those rows while handing `--json` a clean stream by accident at the one place `C11-5` exists to defend. Their ORDER is fixed here too,
because "which line lands where" is the other half of a rule an implementer can satisfy and
still print an unreadable page: the `PASS` line when there is one, then the grouped finding
lines, then — where there was no `PASS` line — the units-compared/skipped summary, then the
remedy plan, then the coverage line where any entry is not `full` — a slot that is EMPTY, not blank, on a run whose four entries are all `full` (round 16, `OP16-4`; §6 row 38). ⚠️ **AND "when there is one" is a CONDITION, whose FALSE side — the findings path, where the exit table above prints no `PASS` line — had no element until round 36 (`OP36-2`):** §6 row **38**'s rendered half now asserts that no line begins `check-stale-dist: PASS` on its two-kind non-advisory fixture, and its mutant is that line gated on the run not having thrown. Without it this clause fixed WHERE the line lands and never WHETHER it lands at all, and the two are the same halves this rule already separates for the summary. ⚠️ **The summary is EXACTLY ONE of those two
positions, never both and never neither**: a run either earns a `PASS` line, which carries
it in its own slot, or it does not, and then it is a line of its own. ⚠️ **Asserted only
since round 21, and by §6 row 39's MAIN half (`OP21-2`), in the shape the coverage-line
clause one sentence above already uses.** The PASS-path slot was pinned by nothing — row 35
is the findings path, row 18's fixture holds zero skipped units, and row 39's NEGATIVE twin
pins the `nothing was checked` carve-out — so a formatter emitting the standalone line
BESIDE the `PASS` line printed the summary twice on every green run and passed every
element in this design. "when there is one"
is what made the skipped units invisible for thirteen rounds on the only path an operator
reads because something is already wrong. ⚠️ **AND THE FIRST ADJACENCY THIS RULE STATES —
the `PASS` line ABOVE the finding lines — had no element anywhere until round 24
(`OP24-1`): §6 row 18 now pins the rendered index of the `PASS` line as STRICTLY LESS THAN
the rendered index of the finding line — the `PASS` line printing FIRST — on the
advisory-only fixture that is the one state the exit table above renders both lines on.**
Three of the four adjacencies were covered and the FIRST was
not: row 35 pins findings→summary and summary→plan, row 39's MAIN half pins summary→plan
and plan→coverage, and every one of those fixtures holds either no `PASS` line (row 35 is
the findings path) or no finding line (row 39 holds ZERO findings), so not one of them
could order the two against each other — while row 18 asserted its two strings as
SUBSTRINGS with no order between them. A `formatFindings` emitting the finding lines FIRST
and the PASS/summary branch at the standalone summary's slot — ONE branch written once,
which is the cheapest way to satisfy "EXACTLY ONE of those two positions" above — therefore
printed the finding ABOVE the `PASS` line on the advisory-only tree §7's day-one row
measures on every developer's machine, and passed every element in this design.
⚠️ **And the precedent this section cites for that line prints the INVERSE:**
`check-citation-runs.mjs` writes its per-item `check-citation-runs: WARN …` lines at `:263`
and the `PASS` line cited twice above at `:290` — items first, verdict last — so an
implementer copying the shape this section names gets the opposite of the order stated
here, which is why the adjacency is pinned by an element and not by the citation.
The shapes given in the rest
of this section are the text that FOLLOWS the prefix. Continuation lines indented under a
prefixed line do not repeat it (`check-marketplace-pin-digest.mjs:377-379`'s shape).
`--json` is unaffected — and it is unaffected on EVERY path, the internal-error one included: `formatInternalError` takes `json` and emits the object stated with that row below, never a prefixed human line (round 12, C11-5). ⚠️ **"Unaffected" covers the EXIT CODE as well as the text, and of the two paths this sentence quantifies over only the internal-error one had an element until round 29 (`OP29-2`).** TWO rules discharge it, both stated with the internal-error row below: one over `--json` PROCESS invocations against THROWING fixtures (round 28, `OP28-1`), one over `--json` PROCESS invocations against COMPLETING ones (round 29, `OP29-2`), whose extension is §6 row 43's CLI twin at exit **0** flagless and exit **1** under `--json --strict` on one findings-carrying end state. Until the second, a success path spelled `if (json) { console.log(JSON.stringify(result)); process.exit(0) }` exited **0** on `npm run check:stale-dist -- --strict --json` against a stale tree — this sentence falsified at the path it was never read on.

Without it the check is unattributable in both wirings §5 targets. Under `check:all` it is
member 0 of 15, `&&`-chained into one stream beside 14 neighbours (measured 2026-08-22: 14
members today, `check:tarball` at 12, `check:install-smoke` at 13), so its lines arrive
orphaned and cannot be grepped by name. Under
`pretest` it is worse by §5.1's own measurement: the line lands at line **5 of 204**, ~199
lines above vitest's summary, and §5.1 records that the coverage line is the ONLY notice a
degraded run ever gives — one line, once. An unattributed line in 204 lines of scrollback
is the founding incident's own failure mode: two hours of misdiagnosis by someone reading
the bottom of the output.

⚠️ **FOUR kinds have no delta to print, and the line shape predates them.**
`Finding = { kind, unit, newerInput, olderOutput, deltaMs, remedy }` assumes a
comparison. `unbuilt`'s finding IS the absence of an output, and
`bundle-provenance-missing` has neither side; `orphan-output` has no newer input by
definition (round 7); and `stale-shipped-declarations` is byte-keyed, so its line prints
`(<a> vs <b> bytes)` and no mtime delta at all (round 9). All four are NAMED here rather
than counted, because §2 carried the count and it drifted (round 11). Printing the common
shape gives `unbuilt <unit> undefined undefined NaN`. ⚠️ **The example carried here until
round 12 named the wrong unit, the wrong count and the wrong state (C11-3).** Re-derived
2026-08-20: `tsc -b --clean` deletes `.tsbuildinfo` in EVERY project (`npx tsc -b --clean
--dry` at the root — which is what `build:clean` runs — lists 5,051 deletions across all
19 units, including 19 `*/dist/.tsbuildinfo`), so §3.3's reason table gives those units an
empty `dist` and `skipped`. After `npm run build:clean` this design therefore produces
**18 `skipped` units and exactly ONE `unbuilt` finding — `packages/cli`**, whose esbuild
chunks and `dist/plugin/**` survive; **zero** `unbuilt packages/contracts` findings occur
in that state, and the count and unit were carried forward from the pre-round-8 rule. The
state that DOES yield one finding per unit is §6 row 9's — outputs deleted, `.tsbuildinfo`
kept, which is exactly what `bundle-cli.mjs:113-119` does — applied to N units. The
argument is unaffected either way: one such line is enough. So those three fields are
optional and each kind states its own line:

- `unbuilt <unit> — <reason>`, printing `unitState()`'s reason verbatim. Today the only generic reason is `dist/ holds only .tsbuildinfo — outputs were deleted without it`. The sentence is not hard-coded here: the rule behind it changed in four of nine rounds and the line was twice left asserting what the previous rule meant (round 7's OP-2).
- `stale-shipped-declarations packages/cli — dist/index.d.ts does not match
.dts-cache/index.d.ts (<a> vs <b> bytes)` —
  ⚠️ **cause-NEUTRAL since round 13 (C13-2).** It read "the cached declarations were
  never copied in", which is FALSE in the branch §3.5 and §7 row 4 now name: with the
  marker absent, `tsc -b` writes its own barrel OVER a copy that WAS made, and the line
  told the operator the opposite of what happened. A wrong diagnosis is the OP-2 failure
  this section files two bullets below its own line list, and a wrong diagnosis is what
  the founding incident WAS. Both causes share one remedy (`npm run bundle:cli`, the
  table below), so the line states the OBSERVATION and the table states the fix.
  ⚠️ **And since round 31 (`OP31-3`) it carries NO trailing clause.** It ended
  `; the shipped copy is not the current cache`, which restated its own head in plain
  English, added no fact the operator acts on, and was read by NOTHING — its one
  occurrence file-wide was its own definition here, and §6 row 29's fourth fixture
  glosses this kind's line as byte-based with no mtime delta. Requirement 8 is discharged
  by §3.3 and §3.5, never by that clause. DELETED — the shrink `C14-4` took at this
  section's reason list and round 10 took at the unreachable `reduced` status.
- `bundle-provenance-missing packages/cli — .bundle-meta/metafile.json absent; the bundle comparison did not run (advisory: does not fail --strict)`, and its twin for the unreadable case, `bundle-provenance-missing packages/cli — .bundle-meta/metafile.json unreadable; the bundle comparison did not run (advisory: does not fail --strict)` (round 10, C10-3: `absent` is false there, and this line is the only place the operator learns that `npm run build` — never `rm -rf packages/cli/dist`, which leaves the sibling `.bundle-meta/` untouched — is what clears it; §6 row 29's FIFTH fixture is the non-vacuity row for this twin, added in round 18 (`OP18-5`) because row 29's `bundle-provenance-missing` fixture is row 21's, marker ABSENT, so nothing anywhere rendered this line and a single hard-coded line spelling `absent` passed everything — the shape round 12's `C11-3` gave row 32 for the `unbuilt` kind's second variant). ⚠️ **That is the FINDING form, and it fires only on a unit that is
  otherwise `compared` — §3.2's DISJOINTNESS RULE, NEVER a precedence rule (round 13,
  CF13-4).** §3.2 and §3.3 both abolished precedence in round 9 and now state the
  opposite in terms ("NO precedence rule, because the two kinds are DISJOINT"; "disjoint
  by construction and need no precedence rule"), so this section cited a rule its two
  authoritative sections explicitly DENY, and an implementer sent to §3.2 for it finds
  the denial and cannot tell which section is current. The rule this line actually rests
  on where the coverage entry is concerned is §3.2's `unbuilt` suppression (round 10,
  C10-1). The coverage entry for the same comparison fires in **every** not-run case,
  including the ones that suppression or the skip rule silence, so both can print on one
  run and neither is redundant. **Round 9** settled the rest: this is the
  permanent, stateless reading. The check holds no state, so it cannot say "until the first
  build after adoption"; it prints the same line on a pre-adoption tree and on a build
  interrupted between `bundle-cli.mjs:178` and `:181`, because those two trees are identical
  on disk. It never implies `unbuilt`, and its remedy never deletes `dist`.
- `orphan-output <unit> — dist/<x>.js has no src/<x>.ts` — **round 7**: an orphan has no
  newer input by definition, so it printed `undefined … NaN` too. The same defect round 4
  filed for `unbuilt`, at a site that fix did not reach.
- `unbuilt packages/cli — bundle:cli did not finish; missing: <gap list>` — e.g.
  `missing: chunk-*.js, dist/plugin/skills`. ⚠️ **AND ITS `--clean` TWIN,
  `unbuilt packages/cli — tsc -b --clean removed the compiler outputs from a completed
bundle; missing: <gap list>` (round 12, C11-3).** After `npm run build:clean` the gap
  list is `dist/bin.js, dist/index.js, dist/bin/supervisord.js, dist/index.d.ts` —
  measured, those four are the tsc-written members of clause 1's set and `--clean` deletes
  exactly them — on a tree where `bundle:cli` **finished normally** and `tsc -b --clean`
  deleted afterwards. Printing "bundle:cli did not finish" there is verbatim the OP-2
  failure this section files two bullets below ("the generic line says … which is FALSE"):
  the operator is sent after a step they never ran, at the tree state §3.3 and §8(h)
  discuss most. §3.3's cli reason table owns the predicate that separates the two, because
  §6 row 30 forbids the reporter inventing a sentence and this line prints `unitState()`'s
  reason verbatim; §6 row **32** is the non-vacuity row for the `--clean` spelling and §6 row **20**'s RENDERED TWIN is the one for THIS line's (round 19, `OP19-1`). ⚠️ **"the non-vacuity row", singular, covered one of the two sentences from the round that split them (round 12, `C11-3`) to round 19.** Row 32 pins the `--clean` line by equality; nothing pinned this one — rows 11, 20, 25, 33 and 46 assert a verdict, a gap list or a coverage entry and never a reason sentence, rows 19 and 37 name the clause-2 reason's offending FILE, which is a different row of the same table and which neither other mandated sentence satisfies, and row 36's subject is grouping, its cli half being row 32's own `--clean` signature. So a `unitState` collapsing §3.3's first two cli rows onto the `--clean` sentence passed the whole battery, the false-negative battery, the wiring assertions and the live smoke test, and printed `tsc -b --clean removed the compiler outputs from a completed bundle` on a `bundle:cli` interrupted at `:178` — this bullet's own wrong diagnosis with its two sentences swapped, at the row `C11-3`'s fix created. Standing rule 8 on that fix, and `OP18-5`'s shape at a second kind. The remedy is unchanged
  (`rm -rf packages/cli/dist` then `npm run build`), so the remedy table needs no row —
  only the diagnosis was wrong, and a wrong diagnosis is what the founding incident was.
  This is §3.3's artifact-set clause, and the
  gap list is the whole of it: the verdict is a gap over a MULTI-MEMBER set (`:121`'s
  chunk, the three entry outputs, `:153`'s `dist/index.d.ts`, `:178`'s six plugin
  entries), so the line names WHICH member is absent instead of asserting one step.
  ⚠️ **An absent marker is NOT one of these reasons** — after round 9 it reports
  `bundle-provenance-missing`, advisory, and never `unbuilt` (§3.3's cli table, §6 row
  10); printing "no completed bundle" for it would be the same English sentence this
  section prints about a tree that is fine. — **round 7**: the generic line says
  "holds no compiler output", which is FALSE
  in the case round 6 added the rule for. Interrupt `npm run build` during `bundle:types`
  and `dist` holds `bin.js`, `index.js`, `bin/supervisord.js` and `index.d.ts`. An
  operator who runs `ls` sees plenty of output, concludes the check is broken, and mutes
  it. The rule changed in round 6; the line did not.
- `unbuilt packages/cli — dist/<file> (<mtime>) is NEWER than the last completed bundle
(.bundle-meta/metafile.json, <mtime>); tsc -b has overwritten the bundled output` —
  **round 9**, for §3.3's clause 2, C9-1's marker-freshness clause — whose cli-reason-table row is round 13's (C13-1). Until then NO row produced this string, while §6 row 30 forbids the reporter inventing one and this line prints `unitState()`'s `reason` verbatim, so the line had no producer at all: the `--clean` twin two bullets above escaped the same fate only because C11-3 gave it its own row. ⚠️ **WHICH part of this line each element pins is stated here, because until round 30 (`OP30-1`) the elements between them reached only its two ENDS.** §6 rows 19 and 37 pin the RETURN value's reason by REFERENCE — row 19 naming the offending FILE (`dist/bin.js`) and row 37 naming "the clause-2 reason", both rows round 19 (`OP19-1`) deliberately kept OUT of the rendered group; §6 row 26's MAIN half is this line's only RENDERED assertion, and since round 30 it is anchored over the WHOLE line with only the two mtime slots free, where round 29 (`OP29-1`) had pinned its tail alone. Until then a reason reading `dist/bin.js is stale; tsc -b has overwritten the bundled output` — naming the file and ending with the mandated literal but carrying NEITHER the marker path NOR either mtime — satisfied every element in this design, on the tree §7 row 2 names as a CORRECT firing nobody may mute as noise. The line above is
  FALSE here in the other direction: the marker is PRESENT and a bundle DID finish. What
  is wrong is that `tsc -b` has since rewritten `dist/bin.js` and `dist/index.d.ts` over
  it — which nothing an operator can see with `ls` will reveal, and which §7 names as a
  CORRECT firing nobody may mute as noise. So the line names the offending file and both
  mtimes. Same defect as round 7's OP-2 — the rule changed and the line did not — at the
  site round 7's own fix created.

**Findings of the same `kind` are grouped, and the remedy is PER KIND.** ⚠️ **PER KIND names what the ordered PLAN below is COMPUTED FROM, and the table below is that plan's SOURCE — never a line printed beside a grouped block (round 21, `OP21-1`).** The plan is a pure function of the finding KINDS, of the non-`full` coverage entries' reasons and of step 1's exempt UNIT (§2), so no rendered line ever carries a table row verbatim; §6 row 34's second mutant is what makes that non-vacuous, and §2's `Finding` note states the field's one surface. ⚠️ **WHICH kinds
collapse is stated here, because "grouped" was written for every kind while this section
fixes a grouped shape for exactly ONE (round 13, OP13-3).** The FOUR delta-carrying kinds
group — `stale-unit`, `stale-bundle`, `stale-declarations`, `stale-plugin-assets` — to
`<kind> ×N — <units>`, the shape spelled out for `stale-unit` below. ⚠️ **THE COLLAPSE APPLIES AT EVERY N INCLUDING N=1 — `stale-unit ×1 — packages/contracts`, never a second shape (round 15, OP15-3).** ⚠️ **AND THREE OF THE FOUR ARE STRUCTURALLY N=1, so the collapse is their ONLY rendering (round 18, OP18-2).** `stale-bundle`, `stale-declarations` and `stale-plugin-assets` each come from ONE `packages/cli`-scoped comparison — a min over chunks (§3.2), one cache comparison (§3.3), one max over the six entries (§3.4) — so N is always 1 for them and N≥2 is unreachable, which is exactly the N at which round 15's own mutant fires. Round 15 fixed the collapse at the row that renders `stale-unit` and at nothing else: re-derived read-only at `896dd84`, `stale-bundle ×`, `stale-declarations ×` and `stale-plugin-assets ×` occur **zero** times in this design, so a formatter collapsing only at N≥2 kept passing for three of the four kinds this rule governs — standing rule 9 at `OP15-3`'s own fix. ⚠️ **ONE ROW WAS NAMED HERE FOR A THREE-KIND GAP, and two rounds later one of the three was still rendered by NOTHING (round 20, `OP20-1`).** The closure is the SET of grouped literals §6 asserts by EQUALITY, never a count and never one row — and every one of them carries this section's prefix, which §6's own group rule makes mandatory: `check-stale-dist: stale-unit ×2 — <unit>, <unit>` and `check-stale-dist: stale-unit ×1 — <unit>` at row 28 and its N=1 twin, `check-stale-dist: stale-bundle ×1 — packages/cli` at row 38, `check-stale-dist: stale-declarations ×1 — packages/cli` at row 43's CLI twin (round 19, `CF19-1`, which closed a second of the three incidentally, its own subject being the exit code), and `check-stale-dist: stale-plugin-assets ×1 — packages/cli` at row 8's RENDERED TWIN. Until round 20 the fourth kind was rendered by no element at all — rows 8 and 24 are its only §6 rows and both assert the RETURN value; re-derived read-only at `9c247eb`, `grep -on 'stale-plugin-assets ×'` returns **three** hits and every one is PROSE — so a `formatFindings` whose grouped-kind set omits ONE member and falls through to the generic comparison shape for it passed every element in this design while printing the three `--json`-only fields on a human path, at `check:all` member 0, on the ordinary state row 8's own mutation creates. That mutant is NOT row 28's N=1 twin's, which collapses at N≥2 for every kind and which the three rendered rows already kill: one rule, four kinds, and a fix is not landed until every kind it governs is rendered somewhere (standing rule 9). No threshold stood here and no row rendered a delta-carrying kind at N=1: row 28's fixture is ≥2 units, row 29's four kinds are the delta-LESS ones, and row 34 names kinds descriptively without asserting a rendered finding line, and row 38 did too until round 18 (`OP18-2`) gave it the `stale-bundle ×1 — packages/cli` equality. So `stale-unit ×1 — packages/contracts` and `stale-unit packages/contracts — src/index.ts newer than dist/index.js by 20000ms` both passed the whole battery, for the commonest kind on the commonest tree, while this section elsewhere calls two shapes for one slot a defect in terms ("ONE shape and never two"). One shape at every N is also what keeps the grouped line assertable; row 28's N=1 twin is the row. The FOUR kinds whose
line above carries PER-FINDING text print one line EACH, ungrouped, whatever N is:
`unbuilt` (`— <reason>`, verbatim from `unitState()`, plus `packages/cli`'s gap list **WHERE IT IS NON-EMPTY — the scope §2's cli branch list already states, and this clause licensed the rendering unconditionally for twenty-eight rounds (round 29, `OP29-1`)**: §2's THIRD cli branch is `unbuilt` on an EMPTY gap set, carrying clause 2's reason, so an implementation with ONE composition point for this line appends `; missing: ` as a bare trailing container after `tsc -b has overwritten the bundled output`, on the tree §7 row 2 names as a CORRECT firing nobody may mute, at `check:all` member 0 and at line 5 of §5.1's measured 204 under `pretest`. §6 row 26's MAIN half is the element, and this is the FOURTH site of the bare-container class this design already guards at three — `OP25-2` in the `PASS` slot, `OP26-1` in the STANDALONE slot and `OP16-4` at the coverage label),
`orphan-output` (`dist/<x>.js has no src/<x>.ts`), `bundle-provenance-missing` (its absent
and unreadable twins) and `stale-shipped-declarations` (`(<a> vs <b> bytes)`). They are the
same four §2 names delta-less, and for the same reason: a grouped line has nowhere to put
text that differs per finding. An implementer who groups `unbuilt` prints
`unbuilt ×2 — packages/cli, packages/contracts` and discards two reasons drawn from
DIFFERENT tables — the field round 9 added and round 12 split in two precisely so the
operator is not sent after a step they never ran — and passes the whole battery, because no
row before 36 renders two `unbuilt` findings at once; §6 row **36** is the mutant-proved
row. Both were
round-1 findings and both are load-bearing:

| `kind`                       | remedy — the ordered plan's SOURCE, never a printed line                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `stale-unit`                 | **`rm -rf <unit>/dist` then `npm run build`** — `npm run build` alone often CANNOT clear this                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `stale-bundle`               | **`npm run build`** — NOT `npm run bundle:cli`, which throws when `.dts-cache` is absent (`bundle-cli.mjs:146-151`), precisely the standalone invocation its own error text warns about                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `orphan-output`              | **`rm -rf <unit>/dist` then `npm run build`** — neither `npm run build` NOR `tsc -b --clean` clears this                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `stale-declarations`         | **`npm run bundle:types -- --force`**, which clears it for EVERY input; and `npm run build` also clears it whenever the newest input is a `packages/*/src/**.ts`. ⚠️ **"`npm run build` alone CANNOT clear this" stood here for fourteen rounds and was true for two inputs out of thousands (round 14, C14-2).** Re-derived 2026-08-21: `bundle-types.mjs:70` is `if (!force && existsSync(OUTPUT) && statSync(OUTPUT).mtimeMs >= newestSourceMtime())`, and `newestSourceMtime()` (`:43-61`) walks every `packages/*/src` recursively for `.ts` — so a source-side trigger regenerates WITHOUT `--force`, and `--force` is required only for the two inputs §3.3 ADDS to the comparand (`packages/cli/tsconfig.dts.json` and its chain, `scripts/bundle-types.mjs`), which that predicate stats not at all. ⚠️ **That first clause is an IDENTITY between two SCOPES, and only one side of it was pinned until round 20 (`C20-2`).** It holds because §3.3's comparand member 1 is `packages/*/src` — the same set `newestSourceMtime()` walks — and NOT the 19 enumerated units. Under `units.map(u => u.srcDir)`, the reading §2's `checkDeclarationCache(units)` signature makes available, an `e2e/report/src` trigger is a source-side trigger for the COMPARAND and not for this PREDICATE, so `:70` skips, this row's `npm run build` branch clears nothing, and the operator is left with a ~5-minute `--force` the row does not print for that input — a printed step that cannot clear the finding it is printed for, `CF14-1`'s class at this table a second time. §6 row 42's round-20 SCOPE TWIN is what makes the identity falsifiable. ⚠️ **ITS DIRECTORY HALF, and since round 30 (`C30-1`) its EXTENSION half too.** The identity is two-sided in BOTH coordinates — `packages/*/src` on each side AND `.ts` on each side, the clause one line above stating the second — and that twin as round 20 wrote it discriminated only the first. So a comparand member 1 walking EVERY file under `packages/*/src`, accept-less or `isCompilerInput`-filtered, breaks the identity in the other direction: the newest comparand member is then one of the **18** non-`.ts` files §3 counts under the 19 `src/` trees, ALL of them under a `packages/*/src`, this row's `npm run build` branch clears nothing (`newestSourceMtime()` stats no such file, so `:70` skips), and the operator is left with the ~5-minute `--force` this row does not print for that input — `CF14-1`'s class at this table a THIRD time. That twin's fixture now carries a `packages/cli/src/fixture.json` newer than the cache, and a SECOND MUTANT. Round 1's `O-1` measured exactly those two and this row generalised them to the whole kind, and a wrong diagnosis is what the founding incident was. ⚠️ **The ordered recipe's step 3 stays UNCONDITIONAL, and that was measured rather than assumed (round 14, consolidation).** Conditioning step 3 on `newerInput` breaks the run this section's own step-2 carve-out creates: a sole `stale-declarations` whose trigger IS a `packages/*/src` file is reachable — edit `packages/perf/src`, which is outside §3.2's inlined 16 and outside `packages/cli`'s closure, then `npm run typecheck` — and there step 2 does not run either, so both steps would be skipped and the plan would emit no build step at all. ⚠️ **A SECOND consequence stood here — "it would also make §6 row 34's asserted step 4 unreachable on row 34's own fixture" — and row 34's main half asserts NO STEP AT ALL (round 34, `OP34-1`).** Round 25 (`OP25-1`) established exactly that of the same verdict cell, at the same sentence's sibling claim about step 1, and row 34's SOLE-KIND TWIN's step 4 sits on a `stale-shipped-declarations`-only fixture where step 3 never runs — so the citation named an assertion no fixture in §6 carries, and this rule's only §6 warrant was a `CF14-1`-class measurement of what a wrong plan CANNOT clear. DELETED, and the rule's element is §6 row 43's CLI twin's PLAN conjunct: on the one fixture in that section pairing a SOLE `stale-declarations` with a `packages/*/src/**.ts` trigger, the flagless process's rendered plan must carry this step's command. Row 42's PLAN TWIN, the only other element asserting step 3, is PRODUCER-triggered — `scripts/bundle-types.mjs` sits outside every `packages/*/src` — so the conditioned body emits step 3 there exactly as a correct implementation does and passed it, along with every other element in this design, for twenty rounds. `--force` clears both input classes; the redundancy is one measured ~5 minutes and is accepted here rather than traded for a plan that cannot clear its own finding. ⚠️ **~5 minutes**; `bundle-types.mjs:76` says so itself, and under `check:all --strict` that is a five-minute wait before the chain's other 14 members can run — never a wait to unblock a PUSH, which this cell said for sixteen rounds (round 17, OP17-1): §5 measures `check:all` as invoked by no workflow and no hook, and `core.hooksPath`'s `pre-push` runs `lint typecheck build test`, rebuilding before it tests |
| `stale-shipped-declarations` | **`npm run bundle:cli`** — the SECOND sanctioned standalone invocation, safe for the same reason as the recipe's step 4: this kind cannot fire unless `.dts-cache/index.d.ts` EXISTS, which is exactly what the guard at `bundle-cli.mjs:146-151` requires. Seconds, not the generator's ~5 minutes. `npm run build` also clears it, at the cost of a full `tsc -b`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `stale-plugin-assets`        | **`npm run build`** — NOT `npm run bundle:cli`, for the reason the `stale-bundle` row gives                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `unbuilt`                    | **`rm -rf <unit>/dist` then `npm run build`** — plain `npm run build` clears it only when `.tsbuildinfo` went with the outputs — ⚠️ **for the 18 tsc-only units, that being the scope of the MEASUREMENT and not of the command (round 29, `OP29-3`)**. Measured over `tsc -b`: delete outputs but KEEP `.tsbuildinfo` (what `bundle-cli.mjs:113-119` does) and `tsc -b` re-emits nothing, twice over. `npm run build` is a THREE-step script whose third step is the very wipe cited here, so for `packages/cli` — the unit that wipe BELONGS to, and the unit carrying all three of this table's cli `unbuilt` branches — the `only when` above is FALSE; step 1's FOURTH carve-out below is where that is ruled on, CITED and never restated here (`CF6-2`'s remedy). ⚠️ **Narrowing the justification does NOT widen that carve-out to this kind:** the exemption is by KIND and by UNIT, `unbuilt packages/cli` keeps step 1, and the PRINTED plan is therefore unchanged — only this cell's justification was over-scoped, which is round 1's `O-1` generalised in this table a second time, after `C14-2` did it at the `stale-declarations` row                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `bundle-provenance-missing`  | **`npm run build`** — clears BOTH readings, absent and unreadable: `bundle-cli.mjs`'s wipe-step clear removes `.bundle-meta/` and the `mkdir`-then-write after the copy replaces it — the DIRECTORY by the `mkdir` and the file by the write, the write alone throwing the `ENOENT` `C21-2` measured, while `rm -rf packages/cli/dist` clears NEITHER (sibling, §3.3's opening sentence). **advisory**: listed, never fails `--strict` (the exit-code table below). **NOT** `rm -rf <unit>/dist` first, unlike every neighbouring row: the tree is complete and only its provenance record is missing, which on the first run after this lands is EVERY tree, so step 1 would buy a full `tsc -b` plus the generator's ~5 minutes to restore a bookkeeping file. One build clears it permanently. §3.2: the bundle comparison did **not run** (coverage `not-run`, §4's coverage line). `not-run` is the only non-`full` status this design produces, since round 10 deleted the unreachable `reduced` (§2, §3.2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

⚠️ **`npm run build` was the single printed remedy and it is wrong for `orphan-output`
and for `stale-declarations` — the two kinds this paragraph MEASURES. NAMED rather than
counted since round 30 (`OP30-3`): the fraction `two of the four` outlived round 1's
four-member kind set, its denominator had no referent left in §2's EIGHT-member `kind`
union, and its numerator counted fewer kinds than this section's own next paragraph and
remedy table put beyond a bare `npm run build`.** Measured with the repo's pinned `typescript@6.0.3` on a composite fixture:
delete a source file, run `tsc -b`, and the orphaned `a.js` and `a.d.ts` **survive** —
`tsc` never removes outputs whose source is gone. And `bundle-types.mjs:70` skips
regeneration whenever the cache is newer than `newestSourceMtime()`, a predicate that
stats no `.json` and not itself — so the two inputs §3.3 adds specifically to catch
staleness yield a finding `npm run build` will never clear. An operator running the
printed remedy at 3am would see the check fire again, identically, forever, with no path
stated — at line 5 of every `npm test` under `pretest`, and halting the other 14 members
under `check:all --strict`. ⚠️ **This read "the push stays blocked" for sixteen rounds and
no run of this check blocks a push (round 17, OP17-1)**: §5 measures `check:all` as invoked
by no workflow and no hook.

⚠️ **`npm run build` cannot clear a `stale-unit` finding either — the kind that fires
most — and this design printed it as the remedy for two rounds.** Measured on a fixture
extending this repo's own `tsconfig.base.json` with the pinned `typescript@6.0.3`,
touching a source without changing its content:

```
after touch src/a.ts + tsc -b:
  dist/a.js          1787210164   <- NOT re-emitted
  dist/.tsbuildinfo  1787210184   <- only this moved
  src/a.ts           1787210184
```

`tsc -b` decides re-emission from `.tsbuildinfo`'s content versions, so a source that is
newer but unchanged produces **no compiler output at all** — exactly §7 row 1's top
false-positive scenario (format-on-save, `git checkout`), on the most common kind, with no
path stated — reprinted on every `npm test`, and halting the other 14 `check:all` members
under `--strict`; never a blocked push (round 17, OP17-1).

⚠️ **This settles a question the design had left open, and the answer flips the
behaviour.** A first measurement said the remedy DID clear it — because `.tsbuildinfo`
lives inside `dist` and its mtime moves even when nothing is emitted. Per-file
measurement showed the compiler output untouched. **So `.tsbuildinfo` is EXCLUDED from
the output set.** Including it would let every `stale-unit` self-clear on the next
`tsc -b` with nothing rebuilt — a check reporting clean because its own oracle moved.
`rm -rf <unit>/dist` then `npm run build` does clear it, verified: every unit's
`tsBuildInfoFile` is inside `dist`, so deleting it forces a full re-emit.

⚠️ **`tsc -b --clean` does not clear an orphan either, and this design said it did for
one round.** Measured on the same fixture: after `--clean`, `b.d.ts`/`b.js` were removed
and the orphaned `a.d.ts`/`a.js` **survived** — `--clean` removes only what the current
build info knows about, and an orphan from a deleted source is precisely what it does
not know about. `rm -rf <unit>/dist` then `npm run build` does clear it, verified.

Recorded rather than quietly swapped, because of how it was found: this design
prescribed `tsc -b --clean` as the fix for O-1 **without running it**, which is the same
defect O-1 itself was. Caught by pre-checking the fix before the next review round —
one round after the standing rule was written down.

`stale-unit ×N` from a single `tsconfig.base.json` edit collapses to one grouped line
naming the count **and the units**, not 19 near-identical lines printed above 83 vitest
failures. Naming only the count would make the recipe's step 1 unrunnable — the operator
cannot `rm -rf <unit>/dist` for units nobody listed. The success path already names its
skipped unit; the failure path must match:
`check-stale-dist: stale-unit ×4 — packages/contracts, packages/engine-claude, packages/gates, packages/plugin`. ⚠️ **A UNIT IS ALWAYS PRINTED AS ITS `Unit.dir` (round 15, OP15-1).** That is the string `enumerateRootReferences` returns (§2's `Unit`, §3's **Units** step) — re-derived 2026-08-21 by calling it on the live tree: 19 path-form entries, `packages/contracts` … `e2e/report`, never basenames. This example printed the basenames for thirteen rounds while step 1 below concatenates the same token into `rm -rf <unit>/dist`: `rm -rf contracts/dist` names a path that does not exist at the repo root (verified — none of `contracts/`, `gates/`, `plugin/`, `engine-claude/`, `report/` does), `rm -rf` on a missing path exits **0** with no output, step 2's `npm run build` then re-emits nothing for a touched-but-unchanged source by this section's own pinned-`typescript@6.0.3` measurement, and the re-run at step 5 reports the identical four findings — verbatim this section's own "the check fires again, identically, forever", and verbatim `CF14-1`'s class one round earlier. `e2e/report` is the sharp case: its basename `report` is neither a root directory nor unique. No rule anywhere licenses a short form (`grep -c -i 'basename|short name|short form'` → **0**), and every other unit literal in this section (`packages/cli`, `packages/contracts`) and in §2's `states` comment is path-form. This binds `Finding.unit` too: the reporter never shortens what `unitState` and `buildUnits` hand it.

⚠️ **When more than one kind fires, the remedies have exactly one correct order and the
design must print it, not a list (round 4).** Run the per-kind advice as listed and
`npm run build` executes before `bundle:types --force`, so the declarations finding
survives and the second full rebuild is wasted; do the `rm -rf` after the build and you
undo it. The live tree already returns four `stale-unit` findings, so this is not
hypothetical. ⚠️ **The recipe has an OWNER, a POSITION and an assertion, all three stated
here because for twelve rounds it had none (round 12, OP12-4).** `formatFindings` renders
it (§1, §2) as a `remedy plan:` block computed from the finding SET — the KINDS, plus the UNIT for step 1's fourth carve-out (round 14, C14-5), so it is no longer pure over kinds alone — with the FOUR
carve-outs below applied; its first line carries §4's `check-stale-dist: ` prefix with the
steps indented beneath it (`check-marketplace-pin-digest.mjs:377-379`'s continuation
shape), and it prints AFTER the findings list and BEFORE the coverage line (the prefix rule
above) — ⚠️ **whose BEFORE-coverage half is pinned by §6 row 39 ALONE, and never by row
34 (round 22, `OP22-2`):** the coverage line is CONDITIONAL (`OP16-4`), row 39's fixture
carries ≥1 non-`full` entry by construction and row 34's does not, so row 34 pins the
AFTER-findings half alone and a later round must not re-point this clause at it. ⚠️ **A DEGRADED RUN CARRIES A NEXT ACTION — the plan prints whenever the run
reports at least ONE finding OR at least ONE non-`full` coverage entry, and NOT only on
the multi-kind case (round 14, OP14-1).** ⚠️ **AND IT IS A GATE IN BOTH DIRECTIONS, WHOSE
FALSE SIDE HAD NO ELEMENT UNTIL ROUND 36 (`OP36-3`): where BOTH disjuncts are false the plan
must NOT print, and §6 row 38's CLEAN side is the element** — the one fixture in that section
provably zero-finding AND all-four-`full`, its mutant being the plan gated on the NEGATION of
the `nothing was checked` carve-out alone, which prints `check-stale-dist: remedy plan:` and
`re-run the check` on every green run. The only element asserting the plan's ABSENCE before
it, row 39's NEGATIVE twin, carries four `not-run` entries, so it exercised the carve-out
below and never this gate, and every element asserting the plan's PRESENCE has a finding or a
non-`full` entry — so a body reading the carve-out as the whole condition passed all of them.
Two widenings, each earned from a state this
section already describes. Multi-kind is not the trigger, because this section's own
carve-outs make single-kind runs multi-step: a run whose sole finding is
`stale-declarations` needs step 3 AND step 4, and that kind's remedy-table row names only
step 3, so a plan withheld there withholds half the remedy. And a FINDING is not the
trigger either, because this recipe's own step 1 reaches a tree with none: after
`rm -rf packages/cli/dist` on an otherwise complete tree, `packages/cli` is `skipped`
(§3.3's unit-state table, row 1), the other 18 compare clean, and the run holds **zero**
findings with **three** `not-run` entries — `bundle-freshness`, `plugin-assets` and
`shipped-declarations`, requirement 3's among them. The plan is the ONLY element that
ever prints step 5, and the coverage line carries no remedy of its own, so a
finding-gated plan leaves that page stating no action at all, at exit 0. A second reach
needs no aborted recipe: `rm -rf packages/cli/.dts-cache` on a complete tree leaves
`packages/cli` `compared` (the cache is not a member of §3.3's artifact set), sets
`declaration-cache` and `shipped-declarations` to `not-run`, fires no finding, and the
command that clears it appears nowhere, because this section's remedy table is keyed by
finding KIND and no kind fired. In the zero-finding case **step 5 IS the whole plan**,
preceded by the PRODUCER of what each `not-run` REASON names — the ARTIFACT where the reason
is an absence, and the missing BUILD STEP where it is not — which is
`npm run build` for EVERY `not-run` REASON reachable in this case, and not only for the ABSENCE spellings §3.2, §3.3, §3.4 and §3.5 mandate. ⚠️ **The rule was written over ABSENCE spellings and round 17's own new reason is not one (round 18, OP18-4).** `packages/cli/dist holds no esbuild chunk` (§3.2's Rule; round 17, C17-1) is explicitly a PRESENCE spelling — this section states it for a `packages/cli/dist` that is PRESENT and matches no chunk file — so the qualifying clause did not reach it, it is named in neither this rule nor the bound below, and the producer of the artifact it names resolves to producing a directory that already exists. Re-derived read-only at `896dd84` over this section's own verbatim reason list: NINE mandated reasons, five absence spellings this rule reaches, three named in the bound as unreachable at zero findings, and this one in NEITHER list. It IS reachable at zero findings, on §6 row 46's own fixture: `packages/cli` is `skipped`, every other unit is clean, units compared is greater than zero so the `nothing was checked` carve-out does not apply, and THREE entries are non-`full`. `npm run build` is the producer there too, because it is the step that writes the chunks the reason says are absent. Stated as the rule over reasons rather than extended by one, per `C11-4` and per the same paragraph's own round-17 disposition. ⚠️ **That is
written as the RULE and not as a list, per `C11-4`, because the list that stood here named
THREE of the four spellings reachable at zero findings and omitted the one this recipe's own
step 1 produces (round 17, OP17-2).** It gave `npm run build` for a
`packages/cli/dist absent` entry, for a `packages/cli/.dts-cache/index.d.ts absent` one and
for an inlined unit's `<unit>/dist absent` entry (§3.2; round 15, C15-2), and never for
`packages/cli/dist/index.d.ts absent`, which is §3.5's own mandated reason: after
`rm -rf packages/cli/dist` the cache survives beside it, so `shipped-declarations` is
`not-run` on the ABSENT shipped copy while `declaration-cache` runs `full` — which is the
third of the three entries this paragraph counts four sentences above, and what §6 row 39's
fixture states, so the design mandated two different pages for one tree and the incomplete
one was the site an implementer builds the renderer from. `C14-4`'s mirror: that round
deleted an UNREACHABLE reason from this section's verbatim list, and this was a REACHABLE
reason missing from its producer list. The spellings NOT reachable in the zero-finding case
this rule governs are bounded rather than omitted, and the bound is over REASONS rather than
over absence spellings (round 18, OP18-4): `packages/cli unbuilt` names no artifact
at all and carries the `unbuilt` finding, and both `.bundle-meta/metafile.json` spellings
carry the advisory `bundle-provenance-missing`, which is a finding. `npm run build` suffices
for the cache without `--force` because an ABSENT cache cannot be newer than its sources and
`bundle-types.mjs:70`'s skip therefore does not fire, unlike the STALE case that makes
step 3 carry `--force`. ⚠️ **Standing rule 8, at the site of the fix:** the widened gate
must NOT print a plan on a fresh clone, where all four comparisons
are `not-run`, no unit was compared and no finding fired — that is the qualified
`nothing was checked` PASS below, which research Q5 requires to be quiet, and it is
carved out explicitly rather than left to the implementer.
§6 rows 21, **28**, 34, 38, 39 and 42 are the non-vacuity rows — one per carve-out, plus the multi-kind and the zero-finding case, plus — since round 36 (`OP36-3`) — the NOT-DEGRADED case at row 38's CLEAN side, where both disjuncts of this gate are false and the plan must NOT print, which is the gate's own false side and not a carve-out, plus step 1's own PRESENCE at row **28**'s PLAN TWIN (round 25, `OP25-1`) and its THREE-KIND SELECTOR at that SAME twin, whose fixture carries one unit per kind step 1 names since round 33 (`OP33-1`), stated as the SET rather than as a pair, and row 38 belongs on it too because its mutant-formatter half has asserted step 1's FOURTH carve-out since round 14 while both lists here and in §2 named only 34 and 39 (round 18, `OP18-3`). ⚠️ **AND row **43**'s CLI twin joins that SET in round 34 (`OP34-1`), for step 3's COMMAND on a SOLE `stale-declarations` whose trigger is a `packages/*/src/**.ts`** — the one fixture in §6 pairing those two, and the element this recipe's UNCONDITIONAL-step-3 rule went twenty rounds without, every other `stale-declarations` fixture in that section being either multi-kind or PRODUCER-triggered; §2's mirror of this SET carries it too, the two being one list stated twice, which is what this sentence's own "both lists here and in §2" already says (standing rule 9). ⚠️ **AND ROUND 35 APPENDS FOUR MORE — THREE FOR THE PLAN'S WITHHELD SIDE (`OP35-1`) AND ONE FOR A COMMAND (`OP35-2`).** The `nothing was checked` carve-out below is a THREE-conjunct predicate whose ONE element, row 39's NEGATIVE twin, holds all three at once, so each conjunct alone was deletable; the EXCLUDING element for each joins this SET — row **36** for "no finding fired", row **39**'s ALL-NOT-RUN TWIN for "NO unit was compared" and row **39**'s CACHE-ONLY TWIN for "no comparison ran", all three asserting that the plan PRINTS. And row **34**'s SOLE-KIND TWIN, already on this SET for the sole-kind case, now renders step 4's `npm run bundle:cli` by LITERAL — the command §4 calls the whole remedy for that kind, and one that both elements reaching that step had named by step NUMBER alone. §2's mirror of this SET carries all four, the two being one list stated twice. ⚠️ **The other five — that SET AS IT STOOD AT ROUND 25'S APPEND, scoped to that round rather than left as a claim about its members today (`C11-4`) — each assert a step-1 NEGATIVE or name no step at all, so for twenty-four rounds this SET closed all four carve-outs and left the step they carve OUT OF rendered by nothing (round 25, `OP25-1`).** Of the five steps of this one ordered recipe, FOUR had a rendered element and step 1 — the FIRST, and the only one that makes step 2's `npm run build` able to clear the commonest kind — had none, so a plan assembler with no step-1 branch passed rows 21, 34, 38, 39, 42 and 46 while reducing this document's own live prediction of four `stale-unit` findings to `npm run build` then step 5: this section's own pinned-`typescript@6.0.3` measurement says a touched-but-unchanged source re-emits nothing and only `.tsbuildinfo` moves, which §3 excludes, so step 5 reports the identical four findings — verbatim this section's "the check fires again, identically, forever" and `CF14-1`'s class, reached through the step added to prevent it. A second, weaker survivor is a formatter emitting step 1 for the FIRST selected unit only, byte-identical to a correct one at N=1, which no fixture reached at N≥2 until row 28's twin. Without it an implementer who reads "the remedy is PER
KIND" plus `Finding.remedy` prints the per-kind remedy beside each grouped block AND NO
PLAN passes EVERY OTHER row in §6's battery, the false-negative battery, the wiring
assertions and the live smoke test, and
hands the operator exactly the unordered list this paragraph measures as harmful.
⚠️ **THE CONJUNCTION IS LOAD-BEARING AND THIS SENTENCE CARRIED THE WIDER CLAIM (round 21,
`OP21-1`).** An implementer who prints the per-kind remedy AND the correct ordered plan
passed §6 row 34 as well, whose single mutation was that same conjunction, and rows 21, 38,
39 and 42 — and, since round 25, row 28's PLAN TWIN (`OP25-1`), since round 34 row 43's
CLI twin (`OP34-1`), and since round 35 row **36** and row **39**'s ALL-NOT-RUN and
CACHE-ONLY twins (`OP35-1`) — assert their plans' presence and steps and never the absence of other
remedy
text — so the page this paragraph measures as harmful was reachable through an EXTRA line
rather than through a missing plan, with every element in §6 green. Row 34's second mutant
is what closes it, and standing rule 9 is why the claim is narrowed here as well as fixed
there. One
ordered recipe:

1. every `rm -rf <unit>/dist` first — for the units reported `stale-unit`, `orphan-output` or `unbuilt` ONLY, **ONE line per selected unit**, each `<unit>` printed as that unit's `Unit.dir` (**A UNIT IS ALWAYS PRINTED AS ITS `Unit.dir`**, above). ⚠️ **§6 row 28's PLAN TWIN is that clause's non-vacuity element and it is round 25's (`OP25-1`); before it, this step's PRESENCE was demanded by NOTHING while four elements demanded its ABSENCE** — row 21's `NO step 1`, row 38's "a remedy plan that does NOT prescribe `rm -rf packages/cli/dist`", and rows 34, 39 and 42 naming other steps and never this one — so a plan assembler with no step-1 branch at all passed the whole battery, and a formatter emitting one `rm -rf` for the FIRST selected unit only is byte-identical to a correct one at N=1. Row 28's ≥2-unit fixture is the ONE fixture in that battery where this step selects more than one unit, which is why the element had to be a NEW twin rather than a widened clause on an existing row. ⚠️ **AND the SELECTOR this step opens with — the THREE kinds together, never `stale-unit` alone — was pinned by NOTHING until round 33 (`OP33-1`)**, that same twin's fixture now carrying one unit per kind named here, and its selector mutant being any PROPER SUBSET of them. A run whose only finding is `bundle-provenance-missing` starts at **step 2** — §6 row 21's PLAN TWIN is the non-vacuity element, added in round 18 (`OP18-3`) because this carve-out had a fixture from round 11 and an assertion from nothing: that tree is complete, and step 1 would delete a working `packages/cli/dist` to restore a missing bookkeeping file. ⚠️ **And `packages/cli` is exempt when its kinds are drawn ONLY from `stale-unit` and `stale-bundle` — the FOURTH carve-out (round 14, C14-5).** Those two are the same fact reported twice (§3.2's own overlap clause), and `bundle-cli.mjs:113-119` wipes `dist` on every bundle, so the `npm run build` at step 2 rewrites every compared output without the deletion — round 4's `C-R4-3` recorded that `npm run build` clears it for `packages/cli` "by accident, because `bundle:cli` rewrites the chunks", which is an accident only for the other 18 units. The exemption is by KIND and by UNIT, never by unit alone: `unbuilt packages/cli` keeps step 1, and `packages/cli` is exempt from `orphan-output` by construction anyway (§3.1);
2. `npm run build` — **before** the generator, not after. ⚠️ **A run whose only finding is
   `stale-shipped-declarations` starts at STEP 4, and THIS step does not print (round 14,
   OP14-3).** That is the SAME carve-out step 4 states, written at BOTH steps it
   suppresses rather than only at the step it selects, so it adds no carve-out of its own
   — the count §1, §2 and this section state moves to FOUR only for round 14's step-1
   exemption (C14-5). Without it a plan assembled by walking steps 1-5 and applying each
   step's own condition printed `npm run build` here and then printed step 4 saying steps
   1-2 were unnecessary — one plan contradicting itself, and two defensible
   implementations a full `tsc -b` across 19 units apart, neither of which §6 row 34's
   `stale-unit` + `stale-declarations` fixture discriminates. Written in the shape step 1's
   `bundle-provenance-missing` clause already uses. ⚠️ **A run whose only finding is
   `stale-declarations` starts at step 3** — the third carve-out, whose non-vacuity element
   is §6 row 42's PLAN TWIN (round 18, `OP18-3`: row 42 supplied this carve-out's fixture in
   round 15 and nothing asserted the plan it selects, so a formatter implementing only the
   two carve-outs the battery forced passed every row) — in the shape step 1's
   `bundle-provenance-missing` clause and step 4's `stale-shipped-declarations` clause
   already use. Nothing was deleted, so every dependency `dist` that C-R5-2 requires is
   present, and step 4 then lifts the regenerated cache. The state is ordinary rather than
   degraded, and its reachable form is a PRODUCER edit: `scripts/bundle-types.mjs` is in
   this comparison's comparand (§3.3) and in no unit's input set, so touching it makes the
   cache stale with no `dist` stale — this kind alone. Step 2 there buys a full `tsc -b`
   across 19 units plus a duplicate `bundle:cli`, on top of the generator's ~5 minutes,
   and cannot clear the finding anyway: `bundle-types.mjs:70` skips regeneration whenever
   the cache is newer than a predicate that stats neither `.json` nor itself, which is why
   step 3 carries `--force`;
3. `npm run bundle:types -- --force` if `stale-declarations` fired (**~5 minutes**) — ⚠️ **and this step's COMMAND was rendered by NO element for twenty rounds, row 42's PLAN TWIN asserting it by step NUMBER alone (round 34, `OP34-1`): §6 row **43**'s CLI twin's FLAGLESS process run is the element**, on the one fixture in that battery pairing a SOLE `stale-declarations` with a `packages/*/src/**.ts` trigger — the trigger class for which a step-3 branch conditioned on `newerInput` prints no build step at all, step 2 being carved out on that same fixture and step 4 needing this step to have run;
4. `npm run bundle:cli` if step 3 ran OR `stale-shipped-declarations` fired, so
   `bundle-cli.mjs:153` lifts the cache into `dist/index.d.ts` — step 3 alone leaves the
   pre-force copy shipped, which is precisely the state §3.5 now reports. If
   `stale-shipped-declarations` fired **alone**, this step is the whole remedy — and step
   2 above carries the same clause, because a plan assembled step by step would otherwise
   print `npm run build` before this sentence (round 14, OP14-3). ⚠️ **AND THIS STEP'S
   COMMAND WAS RENDERED BY NO ELEMENT UNTIL ROUND 35 (`OP35-2`) — the state round 34
   closed one step up (`OP34-1`) and left standing here, standing rule 9 at the adjacent
   step: §6 row **34**'s SOLE-KIND TWIN is the element**, on the one fixture where this
   step is the whole remedy. That twin and row 42's PLAN TWIN are the two elements that
   select step 4 and BOTH named it by step NUMBER, so a plan assembler whose step-4 line
   copies its neighbour's `npm run bundle:types -- --force` — standing rule 7's
   part-of-a-shape copy at the adjacent step — passed every element in this design while
   printing, on a sole `stale-shipped-declarations`, a command that regenerates the cache
   and never lifts it into `dist/index.d.ts`: §3.5 is byte-keyed, so the finding survives
   verbatim, step 5 reports it identically, and the operator has spent the generator's
   ~5 minutes for nothing — `CF14-1`'s class at the step this recipe calls the whole
   remedy. The sibling mutant spelling `npm run build` here clears the finding and re-opens
   the full `tsc -b` across 19 units this step exists to avoid, and it too passes a
   step-NUMBER assertion.
   ⚠️ **The reason is step
   2's ALONE, and this line named step 1 too:** step 1 is already conditioned on
   `stale-unit`, `orphan-output` and `unbuilt`, none of which fired here, so it deletes
   nothing and "steps 1-2 would delete" named a consequence that cannot occur. What step 2
   WOULD do is rebuild outputs that are already current — a full `tsc -b` across 19 units
   plus a duplicate `bundle:cli` — which is the whole of why this step is the whole
   remedy.

   ⚠️ **These are the ONLY TWO sanctioned standalone `bundle:cli` invocations** — this
   step, and §4's `stale-shipped-declarations` row — and the table bans it everywhere
   else. Both are the same case: the ban's stated cause is `.dts-cache` absence
   (`bundle-cli.mjs:146-151`); step 3 has just written that cache, and
   `stale-shipped-declarations` cannot fire unless it is already present. So the guard
   those lines implement is satisfied in exactly these two places and nowhere else.
   Round 6 traced the first; round 9 added the second on the same argument.

5. **re-run the check.** A comparison the coverage line reported `not run` was **not
   performed** — the first pass's `PASS` does not cover it, and only the re-run says
   whether it is clean. Steps 1-4 are what make a not-run comparison runnable again:
   after step 1's `rm -rf packages/cli/dist` three of the four cli-scoped comparisons are
   `not-run` — `bundle-freshness`, `plugin-assets` and `shipped-declarations` — while
   `declaration-cache` still RUNS, `full`, because `.dts-cache/` is a sibling of `dist`
   and step 1 leaves it (§3.3's opening sentence). Three, named, not "every": step 5's own
   rule turns a coverage line into a claim about what WAS compared, so a count that
   overstates it is the unearned green line one level up,
   so an operator who stops at step 4 is left with exactly the unearned green line the
   coverage line exists to prevent. This is the recipe's multi-kind case, which is the
   degraded-tree case. ⚠️ **AND THE ZERO-FINDING CASE, where this step is the WHOLE plan
   (round 14, OP14-1).** A degraded run need not carry a finding at all. After step 1's
   own `rm -rf packages/cli/dist` it carries none — `packages/cli` is `skipped`, the
   other 18 compare clean, three comparisons report `not-run`. After
   `rm -rf packages/cli/.dts-cache` on a complete tree it carries none either: the cache
   is not a member of §3.3's artifact set, so the unit stays `compared`,
   `declaration-cache` and `shipped-declarations` both decline, and the command that
   restores the cache is named by no remedy-table row because no KIND fired. So where
   coverage alone triggers the plan, steps 1-4 are all unselected and this step prints
   ALONE, with what each `not-run` entry's REASON names — its absent artifact, or the build
   step missing where nothing is absent, which is §3.2's chunkless spelling (round 18,
   OP18-4) — named beside the command that
   produces it — `npm run build` for both trees above, since an ABSENT cache cannot be
   newer than its sources and `bundle-types.mjs:70`'s skip therefore does not fire.

⚠️ **An earlier ordering put the generator at step 2 and it could not run there
(round 5).** `packages/cli/tsconfig.dts.json` declares `"references": []` and
`"composite": false`, and neither it nor `tsconfig.base.json` declares `paths` — so
`@crabgic/*` resolves through the workspace symlink to
`exports["."].types === "./dist/index.d.ts"`. **Step 1 deletes exactly those files.**
Measured on a fixture with the same generator and `--no-check`: dependency `dist`
present → inlines the type, exit 0; deleted → `TS2307: Cannot find module`, exit 1, no
output written. `bundle-types.mjs` uses `execFileSync`, so it throws outright. The recipe
was wrong precisely in the multi-kind case it was written for. ⚠️ **Standing rule 8, at the
site of step 2's `stale-declarations` carve-out:** the carve-out does not re-open C-R5-2,
because C-R5-2's cause is step 1 DELETING the dependency `dist` that
`packages/cli/tsconfig.dts.json` resolves through the workspace symlink — and the carve-out
applies only to a run whose sole finding is `stale-declarations`, which deletes nothing.
Step 1 is already conditioned on `stale-unit`, `orphan-output` or `unbuilt`, so the two
clauses can never both be live.

| condition                                                                                      | default                                       | `--strict`          |
| ---------------------------------------------------------------------------------------------- | --------------------------------------------- | ------------------- |
| no findings                                                                                    | `PASS` line, exit **0**                       | exit **0**          |
| only advisory findings (`bundle-provenance-missing`)                                           | `PASS` line + the advisory listed, exit **0** | same, exit **0**    |
| findings                                                                                       | listed, exit **0**                            | listed, exit **1**  |
| internal error — **ANY** throw the catch can SEE (§1's IMPORT RULE), not only malformed config | `WARN` line, exit **0**                       | `ERROR`, exit **2** |

⚠️ **That row's OWNER and its TEXT, both stated because for eleven rounds neither was
(round 11).** No element in §2 could produce it — `exitCodeFor` takes a `result` and a throw
yields none, `formatFindings` renders findings and coverage, `kind` has no error member, and
§1 gave `check-stale-dist.mjs` "arg parsing, CLI" and no catch. So `check-stale-dist.mjs`
wraps `checkStaleDist` and `formatFindings` in `try`/`catch` (§1) and prints
`formatInternalError(err, { strict, json })` (§2):

```
check-stale-dist: WARN internal error — <err.message>; the staleness check did NOT run, so this is not a verdict on your tree
check-stale-dist: ERROR internal error — <err.message>; the staleness check did NOT run, so this is not a verdict on your tree; re-run `npm run check:stale-dist` for the same diagnosis at exit 0; if this ran as part of `npm run check:all` it is member 0, so the members after it did not run
```

⚠️ **And under `--json` NEITHER of those lines is printed (round 12, C11-5).** `--json`
plus a throw was an input pair no section named, so both texts went to **stdout** and the
JSON stream stopped being JSON — the failure `scripts/bundle-types.mjs:63-68` records this
repository already shipping, `check-published-tarball.mjs` dying on
`Unexpected token 'b', "bundle-typ"... is not valid JSON` because a producer wrote human
chatter there. `formatInternalError` takes `json` exactly as `formatFindings` does (§2) and
emits one object instead:

```json
{ "internalError": "<err.message>", "ran": false }
```

It carries **no** `findings`, `states` or `coverage` key, so a consumer cannot read it as a
clean run; `<err.message>` already carries the `<path>: ` prefix §3's read wrapper adds, so
the machine path loses no locus; the exit code is unchanged (**0** non-strict, **2**
strict). ⚠️ **BOTH halves of that pair are read only since round 28 (`OP28-1`): the exit code was mandated HERE and in §1's `check-stale-dist.mjs` cell, and re-derived read-only at `e70c9bb`, `--json` reached an INVOCATION at exactly four sites in §6 — row 27's third, row 27's `C18-5` twin's third, row 39's fourth and row 43's CLI twin's third — and not one of the four asserted an exit code, while `grep -c -- '--json --strict'` and `grep -c -- '--strict --json'` over the whole file each returned **zero**, so the two flags had never been run together at all.** Two fixed-string searches and never one alternation, because the escape an alternation needs renders as a bare pipe once this file is read as markdown, and the command a reader copies back would then match neither spelling — standing rule 1 at the command an absence claim is reported with. The rule is therefore stated as a SET and not as a list of the two invocations that carry it today, because a list is how this one went unread (round 27's own lesson): **EVERY `--json` invocation in §6 that runs `check-stale-dist.mjs` as a process against a THROWING fixture asserts the exit code the catch returns**, and its extension today is exactly row 27's third invocation, which asserts the object AND exit **0**, and that row's round-18 twin (`C18-5`) — which asserts that object against a malformed **ROOT** `tsconfig.json`, the read §3's wrapper reaches only through `enumerateRootReferences` and which row 27's own unit-config fixture cannot exercise — whose third invocation is `--json --strict` at exit **2**. Row 39's fourth invocation and row 43's CLI twin's `--json` ones are outside THAT rule by its own terms — and row 39's for a second reason round 28 did not state: its fixture holds ZERO findings, so **0** is the correct code under BOTH `strict` values there and row 39's own round-27 conjunct (`OP27-1`) already pins that pair, while its single mutation is a MUTANT FORMATTER rather than a flag (this section's group bullet). ⚠️ **The COMPLETING surface is a SECOND rule and not a gap, since round 29 (`OP29-2`), because this section's own "`--json` is unaffected … on EVERY path" quantifies over both: EVERY `--json` invocation in §6 that runs `check-stale-dist.mjs` as a PROCESS against a COMPLETING fixture asserts the code `exitCodeFor` returns for that fixture under the `strict` it was passed.** Its extension today is exactly row 43's CLI twin's FLAGLESS `--json` at exit **0** and its `--json --strict` fourth at exit **1**; row 39's fourth is outside BOTH rules for the reason above, and row 31's clean baseline is FLAGLESS and holds ZERO findings, where **0** is correct either way. Stated as a SET for the same reason the first rule is — a list is how the first one went unread (round 27's lesson) — and the two together are why the success path can no longer short-circuit to **0** under `--json`, which is `CF19-1`'s shape at the sibling of the path `OP28-1` closed in the catch. The PAIR is what discriminates, in row 43's CLI twin's own stated shape: a flagless half alone is satisfied by a catch that always exits **0** under `--json`, and a strict half alone by one that always exits **2**. Both rows' OBJECT assertions are unchanged by the added flag (standing rule 8), because this paragraph mandates ONE object for both flag settings with no `strict`-varying member.
Latent today — nothing consumes `--json` yet — which is why it is stated now rather than
after a consumer exists.

Both text lines carry this section's prefix. **What the operator does is PRINTED, not
merely stated here — round 12 (OP12-5) found both facts written to the wrong audience.**
They are not in the remedy table for the reason the coverage line carries no remedy: that
table is keyed by finding KIND, and an internal error is not a finding. So the `ERROR` line
itself carries them: re-run non-strict with `npm run check:stale-dist`, which prints the
same diagnosis and exits **0**; and — CONDITIONALLY, and SET-VALUED rather than counted
— that IF the invocation came from `npm run check:all` this check is member **0** and the
members after it did not run (§5). ⚠️ **Both qualifications are round 14's (OP14-4).**
`--strict` is NOT evidence of `check:all`: §5 prints `npm run check:stale-dist -- --strict`
as the chain member and §5.1 confirms the script's own definition carries no flag, so an
operator who copies that command — from §5, from a CI log, or from this recipe — and hits
a throw was told that other members had been blocked when none was ever invoked and
nothing was blocked. That is a wrong diagnosis printed at exit **2**, the class this
section itself calls "what the founding incident was", and the check cannot observe its
caller. The COUNT is dropped for the second reason: §5's own block records this set
drifting three times (`C11-4`, deleted rather than corrected), and a literal written into
a printed string is the one place this design emits a figure at RUNTIME, where a later
member makes it silently wrong. The arithmetic itself is sound today — re-derived
2026-08-21, `check:all` has **14** members with `check:tarball` at 12 and
`check:install-smoke` at 13, so this check as §5's FIRST member makes 15 — which is why
this is drift-proofing and not a correction. If a count is ever wanted back it is computed
from the root `package.json` at print time and never written into the string. The `WARN`
line carries neither — asserted by §6 rows 27 and 31 since round 18 (`OP18-6`), whose
non-strict halves had asserted `contains` of the prefix and forbidden no extra text, so a
`formatInternalError` building ONE string and varying only the `WARN`/`ERROR` token passed
both while printing, on the one path that is never chained, a member-0 sentence guaranteed
FALSE. The re-run advice
is what the operator is already doing, and nothing was blocked. ⚠️ **What BOTH lines must CARRY has its own elements only from round 30 (`OP30-2`), one round after the sentence above closed what the `WARN` line must NOT carry.** The verdict clause `; the staleness check did NOT run, so this is not a verdict on your tree` is the whole warrant for degrading an internal error to a warning at exit 0, and no element in §6 read it — as a literal or descriptively — for twenty-nine rounds, so a `formatInternalError` emitting the head alone plus, under `strict`, the re-run and member-0 sentences passed §6 rows 27 and 31, row 27's `C18-5` twin and every other element, while printing at line 5 of §5.1's measured 204 and ~199 lines above a GREEN vitest summary a `WARN` an operator reads as a verdict — verbatim the two hours of misdiagnosis by someone reading the bottom of the output that this section names. §6 rows 27 and 31 and row 27's `C18-5` twin now assert it on BOTH lines, and the `ERROR` half additionally asserts the re-run sentence by its LITERAL rather than by name. The PATH inside `<err.message>` is the config or
artifact whose read threw, supplied by §3's read wrapper because `JSON.parse` messages
carry no locus. ⚠️ **The template's own `<path>: ` slot, and the sentence that existed only
to say when it is omitted, were BOTH DELETED in round 31 (`OP31-4`).** §3's wrapper
re-throws as `<path>: <err.message>`, so at the catch there is no `path` binding beside the
message: the two-slot spelling made `<err.message>` denote the WRAPPED message in the
`--json` object above and the INNER one here, and an implementer taking the template
literally prints `${err.path}: ${err.message}` and renders the locus TWICE — which §6 rows
27 and 31 and row 27's `C18-5` twin, all `contains` assertions, cannot tell from a correct
rendering. The omission sentence was not a branch an implementer could act on either: a
correct implementation writes `— ${err.message}` for a read-borne error and for a link
failure or a bug in the check alike, so both arms were ONE expression. ONE template, ONE
value, ONE expression. Re-derived 2026-08-20 in a scratchpad package (npm 11.16.0, node
v24.18.0, nothing else running but the shell): an UNCAUGHT throw in `pretest` exits npm
**1** and `npm test` never runs — the same total block round 1's `O-2` filed (§5.1).

⚠️ **The advisory row is round 9's, and without it the fix trades one day-one failure for
another.** `.bundle-meta/` exists on no tree built before this change set lands (measured
2026-08-20), so an unconditional provenance finding with no carve-out exits **1** from
`check:all -- --strict` for every developer until they rebuild. `exitCodeFor` therefore
partitions by KIND (`ADVISORY_KINDS`, §2), not by count, **and it never ignores its
`strict` argument**: without `--strict` the answer is **0** for every finding set, which is
this table's DEFAULT column and what makes `pretest` survivable (§5.1). ⚠️ **That column was
asserted by nothing for seventeen rounds (round 18, `OP18-1`)** — every `exitCodeFor` call in
§6 passed `strict: true` and the literal `strict: false` occurred nowhere in this design —
so §6 row 21 now calls it BOTH ways on BOTH of its fixtures, an `exitCodeFor` ignoring the
argument having otherwise blocked `npm test` for every developer from the first run.
⚠️ **That closed the FUNCTION and left the WIRING open for one more round (round 19,
`CF19-1`).** Row 21's four calls are DIRECT, so the argument `check-stale-dist.mjs` passes
on its success path was observed by nothing; the only §6 elements that run the CLI as a
process are rows 27 and 31, and neither runs it on a tree carrying a finding — row 27's
fixture is a malformed config, every exit code row 31 asserts is its LOAD-TIME mutant's,
and row 31's clean baseline has ZERO findings, where exit 0 is correct under either
`strict`. A CLI hardcoding `{ strict: true }` — equivalently
`process.exit(result.findings.length ? 1 : 0)` — therefore passed every element in §6 while
`pretest`, which carries no flag (§5, §5.1), exited **1** on the four `stale-unit` findings
§6 predicts for this working copy, and this table's DEFAULT column was silently void with
every assertion green. §1 now mandates the parsed flag at that call and §6 row **43**
carries the CLI twin: the flagless process at exit **0** with the finding still RENDERED,
and `--strict` at exit **1** on the same fixture — the PAIR being what discriminates, since
a flagless half alone is satisfied by a CLI that always exits 0. And the
`PASS` line still names
skipped units so a degraded run is still distinguishable from a failing one.
⚠️ **And BOTH of those closed the partition by `strict` while this table's THIRD row applied
to a set that ALSO holds the advisory — the MIXED case — was asserted by nothing for five
more rounds (round 23, `OP23-1`).** Neither of row 21's two fixtures is required to carry
both kinds at once, so an `exitCodeFor` spelled `some((f) => ADVISORY_KINDS.has(f.kind))`
where §2's rule needs `every((f) => ADVISORY_KINDS.has(f.kind))` as the exit-**0** test
under `--strict` exits **0** on every tree holding an advisory beside a real finding — the
day-one tree §7 names and the one §6's live paragraph measures — while agreeing with a
correct implementation on advisory-only and on non-advisory-only and passing every element
in §6. §6 row 21's EXIT-CODE MIX TWIN is the element, and it is what keeps this table's
second and third rows apart. ⚠️ **That mutant differs on the EMPTY set as well, and this
table's FIRST row was asserted by nothing in its `--strict` column for twenty-six rounds
(round 27, `OP27-1`).** Every `exitCodeFor` assertion in §6 carries at least one finding and
the one zero-finding PROCESS run, row 31's clean baseline, is FLAGLESS — so
`strict ? (findings.length > 0 && findings.every(isAdvisory) ? 0 : 1) : 0`, this table's
rows 1 and 2 merged, agreed with a correct implementation on every state §6 asserted and
exited **1** at `check:all` member 0 on every CLEAN, fully built tree, permanently, taking
the other 14 members with it: `OP18-1`'s failure inverted, FAIL on a correct tree rather
than PASS on a broken one. §6 row 39's exit-code conjunct is the element. The table itself is unchanged: its four conditions already partition correctly, a
mixed set not being "only advisory findings".

Coverage **never changes the exit code** — it is reported, not enforced (§8(j)); a
`not-run` bundle comparison exits **0** even under `--strict`. One qualification, taken
from `check-citation-runs.mjs:274-279`, which FAILS rather than print a pass it did not
earn: when **no** comparison ran and **no** unit was compared — a fresh clone, `npm ci`
then `npm test` with no build in between (§5; never a CI job — §5's **THE CHECK HAS ONE CI
OCCURRENCE** rule, round 25, `CF25-3`) — the PASS line says so in words:
`check-stale-dist: PASS — 0 units compared, 19 skipped; nothing was checked`. ⚠️ **THIS LINE'S CARVE-OUT HAS TWO CONJUNCTS AND THE PLAN'S BELOW HAS THREE, WHICH IS A DIFFERENCE AND NOT A RESTATEMENT (round 35, `OP35-1`).** The `PASS` line is absent from the findings path entirely — this section's own rule, stated below, is that on that path there IS no `PASS` line — so its form never has to separate a run that fired findings from one that did not, while the plan prints on BOTH paths and its form does. A `formatFindings` sharing ONE predicate between the two surfaces — the natural body, both firing on the same tree — therefore withholds the plan wherever nothing was compared and nothing ran, findings or not, which is the state `npm run build:clean` leaves. §6 row **36** is the element that fails it, and before round 35 this design had none. ⚠️ **AND THIS LINE'S OWN TWO CONJUNCTS EACH CARRIED AN ACCEPTING ELEMENT AND NO EXCLUDING ONE UNTIL ROUND 36 (`OP36-1`).** Round 35 separated this carve-out from the plan's by ARITY and left its COVERAGE where it found it. Enumerated over §6: exactly THREE elements assert a rendered `PASS` line — row 18 at `M = 0` by EQUALITY, row 39's MAIN half at `M = 1` by EQUALITY, and row 39's NEGATIVE twin in this carve-out's shape — and their conjunct values are (false, false), (false, false) and (true, true), so no fixture in this design held exactly ONE conjunct and a body spelled with either conjunct ALONE agreed with a correct implementation everywhere. Each now carries an EXCLUDING element, and it is the twin round 35 built for the PLAN's matching conjunct, WIDENED rather than duplicated: **no unit was compared** at §6 row 39's ALL-NOT-RUN TWIN, where every unit but `packages/cli` still COMPARES while all four entries read `not-run`; **no comparison ran** at §6 row 39's CACHE-ONLY TWIN, where nothing is compared while `declaration-cache` RUNS and reads `full`. Both assert this line in the ORDINARY shape — parenthetical PRESENT, no `; nothing was checked` — off their own fixtures' counts (`OP16-3`), and the mutant for each is this carve-out with the OTHER conjunct deleted. ⚠️ **THE SUPPRESSION RULE BELOW RIDES THE SAME PREDICATE AND TAKES THE SAME TWO ELEMENTS:** a body suppressing the clause list on a ONE-conjunct test prints `; nothing was checked` and no clause list on a run that compared units, or on one whose declaration cache was read and reported clean — the unearned green line this whole qualification exists to prevent, at `check:all` member 0 and at line 5 of §5.1's measured 204 under flagless `pretest`. ⚠️ **THE CLAUSE LIST IS SUPPRESSED ON THIS LINE AND ON NOTHING ELSE (round 15, OP15-5) — the summary's ONE carve-out, in the shape the ordered plan's own `nothing was checked` exemption already uses.** ⚠️ **SUPPRESSED is not EMPTY, and the rule below now says which is which (round 25, `OP25-2`):** this line is the only place a NON-EMPTY clause list is withheld; where the list is EMPTY — `M = 0`, the steady state after the first post-adoption build — the parenthetical is simply not rendered, on this line and on the findings-path line alike, which is the general shape and not a second carve-out. Without that distinction "ON NOTHING ELSE" reads as mandating `0 skipped ()` on every green run. ⚠️ **THE EXEMPTION IS A SHAPE, NOT THIS LITERAL (round 16, `OP16-3`):** it is `<N> units compared, <M> skipped; nothing was checked` — the rule's general shape with the parenthetical removed — and the `0` and the `19` printed above are THIS repository's live root-reference count on a tree nobody built, re-derived 2026-08-21. That makes the line correct as a PREDICTION about this repo, which is exactly how §5's fresh-clone prediction uses it, and wrong as an ASSERTION off a synthetic fixture: §6 row 39's negative twin asserts the SHAPE, with both counts derived from its own fixture. Until round 15 this literal printed no parenthetical while the rule below mandates `<N> units compared, <M> skipped (<clause>, <clause>, …)` as **ONE shape and never two**, and on the tree this literal describes there IS a clause to print: all 19 units are `skipped` and all share one reason, so the shape-conforming line is `check-stale-dist: PASS — 0 units compared, 19 skipped (e2e/report, packages/cli, …, packages/testkit: no dist/); nothing was checked` — re-derived 2026-08-21 against the live 19 root `references` and the `no dist/` literal at **451** characters, **6** terminal rows at 80 columns and **4** at 120, against **74** and **1** here. Two mandated renderings for the quietest and most frequently reached tree this design has — a fresh clone (§5; NOT a CI job — §5's **THE CHECK HAS ONE CI OCCURRENCE** rule, round 25, `CF25-3`) — and §6 row 39's negative twin runs this exact fixture while asserting only that the plan does NOT print, so no row discriminated them. Suppressed rather than printed, for the same reason the plan is: research Q5 requires a clone nobody built to be quiet, the sentence already says nothing was checked, and naming all 19 units with one shared reason adds no fact. Still exit 0, because
research Q5 requires a fresh clone to be quiet, but never a bare green line.
⚠️ **This state is the ONE carve-out from A DEGRADED RUN CARRIES A NEXT ACTION (round 14,
OP14-1) — standing rule 8, stated at the site of that fix.** All four comparisons are
`not-run` here, so a gate reading "≥1 non-`full` entry" alone would print an ordered
remedy plan telling every fresh clone to `npm run build`:
noise on a tree nobody has built, and a plan under a line that already says nothing was
checked. The plan is therefore withheld when NO unit was compared AND no comparison ran
AND no finding fired, and on nothing else **among DEGRADED runs** — the scope this sentence
left implicit until round 36 (`OP36-3`), and which a body reading the sentence as the GATE's
definition turns into a remedy plan on every healthy tree; every other degraded state keeps
it, and a run that is not degraded at all — zero findings with all four entries `full`, the
steady state after the first post-adoption build — never reaches this gate, which §6 row 38's
CLEAN side now asserts.
⚠️ **THREE CONJUNCTS, AND EACH ONE WAS INDIVIDUALLY DELETABLE UNTIL ROUND 35 (`OP35-1`,
reached by two lenses on this one sentence).** §6 row 39's NEGATIVE twin is this rule's
ACCEPTING element — all three conjuncts HOLD there and the plan must NOT print — and it
was the ONLY element the rule had, while every element asserting that the plan PRINTS has
units compared greater than zero, so all three failed together there too. A
`formatFindings` sharing the `PASS` line's TWO-conjunct carve-out ten lines above, or the
ONE-conjunct 0 units compared, therefore agreed with a correct implementation on every
element in §6, on the false-negative battery, on the wiring assertions and on the live
smoke test. Each conjunct now carries an EXCLUDING element as well — a fixture where that
conjunct ALONE fails, the other two holding, and the plan must PRINT:
**no finding fired** at §6 row **36**, whose unit set is pinned to exactly its two
`unbuilt` units, so nothing is compared, `packages/cli unbuilt` suppresses all four
cli-scoped comparisons (§3.2's REASON PRECEDENCE rank 1, evaluated before any comparison
inspects its own comparand) and two findings fire — the tree `npm run build` then
`npm run build:clean` produces with no manual `rm`, whose skipped and `unbuilt` counts this
section's own `C11-3` paragraph measures and which that element reproduces at TWO units so
it carries no live workspace count (`OP16-3`);
**no comparison ran** at §6 row **39**'s CACHE-ONLY TWIN, where nothing is compared and a
surviving `packages/cli/.dts-cache/index.d.ts` keeps `declaration-cache` RUNNING at `full`;
and **NO unit was compared** at §6 row **39**'s ALL-NOT-RUN TWIN, where `packages/cli`
loses `dist` AND `.dts-cache` while the other units still compare and all four entries read
`not-run`. Withholding the plan on any one of the three leaves a degraded run with NO
stated action — on row 36's tree at exit **1** under `--strict` from `check:all` member 0
and at line 5 of §5.1's measured 204 under flagless `pretest` — which is verbatim
`OP14-1`'s **A DEGRADED RUN CARRIES A NEXT ACTION**, re-opened through a conjunct nothing
read. A conjunctive rule needs BOTH sides of every clause: with the accepting side alone
the clause is deletable, and with the excluding side alone a body that always fails it
passes too.

⚠️ **SKIPPED UNITS ARE NAMED ON BOTH PATHS, because otherwise the design's own remedy leads to a silent failure (round 5; widened to the findings path in round 13, OP13-1).** An operator gets `stale-unit` on `packages/cli`, runs the printed `rm -rf packages/cli/dist`, and `npm run build` then fails — tsc error, Ctrl-C, disk. Re-running the check finds no `dist` at all, which is `skipped`; the other 18 compare clean; it prints **PASS, exit 0** on a tree with no CLI. Since round 8 BOTH routes are silent at unit granularity: `tsc -b --clean` deletes `.tsbuildinfo` along with the outputs, so a `--clean`ed unit holds nothing at all and §3.3's reason table makes it `skipped`, not `unbuilt`; the design's own `rm -rf` leaves no `dist` at all — also `skipped`. `packages/cli` is the one unit that still yields a finding there, and it does so through §3.3's entry-output clause, never through what `dist`'s emptiness says. The `PASS` line is the surface for either route on the no-findings and advisory-only rows of the exit table above — and there it is the ONLY one. ⚠️ **On the findings path there IS no `PASS` line (the exit table prints it on those two rows only, and the prefix rule above says "the `PASS` line when there is one") — ⚠️ **an ABSENCE stated in prose here and asserted by NOTHING until round 36 (`OP36-2`).** §6 row **38**'s rendered half now requires that its two-kind non-advisory output carry NO line beginning `check-stale-dist: PASS`, with a `formatFindings` gating that line on the run not having THROWN as its mutant — `check-citation-runs.mjs`'s own shape, whose `PASS` at `:289-291` sits after all three of its `process.exit(1)` guards and carries no condition of its own. Every §6 site that had SAID this is fixture prose about which row owns which summary slot, and every rendered findings-path element there asserts a line's PRESENCE, so a body printing this line beside the findings passed all of them, the false-negative battery, the wiring assertions and the live smoke test —, so `formatFindings` prints the SAME units-compared/skipped summary as a line of its own, after the grouped finding lines and before the remedy plan (round 13, OP13-1).** Its text is the `PASS` line's slot verbatim, **ONE shape and never two** — `<N> units compared, <M> skipped (<clause>, <clause>, …)`, with EXACTLY ONE exemption, the `nothing was checked` PASS above, where the clause list is suppressed (round 15, OP15-5: unexempted, that line carries a 451-character parenthetical naming all 19 units with one shared reason, on every fresh clone) — ⚠️ **and with the PARENTHETICAL OMITTED WHENEVER THE CLAUSE LIST IS EMPTY, which is `M = 0`: `check-stale-dist: PASS — <N> units compared, 0 skipped` (round 25, `OP25-2`).** An EMPTY list not rendered is not a NON-EMPTY list suppressed, so this is the general shape and NOT a second exemption, and the **THE CLAUSE LIST IS SUPPRESSED ON THIS LINE AND ON NOTHING ELSE** rule above is untouched (standing rule 8) — as is §2's one carve-out clause. It is also the form this section already prints for that state, at the `PASS — 19 units compared, 0 skipped` literal in the coverage paragraph above, so the ruling is the reading already on the page rather than a new one. It needed a ruling because `M = 0` is the STEADY STATE, not an edge: after the first post-adoption build all 19 units compare, none is skipped, no finding fires and all four coverage entries are `full`, so this line is the WHOLE human output of every green run — `check:all` member 0, both `test` legs of `ci.yml:129` (§5) and line 5 of §5.1's measured 204 under `pretest` on every developer's `npm test`. Two implementations followed this section and differed there: a direct transcription of the shape above, `` `${n} units compared, ${m} skipped (${clauses.join(", ")})` ``, prints `0 skipped ()` on every green run — `OP16-4`'s bare-container class at the sibling line, which round 16 thought worth its own mutant at §6 row 38 — while one dropping the parenthetical unlicensed prints a SECOND shape under a rule that says ONE, which is `OP16-3`'s and `CF14-3`'s class at the line `OP21-2` was written to close. §6 row **18** is the element for the `PASS` slot: its fixture holds ZERO skipped units and it now pins this line's `M = 0` TEXT by EQUALITY, with `<N>` derived from THAT fixture and never from this section's live figure (`OP16-3`). ⚠️ **AND §6 ROW 38 IS THE ELEMENT FOR THE STANDALONE SLOT, WHICH HAD NONE (round 26, `OP26-1`).** Row 18's fixture is ADVISORY-ONLY, so it renders a `PASS` line and can never reach the other slot, while row 39's MAIN half runs M=1, row 35 M≥2 and row 34's `M = 0` fixture asserts no summary text at all — so a `formatFindings` carrying the omission on the `PASS` line and a SECOND template for the standalone one printed `check-stale-dist: <N> units compared, 0 skipped ()` on every run holding a finding against a tree whose units are all built, and passed every element in this design. The standalone form is this same text with no `PASS — ` lead-in, and §6 row 38 pins it by EQUALITY off its own fixture. ⚠️ **The parenthetical's reach clause above named a CI job this check never runs in until round 25 (`CF25-3`)** — §5's **THE CHECK HAS ONE CI OCCURRENCE** rule puts its only CI run at `pretest` on `ci.yml:129`, after `ci.yml:86`'s build — and the fresh clone, which reaches this tree on every developer's first `npm test`, is the true and sufficient subject. And ⚠️ **THE SKIPPED SUMMARY GROUPS BY REASON: one clause per DISTINCT `unitState()` reason, written `<units>: <reason>`, the units comma-joined and the reason verbatim (round 14, OP14-2).** With one unit that renders `packages/cli: no dist/` — byte-identical to the `PASS` line's own literal later in this rule and to §2's `states` comment — so this GENERALISES that shape rather than respelling it, and no existing literal moves. ⚠️ **The worked example that stood here fitted NEITHER tree this paragraph names, and offered a second shape under a rule that says "verbatim" (round 14, CF14-3).** It read `check-stale-dist: 18 skipped (packages/cli: no dist/)`. On the `rm -rf packages/cli/dist` tree this rule opens with, the line stated later in this same rule is `18 units compared, 1 skipped` — ONE skipped, not eighteen — and that tree carries no finding, so it can never take this path at all. On the `npm run build:clean` tree the sentence goes on to name, this section's own C11-3 paragraph measures **18 `skipped` units and exactly ONE `unbuilt` — `packages/cli`**, so `packages/cli` is a FINDING there and must not appear in the skipped summary, while the 18 that ARE skipped carry §3.3's reason-table row-1 string and never `no dist/`, which §3.3's unit-state table reserves for the ABSENT-`dist/` case (`tsc -b --clean` leaves the directory). The line on that tree is `check-stale-dist: 0 units compared, 18 skipped (packages/contracts, packages/engine-claude, …: dist/ exists but is empty — a --clean, a test's mkdir, or a manual delete; nothing here proves a build ever ran)`, printed beside the one `unbuilt packages/cli` line. ⚠️ **And the per-unit form is why grouping is a RULE and not a preference.** Re-derived 2026-08-21 against this repository's **19** root `references` and that literal **111**-character reason: one `unit: reason` pair per unit is **2,433** characters — **31** wrapped rows at 80 columns, **21** at 120 — one sentence repeated eighteen times on a single unwrappable line at line **5** of §5.1's measured 204 under `pretest`; grouped it is **512** characters and **7** rows. That is round 1's `O-3` at unit granularity, reinstated by the round-13 fix that put this summary on the findings path, and the rule binds BOTH slots because both render the same text — ⚠️ **with an element on each only since round 21 (`OP21-2`): §6 row 35 for the findings slot and §6 row 39's MAIN half for the `PASS` slot**, before which the `PASS` slot's ordinary rendering, one or more skipped units outside the `nothing was checked` carve-out, was asserted by nothing in this design. An implementer copying the old example emitted, on the only path an operator reads because something is already wrong, a count off by 17, the one unit that is NOT skipped, and a reason no `unitState()` branch returns on that tree — which §6 row 30 forbids the reporter inventing: `OP13-2`'s defect class at the site `OP13-1` created, in the same round. §6 row 35 is the mutant-proved row for both halves. This is the coverage line's own "both paths, never one" rule applied to the other half of the output, and without it the state this section's own `--clean` twin measures above is silent where it matters: after `npm run build:clean` the operator's whole human output is one `unbuilt packages/cli` line, the remedy plan and a coverage line whose four entries are all comparison-scoped, while **18** of 19 units hold zero build output and `states` reaches nobody outside `--json`. §6 row 35 is the mutant-proved row; §3's coverage bullet and §3.1's orphan bullet stop resting on a line that path never prints. On the `rm -rf` tree this rule opens with there is no finding, so it is the `PASS` line that carries the summary, and its text is unchanged: The line reads `check-stale-dist: PASS — 18 units compared, 1 skipped (packages/cli: no dist/)` — a PREDICTION about this repository's 19 root references, asserted as a SHAPE off §6 row 39's MAIN fixture's own counts and never byte-equal to this literal (round 21, `OP21-2`; `OP16-3`'s rule, in the shape that row's NEGATIVE twin already uses for the carve-out form), **followed by the ordered REMEDY PLAN and then the coverage line whenever any comparison is not `full`** (round 14, OP14-1: for thirteen rounds this rendering stopped at the coverage line, so this design's own recipe step 1 printed a `PASS` line, three `not-run` entries and no stated action at all — **A DEGRADED RUN CARRIES A NEXT ACTION** is the gate that closes it, and this paragraph's own tree is its counterexample) — on that exact tree `bundle-freshness`, `plugin-assets` and `shipped-declarations` are all `not-run`, while `declaration-cache` RUNS `full` — `.dts-cache/index.d.ts` survives `rm -rf packages/cli/dist` (§3.3's opening sentence), which is the whole reason it is a separate comparison. Three of FOUR, named individually rather than counted, so a bare PASS there is the silent route this round-5 fix closed for units, still open one level up. Naming skipped UNITS and not skipped COMPARISONS is the same defect at comparison granularity (round 9, O9-1). The parenthetical is `unitState()`'s `reason` printed verbatim, never a sentence the reporter invents — this is the design's only other consumer of that field. THREE tree states reach `skipped` and the operator needs to know which one they are in — two mean no build ever ran, one means their own `rm -rf` did (round 9, CF9-1) — so each carries its own LITERAL `reason`: `no dist/` for the absent case, which §3.3's unit-state table now CARRIES as well as decides, one table ABOVE its reason table — the verdict and the printable string in one cell since round 15 (OP15-2), because until then that table had two columns and no reason at all, so this sentence cited a producer for a literal nothing produced; and `dist/ exists but is empty — a --clean, a test's mkdir, or a manual delete; nothing here proves a build ever ran` plus the scratch row's own sentence, which are the reason table's TWO `skipped` rows. ⚠️ **This sentence said "§3.3's reason table has three `skipped` rows" and that table has two (round 13, OP13-2)**, and the empty case had no printable reason at all — its cell read `indistinguishable — see below` — so this count and §6 row 30's "three DISTINCT reasons" were both unsatisfiable from the table they cite, and the one reason a reporter could not invent was the one the design had not written.

⚠️ **The internal-error row changed in round 1, and the reason is measured.** It was
exit **2** in both columns. On npm 11.16.0 a `pretest` exiting **2** blocks `npm test`
exactly as exit 1 does — verified: `TEST RAN` never prints. So the "`pretest` is
non-strict, therefore it cannot block testing" argument did **not** cover the check's
own errors, and §3's skip rule made that path reachable from this design's OWN advice:
`rm -rf packages/cli/dist` and the bundle comparison has nothing to stat. Following the
documented remedy would have left the operator unable to run the suite at all.
Non-strict now degrades an internal error to a warning and exits 0 — which holds only if
something CATCHES. Re-derived 2026-08-20 (npm 11.16.0, node v24.18.0): an UNCAUGHT throw in
`pretest` exits npm **1** and blocks `npm test` exactly as exit 1 does, so round 1's `O-2`
is re-openable by omission alone. That is why the row above now names its owner and its
text.

Report-by-default with `--strict` on the chained entry is `check-claim-scope.mjs`'s
shape verbatim (`:25-27`; `check:claim-scope` is `--strict` at `package.json:44`).
⚠️ **It does NOT matter "because a false positive now blocks a push", and every site that
priced a finding that way was contradicted by §5 of this same document (round 17,
OP17-1).** §5's **The check's discriminating power is local** measures `check:all` as
invoked by no workflow and no hook, and §5's own bullets put `core.hooksPath`'s `pre-push`
at `lint typecheck build test` with `build` immediately before `test`, so the non-strict
`pretest` can only ever see clean there and `--strict` never executes on a push at all.
What the split buys is attributable severity on the one chain a human runs deliberately: an
ad-hoc local run stays non-punitive, and only the chained invocation fails.

Exit **2** is produced by `check-stale-dist.mjs`'s catch (§1), never by `exitCodeFor`,
whose range is therefore `0 | 1` (§2). It is a NEW convention in this repository —
`grep -rn "exit(2)" scripts/` returns zero hits today. It is worth introducing:
"your dist is stale" and "the check broke" must not share an exit code on the one chain a
human runs deliberately: this check is `check:all`'s member 0, so a non-zero exit stops the
other 14 before any of them prints, and the code is the only attribution the operator gets.
⚠️ **The warrant read "when the check gates a push" for sixteen rounds and no such gate
exists (round 17, OP17-1).** It is restated rather than withdrawn, because the attribution
argument survives the measurement — it is §5's own reason for putting this check at index 0
— while the push argument does not.

## 5. Wiring — requirement 10

- `"check:stale-dist": "node scripts/check-stale-dist.mjs"` in `package.json` — no `--strict` in the definition itself (§5.1).
- Chained into `check:all` as the **first** member, verbatim `npm run check:stale-dist -- --strict`, making it the **15th**
  (`node -e 'console.log(require("./package.json").scripts["check:all"].split("&&").length)'` → 14 today; `check:tarball` is member 12 and `check:install-smoke` member 13, so _appending_ puts this check after both).
  First, not last, because `check:tarball` and `check:install-smoke` already read local
  `dist` state and returned three different verdicts in one session for that reason
  (`docs/evidence/criteria-closeout/defects/25-install-smoke-depends-on-local-dist-state.md`);
  running this first turns their ambiguity into a named precondition. ⚠️ **Index 0 in an
  `&&` chain is also what makes §4's internal-error row load-bearing**: any non-zero exit
  here stops the other **14** members from running at all (re-derived 2026-08-20:
  `check:all` splits on `&&` into 14 members today), so an uncaught throw costs the whole
  of `check:all` and prints no attributable line. That is the cost the row's `WARN`/exit-0
  default avoids, and why §4's `ERROR` line tells the operator — CONDITIONALLY, because
  the check cannot observe its caller, and as a SET rather than a count, because this very
  block records the member figure drifting three times — that the members after it did not
  run (round 14, OP14-4). Leaving this sentence at "the other 14" would have made §5 assert
  the literal §4 had just deleted, in the same commit.
- ⚠️ **§6 pins both by index, not by substring (CF9-3).** `expect(members[0]).toBe("npm run check:stale-dist -- --strict")`. A `toContain("check:stale-dist")` — the shape of the two checks §6 cites — is satisfied by a member appended last with no flag, which runs after the two members this bullet orders it before and exits 0 on a stale tree: every assertion green, the wiring's entire value gone.

⚠️ **What this wiring does and does not buy, measured rather than assumed.**
`npm run check:all` is invoked by **no** workflow and **no** hook. Measured — the WORKFLOW
half by the two commands below, the HOOK half by this section's `core.hooksPath` bullet
further down and by round 17's `OP17-1` probe, because `git ls-files` enumerates TRACKED
files and a git hook is never one (round 29, `CF29-1`):

```
git ls-files -z | xargs -0 grep -n "run check:all" | grep -v stale-dist-check-design   # 6 hits (2026-08-20), ALL in docs/evidence prose
      # THIS design file is excluded BY CONSTRUCTION, and NO unfiltered figure is stated
   # (round 12, C11-4). It is unmaintainable: round 11 wrote "9" while the true value was
   # 10, because the very comment it added to explain the drift quoted the string a
   # FOURTH time — the third recurrence of round 1's own O-4, one round after the second,
   # and again at the site the fixing round created. Any sentence explaining the drift
   # re-triggers it, so the sentence is gone rather than corrected. Re-derived
   # 2026-08-20: filtered 6, all in docs/evidence prose. ⚠️ The 6 carry the WORKFLOW
   # half of the claim above and NOT the hook half, and this comment read "entirely"
   # for twelve rounds (round 29, CF29-1). `git ls-files` enumerates files TRACKED IN
   # THIS REPOSITORY and no git hook is one: re-derived read-only 2026-08-22 at
   # c672565, clean tree before and after, `git config core.hooksPath` resolves to
   # `/home/eimi/.codex/git-hooks` — OUTSIDE this repository — the default
   # `.git/hooks/` is untracked as well, and `git ls-files | grep -i hook` returns **17**
   # paths — 14 `.ts`/`.mjs` SOURCES plus 3 `.json` — while
   # `git ls-files | grep -iE '(^|/)(pre-commit|pre-push|commit-msg)$'` returns **0**, so
   # not one of them is a hook SCRIPT. So NO spelling of `git ls-files`
   # can return evidence about a hook on any machine: add `npm run check:all` to that
   # `pre-commit` and this command still returns 6 while the sentence above goes false.
   # The hook half is carried by this section's `core.hooksPath` bullet below and by
   # round 17's OP17-1 probe — re-derived unchanged today, `pre-push` loops
   # `for script_name in lint typecheck build test` and names `check:all` nowhere, and
   # `pre-commit` runs no `npm run` at all — and it is a PER-CLONE fact about this
   # machine rather than a property of this repository. Standing rule 1 applied to a
   # MEASUREMENT: measured over tracked files, claimed over hooks. Round 25's CF25-3
   # drew only the workflow conclusion from this same 6, which is the half it carries.
git ls-files -z | xargs -0 grep -ln "check:all" | grep -v ^docs/ | wc -l   # 9 files: the
      # definition at package.json:45, 6 prose comments, 2 toContain assertions — no
   # invocation. Re-derived 2026-08-20 (round 13, CF13-7). Comment-only FILES, six:
   # .github/workflows/ci.yml (:263, :385), scripts/repo-census.mjs:11,
   # scripts/check-claim-scope.mjs:11, scripts/check-claim-scope.test.mjs:12,
   # scripts/check-published-tarball.test.mjs:21, scripts/repo-census.test.mjs:8.
   # Assertion FILES, two: check-marketplace-pin-digest.test.mjs:339 and
   # check-support-window-freshness.test.mjs:530, both
   # expect(root.scripts["check:all"]).toContain(...) -- the exact shape section 1 calls
   # "membership only, blind to position and to flags". Round 12 wrote "4 and 4" at the
   # commit that DELETED the unfiltered figure for having drifted three times, which
   # makes this the fourth recurrence of round 1's O-4 in this same block, and it
   # overstated by 2x how much of check:all's membership any test pins -- in the section
   # arguing that section 6 must pin member 0 by index and by equality.
```

CI runs THIRTEEN of `check:all`'s fourteen EXISTING members as individual steps, across
THREE jobs — `ci.yml:233` `engine-pin-lint` (`check:engine-pin`, `ci.yml:244`), `ci.yml:246`
`meta-checks` (ten members) and `ci.yml:403` `packaging` (`check:tarball`,
`check:install-smoke`) — never `check:all` itself, so adding a member to
it adds NO CI step, and §1 edits `scripts/bundle-cli.mjs`, `.gitignore` and `package.json`
and carries no `ci.yml` row. ⚠️ **`EXISTING members … in two jobs` was a UNIVERSAL over all
fourteen, backed by no measurement, and wider than the tree in TWO independent ways (round 30,
`CF30-1`).** Re-derived read-only 2026-08-22 at `187eb2d`, `git status --porcelain` empty
before and after: `grep -rn "check:claim-scope" .github/` returns NOTHING, so
`check:claim-scope` — §4's own report-by-default precedent, `--strict` at `package.json:44` —
is a `check:all` member with no CI step at all, which makes that state ALREADY this
repository's normal case rather than something this design introduces; and
`grep -n "npm run check:engine-pin" .github/workflows/ci.yml` returns `:244`, inside
`engine-pin-lint` (`:233`) and not inside either named job — a THIRD job whose header the
re-derivation below ENUMERATES while mapping no step onto it, standing rule 4 at the
granularity of jobs versus steps. The conclusion this sentence carries is unchanged and never
rested on the universal: no workflow runs `check:all` at all (the repo-wide census above), so
no member gains a CI step by being added to it. ⚠️ **THE CHECK HAS ONE CI OCCURRENCE (round 25, `CF25-3`):**
`pretest` on `ci.yml:129`'s `npm test`, twice per push in the two-leg `test` matrix, AFTER
`ci.yml:86`'s build — so in CI it can only ever report clean, which is verbatim what §8(c)
already says and what §5.1's opening already measured ("they were told that `check:all`
runs in CI and in every pre-push. It runs in neither"). Re-derived read-only at `7837d25`,
working tree clean before and after: `grep -n "npm test" .github/workflows/ci.yml` returns
exactly ONE run line, `:129`, and `grep -nE '^  [a-z-]*:$'` returns EIGHT headers — `push:`
4 (the workflow trigger), `lint:` 13, `typecheck:` 30, `test:` 49, `commitlint:` 196,
`engine-pin-lint:` 233, `meta-checks:` 246, `packaging:` 403 — which put both `:129` and
`:86`'s `npm run build` inside `test:`; `meta-checks` and `packaging` each run `npm ci` and
then individual `npm run check:*` steps and never `npm test`, so `pretest` cannot fire in
either. ⚠️ **THAT GREP READS ONE FILE AND THE RULE ABOVE QUANTIFIES OVER CI (round 30,
`CF30-2`).** `.github/workflows/` holds TEN workflow files; a `pretest` firing in any of the
other NINE would falsify **THE CHECK HAS ONE CI OCCURRENCE**, §8(c)'s "it can only ever report
clean" and the fresh-clone re-attribution `CF25-3` made — and no spelling of a command scoped
to `ci.yml` can see it. Re-derived read-only 2026-08-22 at `187eb2d`, `git status --porcelain`
empty before and after, over the DIRECTORY rather than over one file: `grep -rn "npm test"
.github/workflows/` returns exactly ONE `run:` line, `ci.yml:129` (the other hits are prose in
`journal-crash-suite.yml` and `gates-conformance.yml`); `grep -rn "run: npm run test"
.github/workflows/` returns `engine-live.yml:81` (`test:live`) and `release-e2e.yml:389`
(`test:e2e:release-evidence`), which npm's NAME-EXACT pre-hook (§5.1) sends to `pretest:live`
and `pretest:e2e:release-evidence`, neither declared; every remaining test-running step there
is either an `npx vitest run` — §5.1's own bypass set instantiated in CI, invoking no npm
script and so firing no pre-hook — or `jira-datacenter-smoke.yml:28`'s
`docker/jira-datacenter/smoke-test.sh`, in which `grep -n npm` returns NOTHING; and the one
reusable-workflow call, `publish.yml:64`
`uses: ./.github/workflows/release-e2e.yml`, carries no `npm test` either, so none reaches CI
through `workflow_call`. The rule stands as written; what it lacked was a measurement as wide
as itself. `core.hooksPath` here points at a `pre-push` that runs `lint typecheck build test`.
Consequences over the THREE member-holding jobs the paragraph above names, plus the
`pre-push` hook and `ci.yml:129` below, stated so nobody re-derives them — ⚠️ **and this
list enumerated TWO of the three jobs until round 30 (`CF30-1`)**: `engine-pin-lint` is
`meta-checks`'s shape, `npm ci` with no build, carrying one member's step in place of ten,
so the first bullet's verdict is its verdict and nothing below moves:

- in `meta-checks` (`npm ci`, no build) THIS member has **no step at all**, so nothing it could print prints there, on this push or any other (round 25, `CF25-3`). The OUTPUT PREDICTION below is KEPT and re-attributed to the tree that actually reaches the check — **a fresh clone: `npm ci`, then `npm test`, with no build in between** — which is the tree §6 row 39's negative twin fixtures and what makes §4's carve-outs load-bearing. There every unit reports `skipped`, each of the four cli-scoped entries carrying the one reason its own section's precedence mandates — `packages/cli/dist absent` for `bundle-freshness` (§3.2 rank 2, outranking the equally true `.bundle-meta/metafile.json absent`) and for `plugin-assets` (§3.4 rank 2), `packages/cli/.dts-cache/index.d.ts absent` for `declaration-cache` (§3.3's per-comparison skip target), and `packages/cli/dist/index.d.ts absent` for `shipped-declarations` (§3.5's `C17-3` both-absent precedence) — asserted since round 18 by §6 row 39's negative twin (`C18-2`), which runs this exact fixture — no `dist` exists — and all FOUR cli-scoped comparisons `not-run` — `bundle-freshness`, `declaration-cache`, `plugin-assets` and `shipped-declarations`, since `npm ci` creates neither `dist` nor the gitignored `.dts-cache` — so what prints there is the qualified `check-stale-dist: PASS — 0 units compared, 19 skipped; nothing was checked` — clause list suppressed, §4's one exemption from **THE SKIPPED SUMMARY GROUPS BY REASON** (round 15, OP15-5), without which that line names all 19 units at 451 characters — plus §4's coverage line, and **NO separate units-compared/skipped line**, that summary being in the `PASS` line's own slot here and in exactly one of its two positions per run (round 21, `OP21-2`; §6 row 39's MAIN half is the element, without which a formatter emitting the standalone line beside the `PASS` line re-prints `OP15-5`'s suppressed 451-character clause list on every such clone) — and **NO remedy plan**, this being the one state §4 carves out of **A DEGRADED RUN CARRIES A NEXT ACTION** (round 14, OP14-1): four `not-run` entries on a tree nobody built would otherwise print an ordered plan instructing a fresh clone to `npm run build`, which research Q5 requires to stay quiet — never a bare green line;
- in `packaging` (`npm ci`, then `npm run build`, then `check:tarball` and
  `check:install-smoke` as individual steps) this member likewise has no step; were one
  added it could only ever report clean, the build running immediately before it;
- the `pre-push` hook rebuilds before testing, so it too can only see clean;
- **`ci.yml:129` (`run: npm test`) DOES run it** — the two-leg `test` matrix
  (`ubuntu-latest`, `ubuntu-24.04-arm`), on every push to `main` and every PR — so with
  §5.1's `pretest` the check fires **twice per push**. `ci.yml:86` builds immediately
  before, so it reports clean every time. This bullet was missing for two rounds while
  §8(c) asserted the check could not fire in CI at all.

**The check's discriminating power is local.** A smaller claim than "runs in CI and
in every pre-push", and the one the repository supports today. A dedicated CI step is
deliberately NOT proposed: it could only be permanently green, and a permanently green
step is a check muted in advance.

### 5.1 `pretest` — owner ruling 2026-08-20, amending requirement 10

⚠️ **The original ruling rested on a false premise, which this design measured and
which was taken back to the owner.** They were told that `check:all` "runs in CI and
in every pre-push". It runs in neither. Re-ruled with the corrected facts:

```json
"pretest": "node scripts/check-stale-dist.mjs",
"check:stale-dist": "node scripts/check-stale-dist.mjs"
```

⚠️ **`check:stale-dist` carries NO `--strict` in its own definition.** §5 chains it as
`npm run check:stale-dist -- --strict`, so declaring it strict here would contradict
§4's "an ad-hoc local run stays non-punitive, and only the chained invocation fails" AND
pass `--strict --strict` from `check:all`. An earlier draft declared it strict; that was
a round-3 finding.

`pretest` is the ONLY trigger in this repository that can fire on a stale tree. It runs
immediately before `npm test`, which is the exact moment the founding incident bit —

⚠️ **BUT IT DOES NOT PRINT "IMMEDIATELY ABOVE" THE FAILURES, AND AN EARLIER DRAFT
CLAIMED IT DID.** `pretest` completes before `vitest` starts, and `vitest run` prints
failure detail and its summary at the END. Measured with an 83-failure suite — the
founding incident's own count — and a one-line warning: **204 lines of output, the
warning at line 5**, so ~199 lines of scrollback separate the explanation from the place
an operator reads. Real failures carry diffs and stacks, so the true gap is larger. The
founding incident WAS two hours of misdiagnosis by someone reading the bottom of the
output.

The reliable form is a vitest reporter or `globalSetup` that re-emits at the end;
`posttest` does not run when `test` fails. Until that element exists, the limit is
stated here rather than in the round log: **the warning can scroll away — and so does §4's coverage line, which prints on the PASS path and is therefore the ONLY notice a degraded run ever gives.** A finding at least repeats on the next run; a coverage entry on a passing tree is one line, once.

83 failures from a `dist` that predated its source, misdiagnosed for two hours because
nothing said so.

**It runs WITHOUT `--strict`, and that is load-bearing.** Measured on npm 11.16.0:

```
pretest exits 0  ->  PRETEST RAN / TEST RAN
pretest exits 1  ->  PRETEST FAILED, and `test` NEVER RUNS
pretest exits 2  ->  npm exits 2,  and `test` NEVER RUNS
pretest THROWS   ->  npm exits 1,  and `test` NEVER RUNS   <- the UNCAUGHT case (§4)
```

So a strict `pretest` turns any false positive into a total block on local testing — **and
so does an uncaught throw, `--strict` or not**, which is the whole reason §4's
internal-error row must have an owner in §2 rather than being prose. ⚠️ **And an owner in
§2 is not enough on its own (round 12, OP12-1):** a catch covers only what is LOADED inside
it, so §1's IMPORT RULE — the entry module reaching `compare.mjs` and `report.mjs` through
`await import(...)` inside the try — is what puts the six-module graph in that catch's
scope. Measured on the design's own import shape, a STATIC entry import of a module that
throws at load exits **1** with no `check-stale-dist: ` line, which is this table's fourth
row and a total block on `npm test`. Re-derived 2026-08-20
in a scratchpad package (npm 11.16.0, node v24.18.0, nothing else running but the shell):
0 → npm 0 with the suite running; 1 → npm 1; 2 → npm 2; uncaught throw → npm 1; the suite
never ran in the last three. The
check therefore reports and continues there — which rests on TWO properties and had an
element for only one of them until round 19 (`CF19-1`). The first is that
`check-stale-dist.mjs` PASSES `exitCodeFor` the `strict` it parsed rather than a hardcoded
`true`; that is a PROCESS property, and it is the one this paragraph's claim is actually
about, `pretest` being `node scripts/check-stale-dist.mjs` with no flag. §6 row 21 CALLS
the function directly, and rows 27 and 31 — the only §6 elements that run the CLI at all —
neither run it on a findings-carrying tree: row 27's fixture is a malformed config, every
exit code row 31 asserts is its LOAD-TIME mutant's, and row 31's clean baseline has ZERO
findings, where exit 0 is correct under either `strict`. §6 row **43**'s CLI twin closes
it: the flagless process on that row's own findings-carrying end state at exit **0** with
the `stale-declarations` line still rendered, and `--strict` at exit **1** on the same
fixture. The second is `exitCodeFor` honouring its
`strict` argument, so that this table's second row is unreachable from a finding; §6 row 21
asserts the non-strict column on both of its fixtures since round 18 (`OP18-1`), an
`exitCodeFor` that ignored the argument having exited **1** here on the four `stale-unit`
findings §6 predicts for this working copy — so that a false positive cannot block local
testing. ⚠️ **It does NOT print "immediately above the failures", and this sentence said it
did for nine rounds after the paragraph above measured it false (round 12, OP12-3).** The
warning lands ~199 lines above vitest's summary and can scroll away — which is where the
founding incident's two hours of misdiagnosis happened, at the top of scrollback, read from
the bottom; §4's coverage line has it worse, being one line, once, on a passing tree.
"Immediately above" is a property of the reporter or `globalSetup` re-emit named above as
the future element, never of the `pretest` wiring. Rounds 2 (`O-A`) and 3 (`OP-4`) both
filed this and both fixes reached the measuring paragraph and not the argument resting on
it. `--strict` is
reserved for `check:all`, where a wrong verdict costs a re-run rather than a working
session.

**Honest bound — the measured set is FIVE, not the one this line named for twelve rounds
(round 12, OP12-2).** npm's pre-hook is NAME-EXACT. Re-derived 2026-08-20 (npm 11.16.0) in
a scratchpad package declaring `pretest`, `test`, `test:watch` and `test:e2e`: `npm test`
printed `PRETEST RAN / TEST RAN`, while `npm run test:watch` and `npm run test:e2e` each
printed their own line ALONE — npm looks for `pretest:watch`, not `pretest`. ⚠️ **What that
run covers, stated AT the run (round 30, `CF30-3`): TWO of the four siblings, and the measured
property is the NAME-EXACTNESS rather than the four names.** `test:live` and
`test:e2e:release-evidence` are bypass members by that property, and `npx vitest` by invoking
no npm script at all — a KIND no npm pre-hook run can reach. This
repository declares **four** siblings beside `test`: `test:watch` = `vitest`
(`package.json:20`), `test:live` (`:21`), `test:e2e` (`:22`) and
`test:e2e:release-evidence` (`:23`). So the bypass set is those four plus `npx vitest`.
This catches `npm test` and nothing else — and `test:watch` is the vitest WATCH loop, the
developer's inner loop and precisely the founding incident's context, so a developer who
lives there never sees the check at all. Combined with §5's measurement that no workflow
and no hook invokes `check:all`, the honest reach of this design is `npm test`.
⚠️ **Extending it is a NAMED future element, not silently taken here**, in the shape the
reporter re-emit above already uses: add `pretest:watch`, `pretest:live` and `pretest:e2e`
to §1's `package.json` row and to this section's JSON block, each pinned by equality in
CF9-3's shape, since a membership-only assertion is what let the last `package.json` wiring
gap through. ⚠️ **Those THREE names reach THREE of the four declared siblings and can never
reach the fifth bypass member (round 30, `CF30-3`)**: `test:e2e:release-evidence` would need
a fourth, `pretest:e2e:release-evidence`, which this list does not carry, and `npx vitest`
invokes no npm script at all, so no `pre*` hook of any spelling fires for it — the one
member of the five no extension of this wiring can close, and the reason "extending it" is
not the same as closing the bound measured above. It is not taken here because §5.1's ruling is the owner's and names `pretest`;
what this line owed was the measurement.

## 6. Testing, and how non-vacuity is proven

Fixtures are synthetic trees under `mkdtempSync(tmpdir())`, for the reason
`repo-census.test.mjs:17-20` states: asserting against the real repository makes a
test restate today's file list and fail on every unrelated addition.

**Non-vacuity battery.** Per finding kind, **per unit state and per coverage status**: one
fixture reporting clean, then ONE `utimesSync`/`rm`/`writeFileSync` mutation that must flip
it to the stated verdict — or, for the RENDERED-TEXT rows — 16-18, 26-36, 38 and 39, plus the rendered TWIN halves rows 8, 20, 21, 42, 43 and 46 carry beside a return-value main half (round 18, `OP18-3`/`OP18-4`; rows 20 and 43 are round 19's, `OP19-1` and `CF19-1`, and row 8 is round 20's, `OP20-1`) — that must make the check SAY what it stopped
covering. (`writeFileSync` is row 22's alone: §3.5 keys on bytes, not mtime, so a
`utimesSync` mutation there would prove nothing.)

Three rules the rows below carry, each earned from a defect a green battery passed:

- **Rows 16-18, 26-36, 38 and 39 assert `formatFindings`' RENDERED text, never the return value, in every assertion of theirs whose subject is what the reporter PRINTS — the subject rule below governs per ASSERTION and not per row, which is what puts row **39**'s round-27 exit-code conjunct outside this group (`OP27-1`) — and so do the TWIN halves of rows 8, 20, 21, 42, 43 and 46, which keep a return-value main half (round 18, `OP18-3`/`OP18-4`; row 20's twin is round 19's, `OP19-1`, and pins §3.3's `bundle:cli did not finish` reason, which no element in this section rendered before it — row 32 pinned the `--clean` spelling alone, so a `unitState` collapsing both onto it passed every cli-`unbuilt` row; row 43's is round 19's `CF19-1`, and reads the CLI's stdout AND its exit status, the surface rows 21 and 27 each pin only half of; row 8's is round 20's `OP20-1`, and pins `check-stale-dist: stale-plugin-assets ×1 — packages/cli`, the last of §4's four grouped kinds that no element here rendered — rows 8 and 24 are that kind's only rows and both read the RETURN value, so the grouped shape was mandated for it and asserted nowhere).** Membership is per HALF, by RULE: a half is in this group when its subject is what the reporter PRINTS. Rows **38** and **39** are round 14's, both in row 34's MUTANT-FORMATTER shape; row **37** is deliberately NOT in this group, its subject being a `unitState` verdict, for which the return value is the only surface (the reason rows 12 and 23 are out too). Round 15's four are all out for one of those two reasons: row **40** is a DIRECT-CALL row in rows 12 and 23's shape, and rows **41**, **42** and **43** are verdict rows in row 37's shape — row **43** keeping that return-value main half while the CLI twin round 19 gave it (`CF19-1`) is in this group, membership being per HALF and never per row. ⚠️ **Except that row 42 carries a rendered PLAN TWIN as of round 18 (`OP18-3`), and so does row 21, which is an `exitCodeFor` row and was in no list here at all. ⚠️ **And rows 20 and 43 carry twins as of round 19 (`OP19-1`, `CF19-1`), in this group for the same per-half reason:** row 20's asserts by EQUALITY the RENDERED `unbuilt packages/cli — bundle:cli did not finish; missing: dist/plugin/skills` line on that row's own end state, the second of the TWO GAPS-NAMED sentences §3.3's cli reason table mandates and the one nothing rendered before it — ⚠️ **TWO of its THREE cli `unbuilt` reasons, the count taken over the gaps-named rows and never over the table (round 29, `OP29-1`; `C11-4`'s remedy: state the branch, never a count the table refutes)**: the third is clause 2's, whose row reads `the offending file named` and whose gap list §2's third branch leaves EMPTY, and while this bullet counted TWO the enumeration of which cli `unbuilt` sentences are RENDERED structurally could not see the one that was not. Row **26**'s MAIN half is now that third sentence's element; row 43's runs `check-stale-dist.mjs` as a PROCESS against that row's own end state over the SET of flag settings {none, `--strict`, `--json` FLAGLESS, `--json --strict`} — the last of the four added in round 29 (`OP29-2`) and the third strengthened with an exit code there, this enumeration having read THREE while the twin's `--json` surface carried no code at all — so what it inspects is what the reporter PRINTS on BOTH of its surfaces plus the code the CLI returns on BOTH of them.** Membership is per HALF and never per row: a half is in this group when its subject is what the reporter PRINTS, and out of it when the return value is the only surface. Both rows keep a return-value main half and take a MUTANT FORMATTER for the twin. Round 16's two are out for the same two reasons: row **44** is a verdict row in row 37's shape — its subject is `checkUnitFreshness`'s and `checkOrphans`' return over one fixture — and row **45** is a DIRECT-CALL row in rows 40 and 41's shape, its mutation column calling `checkBundleFreshness` directly. Round 17's three are out for the same two reasons — ⚠️ **except for row 46's rendered PLAN TWIN, added in round 18 (`OP18-4`), its fixture being the only one in this battery that reaches §3.2's chunkless `not-run` reason at ZERO findings.** Row **46** is otherwise a DIRECT-CALL row in rows 12, 23 and 40's shape, its mutation column calling `checkBundleFreshness` and `checkPluginAssets` directly because §3.2's and §3.4's not-run causes are reached by one fixture; and rows **47** and **48** are verdict rows in rows 37's and 44's shape — their subject is `checkUnitFreshness`'s return over one fixture and the input SET feeding it, not the reporter, the rendering of the grouped `stale-unit` line being row 28's subject and already pinned by equality (`OP15-1`). Row **2**, whose verdict round 16 tightened from `×N` to an equality on `Unit.dir` (`CF16-3`), was never in this group and does not join it: the RENDERING of the grouped `stale-unit` line is row 28's subject and is already pinned by equality (`OP15-1`).
  Rows 16-18 and 26 are the COVERAGE side of that group; rows 27-30 are the FINDING and
  internal-error side, added in round 11; rows 31-34 are round 12's — the LOAD-TIME
  internal error (OP12-1), the `--clean` reason (C11-3), the chunk SET (C11-6) and §4's
  ordered remedy plan (OP12-4), of which the first and last are surfaces §4 mandates that
  nothing in this section read. Re-derived: `formatFindings` occurred exactly
  TWICE in this design — §2's signature and this bullet — so every rendered assertion in §6
  concerned a coverage entry or the prefix, and §4's per-kind line shapes, its grouped
  `stale-unit ×N — <units>` line and its `WARN`/`ERROR` lines were proved by NOTHING — and its
  ordered REMEDY PLAN still was, until round 12 (OP12-4). Rows
  28-30, 34, 35, 36 and 39 take a MUTANT FORMATTER as their single mutation rather than a filesystem change, and so do the rendered PLAN TWINS of rows 21 and 42 (round 18, `OP18-3`) and of row 46 (round 18, `OP18-4`), and so does row 8's RENDERED TWIN (round 20, `OP20-1`) — whose mutant is a `formatFindings` whose grouped-kind SET omits `stale-plugin-assets`, NOT one that collapses at N≥2 alone, that second mutant being already dead at rows 28, 38 and 43's CLI twin. ⚠️ **"every rendered TWIN" stood here for one round and was false at the very next append (round 19, `CF19-1`/`C19-1`/`OP19-1`):** row 43's twin takes a PROCESS RUN of `check-stale-dist.mjs` at the flag settings {none, `--strict`, `--json` FLAGLESS, `--json --strict`} — FOUR since round 29 (`OP29-2`), and stated as the SET rather than as a count because this very sentence carried THREE from round 22 to round 29, which is the class the next clause names, row 20's takes that row's OWN `rm -rf packages/cli/dist/plugin/skills`, and row 26's takes five deletions on its own fixture — not one of the three is a mutant formatter, and an implementer building any of them from this list would swap one in and prove nothing about `exitCodeFor`'s caller, about `unitState`'s reason or about §3.2's rank 1. A universal over a list every appending round re-invalidates is `C11-4`'s class at a universal rather than at a count; the rule stated in the next sentence governs, and it already says the mutation is read off the row. WHICH mutation a row in this group takes is read off that row's OWN mutation column and never from a list here (round 18, `CF18-2`/`OP18-3`; `CF17-2`'s remedy shape at the sibling enumeration, and a list every amending round re-invalidates): rows 37 and 38 are the two whose mutation column is a filesystem `utimesSync`, row 38 carrying its mutant formatters as ADDITIONAL halves —
  because what they pin is what the reporter prints, and the rule stated one line above —
  that a row reading a return value goes green on the printing defect — applies to findings
  exactly as it does to coverage. O9-1 was a
  coverage value computed and never printed, so a row reading `result.coverage` goes green
  on the exact defect it exists to catch (round 9). ⚠️ **A row whose mutation column CALLS a comparison directly is out of this group by RULE
  and never by enumeration (round 17, `CF17-2`):** there is no rendered output to read and
  the return value is the only surface there is. It read "rows 12, 23 and 40" while this
  same bullet says one sentence above that round 16's row **45** calls
  `checkBundleFreshness` directly, and round 17's row **46** calls two comparisons on one
  fixture — `CF14-4`'s "never restate a subset" at this section's own bookkeeping, and a
  list every appending round re-invalidates. Stated as the rule, no future append can make
  it a subset again. ⚠️ **AND a TWIN is in this group by what IT asserts, never by its
  ROW's number (round 20, `C20-2`).** Rows 42, 21 AND 28 each now carry TWO — at 42 the round-18
  PLAN TWIN, which reads rendered text, and the round-20 SCOPE TWIN, which is a
  return-value row; at 21 the round-18 PLAN TWIN, which reads rendered text, and the
  round-23 EXIT-CODE MIX TWIN, which is a return-value row (round 23, `OP23-1`); and at 28
  the round-15 N=1 TWIN and the round-25 PLAN TWIN (`OP25-1`), BOTH of which read rendered
  text —
  so "the TWIN halves of rows 8, 20, 21, 42, 43 and 46" above and in the paragraph opening
  this section name the PLAN twin at BOTH 42 and 21, and any citation to "row 42's twin",
  to "row 21's twin" or to "row 28's twin" must say which. ⚠️ **Row 28's PLAN TWIN does NOT
  join that "TWIN halves" list (round 25, `OP25-1`):** that list names rows whose MAIN half
  is a return-value row, and row 28's main half is rendered text and already in this group
  by number — membership per HALF, and the list is a different question from the group.
  The five sites reading "row 28's twin's" are re-pointed to "row 28's N=1 twin's" in the
  same round, at §2's grouped-literal closure, §6 row 8's RENDERED TWIN, §6 row 38 twice
  and §9's round-20 entry. Row 44's round-20 twin (`C20-1`) is a return-value half and is in neither
  list for the same reason. ⚠️ **Rows 5, 6 and 24's EQUAL-MTIME TWINS are three more, by
  that same rule (round 36, `C36-1`):** each asserts the ABSENCE of ONE finding kind on its
  host row's own clean fixture, which is a return value and not what the reporter PRINTS, so
  none of the three joins this group or that list. Row **5** thereby carries TWO twins and
  falls under the must-say-WHICH clause above. Re-derived read-only 2026-08-22 at `d0b706e`
  with WHITESPACE COLLAPSED FIRST, because a per-LINE search misses a citation split across a
  line break and one of these is (standing rule 1): the citations naming row 5's TWIN without
  saying WHICH are TWO, both live — §2's `isCompilerOutput` comment and §3's
  minus-bullet-attribution paragraph — and BOTH are re-pointed to the ROUND-23 twin in this
  same round. §0's requirement-1 row, §3's other citation and §3.2's Rule already read
  `round-23 TWIN`, and §9's are historical and left as written. Rows **6** and **24** each
  carry their FIRST, so no citation of theirs is ambiguous and neither is appended to that
  clause's list, which is a RULE and not a census (`OP12-6`; round 35's own precedent at row
  39's fourth twin). Stated as the rule because the sentence above is: a row may
  acquire a second twin in any later round, and an enumeration keyed on row numbers is
  `C11-4`'s class one level in. ⚠️ **And every
  assertion in this group includes §4's `check-stale-dist: ` prefix — row 18 on the PASS
  line and the findings list at once (its fixture is advisory-only, the one state §4's exit
  table renders both) — ⚠️ **and since round 24 (`OP24-1`) row 18 ALSO pins the ORDER of
  those two, the `PASS` line's rendered index STRICTLY LESS THAN the finding line's so that
  the `PASS` line prints FIRST: §4's ORDER rule's FIRST adjacency, the one of its four no
  element in this section read while rows 35 and 39 covered the other three, each of them
  on a fixture holding either no `PASS` line or no finding line** — and row 39's MAIN half on the PASS line by EQUALITY, in §4's full
  shape with a skipped unit NAMED and its `unitState()` reason beside it (round 21,
  `OP21-2`; row 18's fixture holds zero skipped units, so the prefix was all it could pin of
  that line's TEXT until round 25 — round 24's conjunct pinned its POSITION and still not its shape, and round 25 (`OP25-2`) gives that row the `M = 0` rendering by EQUALITY, `<N> units compared, 0 skipped` with NO parenthetical and no empty pair of parentheses, `<N>` off that fixture (`OP16-3`). That is the STEADY-STATE green line and the one shape of this summary no element here reached: row 39's MAIN half runs M=1, row 35 M≥2, row 39's NEGATIVE twin the `nothing was checked` carve-out, and row 34 runs an M=0 fixture asserting no summary text at all, row 38's M=0 fixture having taken the STANDALONE slot in round 26 (`OP26-1`) — and the ordinary rendering of that line was pinned nowhere), rows 16, 17 and 26 on the coverage line and row **26** additionally on the ungrouped clause-2 `unbuilt packages/cli` line's SUFFIX (round 29, `OP29-1`), the third of §3.3's cli `unbuilt` reason sentences and the only one whose gap list is EMPTY by its own branch's precondition, rows 27-30 on the
  finding, grouped and `WARN`/`ERROR` lines (round 11), rows 31-34 on the load-time
  `WARN`/`ERROR` lines, the two `unbuilt` lines and the remedy plan's first line
  (round 12), rows 35 and 36 on the findings-path units-compared/skipped summary and
  on the two UNGROUPED `unbuilt` lines (round 13), rows 21, **28**, 42 and 46 on the remedy plan's
  first line as printed for §4's step-1 and step-2 carve-outs, for step 1's own PRESENCE on
  a ≥2-unit tree and for its THREE-KIND SELECTOR on that same tree (row 28's PLAN TWIN,
  round 25, `OP25-1`; round 33, `OP33-1`) and for a chunkless
  `packages/cli/dist` (round 18, `OP18-3`/`OP18-4`), row 20's twin on the ungrouped
  `unbuilt packages/cli — bundle:cli did not finish; missing: dist/plugin/skills` finding
  line (round 19, `OP19-1`), row 43's CLI twin on the grouped
  `stale-declarations ×1 — packages/cli` line it must still print when the process is run
  with NO flag and exits **0** (round 19, `CF19-1`) and, on that same flagless run, on the ordered
  plan carrying recipe step 3's `npm run bundle:types -- --force` — §4's UNCONDITIONAL-step-3
  rule, on the one fixture here pairing a SOLE `stale-declarations` with a `packages/*/src/**.ts`
  trigger (round 34, `OP34-1`), and rows 38 and 39 on the ordered
  remedy plan's first line — row 39 as printed by a run holding ZERO findings (round 14) — and row 38 additionally on the grouped `stale-bundle ×1 — packages/cli` line (round 18, `OP18-2`) and on the ABSENCE of any `check-stale-dist: ⚠️ coverage: ` line, ROW 38's fixture being the one in this group whose four coverage entries are all `full` (round 16, `OP16-4`), and — since round 36 (`OP36-2`) — on the ABSENCE of any line beginning `check-stale-dist: PASS`, §4's exit table rendering that line on its first two rows only while every rendered findings-path element in this group asserted a line's PRESENCE and none its absence, and row 38 additionally on the STANDALONE units-compared/skipped summary at `M = 0` — `check-stale-dist: <N> units compared, 0 skipped`, by EQUALITY, `<N>` off its own fixture and no parenthetical — which is §4's `M = 0` ruling at the slot row 18's advisory-only fixture cannot reach (round 26, `OP26-1`), and row 38's CLEAN side additionally on the ABSENCE of any line beginning `check-stale-dist: remedy plan:` — §4's **A DEGRADED RUN CARRIES A NEXT ACTION** read in its FALSE direction, on the one fixture in this section provably zero-finding AND all-four-`full` (round 36, `OP36-3`), and row 8's twin on the grouped `stale-plugin-assets ×1 — packages/cli` line, the fourth and last of §4's grouped kinds to be rendered anywhere in this section (round 20, `OP20-1`), and row 34 on the ABSENCE of any §4 remedy-table command — `rm -rf`, `npm run build`, `npm run bundle:types` or `npm run bundle:cli` — on the prefixed lines that PRECEDE its plan's first line (round 21, `OP21-1`), which is the second rendering §4 forbids in prose and no element read.** ⚠️ **AND ROUND 35 APPENDS FOUR ITEMS TO THIS ENUMERATION, every one of them a plan rendering: row **34**'s SOLE-KIND TWIN on recipe step 4's `npm run bundle:cli`, the command §4 calls the whole remedy for a sole `stale-shipped-declarations` and which both elements selecting that step had named by NUMBER alone (`OP35-2`); row **36** on the ordered plan printed where TWO `unbuilt` findings sit on a tree with nothing compared and every comparison suppressed; and row **39**'s ALL-NOT-RUN TWIN and row **39**'s CACHE-ONLY TWIN on the ordered plan printed at ZERO findings, the first with units compared and all four entries `not-run`, the second with nothing compared while `declaration-cache` runs `full`, BOTH of them ALSO on the rendered `PASS` line in §4's ORDINARY shape — parenthetical PRESENT, no `; nothing was checked` — since round 36 (`OP36-1`), which makes them the two EXCLUDING elements of the `PASS`-line carve-out's OWN two conjuncts as they already are of the plan carve-out's — the three EXCLUDING elements of §4's THREE-conjunct `nothing was checked` carve-out, whose one prior element held all three conjuncts at once (`OP35-1`).** For rows 16, 17 and 26 the
  prefix, the `⚠️ coverage: ` label and the asserted entry form ONE contiguous substring (⚠️ **which is also what forbids any implementation from resolving a presentation profile for that glyph — residual §8(o), round 14, OP14-5**),
  because §4 renders only the non-`full` entries, in §2's union order, and on each of those
  three fixtures the asserted entry is FIRST among them — verified per row. ⚠️ **Round 10
  wrote the previous form of this bullet while rows 16 and 17 still asserted the bare
  entry**, so the property held for row 18 alone and the two rows it named would have
  FAILED a correct implementation, while passing one that emits no prefix at all — the very
  defect the group exists to catch. An implementer who omits the prefix now fails EVERY row this
  bullet enumerates instead of one — the SET, never a count. ⚠️ **"seven" stood here and
  was already wrong by five when round 13 read it against the twelve rows named one
  sentence above (OP13-3)**, round 12 having appended 31-34 to the group without touching
  the figure; a number this bullet re-invalidates on every appending round is `C11-4`'s
  own class of defect, and `C11-4`'s own remedy is to state the set, and ships lines nobody can attribute in `check:all`'s 15-member
  stream — the same shape of gap as a coverage value nobody rendered, one level out.
  ⚠️ **A PREDICATE IS NEVER SPLIT BY ANOTHER ROW, and an appositive in this enumeration
  names the ROW it belongs to rather than saying "its" (round 21, `CF21-1`).** Round 20's
  apply inserted row 8's item BETWEEN the two conjuncts of row 38's predicate: re-derived
  read-only, at `9c247eb` — the document round 20 READ — those two conjuncts are contiguous
  with no item between them, and at `3095116` that contiguous form matches ZERO times. The
  ABSENCE conjunct and its "its fixture" appositive therefore re-pointed onto row 8's twin,
  which is bound by NEITHER the `C15-2` sibling-fixture rule below (it asserts no bundle
  coverage) NOR the complete-cli-build cache clause above (which licenses "or omit the
  cache entirely"), so a CORRECT `formatFindings` renders a `check-stale-dist: ⚠️ coverage: `
  line on its fixture and the re-pointed assertion FAILS a correct implementation —
  `OP16-3`'s and `CF20-1`'s class, and a SET statement false of the rows it enumerates,
  which is `C11-4`'s defect inside `C11-4`'s remedy one sentence above. `CF13-4`'s splice
  shape inside a compound predicate rather than across a full stop, and round 10's "guard
  WHAT an apply inserts, not only WHERE" at a live edit rather than at a proposal. Rows 8
  and 38 state the split correctly in their own cells and round 21's fix changed NEITHER (standing rule 8) — the present tense stood here from round 21, and round 28 (`C28-1`) changes row 8's cell for an unrelated obligation, its SOURCE-side SCOPE mutant, touching neither row's half of this split, so the sentence is scoped to the append it was written about (`C11-4`'s class at a state description),
  and §4's coverage-line order sentence and §2's `formatFindings` comment already name row
  38 and row 8's twin for their own halves. This is the second enumeration in this bullet
  an append has invalidated — round 17's `CF17-2` was the first, by restating a subset — so
  it is written as the rule rather than as this round's remedy, per `OP12-6`.
- **Rows 9, 13, 14 and 15 are the unit-state set** — one fixture per `unitState().state`,
  plus the reasons. A `built: boolean` implementation passes any two of the three states and
  fails the third, which is the only thing that makes §2's three-value enum testable (CF9-1).
- **Row numbers are cited from OUTSIDE this section, so nothing here is renumbered
  casually.** §0's requirement 1 row cites fixture rows 7, 8 and 22; requirement 6 cites 2;
  requirement 8 cites 6, 17, 22 and 23; requirement 9 cites 3; §2's type note cites 9, 13
  and 14. Round 9 appended three separate groups of rows and each was drafted as "row 13":
  the state rows keep 13-15 so §2 stays true, and §3.5's two rows moved to 22-23. Round 10
  re-pointed §0's requirement 1 and 8 rows off the stale "row 13" onto those numbers, and
  appended rows 24-25 rather than renumbering anything. Round 11 appended rows 26-30 the
  same way, and §0's requirement 8 row now cites **29** as well. Round 12 appended **31**,
  **32**, **33** and **34** the same way, round 13 appended **35** and **36**, round 14
  appended **37**, **38** and **39**, round 15 appended **40**, **41**, **42** and
  **43** BELOW A BLANK LINE — so not one of the four was on the page until round 16
  (`CF16-1`/`OP16-1`) deleted it — round 16 appended **44** and **45**, and round 17 appended
  **46**, **47** and **48**, and round 21 appended **49**, all BELOW THE LAST ROW per the APPEND RULE below, with prettier and
  the preamble's orphan sweep re-run on the candidate before the commit; nothing
  renumbered, and row **3**'s VERDICT changed
  in place (C14-3) without moving its number, which §0's requirement 9 row cites.
  ⚠️ **APPEND RULE (round 16, `CF16-1`/`OP16-1`): a row is appended BELOW THE LAST ROW of
  this table, never below a blank line, and the preamble's orphan sweep is re-run until it
  returns nothing.** GFM ends a table at the first blank line, and a following pipe line
  begins a NEW table whose first line is its header — with no delimiter row it is not a
  table at all, so the appended rows render as a paragraph of literal pipes. Neither check
  that reads rows can see it: the escape-aware row census is blind by construction (a blank
  line resets its header, so the orphan becomes its own one-row header and matches itself)
  and the anchor grep reads citations, not tables. Round 14 REJECTED this exact anchor as a
  proposal and round 15 committed it here, in the round that filed it against §7.
  ⚠️ **A TWIN CELL SAYS THAT IT ADDS NO ROW, NEVER a live battery count §1 also states
  (round 22, `OP22-3`).** Row 8's twin cell read "§1's battery count of 48 stays correct"
  and round 21 then appended row 49 and moved §1's figure with it, leaving one live figure
  stated two ways and a reader resolving that citation against §1 finding it refuted —
  `C11-4`'s class at the one site round 21's own fan-out missed, which is standing rule 9. A
  count is re-invalidated by every append; "adds no row" is not. Stated as the rule rather
  than as this round's remedy, per `OP12-6`.
  ⚠️ **The outside-citer list is now DERIVED, not restated (round 13, CF13-5)** —
  `grep -onE '§6 rows? [0-9]+' <file>` run over everything outside this section.
  ⚠️ **The DERIVATION is the SET that command returns, and round 13 restated FOUR of its
  members (round 14, CF14-4).** Re-derived against the document at a NAMED sha, which is
  this file's own convention for a figure a later round must be able to tell from damage —
  `git show f3aed16:<file>` — the command returns **36** hits and **19** distinct rows
  outside this section: 7, 9, 10, 11, 13, 15, 16, 19, 20, 22, 23, 26, 27, 30, 31, 32, 33,
  34, 35. Round 13's half of this bullet named 31 (§1, §2 — plus §8(n) in a bolded form
  this grep does not match), 32 (§4's `unbuilt` line shape), 34 (§2 and §4's
  ordered-recipe paragraph) and 33 (§3.3's clause 1, a citation round 13 ADDED); the
  by-hand sentence above names 2, 3, 6, 7, 8, 9, 13, 14, 17, 22 and 23; and this bullet's
  tail names 35 and 36. So **10, 11, 15, 16, 19, 20, 26, 27 and 30 were cited from outside
  §6 and named in NEITHER half** — NINE rows, renumbering any of which breaks a link this
  registry says does not exist. That is verbatim the failure `CF13-5` filed one round
  earlier, at the fix for it. Per `C11-4` the enumeration is therefore replaced by the
  command and the SET it returns: **re-run the command before renumbering §6, and never
  restate a subset of its output** — including for a row this round appends, which is why
  rows 37-39's incoming citations are not listed here. Round 12 wrote this bullet by hand and it was wrong in both directions
  at the commit that wrote it: it asserted "§3.3's clause 1 cites 33" while
  `grep -n "row 33"` returned **nothing anywhere in this file**, and it named neither §1
  nor §2 for rows 31 and 34 — so renumbering §6 would have broken links the registry said
  did not exist and preserved one it said did. The one round-12 rule with the loudest
  consequence, `PASS` exit 0 under `--strict` on a `bin.crabgic` that dies with
  `ERR_MODULE_NOT_FOUND`, was the only one of the four whose non-vacuity row had no
  incoming citation at all. Round 13's two are cited the same way: §4's skipped-units rule
  and this section's false-negative battery cite 35, §4's grouping rule cites 36. All of
  those sections join the outside-citer list above.

| #   | clean fixture                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | single mutation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | must become                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | dist newer than src, and NESTED ON BOTH SIDES (round 18, `C18-1`): `src/a.ts` plus `src/sub/b.ts`, `dist/a.js` plus `dist/sub/b.d.ts`, ordered `dist/a.js` OLDER than `src/sub/b.ts` OLDER than `dist/sub/b.d.ts` — so `dist/sub/b.d.ts` is the NEWEST member of §3's filtered output set, every source is older than it, and the fixture reports CLEAN. ⚠️ **TWO mutants, and BOTH must FAIL on this row's own fixture, needing no mutant formatter:** (a) a SRC walk whose `readdirSync` omits `recursive: true` never sees `src/sub/b.ts`, so the mutation below flips nothing and the row stays clean; (b) a DIST walk with the same omission reads `dist/a.js` as the newest output and reports `stale-unit` at BASELINE, which the ordering clause above is what forces. Re-derived read-only at `896dd84`: NO row in this battery required either walked side to hold a member below depth 0 whose depth changes a verdict — `dist/bin/supervisord.js` is depth 1 on every cli-complete fixture but is EXISTENCE-checked by §3.3's clause 3 and never required to be the newest output — so §3's `recursively` and its `<unit>/dist/**` glob were mandated by §3 and §2 and read by nothing, `C16-1`'s and `CF17-1`'s shape at the two primitives every comparison is built on                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `utimesSync src/sub/b.ts` forward past `dist/sub/b.d.ts` — the NESTED source, never the depth-0 `src/a.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `stale-unit`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2   | TWO units, each carrying its own `tsconfig.json` whose `extends` names the fixture's own `tsconfig.base.json`, both units' `dist` newer than their `src` AND newer than each unit's own `tsconfig.json` and than that `tsconfig.base.json` — without which the baseline is ALREADY `stale-unit ×2` under a correct rule 3, the mutation flips nothing and BOTH halves of this row are vacuous by construction (round 18, `CF18-1`; `CF8-2`'s shape, and row 3's own without-that-clause precedent). This cell constrained only `src` against `dist` and said nothing about configs, so the clause is required for the ORIGINAL half too — ⚠️ **the second unit is round 16's fix (`CF16-3`): this cell read `dist newer than src`, the same three words as row 1's, so nothing required a second unit and the verdict below was satisfied at N=1**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | touch `tsconfig.base.json` forward                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | the reported `stale-unit` unit set must EQUAL both fixture units' `Unit.dir` values (req 6) — ⚠️ **never `×N`, which any N≥1 satisfies (round 16, `CF16-3`)**. §3 input rule 3 is CORRECT and states the arithmetic in terms ("Chain length is 2 for all 19 today"); what nothing asserted was that the chain is walked for EVERY unit. The implementation this now excludes is the ordinary memoisation shape: ONE shared `extends`-chain resolver serving all 19 units with a MODULE-LEVEL visited set, which adds `tsconfig.base.json` to the FIRST unit walked and skips it for the other 18 — it renders `stale-unit ×1` here and FAILS on this row's own fixture, which is stronger than a mutant because it needs no injection point. Nothing else in the battery discriminates it: this is the only row in §6 whose verdict pins a MULTI-unit `stale-unit` set by EQUALITY, which is the property the memoised shared visited set turns on, a single-unit fixture being satisfied by both readings — the PROPERTY and never the occurrence set, because the SET moved with the next append exactly as the count did (round 29, `C29-1`): re-derived read-only at `c672565`, `extends` occurs in §6 at rows 2, 3, 47, 48 and 49, and each of the other four pins ONE unit in its own verdict cell — rows 47 and 49 read `EQUAL to the single Unit.dir packages/A`, row 48 `EQUAL to that unit's Unit.dir`, and row 3 is the cli fixture whose verdict is `stale-declarations` and explicitly NOT `stale-unit` (`C14-3`), its round-18 twin (`C18-4`) quantifying `stale-unit` over every unit that fixture holds rather than pinning a set of two. The historical figures keep their shas: **zero** is correct at `git show db857c3:<file>`, the document round 16 read, and at `git show badf45a:<file>` it is **three**, all three inside this cell's own new two-unit fixture, and the SET form round 17 wrote (`CF17-2`) was true only there; row 1 is src-side, row 3's trigger is `tsconfig.dts.json` and its verdict is `stale-declarations` (`C14-3`), rows 28 and 34 are the only other multi-unit `stale-unit` fixtures and BOTH are src-triggered so neither walks rule 3's chain, and the live smoke test asserts the unit SET, which is unaffected because the 18 under-covered units are still reported — merely reported clean. ⚠️ **Asserted on the RETURN value, deliberately, so this row does NOT join §6's rendered-text group:** its subject is §3 input rule 3 reaching every unit, while the RENDERING of the grouped line is row 28's subject and is already pinned to `Unit.dir` by equality (round 15, `OP15-1`), so a basename-printing reporter fails row 28 and needs no second row. §7 row 1 states the owner-accepted cost "at §6 row 2's scale", so the SCALE is precisely what this row must pin. ⚠️ **TWIN, in row 33's shape so nothing is renumbered (round 18, `CF18-1`): rule 3 has TWO members and this row exercised only the SECOND.** Same two-unit fixture with the baseline clause above; mutation `utimesSync` on ONE fixture unit's OWN `tsconfig.json` (`packages/A/tsconfig.json`, in rows 45 and 47's naming) forward past the newest output under that unit's `dist`; verdict `stale-unit` with the reported unit set EQUAL to `packages/A` ALONE. Re-derived read-only at `896dd84` over this battery's whole mutation column: NO row's mutation is a unit's OWN `tsconfig.json` — this row's is the shared ANCESTOR `tsconfig.base.json`, which an ancestors-only walk still attributes to both units and which therefore satisfies the equality half above; row 3's is `tsconfig.dts.json` and its verdict has been `stale-declarations` and explicitly NOT `stale-unit` since `C14-3`; row 47's is rule 4's descendant; row 48's is a `src/**.json`; rows 1, 6, 28, 34 and 44 move `*.ts`; row 27's malformed-config mutation discriminates nothing, because the chain resolver opens `<unit>/tsconfig.json` to read its `extends` whether or not it adds it as an input, so the wrapped read throws with the path either way; the false-negative battery moves no config; the wiring assertions read import STATEMENTS; and the live smoke test asserts the unit SET and the coverage shape and explicitly not the finding set. So an input set assembled from the resolved ANCESTORS alone passed everything, and on the live tree `utimesSync packages/gates/tsconfig.json` — a changed `outDir`, `target`, `strict`, `paths` or `include` — left `packages/gates/dist` compiled under the old config while the check printed PASS, exit 0 even under `--strict`, at `check:all` member 0 and at `pretest`. The ALONE half is load-bearing twice over: it also kills the implementation that adds every enumerated config to EVERY unit's input set, which is otherwise the cheapest way to pass the added half and would render `×2` here. Mutant = the ancestors-only walk: it leaves this fixture clean and FAILS this twin on the row's own fixture, so it needs no injection point — rows 44, 47 and 48's own discipline                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 3   | cli unit, dist newest, and `.dts-cache/index.d.ts` PRESENT, byte-identical to `dist/index.d.ts`, and NEWER than every member of §3.3's comparand — without that clause this row takes the "or omit the cache entirely" branch of the byte rule below, `checkDeclarationCache` SKIPS, and the mutation flips nothing: CF8-2's defect, at the row round 14 repointed. ⚠️ **AND the fixture's `packages/cli/tsconfig.dts.json` declares `"composite": false` EXPLICITLY while declaring NO `outDir` of its own, inheriting `"outDir": "./dist"` and `include` from the fixture's own `packages/cli/tsconfig.json` — the same shape the live config has, re-derived read-only at `8fdf3ed` (round 24, `C24-1`).** That makes it PASS §3 input rule 4's clauses (i) and (ii) and FAIL clause (iii) ALONE, which is the whole discriminator this row's **NOT `stale-unit`** half turns on. Until round 24 this cell constrained only `dist newest` and the cache, so `composite` was a FREE VARIABLE: a fixture silent on it leaves the mutant below with nothing to flip, and a fixture declaring it `true` would make a CORRECT implementation report `stale-unit` here and FAIL this row — `CF20-1`'s and `OP16-3`'s class. Pairs with §6 row 47's fixture cell, which pins the INHERITED-TRUE mirror                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | touch `tsconfig.dts.json` forward                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `stale-declarations` (req 9) via `checkDeclarationCache`, and **NOT** `stale-unit` — round 14 (C14-3): `packages/cli/tsconfig.dts.json` FAILS §3 input rule 4's clause (iii) — its `"composite": false` is EXPLICIT — while PASSING clauses (i) and (ii), its chain resolving into `packages/cli` and its INHERITED `outDir` resolving to that unit's `dist` (`dist/index.d.ts` is §3's one exclusion, and that exclusion is a fact about `bundle-cli.mjs:153`, never this rule's criterion), so it is in no unit's input set. ⚠️ **That is round 24's restatement (`C24-1`): the previous wording — `governs .dts-cache/ and nothing in packages/cli's COMPARED output set` — is not a predicate `descendantConfigs(unitDir, allConfigs)` can evaluate, and this row and row 47 read it to OPPOSITE verdicts on inputs it cannot separate.** ⚠️ **MUTANT (round 24, `C24-1`) — adds no row and renumbers nothing:** a `descendantConfigs` implementing clauses (i) and (ii) ALONE, which is the reading row 47's own cell spelled out. It attaches this fixture's `tsconfig.dts.json` to `packages/cli`'s input set, so this row's own `touch tsconfig.dts.json forward` mutation renders `stale-unit packages/cli` beside the `stale-declarations` and this row's **NOT `stale-unit`** half FAILS on this row's own fixture — no injection point needed, in row 47's mutant shape. It must FLIP, and row 47 must stay green under the clause-(iii) implementation, which is what the two paired fixture pins buy. A row still mandating `stale-unit` would assert the behaviour C14-3 removes, and would send the operator to `rm -rf packages/cli/dist` plus a 19-unit rebuild. ⚠️ **TWIN, in row 33's shape so nothing is renumbered (round 18, `C18-4`):** the same fixture, mutation `utimesSync` on the FIXTURE's own `tsconfig.base.json` forward past the newest output under EVERY unit's `dist` — which this fixture's `dist newest` clause makes a fortiori past `.dts-cache/index.d.ts` too, so both halves below fire deterministically. That file is the CHAIN member of §3.3's comparand: `packages/cli/tsconfig.dts.json` declares `"extends": "./tsconfig.json"` which declares `"extends": "../../tsconfig.base.json"` (both re-derived read-only at `896dd84`), the chain `dts-bundle-generator --project` resolves at `bundle-types.mjs:84`. Verdict `stale-declarations packages/cli` — the load-bearing half — AND `stale-unit` on every unit the fixture holds, the base being in rule 3's upward chain for all of them, which is why this twin's differing verdict does NOT re-open `C14-3` (that finding's subject is `tsconfig.dts.json`, which is in no unit's input set, never the base). Mutant = a comparand holding the leaf alone: it renders the `stale-unit` findings and NO `stale-declarations`, and must FAIL. Without it `and its chain` was mandated by §3.3 and read by nothing, while `bundle-types.mjs:43-61` stats no `.json` at all — so a `tsconfig.base.json` edit invalidated the cache from neither inside nor outside and the blind-spot-9 closure shipped inert, verbatim what `C15-3` filed for the two members beside it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 4   | 1:1 stems, and NESTED (round 18, `C18-1`): `src/x.ts` with `dist/x.js` at depth 0 AND `src/sub/y.ts` with `dist/sub/y.js` beneath it, both pairs in 1:1 stem correspondence, every output newer than every source — reports clean, and in particular ZERO `orphan-output`. This is the STEM walk's recursion row and the only one: a `stemsUnder` whose `readdirSync` omits `recursive: true` sees neither side of the nested pair, reports clean at BASELINE and clean after the mutation below, and passes rows 14 and 44 unchanged, their scratch exclusions being exclusions a shallow walk also satisfies                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | delete `src/sub/y.ts`, keep `dist/sub/y.js` — the NESTED source, leaving the depth-0 pair intact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | EXACTLY ONE `orphan-output` (req 2), naming `dist/sub/y.js` — and NOT a second one for `dist/x.js`, whose `src/x.ts` still exists. Mutant = the shallow `stemsUnder` above, which reports NO finding at all and must FAIL (round 18, `C18-1`; the count and the named path are what discriminate it, in row 2's `CF16-3` shape). ⚠️ **TWIN, in row 33's shape so nothing is renumbered — it ADDS NO ROW (`OP22-3`) — round 23, `C23-1`: `Unit.kind` decides whether requirement 2 runs for a unit and its derivation was stated in no section, `C21-1`'s shape at the third field of the same struct.** Fixture: cli-scoped and cli-COMPLETE per the complete-cli-build rule below (a chunk-pattern file, the three entry outputs, `dist/index.d.ts`, the six plugin entries, the marker present with its `outputs` naming exactly the chunk files written and its mtime at or after the newest §3-filtered output, and `.dts-cache/index.d.ts` byte-identical to `dist/index.d.ts` per `CF8-2` AND newer than every member of §3.3's comparand, row 3's own clause, so no `stale-declarations` fires beside the finding this twin counts), whose `packages/cli/tsconfig.json` references `packages/A` and whose marker's `inputs` name `packages/A/dist/x.js`, so `packages/A` is that fixture's whole inlined set and holds qualifying output; `packages/A` in THIS row's own END state — `src/x.ts` with `dist/x.js`, plus `dist/sub/y.js` whose `src/sub/y.ts` is deleted — every `packages/A` output NEWER than every `packages/A` source and OLDER than `bundleAt`, so `packages/A` is `compared` (never `skipped`, never `unbuilt`, without which `checkOrphans` never runs for it and this twin is vacuous by construction, `CF8-2`) and no SECOND `orphan-output` can fire. Like row 43 the fixture deliberately does NOT report clean and this cell says so, because the finding must EXIST for a mutant to suppress it. NO mutation — the discriminating state IS the baseline. Verdict: the reported `orphan-output` set EQUALS `{ packages/A dist/sub/y.js }` — exactly one, naming that path, and **ZERO** for `packages/cli`, which pins the bundled-unit exemption POSITIVELY for the first time (§3.1 has asserted it in prose since round 1 and no element read it). TWO mutants, each failing on this twin's OWN fixture and needing no injection point, rows 44, 47 and 48's discipline: **(a)** `kind` derived from `packages/cli`'s reference closure, or equivalently from `Unit.references` — the field nothing else in §3-§6 reads — which marks `packages/A` `"bundled"`, exempts it and reports ZERO, so it must FAIL; on the live tree that derivation marks **17 of 19** and leaves `checkOrphans` running on `packages/perf` and `e2e/report` alone, requirement 2 inert for the other 16 at PASS, exit **0 even under `--strict`** (re-derived read-only at `53996f5`: root references 19, `packages/cli` direct references 16, closure 16, complement exactly those two); **(b)** `kind: "tsc"` for all 19, which walks `packages/cli`'s chunk-pattern file for a stem no `packages/cli/src` file supplies and reports it as `orphan-output`, so it must FAIL the ZERO half. Nothing else discriminates either: no other fixture in this section builds a `packages/cli` beside a unit that `packages/cli/tsconfig.json` references and that carries an orphan — rows 14, 44, 48 and 49 reach the stem side on trees holding no `packages/cli` at all, row 28's PLAN TWIN reaches it from round 33 (`OP33-1`) through an `orphan-output` unit its FIXTURE ROOT references and `packages/cli/tsconfig.json` does not, so it is not that pairing either, rows 40, 45 and 46 call comparisons DIRECTLY, row 42's scope twin asserts NO `stale-declarations` and `declaration-cache` coverage `full`, row 34's fixture already carries two findings, row 39's is zero findings by construction, and the live smoke test asserts the unit SET and the coverage SHAPE and explicitly not the finding set on a tree holding **0** non-scratch orphans (re-derived read-only at `53996f5` across all 18 tsc units). A RETURN-VALUE twin, so it does not join this section's rendered-text group (`C20-2`); it asserts neither `stale-bundle` nor a `full` `bundle-freshness` entry, so `C15-2`'s sibling-fixture rule below does not quantify over it (row 8's twin's precedent). Row 4's MAIN half and its shallow-`stemsUnder` mutant are unchanged and nothing they pin is vacated (standing rule 8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 5   | bundle newest, marker newest, with **TWO chunk-pattern files at DIFFERENT mtimes** (round 18, `C18-7`): `T_old` and `T_new` with `T_new > T_old`, the marker at or after both and naming exactly those two in its `outputs` (so the complete-cli-build rule below is satisfied and this row stays conforming, round 12's `C11-6`), the inlined unit's `dist/index.js` at `T_old` minus epsilon and every other member of §3.2's input side older still — reports clean. Without two chunks nothing in this section separates §3.2's mandated **minimum** over the chunk set from a maximum: rows 25 and 33 exercise chunk MEMBERSHIP by deletion, row 38's mutation moves `packages/cli/src/index.ts` past everything so min and max both fire, and row 46's chunk set is empty                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `utimesSync <inlined unit>/dist/index.js` to strictly BETWEEN `T_old` and `T_new` — forward past the OLDEST chunk and not past the newest                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `stale-bundle` (req 3). Mutant = `newestUnder(join(cwd, "packages/cli/dist"), isChunk)` — the MAXIMUM, and the path of least resistance because §2 declares no minimum helper at all — which reports CLEAN and must FAIL (round 18, `C18-7`). The mutation is OUTSIDE `packages/cli/dist`, so §3.3's clause 2 still holds and the unit stays `compared`. ⚠️ **TWIN, in row 33's shape so nothing is renumbered — it ADDS NO ROW (`OP22-3`) — round 23, `C23-2`: §3.2's FIRST input member is the one comparand member in that list with no pointer to the set it walks, and §3's output filter had no element anywhere at that consumer.** This row's own clean fixture, plus TWO files under the SAME inlined unit's `dist`: `.tsbuildinfo` and `eo-crash-fixture-XX/x.js`, BOTH strictly NEWER than `T_new`, with that unit's `src` older than its `dist/index.js`. NO mutation — the discriminating state IS the baseline. Verdict: **CLEAN** — no `stale-bundle` anywhere, `bundle-freshness` coverage `full`. TWO mutants, each failing on this twin's OWN fixture with no injection point (rows 44, 47 and 48's discipline): **(a)** a member-(i) walk with NO accept predicate at all — the reading this bullet licensed while every other comparand member carried a pointer to its rule — which reads BOTH added files as output; and **(b)** one carrying §3's four-extension WHITELIST but not its `dist/eo-*-fixture-*/**` minus-bullet, the whitelist ACCEPTING a scratch `.js`, which is exactly why `C16-1` had to close that exclusion at two sites. Each reads a comparand NEWER than `bundleAt` and reports `stale-bundle packages/cli` at BASELINE. ⚠️ **A third mutant is deliberately NOT claimed: dropping the `.tsbuildinfo` minus-bullet ALONE flips nothing here, because the four-extension whitelist already rejects that file by extension** — so a cell claiming one mutant per excluded file would be asserting a mutation that cannot fail, `CF8-2` read the other way and caught before the row was written; the `.tsbuildinfo` is in the fixture as the member mutant (a) alone reads, and it is the live hazard because `tsc -b` refreshes it on a touched-but-unchanged source (§7 row 1). ⚠️ **It provably cannot FAIL a correct implementation (`OP16-3`'s disqualifier, settled before the twin was written):** both added files are outside §3's filtered set for that unit, so member (i) stays at `T_old` minus epsilon; the unit still holds `dist/index.js`, so §3.2's absence rule does not fire and `C15-2`'s sibling-fixture rule below is SATISFIED rather than excepted; the `eo-*` file is outside the stem set and the `.tsbuildinfo` outside the four-extension whitelist, so `checkOrphans` stays at zero and `checkUnitFreshness` stays quiet; and both sit under an INLINED unit and NOT under `packages/cli/dist`, so §3.3's clause 2, the complete-cli-build rule's marker constraint and this row's own two-chunk MINIMUM are untouched — `bundleAt` is still `T_old`, and the `newestUnder(join(cwd, "packages/cli/dist"), isChunk)` MAXIMUM mutant is still failed by the main half and by it alone (§2), since under the max reading `bundleAt` becomes `T_new` and this twin's `dist/index.js` at `T_old` minus epsilon is older still. ⚠️ **The live cost is MEASURED, not asserted (standing rule 2), and it is NOT live on this working copy — stated rather than hidden:** `packages/journal` is one of the 16 inlined units AND the one unit this repository's suite writes into, re-derived read-only 2026-08-21 at `53996f5` (`git status --porcelain` empty before and after; no import of `scripts/bundle-cli.mjs`) — `packages/journal/dist/eo-crash-fixture-6dP02V/crash-fixtures/append-chain-snapshot-operation.js` at mtimeMs **1787308024588.285** against the newest FILTERED output across the 16 at **1787156422460.689**, a **1.75-day** forward shift — so on any tree whose `npm test` ran after its last `bundle:cli` the unfiltered walk prints `stale-bundle packages/cli` at `check:all` member 0, exit **1** under `--strict`, and at line 5 of §5.1's measured 204 under flagless `pretest`, re-fired by every later test run. Here `bundleAt`, the MIN over the five chunks, is **1787338130243.025** — 0.35 days AHEAD of that file, and ahead of the newest `.tsbuildinfo` across the 16 (**1787158151980.000**) too — so both mutants report clean on this tree, which is exactly why the live smoke test cannot see either and why this twin is the only element there is. A RETURN-VALUE twin, so it does not join this section's rendered-text group (`C20-2`). Row 5's MAIN half, its two-chunk fixture and its MAXIMUM mutant are unchanged and nothing they pin is vacated (standing rule 8). ⚠️ **SECOND TWIN, in row 33's shape so nothing is renumbered and NO ROW is added (`OP22-3`) — the EQUAL-MTIME TWIN (round 36, `C36-1`): §3.2's Rule mandates STRICTLY newer and no fixture in this section took that BOUNDARY.** This row's OWN clean fixture, NO mutation — the discriminating state IS the baseline — with the inlined unit's `dist/index.js`, §3.2's FIRST input member, re-pinned from `T_old` minus epsilon to EXACTLY `T_old`, which IS `bundleAt`, the MINIMUM over this row's two chunks. `T_old` and never `T_new`: at `T_new` a CORRECT body reports `stale-bundle` at BASELINE and this twin would FAIL it (`OP16-3`'s and `CF20-1`'s disqualifier), and pinning the MINIMUM leaves row 5's MAXIMUM mutant failed by the MAIN half and by it alone (standing rule 8 at `C18-7`). Verdict: NO `stale-bundle` finding anywhere — that ALONE, the four coverage entries and every other kind being FREE VARIABLES on this fixture, which is row 19's twin's own consolidation ruling (`C19-2`). Mutant = §3.2's comparison spelled `>=`, which reports `stale-bundle packages/cli` on this twin's own fixture and so needs no injection point. Nothing else moves: the marker sits at or after `T_new`, so §3.3's clause 2 holds and the unit stays `compared`; the re-pinned file is that unit's own output and its `src` is older still, so `checkUnitFreshness`, `checkOrphans` and `C15-2`'s sibling-fixture rule are untouched. A RETURN-VALUE twin, so it joins neither this section's rendered-text group nor its TWIN-halves list (`C20-2`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 6   | cache newest, `dist/index.d.ts` byte-identical to it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | touch any `packages/*/src/*.ts` forward                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `stale-declarations` (req 8) — and NOT `stale-shipped-declarations`, which the bytes keep quiet. ⚠️ **TWIN, in row 33's shape so nothing is renumbered and NO ROW is added (`OP22-3`) — the EQUAL-MTIME TWIN (round 36, `C36-1`): §3.3's declaration-cache rule mandates STRICTLY exceeds and no fixture in this section took that BOUNDARY.** This row's OWN clean fixture, NO mutation, with the newest `packages/*/src/**.ts` — the member this row's own mutation moves — re-pinned to EXACTLY the mtime of `packages/cli/.dts-cache/index.d.ts`. Verdict: NO `stale-declarations` finding — that ALONE, every other kind and all four coverage entries being FREE VARIABLES here (`C19-2`'s consolidation ruling), because a source re-pinned FORWARD may reach `checkUnitFreshness` and §3.2's input side on this fixture and an assertion over either would FAIL a correct implementation. The comparison RUNS here by this row's MAIN half's own verdict, so the twin adds no fixture clause. Mutant = §3.3's cache comparison spelled `>=` — the PRODUCER's guard read back, `bundle-types.mjs:70` being `statSync(OUTPUT).mtimeMs >= newestSourceMtime()` — which reports `stale-declarations packages/cli` on this twin's own fixture and so needs no injection point. A RETURN-VALUE twin, so it joins neither this section's rendered-text group nor its TWIN-halves list (`C20-2`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 7   | **stale src, marker newest, then** touch `dist/plugin/hooks/hook.js` **to now** — ⚠️ **A WHITELISTED EXTENSION UNDER `dist/plugin`, AND THIS MUTATION WAS `dist/plugin/.mcp.json` UNTIL ROUND 35 (`C35-1`), WHOSE `.json` IS OUTSIDE THE FOUR-EXTENSION WHITELIST §3 ADDED IN ROUND 20 (`C20-1`).** So for fifteen rounds this row's verdict flipped for the WHITELIST and never for the `dist/plugin/**` minus-bullet that §3's minus-bullet list, §0's requirement 1 row and §2's `isCompilerOutput` comment all cite it for, and an `isCompilerOutput` carrying the whitelist and the other three bullets while OMITTING this one passed it unchanged. Nothing else in this section reaches it either: row 20 DELETES a top-level entry (which only LOWERS the newest output), rows 8 and 24 move plugin SOURCES, and §8(p)'s bullet empties `dist/plugin/skills/` with the marker ABSENT. The fixture therefore PINS one file beneath a `PLUGIN_ASSET_ENTRIES` entry whose extension IS in that whitelist — `hooks/hook.js` — and the mutation moves THAT file. Re-derived read-only 2026-08-22 at `a5605e9` (`git status --porcelain` empty before and after; no import of `scripts/bundle-cli.mjs`): `packages/cli/dist/plugin` holds **26** files over the four extensions `.json`, `.md`, `.mjs` and `.mts` and **ZERO** with a whitelisted one, so the whitelist subsumes this bullet on today's tree — CONTINGENTLY, unlike `.tsbuildinfo`'s, which it subsumes unconditionally — while `packages/plugin/hooks/` already holds **five** `.mjs` RECURSIVELY — four at its top level and one under `lib/`, re-derived read-only 2026-08-22 with `find packages/plugin/hooks -type f -name '*.mjs'`, the recursive form because a `.js` ANYWHERE beneath an entry is copied by `bundle-cli.mjs:178` (standing rule 1), so the `.js` this fixture pins is one commit away on the real tree. Nothing else about this fixture moves: `dist/plugin/**` is outside the filtered comparand under BOTH walked sides, so the marker's at-or-after-the-newest-§3-filtered-output clause is unaffected, `packages/cli` is exempt from §3.1 by `kind`, and the added file is beneath an entry clause 1 already requires to EXIST                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | still `stale-unit` (req 1) — and **NOT `unbuilt`**: `dist/plugin/**` is outside §3's filtered output set, so §3.3's clause 2 is unaffected. This row is the proof that the freshness comparand is the filtered set and not a raw `dist/**` walk. ⚠️ **SECOND MUTANT (round 35, `C35-1`), on this RETURN-VALUE main half: an `isCompilerOutput` carrying §3's four-extension whitelist and its `.tsbuildinfo`, `dist/index.d.ts` and `dist/eo-*-fixture-*/**` bullets while OMITTING `dist/plugin/**`.** On the AMENDED mutation it reads `dist/plugin/hooks/hook.js` at `now` as this unit's newest output, so the stale `src` is no longer newer and `stale-unit` does NOT fire, while the marker — at or after the newest §3-filtered output per the complete-cli-build rule — is now OLDER than that file, so §3.3's clause 2 returns `unbuilt packages/cli`: it must FAIL on BOTH halves of this verdict, on this row's OWN fixture and with no injection point (rows 44, 47 and 48's discipline). Its harm is `C18-6`'s one bullet over and its window is STRICTLY WIDER — a `packages/cli/src` edit landing between the esbuild write (`bundle-cli.mjs:121`) and the plugin-copy loop (`:177-179`), re-derived read-only 2026-08-22 at `a5605e9` as **36.46 ms** on this working copy (the newest file beneath `packages/cli/dist/plugin` at mtimeMs **1787410216218.5693** against the newest §3-filtered output at **1787410216182.1096**) against the **11.10 ms** `C18-6` measured for `:121`→`:153` — masked SILENTLY at PASS, exit **0 even under `--strict`**, at `check:all` member 0 and under flagless `pretest`. ⚠️ **`OP16-3`'s and `CF20-1`'s disqualifier checked AT the fix: a CORRECT body cannot FAIL this row on the amended mutation** — `dist/plugin/**` is outside the filtered comparand under both walked sides, so the touch moves nothing `checkUnitFreshness` or clause 2 reads and both halves of this verdict stand. ⚠️ **Standing rule 8: the raw-`dist/**` mutant this row already killed is killed by the amended mutation too**, a raw walk taking `hook.js` exactly as it took the `.json`, and this row's round-18 TWIN is untouched, its own mutation being `packages/cli/dist/index.d.ts`. ⚠️ **TWIN, in row 33's shape so nothing is renumbered (round 18, `C18-6`):** the same fixture — stale `packages/cli/src`, marker newest — with `utimesSync packages/cli/dist/index.d.ts` to now INSTEAD of `dist/plugin/hooks/hook.js` (`dist/plugin/.mcp.json` before round 35, `C35-1`); verdict still `stale-unit packages/cli`, explicitly NOT `unbuilt` and NOT clean. This row's own mutation exercises the PLUGIN exclusion and its verdict says so, so an `isCompilerOutput` carrying the `dist/plugin/**` and `dist/eo-*-fixture-*/**` exclusions but KEEPING `dist/index.d.ts` passed it unchanged. Re-derived read-only at `896dd84` over this battery's whole mutation column: ZERO rows move `packages/cli/dist/index.d.ts`'s mtime — row 8's mutation is a plugin SOURCE, row 44 is a non-cli scratch row, row 32 DELETES the file rather than moving it, and row 22's is a CONTENT rewrite at `mtime UNCHANGED or NEWER` whose verdict is byte-keyed, so under the UNCHANGED reading nothing on the mtime side moves at all. Mutant = that implementation, which reads the `copyFile` timestamp as this unit's newest output and reports CLEAN — or, where the marker is then older than it, `unbuilt` through §3.3's clause 2 — and must FAIL on BOTH branches. ⚠️ **The live exposure is BOUNDED and the bound is re-derived rather than asserted (standing rule 2):** read-only on this working copy, `packages/cli/dist/index.d.ts` at `1787319590020415462` against the newest esbuild output `dist/index.js` at `1787319590009315482` — **11.10 ms**, EQUAL at second granularity — so on a completed build the masked window is a `packages/cli/src` edit landing between `bundle-cli.mjs:121` and `:153`, which bounds the cost and does not excuse the missing element                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 8   | plugin assets fresh — PLUS exactly ONE path under `packages/plugin/` that is NOT one of the six `PLUGIN_ASSET_ENTRIES`: `packages/plugin/workflows/stage-loop.mjs`, a plain FILE under a NON-MEMBER DIRECTORY, with an mtime NEWER than the newest file beneath `packages/cli/dist/plugin` (round 28, `C28-1`). That seventh path is what makes §3.4's source-side SCOPE observable, and it is the live tree's own non-member: `packages/plugin/workflows/` exists in the source tree today, holds two tracked files, is copied nowhere by `bundle-cli.mjs` and is reached by no input rule — §3.3's clause 1 and this section's `walk.mjs` import comment already name it as the drift vector. It sits outside every unit's `src` and `dist` and under no unit this fixture's root `tsconfig.json` references — row 41's own wording, copied whole (standing rule 7) — so no unit, no stem and no output-set member is introduced and §3.1 is untouched; and it is spelled as a plain file under a non-member DIRECTORY and never as `packages/plugin/dist/.tsbuildinfo`, which would make this fixture's `packages/plugin` a unit and move this row's unit counts. It adds no member to `PLUGIN_ASSET_ENTRIES` and says nothing about how many of the six this fixture holds, so row 24's "its fixture need not contain the file member at all" survives verbatim (round 26, `C26-1`'s own ruling)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | touch `packages/plugin/skills/**` forward                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `stale-plugin-assets` (§3.4) at the END state, and NO finding at BASELINE — this section's own "one fixture reporting clean, then ONE mutation that must flip it" rule, spelled out here because the SECOND MUTANT below fails on the BASELINE. ⚠️ **SECOND MUTANT (round 28, `C28-1`), on this RETURN-VALUE main half: a `checkPluginAssets` whose SOURCE side is `newestUnder(join(cwd, "packages/plugin"))`** — one call, no mixed-type handling, and `cwd` is the only argument §2's signature offers. It reports `stale-plugin-assets packages/cli` at BASELINE on this row's own fixture, whose seventh `packages/plugin/` path is NEWER than the newest file beneath `packages/cli/dist/plugin`, so it FAILS with no injection point (rows 44, 47 and 48's discipline); the END state's assertion still PASSES under it, so the BASELINE is the half that kills it. ⚠️ **Nothing else in this design sees it, which is why the clause is here:** §3.4's mandate is TWO obligations — the SCOPE (the six entries, never the package that CONTAINS them) and the MIXED TYPE — and §2's `newestOfEntries` declaration and row 24 both argue the mixed-type half alone, whose `ENOTDIR` a walk of the package ROOT never reaches, never handing `.mcp.json` to `readdirSync`; this row's and row 24's mutations both move a path INSIDE the six, so both flip under the wrong body; the `importsAssetEntries(walk)` wiring assertion still passes, §3.3's clause 1 needing that same import for its own presence check; and the live smoke test asserts the unit SET and the coverage SHAPE and explicitly not the finding set. ⚠️ **It cannot FAIL a correct implementation (`OP16-3`'s and `CF20-1`'s disqualifier, settled before the clause was written):** `newestOfEntries(PLUGIN_ASSET_ENTRIES)` never reads that path, so the baseline stays clean and the END state keeps its single finding. ⚠️ **Standing rule 8:** this row still asserts no `full` `plugin-assets` COVERAGE entry, so the complete-cli-build rule's plugin-SOURCE clause — keyed on what a row ASSERTS — still does not reach it, and row 24's non-redundancy argument is untouched, which is the collision round 26 refused the row-list form for. ⚠️ **RENDERED TWIN, in row 33's shape so nothing is renumbered and §1's battery count is UNAFFECTED — the SET-shaped form `C11-4` mandates, and never the figure itself, which round 21's appended row moved out from under the "of 48" this cell carried until round 22 (round 20, `OP20-1`; corrected round 22, `OP22-3`):** on this row's own END STATE, `formatFindings`' output must carry, as a whole LINE and by EQUALITY, `check-stale-dist: stale-plugin-assets ×1 — packages/cli` — §4's collapse applies at every N including N=1 (`OP15-3`), this kind is structurally N=1 (`OP18-2`), and every assertion in this group carries the prefix, so the uncollapsed spelling would FAIL a correct implementation (`OP16-3`'s class, settled before the row was written). Mutant = a `formatFindings` whose grouped-kind SET omits this kind — `new Set(["stale-unit", "stale-bundle", "stale-declarations"])` — and falls through to §4's generic comparison shape `<kind> <unit> <newerInput> <olderOutput> <deltaMs>` for it while collapsing correctly at every N for the other three. It is NOT row 28's N=1 twin's mutant, which collapses at N≥2 for EVERY kind and which rows 28, 38 and 43's CLI twin already kill; this one passes all three of those, passes this row's and row 24's return-value halves (both read the KIND, not the line), passes row 29 (whose five fixtures are the delta-LESS kinds), row 36 (two UNGROUPED `unbuilt` lines), the remedy-plan rows, the false-negative battery, the wiring assertions and the live smoke test — while on this row's own end state, a plugin source edited without a rebuild, it prints the three fields §4 states are `--json`-only and reach NO human path (round 15, `OP15-3`), at `check:all` member 0, exit **1** under `--strict`, and at line 5 of §5.1's measured 204 under `pretest`. It needs no injection point beyond the formatter swap, because it FAILS on this row's own fixture, whose end state carries exactly ONE finding. This row is the host rather than row 24, whose fixture is the FILE-member case and whose subject is that member (`CF17-3`'s shape: one fixture, one subject); this row is already in the complete-cli-build list, so `packages/cli` is `compared` and `checkPluginAssets` runs. This TWIN changed nothing in the return-value main half, so nothing that half already pinned was vacated (standing rule 8) — the present tense stood here from round 20, and round 28's SECOND MUTANT above is a MAIN-half element and no part of this twin, so the sentence is scoped to the append it was written about rather than left as a claim about this row's current state (`C11-4`'s class at a state description)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 9   | unit built                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `rm -rf <unit>/dist/*` keeping `.tsbuildinfo`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `unbuilt` (round 3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 10  | bundle built WITH metafile, all outputs present                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | delete `.bundle-meta/metafile.json` only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `bundle-provenance-missing`, and **not** `unbuilt` — the marker no longer feeds the `unbuilt` rule (§3.2, round 9) **AND**, in the RENDERED output, the entry `bundle freshness not run (.bundle-meta/metafile.json absent)` — §4's renderer, not the raw value shape: `bundle-freshness: not-run (…)` is what `result.coverage` HOLDS and what the direct-call rows 12 and 23 assert, and it never appears in rendered text (round 10). §3.2's `unbuilt` suppression silences a FINDING, never the coverage entry — there is no precedence rule and this cell asserted one (round 13, CF13-4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 11  | cli built COMPLETE: metafile + 5 chunks + 3 entry outputs + `dist/index.d.ts` + the six plugin entries                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `rm dist/bin.js` only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `unbuilt` — the fixture must be complete, or the baseline already carries a finding and the row is vacuous. Round 9: without the metafile the baseline is now `bundle-provenance-missing` rather than `unbuilt`, and with `dist/index.d.ts` or the plugin entries missing it is `unbuilt` before the mutation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 12  | false-negative: `packages/cli/dist` absent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | run `checkPluginAssets`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | no finding, no throw, and coverage `plugin-assets: not-run (packages/cli/dist absent)` — bare "skipped" is unassertable under §2's return shape                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 13  | unit built: outputs + `.tsbuildinfo`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `rm -rf <unit>/dist/*` INCLUDING `.tsbuildinfo`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `skipped`, not `unbuilt` — row 9's twin; the pair is what a `built: boolean` cannot satisfy (§3.3's reason table, rows 1-2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 14  | unit whose `dist/` holds outputs + an `eo-*-fixture-*/` dir **that holds at least one `.js` whose stem has no `src` counterpart** — ⚠️ **the clause is round 16's fix (`C16-1`): without it the scratch directory may be EMPTY, and then this row's baseline and its end state read the same under an implementation that excludes scratch and one that does not, so nothing in this battery read §3's exclusion at all. With it, the BASELINE fails a `stemsUnder` carrying no exclusion — it reports an orphan before the mutation runs — and the END STATE fails an `isCompilerOutput` carrying none, which reads the scratch `.js` as a qualifying output and returns `compared` rather than `skipped`. Row 44 pins the remaining half, the FRESHNESS comparison's newest-output side, which this row's mutation never reaches**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `rm` the outputs and `.tsbuildinfo`, keep the scratch dir                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `skipped` — a test `mkdir`ed it; nothing was ever built here (§3.3's reason table, row 3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 15  | rows 9, 13 and 14's end states                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | none — assert `unitState(unit).reason`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | the three LITERAL reason strings §3.3's reason table gives its `.tsbuildinfo`, empty and scratch rows (round 13, OP13-2), not merely three that differ — §4's `unbuilt` line prints one verbatim and the units-compared/skipped summary names the others on BOTH paths (round 13, OP13-1), so one collapsed string is a silent regression and one unprintable string is a worse one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 16  | cli built COMPLETE, as row 11, marker present and newest                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `rm -rf packages/cli/dist` (§4 recipe step 1, the design's own printed remedy)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | rendered output contains `check-stale-dist: ⚠️ coverage: bundle freshness not run (packages/cli/dist absent)` — §4's PREFIX and LABEL, then the entry: a leading substring of the coverage line, never the whole line. Contiguous because §4 renders only the non-`full` entries and `bundle-freshness` is FIRST among them here — this mutation also puts `plugin-assets` and `shipped-declarations` at `not-run` (§3.3, §3.5), while `declaration-cache` still runs `full` because `.dts-cache/` is a sibling of `dist`, so nothing renders between the label and this entry. ⚠️ **Round 11: the previous text asserted the BARE entry**, which fails a correct implementation (§4 mandates prefix + label before the first entry) and passes one that emits no prefix at all. Unit `skipped`, still exit 0. ⚠️ **Round 10 replaced the previous fixture and mutation**, whose asserted text `bundle freshness reduced (no esbuild chunk)` was UNSATISFIABLE — a chunkless `packages/cli` is `unbuilt` through §3.3's clause 1, so §3.2's fallback never ran — and whose "metafile + 3 entries + 5 chunks" baseline was already `unbuilt` before the mutation, missing `dist/index.d.ts` and the six plugin entries. Row 25 now carries the chunk gap                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 17  | cli built, `.dts-cache/index.d.ts` present                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `rm -rf packages/cli/.dts-cache`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | rendered output contains `check-stale-dist: ⚠️ coverage: declaration cache not run (packages/cli/.dts-cache/index.d.ts absent)` — §4's prefix, label and renderer applied to §2's comparison id and §3.3's mandated reason, all verbatim. A leading substring of the coverage line, never the whole line, and contiguous because `declaration-cache` is FIRST among the non-`full` entries here: this row starts from a COMPLETE cli build with the marker written last (the rule below), so `bundle-freshness` and `plugin-assets` both stay `full`, and this mutation also puts `shipped-declarations` at `not-run` (§3.5, same absent file) BEHIND it in §2's union order. ⚠️ **Round 11 added the prefix and label**, for row 16's reason. No finding, exit 0. ⚠️ **Round 10:** the previous text `declarations not run (.dts-cache absent)` failed on BOTH halves — no renderer produces `declarations`, and that reason substring occurs in no section. ⚠️ **SECOND ASSERTION on this row's rendered output (round 18, `C18-3`):** it must ALSO contain `shipped declarations not run (packages/cli/.dts-cache/index.d.ts absent)`, §3.5's SECOND mandated spelling, which follows the first here separated by §4's `·` join and is not part of this row's existing leading-substring assertion. This row's mutation is already `rm -rf packages/cli/.dts-cache`, so that spelling is producible here and nowhere else in this section: rows 23 and 26 and row 23's round-17 twin assert `packages/cli/dist/index.d.ts absent` or `packages/cli unbuilt`, and no other fixture leaves the cache absent while the shipped copy survives. Mutant = a `checkShippedDeclarations` hardcoding `packages/cli/dist/index.d.ts absent` for BOTH absent sides: it passed every row while §4's recipe step 5 named an artifact that EXISTS and left the absent one unnamed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 18  | a fixture whose findings are ADVISORY-ONLY (exactly one `bundle-provenance-missing`, as row 21) AND with ≥1 non-`full` comparison — the only combination §4's exit table renders a `PASS` line and a finding together (round 11: with a non-advisory finding there IS no PASS line, so the previous fixture could not assert the prefix on it)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | none — assert the RENDERED text of both paths                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | the coverage line appears on the findings path too; asserting only the PASS path is how this fix half-lands. Both asserted strings begin `check-stale-dist: ` (§4) — this is the row an unprefixed implementation fails. ⚠️ **AND the rendered coverage line carries NO command from §4's remedy table (round 21, `OP21-1`)** — `rm -rf`, `npm run build`, `npm run bundle:types` or `npm run bundle:cli`. §4 rules that the line carries no remedy of its own because that table is keyed by finding KIND, and nothing read it: this row and rows 16, 17 and 26 assert the prefix, the label and one entry as a contiguous SUBSTRING, which a formatter appending the entry's producer still satisfies, and row 38 asserts only the label's absence on an all-`full` fixture. Mutant = a `formatFindings` that appends step 5's producer to each non-`full` entry, printing `bundle freshness not run (.bundle-meta/metafile.json absent) — npm run build` beside a plan that already says it, on this fixture and on every degraded tree. This fixture's one entry provably carries no such command, so the clause cannot fail a correct implementation (`OP16-3`'s class), and it is scoped to the coverage LINE and never to the output, because this fixture's plan legitimately prints step 2 and step 5. This row's two existing assertions are unchanged (standing rule 8). ⚠️ **AND THE ORDER OF THE TWO ASSERTED LINES ABOVE (round 24, `OP24-1`)** — on this row's own fixture the rendered index of `check-stale-dist: PASS — ` must be LESS than the rendered index of `check-stale-dist: bundle-provenance-missing packages/cli`, which is §4's ORDER rule's FIRST adjacency and the one of its four that no element in this design read. Mutant = a `formatFindings` that emits the grouped finding lines FIRST and the PASS/summary branch at the STANDALONE summary's slot — one branch written once, the cheapest way to satisfy §4's "EXACTLY ONE of those two positions, never both and never neither" — so it needs no injection point and FAILS on this row's own fixture. It passed everything else: rows 35 and 39 carry §4's other three adjacencies and every one of their fixtures holds either no `PASS` line (row 35, the findings path) or no finding line (row 39, ZERO findings); row 21's PLAN TWIN reads the plan block and row 29's THIRD fixture reads the finding line, both on this same advisory-only tree and neither on the `PASS` line; and this row's own two strings are asserted as SUBSTRINGS with no order between them. It cannot fail a correct implementation (`OP16-3`'s class, settled before the clause was written): this row's clean cell already establishes that BOTH lines render here — the only combination §4's exit table renders a `PASS` line and a finding together — and §4's ORDER rule and §2's `formatFindings` comment mandate this order in terms. ⚠️ **The wrong order is what this design's own cited precedent prints:** `check-citation-runs.mjs` writes its per-item `check-citation-runs: WARN …` lines at `:263` and the `PASS` line §4 cites TWICE at `:290` (re-derived read-only at `8fdf3ed`), items first and verdict last, so an implementer copying that shape gets the inverse of §4's rule on the tree §7's day-one row measures on every developer's machine. Every assertion this row already carries — its two rendered strings and round 21's coverage-command clause — is unchanged (standing rule 8), and nothing is renumbered: this clause ADDS NO ROW, which is the property to state rather than a live battery count §1 also carries (`OP22-3`). ⚠️ **AND THE `PASS` LINE'S SUMMARY TEXT AT `M = 0`, BY EQUALITY (round 25, `OP25-2`)** — `check-stale-dist: PASS — <N> units compared, 0 skipped`, `<N>` derived from THIS fixture and NEVER from §4's live figure (`OP16-3`), with NO parenthetical and no empty pair of parentheses. This row's fixture is the ONLY one in this section that reaches `M = 0` on a rendered `PASS` line — row 39's MAIN half pins that line at M=1 (`1 skipped (packages/cli: no dist/)`), row 35 pins the findings-path line at M≥2, row 39's NEGATIVE twin pins the `nothing was checked` carve-out, row 34 holds an `M = 0` fixture that asserts no summary text at all, and row 38's `M = 0` fixture pins the STANDALONE slot since round 26 (`OP26-1`) — both of its kinds are non-advisory, so §4's exit table renders no `PASS` line there and this row keeps the `PASS` slot to itself — and `M = 0` is the STEADY STATE, not an edge: after the first post-adoption build every unit compares, none is skipped, no finding fires and all four entries are `full`, so this ONE line is the entire human output of every green run, at `check:all` member 0, in both `test` legs of `ci.yml:129` and at line 5 of §5.1's measured 204 under `pretest`. Mutant = a `formatFindings` that renders §4's shape by direct transcription, `` `${n} units compared, ${m} skipped (${clauses.join(", ")})` ``, printing `0 skipped ()` — `OP16-4`'s bare-container class at the sibling line, which needs no injection point beyond the formatter swap this clause's siblings already use and FAILS on this row's own fixture. It cannot fail a correct implementation: §4's rule now mandates the omission at an empty clause list in terms (`OP16-3`'s class, settled before the clause was written). This clause ADDS NO ROW either, and round 24's ORDER conjunct is unchanged (standing rule 8).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 19  | cli built COMPLETE, marker newest                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `utimesSync` `dist/bin.js` forward past the marker                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `unbuilt` via §3.3's clause 2 **and its cli-reason-table row (round 13, C13-1 — until then the table gave this very fixture `compared`)**, reason naming `dist/bin.js` — one of **TWO** rows a presence-only marker implementation fails. Row 26 is the other, on THIS row's own end state: such an implementation reports the unit `compared`, so all four cli-scoped comparisons RUN and render `full`, where row 26 requires four `not run (packages/cli unbuilt)` entries. "The ONLY row" was false by one from the round that wrote it (round 13, C13-4), and it UNDERSTATED the battery's coverage of exactly the gap C13-1 names — which is the reassurance a reader would otherwise take from it. Together the two are what make the freshness clause non-vacuous (C9-1). ⚠️ **TWIN, in row 33's shape so nothing is renumbered (round 19, `C19-2`): clause 2's `>=` at its own BOUNDARY.** This row's OWN clean fixture, no mutation to `dist/bin.js`, with `mtime(.bundle-meta/metafile.json)` set **EXACTLY EQUAL** to the newest member of §3's filtered output set — what `bundle-cli.mjs:178` → `:181` produces on any second-granularity filesystem, and the state §7's `Second-granularity mtimes` row claims as mitigated. Verdict: `unitState("packages/cli").state` is **`compared`** and specifically NOT `unbuilt`, with NO `unbuilt packages/cli` finding. Mutant = clause 2 spelled `>` rather than `>=`, which returns `unbuilt packages/cli` on the twin's own fixture and so needs no injection point. ⚠️ **The twin asserts the unit STATE and NOTHING about the four coverage entries, deliberately (`OP16-3`'s class, caught in consolidation):** this row's clean fixture is bound by neither §6's `C15-2` sibling-fixture rule — its main half asserts `unbuilt`, not `stale-bundle` and not a `full` `bundle-freshness` entry — nor row 3's `.dts-cache/index.d.ts` clause, so `bundle-freshness`, `declaration-cache` and `shipped-declarations` are FREE VARIABLES on it and an `all four full` assertion would FAIL a correct implementation. That is the escape `OP18-4` closed at row 46 by giving its twin row 3's cache clause; here the state assertion alone is what the boundary needs, so no fixture clause is added. Until this twin the EQUAL case was a free variable everywhere in this section: the complete-cli-build rule licenses `at or after`, every other cli-scoped row says `marker newest` or `marker present and newest`, and the one equal-mtime element in the false-negative battery is §3's input-versus-output Verdict, not this mirror — so `>` and `>=` were discriminated by nothing while `>` prints `rm -rf packages/cli/dist` plus the ~5-minute rebuild at `check:all` member 0, exit 1 under `--strict`, on a build that completed correctly. ⚠️ **This row's MAIN half keeps `marker newest` and its forward mutation, so nothing it already pins is vacated (standing rule 8)**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 20  | cli built COMPLETE, as row 11                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `rm -rf packages/cli/dist/plugin/skills` only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `unbuilt`, gaps naming `dist/plugin` — the `:178` step's own row; without it the artifact set can ship as chunks + entries and rows 10-12 all still pass. ⚠️ **AND A RENDERED TWIN, in row 33's shape and added in round 19 (`OP19-1`):** on THIS row's own end state the printed finding line must read, by EQUALITY, `check-stale-dist: unbuilt packages/cli — bundle:cli did not finish; missing: dist/plugin/skills` — §3.3's cli reason table's `any other member of the §3.3 artifact set missing` row, printed verbatim by §4, whose own worked example spells the gap for this very mutation (`missing: chunk-*.js, dist/plugin/skills`) because clause 1 tests the six `PLUGIN_ASSET_ENTRIES` for the EXISTENCE of their TOP-LEVEL path. Mutant = a `unitState` that collapses that table's first two rows onto the ONE sentence `tsc -b --clean removed the compiler outputs from a completed bundle` and appends the gap list: it PASSES the verdict half and FAILS the twin — the exact mirror of row 32's mutant, so the two rows kill each other's collapse in both directions. Until this twin, row 32 was the ONLY cli-`unbuilt` row pinning any reason SENTENCE by equality and it pins the `--clean` spelling, so the collapse also passed rows 11, 19, 20, 25, 33, 36, 37 and 46, the false-negative battery, the wiring assertions and the live smoke test, and told an operator whose `bundle:cli` died at `bundle-cli.mjs:178` — or whose `npm run build` was interrupted during the ~5-minute `bundle:types` step — that a command they never ran had deleted their outputs, at exit **1** under `--strict` from `check:all` member 0. §4's own OP-2 wrong-diagnosis class, and `OP18-5`'s shape (one kind, two mandated spellings, only one asserted) at a second kind. Its single mutation is this row's OWN `rm -rf packages/cli/dist/plugin/skills`, NOT a mutant formatter — read off this row's mutation column, per the group bullet's rule. ⚠️ **The literal is `dist/plugin/skills` and not `dist/plugin` (round 19, consolidation):** §4's worked example, §3.3 clause 1's top-level-path test and `PLUGIN_ASSET_ENTRIES` all give the entry path, so the shorter spelling would FAIL a correct implementation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 21  | cli built COMPLETE, marker absent → exactly one advisory `bundle-provenance-missing`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | call `exitCodeFor(result, { strict: true })` AND `exitCodeFor(result, { strict: false })`, on this row's advisory-only fixture AND on the non-advisory fixture the verdict cell names — FOUR calls, two fixtures by two `strict` values (round 18, `OP18-1`)                                                                                                                                                                                                                                                                                                                                                            | **0**; and **1** for the same call on a fixture whose findings include a non-advisory kind. ⚠️ **AND exit 0 for BOTH fixtures under `{ strict: false }` — §4's exit-table DEFAULT column, asserted by nothing for seventeen rounds (round 18, `OP18-1`).** Re-derived read-only at `896dd84`: `exitCodeFor` occurs in §6 at this row and at the false-negative battery's truncated-marker bullet, both under `strict: true`, and the literal `strict: false` occurs **zero** times in this whole design — so an `exitCodeFor` that IGNORES its `strict` argument, `(r) => r.findings.some((f) => !ADVISORY_KINDS.has(f.kind)) ? 1 : 0`, passed both halves of this row and every other element in §6, none of which calls `exitCodeFor` at all. Rows 27 and 31 do run the CLI non-strict, and their exit 0 comes from `check-stale-dist.mjs`'s catch on a THROWING fixture, never from `exitCodeFor`. ⚠️ **Which is exactly why this row closes the FUNCTION and not the WIRING, and round 19 (`CF19-1`) had to put the process half at row 43:** these four calls are DIRECT, so the argument the CLI passes on its success path is observed by no element here, and neither CLI-running row observes it either — row 27's fixture is a malformed config and every exit code row 31 asserts is its LOAD-TIME mutant's, while row 31's clean baseline is a ZERO-finding tree where exit 0 is correct under either `strict`. A CLI hardcoding `{ strict: true }` — equivalently `process.exit(result.findings.length ? 1 : 0)` — therefore passes this row and every other element while blocking every developer's `npm test`. Row 43's CLI twin runs the flagless process on a findings-carrying fixture and asserts exit **0** with the finding rendered, plus `--strict` at exit **1**; the two halves are complementary, neither replaces the other, and this row's four calls are unchanged (standing rule 8). Wrong result: `pretest` is `node scripts/check-stale-dist.mjs` with no flag (§5, §5.1, and this section's own `expect(root.scripts.pretest).toBe(...)`), this section's live smoke prediction is FOUR `stale-unit` findings on this working copy, and §5.1's measured table gives a `pretest` exiting 1 as PRETEST FAILED with `test` never running — so every developer's `npm test` is blocked from the first run, round 1's `O-2` reached through the exit code rather than through a throw, and §4's whole report-by-default split silently void with every assertion in this section green. Without this row an implementer makes the advisory blocking and every row above still passes (§4's exit table, `ADVISORY_KINDS`). ⚠️ **Its PLAN TWIN, asserted in the same row in row 33's shape (round 18, `OP18-3`):** on this row's own advisory-only fixture, run `checkStaleDist` and `formatFindings` and the rendered output must carry `check-stale-dist: remedy plan:` whose steps are **step 2 and step 5, and NO step 1** — §4 step 1's `bundle-provenance-missing` carve-out, which has had a fixture here since round 11 and an assertion nowhere. Mutant = a formatter whose step-1 trigger is the finding SET rather than the three kinds step 1 names, which is what §4's remedy table invites by mapping this kind to a rebuild: it prints `rm -rf packages/cli/dist` on a COMPLETE tree, deleting a working CLI to restore a bookkeeping file, which is the exact consequence step 1's clause exists to prevent, and it passes every other row — rows 34, 38 and 39 — and, since round 34 (`OP34-1`), row 43's CLI twin — assert plans on fixtures where step 1 is correctly exempt by UNIT or unselected by kind. ⚠️ **"correctly SELECTED" stood in that list for six rounds and named a property of row 34's FIXTURE and never of its ASSERTION (round 25, `OP25-1`):** step 1 IS selected on row 34's second unit, and row 34's mutant-(a) verdict reads only "ONE `check-stale-dist: remedy plan:` block with its steps as indented continuation lines, in §4's stated order, positioned after the findings list" — it names no step at all, so no element anywhere in this section demanded step 1's PRESENCE. §6 row **28**'s PLAN TWIN is that element, added in round 25. It does NOT subsume this twin and this twin does not subsume it (standing rule 8): EVERY finding on row 28's PLAN TWIN's fixture sits on a unit step 1 SELECTS — `stale-unit` on two, `orphan-output` on one and `unbuilt` on one since round 33 (`OP33-1`) — so a step-1 trigger keyed on the finding SET, the mutant stated here, still selects step 1 correctly there and passes, while the mutants row 28's PLAN TWIN kills (no step-1 branch; first-selected-unit only; a selector keyed on any proper subset of step 1's three kinds) all pass this twin's advisory-only fixture, on which step 1 prints nothing under any of them. ⚠️ **The reason is stated over WHERE the findings sit and no longer as "row 28's finding set is `stale-unit` throughout" (round 33, `OP33-1`):** that sentence was true only of the unextended fixture and would have gone FALSE in the commit that extended it, while the non-subsumption it justifies never depended on the kinds at all. ⚠️ **The mutant is stated that way deliberately:** a formatter applying step 1's OWN printed condition already selects nothing here, since no `stale-unit`, `orphan-output` or `unbuilt` fired, which §4 step 4 states in terms — so a step-walking mutant would pass and this twin would be vacuous. ⚠️ **Its EXIT-CODE MIX TWIN, asserted in the same row in row 33's shape so nothing is renumbered and no battery row is added (round 23, `OP23-1`) — this row now carries TWO twins, this one and the round-18 PLAN TWIN above, so a citation must name which (`C20-2`):** on this row's own advisory-only fixture PLUS a SECOND, NON-cli unit under `packages/` whose `src` is newer than its `dist` — row 34's own second-unit shape, so every cli-scoped rule already binding this row is untouched — with that unit's `src` and `dist` in 1:1 stem correspondence so `checkOrphans` stays silent, and with `.dts-cache/index.d.ts` NEWER than every member of §3.3's comparand (row 3's own clause) so the added `src` fires no `stale-declarations` beside it. The finding set is then MIXED: exactly one advisory `bundle-provenance-missing` beside exactly one non-advisory `stale-unit`. `exitCodeFor(result, { strict: true })` must be **1** and `exitCodeFor(result, { strict: false })` **0**. Mutant = `result.findings.some((f) => ADVISORY_KINDS.has(f.kind))` standing in for `every((f) => ADVISORY_KINDS.has(f.kind))` as the exit-**0** test under `--strict` — "an advisory is PRESENT" read for "the findings are ONLY advisory", the De Morgan slip §2's own sentence is worded against — which needs no injection point because it FAILS on this twin's own fixture. It differs from a correct `exitCodeFor` on the MIXED set and — since round 27 MEASURED that claim rather than reasoning it (`OP27-1`) — on the EMPTY one too, returning **1** where a correct implementation returns **0**; advisory-only and non-advisory-only agree, which is why it survived to round 23, and §6 row 39's exit-code conjunct is what kills its second half. It cannot fail a correct implementation: §4's THIRD exit-table row and §2's "`--strict` exits 1 only when some finding's kind is NOT advisory" both mandate **1** here in terms (`OP16-3`'s class, settled before the twin was written). Until round 23 that mutant passed EVERYTHING, the MIXED set being a FREE VARIABLE at every site that could have pinned it: this row's main half names "the non-advisory fixture", which an implementer builds with no advisory in it; the false-negative battery's truncated-marker bullet, its `C10-4` conjunction and its §8(p) partial-copy bullet each carry exactly ONE advisory and assert exit 0 under `--strict`, which the mutant also gets right; row 43's CLI twin's end state carries exactly one NON-advisory `stale-declarations`, which it also gets right; rows 27 and 31 throw or hold zero findings; no mutant-formatter row CALLED `exitCodeFor` until round 27 put the conjunct on row **39** (`OP27-1`) — re-derived read-only at `53996f5`, the name occurred in this section at rows 21, 29 and 43 and in the false-negative battery, and row 29's occurrence is a CITATION to that bullet rather than a call; and the live smoke test asserts the unit SET and the coverage shape and explicitly no exit code. Re-derived read-only 2026-08-21 at `53996f5`: `ls -d packages/cli/.bundle-meta` returns no such file while `packages/cli/dist` holds `bin.js`, `index.js`, `index.d.ts`, `bin/`, `plugin/` and five chunk-pattern files, so THIS working copy is the mixed state — the four `stale-unit` findings this section predicts beside the one advisory §3.4 and §7 measure on every tree built before this change set lands — and under that mutant `npm run check:stale-dist -- --strict` exits **0** at `check:all` member 0, the other 14 members run, and the chain reports green on a stale `dist`: `--strict` void in the one direction §4 calls the worst available answer for the incident this check is named for, the exact mirror of `OP18-1`, which closed this function in the other direction and left this half open. A RETURN-VALUE twin, so it does not join this section's rendered-text group (`C20-2`). This row's MAIN half — its FOUR calls and its two existing fixtures — and its PLAN TWIN are unchanged, so nothing they already pin is vacated (standing rule 8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 22  | cli `compared`: `.dts-cache/index.d.ts` and `dist/index.d.ts` byte-identical                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | rewrite `dist/index.d.ts` as a small `export * from "./errors.js"` barrel, mtime UNCHANGED or NEWER                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `stale-shipped-declarations` (§3.5) — the mutation is `tsc -b`'s own output, so a row that flips only on `utimesSync` would pass an mtime oracle and prove nothing. ⚠️ **Its TWIN, asserted in the same row, in row 33's shape (round 13, C13-2):** the same fixture with the marker **ABSENT** — the state of every tree until the first build after this lands — and with `dist/index.js` and `dist/index.d.ts.map` rewritten in the SAME emit, which is what a real `tsc -b` does (`tsconfig.base.json:22-24` sets `declaration`, `declarationMap` AND `sourceMap`, measured 2026-08-20). Clause 2 does not evaluate, so `packages/cli` stays `compared` and the run must report exactly this kind PLUS one advisory `bundle-provenance-missing`, with `bundle-freshness` at `not-run (.bundle-meta/metafile.json absent)` and the other three comparisons `full`. Mutant = an implementation built from §7 row 4's pre-C13-2 cell, suppressing §3.5 whenever a tsc barrel is present: it passes the marker-PRESENT half and FAILS this one. ⚠️ **AND THIS TWIN'S TWO DECLARATION FILES ARE PINNED TO ONE BYTE LENGTH, BECAUSE `bytes differ` AND `sizes differ` COINCIDED AT EVERY ELEMENT IN THIS DESIGN (round 34, `C34-1`).** On this TWIN's fixture ALONE, `.dts-cache/index.d.ts` and `dist/index.d.ts` are byte-identical AND exactly **28** bytes — `export declare const abc: 1;`, a plain declaration and deliberately NOT itself a barrel, so this twin's EXISTING mutant still sees a barrel at the END state and nowhere else — and this twin's rewrite writes `export * from "./errors.js";`, a DIFFERENT `tsc`-shaped barrel of the SAME **28** bytes, so at the end state the two files' SIZES are EQUAL while their BYTES differ. SECOND MUTANT, failing on this twin's own fixture with no injection point (rows 44, 47 and 48's discipline): a `sameBytes` that compares SIZE ALONE and never reads content. §2 declares it `size, then content` and §3.5's Rule spells it `size first, then content`, so dropping the second step is the reading BOTH sites invite, and it reports CLEAN here and must FAIL. Nothing else in this design sees it — re-derived read-only 2026-08-22 against `git show 5e4918f:<file>`, the COMMITTED document round 34 read, the working copy being read only as text and carrying this same round's other lens's in-flight edits, which is why the sha and not the worktree is the subject: `size` occurs **zero** times in this section and `sameBytes` occurs in this design only at §2's declaration and §2's purity sentence, never here — both `grep -c` over the `## 6.`-to-`## 7.` slice, the command written out in §9's round-34 entry rather than inline, a pipe inside a table cell being read as a column boundary (§6 row 26 carries the precedent this deliberately does not copy). Every OTHER fixture that makes the two files DIFFER is this row's MAIN half's barrel rewrite — that half, §6 row 29's FOURTH fixture, which is its end state, and row 34's SOLE-KIND TWIN, which cites `row 22's own mutation` — and on all three the two files' SIZES are a FREE VARIABLE, no cell pinning the cache's length, so the natural construction (a realistic cache against a small barrel, §3.5 measuring the live pair at **230,199** bytes against a `tsc` barrel three orders of magnitude smaller) makes the sizes DIFFER and a size-only body FIRE, agreeing with a correct one. Every remaining cli fixture binds the pair byte-identical (the complete-cli-build rule's `CF8-2` cache clause) or leaves a side absent, and the false-negative battery's `--force`-identical-bytes bullet has EQUAL sizes AND EQUAL bytes, clean under both. The live tree cannot discriminate either: re-derived read-only 2026-08-22, `cmp` reports `packages/cli/.dts-cache/index.d.ts` and `packages/cli/dist/index.d.ts` IDENTICAL at **230,199** bytes each, so §6's live smoke test — which asserts the unit SET and the coverage shape and explicitly not the finding set — is blind to it. Wrong result on the tree §3.5 was ADDED for: §4's recipe step 3 (`npm run bundle:types -- --force`) then stop — the state §3.5's own counterexample paragraph is built on — after any source edit whose regenerated declarations keep the byte COUNT, an exported identifier renamed to another of the same length being the ordinary case, leaves the published `types` entry (`packages/cli/package.json` `exports["."].types`, inside `files`) holding the pre-force copy while the check prints PASS, exit **0 even under `--strict`** at `check:all` member 0 and at line 5 of §5.1's measured 204 under flagless `pretest`. That is §3.5's founding silence at the comparison round 9 added to END it, and it falsifies §8(d)'s stated exception in terms — `compares BYTE FOR BYTE … that establishes the shipped copy is the bytes bundle-types.mjs last produced` — which a size comparison establishes for the LENGTH alone. ⚠️ **`OP16-3`'s and `CF20-1`'s disqualifier, checked BEFORE the clause was written: it cannot FAIL a correct implementation** — §3.5's Rule is `their bytes differ`, and a byte comparison reports this kind here whatever the two sizes are. ⚠️ **Standing rule 8, checked AT the fix:** the pin is this TWIN's alone, so this row's MAIN half keeps its `mtime UNCHANGED or NEWER` rewrite against a size-UNPINNED cache, §6 row 29's FOURTH fixture — that MAIN half's end state — keeps rendering `(<a> vs <b> bytes)` with two DIFFERENT numbers under any construction its own cells license, row 34's SOLE-KIND TWIN is untouched, and this twin's own verdict, coverage entries and existing mutant are unchanged, a same-length barrel being a `tsc` barrel. Written as a twin rather than a new numbered row so nothing is renumbered                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 23  | false-negative: cache present, `dist/index.d.ts` absent (what `rm -rf packages/cli/dist` leaves)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | run `checkShippedDeclarations`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | no finding, no throw, and coverage `shipped-declarations: not-run (packages/cli/dist/index.d.ts absent)` — row 12's shape, and for row 12's stated reason: this row CALLS the comparison, so the return value is the only surface, and a bare "skipped" is unassertable under a `Finding[]` return, where it degenerates to `toEqual([])` — indistinguishable from "ran and found nothing", the early `return []` §3.3's **SKIP RULE — ALL FOUR cli-SCOPED COMPARISONS** bans as "the founding silence in a smaller box". Written in the same round as the return shape that makes it assertable (round 10). ⚠️ **TWIN, in row 33's shape so nothing is renumbered (round 17, `C17-3`):** the same direct call on a fixture where `packages/cli/dist/index.d.ts` AND `packages/cli/.dts-cache/index.d.ts` are BOTH absent — a fresh clone (§5; NOT a CI job, round 25, `CF25-3`) — must return coverage `shipped-declarations: not-run (packages/cli/dist/index.d.ts absent)` and that spelling ONLY, per §3.5's precedence line. Mutant = an implementation naming whichever side it happens to test first: it renders the `.dts-cache` spelling here and passes this row's original half unchanged, so until this twin nothing in this design discriminated the two mandated renderings of its commonest and quietest tree                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 24  | plugin assets fresh, cli built COMPLETE (as row 11)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | touch `packages/plugin/.mcp.json` forward                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `stale-plugin-assets` (§3.4) — the one **FILE** member of `PLUGIN_ASSET_ENTRIES` (`bundle-cli.mjs:89`); the other five are directories. Row 8's mutation is `packages/plugin/skills/**`, a DIRECTORY member, and its fixture need not contain the file member at all — so without this row a directory-typed source walk can pass every other row and still throw `ENOTDIR` on the real tree, which §4 turns into `ERROR`, exit 2 (round 10). ⚠️ **TWIN, in row 33's shape so nothing is renumbered and NO ROW is added (`OP22-3`) — the EQUAL-MTIME TWIN (round 36, `C36-1`): §3.4's Rule mandates STRICTLY exceeds and no fixture in this section took that BOUNDARY.** This row's OWN clean fixture, NO mutation, with the newest of the six `PLUGIN_ASSET_ENTRIES` sources re-pinned to EXACTLY the mtime of the newest file beneath `packages/cli/dist/plugin` — still `plugin assets fresh`, equality being clean under the mandated reading. Verdict: NO `stale-plugin-assets` finding, and NO `full` `plugin-assets` coverage assertion, so the complete-cli-build rule's plugin-SOURCE clause — keyed on EVERY ROW ASSERTING such an entry and quantifying `OLDER than` — does not reach this twin and is neither weakened nor excepted by it (`C26-1`, standing rule 8). Nothing else moves: that same clause puts the six outside every unit's `src` and `dist` and under no unit the fixture's root config references, so the re-pinned source reaches no other comparison and §3.1 is untouched. Mutant = §3.4's comparison spelled `>=`, which reports `stale-plugin-assets packages/cli` on this twin's own fixture and so needs no injection point. A RETURN-VALUE twin, so it joins neither this section's rendered-text group nor its TWIN-halves list (`C20-2`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 25  | cli built COMPLETE, as row 11, its five chunk-pattern files pinned by NAMING FAMILY — FOUR matching `/^chunk-[A-Z0-9]{8}\.js$/` and ONE matching `/-[A-Z0-9]{8}\.js$/` WITHOUT the `chunk-` prefix, which is the live tree's own shape under `splitting: true` and which no fixture in this battery pinned until round 33 (`C33-1`); the marker's `outputs` are bound to exactly those five by the complete-cli-build rule below, cited rather than restated (`CF6-2`'s remedy shape)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `rm` the ONE chunk-pattern file that does NOT carry the `chunk-` prefix, leaving the other four in place — the SECOND naming family DELETED ALONE, which no element in this section did until round 33 (`C33-1`). Until then this cell read `rm packages/cli/dist/chunk-*.js packages/cli/dist/run-dispatcher-*.js` — BOTH live naming families at once (verified 2026-08-20: four `chunk-*.js` plus `run-dispatcher-POLZZ2DH.js`) — and this row names its gap DESCRIPTIVELY rather than by equality, so a body keyed on the `chunk-` prefix, naming four gaps, satisfied it exactly as a correct body naming five did | `unbuilt`, the gap naming the DELETED second-family path — §3.3 clause 1's `:121` chunk member, whose membership is `metafile.outputs`-derived on this row's marker-PRESENT fixture (§3.3, round 12, `C11-6`), so a chunk-pattern entry of `outputs` with no file on disk IS a gap. That member had NO row until round 10. Clause 2 cannot catch it (deleting an output LOWERS the newest output mtime) and clause 3 cannot (the three entries survive). Mutant = an `isChunk` keyed on the `chunk-` PREFIX rather than on the `-[A-Z0-9]{8}\.js$` SUFFIX: the deleted file is in neither its derived set nor its fallback, clauses 2 and 3 still hold, and it reports `compared` with ZERO findings — **exit 0 even under `--strict`** on a `packages/cli` short one esbuild output, verbatim `C11-6`'s harm at the naming family `C11-6` did not name. It fails on this row's own fixture with no injection point, and so does a body carrying NO chunk member at all, which is what this row proved before round 33 — ⚠️ **`the only row that makes the chunk member non-vacuous` stood here and was already false**, round 12's row 33 killing that same body on its own fixture; `C11-4`'s class at an exclusivity claim rather than at a count. ⚠️ **SCOPE, stated rather than overclaimed (round 33, `C33-1`): this element discriminates clause 1's MEMBERSHIP and NOT `bundleAt`'s domain**, whose harm is not live on this tree — re-derived read-only 2026-08-22, all five chunk-pattern files under `packages/cli/dist` carry the identical mtime **1787401912826.4854**, one build having written them, so a minimum over four equals the minimum over five here and the live smoke test cannot see the difference either. It replaces the coverage assertion row 16 used to carry, which no correct implementation could print                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 26  | row 19's end state: cli built COMPLETE, marker newest, then `utimesSync dist/bin.js` forward past the marker                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | none — assert the RENDERED text of the coverage line                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | all FOUR entries present: `check-stale-dist: ⚠️ coverage: bundle freshness not run (packages/cli unbuilt) · declaration cache not run (packages/cli unbuilt) · plugin assets not run (packages/cli unbuilt) · shipped declarations not run (packages/cli unbuilt)` — §4's renderer, §2's union order, prefix and label contiguous with the first entry. NOTHING is absent on this fixture, so an absence-keyed implementation prints no entry at all and emits a line §4's recipe step 5 reads as "all six ran" when four did not, including requirement 3's and `stale-declarations`' (round 10, C10-1). Rows 16-18 are all absence-triggered, so every one of them passes an absence-keyed implementation. ⚠️ **AND, on this SAME fixture, the rendered line beginning `check-stale-dist: unbuilt packages/cli — ` must match the WHOLE mandated line and nothing else — a `^` anchor immediately before `check-stale-dist: `, end-of-line immediately after `output`, and between them, with the two mtime slots left FREE as `\(.+\)`, `check-stale-dist: unbuilt packages/cli — dist/bin\.js \(.+\) is NEWER than the last completed bundle \(\.bundle-meta/metafile\.json, .+\); tsc -b has overwritten the bundled output` (round 29, `OP29-1`, WIDENED from a bare SUFFIX to the whole line in round 30, `OP30-1`)** — the SUFFIX half pinned one END of this line and its MIDDLE was SPELLED by no element at all: re-derived read-only 2026-08-22 at `187eb2d`, `git status --porcelain` listing nothing but this design file before and after, `grep -c -F 'is NEWER than the last completed bundle'` returns **3** over the whole file — §3.3's reason table, §4's line list and §7's `npm run typecheck` row — while `awk '/^## 6\./,/^## 7\./' \| grep -c -F` the same string returns **ZERO**, and the two rows that reach the reason name it by REFERENCE only, row 19's verdict saying "reason naming `dist/bin.js`" — the offending FILE alone — and row 37's saying "carrying the clause-2 reason", a which-clause-fired discrimination (`C14-1`) on a row its own cell calls "a verdict row, not a rendered-text one". So a `unitState` whose clause-2 reason read `dist/bin.js is stale; tsc -b has overwritten the bundled output` — naming the offending FILE and ending with the mandated literal, carrying NEITHER the marker path NOR either mtime — satisfies both references and this row's SUFFIX alike, printing an unverifiable one-clause assertion about a file `ls` shows present and recent on the one tree §7 row 2 calls a CORRECT firing nobody may mute as noise, at `check:all` member 0, exit **1** under `--strict` and at line 5 of §5.1's measured 204. Mutant for the WIDENED conjunct = a `unitState` emitting that one-clause reason, which needs no injection point and fails on this row's own fixture. ⚠️ **Standing rule 8, checked AT the widening:** the two free groups keep the mtime FORMAT unpinned, which is round 29's own stated reason for not pinning it, and the end anchor is carried over UNCHANGED, so round 29's bare-container mutant is still killed here and by nothing else; §3.3's cli reason table and §4's line list mandate this string with only the two mtimes varying, so no correct implementation can fail it (`OP16-3`'s disqualifier). This fixture is the only one in this battery reaching §2's THIRD cli branch, where the gap list is EMPTY by construction, and §4's `unbuilt` line list ends that sentence exactly there, so a correct implementation cannot fail it (`OP16-3`'s disqualifier, settled before the conjunct was written; re-derived read-only at `c672565`, that literal occurs at §3.3's reason table and §4's line list and **zero** times inside this section, while all FIVE `missing:` occurrences inside this section carry NON-EMPTY gap lists). Rows **19**, **26** and **37** are this battery's only clause-2 `unbuilt` fixtures — 19 and 37 are return-value rows round 19 deliberately did not move into the rendered group, and 26's rendered assertion was the coverage line alone — so this row is the only available host. Mutant = ONE composition point for the cli `unbuilt` line, `<reason>` followed unconditionally by `"; missing: " + gaps.join(", ")` — which §4's grouping paragraph licensed in terms until this round — printing a bare trailing container here while still PASSING row 20's RENDERED TWIN and row 32, whose gap lists are non-empty, so it is killed by this conjunct and by nothing else; it needs no injection point, failing on this row's own fixture (rows 44, 47 and 48's discipline), and rows 19's and 37's verdicts assert the clause-2 REASON by containment rather than the rendered line, so neither sees it. The conjunct is on this MAIN half ONLY: the twin's five deletions make its `packages/cli` a clause-1 `unbuilt` with a NON-EMPTY gap list, where the same mutant passes. ⚠️ **Standing rule 8, checked AT the fix:** the coverage-line equality, its absence-keyed mutant and the twin are untouched, nothing they pin is vacated, no fixture clause is added, and the row still asserts no `full` `plugin-assets` entry, so the complete-cli-build rule's plugin-SOURCE clause — keyed on what a row ASSERTS — still does not reach it. ⚠️ **TWIN, in row 33's shape so nothing is renumbered (round 19, `C19-1`) — this row's MAIN half proves the entry is EMITTED, the twin proves it OUTRANKS an available absence string.** Same end state, then `.bundle-meta/` REMOVED, every chunk-pattern file under `packages/cli/dist` removed, `dist/plugin/` removed, `dist/index.d.ts` removed and `.dts-cache/index.d.ts` removed — the three entry outputs surviving, so `dist` still yields qualifying compiler output, the unit-state table still reaches the `compared` branch, and §3.3's clause 1 makes it **`unbuilt`** on its gaps. Every one of the four entries now has a COMPETING absence spelling live: `packages/cli/dist holds no esbuild chunk` and `.bundle-meta/metafile.json absent` for `bundle-freshness` (§3.2 ranks 3 and 4), `packages/cli/.dts-cache/index.d.ts absent` for `declaration-cache` (§3.3's skip target), `packages/cli/dist/plugin absent` for `plugin-assets` (§3.4's third rank) and `packages/cli/dist/index.d.ts absent` for `shipped-declarations` (§3.5's first). The rendered coverage line must STILL read all four as `not run (packages/cli unbuilt)`, byte-identical to this row's main half — which is safe to assert by equality precisely because the suppression makes all four unconditional on the twin's own fixture, no comparand being inspected. Mutant = an implementation that resolves each comparison's reason from its own comparand and falls back to `packages/cli unbuilt` only when nothing is absent — verbatim how `C10-1` introduced this reason, "a coverage entry keyed on absence had nothing to name" — which needs no injection point, failing on the twin's own fixture. Until this twin, `packages/cli unbuilt` was ASSERTED nowhere else in §6 (re-derived read-only at `7dfa1ba`: `grep -n 'packages/cli unbuilt'` returns three hits inside §6, and the other two, in rows 17 and 19, are prose about other rows), and this row's own fixture has NOTHING absent — so §3.2's rank 1 was pinned only where ranks 2-5 are unreachable, which is what this section says of row 16 for rank 2. ⚠️ **The MAIN half stays exactly as written (standing rule 8):** it is the only element pinning that the suppression emits an entry AT ALL against an absence-keyed implementation, so this is an added twin and never an amendment — the fixture-cell clause "except where the row itself deletes or moves it" in the complete-cli-build rule below already covers the twin's five deletions, as `CF17-3` settled for row 43                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 27  | any unit's `tsconfig.json` rewritten as malformed JSON on an otherwise complete tree — the RUNTIME half of §4's "**ANY** throw the catch can SEE" (row 31 is the LOAD-TIME half, which this row's tree mutation provably cannot reach), reached deterministically and without depending on a wrong `newestOfEntries`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | none on the tree — run `check-stale-dist.mjs` against the fixture **three** times: once without `--strict`, once with, and once with `--json` (round 12, C11-5)                                                                                                                                                                                                                                                                                                                                                                                                                                                         | non-strict: stdout contains `check-stale-dist: WARN internal error — ` **followed by the fixture's own tsconfig PATH** and the process exits **0**, ⚠️ **and that line must contain NEITHER `if this ran as part of` NOR the re-run sentence (round 18, `OP18-6`) — the negative clause row 38 already uses for the coverage line**, ⚠️ **AND it must CONTAIN `; the staleness check did NOT run, so this is not a verdict on your tree` — §4's verbatim clause, mandated on BOTH printed lines with no `strict`- or `json`-varying member, so no correct implementation can fail it (`OP16-3`'s disqualifier), and asserted by NOTHING in this design for twenty-nine rounds while §4 rests the whole warrant for degrading an internal error to a WARN at exit 0 on it (round 30, `OP30-2`).** Mutant for THAT conjunct = a `formatInternalError` returning the head alone — `check-stale-dist: <level> internal error — <err.message>` — plus the re-run and member-0 sentences under `strict`, the verdict clause dropped from BOTH lines: it needs no injection point and fails on this row's own fixture, while the `no try/catch` and shared-string mutants below are both blind to it, the first failing on the head and the second on the negative clause. Mutant = a `formatInternalError` that builds ONE string and varies only the `WARN`/`ERROR` token: both non-strict halves asserted `contains` of the prefix and forbade no extra text, so it passed them. Non-strict is exactly the ad-hoc path — §5 chains this check WITH `--strict` and §5.1 confirms the script's own definition carries no flag — so §4's member-0 sentence is guaranteed FALSE on every `WARN` line it would print: nothing was invoked and nothing was blocked, which is `OP14-4`'s own defect at the twin line its fix did not reach; strict: `check-stale-dist: ERROR internal error — <that path>: ` plus the SAME verdict clause the non-strict half now pins, `; the staleness check did NOT run, so this is not a verdict on your tree`, §4 mandating it on BOTH lines (round 30, `OP30-2`) — plus the re-run sentence, asserted from round 30 by its LITERAL, ``re-run `npm run check:stale-dist` for the same diagnosis at exit 0``, so the clause its own WARN twin already forbids BY NAME is asserted by the same spelling on both sides — and the CONDITIONAL member-0 sentence §4's line carries — asserted as a SHAPE that CONTAINS `if this ran as part of` and `member 0` and contains NO `of <N>` member count at all, which is what round 14 (OP14-4) removed from the printed text and what a later `check:all` member would otherwise make false — and exit **2**; `--json` FLAGLESS: stdout `JSON.parse`s and yields `{ internalError, ran: false }` with the path inside `internalError`, contains **neither** human line, **and the process exits 0** — the DEFAULT half of §4's internal-error exit pair on the MACHINE path, mandated by §1's `check-stale-dist.mjs` cell and by §4 and asserted by NO element in this design for twenty-seven rounds (round 28, `OP28-1`); the STRICT half is this row's TWIN below, whose third invocation is `--json --strict` at exit **2** and is the only place in this design where the two flags are passed together (re-derived read-only at `e70c9bb`: `grep -c -- '--json --strict'` and `grep -c -- '--strict --json'` each returned **zero** before this round). Mutant for the PAIR, needing no injection point because it fails on this row's own fixture — rows 44, 47 and 48's discipline: a catch spelled `const out = formatInternalError(err, { strict, json }); if (json) { console.log(JSON.stringify(out)); process.exit(0) }`, the ordinary "machine consumers get a clean stream and a clean code" branch and `CF19-1`'s own shape (a flag parsed for the formatter and then not threaded to the exit) at the CATCH path rather than the success path — it passes THIS conjunct and FAILS the twin's; and its mirror, one exiting **2** whenever `json` is set, passes the twin's and FAILS THIS one, so neither collapse survives and each conjunct has a mutant that flips it ALONE (round 27's set rule). Neither can fail a correct implementation: §4 mandates ONE object for both flag settings and its exit-table internal-error row mandates **0** by default and **2** under `--strict` with no `--json` carve-out (`OP16-3`'s disqualifier, settled before the conjunct was written). Without the pair a consumer reads `ran: false` off a stream whose exit code reported success — verbatim §4's own warrant that "your dist is stale" and "the check broke" must not share an exit code, at a code this design introduces as a NEW convention (re-derived read-only at `e70c9bb`: `grep -rn "exit(2)" scripts/` returns **zero** hits today). The path and the two sentences are round 12's (OP12-5): `JSON.parse` messages carry no locus — measured, `Expected double-quoted property name in JSON at position 7 (line 1 column 8)` and nothing else — so without them the row goes green on a line naming none of the **30** candidate configs and stating no next action; and a `formatInternalError` blind to `json` writes chatter onto the JSON stream, which is the failure `scripts/bundle-types.mjs:63-68` records this repository shipping (round 12, C11-5). Mutant = no `try`/`catch`, letting the throw propagate: node prints an unprefixed stack and exits **1**, failing both halves — and under `pretest` that blocks `npm test` outright (§5.1, round 1's `O-2`). Without this row §4's internal-error row has no owner and every other row passes (round 11, OP11-2). ⚠️ **TWIN, in row 33's shape so nothing is renumbered (round 18, `C18-5`):** THREE invocations against a fixture whose **ROOT** `tsconfig.json` is the malformed file — non-strict, `--strict`, and, since round 28 (`OP28-1`), `--json --strict` rather than a second flagless `--json` — asserting the ROOT path in the `WARN` and `ERROR` lines and inside `internalError`, ⚠️ **and, from round 30 (`OP30-2`), the verdict clause `; the staleness check did NOT run, so this is not a verdict on your tree` in BOTH of those human lines — §4 mandating it with no `strict`- or `json`-varying member, so no correct implementation can fail it and the twin's `--json` object assertion is untouched (standing rule 8)**, **and exit 2 on that third one**: the STRICT half of §4's internal-error exit pair on the machine path, whose DEFAULT half is this row's main `--json` invocation above. The object assertion is unaffected by the added flag (standing rule 8, checked AT the fix): §4 mandates ONE object for both flag settings, with no `strict`-varying member, so the ROOT-path-inside-`internalError` clause holds byte-identically under `--strict`, and this twin ADDS NO ROW, which is the property to state rather than a live battery count §1 also carries (`OP22-3`). Its mutant is the main half's read the other way — a catch that emits the object and exits **0** under `--json` whatever `strict` says, which passes the flagless half above and FAILS here, needing no injection point because it fails on this twin's own fixture. This row's own fixture provably cannot reach §3's census wrap — a malformed UNIT config is read by rules 3 and 4 inside `units.mjs`, while `enumerateRootReferences` `readFileSync`s and `JSON.parse`s the ROOT `tsconfig.json`, which is intact here (re-derived read-only at `896dd84` against `scripts/repo-census.mjs`) — so a `units.mjs` wrapping its own reads and calling the census UNWRAPPED passed this row, row 31 and every other element, and on a malformed root config (a tracked file this repository edits on every workspace addition, carrying 19 `references`) printed `Expected double-quoted property name in JSON at position 7 (line 1 column 8)` with no locus at all, at exit **2** from `check:all` member 0 with **30** candidate configs and no path — verbatim the defect `OP12-5` filed. Mutant = that unwrapped call, which must FAIL                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 28  | a SYNTHETIC tree with `stale-unit` on ≥2 units (two units whose `src` is newer than their `dist`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | none on the tree — swap in a MUTANT `formatFindings` that renders the grouped line as the count alone, `check-stale-dist: stale-unit ×2`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | the row must FAIL on the mutant: the rendered line is `check-stale-dist: stale-unit ×2 — <unit>, <unit>` (§4), prefixed and NAMING the units — ⚠️ **and the two rendered tokens must EQUAL the fixture units' `Unit.dir` values, not merely be non-empty (round 15, OP15-1)**, because §4's **A UNIT IS ALWAYS PRINTED AS ITS `Unit.dir`** is what makes step 1's `rm -rf <unit>/dist` runnable; a `<unit>` placeholder satisfied by a basename passes every row in this battery while `rm -rf contracts/dist` exits 0 having deleted nothing. §4's recipe step 1 is `rm -rf <unit>/dist` **for the units reported `stale-unit`**, so a count-only line makes the printed remedy unrunnable — round 6's `OP6-2`, whose fix landed in §4 prose and became no row until round 11. Synthetic, never the live tree's own four: §6's opening rule and the live-prediction paragraph both keep today's repository state out of the battery. ⚠️ **Its N=1 TWIN, asserted in the same row in row 33's shape (round 15, OP15-3):** the same fixture reduced to exactly ONE `stale-unit` unit must render `check-stale-dist: stale-unit ×1 — ` plus that unit's `dir`. Mutant = a formatter that applies the `×N —` collapse only at N≥2 and falls back to §4's generic comparison shape `<kind> <unit> <newerInput> <olderOutput> <deltaMs>` at N=1: it PASSES this row's main ≥2-unit fixture and every other row in the battery, because row 29's fixtures are all delta-LESS kinds — ⚠️ **and until round 18 no other row rendered a delta-carrying finding AT ALL, which is why this same mutant survived for `stale-bundle`, `stale-declarations` and `stale-plugin-assets`, the three kinds that are structurally N=1 (`OP18-2`); row 38 now renders `stale-bundle ×1 — packages/cli` and fails it too, and row 29 carries a fifth fixture for the `unreadable` line (`OP18-5`), so neither is restated as a count (`C11-4`) — ⚠️ **and naming ONE row for a THREE-kind gap left the third kind rendered by nothing for two more rounds (round 20, `OP20-1`)**: row 43's CLI twin closed `stale-declarations` incidentally in round 19 (`CF19-1`, whose subject was the exit code) and row 8's RENDERED TWIN is the fourth and last. The mutant that survived all of them is not THIS twin's: it is a formatter whose grouped-kind SET omits one member while collapsing at every N for the rest, which this row, row 38 and row 43's CLI twin all pass and which only the row rendering the omitted kind can kill**. Written as a twin so nothing is renumbered and §6's four group lists and the registry all stay true. ⚠️ **Its PLAN TWIN, asserted in the same row in row 33's shape (round 25, `OP25-1`) — this row now carries TWO twins, the N=1 TWIN above and this one, so a citation must say WHICH (`C20-2`); it ADDS NO ROW, which is the property to state rather than a live battery count §1 also carries (`OP22-3`).** On this row's own ≥2-unit fixture, EXTENDED for THIS TWIN ALONE by TWO further units the fixture's own root `tsconfig.json` references — one carrying an `orphan-output` (a 1:1-stem unit holding `src/x.ts` with `dist/x.js` AND `dist/z.js` whose `src/z.ts` is DELETED, BOTH pairs at DEPTH 0, its surviving `dist` outputs NEWER than every file under its own `src` and than its own `tsconfig.json` and chain — row 42's SCOPE TWIN's own wording, copied WHOLE (standing rule 7) — so it carries no `stale-unit` of its own and fires `orphan-output` ALONE) and one carrying an `unbuilt` (row 9's own mutation, `rm -rf <unit>/dist/*` keeping `.tsbuildinfo`, which §3.3's unit-state table makes `unbuilt` and therefore neither compared nor a `stale-unit`) — the ONE fixture in this battery where §4's recipe step 1 selects MORE THAN ONE unit AND the only one where it selects across MORE THAN ONE KIND, this row being outside the cli-scoped list below and therefore outside step 1's FOURTH carve-out, and neither added unit being `packages/cli` — the rendered output must carry `check-stale-dist: remedy plan:` whose step-1 lines are ONE `rm -rf <unit>/dist` per selected unit, FOUR of them here and drawn from ALL THREE of the kinds §4 step 1 names, each `<unit>` EQUAL to that unit's `Unit.dir` (`OP15-1`), and ALL of them ABOVE step 2's `npm run build` line. ⚠️ **The orphan is written at DEPTH 0 deliberately, and is NOT row 4's nested end state (standing rule 8, checked AT the fix):** row 4's cell claims to be this battery's stem-walk recursion row "and the only one", and a NESTED orphan here would let a shallow `stemsUnder` fail this twin too and falsify that claim in the commit that landed this one. ⚠️ **Step 1's KIND SET was asserted by NOTHING until round 33 (`OP33-1`):** §4 step 1 selects the units reported `stale-unit`, `orphan-output` or `unbuilt`, and before this extension NO fixture in this section on which ANY rendered-plan clause rests carried an `orphan-output` or an `unbuilt` finding — each held `stale-unit` alone or no finding at all, and every mention of the other two kinds in those cells is prose about that kind's ABSENCE from the fixture — so a step-1 selector keyed on `stale-unit` alone passed the whole battery while printing, for an `orphan-output`, `npm run build` and step 5 as the entire plan, which §4's own remedy row measures as unable to clear it ("neither `npm run build` NOR `tsc -b --clean` clears this"), and the same for a non-cli `unbuilt`, whose remedy row measures plain `npm run build` as re-emitting nothing for the 18 tsc-only units: `CF14-1`'s class a second time, at the step §4 added to prevent it. Nothing is asserted about this plan's position relative to the coverage line — that is row 39's (`OP22-2`). ⚠️ **Step 1's PRESENCE was asserted by NOTHING for twenty-four rounds.** Every positive rendered-plan step assertion in this section AT THE ROUND-25 APPEND was row 21's PLAN TWIN (`step 2 and step 5, and NO step 1`), row 34's SOLE-KIND TWIN (`step 4 and step 5 and NO step 2`), row 39 (`step 5 as its only step`), row 42's PLAN TWIN (`step 3, step 4 and step 5, and NO step 2`) and row 46's PLAN TWIN (`step 5 as its only step`) — an enumeration over WHICH STEPS PRINT and never over WHICH KINDS SELECT one, which is why not one of them reached step 1's three-kind selector either, and SCOPED to that append rather than left as a claim about this section's current state, this twin's own clauses having been positive rendered-plan step assertions since round 25 (`C11-4`'s class at a state description, in row 8's twin's remedy shape); the only other step-1 clauses here are row 21's `NO step 1` and row 38's "a remedy plan that does NOT prescribe `rm -rf packages/cli/dist`" — FOUR elements demanding step 1's ABSENCE and none its presence, which is `OP18-3`'s shape ("a formatter implementing only the two carve-outs the battery forced passed every row") at the step §4 added to keep a printed remedy able to clear the finding it is printed for. TWO mutants at round 25's append, both failing on this row's own fixture and so needing no injection point (rows 44, 47 and 48's discipline) — the count SCOPED to that append rather than left as a claim about this twin's current set, in row 8's twin's own remedy shape (`C11-4`), a THIRD MUTANT having been added below in round 33: **(a)** a plan assembler with NO step-1 branch at all, which passes rows 21, 34, 38, 39, 42 and 46 today and which reduces this design's own live prediction — four `stale-unit` findings — to `npm run build` then step 5, a plan §4's own pinned-`typescript@6.0.3` measurement says re-emits nothing for a touched-but-unchanged source, so the re-run reports the identical four findings at `check:all` member 0 and at line 5 of §5.1's measured 204 under `pretest`: `CF14-1`'s class, a printed step that cannot clear the finding it is printed for; and **(b)** one emitting step 1 for the FIRST selected unit only, byte-identical to a correct one at N=1 and reached by no other fixture in this battery — `CF18-3`'s N=1 argument at the plan rather than at §3.2's coverage reason, and the rendering that turns §7 row 1's accepted "up to 19 `rm -rf <unit>/dist`" into one. ⚠️ **THIRD MUTANT (round 33, `OP33-1`), for the SELECTOR conjunct, failing on this twin's OWN extended fixture and so needing no injection point either:** a step-1 selector keyed on `stale-unit` ALONE — or on any PROPER SUBSET of the three kinds §4 step 1 names, the reading §4's worked example, this document's live four-`stale-unit` prediction and this twin's own pre-extension fixture all invite — which renders TWO `rm -rf` lines where FOUR are mandated and leaves the `orphan-output` unit and the `unbuilt` unit with `npm run build` and step 5 as their whole printed remedy. It passes this row's MAIN half and its N=1 TWIN, rows 21, 34, 35, 38, 39, 42 and 46, rows 29 and 36 (which DO hold those two kinds but assert the finding LINE and never the plan), row 30, row 43's CLI twin, the false-negative battery, the wiring assertions and the live smoke test — and mutants (a) and (b) both still FLIP on the extended fixture, (a) printing no step-1 line at all and (b) printing ONE where four are mandated (standing rule 8, checked in both directions). No clause here can fail a correct implementation: this fixture selects step 1 by KIND on ALL FOUR units — TWO by `stale-unit`, ONE by `orphan-output`, ONE by `unbuilt`, each named in §4 step 1's own terms — §4 step 1 mandates one `rm -rf <unit>/dist` per selected unit in terms, step 1's FOURTH carve-out reaches `packages/cli` alone and no added unit is `packages/cli`, and the `bundle-provenance-missing` carve-out reaches a finding SET this fixture does not hold (`OP16-3`'s class, re-derived over the EXTENDED fixture rather than carried from round 25). This row's MAIN half and its N=1 TWIN are unchanged (standing rule 8): the extension is this twin's alone, neither added unit carries a `stale-unit` and neither is `packages/cli`, so the MAIN half's `check-stale-dist: stale-unit ×2 — <unit>, <unit>` equality, its own count-only mutant and the N=1 TWIN's reduction to exactly one `stale-unit` unit are all untouched. ⚠️ **AND A RETURN-VALUE CONJUNCT ON THE SAME `checkStaleDist` CALL — `Finding.remedy`'s VALUE, which §4's table defines PER KIND and which ONE kind of eight was read for (round 34, `OP34-2`)** — so it does not join §6's rendered-text group (row 42's SCOPE TWIN's own wording): each of the FOUR findings this twin's extended fixture carries must have a `remedy` naming BOTH an `rm -rf` of that finding's own unit — its `Unit.dir` followed by `/dist`, the substitution `OP15-1` already pins on the printed step — AND `npm run build`, in that order, §4's remedy-table command being the same for all three of the kinds here. Re-derived read-only 2026-08-22 at `5e4918f`: the field's value is read at exactly ONE site in this design, row 43's CLI twin's `remedy` equal to §4's remedy-table command for `stale-declarations`, and row 43's own `--json` deep-equality is against the run's OWN return and therefore pins no value at all — so SEVEN of §4's EIGHT table rows were asserted by nothing. Mutant = a producer deriving `remedy` from the ordered plan rather than from the table — the FIRST plan step that finding's kind selects, which §4's own **the table below is that plan's SOURCE** invites read backwards: it agrees with a correct implementation on `stale-bundle`, `stale-plugin-assets` and `bundle-provenance-missing` (step 2), on `stale-shipped-declarations` (step 4) and on `stale-declarations` (step 3, row 43's one pinned value), and on exactly the three kinds THIS fixture carries it emits `rm -rf <unit>/dist` ALONE, dropping the rebuild — a `--json` remedy that deletes a unit's outputs and never restores them, which is `CF14-1`'s class at the one surface §2 declares this field for. It needs no injection point, FAILING on this twin's own extended fixture. It cannot fail a correct implementation: §4's `stale-unit`, `orphan-output` and `unbuilt` rows all read **`rm -rf <unit>/dist` then `npm run build`** in terms, and the conjunct asserts NAMING and ORDER rather than byte-equality, so a cell's trailing gloss does not break it (`OP16-3`'s disqualifier). This clause ADDS NO ROW, NO TWIN, NO FIXTURE and NO INVOCATION either, and every assertion this twin already carries is unchanged (standing rule 8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 29  | one fixture per DELTA-LESS kind (§2, §4): `unbuilt` (row 9's end state), `orphan-output` (row 4's end state), `bundle-provenance-missing` (row 21's fixture) and `stale-shipped-declarations` (row 22's MAIN half's end state — ⚠️ **the HALF is named from round 34 (`C34-1`): row 22's TWIN pins BOTH of its declaration files to ONE 28-byte length, and this fixture is the design's only element rendering §4's `(<a> vs <b> bytes)` line, which on that twin's tree would print two EQUAL numbers**) — ⚠️ **plus a FIFTH fixture for `bundle-provenance-missing`'s SECOND line variant (round 18, `OP18-5`): the false-negative battery's own truncated-marker tree, marker PRESENT, NEWEST and unparseable, so no new fixture is built**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | none on the tree — swap in a MUTANT `formatFindings` that prints the generic comparison shape `<kind> <unit> <newerInput> <olderOutput> <deltaMs>` for every finding                                                                                                                                                                                                                                                                                                                                                                                                                                                    | all of them must FAIL — FIVE fixtures since round 18 (`OP18-5`), stated as the SET and never as a count (`C11-4`): the generic shape renders `stale-shipped-declarations packages/cli undefined undefined NaN`, round 4's `OP-R4-2` and round 7's `OP-1` verbatim, at the kind round 9 added. Each must instead render its own §4 line, prefixed `check-stale-dist: ` — and for `stale-shipped-declarations` that line is byte-based, `(<a> vs <b> bytes)`, with no mtime delta. ⚠️ **AND the fifth fixture must render §4's `unreadable` twin VERBATIM — `check-stale-dist: bundle-provenance-missing packages/cli — .bundle-meta/metafile.json unreadable; the bundle comparison did not run (advisory: does not fail --strict)` — with a SECOND mutant: a `formatFindings` carrying ONE hard-coded line for this kind, spelling `absent` (round 18, `OP18-5`).** That mutant passed the whole battery, the false-negative battery, the wiring assertions and the live smoke test: re-derived read-only at `896dd84`, `unreadable` occurs in §6 exactly once, in the truncated-marker bullet, which asserts the FINDING's reason, `exitCodeFor(result, { strict: true })` and no throw, and never the rendered line — nor, until round 22 (`C22-1`), the COVERAGE entry's reason, which this cell called asserted while nothing in that bullet named `bundle-freshness` at all. On a tree where every artifact is complete and the marker is truncated (round 10, `C10-3`) it tells the operator a file is ABSENT while it is sitting on disk — a wrong diagnosis, which §4 calls what the founding incident was — and round 12's `C11-3` gave the `unbuilt` kind's SECOND line variant its own row **32** for exactly this reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 30  | rows 9, 13 and 14's end states — one `unbuilt`, two `skipped`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | none on the tree — swap in a MUTANT `formatFindings` that prints one hard-coded sentence in place of `unitState()`'s `reason`, in BOTH of its consumers: §4's `unbuilt <unit> — <reason>` line and the skipped parenthetical of the units-compared/skipped summary — which on THIS fixture is the findings path's own summary line and NOT a `PASS` line, because the fixture carries one `unbuilt` finding and §4's exit table prints no `PASS` line on that row (round 13, OP13-1: as written, this column asserted a surface its own fixture cannot render)                                                          | all three must FAIL: §4 prints that `reason` VERBATIM in both places ("never a sentence the reporter invents") and §3.3's reason table gives two of these states their reasons and §3.3's unit-state table gives the third and FOURTH. ⚠️ **A FOURTH fixture unit joins this row in round 15 (OP15-2): one whose `dist/` is ABSENT, asserting the literal `no dist/`.** The three end states this row shipped with are all `dist`-EXISTS states, so a reporter that invents the ABSENT case's reason — the one state no §3 table spelled until round 15, and the one printed on every fresh clone — passed this row, every other row, the false-negative battery and the smoke test. Assert FOUR literal strings. ⚠️ **Assert the three LITERAL strings, not merely that they differ (round 13, OP13-2)** — row 9's `dist/ holds only .tsbuildinfo — outputs were deleted without it`, row 13's `dist/ exists but is empty — a --clean, a test's mkdir, or a manual delete; nothing here proves a build ever ran`, and row 14's scratch sentence. "Distinct" went green on a cell reading `indistinguishable — see below`, which is distinct and unprintable at once, so the weaker assertion is what let an editor's cross-reference reach the operator. Row 15 asserts the same three on the RETURN value — precisely the surface this section's own rule says goes green on the printing defect — which is why the rendered twin is a separate row (round 11)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 31  | a fixture holding a COPY of this check's own modules — `check-stale-dist.mjs`, `stale-dist/*.mjs`, and a stub `bundle-cli.mjs` that `export`s `PLUGIN_ASSET_ENTRIES` and throws nothing — over a complete cli tree, run as `node <fixture>/check-stale-dist.mjs`: reports clean                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | replace the copied `bundle-cli.mjs` with one whose FIRST statement throws — a module that fails at LOAD, the half of §4's internal-error row that row 27's tree mutation provably cannot reach, because the check's real import graph resolves from the REPOSITORY and no fixture `node_modules` can touch it                                                                                                                                                                                                                                                                                                           | non-strict: stdout contains `check-stale-dist: WARN internal error — ` and exit **0**, with the same negative clause row 27 carries — NEITHER `if this ran as part of` NOR the re-run sentence (round 18, `OP18-6`) — and the same shared-string `formatInternalError` as its mutant; strict: `check-stale-dist: ERROR internal error — ` and exit **2**, both prefixed. ⚠️ **AND BOTH of those lines must CONTAIN `; the staleness check did NOT run, so this is not a verdict on your tree` (round 30, `OP30-2`)** — §4's verbatim clause on both, with no `strict`- or `json`-varying member, so no correct implementation can fail it; row 27's head-only mutant is what this kills, and this row's own static-import mutant still fails on the head, so nothing this row already pins is vacated (standing rule 8). Mutant = the entry module reaching `compare.mjs`/`report.mjs` by a STATIC `import` statement instead of §1's IMPORT RULE `await import(...)` inside the try: measured 2026-08-20 (node v24.18.0, npm 11.16.0, nothing else running but the shell), node prints the throw plus an internal stack, NO `check-stale-dist: ` line at all, and exits **1** in BOTH invocations — failing both halves, and under `pretest` blocking `npm test` outright (§5.1's table, round 1's `O-2`). Row 27's mutant is "no try/catch" over a RUNTIME throw and stays green here, so without this row §8(n) is a residual nothing proves (round 12, OP12-1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 32  | cli built COMPLETE, as row 11, marker present and newest                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `rm packages/cli/dist/bin.js packages/cli/dist/index.js packages/cli/dist/bin/supervisord.js packages/cli/dist/index.d.ts packages/cli/dist/.tsbuildinfo` — the `--clean` SIGNATURE: every tsc-written member gone, the esbuild chunks and `dist/plugin/**` left                                                                                                                                                                                                                                                                                                                                                        | `unbuilt`, gaps naming those four, and the RENDERED line reads `check-stale-dist: unbuilt packages/cli — tsc -b --clean removed the compiler outputs from a completed bundle; missing: dist/bin.js, dist/index.js, dist/bin/supervisord.js, dist/index.d.ts` (§3.3's cli reason table, §4). Mutant = a `unitState` carrying the single `bundle:cli did not finish` reason: it PASSES the verdict and FAILS the line — which is the whole row, because after `npm run build:clean` that sentence blames a step the operator never ran, verbatim §4's own OP-2 failure. A rendered-text row, in rows 27-30's group and for their stated reason (round 12, C11-3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 33  | cli built COMPLETE, as row 11, marker present and newest, its `metafile.outputs` naming all five emitted chunk files                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `rm` three of the four chunks `dist/bin.js` imports (measured 2026-08-20: `bin.js` → `chunk-FRJGAF5Y.js` → `chunk-DVV3SNQ3.js`, `chunk-I6JBP7DT.js`, `chunk-UF6GI6PE.js`), leaving one file that still matches the pattern                                                                                                                                                                                                                                                                                                                                                                                              | `unbuilt`, gaps naming the three deleted chunks — clause 1's chunk membership is `metafile.outputs` whenever the marker exists (§3.3, round 12, C11-6). Mutant = clause 1 requiring only "at least one file matching the pattern": it PASSES, and so do clauses 2 and 3 — a deletion LOWERS the newest output mtime — so the check reports **PASS, exit 0 even under `--strict`** on a `bin.crabgic` that dies with `ERR_MODULE_NOT_FOUND`. Its twin, asserted in the same row: the SAME mutation with the marker ABSENT must still be PASS plus one advisory `bundle-provenance-missing`, because that is §8(i)'s third stated cost and not a defect the row may quietly close                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 34  | cli built COMPLETE (as row 11) plus a second unit whose `src` is newer than its `dist`, and `.dts-cache/index.d.ts` older than `packages/*/src` — TWO kinds, `stale-unit` and `stale-declarations`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | none on the tree — **TWO** mutant `formatFindings`, both of which this row must FAIL (round 21, `OP21-1`): (a) one that prints each kind's remedy-table row beside its own grouped block and NO plan, and (b) one that prints the same per-kind remedy-table row beside each grouped block AND the correct ordered plan — the rendering §2's `formatFindings` comment forbids in terms, and the one mutant (a) cannot reach, its single mutation being a CONJUNCTION                                                                                                                                                    | the row must FAIL on BOTH mutants. Against (a): the rendered output carries ONE `check-stale-dist: remedy plan:` block with its steps as indented continuation lines, in §4's stated order, positioned after the findings list. ⚠️ **This clause names NO STEP, and row 21's PLAN TWIN cited this fixture as the place step 1 is "correctly selected" for six rounds (round 25, `OP25-1`):** step 1 IS selected here, on this row's second unit, and nothing in this cell reads it — the selection is a property of the FIXTURE and the assertion is over the block's shape and position alone, so a plan assembler with no step-1 branch renders `npm run build` then `re-run the check` here and satisfies every word of it. It is left as written rather than widened, because this fixture selects step 1 on exactly ONE unit and therefore cannot tell a per-unit rendering from a first-unit-only one; §6 row 28's PLAN TWIN, whose fixture carries `stale-unit` on ≥2 units plus one unit per OTHER kind step 1 names (round 33, `OP33-1`), is where step 1's presence, its per-unit shape and its KIND SET are read (standing rule 8: row 34's two mutants and its SOLE-KIND TWIN are unchanged). ⚠️ **AND NOT "before the coverage line", which this fixture does not pin (round 22, `OP22-2`):** `OP16-4` made that line CONDITIONAL — §4 prints it only where some entry is not `full` — and on this fixture `declaration-cache` RAN (it fired `stale-declarations`), `shipped-declarations` is `full` because §6's `CF8-2` cache rule binds this cli fixture's cache byte-identical to its `dist/index.d.ts`, `plugin-assets` is `full` on a COMPLETE build, and `bundle-freshness` is a FREE VARIABLE, since `C15-2`'s sibling-fixture rule quantifies only over fixtures asserting `stale-bundle` or a `full` `bundle-freshness` entry and this row asserts NEITHER — §9's round-16 entry says so in terms, "row 34's is not pinned". A legal build therefore leaves all four `full`, §4 SUPPRESSES the coverage line, and a positional assertion against a line that is not there FAILS a correct implementation, which is `OP16-3`'s class. The plan-before-coverage ORDER is **row 39's**, whose clean cell binds its fixture to "≥1 non-`full` coverage entry" by construction and whose verdict already reads "positioned after the units-compared/skipped summary and before the coverage line". ⚠️ **Against (b), and asserted by nothing until round 21 (`OP21-1`): NO rendered line BEFORE that `check-stale-dist: remedy plan:` line carries any command from §4's remedy table** — `rm -rf`, `npm run build`, `npm run bundle:types` or `npm run bundle:cli`. Re-derived read-only at `3095116`: `remedy` occurs twenty times in this section, every one of them about the PLAN, and `Finding.remedy` occurs zero times, while rows 21, 38, 39 and 42 — and, since round 25, row 28's PLAN TWIN (`OP25-1`) — assert their plans' PRESENCE and steps and never the absence of other remedy text — so a formatter rendering both passed every element in this design. On THIS fixture the lines before the plan are the two grouped finding lines and a units-compared summary over ZERO skipped units, none of which can carry such a command, so the clause cannot fail a correct implementation (`OP16-3`'s class, settled before the clause was written). What it does fail is the page §4 measures as harmful: `remedy: rm -rf <unit>/dist then npm run build` and `remedy: npm run bundle:types -- --force` printed immediately above an ordered plan that contradicts their order, the second of them half a remedy — a lone `stale-declarations` needs step 3 AND step 4 and the table's row names only step 3, so the operator who follows the line is left with `stale-shipped-declarations` on the next run, which is `OP14-3`'s one-plan-contradicting-itself reached through an extra line rather than through step 2. Mutant (a)'s assertion above and this row's SOLE-KIND TWIN are unchanged (standing rule 8). The mutant's per-kind list omits step 4 (`npm run bundle:cli`, which lifts the regenerated cache into `dist/index.d.ts` — §4: "step 3 alone leaves the pre-force copy shipped") and step 5 (re-run), so an operator who follows it spends the generator's ~5 minutes and is left with `stale-shipped-declarations` on the next run. Every other row passes the mutant, which is why the plan needed a row (round 12, OP12-4). ⚠️ **Its SOLE-KIND TWIN, asserted in the same row in row 33's shape (round 14, OP14-3):** a fixture whose ONLY finding is `stale-shipped-declarations` — `dist/index.d.ts` rewritten as a `tsc` barrel on an otherwise complete, `compared` cli tree, row 22's own mutation, which touches only an artifact §3 excludes from the mtime side so no other kind can fire — must render a plan of **step 4 and step 5 and NO step 2**. Mutant = a formatter that assembles the plan by walking steps 1-5 and applying each step's own printed condition, which emits `npm run build` because step 2 carried no clause for this kind until round 14; it passes this row's main fixture, whose two kinds both select step 2, and every other row in the battery — one full `tsc -b` across 19 units apart from the correct plan. ⚠️ **AND, ON THAT SAME TWIN'S END STATE, STEP 4's COMMAND BY LITERAL: the rendered plan must carry `npm run bundle:cli` as a plan step (round 35, `OP35-2`).** Both elements that select step 4 named it by step NUMBER — this twin and row 42's PLAN TWIN — and re-derived read-only 2026-08-22 at `a5605e9` over this section as delimited by this file's `## 6.` and `## 7.` headings and BEFORE this apply, `npm run bundle:cli` occurred **4** times here and every one of them was an ABSENCE list or prose about a mutant's per-kind list, so no element rendered it as a plan step. SECOND MUTANT: a plan assembler whose step-4 line copies its neighbour's `npm run bundle:types -- --force` — standing rule 7's part-of-a-shape copy at the adjacent step — which needs no injection point beyond the formatter swap this row already uses and FAILS on this twin's own fixture, where §4 says this step is the whole remedy and steps 1-3 are all unselected: it regenerates the cache and never lifts it into `dist/index.d.ts`, §3.5 being byte-keyed, so the finding survives verbatim, step 5 reports it identically and the generator's ~5 minutes buy nothing — `CF14-1`'s class at the step §4 added to prevent it. The sibling mutant spelling `npm run build` there clears the finding and re-opens `OP14-3`'s full `tsc -b` across 19 units plus a duplicate `bundle:cli`, and it too passes a step-NUMBER assertion. Neither can fail a correct implementation: §4 states this command at step 4 and its remedy table's `stale-shipped-declarations` row gives the same one (`OP16-3`'s disqualifier). This row's TWO mutants, its main assertion, its negative clause — which scopes to lines BEFORE the plan's first line and is therefore unaffected — row 42's PLAN TWIN and row 43's CLI twin are all unchanged (standing rule 8), and this clause ADDS NO ROW (`OP22-3`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 35  | a SYNTHETIC tree carrying ≥1 `stale-unit` finding AND **≥2 units that share ONE `unitState()` reason** with no build output at all — the findings path with skipped units on it, which §4's exit table renders WITHOUT a `PASS` line. ⚠️ **The single-skipped-unit fixture this row shipped with cannot tell a grouped summary from a per-unit one (round 14, OP14-2)**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | none on the tree — **TWO** mutant `formatFindings`, both of which this row must FAIL: (a) one that prints the units-compared/skipped summary only when it prints a `PASS` line, and (b) one that prints one `unit: reason` pair PER UNIT instead of one clause per distinct reason                                                                                                                                                                                                                                                                                                                                      | the row must FAIL on BOTH mutants: the rendered output carries `check-stale-dist: ` plus the units-compared/skipped summary in §4's ONE shape — `<N> units compared, <M> skipped (<units>: <reason>, …)`, both counts derived from this fixture, every skipped unit NAMED, and each distinct `unitState()` `reason` printed exactly ONCE with its units beside it (§4's **THE SKIPPED SUMMARY GROUPS BY REASON**, without which the `npm run build:clean` state renders as one 2,433-character line, 31 rows at 80 columns, measured 2026-08-21 over 19 root references and the 111-character empty-`dist` reason), after the grouped findings and before the remedy plan (§4). Rows 16-18 and 26 assert comparison COVERAGE only, §6's false-negative bullet asserts naming on a fixture with no findings, and row 30's fixture renders one `unbuilt` beside two `skipped` without asserting the summary — so the mutant passes every other row while the skipped units vanish from human output on exactly the path an operator reads because something is already wrong (round 13, OP13-1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 36  | a SYNTHETIC tree with `unbuilt` on TWO units whose reasons DIFFER — `packages/cli` under row 32's `--clean` signature, plus one non-cli unit under row 9's mutation (outputs deleted, `.tsbuildinfo` kept) — ⚠️ **and the tree holds EXACTLY those two units and no third, pinned in round 35 (`OP35-1`) at the one value this row left free**, so **0** units are `compared`, and `packages/cli` being `unbuilt` suppresses all four cli-scoped comparisons (§3.2's REASON PRECEDENCE rank 1, evaluated before any comparison inspects its own comparand), so NO comparison runs either. That is the clause state two PUBLISHED root scripts reach with no manual `rm` — `npm run build` then `npm run build:clean` — whose skipped and `unbuilt` counts §4's own `C11-3` paragraph measures, reproduced here at TWO units so this element carries no live workspace count (`OP16-3`). Unpinned, this fixture admitted a third, `compared` unit, and §4's carve-out conjunct **no finding fired** then failed here by accident rather than by construction                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | none on the tree — swap in a MUTANT `formatFindings` that groups `unbuilt` the way §4 groups `stale-unit`, rendering `check-stale-dist: unbuilt ×2 — packages/cli, <unit>`                                                                                                                                                                                                                                                                                                                                                                                                                                              | the row must FAIL on the mutant: `unbuilt` carries PER-FINDING text — `unbuilt <unit> — <reason>`, `reason` verbatim from `unitState()` — so a grouped line discards both, and the two reasons here come from different tables, which is the whole of C11-3. TWO prefixed lines, one per finding. Rows 28-30, 32 and 34 each render at most ONE `unbuilt`, and row 29 takes its `unbuilt` fixture from row 9 alone, so the grouping mutant passes the entire battery without this row (round 13, OP13-3). ⚠️ **AND, ON THE SAME FIXTURE AND A SECOND MUTANT FORMATTER, THE ORDERED REMEDY PLAN (round 35, `OP35-1`):** the rendered output must carry `check-stale-dist: remedy plan:` AND recipe step 2's `npm run build` line, with steps 3 and 4 unselected. This is §4's `nothing was checked` carve-out with its **no finding fired** conjunct failing ALONE, the other two holding, and it is the ONLY element in this design in which that conjunct comes apart from them: the carve-out's one prior element, row 39's NEGATIVE twin, holds all three at once. SECOND MUTANT: a `formatFindings` sharing the `PASS` line's TWO-conjunct carve-out — no comparison ran AND no unit was compared — which is the predicate §4 spells ten lines above for that line and the body a single shared gate produces. It withholds the plan here and leaves the operator two `unbuilt` lines, a summary and a coverage line with NO stated action, at exit **1** under `--strict` from `check:all` member 0 and at line 5 of §5.1's measured 204 under flagless `pretest` — verbatim `OP14-1`'s defect, through a conjunct nothing read. It needs no injection point beyond the formatter swap this row already makes, and it cannot fail a correct implementation (`OP16-3`'s disqualifier, checked BEFORE the conjunct): §4 mandates the plan on any run reporting ≥1 finding outside that carve-out, and this run reports two. Step 1's two `rm -rf <unit>/dist` lines are SELECTED here and deliberately NOT re-asserted — that step's PRESENCE at N≥2 is pinned at row 28's PLAN TWIN (`OP25-1`), and this row's subject is the carve-out's predicate rather than the recipe's steps. The grouping mutant above, this row's TWO ungrouped `unbuilt` lines and its per-finding-text assertion are unchanged (standing rule 8), and this clause ADDS NO ROW (`OP22-3`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 37  | cli built COMPLETE, as row 11, marker present and NEWEST but TRUNCATED to half its bytes — the false-negative battery's truncated-marker fixture, and the ONE cli-scoped fixture exempt from the "marker's `outputs` must name exactly the chunks written" rule below, on CONTENT only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `utimesSync` `dist/bin.js` forward past the marker — row 19's mutation on a marker that does not parse                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `unbuilt` via §3.3's clause 2, carrying the clause-2 reason — **NOT** `compared` plus one advisory. An implementation keying clause 2 on `readMetafile`'s return (`undefined` for absent AND unparseable) reports `compared` at exit **0 even under `--strict`** on a `bin.crabgic` that is tsc per-file output importing `@crabgic/*`. NO other row discriminates: row 19's marker parses, and the false-negative battery's truncated-marker fixture is NEWEST and unmutated, so clause 2 passes there under either reading (round 14, C14-1). A verdict row, not a rendered-text one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 38  | cli built COMPLETE, as row 11, marker present and newest, and `.dts-cache/index.d.ts` byte-identical to `dist/index.d.ts` with an mtime ABOVE everything the mutation moves, so `stale-declarations` stays clean and exactly TWO kinds can flip                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `utimesSync` `packages/cli/src/index.ts` forward                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **TWO** kinds from ONE fact — `stale-unit packages/cli` (`checkUnitFreshness`, max over §3's filtered output set) and `stale-bundle packages/cli` (§3.2, min over the chunk set) — asserted so the overlap cannot be removed by accident; ⚠️ **AND, on the mutant-formatter half, the RENDERED line `check-stale-dist: stale-bundle ×1 — packages/cli`, by EQUALITY (round 18, `OP18-2`).** §4's collapse applies at every N including N=1, and until this round the only grouped literal anywhere in this design was `stale-unit ×`: re-derived read-only at `896dd84`, `stale-bundle ×`, `stale-declarations ×` and `stale-plugin-assets ×` occur **zero** times, while each of those three kinds is structurally N=1 — one `packages/cli`-scoped comparison apiece (§3.2's min over chunks, §3.3's one cache comparison, §3.4's max over the six entries) — which is precisely the N at which row 28's N=1 twin's mutant fires. So row 28's mutant, a formatter that collapses only at N≥2 and falls back to §4's generic comparison shape `<kind> <unit> <newerInput> <olderOutput> <deltaMs>` at N=1, still passed for THREE of the four kinds the rule governs. ⚠️ **This row closes ONE of those three, and the other two are NAMED here rather than left for a later round to discover (round 20, `OP20-1`):** row 43's CLI twin renders `check-stale-dist: stale-declarations ×1 — packages/cli` (round 19, `CF19-1`) and row 8's RENDERED TWIN renders `check-stale-dist: stale-plugin-assets ×1 — packages/cli`, which no element rendered until round 20. The four grouped literals asserted by EQUALITY are the SET this rule is discharged by (`C11-4`); a formatter whose grouped-kind SET omits one member passes THIS row and is killed only by the row that renders the kind it omits. On this row's own fixture it renders `check-stale-dist: stale-unit ×1 — packages/cli` beside `check-stale-dist: stale-bundle packages/cli <newerInput> <olderOutput> <deltaMs>`: two shapes in one slot, which §4 calls a defect in terms, and three `--json`-only fields rendered on a human path. The `stale-unit ×1` half stays row 28's N=1 twin's subject and is not re-asserted here; AND, on the mutant-formatter half, a remedy plan that does NOT prescribe `rm -rf packages/cli/dist` when `packages/cli`'s only kinds are those two, because `bundle-cli.mjs:113-119` wipes `dist` itself (round 14, C14-5). Row 34's mutant shape for the plan half. ⚠️ **AND a THIRD assertion on this row's rendered half: the output must carry NO line beginning `check-stale-dist: ⚠️ coverage: ` (round 16, `OP16-4`), with its own mutant — a `formatFindings` that emits the label whenever it emits anything, joining the empty list.** ⚠️ **AND ONE ASSERTION ON THIS ROW'S CLEAN SIDE, BEFORE ITS MUTATION (round 36, `OP36-3`): the rendered output must carry NO line beginning `check-stale-dist: remedy plan:`.** §4's **A DEGRADED RUN CARRIES A NEXT ACTION** gates the plan on at least ONE finding OR at least ONE non-`full` entry, and each DISJUNCT had an accepting element — row 34 for findings with all four `full`, row 39's MAIN half for zero findings with a non-`full` entry — while the state in which BOTH are FALSE had none: row 39's NEGATIVE twin, the only element anywhere asserting the plan's ABSENCE, holds four `not-run` entries, so it exercises the `nothing was checked` CARVE-OUT and never the gate. This fixture is the ONLY one in this section provably ZERO-finding AND all-four-`full` — its four grounds are the SET the assertion above already names, and this row may hold NO unit outside the `C15-2` sibling-fixture rule's and the complete-cli-build rule's reach, so a bare unit added later would break this clause exactly as round 26 records for the `M = 0` clause below. Mutant = a `formatFindings` whose plan gate is the NEGATION of §4's `nothing was checked` carve-out alone — the reading §4's own "and on nothing else" licensed when taken as a definition — which renders `check-stale-dist: remedy plan:` and an indented `re-run the check` on every green run: `check:all` member 0, both `test` legs of `ci.yml:129`, and line 5 of §5.1's measured 204 under flagless `pretest`, telling a healthy tree to re-run the check, which is `OP16-4`'s bare-label class at the sibling block. It needs no injection point beyond a formatter swap, FAILS on this fixture, and cannot fail a correct implementation, both disjuncts of §4's gate being false here (`OP16-3`'s disqualifier). A CLEAN side carrying the discriminating half is row 49's own shape, `CF8-2` read the other way. This clause ADDS NO ROW and NO FIXTURE, and ONE invocation — the formatter read on this row's CLEAN state (`OP22-3`). All four cli-scoped comparisons are `full` on this fixture, on the SET of grounds and never on a count of them (`C11-4`): the `C15-2` sibling rule below binds it to hold qualifying output under every unit its own marker names as inlined, the marker parses and is newest, the cache is present and byte-identical, and the plugin-SOURCE clause below — which this very assertion puts this row inside — binds its six `PLUGIN_ASSET_ENTRIES` SOURCES under `packages/plugin/` present and OLDER than the newest file beneath `packages/cli/dist/plugin` (round 26, `C26-1`), the FOURTH ground, unnamed since round 16 while this assertion rested on three that all reach only under `packages/cli/` and without which §3.4's source side yields nothing, the comparison declines, `plugin-assets` reads `not-run` and THIS assertion FAILS a correct implementation (`OP16-3`'s class) while `OP16-4`'s own mutant survives — so §4 SUPPRESSES the coverage line here, and no row in this battery asserted that. Rows 16, 17, 18, 26 and EVERY twin of row 39 — stated as the rule and never as a count, round 35 having appended two more of them (`OP35-1`; `C11-4`'s remedy for a figure an appending round re-invalidates) — run fixtures holding at least one non-`full` entry, and rows 34 and 39 assert the plan rather than the coverage line. That mutant passes rows 1-45, the false-negative battery, the wiring assertions and the live smoke test while printing a bare `check-stale-dist: ⚠️ coverage: ` on every green run — the steady state after the first post-adoption build, on every local `npm test` against a built tree, and in both `test` legs of `ci.yml:129` (§5's own measurement) — at line 5 of §5.1's measured 204 under `pretest`, which is §7's coverage-line false-positive read made unconditional on a healthy tree, on a glyph §8(o) records cannot degrade. ⚠️ **AND A FOURTH ASSERTION ON THIS ROW'S RENDERED HALF: THE STANDALONE UNITS-COMPARED/SKIPPED SUMMARY AT `M = 0`, BY EQUALITY (round 26, `OP26-1`)** — the output must carry `check-stale-dist: <N> units compared, 0 skipped`, `<N>` derived from THIS fixture and never from §4's live figure (`OP16-3`), with NO parenthetical and no empty pair of parentheses. Both kinds this row asserts are non-advisory, so §4's exit table renders NO `PASS` line here and the summary is a line of its own — the STANDALONE slot of §4's **SKIPPED UNITS ARE NAMED ON BOTH PATHS**, whose `M = 0` ruling had an element for the `PASS` slot alone: row 18's fixture is advisory-only and carries that form, row 39's MAIN half runs M=1, row 35 M≥2, row 39's NEGATIVE twin the `nothing was checked` carve-out, and row 34's `M = 0` fixture asserts no summary text at all. `M = 0` is FORCED on this fixture and not incidental — the `C15-2` sibling-fixture rule below binds it to hold qualifying compiler output under the `dist` of every unit its own marker names as inlined and the complete-cli-build rule binds `packages/cli` — and this fixture may hold NO unit outside those two rules' reach, stated because a bare unit added later would make `M` 1 and this assertion would then FAIL a correct implementation (`OP16-3`'s class, settled before the clause was written). Mutant = a `formatFindings` rendering the `PASS`-slot summary with the omission row 18 forces and the STANDALONE line from a second template, `` `${n} units compared, ${m} skipped (${clauses.join(", ")})` `` — two slots being the implementation §4 and §2 both describe in terms — which prints `check-stale-dist: <N> units compared, 0 skipped ()` on every run holding a finding against a fully built tree; re-derived read-only at `e99b066`, that is this repository's own state (19 root references, ZERO of them without a `dist` and ZERO with an empty one) beside §6's live prediction of four `stale-unit` findings, at `check:all` member 0, exit 1 under `--strict` and at line 5 of §5.1's measured 204 under flagless `pretest`. It needs no injection point beyond the formatter swap this row's rendered half already uses and FAILS on this row's own fixture; it cannot fail a correct implementation, §4 mandating the omission at an empty clause list in terms for BOTH slots. This clause ADDS NO ROW, which is the property to state rather than a live battery count §1 also carries (`OP22-3`). ⚠️ **AND A FIFTH ASSERTION ON THIS ROW'S RENDERED HALF: the rendered output must carry NO line beginning `check-stale-dist: PASS` (round 36, `OP36-2`).** §4's **SKIPPED UNITS ARE NAMED ON BOTH PATHS** states in terms that the findings path has no `PASS` line, its exit table printing that line on its first two rows only, and NOTHING in this section asserted it: every site that SAYS so — this row's own fourth-assertion cell, row 18's cell and row 35's fixture cell — is FIXTURE PROSE explaining which row owns which summary slot, never an assertion a body must satisfy, while every rendered findings-path element here asserts the PRESENCE of a line. Both kinds this row asserts are non-advisory, so §4's exit table renders NO `PASS` line on this fixture, which is what this row's fourth assertion already gives as its reason for owning the STANDALONE slot (`OP16-3`'s disqualifier, settled before this clause was written). Mutant = a `formatFindings` whose `PASS` line is gated on the run not having THROWN rather than on that exit table — `check-citation-runs.mjs`'s shape copied WHOLE, standing rule 7, its `console.log` at `:289-291` following all three `process.exit(1)` guards at `:267`, `:278` and `:287` and carrying no condition of its own, re-derived read-only 2026-08-22 at `d0b706e` — printing that line ABOVE the grouped findings so row 18's ORDER conjunct still passes, and leaving the STANDALONE summary in its own slot so this row's FOURTH assertion still passes. It needs no injection point beyond the formatter swap this row's rendered half already uses, FAILS on this row's own fixture, and cannot fail a correct implementation. Under flagless `pretest` that body ends this check's block on a green verdict roughly 199 lines above vitest's summary, at exit **0** by design, on the one path whose only signal is its text — §4's own two hours of misdiagnosis by someone reading the bottom of the output. This clause ADDS NO ROW and NO FIXTURE (`OP22-3`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 39  | a SYNTHETIC tree with **ZERO findings** and ≥1 non-`full` coverage entry: cli built COMPLETE (as row 11), marker present and newest, every other unit clean, then `rm -rf packages/cli/dist` — §4's recipe step 1 applied to the design's own worked example, which §3.3's unit-state table makes `skipped`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | none on the tree — swap in a MUTANT `formatFindings` that prints the ordered remedy plan only when `result.findings.length > 0`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | the row must FAIL on the mutant: the rendered output carries `check-stale-dist: remedy plan:` with recipe **step 5** as its only step, naming each `not-run` entry's absent artifact beside `npm run build`, positioned after the units-compared/skipped summary and before the coverage line (§4's **A DEGRADED RUN CARRIES A NEXT ACTION**). Every one of rows 1-38 PASSES that mutant: rows 16, 17, 18 and 26 assert the COVERAGE line and never the plan, rows 34 and 38 carry findings, and no other row pairs a zero-finding fixture with a non-`full` entry — so without this row the design's own recipe step 1 renders a `PASS` line, three `not-run` entries and no stated action, at exit 0 (round 14, OP14-1). ⚠️ **AND, on the same fixture and the same mutant-formatter swap, the rendered `PASS` line itself by EQUALITY, in §4's FULL shape — `check-stale-dist: PASS — <N> units compared, 1 skipped (packages/cli: no dist/)`, with `<N>` derived from THIS fixture in row 35's own wording and NEVER §4's live `18` (`OP16-3`) — plus the assertion that that summary text appears in the rendered output EXACTLY ONCE (round 21, `OP21-2`).** The summary's ORDINARY rendering — one or more skipped units, outside the `nothing was checked` carve-out — was asserted by nothing anywhere in this design: row 35 is the FINDINGS path, this row's NEGATIVE twin asserts the carve-out form with NO parenthetical, §6's false-negative bullet asserts only that a skipped unit is **named**, and row 18's fixture holds ZERO skipped units. TWO mutants, both failing on this row's own fixture and needing no injection point beyond the formatter swap this row already uses: (a) a `formatFindings` whose `PASS` line drops the parenthetical, printing `1 skipped` with no reason, which leaves the operator unable to tell which of the THREE `skipped` states they are in — `CF9-1`'s distinction, whose `no dist/` literal `OP15-2` wrote for THIS line and which row 30 guards on the findings path alone; and (b) one that emits the standalone summary line BESIDE the `PASS` line, which prints the summary twice on every green run and, on the `nothing was checked` tree, re-emits the 451-character clause list `OP15-5` measured and suppressed — on every fresh clone — because §4 confines that suppression to the `PASS` line while this row's negative twin asserts that line's equality and never the output's line SET. Both counts and the one skipped unit are this fixture's own and a correct implementation renders that text once, so neither clause can fail one (`OP16-3`'s class). This row's plan assertion, its twins — the SET as it stood at ROUND 21'S APPEND, scoped to that round rather than left as a claim about this row's twins today (`C11-4`; round 35 appended two more, `OP35-1`) — and its `--json` invocation are unchanged (standing rule 8), and the TWO mutants above stay scoped to the `PASS` line and are unmoved. ⚠️ **AND its EXIT CODE, on this same fixture, under BOTH `strict` values (round 27, `OP27-1`):** `exitCodeFor(result, { strict: true })` and `exitCodeFor(result, { strict: false })` must BOTH be **0**. This is the battery's ONLY `ZERO findings` fixture and nothing anywhere read the exit code on an empty finding set: row 21's four calls and its EXIT-CODE MIX TWIN, the false-negative battery's truncated-marker, `C10-4` and §8(p) bullets and row 43's CLI twin all carry ≥1 finding; rows 27 and 31 take their exit from §1's catch; row 31's clean baseline, the one zero-finding PROCESS run, is FLAGLESS; and the live smoke test asserts the unit SET and the coverage shape and explicitly no exit code. So `strict ? (findings.length > 0 && findings.every(isAdvisory) ? 0 : 1) : 0` — §4's exit-table rows 1 and 2 merged, `OP23-1`'s guarded-quantifier family one row up — agreed with a correct implementation on every state this section asserted while exiting **1** at `check:all` member 0 on every CLEAN, fully built tree, forever, which §4's own warrant makes the other 14 members' failure too. It needs no injection point, FAILING on this row's own fixture, and it cannot fail a correct implementation: §4's first exit-table row mandates **0** in BOTH columns and §4 states that coverage never changes the exit code, while this fixture's `not-run` coverage entries are its whole degradation (`OP16-3`'s disqualifier). ONE conjunct, TWO wrong bodies — round 23's own `some((f) => ADVISORY_KINDS.has(f.kind))` mutant also returns **1** here against a correct **0**, so this is its second killer and the only one that reaches it on an empty set. Its TWIN, asserted in the same row in row 33's shape: `rm -rf packages/cli/.dts-cache` on the same complete tree, where the unit stays `compared`, `declaration-cache` and `shipped-declarations` report `not-run`, findings are again empty, and the plan must still print. ⚠️ **AND TWO FURTHER TWINS, ONE PER REMAINING CONJUNCT OF §4's `nothing was checked` CARVE-OUT, WHICH HAD THREE CONJUNCTS AND ONE ELEMENT (round 35, `OP35-1`), both written in row 33's shape so nothing is renumbered and NO ROW is added — a TWIN, not a ROW (`OP22-3`).** Its **ALL-NOT-RUN TWIN** is this row's MAIN fixture plus `rm -rf packages/cli/.dts-cache`: `packages/cli` is `skipped`, every other unit still COMPARES, all FOUR entries read `not-run` — `packages/cli/dist absent` for `bundle-freshness` and `plugin-assets`, `packages/cli/.dts-cache/index.d.ts absent` for `declaration-cache`, `packages/cli/dist/index.d.ts absent` for `shipped-declarations` — no finding fires, and the plan must print with recipe step 5 as its only step. ⚠️ **AND, on this same twin and the same formatter swap, the rendered `PASS` line by EQUALITY in §4's ORDINARY shape — parenthetical clause list PRESENT, and NO `; nothing was checked` suffix — `check-stale-dist: PASS — <N> units compared, 1 skipped (packages/cli: no dist/)`, with `<N>`, the skipped count and the reason all derived from THIS twin's OWN fixture and never from §4's live figure (`OP16-3`): the EXCLUDING element for the `PASS`-line carve-out's "no unit was compared" conjunct (round 36, `OP36-1`).** That carve-out has TWO conjuncts and, until this round, ONE element — this row's NEGATIVE twin, where BOTH hold at once — so each was individually DELETABLE exactly as the plan's THREE were, and no fixture in this section held exactly one of them true. Its mutant is §4's `PASS`-line carve-out with **no unit was compared** DELETED, which fires the suffix and the clause-list suppression HERE, where every unit but `packages/cli` still COMPARES and a correct implementation must name the skipped one; it needs no injection point beyond the swap this twin already uses, FAILS on this twin's own fixture, and cannot fail a correct implementation, §4 confining both the suffix and the suppression to the TWO-conjunct state in terms (`OP16-3`'s disqualifier). Its **CACHE-ONLY TWIN** is this row's NEGATIVE twin's fixture plus a `packages/cli/.dts-cache/index.d.ts` NEWER than every member of §3.3's comparand: NO unit is compared, `declaration-cache` RUNS and reads `full` — `skipped` suppresses only `checkUnitFreshness` and `checkOrphans` and never these four (§3.3, round 15, `C15-4`) — the other three decline, no finding fires, and the plan must print, step 5 alone. ⚠️ **AND, on this same twin and the same formatter swap, the rendered `PASS` line by EQUALITY in §4's ORDINARY shape — parenthetical clause list PRESENT, and NO `; nothing was checked` suffix — `check-stale-dist: PASS — 0 units compared, <M> skipped (<units>: no dist/)`, with `<M>` and the unit list derived from THIS twin's OWN fixture and never from §4's live `19` (`OP16-3`): the EXCLUDING element for the `PASS`-line carve-out's "no comparison ran" conjunct (round 36, `OP36-1`).** Its mutant is §4's `PASS`-line carve-out with **no comparison ran** DELETED, which fires the suffix and suppresses the clause list HERE, where `declaration-cache` RAN and reported `full` and a correct implementation must therefore say what was skipped rather than that nothing was checked; it needs no injection point beyond the swap this twin already uses, FAILS on this twin's own fixture, and cannot fail a correct implementation, §4 confining both the suffix and the suppression to the TWO-conjunct state in terms (`OP16-3`'s disqualifier). ⚠️ **This twin and its NEGATIVE base are the two sides of the suppression rule on ONE fixture — the same all-skipped tree, the clause list SUPPRESSED there and RENDERED here, the carve-out not applying once a comparison has run.** That is the shape `OP15-5` measured at **451** characters ON THE LIVE TREE and never on either fixture, both literals being derived from the fixture (`OP16-3`). Each takes the mutant-formatter swap this row already uses and needs no further injection point. The mutant for the first is a `formatFindings` withholding the plan on "no comparison ran" AND "no finding fired" alone; for the second, one withholding on "NO unit was compared" AND "no finding fired" alone — each of them §4's carve-out with ONE conjunct deleted, each agreeing with a correct implementation on every element this design carried before them, and each FAILING on its own twin's fixture. Neither can fail a correct implementation: §4 mandates the plan wherever any entry is not `full`, and these fixtures carry four and three respectively (`OP16-3`'s disqualifier). Both hold the marker as their base fixture leaves it — present and parseable on the ALL-NOT-RUN twin, absent on the CACHE-ONLY twin — so neither reaches the present-and-unreadable coverage state, and this row's MAIN half, its first TWIN, its NEGATIVE twin, its exit-code conjunct, its two `PASS`-line mutants and its `--json` invocation are all unchanged (standing rule 8). And its NEGATIVE twin, which is BOTH carve-outs (round 15, OP15-5: this twin asserted only the plan's absence, so the two mandated renderings of the PASS line on its own fixture were indistinguishable to it — it must now assert the RENDERED `PASS` line as well, EQUAL to §4's SHAPE for that line — `check-stale-dist: PASS — <N> units compared, <M> skipped; nothing was checked`, with `<N>` and `<M>` derived from THIS fixture in row 35's own wording, and NO parenthetical between `<M> skipped` and the semicolon. ⚠️ **NEVER byte-equal to §4's literal (round 16, `OP16-3`):** that literal's `19` is this repository's LIVE root-reference count — re-derived 2026-08-21 by calling `enumerateRootReferences()`, 19 path-form dirs, `e2e/report` … `packages/testkit` — while every fixture in this section is a synthetic tree under `mkdtempSync(tmpdir())` and no rule here gives this one 19 units, so on any fixture with N other than 19 the byte-equal form FAILS a correct implementation. That is `CF15-1`'s class and rows 16 and 17's, at the twin `OP15-5` filed to END the state where the two mandated renderings of the quietest tree in this design are discriminated by nothing, and its only two escapes are both defects: pin today's workspace count into the fixture, which rots on the 20th unit (`C11-4`), or weaken to a `toContain` of the trailing sentence, which passes BOTH renderings and vacates the twin. Its own MUTANT, without which it is vacuous: a `formatFindings` that renders the clause list on this fixture — the shape-conforming line `OP15-5` measured at 451 characters — which this twin must FAIL. Clause list suppressed): a `meta-checks` fixture with no `dist` anywhere and no `.dts-cache`, where all four entries are `not-run`, no unit was compared and no finding fired, and the plan must **NOT** print — without it this row's own fix makes every fresh clone noisy — the two `test` legs of `ci.yml:129` build at `ci.yml:86` first (§5's **THE CHECK HAS ONE CI OCCURRENCE**, round 25, `CF25-3`), so no CI leg ever reaches this tree — standing rule 8. ⚠️ **AND that negative twin now asserts the four coverage entries' REASONS verbatim as well as the plan's absence (round 18, `C18-2`)** — `bundle-freshness: not-run (packages/cli/dist absent)`, `declaration-cache: not-run (packages/cli/.dts-cache/index.d.ts absent)`, `plugin-assets: not-run (packages/cli/dist absent)` and `shipped-declarations: not-run (packages/cli/dist/index.d.ts absent)`, each the one spelling its own section's precedence mandates on a tree where BOTH sides are gone. This fixture is the tree §3.2's REASON PRECEDENCE line was written for in terms — `packages/cli/dist` and `.bundle-meta/metafile.json` are BOTH absent, so ranks 2 and 4 are live at once and "that tree has two admissible spellings" is this design's own sentence — and no row pinned that pair: row 16 and this row's MAIN half both delete `packages/cli/dist` while `.bundle-meta/` survives beside it, so rank 4 is unreachable there; row 22's twin and row 46's marker-absent twin have `dist` present. Its mutant is an implementation that reads the MARKER first, rendering `bundle-freshness: not-run (.bundle-meta/metafile.json absent)` at `check:all` member 0 on every fresh clone, and it must FAIL. ⚠️ **AND a FOURTH invocation, under `--json`, on this row's MAIN fixture (round 18, `OP18-7`):** the parsed object's `coverage` must deep-equal the same run's `result.coverage`. Mutant = a serialiser that emits `{ findings, states }` and drops `coverage`. Re-derived at `896dd84`: `--json` is read in §6 at row 27 alone, whose three invocations all run a THROWING fixture and whose asserted object is `{ internalError, ran: false }` — the one §4 states carries NO `findings`, `states` or `coverage` key — so nothing anywhere read `--json` on a run that COMPLETES, and a CLI dropping `coverage` passed every row, the false-negative battery, the wiring assertions and the live smoke test while the machine path lost the only signal that requirement 3's comparison did not run: `O9-1`'s computed-and-never-rendered class one path over, at the bullet claiming the two paths cannot drift |
| 40  | row 5's clean bundle fixture: `packages/cli` COMPLETE (as row 11) with the marker present and newest, and qualifying compiler output under the `dist` of EVERY unit that fixture's own marker names as inlined (§3.2's metafile derivation; sixteen on the live tree)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `rm -rf <inlined unit>/dist` for one of them (`packages/contracts`), then run `checkBundleFreshness` directly                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | no finding, no throw, and coverage `bundle-freshness: not-run (packages/contracts/dist absent)` — rows 12 and 23's shape and for their stated reason: the mutation column CALLS the comparison, so the return value is the only surface. Mutant (a) = an implementation that drops the `undefined` member and compares the rest: it returns coverage `full` and FAILS this row — the silent partial §4's recipe step 5 licenses an operator to read as "requirement 3's comparison covered its inputs". Mutant (b) = one that reads `.mtimeMs` off `undefined`: it throws, reaching §4's internal-error row at exit **2** from `check:all` member 0. No other row discriminates, and that is now a RULE rather than an observation: the fixture rule below requires every row asserting this comparison to build every inlined member, so no other row reaches the `undefined` case at all, and this row is its ONE deliberate exception (round 15, C15-2). ⚠️ **TWIN, in row 33's shape so nothing is renumbered and this row stays that single exception (round 18, `CF18-3`): the reason's JOIN was satisfied at N=1.** The same clean bundle fixture, whose marker names both as inlined AND names `packages/gates/dist/index.js` BEFORE `packages/contracts/dist/index.js` in its `inputs`, so the mandated `buildUnits` order and `inlinedUnitsFromMetafile`'s key order are OPPOSITE on this fixture and the EQUALITY below discriminates them (round 26, `C26-2`) — without that clause the two COINCIDE, `enumerateRootReferences` sorting (`scripts/repo-census.mjs:293`) and `packages/contracts` sorting before `packages/gates`, so a body filtering `inlinedUnitsFromMetafile(metafile)`, whose order is `metafile.inputs`' own insertion order, renders the mandated string HERE while rendering `packages/gates/dist, packages/contracts/dist absent` on §4 recipe step 1's own two-offender tree: one state in two spellings, which §4's verbatim-reason rule calls a defect in terms. That body needs NO mutant swap, FAILING on this fixture once the orders are opposite, so the TWO mutants below are unchanged and this cell adds none (standing rule 8); and the mandated string is unchanged, a `buildUnits`-order body rendering it whatever the marker's key order is (`OP16-3`'s disqualifier, checked before the clause was written). Mutation `rm -rf` on TWO of its inlined units (`packages/contracts` and `packages/gates`), then `checkBundleFreshness` called directly against the fixture as its `cwd` in row 41's shape; verdict no finding, no throw, coverage `bundle-freshness: not-run (packages/contracts/dist, packages/gates/dist absent)` — EQUALITY on the WHOLE reason string, in the join order §3.2 now states, so the row cannot go green on a set that happens to sort the other way. At N=1 the mandated comma-join, a per-path rendering (`paths.map((p) => p + " absent").join(", ")`) and a first-offender-only rendering (`paths[0] + " absent"`) are BYTE-IDENTICAL, and N=1 was the only case any element reached: this row's original mutation deletes ONE `dist`, and the `C15-2` sibling-fixture rule below binds every OTHER row asserting `stale-bundle` or a `full` `bundle-freshness` entry to hold qualifying output under every inlined member, so by RULE no other row can reach a second offender. The two-offender tree is reachable and is this design's own: §4 recipe step 1's FOURTH carve-out exempts `packages/cli`, so two non-cli units' `dist` go while `packages/cli` stays complete with a fresh marker — after which §4's **A DEGRADED RUN CARRIES A NEXT ACTION** names ONE absent artifact of two under the first-offender rendering and prints a SECOND spelling of one state under the per-path one, which §4's own verbatim rule calls a defect in terms. TWO mutants, each of which must FLIP: the per-path-suffix rendering and the first-offender-only rendering. This row's single-offender half is unchanged, so nothing it already pins is vacated (standing rule 8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 41  | row 5's clean bundle fixture (cli COMPLETE, marker present and newest) with a `scripts/bundle-cli.mjs` in the FIXTURE tree, older than `bundleAt`, and the check run against the fixture as its `cwd`. ⚠️ **AND the fixture also holds the six `PLUGIN_ASSET_ENTRIES` SOURCES under `packages/plugin/` — five DIRECTORIES and the `.mcp.json` FILE, per §3.4's mixed-type rule — every one of them OLDER than the newest file beneath `packages/cli/dist/plugin`, which is row 8's own `plugin assets fresh` shape copied WHOLE and NOT merely "older than `bundleAt`" (round 25, `CF25-2`; standing rule 7).** The weaker bound would have been `CF8-2`'s defect inside the fix: the marker is written LAST, so `newest(dist/plugin)` is at or before `bundleAt`, and a source older than `bundleAt` alone can still be NEWER than its shipped copy, firing `stale-plugin-assets` at BASELINE where the mutation then flips nothing. With the correct bound `plugin-assets` reports `full` and CLEAN at baseline and §3.4 is REACHED rather than `not-run` — without that clause §3.4's SOURCE side is absent, the comparison declines, and the SOLE-kind half below could not fail the implementation it exists to catch, which is CF8-2's defect at the conjunct added to close a gap. Those six sources sit OUTSIDE every unit's `src` and `dist` and under no unit this fixture's root `tsconfig.json` references, so no unit, no stem and no output-set member is introduced and §3.1 is untouched. ⚠️ **AND `.dts-cache/index.d.ts` is either omitted or byte-identical to `dist/index.d.ts` AND newer than every member of §3.3's comparand** (row 3's own clause), so `stale-declarations` and `stale-shipped-declarations` are deterministically absent and the SOLE-kind assertion is decided by the wiring under test and not by the fixture's own drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `utimesSync scripts/bundle-cli.mjs` forward past `bundleAt`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `stale-bundle packages/cli` and **NOT** `stale-unit` — the producer is in no unit's `src`, so §3.2's third input member is the only thing that can fire. The path is resolved against the `cwd` `checkStaleDist` was called with (§2's `PRODUCER_INPUTS`), never against `process.cwd()` or the check's own `import.meta.url`, or the fixture cannot mutate what the check reads and this row is vacuous by construction. Mutant = an input side of the sixteen dists plus `packages/cli/src` alone: it passes rows 5 and 38 and every other row, and ships §3.2's producer member hardcoded never to fire (round 15, C15-3). ⚠️ **AND `stale-bundle packages/cli` is the run's SOLE kind — NOT `stale-unit`, NOT `stale-plugin-assets`, NOT `stale-shipped-declarations` — in row 42's own wording (round 25, `CF25-2`).** SECOND MUTANT: an input wiring that puts `scripts/bundle-cli.mjs` on §3.4's SOURCE side as well as §3.2's input side — the reading §8(b) names in terms when it says the design avoids "three findings", and a defensible one, since `bundle-cli.mjs` does write `dist/plugin/**` and `dist/index.d.ts`. On this row's own mutation it renders `stale-bundle packages/cli` AND `stale-plugin-assets packages/cli`, two remedy rows out of one edit, and it must FAIL — on this row's own end state, with no injection point. ⚠️ **Until round 25 this row's verdict permitted any number of additional kinds**, so §0 requirement 7's `each producer script is an input to ONE artifact it produces` and §8(b)'s partial closure were discharged for `bundle-types.mjs` (row 42's `SOLE kind`) and by NOTHING for `bundle-cli.mjs`: rows 8 and 24 mutate plugin SOURCES and assert the kind fires, never that the source side is exactly the six `PLUGIN_ASSET_ENTRIES`; row 8's round-20 rendered twin asserts `check-stale-dist: stale-plugin-assets ×1 — packages/cli` on ROW 8's end state, where this script is not moved; row 22 is byte-keyed; and row 42 constrains `bundle-types.mjs` alone. It cannot FAIL a correct implementation: the mutation moves only `scripts/bundle-cli.mjs`, which is in no unit's `src`, under no unit's `dist`, and in neither §3.4's nor §3.5's comparand                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 42  | row 6's clean cache fixture (cache newest, `dist/index.d.ts` byte-identical to it) on a `compared` cli with the marker present and newest, plus a `scripts/bundle-types.mjs` in the FIXTURE tree older than the cache, the check run against the fixture as its `cwd`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `utimesSync scripts/bundle-types.mjs` forward past `.dts-cache/index.d.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `stale-declarations packages/cli` as the run's SOLE kind — which is also the non-vacuity fixture §4 step 2's sole-`stale-declarations` carve-out never had (row 34's main fixture carries two kinds and its twin is `stale-shipped-declarations`), and the reachable form §4 step 3 names in terms: a PRODUCER edit makes the cache stale with no `dist` stale. Same `cwd` resolution rule as row 41, and vacuous without it. Mutant = a comparand of `packages/*/src` plus `tsconfig.dts.json` alone: it passes rows 3 and 6, the false-negative battery, the wiring assertions and the live smoke test, and ships §3.3's third comparand member hardcoded never to fire (round 15, C15-3). ⚠️ **Its PLAN TWIN, asserted in the same row in row 33's shape (round 18, `OP18-3`):** on this row's own end state the rendered output must carry `check-stale-dist: remedy plan:` whose steps are **step 3, step 4 and step 5, and NO step 2** — §4 step 2's sole-`stale-declarations` carve-out, whose fixture `C15-3` recorded this row as supplying and which nothing has asserted since; supplying a fixture is not asserting it. Mutant = a formatter that assembles the plan by walking steps 1-5 and applying each step's own printed condition, which here DOES print step 2 because step 2 carries no finding condition of its own: it prints `npm run build` first, which §4 measures as a full `tsc -b` across 19 units plus a duplicate `bundle:cli` on top of the generator's ~5 minutes, and which cannot clear this finding anyway — `bundle-types.mjs:70` skips regeneration while the cache dominates `newestSourceMtime()`, a predicate that stats neither `.json` nor itself — a printed step that cannot clear the finding it is printed for, `CF14-1`'s class at a plan step rather than at a table row. ⚠️ **SCOPE TWIN, in row 33's shape so nothing is renumbered (round 20, `C20-2`) — row 42 now carries TWO twins, this one and the round-18 PLAN TWIN above, so a citation must name which.** Fixture: this row's own tree, run against the fixture as its `cwd`, with ONE further unit whose `Unit.dir` is NOT under `packages/` — `e2e/report`, the live tree's own 19th root reference and the exact member `enumerateRootReferences` (19) has and `workspaces` (18) does not (§3's **Units** step) — added to the fixture's root `tsconfig.json` `references`, its `dist` outputs NEWER than every file under its own `src` and than its own `tsconfig.json` and chain, so it carries no `stale-unit` of its own. Ordering, which is the whole of the row: every `packages/*/src` **`*.ts`** file OLDER than `.dts-cache/index.d.ts` — **`*.ts`**, because comparand member 1's EXTENSION half is what the clause below pins and the unqualified form contradicts it (round 30, `C30-1`) — which is OLDER than that unit's newest `src/**.ts`, which is OLDER than its `dist` outputs; `.dts-cache/index.d.ts` byte-identical to `dist/index.d.ts` and newer than every other member of §3.3's comparand; `packages/cli` COMPLETE with the marker present and newest, as this row's main half; and ⚠️ **ONE further file, `packages/cli/src/fixture.json`, NEWER than `.dts-cache/index.d.ts` and OLDER than `bundleAt` (round 30, `C30-1`)** — under a `packages/*/src`, so member 1's DIRECTORY set already holds it and only its EXTENSION filter decides. ⚠️ **That interval is NON-EMPTY only because this clause pins it, which is stated rather than left to a fixture author to discover:** nothing else here orders `.dts-cache/index.d.ts` against `bundleAt` — §3.5 keys on BYTES, §3.3 compares the cache only against its own comparand, and the complete-cli-build rule constrains the MARKER and not the cache — so this clause places the cache BELOW the chunk set, which no rule this fixture is bound by forbids. A `*.json` and NOT a `.snap`, `.info` or `.mjs` deliberately: it is the ONE extension both wrong bodies below accept, so ONE clause kills both, where a `.snap` kills the accept-less body alone and `isCompilerInput` declines it. `OP16-3`'s and `CF20-1`'s DISQUALIFIER, checked BEFORE the clause was written: `bundleAt` is the MINIMUM over the chunk set and every chunk is a `.js` under `packages/cli/dist` reached by no minus-bullet, so it is a member of §3's filtered output set and this file is older than this unit's newest output — no `stale-unit` fires; it IS §3.2's second input member (newest compiler input under `packages/cli/src`, §3 rules 1-2) and is older than `bundleAt`, so no `stale-bundle`; `packages/cli` carries `kind` `"bundled"` and §3.1 exempts it structurally, so no `orphan-output`; and it adds no output, no stem and no unit, so the complete-cli-build rule, `C15-2`'s inlined-set rule (`^packages/([^/]+)/dist/` never matches a `src` path) and this twin's own conformance argument below are all untouched — a CORRECT implementation keeps BOTH verdicts this twin asserts. NO mutation — the discriminating state IS the baseline. Verdict: **NO `stale-declarations` anywhere in the run**, and `declaration-cache` coverage `full`. Mutant = a `checkDeclarationCache` whose source side is `units.map(u => u.srcDir)` — the only source-side collection §2's signature hands it — which reports `stale-declarations packages/cli` on this twin's own fixture and must FAIL; it needs no injection point, rows 44, 47 and 48's discipline. ⚠️ **SECOND MUTANT, in row 8's own `SECOND MUTANT` spelling (round 30, `C30-1`; standing rule 7):** a `checkDeclarationCache` whose member-1 walk is SCOPE-correct and whose EXTENSION filter is absent or wrong — `newestUnder(join(cwd, "packages", pkg, "src"))` with NO accept, §2 declaring `newestUnder(dir, accept)` and §3.4's own dist side being specified as "a plain recursive walk", or the same call carrying §2's `isCompilerInput`, which §3's input rule 2 makes TRUE of a `src/**.json`. Both read this fixture's `packages/cli/src/fixture.json` as a comparand member and report `stale-declarations packages/cli` at this twin's own BASELINE, so both FAIL on this twin's own fixture with no injection point; the twin's EXISTING verdict is what they fail, so no new verdict is written. Member 1 is `every *.ts under every packages/*/src` — a DIRECTORY set AND an EXTENSION filter — and the mutant above discriminates the first alone (`C20-2`), which is the whole of what this second one adds. Standing rule 8, checked here: that first mutant still fires off `e2e/report/src/**.ts`, which this clause does not touch. Nothing else in §6 reaches it: EVERY fixture unit in this section is under `packages/` (re-derived read-only 2026-08-21 at `9c247eb` over `NR>=2798 && NR<=3275`, the only four `e2e/report` hits being prose about `enumerateWorkspaces`, row 39's PASS-line literal and the wiring block — not one a fixture unit), row 6's mutation is `touch any packages/*/src/*.ts`, rows 3 and 3's twin move configs, row 34 is src-triggered under `packages/`, and the live smoke test asserts the unit SET and the coverage shape and explicitly not the finding set. ⚠️ **And nothing else reaches the EXTENSION half either — a SEPARATE census, because the two halves have different non-discriminators (round 30, `C30-1`):** re-derived read-only 2026-08-22 at `187eb2d` over this section as delimited by this file's `## 6.` and `## 7.` headings, clean tree before and after, exactly TWO places in §6 put a non-`.ts` file under any `src`, and NEITHER can hold a `packages/cli/.dts-cache/index.d.ts`. The first is row **48**, whose own cell opens **NOT a cli-scoped row**, so §3.3's **SKIP RULE — ALL FOUR cli-SCOPED COMPARISONS** makes the declaration comparison `not-run` there under EVERY body. The second is the false-negative battery's `.info`/`.snap`/`.mjs` bullet, which is a BULLET and not a ROW: the complete-cli-build rule quantifies over `Every cli-scoped row` and its plugin-SOURCE clause over `EVERY ROW ASSERTING` — `C26-1`'s keying, on what a ROW asserts — so no rule in this section binds that bullet to hold a cache, and it declines the same way. Every cache-bearing fixture here therefore holds `.ts` sources ALONE, so all three bodies agree on every one of them; the census is what carries that, not a row list, an enumeration keyed on row numbers being `C11-4`'s class and re-invalidated by every append where a census stays complete. On the live tree the mutant reports `stale-declarations packages/cli` for a `touch e2e/report/src/<any>.ts` at `check:all` member 0, exit **1** under `--strict`, on a cache `bundle-types.mjs`'s `newestSourceMtime()` (`:43-61`) cannot see — so `:70` skips and §4's `npm run build` branch cannot clear it. This twin CONFORMS to the complete-cli-build rule and to `C15-2`'s inlined-set rule and is NOT a fourth exception: the added unit is outside `packages/`, so `inlinedUnitsFromMetafile`'s `^packages/([^/]+)/dist/` filter can never name it and it falls outside the set that rule quantifies over — row 45's own precedent. A RETURN-VALUE twin, so it does not join this section's rendered-text group. Row 42's main half and its PLAN TWIN are untouched, so nothing they already pin is vacated (standing rule 8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 43  | cli COMPLETE (as row 11) with `.dts-cache/index.d.ts` present and OLDER than the newest `packages/*/src/**.ts`, every other unit clean. ⚠️ **This fixture deliberately does NOT report clean, and that is forced rather than an oversight (round 8, `CF8-2`, read the other way):** the finding must already exist for the mutation to be able to suppress it, so what the single mutation flips is the unit STATE and never the kind — a verdict row, in row 37's shape                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `rm -rf packages/cli/dist` — §4's recipe step 1, which leaves `.dts-cache/` beside it (§3.3's opening sentence)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `packages/cli` is `skipped` with reason `no dist/` **AND** a `stale-declarations packages/cli` finding is reported in the SAME run — one unit named in the units-compared/skipped summary and as the subject of a finding at once, exit **1** under `--strict`. Mutant = an implementation that suppresses every finding for a `skipped` unit, which §3's skip-rule table licensed until round 15: it passes rows 13, 14 and 15 (whose `skipped` fixtures carry no `.dts-cache`) and rows 12 and 23 (where the comparison declines rather than fires), and produces a false negative for requirement 8's kind on the tree §4's recipe step 1 creates and step 5 tells the operator to re-run against (round 15, C15-4). ⚠️ **CLI TWIN, in row 33's shape so nothing is renumbered (round 19, `CF19-1`):** on this row's own END STATE — `packages/cli` `skipped`, exactly one NON-advisory `stale-declarations packages/cli` finding — run `check-stale-dist.mjs` as a PROCESS against the fixture as its `cwd`, in row 27's invocation shapes — ⚠️ **BOTH of them since round 29 (`OP29-2`), where before it was the MAIN half's alone: round 28 (`OP28-1`) made that row's `C18-5` twin's third invocation `--json --strict` while its main half's stays flagless, so "row 27's invocation shape" names two (`C20-2`'s rule) and this twin now runs each** — over the SET of flag settings {none, `--strict`, `--json` FLAGLESS, `--json --strict`}, stated as the SET rather than as a count so no later append re-invalidates it (`OP22-3`): with NO flag it must exit **0** with `check-stale-dist: stale-declarations ×1 — packages/cli` still RENDERED on stdout, with `--strict` it must exit **1**, and ⚠️ **with `--json` FLAGLESS, on that SAME end state, stdout must `JSON.parse` and the parsed object's `findings` must DEEP-EQUAL the same run's `result.findings` (round 22, `OP22-1`) — AND the process must exit **0** (round 29, `OP29-2`), the DEFAULT half of §4's exit table read on a COMPLETING fixture and the half §4's "`--json` is unaffected on EVERY path" mandates while no element read it** — whose SOLE member carries a `remedy` equal to §4's remedy-table command for `stale-declarations`, a `newerInput`, an `olderOutput` and a NUMERIC `deltaMs`. Deep-equality against the run's OWN return value and never against literals, so no mtime is pinned — row 39's fourth invocation's own shape. ⚠️ **AND, on that SAME invocation, the parsed object's `states` must DEEP-EQUAL the same run's `result.states` (round 24, `OP24-2`)** — the THIRD member §2's `checkStaleDist` declares and the one read on NEITHER surface until this round. It is asserted HERE rather than at row 39's fourth invocation because this twin's end state carries a `skipped` unit BESIDE a finding, which is the exact case §2's `states` comment is written about, while row 39's fixture holds ZERO findings; deep-equal against the run's own return here too, so no unit list and no reason string is pinned. Mutant = a serialiser projecting each finding to `{ kind, unit }`, equivalently one emitting `{ states, coverage }` and dropping `findings`; §2 permits that projection outright while `newerInput?`, `olderOutput?` and `deltaMs?` are OPTIONAL, and it needs no injection point because it FAILS on this row's own fixture — rows 44, 47 and 48's discipline. ⚠️ **SECOND mutant, for the `states` conjunct, and it needs no injection point either: a `formatFindings(result, { json })` emitting `{ findings, coverage }` and dropping `states`** — which is what §1's `report.mjs` cell prescribed in terms until round 24, "findings and coverage → text/JSON", TWO of the three members §2's return declares and the site an implementer builds this path from. Under it a `--json` consumer reads one finding and four coverage entries and NOTHING about the skipped unit, and on §4's own `npm run build:clean` example it loses all **18** `skipped` units while the human page still names them. ⚠️ **Both rounds that closed a sibling key wrote a mutant that KEEPS this one** — row 39's fourth invocation's `{ findings, states }` (round 18, `OP18-7`) and this row's own `{ states, coverage }` (round 22, `OP22-1`) — so the third member of one object survived two rounds spent closing the other two: standing rule 9 across ROUNDS rather than across sites. Until round 22 the four fields §2 declares `--json`-only and §4 justifies as such were read on NO surface: row 27's three invocations run a THROWING fixture whose object carries no `findings` key at all, row 39's fourth invocation reads `coverage` on a ZERO-finding fixture, and every other §6 occurrence of `--json`, `deltaMs`, `newerInput` or `olderOutput` is prose or a MUTANT human shape — so `OP18-7`'s own defect stood at the sibling key of the same object, on the one findings-carrying fixture this battery runs as a process. ⚠️ **That rendered line is §4's COLLAPSED form and never `stale-declarations packages/cli` (round 19, consolidation):** this kind is one of §4's four delta-carrying kinds, the collapse applies at every N including N=1 (`OP15-3`) and this kind is structurally N=1 (`OP18-2`), so the uncollapsed spelling would FAIL a correct implementation — `OP16-3`'s class, caught before the row was written rather than a round later. The EXIT-CODE PAIR is what discriminates — a flagless half alone is satisfied by a CLI that always exits 0, and a `--strict` half alone by one that always exits 1. ⚠️ **And there are TWO such pairs on this twin since round 29 (`OP29-2`), one per SURFACE: the `--json` invocation above no longer stands beside the human pair, and a FOURTH invocation, `--json --strict`, on this same end state, must exit **1**.** It asserts the exit code and NOTHING else — the object is pinned ONCE, at the FLAGLESS `--json` invocation above, so the TWO live citations elsewhere in this design to "row 43's CLI twin's `--json` invocation" (§2's `checkStaleDist` `states` comment and §4's per-kind shapes paragraph) still name that third one and the fourth is cited by its own spelling (`C20-2`'s rule discharged AT the fix rather than by re-pointing them); §4's `e70c9bb` four-site figure and §9's round-28 entry both spell it "third" already. Mutant for the `--json` PAIR, needing no injection point because it fails on this row's own fixture (rows 44, 47 and 48's discipline): a success path spelled `if (json) { console.log(JSON.stringify(result)); process.exit(0) }` ahead of `process.exit(exitCodeFor(result, { strict }))` — the ordinary "machine consumers get a clean stream and a clean code" branch, `CF19-1`'s own shape at the SIBLING of the path `OP28-1` closed in the catch — which passes both human halves, both deep-equalities and the flagless `--json` half and FAILS here; and its mirror, one exiting non-zero whenever `json` is set, which passes here and FAILS the flagless half, so each conjunct has a mutant that flips it ALONE (round 27's set rule). Neither can fail a correct implementation (`OP16-3`'s disqualifier): §4's exit table gives **0** and **1** for this fixture's finding set under the two `strict` values and §4 states that `--json` does not change the exit code, with no `--json` carve-out anywhere. Without the pair, `npm run check:stale-dist -- --strict --json` exits **0** on a stale tree and hands a consumer a populated `findings` array on a stream that reported success — §4's own "unaffected on EVERY path" falsified at the path no element read. Mutant = a `check-stale-dist.mjs` whose success path is `process.exit(exitCodeFor(result, { strict: true }))`, the argument hardcoded rather than threaded from the flag it parsed — equivalently `process.exit(result.findings.length ? 1 : 0)` — which §1 now forbids in terms; it needs no injection point because it FAILS the flagless half on this row's own fixture, rows 44, 47 and 48's own discipline. Until round 19 it passed EVERYTHING: `exitCodeFor` was CALLED directly, at that round, at row 21 and at the false-negative battery's truncated-marker bullet, both under `strict: true` — row **39**'s conjunct under BOTH values is round 27's (`OP27-1`) and post-dates this sentence; row 27's fixture is a malformed config and every exit code row 31 asserts is its LOAD-TIME mutant's, so each asserted non-strict exit 0 there is §1's catch, while row 31's clean baseline is a ZERO-finding tree where **0** is what a correct implementation and the hardcoded one BOTH return; and every other rendered element calls `formatFindings` directly, so no process exit is observed there at all. `pretest` is `node scripts/check-stale-dist.mjs` with no flag (§5, §5.1, and this section's own `expect(root.scripts.pretest).toBe(...)`), so that implementation exits **1** on the four `stale-unit` findings this section predicts for this working copy, and §5.1's measured table makes that PRETEST FAILED with `test` NEVER RUNNING — round 1's `O-2` reached through the CLI's argument rather than through the function or a throw. This row's return-value main half is unchanged, and so is every assertion the CLI twin's flagless and `--strict` invocations already carried. ⚠️ **Round 29 (`OP29-2`) STRENGTHENS the FLAGLESS `--json` one with an exit code and appends a `--json --strict` fourth, and neither vacates anything already pinned (standing rule 8, checked AT the fix):** §4 mandates ONE serialisation for both `strict` settings with no `strict`-varying member, so the `findings` and `states` deep-equalities and the rendered `stale-declarations ×1 — packages/cli` line hold byte-identically, and the human pair's own mutant — the hardcoded `{ strict: true }` success-path argument — still fails the flagless half exactly as before. ⚠️ **AND, on that SAME FLAGLESS invocation, the rendered ordered plan must carry step 3's command `npm run bundle:types -- --force` (round 34, `OP34-1`)** — §4's **The ordered recipe's step 3 stays UNCONDITIONAL** rule, which had NO element for twenty rounds. This end state's SOLE finding is a `stale-declarations` whose newest comparand member IS a `packages/*/src/**.ts` — this row's own clean cell puts the cache OLDER than exactly that set — and it is the ONE fixture in this section pairing that trigger class with a SOLE `stale-declarations`. Mutant = a plan assembler that conditions step 3 on `newerInput`, taking §4's `stale-declarations` remedy row's OTHER branch — `npm run build` also clears it whenever the newest input is a `packages/*/src/**.ts` — as a licence to drop the generator's measured ~5 minutes: it needs no injection point, FAILING on this twin's own end state, and §4 step 2's sole-`stale-declarations` carve-out suppresses step 2 there as well, so the printed plan carries NO build step at all, step 4 never fires either, and `re-run the check` is the whole remedy for a finding it cannot clear — `CF14-1`'s class at the step §4 states as unconditional in terms, and the run §4's own step-3 paragraph measures as reachable (edit `packages/perf/src`, then `npm run typecheck`). Row 42's PLAN TWIN is the only other element asserting step 3 and it CANNOT reach that mutant: its trigger is a `scripts/bundle-types.mjs` PRODUCER edit, a path outside every `packages/*/src`, on which the conditioned body emits step 3 exactly as a correct implementation does — an INCIDENTAL property of that fixture, forced there by its main half's `C15-3` subject and by §4 step 2's carve-out paragraph naming the producer edit as the state's reachable form, so the two trigger classes were never separated. It cannot fail a correct implementation: §4 step 3 reads `npm run bundle:types -- --force` **if `stale-declarations` fired**, with no clause on the trigger, and §4's own consolidation rejected the carve-out in terms (`OP16-3`'s disqualifier, settled before the conjunct was written). Scoped to step 3's PRESENCE and asserting nothing about step 2 or about the step SET, so row 42's PLAN TWIN keeps §4 step 2's sole-`stale-declarations` carve-out to itself and neither subsumes the other (standing rule 8); every assertion this twin already carries is unchanged, and this clause ADDS NO ROW, NO TWIN, NO FIXTURE and NO INVOCATION, which is the property to state rather than a live battery count §1 also carries (`OP22-3`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 44  | a `compared` non-cli unit whose `dist` holds real outputs NEWER than `src` in 1:1 stem correspondence with it, PLUS an `eo-*-fixture-*/` directory holding at least one `.js` whose stem has no `src` counterpart, and whose scratch `.js` is the NEWEST file under `dist` — the live tree's own shape, re-derived read-only 2026-08-21: `packages/journal/dist/eo-crash-fixture-6dP02V` holds **20** such `.js`, newest mtime **1787308024** against the newest non-scratch compiler output **1787133002**, a **2.03-day** forward shift. Baseline reports CLEAN, and in particular ZERO `orphan-output`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `utimesSync` one `src/**.ts` forward past the newest NON-scratch output and still BEHIND the scratch `.js`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `stale-unit <unit>`, and still zero `orphan-output`. ⚠️ **This is the FRESHNESS half of §3's `dist/eo-*-fixture-*/**` exclusion, and no row reached it before round 16 (`C16-1`).** An `isCompilerOutput` omitting the exclusion takes the scratch `.js` as the unit's newest output, so the mutation moves nothing past it and `stale-unit` never fires — the 2.03-day window measured above is how much staleness that masks on the live tree today. Row 14's amended fixture pins the STEM half in its baseline and the unit-STATE half in its end state; neither row subsumes the other, because a `stemsUnder` accept-predicate and an `isCompilerOutput` are two places one exclusion has to land and this design's own history is a fix that reaches one of them. Not a cli-scoped row, so the complete-cli-build rule below does not reach it. ⚠️ **TWIN, in row 33's shape so nothing is renumbered (round 20, `C20-1`): this row asserts the EXCLUSION side of §3's output filter and the WHITELIST side had no element anywhere in §6.** Same shape, with the scratch directory REPLACED by two files. (1) A `.d.ts.map` written BESIDE an output pair this fixture already holds and sharing ITS stem — `dist/sub/b.d.ts.map` beside `dist/sub/b.js` and `dist/sub/b.d.ts`, whose `src/sub/b.ts` this row's own 1:1 correspondence already supplies — as the NEWEST file under `dist`. ⚠️ **The SHARED stem is load-bearing and was corrected in consolidation:** a `.map` at a stem no `src` file carries is itself an `orphan-output` under the CORRECT whitelist, so the baseline this row asserts CLEAN would already have been failing and the mutation would have flipped nothing — `CF8-2`'s vacuity, caught before the row was written. It is also what `declarationMap: true` (`tsconfig.base.json:22-24`) actually emits. (2) `dist/build-info.json`, OLDER than every compiler output, whose stem `build-info` has NO counterpart under `src` under ANY extension — that absence being what mutant (b) needs and what the whitelist makes harmless. Mutation: `utimesSync` one `src/**.ts` forward past the newest `.js`/`.d.ts` under that unit's `dist` and still BEHIND the `.d.ts.map` — row 44's own mutation shape, aimed into the window the `.map` opens. Verdict: **CLEAN at baseline AND at the end state** — no `stale-unit`, and ZERO `orphan-output` in both. TWO mutants, one per walked side, both failing on this twin's OWN fixture and needing no injection point, rows 44, 47 and 48's discipline: **(a)** an MTIME-side accept predicate whose whitelist is `.js`/`.d.ts` alone — carrying all four minus-bullets and `recursive: true` correctly — reads the newest output as the `.js`/`.d.ts`, so the mutation flips it to `stale-unit` and it must FAIL; **(b)** a STEM-side accept predicate with the four minus-bullets and NO whitelist accepts `dist/build-info.json`, whose stem no `src` file carries, and reports one `orphan-output` at BASELINE, so it must FAIL before the mutation. The two are separated by construction: the `.json` is older than everything, so it never moves the mtime verdict, and the `.map` shares a stem, so it never moves the orphan verdict. That the two mutants land on DIFFERENT walks is the point — this row's own cell already records that an accept predicate and an `isCompilerOutput` are two places one filter has to land (`C16-1`), and round 20 reads that at the inclusion side. ⚠️ **The live cost of mutant (a) is MEASURED, not asserted (standing rule 2):** re-derived read-only 2026-08-21 at `9c247eb` (`git status --porcelain` empty before and after), `packages/supervisor`, `packages/scheduler` and `packages/learning` are each in this twin's exact baseline shape — newest filtered output a `.d.ts.map` at 1787133004.80 / 1787133015.08 / 1787133016.63, newest `src` input at 1787065543.56 / 1787078266.57 / 1786871043.74, newest `.js`/`.d.ts` at 1787058900.27 / 1787072756.96 / 1786862792.88 — so mutant (a) reports **SEVEN** `stale-unit` against the **FOUR** this section's live paragraph predicts, exit **1** under `--strict` at `check:all` member 0. Nothing else discriminated either mutant: row 48 CITES the four-extension filter and holds no `dist/**.json` and no `.map`, row 22's twin writes `dist/index.d.ts.map` in the same emit as `dist/index.js` so it moves no verdict under either reading, and the live smoke test asserts the unit SET and the coverage shape and explicitly not the finding set, so 7-vs-4 is invisible to it. The twin's BASELINE is §7's `Stale .d.ts.map lingering after declarationMap: false` row — an accepted false NEGATIVE that this row asserts rather than a better one, §6's own discipline for §8(i) and §8(p). A RETURN-VALUE twin, so it does not join this section's rendered-text group. Row 44's own fixture holds no `.map` and no `dist/**.json`, so nothing its main half already pins is vacated (standing rule 8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 45  | row 5's clean bundle fixture — `packages/cli` COMPLETE (as row 11) with the marker present and newest — whose marker's `inputs` name `packages/A/dist/index.js` ONLY, while that fixture's `packages/cli/tsconfig.json` references BOTH `packages/A` and `packages/B` and both hold qualifying compiler output. **The ONE fixture in this section where the metafile-derived inlined set and the tsconfig reference closure deliberately DIFFER**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `utimesSync` `packages/B/dist/index.js` forward past `bundleAt` — and, on a second copy of the same fixture, `packages/A/dist/index.js` instead — then run `checkBundleFreshness` directly against the fixture as its `cwd`, in row 41's shape                                                                                                                                                                                                                                                                                                                                                                          | (i) the **B** mutation must leave the run CLEAN with `bundle-freshness` coverage `full`; (ii) the **A** mutation must report `stale-bundle packages/cli`. ⚠️ **Half (i) is the assertion; half (ii) is what stops an implementation that never fires from satisfying it (round 16, `C16-2`).** §3.2's rule that the inlined set is `inlinedUnitsFromMetafile(metafile)` — never the package.json graph, never the tsconfig closure — was exercised by NOTHING for fifteen rounds: re-derived 2026-08-21, that name is ASSERTED in §6 in this row ALONE — the ASSERTION and never the occurrence set, because the SET moved with the next append exactly as the count did (round 29, `C29-1`): re-derived read-only at `c672565` it occurs in §6 at rows 40, 42 and this one, row 40 inside its twin's fixture-construction clause about the marker's `inputs` ORDER (`C26-2`) and row 42 inside a fixture-conformance argument about `C15-2`'s inlined-set rule, and neither asserts §3.2's derivation. The historical figures keep their shas: **three**, none in §6, is correct at `git show db857c3:<file>` and is **six**, one in §6, at `git show badf45a:<file>`, this cell being that one, and the SET form round 17 wrote (`CF17-2`) was true only there. A `checkBundleFreshness` taking the transitive closure of `packages/cli/tsconfig.json`'s references — re-derived read-only 2026-08-21, **16** direct references — passes every other row, the false-negative battery, the five bundler source properties, the `units.mjs` and `walk.mjs` import reads and the live smoke test, which asserts the UNIT set and never the INLINED set, because every other cli-scoped fixture makes the two derivations COINCIDE by construction and neither the marker-`outputs` rule nor the inlined-`dist` rule below forces them apart at any row. It fails half (i) with `stale-bundle packages/cli` against a bundle containing none of B's code, verbatim round 1's `CF-1`, whose fix this rule is; esbuild erases type-only imports and tree-shakes unused runtime surface while `tsc` project references do neither, which is why the two sets are not the same question whatever §3.2's ℹ️ measures them at today                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 46  | **NOT a cli-COMPLETE fixture, and the exception list below records it (round 17, `C17-1`).** `packages/cli` built COMPLETE in row 11's shape — marker present and newest, `.dts-cache/index.d.ts` present, qualifying compiler output under the `dist` of every unit that fixture's own marker names as inlined — every other unit clean; then `packages/cli/dist` is left PRESENT and EMPTY, no `dist/.tsbuildinfo`, no `dist/bin/`, no `dist/plugin/`, while `.bundle-meta/` and `.dts-cache/` survive beside it (§3.3's opening sentence, `C8-2`'s fact). §3.3's reason table makes that **`skipped`** and NOT `unbuilt` — `unbuilt` needs `.tsbuildinfo` — and §3.3's cli three-clause conjunction is evaluated only INSIDE the `compared` branch, so no clause runs. Reachable with two published root scripts and no manual `rm`: fresh clone, `npm run typecheck` (`tsc -b`), `npm run build:clean` (`tsc -b --clean`). Re-derived read-only 2026-08-21 at `badf45a`, `./node_modules/.bin/tsc -b packages/cli --clean --dry` under the pinned typescript 6.0.3 lists **4,781** file paths, **five** under `packages/cli/` (`dist/bin.js`, `dist/index.js`, `dist/index.d.ts`, `dist/bin/supervisord.js`, `dist/.tsbuildinfo`) and **zero** directory entries — `--clean` deletes files, `.tsbuildinfo` among them, and leaves every `dist` standing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | run `checkBundleFreshness` and `checkPluginAssets` DIRECTLY, then run both again on the same tree with `.bundle-meta/` removed — the marker-ABSENT twin, in row 33's shape so nothing is renumbered                                                                                                                                                                                                                                                                                                                                                                                                                     | no throw and NO finding of any kind on EITHER twin, with coverage `bundle-freshness: not-run (packages/cli/dist holds no esbuild chunk)` and `plugin-assets: not-run (packages/cli/dist/plugin absent)`, IDENTICAL on both twins — rows 12, 23 and 40's direct-call shape and for their stated reason, the return value being the only surface. Mutant (a) = an implementation keeping the pre-round-17 rule that `bundleAt` is always defined: it reads `.mtimeMs` off `undefined` and THROWS, reaching §4's internal-error row at exit **2** from `check:all` member 0, halting the other 14. Mutant (b) = one that guards the `undefined` and renders `bundle-freshness` `full`: a comparison that compared nothing, which §4's recipe step 5 licenses an operator to read as covered. Mutant (c) = one that gates `bundle-provenance-missing` on `packages/cli/dist` EXISTING, which is what §3.2's structural-silence bullet said until this round: it stays silent on the false-negative battery's dist-ABSENT bullet and fires the advisory on this row's marker-absent twin, on a `packages/cli` holding zero build output. No earlier row discriminates any of the three — rows 12, 39 and 43 delete the WHOLE `packages/cli/dist`, so the skip fires on `packages/cli/dist absent` before `bundleAt` is ever read, and every other cli-scoped row is bound by the complete-cli-build rule below to a fixture holding a chunk. ⚠️ **Its PLAN TWIN, asserted in the same row in row 33's shape (round 18, `OP18-4`):** on this row's marker-PRESENT tree, with `.dts-cache/index.d.ts` additionally NEWER than every member of §3.3's comparand (row 3's own clause, without which a `stale-declarations` here voids the zero-finding premise), run `checkStaleDist` and `formatFindings` rather than the two direct calls. This fixture then holds ZERO findings — `packages/cli` is `skipped` and every other unit is clean — with units compared greater than zero, so §4's `nothing was checked` carve-out does not apply and **A DEGRADED RUN CARRIES A NEXT ACTION** fires. The rendered output must carry `check-stale-dist: remedy plan:` with step 5 as its only step, naming `npm run build` as the producer for ALL THREE non-`full` entries: `bundle-freshness` (`packages/cli/dist holds no esbuild chunk`), `plugin-assets` (`packages/cli/dist/plugin absent`) and `shipped-declarations` (`packages/cli/dist/index.d.ts absent`), while `declaration-cache` runs `full`. Mutant = a renderer built from the pre-round-18 wording, which named the PRODUCER of each ABSENT ARTIFACT: the chunkless reason names a directory that is PRESENT, so it emits producers for two entries of three and nothing for requirement 3's — verbatim `OP17-2`'s own defect, at the spelling round 17 created, in the same commit. No other row in this battery renders a plan on a tree carrying that reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 47  | **NOT a cli-scoped row**, so neither the complete-cli-build rule nor `C15-2`'s inlined-set rule below reaches it (row 44's shape). A synthetic tree whose root `tsconfig.json` references `packages/A` only; `packages/A` `compared`, 1:1 stems, every `dist` output NEWER than every `src` file AND newer than `packages/A/tsconfig.json`, the fixture's own `tsconfig.base.json` and the root `tsconfig.json`; PLUS `tsconfig.emit-into-A.json` AT THE FIXTURE ROOT — OUTSIDE `packages/A/`, from round 33 (`CF33-1`) — declaring `"extends": "./packages/A/tsconfig.json"` with `"outDir": "./packages/A/dist/emit"` (round 27, `C27-1`, whose `"./dist/emit"` this re-spells against the config's own directory to the SAME resolved `packages/A/dist/emit`) — a STRICT subdirectory of `packages/A`'s `distDir`, reached by NO minus-bullet of §3's output set and therefore IN that unit's COMPARED output set, and a directory this fixture NEVER creates, exactly as the twin's is, so §3.1's stem bijection is untouched and no new stem is introduced — that config OLDER than the newest output under `packages/A/dist`. `isVariantTsconfig("tsconfig.emit-into-A.json")` returns true (`repo-census.mjs:75-79`) and `enumerateTsconfigs`' walk pushes a ROOT-LEVEL variant through its `rel === ""` branch, as the bare basename (`repo-census.mjs:316-318` — the branch `tsconfig.base.json` takes on the live tree), both re-derived read-only 2026-08-22 at `2ef2ce5`, so `enumerateTsconfigs(cwd)` surfaces it from the fixture's own tree; its chain resolves INTO the unit, its RESOLVED `outDir` lands in the unit's COMPARED output set, AND its RESOLVED `composite` is `true` — INHERITED from `packages/A/tsconfig.json`, which declares `"composite": true` and which `tsconfig.emit-into-A.json` does NOT override — **all three of the clauses §3 input rule 4 states, clause (iii) pinned here from round 24 (`C24-1`)**. ⚠️ **AND ITS LOCATION IS CLAUSE (i)'s PIN, from round 33 (`CF33-1`): this config sits at the FIXTURE ROOT and reaches `packages/A` by its `extends` chain ALONE, which is exactly what a `descendantConfigs` spelling clause (i) as PATH CONTAINMENT — `allConfigs.filter((c) => c.startsWith(unitDir + "/"))`, `unitDir` being a PATH and that function's first argument — cannot see.** Until that round every rule-4 config in this design sat INSIDE its unit's directory, so the two readings agreed on all of them and A MANDATED CONJUNCT THAT EVERY ELEMENT AGREES ON IS A CONJUNCT NOTHING TESTS (round 25, `CF25-1`), one clause over; the live tree agrees too, `packages/cli/tsconfig.dts.json` being the one config whose chain resolves into a unit and sitting under that same unit (re-derived read-only 2026-08-22 at `2ef2ce5`). ⚠️ **This cell spelled the criterion as the first two clauses alone, and `packages/cli/tsconfig.dts.json` satisfies BOTH of those, so an implementation reading it as written passed this row and FAILED §6 row 3's NOT-`stale-unit` half on the live tree (`C24-1`).** ⚠️ **And without the `composite` pin this fixture is SILENT on the property the corrected rule requires, so a CORRECT implementation carrying clause (iii) would report nothing here and FAIL this row on its own fixture** — `CF20-1`'s and `OP16-3`'s class, an element that fails a correct implementation. Clause (iii) is the only CLAUSE on which this config and row 3's DISAGREE — both SATISFY (i) and (ii), row 3's INHERITED `outDir` resolving to its unit's `distDir` and this one's, from round 27 (`C27-1`), to a STRICT subdirectory of `packages/A`'s, which is a difference in LITERAL and never in clause and is stated here so a later round does not file it as an inconsistency. ⚠️ **That is a statement about THIS PAIR and NEVER a coverage claim for clause (ii), which BOTH of them SATISFY (round 25, `CF25-1`)** — a mandated conjunct every element AGREES on is a conjunct nothing tests, and clause (ii)'s own element is this row's ROUND-25 TWIN in the verdict cell, whose config passes (i) and (iii) and FAILS (ii). Baseline reports NO finding | `utimesSync tsconfig.emit-into-A.json` forward past the newest output under `packages/A/dist`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `stale-unit` with the reported unit set EQUAL to the single `Unit.dir` `packages/A` — equality, never `×N` (`OP15-1`, `CF16-3`). ⚠️ **This is requirement 9's DOWNWARD half, and NOTHING exercised it for sixteen rounds (round 17, `CF17-1`).** Re-derived read-only 2026-08-21 at `badf45a`: `descendantConfigs` occurs **once** in this whole design, at §2's declaration, and `downward`/`DOWNWARD` **twice**, at §0's requirement 9 row and that same line; none of the three occurs anywhere in §6. THREE mutants (round 27, `C27-1`; the third from round 33, `CF33-1`), ALL leaving this fixture clean and FAILING this row on the row's own fixture, so none needs an injection point: (a) `descendantConfigs` returning the empty array, or an input set built from rules 1-3 alone; and (b) one spelling clause (ii) as EQUALITY between the config's resolved `outDir` and the unit's `distDir`, which declines this row's `"./packages/A/dist/emit"` — the reading every accepting-side config in this design satisfied until round 27, both of them resolving to `distDir` itself; and (c) one spelling clause (i) as PATH CONTAINMENT — `allConfigs.filter((c) => c.startsWith(unitDir + "/"))` — which declines this config for sitting at the FIXTURE ROOT while its chain resolves INTO `packages/A` (round 33, `CF33-1`). ⚠️ **`OP16-3`'s and `CF20-1`'s disqualifier checked AT the fix: a CORRECT body ACCEPTS this config on ALL THREE clauses**, the COMPARED output set being a set of FILES that need not exist yet and no minus-bullet reaching `dist/emit/**`, and clause (i) being a test on the RESOLVED `extends` chain rather than on where the config file sits, so this half cannot fail a conforming implementation. No earlier row reaches it — §0 cited row **3**, whose verdict has mandated `stale-declarations` and explicitly **NOT** `stale-unit` since `C14-3`, so the omitting implementation PASSES row 3; row 2 walks rule 3's UPWARD chain; rows 28 and 34 are src-triggered; the census-import predicate matches the import STATEMENT, so importing `enumerateTsconfigs` and discarding its return passes it; and the live tree cannot discriminate, rule 4 having **0** members there (§3). A RETURN-value row: the subject is the input SET, not the reporter. ⚠️ **TWIN, in row 33's shape so nothing is renumbered and NO ROW is added — it adds a TWIN, not a ROW, which is the property to state rather than a live battery count §1 also carries (`OP22-3`) — and it is clause (ii)'s FAILING-side element, which nothing in this design carried (round 25, `CF25-1`).** This row's MAIN half and row 3 BOTH SATISFY clauses (i) and (ii) and differ on (iii) alone, so between them a `descendantConfigs` implementing clauses (i) and (iii) ALONE — clause (ii) DROPPED — passes both; round 27 (`C27-1`) pins the EQUALITY reading of clause (ii) at the MAIN half and leaves that DROPPING body to this twin. FIXTURE: this row's own synthetic tree PLUS a SECOND variant config `packages/A/tsconfig.emit-elsewhere.json`, declaring `"extends": "./tsconfig.json"` — so its RESOLVED `composite` is INHERITED-TRUE from `packages/A/tsconfig.json` exactly as `tsconfig.emit-into-A.json`'s is, clauses (i) and (iii) BOTH holding — and declaring `"outDir": "./dist/eo-elsewhere-fixture-1"`, a directory this fixture NEVER creates and which lies INSIDE `packages/A/dist` RAW and OUTSIDE that unit's COMPARED output set — through §3's `dist/eo-*-fixture-*/**` minus-bullet, which is in BOTH families and is NOT cli-scoped, rows 14 and 44 being its non-cli rows — so clause (ii) FAILS under the COMPARED-set wording round 14 chose (`C14-3`) and it is the ONLY property separating the two configs in ONE fixture; that file OLDER than the newest output under `packages/A/dist` at baseline, and with NO `packages/A/dist/eo-elsewhere-fixture-1/` written, so §3.1's stem bijection and the unit's output set are untouched and no new stem is introduced. ⚠️ **AND A THIRD VARIANT CONFIG, `packages/A/tsconfig.standalone.json`, which is clause (i)'s FAILING-side element and this twin's SECOND non-member (round 34, `CF34-1`).** It declares NO `extends`, so its chain reaches NO config inside `packages/A` and clause (i) FAILS — exactly as the `packages/gates/tsconfig.standalone.json` §3 HYPOTHESISES does — a config §3 states in terms and NOT a file on this tree, corrected in round 35 (`CF35-3`) and re-derived read-only 2026-08-22 at `a5605e9`: `git ls-files \| grep -i standalone` returns exactly ONE path, `docs/evidence/phase-15/closeout-c7-standalone-entry-point.txt`; `ls packages/gates/` returns `dist`, `goldens`, `package.json`, `src` and `tsconfig.json`; and `git ls-files \| grep -E 'tsconfig.*\.json$' \| wc -l` returns **30** with no `standalone` member anywhere — while the FILE itself sits INSIDE the unit, which is the one placement a CHAIN test and a PATH test disagree on, this row's MAIN half being the same disagreement read the other way. It declares `"composite": true` DIRECTLY, so (iii) holds without inheritance, and `"outDir": "./dist"`, resolving to `packages/A/dist`, that unit's `distDir` ITSELF, so (ii) holds by the same at-or-below containment row 3's config satisfies, past every minus-bullet of §3's output set. It is set NEWER than the newest output under `packages/A/dist`— the MIRROR of `tsconfig.emit-elsewhere.json`, which is OLDER — so the BASELINE is the state at which it discriminates. It writes no directory, sits at the unit ROOT rather than under `srcDir`, and is neither `packages/A/tsconfig.json` nor a member of that file's upward chain, so rules 1, 2 and 3 cannot reach it either and "in NO input set" stays exact; §3's output set, §3.1's stem bijection and `packages/A`'s `compared` state are untouched. `isVariantTsconfig("tsconfig.standalone.json")` returns true (`repo-census.mjs:75-79`) and the walk pushes it through the `rel`-prefixed branch (`:316-318`) as `packages/A/tsconfig.standalone.json`— this twin's other config's own branch, and not the ROOT-LEVEL one the MAIN half takes — both re-derived read-only 2026-08-22 at `5e4918f`. `isVariantTsconfig("tsconfig.emit-elsewhere.json")` returns true (`repo-census.mjs:75-79`, re-derived read-only at `7837d25`), so `enumerateTsconfigs(cwd)` surfaces it from the fixture's own tree as it surfaces this row's MAIN half — though NOT through the same branch: this twin's config takes the `rel`-prefixed branch of `repo-census.mjs:316-318` and that half the ROOT-LEVEL one, from round 33 (`CF33-1`) — and rules 1, 2 and 3 cannot reach it either — it sits at the unit ROOT, not under `srcDir`, and it is neither `packages/A/tsconfig.json` nor a member of that file's upward chain, which is what makes "in NO input set" exact rather than approximate. MUTATION: `utimesSync packages/A/tsconfig.emit-elsewhere.json` forward past the newest output under `packages/A/dist`. VERDICT: **CLEAN at BASELINE and at the END state** — zero findings, ZERO `orphan-output`, no `stale-unit`, and `packages/A` still `compared`. THREE mutants (round 26, `CF26-1`; the third from round 34, `CF34-1`), ALL failing on this twin's OWN fixture with no injection point (rows 44, 47 and 48's discipline) and ALL rendering `stale-unit packages/A` where a correct body renders nothing: (a) a `descendantConfigs` implementing clauses (i) and (iii) ALONE, and (b) one evaluating clause (ii) as RAW path containment under `<unit>/dist` instead of against the COMPARED output set — the cheaper reading, an `outDir` being a DIRECTORY while the compared set is a set of FILES that need not exist yet, and the reading `C14-3` refused — both attaching `tsconfig.emit-elsewhere.json` at the END state; and (c) one implementing clauses (ii) and (iii) ALONE, with NO chain test at all, which attaches `packages/A/tsconfig.standalone.json`— (ii) and (iii) BOTH holding there — and renders `stale-unit packages/A` at BASELINE, the state every other reading reports clean. ⚠️ **Standing rule 8 at the two mutants already here: NEITHER is vacated by the third config.** Both carry clause (i) as the CHAIN test, so both DECLINE a config with no `extends`, and both still fire on `tsconfig.emit-elsewhere.json` at the END state exactly as before. And this row's MAIN-half PATH-CONTAINMENT mutant is ALSO killed here — it ATTACHES this config, the file sitting inside the unit — which is a SECOND element for a body the MAIN half already names, deliberately NOT counted again in this cell. ⚠️ **No other site restates this twin's mutant count**, this section carrying no aggregate mutant figure — re-derived read-only 2026-08-22 at `d0b706e`, the mutant-count census over §6 returns **14** hits. ⚠️ **That figure read 13 until round 36, and it had DRIFTED at this, the ONE live site carrying it (round 36, `OP36-1`).** Round 35 re-derived 13 at `a5605e9` BEFORE its apply and then wrote a standing-rule-8 sentence into row **34** carrying a COUNT WORD before `mutants`, which this cell's own quoted command matches — the promise that every ADDED ELEMENT would be spelled `SECOND MUTANT` held for the elements and not for the prose written beside them, which is standing rule 3 at a figure whose command is the reproducible part. Round 36's own additions are spelled `Its mutant is` and `Mutant =`, which that command does not match, so **14** is the figure before and after that apply. The hits are TEN in other rows' own cells (one each at rows 1, 4, 5, 28, **34** and 44, two each at rows 39 and 40), TWO at this row (its MAIN half's and this cell's) and TWO inside the wiring-assertion comment blocks, and not one an aggregate over the battery; the command is in §9's round-26 entry, and moving this cell from TWO to THREE changes the WORD before `mutants` and no count. ⚠️ **The figure this sentence carried, 10 at `e99b066`, is SUPERSEDED rather than preserved** — this round moves the very count it was measured to protect, and a sha-pinned figure is kept only while the claim above it stands. ⚠️ **The END state is the discriminating half for mutants (a) and (b), and from round 34 (`CF34-1`) the BASELINE is the discriminating half for mutant (c) — row 49's mirror read the other way (`CF8-2`):** there the baseline carries the load because the mutant is already `skipped` before the mutation; here the MUTATION separates (a) and (b) from a correct body while the BASELINE separates (c), which this cell read as `clean under BOTH readings` while (c) was unbuilt. ⚠️ **`OP16-3`'s and `CF20-1`'s disqualifier checked AT the fix: this twin provably cannot FAIL a correct implementation** — under all three clauses NEITHER config is in any input set, `tsconfig.emit-elsewhere.json` failing (ii) and `tsconfig.standalone.json` failing (i) on the CHAIN, which is what clause (i) tests and never where the file sits (round 33, `CF33-1`), so both of its states are clean. ⚠️ **Standing rule 8, checked the other way:** `packages/A/dist/eo-elsewhere-fixture-1/` is never created and `packages/A/tsconfig.standalone.json` writes no directory either, so §3's output set, §3.1's stem bijection, rows 14 and 44's `eo-*-fixture-*` elements and this row's MAIN half — its own mutation, its `stale-unit packages/A` equality and ALL THREE of its mutants — are all untouched. A RETURN-VALUE half too, by `C20-2`'s per-half rule: its subject is the input SET and not the reporter, so it joins none of this section's rendered-text, mutant-formatter or prefix enumerations, exactly as this row's main half joins none; and this row is NOT cli-scoped, so neither the complete-cli-build rule nor `C15-2`'s inlined-set rule reaches it. BOTH configs are SYNTHETIC, so rule 4's live member count stays **ZERO** and §3's 21-of-30 arithmetic, its 20/9 split, §0's `29 PATHS` and §8(l)'s NINE are all unmoved. A RETURN-value row: the subject is the input SET, not the reporter                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 48  | **NOT a cli-scoped row** (row 44's shape). A `compared` non-cli unit whose `src` holds one `*.ts` AND one `*.json`, 1:1 stems on the `.ts` side, with every `src` file, the unit's `tsconfig.json` and its full `extends` chain all OLDER than every output under the unit's `dist`. Baseline reports NO finding, and in particular ZERO `orphan-output` — the `.json` is an INPUT and never an output stem, §3's output filter being `.js`, `.d.ts`, `.js.map`, `.d.ts.map` — ⚠️ **which this cell CITES and does not assert (round 20, `C20-1`)**: this fixture holds no `dist/**.json` and no `.map` at all, so BOTH wrong readings of that filter — a `.js`/`.d.ts`-only whitelist and a reading with no whitelist — pass this row unchanged, and §6 row 44's round-20 twin is where the filter itself is pinned                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `utimesSync` the `src/**.json` forward past the newest output under the unit's `dist`, leaving the `*.ts` untouched                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `stale-unit` with the reported unit set EQUAL to that unit's `Unit.dir` (`OP15-1`). ⚠️ **§3's input rule 2 was in exactly rule 4's state, and standing rule 9 is why it is closed in the same pass (round 17, `CF17-1`).** Every src-triggered row in this battery moves a `*.ts` (rows 1, 6, 28, 34, 44), and the false-negative battery's `.info`/`.snap`/`.mjs` bullet is the EXCLUSION side, which a `*.ts`-only walk also passes — so an input walk filtered to `*.ts` shipped rule 2 hardcoded never to fire against the **3** members it has on the live tree, re-derived read-only 2026-08-21 at `badf45a`: `packages/connectors-jira/src/testkit/fixtures/read-scenario.cassette.json`, its `write-scenario` sibling, and `packages/engine-claude/src/live/fixtures/live-verdicts.json`. Mutant = that walk: it leaves this fixture clean and FAILS this row on the row's own fixture. A RETURN-value row                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 49  | **NOT a cli-scoped row**, so neither the complete-cli-build rule nor `C15-2`'s inlined-set rule below reaches it (rows 44, 47 and 48's shape). A synthetic tree whose root `tsconfig.json` references `packages/A` only; `packages/A/tsconfig.json` extends the fixture's own `tsconfig.base.json` and declares `"rootDir": "./source"`, `"outDir": "./build"` and `"include": ["source"]` — the ONLY fixture in this battery whose layout is not `src`/`dist`; `packages/A/source/a.ts` with `packages/A/build/a.js` and `packages/A/build/a.d.ts` in 1:1 stem correspondence, every output NEWER than every source and than `packages/A/tsconfig.json`, the fixture's own `tsconfig.base.json` and the root `tsconfig.json`. ⚠️ **The BASELINE is the load-bearing half and is asserted:** NO finding, in particular ZERO `orphan-output`, and `packages/A` `compared` — never `skipped` — because under the mutant the baseline is ALREADY a `skipped` unit, so asserting the end state alone would leave `CF8-2`'s vacuity                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `utimesSync packages/A/source/a.ts` forward past the newest output under `packages/A/build`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `stale-unit` with the reported unit set EQUAL to the single `Unit.dir` `packages/A` — equality, never `×N` (`OP15-1`, `CF16-3`). ⚠️ **§3's Units step mandates the per-unit `rootDir`/`outDir` reading and NOTHING in this section exercised it for twenty rounds (round 21, `C21-1`, standing rule 9).** Mutant = a `buildUnits` setting `srcDir = join(cwd, dir, "src")` and `distDir = join(cwd, dir, "dist")` — the path of least resistance, and the reading §3's own rule text spelled until this round: it stats an absent `packages/A/dist`, returns `skipped` with reason `no dist/` at BASELINE and again after the mutation, and so FAILS on this row's own fixture, needing no injection point (rows 44, 47 and 48's discipline). On the live tree the two implementations are indistinguishable — re-derived read-only 2026-08-21 at `3095116`, `rootDir === "./src" && outDir === "./dist"` for **19 of 19**, exception set EMPTY — which is why this fixture is SYNTHETIC and why §3's 21-of-30 arithmetic, §8(f) and §8(l) are unaffected and the live tree stays 19-of-19 uniform                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

**Every `packages/cli` fixture that writes `dist/index.d.ts` must also write
`.dts-cache/index.d.ts` with the SAME bytes, or omit the cache entirely.** Otherwise the
baseline already carries `stale-shipped-declarations` and rows 3, 5, 7, 10, 11 and 12
stop being clean-to-stale flips — CF8-2's defect, which reached two rows nobody looked
at.

⚠️ **That clause binds the PAIR and never their LENGTH, and row 22's TWIN is the one
fixture that takes the BOUNDARY (round 34, `C34-1`)** — its `.dts-cache/index.d.ts` and
`dist/index.d.ts` are pinned byte-identical AND to ONE 28-byte length, and its rewrite is a
`tsc`-shaped barrel of that SAME length, because a battery in which every DIFFERING pair
also differs in SIZE cannot tell §3.5's rule `their bytes differ` from the same rule read
as `their sizes differ`. Every other cli-scoped fixture keeps its cache length a FREE
VARIABLE: this is one twin narrowed, not the clause changed, and it is recorded here so a
later round does not file the pair as one fixture with two mandates — row 19's twin's
`C19-2`/`CF17-3` shape, one fixture rule over.

Row 7 is the decisive one: the exact state in which the naively specified check
reports clean. The fixture asserts the plugin asset copy cannot mask a skipped compile.

⚠️ **Every cli-scoped row starts from a COMPLETE cli build, and from a marker WRITTEN
LAST** — rows 3, 5, 7, 8, 10, 11, 16, 17, 19, 20, 21, 22, 24, 25, 26, 32, 33, 34, 36, 37, 38, 39, 40, 41, 42, 43, 45, **row 4's round-23 TWIN** — named as a FIXTURE and never as a row, in 29's own precedent, because row 4's MAIN half builds no `packages/cli` at all and a rule naming the ROW would be false of the half it enumerates (round 23, `C23-1`; `C11-4`'s class inside `C11-4`'s remedy) — and 29's THREE cli fixtures (its `bundle-provenance-missing`,
`stale-shipped-declarations` and truncated-marker states — the third is row 11's own
complete build with the marker TRUNCATED rather than deleted or moved, so this rule binds
it and its baseline may not be short of a member; the count read `two` from round 18's
append (`OP18-5`) until round 22, `C22-1`): a chunk, the three entry
outputs, `dist/index.d.ts`, the six plugin entries, plus `.bundle-meta/metafile.json` with
an mtime at or after the newest member of §3's filtered output set, except where the row
itself deletes or moves it. ⚠️ **AND EVERY ROW ASSERTING A `full` `plugin-assets` ENTRY —
directly, or through an `all four` or `the other three` aggregate — ADDITIONALLY holds the
six `PLUGIN_ASSET_ENTRIES` SOURCES under `packages/plugin/`, five DIRECTORIES and the
`.mcp.json` FILE per §3.4's mixed-type rule, every one of them OLDER than the newest file
beneath `packages/cli/dist/plugin` (round 26, `C26-1`)** — row 8's own `plugin assets
fresh` shape copied WHOLE (standing rule 7) — a bound over the SIX ENTRIES and over NOTHING ELSE, which is what lets row 8's own fixture carry, from round 28 (`C28-1`), a NON-member path under `packages/plugin/` that is deliberately NEWER: the quantified set here is the entries and never the directory that contains them (§3.4's SCOPE half), so a fixture author copying this shape copies the bound on the six and not that seventh path — and NEVER the weaker "older than `bundleAt`"
round 25 rejected, so no baseline gains `stale-plugin-assets` and no SOLE-kind assertion
moves. Without those sources §3.4's SOURCE side yields nothing, §3.3's **SKIP RULE — ALL
FOUR cli-SCOPED COMPARISONS** declines, and `plugin-assets` reads `not-run` with NO
admissible spelling, §3.4's three ranked reasons all naming the DIST side — so every such
assertion FAILED a correct implementation (`OP16-3`'s class), among them row **38**'s THIRD
assertion, the SOLE element for §4's coverage-line suppression. ⚠️ **This clause is keyed
on what a row ASSERTS, in the `C15-2` sibling-fixture rule's own shape below, and
deliberately NOT on the cli-scoped row list above (standing rule 8):** row 8's fixture
"need not contain the file member at all" is row **24**'s whole stated reason for existing,
and row **19**'s twin had an `all four ... full` clause REJECTED in round 19 (`C19-2`)
because three entries are free variables there — a clause binding every cli-scoped fixture
would have falsified both in the commit that landed it. ⚠️ **And it is a FIXTURE
requirement that adds NOTHING to §3.3's clause-1 artifact set or to row 11's completeness
enumeration, both of which stay `packages/cli/dist`-scoped:** a missing source is not an
`unbuilt` trigger and no verdict in §3 moves. The six sit outside every unit's `src` and
`dist` and under no unit the fixture's root `tsconfig.json` references, so no unit, no stem
and no output-set member is introduced and §3.1 is untouched — row 41's own wording, copied
whole. ⚠️ **Row 41 states this same bound for its own fixture (round 25, `CF25-2`) and is
LEFT AS WRITTEN:** this clause GENERALISES it rather than replacing it, and the pair is
recorded here so a later round does not file it as one mandate in two places (`CF17-3`'s
shape). ⚠️ **The two false-negative-battery fixtures that assert `plugin-assets` `full` are
NOT rows and this clause cannot reach them — the `C10-4` conjunction and the §8(p) partial
copy, whose markers are ABSENT — so each carries a POINTER in its own bullet rather than
the mandate (`CF6-2`'s remedy shape).** ⚠️ **`at or after` is a LICENCE, and row 19's round-19 twin is
the one row that takes its BOUNDARY (round 19, `C19-2`)** — that twin sets the marker
EXACTLY EQUAL to the newest member of that set, because a rule every strictly-after fixture
satisfies cannot tell §3.3's clause 2 spelled `>=` from the same clause spelled `>`. Every
other cli-scoped row may keep writing the marker strictly last: this is one row narrowed,
not the rule changed, and it is recorded here so a later round does not file the pair as
one fixture with two mandates (`CF17-3`'s shape). ⚠️ **AND that marker's `outputs` must name EXACTLY the
chunk-pattern files the fixture wrote (round 12, C11-6).** Clause 1's chunk membership is
`metafile.outputs`-derived whenever a marker exists, so a fixture whose marker names a
chunk the tree does not hold is already `unbuilt` at baseline, the mutation flips nothing
and the row is **vacuous** — CF8-2's defect, at every cli-scoped row at once, which is why
this requirement is stated here rather than in the one row that introduced the rule. Otherwise the baseline is already `unbuilt` — through the
artifact set when a member is missing, through §3.3's clause 2 when the marker is merely
present and older — the mutation flips nothing and the row is **vacuous**. That was row
11's parenthetical, generalised in round 9 after CF9-2 found the requirement stated for one
row out of seven, and extended the same round by C9-1: once the marker became a freshness
oracle, writing it into the fixture stopped being enough. Rows 12, 23 and 46 are the deliberate
exceptions, and they are the only three: their CLEAN FIXTURE is itself the absent artifact —
`packages/cli/dist` absent for row 12, cache present with `dist/index.d.ts` absent for row
23, and for row **46** (round 17, `C17-1`) `packages/cli/dist` PRESENT and EMPTY with
`.bundle-meta/` and `.dts-cache/` surviving beside it, the one state in which `packages/cli`
is `skipped` while the marker is still present AND parseable. ⚠️ **Row 43 was listed here
and belongs in the conforming list above (round 17, `CF17-3`).** Its clean-fixture cell
reads `cli COMPLETE (as row 11)` and the absence of `packages/cli/dist` is its END STATE,
written by its own `rm -rf` mutation, which the `except where the row itself deletes or
moves it` clause above already covers. The two sites gave opposite mandates for one
fixture, and the site an implementer reads to BUILD it was the one licensing the omission —
`CF13-4`'s shape.

⚠️ **AND every fixture asserting `stale-bundle`, or a `full` `bundle-freshness` coverage
entry, must hold qualifying compiler output under the `dist` of EVERY unit its own marker
names as inlined (round 15, C15-2).** §3.2's absence rule makes that comparison `not-run`
when even one member is bare, so a fixture short of its own inlined set renders `not-run` at
baseline, the mutation flips nothing and the row is **vacuous** — CF8-2's defect at rows 5,
38 and 41 at once, and it would have been introduced by the fix that WROTE the absence rule,
which is standing rule 8 caught in consolidation rather than a round later. Row **40** is the
deliberate exception, its whole subject being its absent members — ONE on its original half and TWO on its round-18 twin (`CF18-3`), that twin being the only place in this battery where the comma-JOIN over offenders is exercised at all, since this rule is what forbids every other row from reaching a second one. ⚠️ **Row 45 CONFORMS to
this rule and is NOT a second exception (round 16, `C16-2`):** its marker names
`packages/A` alone and `packages/A/dist` holds qualifying output, while `packages/B` —
built, referenced by that fixture's `packages/cli/tsconfig.json`, and deliberately NOT
named by that marker — is outside the set this rule quantifies over, which is the whole of
what that row tests.

**False-negative battery** (the check must stay quiet):

- a unit with no `dist` → `skipped`: no finding, and **named** in the units-compared/skipped summary (§4's **SKIPPED UNITS ARE NAMED ON BOTH PATHS**) — "not reported" has been wrong since round 5, and beside a coverage line it would read as a mandate to stay silent. ⚠️ **This bullet was the ONLY assertion anywhere in §6 that a skipped unit is NAMED until round 21, and its fixture holds no findings, so it proves nothing about the findings path (round 13, OP13-1)** — battery row 35 is that path's row, rows 16-18 and 26 assert comparison coverage only, and §6 row 39's MAIN half is the `PASS` path's row, pinning that line by equality with the unit named and its `unitState()` reason beside it (round 21, `OP21-2`). This bullet asserts NAMING and never the line's SHAPE, which is why a `PASS` line printing `1 skipped` with the parenthetical dropped satisfied it. ⚠️ **And "stay quiet" now has ONE bounded exception (round 14, OP14-1):** where the skipped unit is `packages/cli`, at least one comparison is non-`full`, so §4's REMEDY PLAN prints — step 5 alone, no finding, exit unchanged. Quiet means no FINDING and no non-zero exit; it has never meant no next action, and battery row 39 is where that distinction is proved;
- `.tsbuildinfo` newer than every `.js` → clean — asserted **twice**: once for
  `checkUnitFreshness`, and once for `packages/cli` with `dist/.tsbuildinfo` touched
  forward PAST the marker, which must stay `compared` and must not become `unbuilt`
  (round 9). Measured on a composite fixture with the pinned `typescript@6.0.3`:
  `touch src/a.ts && tsc -b` moves `.tsbuildinfo` and no compiler output, so this is the
  only false positive clause 2 can produce and §3's exclusion is its entire mitigation;
- a `.info`/`.snap`/`.mjs` fixture under `src` newer than dist → clean;
- input mtime exactly equal to output mtime → clean — ⚠️ **`checkUnitFreshness`'s comparand
  ALONE, and the GENERIC words `input` and `output` are what let this bullet read as the
  boundary element for all five mtime comparisons (round 36, `C36-1`):** §3.3's clause-2
  mirror is §6 row 19's EQUAL-mtime twin (round 19, `C19-2`), and §3.2's, §3.3's cache and
  §3.4's own boundaries are rows 5, 6 and 24's EQUAL-MTIME TWINS — battery ROWS, and this is
  not one;
- `.dts-cache/index.d.ts` regenerated with IDENTICAL bytes (a `--force` run on unchanged
  sources), mtime newer than `dist/index.d.ts` → clean. §3.5 keys on bytes, so a copy
  that already holds the current declarations is not a finding — and this is the row
  that fails if an implementer adds an mtime disjunct;
- `packages/cli/dist` absent AND the marker absent → `skipped`, **not**
  `bundle-provenance-missing`: the kind fires only on a unit that is otherwise `compared`,
  so a fresh clone stays silent (round 1's `C-1` and round 4's `C-R4-2`, re-opened for the
  kind round 9 made reachable on every unrebuilt tree);
- the marker PRESENT but truncated to half its bytes on row 11's complete fixture — **whose
  marker mtime is at or after the newest §3-filtered output, as every cli-scoped fixture's
  is**, stated here because without that clause this row passes under BOTH readings of
  C14-1 and discriminates neither, which is why the contradiction survived to round 14; row
  **37** is the row that does discriminate, and §6 row 29's FIFTH fixture is this same tree
  again, there rendering §4's `unreadable` LINE, which nothing asserted until round 18
  (`OP18-5`) →
  exactly ONE advisory `bundle-provenance-missing`, reason
  `.bundle-meta/metafile.json unreadable`, coverage
  `bundle-freshness: not-run (.bundle-meta/metafile.json unreadable)` on `result.coverage`
  in row 12's own idiom, `exitCodeFor(result, { strict: true })` → **0**,
  and **no throw**: a `readMetafile` that lets `JSON.parse` escape reaches §4's
  internal-error row and exits **2** from `check:all` member 0, halting the other 14
  (round 10, C10-3). ⚠️ **The COVERAGE conjunct is round 22's (`C22-1`), and before it §4's
  verbatim reason list mandated that spelling on the COVERAGE side while nothing in §6
  asserted it there.** No row reaches this state on the coverage side: rows 10 and 21 and
  row 39's negative twin hold the marker ABSENT — as does row 39's CACHE-ONLY twin, whose
  fixture is that twin's, while row 39's ALL-NOT-RUN twin holds it present and parseable
  like row 16, both appended in round 35 (`OP35-1`) — row 16 holds it present and parseable so
  its entry takes rank 2's `packages/cli/dist absent`, row 37's mutation makes the unit
  `unbuilt` so §3.2's rank-1 suppression spells all four `packages/cli unbuilt`, and row
  29's FIFTH fixture is this same tree reading the rendered FINDING line alone. So a
  `checkBundleFreshness` keying the COVERAGE reason on its ARGUMENT — `metafile` being
  `undefined` gives `.bundle-meta/metafile.json absent` — while distinguishing the two
  states for the FINDING, which row 29's fifth fixture forces, passed every element in this
  design and printed both spellings for one state on one page, telling the operator a file
  is absent while it sits on disk: `OP18-5`'s own diagnosis at the sibling surface, and §4's
  own "A battery row asserting any other spelling fails an implementation that follows the
  sections" with no row to enforce it. That implementation fails on THIS bullet's own
  fixture, so the conjunct needs no mutant swap. ⚠️ **Standing rule 8, at the site of the
  fix: the conjunct vacates nothing** — row 29's fifth fixture keeps the rendered
  `unreadable` line and its `absent`-spelling mutant, row 37 keeps its `unbuilt` verdict,
  and this bullet's four existing conjuncts are unchanged;
- cli `dist` present WITH a gap in §3.3's artifact set and the marker absent → `unbuilt`
  alone, never both kinds — the disjointness §3.3's cli table states, asserted rather than
  assumed. ⚠️ **The GAP is NAMED rather than left free (round 32, `C32-1`), which makes
  this bullet the element for clause 1's chunk member on the no-metafile branch:** EVERY
  chunk-pattern file absent, with every other member of that set present — the three entry
  outputs, `dist/index.d.ts` and the six plugin entries under `dist/plugin`. The verdict
  and this bullet's subject are unchanged: `unbuilt` alone, never both kinds, its gaps
  naming the chunk, reached through §3.3's cli reason table's `bundle:cli did not finish`
  row and not through its `--clean` signature row, whose own precondition is the inverse —
  the tsc-written members ABSENT while a chunk SURVIVES. A `cliArtifactGaps` whose chunk
  requirement is the metafile's outputs when there is a metafile and NOTHING when there is
  not reports `compared` plus one advisory on this fixture and fails it, needing no mutant
  swap, while an implementation emitting BOTH kinds still fails the disjointness half — so
  nothing this bullet already proved is vacated;
- **the C10-4 conjunction, pinned rather than left as prose (round 10):** cli `dist`
  COMPLETE — and holding the six `PLUGIN_ASSET_ENTRIES` SOURCES under `packages/plugin/`,
  OLDER than the newest file beneath `dist/plugin`, the complete-cli-build rule's
  plugin-SOURCE clause CITED rather than restated (`CF6-2`'s remedy shape), that clause
  being keyed on rows and this being a bullet; without them §3.4's source side yields
  nothing, the comparison declines and the `full` below FAILS a correct implementation
  (round 26, `C26-1`) — marker ABSENT, `packages/cli/.dts-cache` ABSENT, and `dist/bin.js`
  rewritten as
  `tsc`-style per-file output importing `@crabgic/*` → the design's verdict is **PASS, exit
  0 even under `--strict`**, with exactly one advisory `bundle-provenance-missing` and
  THREE `not-run` coverage entries (`bundle-freshness`, `declaration-cache`,
  `shipped-declarations`; `plugin-assets` runs `full`). Clause 2 does not evaluate — no
  marker — and §3.5 declines — no cache — so nothing left can see it. The row asserts that
  verdict rather than a better one, because §8(i) states it as an accepted residual; what
  the row forbids is the verdict changing by accident in a later round.

- **the §8(p) partial-copy state, pinned rather than left as prose (round 15, C15-1):** cli
  `dist` COMPLETE by clause 1's own test — a chunk, the three entry outputs,
  `dist/index.d.ts` and all six `PLUGIN_ASSET_ENTRIES` present under `dist/plugin` — with
  `dist/plugin/skills/` EMPTIED of its contents while the directory itself stays, its mtime
  at copy time, the six `PLUGIN_ASSET_ENTRIES` SOURCES present under `packages/plugin/` and
  unchanged — the complete-cli-build rule's plugin-SOURCE clause CITED rather than
  restated, that clause being keyed on rows and this being a bullet, and without which
  §3.4's source side yields nothing, the comparison declines and the `full` below FAILS a
  correct implementation (round 26, `C26-1`) — and the marker ABSENT → the design's verdict is
  **`compared` plus exactly one advisory `bundle-provenance-missing`, PASS at exit 0 even
  under `--strict`**, with `plugin-assets` reporting `full`. Clause 1 sees six present paths,
  §3.4's dist side is `now`, clause 2 does not evaluate. The row asserts that verdict rather
  than a better one, because §8(p) states it as an accepted residual; what it forbids is the
  verdict changing by accident in a later round. Row 20's mutation removes the TOP-LEVEL
  directory and so discriminates nothing here.

**Wiring assertions.** The source reads match
`check-support-window-freshness.test.mjs:543-544` and
`check-marketplace-pin-digest.test.mjs:353-358`; the source-ORDER comparison matches
`scripts/run-e2e-suites.test.mjs:31`, which is `indexOf(a) > indexOf(b)` with both
searches from 0 — the only positional precedent in `scripts/`, and the shape round 8
missed. ⚠️ **SIX of the bundler predicate's EIGHT order operands use `search()` rather than
`indexOf` — two since round 20 (`CF20-1`), four more since round 22 (`CF22-1`) added the two
`mkdir` conjuncts**, their anchors being regexes; the two `indexOf` operands are the LITERAL
anchors `BUILD` and `COPY`. `search()` is `indexOf` for a
pattern and is the same from-0 comparison, so the cited precedent still governs and the
change is in the ARGUMENT type, never in the comparison. The `package.json` reads deliberately do **NOT** copy those two files' `:530` and
`:339`: both are `expect(root.scripts["check:all"]).toContain("check:…")`, membership
only. That is all those two checks need; here it is exactly the assertion a member
appended LAST with no flag passes, while §5 orders this member FIRST and chains it with
`--strict` (CF9-3). So the chain is asserted BY INDEX:
`const members = root.scripts["check:all"].split("&&").map((s) => s.trim())
expect(members[0]).toBe("npm run check:stale-dist -- --strict") // position AND flag
expect(members.filter((m) => m.includes("check:stale-dist"))).toHaveLength(1)
// no member COUNT is asserted, so a 16th member must not break this, and no ci.yml
// assertion: unlike the two files above, §5 proposes no CI step, so copying their
// second it() would pin a wiring this design refuses.
expect(root.scripts["check:stale-dist"]).toBe("node scripts/check-stale-dist.mjs")
expect(root.scripts.pretest).toBe("node scripts/check-stale-dist.mjs")
// That equality pins the SCRIPT TEXT and nothing about what the process does with it
// (round 19, CF19-1). The flagless invocation is load-bearing only if the CLI threads the
// flag it parsed into exitCodeFor (see section 1), and until round 19 no element here or
// in the battery ran the process on a tree carrying a non-advisory finding: row 27's
// fixture is a malformed config, every exit code row 31 asserts is its load-time mutant's,
// and row 31's clean baseline has zero findings, where 0 is correct either way. Row 43's
// CLI twin is what makes this line's value falsifiable; without it a CLI hardcoding
// strict true passes every assertion in this section and blocks npm test for every
// developer from the first run.
// No `pretest:watch`/`pretest:live`/`pretest:e2e` assertion, and that is a stated
// decision rather than an omission: npm's pre-hook is NAME-EXACT, so the four sibling
// test scripts §5.1 names bypass this wiring, and §5.1 states that as the measured bound
// rather than closing it (round 12, OP12-2).
// If a later round takes the reach, they are pinned HERE
// by equality in this same shape — never by membership, CF9-3's rule.

// The EXISTING-file edits, asserted by READING them — FIVE SOURCE PROPERTIES of
// `scripts/bundle-cli.mjs` (§1), out of the SIX edits §1 makes to that file: only the
// `writeFile` import is implied by the WRITE anchor below and NOT separately asserted
// (round 12, CF12-4). ⚠️ The `mkdir` of `.bundle-meta/` IS asserted, since round 22
// (CF22-1): round 21 withheld a property from it because a mutant "could only restate the
// absence it asserts against" — true of an EXISTENCE assertion, false of an ORDER one,
// whose mutant is a HOIST. A `mkdir` above the wipe-step clear is deleted by it and
// `writeFile` throws the same ENOENT with all six edits PRESENT, a case §8(k)'s round-21
// form left named in no section. Plus one in `.gitignore`. Without these an
// implementer builds the six new files, passes every battery row and the line
// above, and leaves requirement 3's comparison permanently muted.
const bundler = readFileSync("scripts/bundle-cli.mjs", "utf8")

// ALL THREE edits name `.bundle-meta`, so they are separated by CALLEE plus one distinct
// literal, never by the path alone. Anchoring on the bare path finds the wipe-step CLEAR
// and passes with no write in the file at all — measured, round 9 / CF9-4.
// ⚠️ REGEXES, not contiguous substrings, since round 20 (CF20-1). The conforming one-line
// write at this file's 2-space indent —
// await writeFile(join(CLI_ROOT, ".bundle-meta", "metafile.json"), JSON.stringify(result.metafile));
// — is exactly 100 characters; `.prettierrc.json` sets printWidth 100, `.prettierignore`
// does not cover `scripts/`, and `npm run format` is `prettier --check .`. So the anchored
// line sits AT the width with zero margin: one further argument and the formatter wraps the
// call across four lines, on which a substring anchor is FALSE while all of §1's edits are
// landed and correctly ordered — an assertion that fails a correct implementation. Measured
// 2026-08-21 at 9c247eb with this repository's prettier 3.9.5, in both directions. A negated
// class spans newlines, so both halves match the wrapped form AND the `const metaDir =
// join(CLI_ROOT, ".bundle-meta")` factoring an implementer needs anyway, `writeFile` being
// unable to create the directory the clear removed. The CLEAR line has 23 columns of margin
// at 77 characters, re-derived rather than assumed, so the exposure was the WRITE half plus
// any later edit to the clear. The quantifier is a plus and never a star: CF15-1's rule for
// this span, where a star pair is emphasis to a markdown formatter.
const CLEAR = /rm\([^;]+"\.bundle-meta"/
// MKDIR is round 22's (CF22-1). `:176`'s own `await mkdir(pluginOut, { recursive: true });`
// is NOT a match — it names no `.bundle-meta`, and `[^;]+` cannot cross that statement's own
// semicolon under `semi: true`. §1 mandates the literal INSIDE this call for the reason it
// mandates it inside `rm(`: on the `const metaDir` factoring the WRITE anchor survives, a
// path-free `mkdir(metaDir, …)` would not, and the anchor would then fail a correct
// implementation — OP16-3's class, disqualified before the conjunct was written.
const MKDIR = /mkdir\([^;]+"\.bundle-meta"/
const WRITE = /writeFile\([^;]+"metafile\.json"/
const COPY = "for (const entry of PLUGIN_ASSET_ENTRIES)" // the loop at :177, NOT the const at :89
const EXPORTED = "export const PLUGIN_ASSET_ENTRIES = [" // §1's `export` edit — NAMED, never numbered (round 21, C21-2, which inserted the `mkdir` above it); `:89` is a bare `const` today
const GUARD = "statSync(process.argv[1]).ino === statSync(fileURLToPath(import.meta.url)).ino"
const BARE_MAIN = "\nawait main();" // the file's LAST statement today, re-derived 2026-08-20
const BUILD = "await build({"
// COPY and BUILD each occur exactly once in `scripts/bundle-cli.mjs` today (re-derived
// 2026-08-21 at 9c247eb, `grep -cF`, both 1). CLEAR, MKDIR and WRITE MATCH ZERO times —
// re-derived read-only 2026-08-21 at 5fef126: `grep -n mkdir scripts/bundle-cli.mjs` returns
// `:55` and `:176` only, neither naming `.bundle-meta` — they ARE
// the edits §1 prescribes, and each must be introduced exactly once by them; `grep -c
// bundle-meta scripts/bundle-cli.mjs` returns 0 today. The mutants below are what prove the
// assertion sees their absence. "All four are unique in the file today" was an anchor
// claim asserted rather than measured, and false for half of them (round 10).

// ONE predicate, so the mutants below exercise the code the real tree exercises.
// `search()` is `indexOf` for a pattern — the same from-0 comparison, so the
// `run-e2e-suites.test.mjs:31` precedent cited above still governs (round 20, CF20-1).
const placedCorrectly = (src) =>
CLEAR.test(src) && MKDIR.test(src) && WRITE.test(src) &&
src.search(CLEAR) < src.indexOf(BUILD) && // cleared before the build — C8-2
src.search(CLEAR) < src.search(MKDIR) && // dir MADE after the clear — round 22, CF22-1
src.search(MKDIR) < src.search(WRITE) && // …and before the write — round 22, CF22-1
src.indexOf(COPY) < src.search(WRITE) // written after the copy — round 7
expect(placedCorrectly(bundler)).toBe(true)

// NON-VACUITY, in the battery's own shape: one mutation each, every one must FLIP.
// STATEMENT-level and no longer LINE-level (round 20, CF20-1): a wrapped call has no single
// line holding the anchor, so `lineWith` returned undefined and the BEFORE-the-copy hoist
// mutant raised `TypeError: Cannot read properties of undefined (reading 'trim')` instead
// of failing readably — measured on the prettier-wrapped and the `metaDir`-factored sources
// alike. Named by its DESTINATION and not as "the third", which round 22 (CF22-1) made point
// at a different mutant by inserting three above it, and not by what it MOVES, which the
// same round widened from the write alone to the PAIR (C11-4: never cite text a same-round
// edit moves).
// Re-derived 2026-08-21 against THREE conforming sources — the one-line, the
// prettier-wrapped and the `metaDir`-factored: the predicate is true on all three
// unmutated, and the THREE mutants that existed then flip it on all three. ⚠️ **That
// measurement covers NONE of round 22's work (CF22-1): the three `mkdir` mutants below are
// new and the BEFORE-the-copy hoist is WIDENED, so FIVE of the SEVEN mutants are unmeasured
// by it — round 22's four and round 27's clear-sink (CF27-1).** Round 22's four are
// re-derived at its apply and round 27's fifth at ITS apply, against those same three
// conforming sources and reported with the sha they were run at (CF16-2).
// ⚠️ **THOSE THREE ARE A SUBSET OF THE CONFORMING SET, AND NAMING THEM AS A LIST IS HOW
// ROUND 28 (CF28-1) GOT IN.** They vary the WRITE's FORMATTING and nothing else, so all three
// carry §1's prescribed `mkdir`-after-the-copy-loop placement — while a source can CONFORM,
// landing all six of §1's edits and building correctly, at any `mkdir` position this
// predicate's own two order conjuncts admit, anywhere between the clear and the write, of
// which §8(k) licenses one OTHER in terms. So the rule, quantified rather than listed: EVERY
// re-derivation here runs over the conforming set as §1 and §8(k) then define it, and a round
// measuring a proper subset names the subset AND what the remainder is left unmeasured for.
// Round 28 re-derived all SEVEN mutants at `e70c9bb` on THREE `mkdir` positions — after the
// copy loop, between `build()` and the copy loop, and in the wipe step beside the clear — and
// the per-position result is the mutant-coverage note below. The predicate is false both
// on a bundler
// carrying round 8's clear and NO write at all, which is CF9-4's control, and on the
// unedited bundler. This file carries semicolons (`.prettierrc.json` sets `semi: true`),
// which is what makes the splitter well-defined here.
const stmtWith = (re) => bundler.split(";").find((s) => re.test(s))
expect(placedCorrectly(bundler.replace(stmtWith(WRITE) + ";", ""))).toBe(false)
expect(placedCorrectly(bundler.replace(stmtWith(CLEAR) + ";", ""))).toBe(false)
expect(placedCorrectly(bundler.replace(stmtWith(MKDIR) + ";", ""))).toBe(false)
expect(placedCorrectly( // clear SUNK BELOW build(), out of the wipe step — C8-2
bundler.replace(stmtWith(CLEAR) + ";", "")
.replace(COPY, stmtWith(CLEAR).trim() + ";\n " + COPY),
)).toBe(false)
expect(placedCorrectly( // mkdir HOISTED above the clear — the ENOENT case, CF22-1
bundler.replace(stmtWith(MKDIR) + ";", "")
.replace(stmtWith(CLEAR) + ";", stmtWith(MKDIR) + ";" + stmtWith(CLEAR) + ";"),
)).toBe(false)
expect(placedCorrectly( // mkdir SUNK below the write — CF22-1
bundler.replace(stmtWith(MKDIR) + ";", "")
.replace(stmtWith(WRITE) + ";", stmtWith(WRITE) + ";" + stmtWith(MKDIR) + ";"),
)).toBe(false)
expect(placedCorrectly( // the PAIR hoisted BEFORE the copy
bundler.replace(stmtWith(MKDIR) + ";", "").replace(stmtWith(WRITE) + ";", "")
.replace(COPY, stmtWith(MKDIR).trim() + ";\n " + stmtWith(WRITE).trim() + ";\n " + COPY),
)).toBe(false)
// ⚠️ THE LAST MUTANT HOISTS THE PAIR, not the write alone, and that is standing rule 8
// checked AT the fix (round 22, CF22-1). With `search(MKDIR) < search(WRITE)` added, hoisting
// the write ALONE flips that conjunct as well as round 7's `indexOf(COPY) < src.search(WRITE)`,
// which would leave round 7's conjunct DELETABLE with every mutant still flipping — the
// vacuity this section exists to refuse. ⚠️ **That is measured on §1's prescribed `mkdir`
// placement and is FALSE on the conforming ones where the `mkdir` precedes the copy loop
// (round 28, CF28-1):** there the write-alone hoist leaves `search(MKDIR) < search(WRITE)` TRUE
// and flips round 7's conjunct ALONE, so hoisting the PAIR is what makes a SINGLE mutant serve EVERY
// conforming placement rather than what makes it non-vacuous on any one — a stronger reason
// than this comment gave, and the reason it keeps. Moving the pair is ONE mutation, §1
// prescribing the two as one unit, and only round 7's conjunct flips under it — on all three
// `mkdir` positions measured at `e70c9bb`.
// ⚠️ THE STANDARD IS A SET, STATED ONCE: EVERY conjunct of `placedCorrectly` — ALL SEVEN —
// must have a mutant above that FLIPS it, so appending a conjunct obliges a mutant and
// deleting a mutant obliges re-deriving that none is left unflipped (round 27, CF27-1). This
// note paired conjunct to mutant for SIX of the SEVEN and omitted `search(CLEAR) <
// indexOf(BUILD)`, which no mutant then standing flipped — CF14-4's never-restate-a-SUBSET
// inside the mutant-coverage argument itself. Round 27's clear-sink discharges it and flips
// it ALONE on a source whose `mkdir` FOLLOWS the plugin-copy loop — §1's prescribed placement,
// and the placement all three conforming sources named above carry, they varying the WRITE's
// FORMATTING alone.
// ⚠️ **NOT ON EVERY CONFORMING SOURCE, AND THE BOUNDARY IS THE SINK DESTINATION (round 28,
// CF28-1).** Where the `mkdir` sits BEFORE the copy loop — in the wipe step beside the clear,
// which §8(k) licenses in terms, or anywhere between `build()` and the loop — the sunk clear
// lands after that `mkdir` too, so it flips `search(CLEAR) < search(MKDIR)` as well and
// `search(CLEAR) < indexOf(BUILD)` is DELETABLE there with all SEVEN mutants still flipping:
// this note's own definition of vacuous, at the conjunct round 27 filed it for. No relocation
// of the clear closes it — an isolating destination must fall between `build()` and a `mkdir`
// above `build()`, and `build()` and the copy loop are the only statements no §1 edit moves,
// which is why round 27 anchored the sink on the loop. So the conjunct is ASSERTED on every
// conforming source and still fails a bundler whose clear sits after `build()` — the PIN is
// placement-independent — while its NON-VACUITY is proved on §1's prescribed placement only.
// ⚠️ A re-derivation run on any other conforming placement must NOT read its own probe as
// licence to DROP the conjunct: moving the `mkdir` back down to §1's position afterwards then
// leaves C8-2 — a clear sitting after `build()` — passing all seven, the marker naming some
// OTHER run's inlined unit set. Re-derived read-only 2026-08-22 at `e70c9bb`, each placement
// built as a scratchpad copy with `scripts/bundle-cli.mjs` read as text and never imported,
// working tree clean before and after. The probe is this note's own deletability question,
// asked per conjunct and never a call to `placedCorrectly`, which takes ONE argument: DELETE
// the conjunct from the predicate, then ask whether all SEVEN mutants still fail it. The
// answer is NO for `search(CLEAR) < indexOf(BUILD)` on the after-the-loop placement and YES on
// the two earlier ones; it is YES for `MKDIR.test` and `WRITE.test` on all three, which the
// two sentences below already state and justify.
// The delete-CLEAR mutant is NOT replaced by round 27's: with the clear absent
// `search(CLEAR)` is -1, which SATISFIES both order conjuncts, so deletion is the only
// mutation that flips `CLEAR.test`. The two `mkdir` order
// conjuncts each have a mutant that flips them ALONE on every placement measured: the hoist
// leaves `search(MKDIR) < search(WRITE)` true, the sink leaves `search(CLEAR) < search(MKDIR)`
// true. `MKDIR.test` is redundant with the hoist conjunct's own -1 and is kept for the shape
// `WRITE.test` already carries, whose deletion mutant flips `MKDIR<WRITE` and `COPY<WRITE`
// with it. ⚠️ **`CLEAR.test` does NOT carry that shape and never did (round 28, CF28-1):** its
// deletion mutant flips it ALONE on every placement measured — which is this note's own -1
// derivation, two sentences up — so it is kept because it is the ONLY element that sees a
// deleted clear at all, never for symmetry.

// The two IMPORTABILITY edits (round 10, C10-2), in the same one-predicate,
// mutant-proved shape. Without them §3.3's clause 1 and §3.4's source side name a const
// no importer can obtain, and the only import that obtains one runs `main()` and
// rebuilds `packages/cli/dist` from inside the check that observes it.
const importable = (src) =>
src.includes(EXPORTED) && src.includes(GUARD) && !src.includes(BARE_MAIN)
expect(importable(bundler)).toBe(true)
// each mutant must FLIP the predicate — §6's battery discipline, applied here
expect(importable(bundler.replace(EXPORTED, "const PLUGIN_ASSET_ENTRIES = ["))).toBe(false)
expect(importable(bundler.replace(GUARD, "true"))).toBe(false)
expect(importable(bundler + BARE_MAIN)).toBe(false) // guard added, bare `await main()` left behind

// REQUIREMENT 5 AND REQUIREMENT 11, READ FROM THE SOURCE (round 14, CF14-2), in the
// mutant-proved shape above. `enumerateWorkspaces` (`repo-census.mjs:270`) sits beside
// `enumerateRootReferences` (`:286`) and returns a STRICT SUBSET — 18 dirs, the 19 minus
// `e2e/report`, re-derived 2026-08-21 — so a `units.mjs` importing it satisfies
// requirement 11's letter, passes every synthetic battery row, and silently drops
// `e2e/report`, whose `dist` holds 41 files emitted from 10 `.ts` sources today. The live
// smoke test's equality assertion catches the SET; this catches the CALL, which is what
// requirement 11 actually is. Until round 14 neither existed.
//
// SCOPED TO THE IMPORT STATEMENT, never to the whole file, and that is not cosmetic: this
// design documents every rejected alternative at the site that rejects it, so a conforming
// `units.mjs` is likely to NAME `enumerateWorkspaces` in a comment explaining why it is not
// called — and a whole-file negated `includes` would then FAIL a correct implementation.
// A search wider than its claim, at the assertion whose whole subject is scope.
// ⚠️ **AND THE SCOPE IS THE `^` ANCHOR, ABSENT FOR FIFTEEN ROUNDS (round 29, CF29-2).**
// Unanchored, `[^;]+` spans newlines, so the match begins at the EARLIEST `import`
// SUBSTRING in the semicolon-free span that reaches the target `from` — which reaches into
// a preceding comment ONLY when that comment itself contains the word `import`, and the
// predicate then goes false ONLY when the same comment also names a forbidden identifier.
// ⚠️ **BOTH CONDITIONS, because the manager re-derived this rather than accepting it
// (standing rule 2, round 29):** the round-29 finding said "any comment", and measured
// 2026-08-22 at `c672565` with node v24.18.0 a comment naming `enumerateWorkspaces` and NOT
// containing `import` leaves the unanchored predicate TRUE — the leftmost match still
// begins at the real statement, the previous import's `;` bounding the span. With both —
// `// do not import enumerateWorkspaces here — requirement 5` — the unanchored predicate is
// FALSE and the anchored one TRUE. Narrow, and a CORRECT implementation writing that one
// comment is refused, which is the failure this paragraph was written to avoid. ⚠️ **The MEASUREMENT and its SCOPE, stated together:**
// re-derived read-only 2026-08-22 at c672565, node v24.18.0, working tree clean before and
// after and `scripts/*` read as text and never imported, over SIX conforming sources
// spanning the property that discriminates them — whether a comment ahead of the statement
// carries the substring `import`, and whether it does so AHEAD of the name. The unanchored
// form returns FALSE on the three that carry `import` ahead of `enumerateWorkspaces` (a
// one-line comment above the statement; the same with `import { join } from "node:path";`
// before it, whose `;` does NOT protect it because the comment's own `import` starts a
// fresh span; and a JSDoc block of that shape) and TRUE on the bare statement and on the
// two comment forms that do not. The anchored form returns TRUE on ALL SIX. The claim is
// therefore quantified over every conforming `units.mjs` and PROVED on those six, which is
// wider than the ONE bare statement round 15's own re-derivation had (§9, CF15-1) and is
// the gap that let this through. `semi: true` (`.prettierrc.json`) bounds the span at the
// previous `;` and prettier never indents a top-level ESM import, so `^` cannot fail a
// formatted source; `m` introduces no `*` and so cannot re-open CF15-1's emphasis mangling
// — MEASURED, not reasoned: `prettier --check` 3.9.5 passes on the anchored file and
// `prettier --write` leaves both regex lines byte-identical.
const units = readFileSync("scripts/stale-dist/units.mjs", "utf8")
const censusImport = (src) =>
(src.match(/^import[^;]+from\s+["'][^"']*repo-census\.mjs["']/m) ?? [""])[0]
const enumeratesReferences = (src) =>
censusImport(src).includes("enumerateRootReferences") &&
censusImport(src).includes("enumerateTsconfigs") &&
!censusImport(src).includes("enumerateWorkspaces")
expect(enumeratesReferences(units)).toBe(true)
// BOTH names, and that is round 15's fix (CF15-2): the predicate pinned
// `enumerateRootReferences` alone, while requirement 11 is discharged by TWO calls. A
// `units.mjs` importing that one name and hand-rolling a `readdirSync` config walk beside
// it returned true — re-derived — and it also passes the live smoke test below (the unit
// SET is unchanged) and every synthetic row, because §3 input rule 4 has no member on
// today's tree and row 3's `stale-declarations` comes from §3.3's named comparand rather
// than from `enumerateTsconfigs`. So no fixture could tell the two implementations apart,
// at the call that feeds §3's rule 4 and §0's requirement 9 arithmetic.
//
// THREE mutants, ONE PER CONJUNCT, and each must FLIP the predicate — this section's own
// battery discipline, in the per-conjunct form CF27-1 states for `placedCorrectly` (round
// 29, CF29-3: this was TWO, one per NAME, and the NEGATED conjunct — requirement 5's whole
// prohibition — was DELETABLE with both of them still flipping, this section's own
// definition of vacuous). The first replace is GLOBAL on purpose: a single-occurrence
// replace would leave a second `enumerateRootReferences` behind and the predicate would not
// flip. The second RENAMES rather than deletes, and its replacement shares no substring
// with any of the three names, so it flips on the missing name and never on the negated
// one. Neither can isolate the negated conjunct: the first REMOVES `enumerateRootReferences`
// in the same edit and so flips positive conjunct 1 with it, and the second never mentions
// the forbidden name — re-derived read-only 2026-08-22 at c672565 with
// `!censusImport(src).includes("enumerateWorkspaces")` DELETED from the predicate, both
// still return false. The THIRD keeps BOTH mandated names present and adds the forbidden
// one, so it flips the negated conjunct ALONE; it is applied THROUGH `censusImport` rather
// than to the whole file — the shape `walk.replace(bundlerImport(walk), LITERAL)` below
// already uses (standing rule 7) — because a bare `replace` lands on the FIRST occurrence,
// which in a `units.mjs` whose comment names `enumerateTsconfigs` above the import is
// OUTSIDE the statement, leaving the predicate true and FAILING A CORRECT IMPLEMENTATION
// (measured at c672565: the bare form returns true on exactly that source, the scoped form
// false on every conforming shape measured and true on none).
expect(enumeratesReferences(units.replace(/enumerateRootReferences/g, "enumerateWorkspaces"))).toBe(false)
expect(enumeratesReferences(units.replace(/enumerateTsconfigs/g, "listConfigs"))).toBe(false)
const withWorkspaces = censusImport(units).replace("enumerateTsconfigs", "enumerateTsconfigs, enumerateWorkspaces")
expect(enumeratesReferences(units.replace(censusImport(units), withWorkspaces))).toBe(false)

// THE DESIGN'S OTHER REPO-INTERNAL IMPORT (round 15, CF15-4), in the one-predicate,
// mutant-proved shape above. §3.3's clause 1 mandates that `PLUGIN_ASSET_ENTRIES` be read
// from the `export`ed const, "never a literal duplicated into `walk.mjs`", and until this
// round NOTHING in this section read that file: a `walk.mjs` carrying its own six-element
// literal passed every battery row (their fixtures are synthetic trees), the
// false-negative battery, the five `bundle-cli.mjs` source properties above — they read the
// BUNDLER, which still exports the const — the `package.json` and `.gitignore` reads and
// the live smoke test. §3.3 measures the resulting drift as one commit away:
// `packages/plugin/workflows/` exists in the source tree today and is not a member, after
// which a seventh asset ships in `dist/plugin` with its absence not a clause-1 gap and its
// edits never firing `stale-plugin-assets`. SCOPED TO THE IMPORT STATEMENT for the reason
// the census read above is — and here the unanchored form failed the OTHER way (round 29,
// CF29-2): it ADMITTED the vacuity this predicate exists to catch. Re-derived read-only
// 2026-08-22 at c672565, node v24.18.0, scratchpad strings importing nothing from this
// repository: a `walk.mjs` whose comment reads `// We import the dep set here, never
// PLUGIN_ASSET_ENTRIES` — the substring `import` AHEAD of the const's name, with no `;` in
// the comment to bound the span — above `import { EXTERNAL_DEPENDENCIES } from
// "../bundle-cli.mjs";` and the six-element literal declared below returned TRUE. Clause 1's
// duplicated literal ships, a seventh asset never fires `stale-plugin-assets`, and every
// element in this section stays green: CF15-4's own diagnosis, re-opened by CF15-4's own
// fix. The anchored form returns FALSE on it and TRUE on the conforming import, with the
// LITERAL mutant still flipping. Two comment shapes that do NOT carry `import` ahead of the
// name returned FALSE even unanchored, which is why the property and not the mere presence
// of a comment is what is stated here. So the two halves of this one regex bug point
// opposite ways — a correct `units.mjs` REJECTED, a forbidden `walk.mjs` ACCEPTED — which
// is why the anchor is stated at both.
const walk = readFileSync("scripts/stale-dist/walk.mjs", "utf8")
const bundlerImport = (src) =>
(src.match(/^import[^;]+from\s+["'][^"']*bundle-cli\.mjs["']/m) ?? [""])[0]
const importsAssetEntries = (src) => bundlerImport(src).includes("PLUGIN_ASSET_ENTRIES")
expect(importsAssetEntries(walk)).toBe(true)
// the mutant is verbatim the implementation clause 1 forbids — the import statement
// replaced by a duplicated literal — and it must FLIP the predicate.
const LITERAL = 'const PLUGIN_ASSET_ENTRIES = ["agents", "hooks", "skills", "statusline", ".mcp.json", ".claude-plugin"]'
expect(importsAssetEntries(walk.replace(bundlerImport(walk), LITERAL))).toBe(false)

expect(readFileSync(".gitignore", "utf8")).toContain("packages/cli/.bundle-meta/")`.

**One live smoke test** runs `checkStaleDist(REPO_ROOT)` and asserts only that it
returns, that the set of reported units **EQUALS** `enumerateRootReferences()` — both sides derived from the live tree, so no literal is pinned and it still cannot rot as the repository grows (round 14, CF14-2: this was MEMBERSHIP for thirteen rounds, and `enumerateWorkspaces` — exported at `repo-census.mjs:270`, beside `enumerateRootReferences` at `:286` — returns a STRICT SUBSET, re-derived 2026-08-21 as 18 dirs, the 19 minus `e2e/report`, so requirement 5's whole point went unasserted and an implementation taking the forbidden call never walks `e2e/report`, whose `dist` holds **41** files emitted by `tsc -b` from **10** `.ts` sources today), and that
every `coverage` entry names one of the four cli-scoped comparisons and carries a
non-empty `reason` whenever its status is not `full` — no count and no named entry, so
it cannot rot as the repository grows or as the marker starts being written. Run against the tree at design time
the algorithm above returns four `stale-unit` findings (`packages/contracts`,
`packages/engine-claude`, `packages/gates`, `packages/plugin`): a true report of this
working copy, and independent evidence the check is not vacuous against real inputs.

⚠️ **Plus one advisory `bundle-provenance-missing packages/cli` today — permanently, and
by design (round 9).** `packages/cli/.bundle-meta/` does not exist until `bundle-cli.mjs`
carries the change §1 specifies — measured 2026-08-20, `ls -d packages/cli/.bundle-meta`
returns no such file on a tree whose `dist` holds all three entry outputs, `index.d.ts`,
the six plugin entries and five chunks. So the first `npm test` after this lands prints one
advisory line for **every developer on a correctly built tree**, exit 0 even under
`--strict`, and one `npm run build` clears it.

Round 8 wrote that as a MIGRATION — `bundle-provenance-missing` "until the first build
after adoption". **The check holds no state, so "after adoption" is not a predicate it can
evaluate**: that tree and a build interrupted between `:178` and `:181` are byte-identical
on disk (C9-3). And §4, which is authoritative for output, carried no migration wording at
all, so an implementer following row 10 would have printed `unbuilt packages/cli` with the
remedy `rm -rf packages/cli/dist` to everyone on day one (O9-2). The rule is therefore
permanent and unconditional: an absent marker is `bundle-provenance-missing`, advisory,
never `unbuilt`; `unbuilt` comes from C9-1's freshness rule. The residual it leaves is
stated in §8(i).

**And the coverage line prints with it.** Measured on this working copy 2026-08-20:
`ls -d packages/cli/.bundle-meta` → no such file, while the five hashed chunks,
`.dts-cache/index.d.ts` and `dist/plugin` all exist. So today's live run renders exactly
one non-`full` entry —
`bundle freshness not run (.bundle-meta/metafile.json absent)`
— and requirement 3's comparison is the one thing this tree cannot cover. ⚠️ **The trailing
`— first build after adoption writes it` was deleted in round 10.** C9-3 dropped the
temporal qualifier from the RULE — thirteen lines above, this section already says "the
check holds no state, so 'after adoption' is not a predicate it can evaluate" — and §3.2
mandates this entry's reason verbatim as `.bundle-meta/metafile.json absent`, so a reason
carrying the migration wording is one the check can no longer produce. Recorded as a
measurement, not asserted by the test above, because it stops being true after the first
build.

## 7. False-positive risks, each with its mitigation

| risk                                                                                                                                                                          | mitigation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Touched-but-unchanged source (format-on-save, `git checkout` rewriting a file)                                                                                                | None available: a content oracle needs a persisted baseline, which is a twentieth build artifact with the same staleness problem (assumption 2). ⚠️ **The remedy is §4's `stale-unit` row — `rm -rf <unit>/dist` THEN `npm run build` — and this cell read "one `npm run build`" for thirteen rounds (round 14, CF14-1).** §4's table is the only remedy source, the rule §3.4 and §3.5 already carry and this row never was; §4 also names this row by number while measuring the opposite. The measurement is §4's own, published there and re-derived by earlier rounds: on a composite fixture with the pinned `typescript@6.0.3`, `touch src/a.ts && tsc -b` leaves every compiler output unmoved and moves only `dist/.tsbuildinfo`, which §3's output set excludes — so `newest(src) > newest(§3-filtered outputs)` is TRUE and `stale-unit` fires. `build` is `tsc -b && npm run bundle:types && npm run bundle:cli` (`package.json:15`) whose other two steps write only `packages/cli/.dts-cache/index.d.ts` (`bundle-types.mjs:39`) and `packages/cli/dist` (`bundle-cli.mjs:63`), so on any of the other 18 units the printed remedy reduces to `tsc -b` alone and the check fires again, identically, forever — reprinted at line 5 of every `npm test` under `pretest`, and halting the other 14 members under `check:all --strict`, never a blocked push (round 17, OP17-1: §5 measures `check:all` as invoked by no workflow and no hook, and `core.hooksPath`'s `pre-push` runs `lint typecheck build test`, rebuilding before it tests, so what the owner accepted is a permanent warning line plus a hand-run chain that stops — not a push that fails) — which is §4's own sentence about this row. ⚠️ **And "reprinted at line 5 of every `npm test`" is itself a PROCESS property that nothing asserted until round 19 (`CF19-1`):** it holds only while `check-stale-dist.mjs` passes `exitCodeFor` the flag it parsed, and every element for that column CALLED the function instead — so a CLI hardcoding `{ strict: true }` turns the cost the owner accepted from a reprinted warning line into `npm test` never running at all, which is a materially different acceptance. §6 row **43**'s CLI twin is the element. **What the owner accepted is that cost at §6 row 2's scale — a scale that row pins by EQUALITY on a TWO-unit fixture's `Unit.dir` set only since round 16 (`CF16-3`), having read `stale-unit` ×N for fifteen rounds, so the one thing this sentence rests on was the one thing the row it cites could not tell from ×1**, never one build: a `tsconfig.base.json` rewrite or a wide `git checkout` yields `stale-unit ×N` across up to all 19 units, so up to 19 `rm -rf <unit>/dist` plus a full rebuild. ⚠️ **And that PRINTED cost — one `rm -rf <unit>/dist` line per REPORTED unit — was rendered by no element until round 25 (`OP25-1`)**, which is this cell's round-19 sentence one surface over: every rendered-plan assertion in §6 demanded step 1's ABSENCE, so a plan assembler with no step-1 branch printed `npm run build` alone here — a remedy §4's own measurement says cannot clear a touched-but-unchanged source, so the cost the owner accepted becomes a finding that never clears — and a first-selected-unit-only formatter printed ONE `rm -rf` where the ruling accepts up to 19, leaving the other units stale after the operator follows the whole plan. Both are materially different acceptances from the one recorded here, and §6 row **28**'s PLAN TWIN is the element that separates them. Accepted by owner ruling on those terms. **It does NOT escalate to `unbuilt packages/cli`:** the only thing `tsc -b` moves in that case is `dist/.tsbuildinfo`, which §3's output set — and therefore §3.3's clause 2 — excludes. Measured on a composite fixture with the pinned `typescript@6.0.3`. |
| A bare `npm run typecheck` (`tsc -b`) that re-emits, after a completed bundle                                                                                                 | Firing is CORRECT, and reports `unbuilt packages/cli` (clause 2), **plus four coverage entries reading `not run (packages/cli unbuilt)`** — §3.2 suppresses all four cli-scoped comparisons on this tree and nothing is absent, which is the routine tree round 10's C10-1 fix exists for. `tsc -b` overwrites `dist/bin.js` and `dist/index.d.ts` with per-file output that still imports `@crabgic/*` — `bundle-cli.mjs:105-112` states it, and `ci.yml`'s `packaging` job comments on the same fact as the cause of defect `25-install-smoke-depends-on-local-dist-state`. Verified: the live `dist/bin.js` imports `./chunk-FRJGAF5Y.js`, so a `tsc -b` over it destroys the bundle. Named here so nobody mutes it as noise.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Branch switch refreshing sources                                                                                                                                              | Firing here is CORRECT — it is the founding incident. Named so nobody mutes it as noise.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `npm run typecheck` clobbering `dist/index.d.ts` with `tsc`'s barrel                                                                                                          | Firing is CORRECT, and ⚠️ **clause 2 SUBSUMES this row — it is row 2's scenario, not a distinct one (round 10).** Measured 2026-08-20: `tsconfig.base.json:22-23` sets `declaration`+`declarationMap`, and no config sets `emitDeclarationOnly` or `noEmit` for `packages/cli`, so a `tsc -b` that writes the barrel `dist/index.d.ts` writes `dist/index.js` and `dist/index.d.ts.map` in the same emit. Those two ARE in §3's filtered output set, so `mtime(newest output) > mtime(marker)`, clause 2 fails, and the verdict is `unbuilt packages/cli — dist/index.js (…) is NEWER than the last completed bundle`. ⚠️ **That subsumption is CONDITIONAL on the marker, and this cell stated it unconditionally (round 13, C13-2).** Clause 2's predicate is "**if** `packages/cli/.bundle-meta/metafile.json` exists". **Marker PRESENT:** `packages/cli` is `unbuilt`, so §3.5 and §3.2 do NOT run — neither `stale-shipped-declarations` nor `stale-bundle` fires — and the remedy is the `unbuilt` row's (`rm -rf packages/cli/dist` then `npm run build`), never recipe steps 3-4. **Marker ABSENT — every tree until the first build after this lands**, measured 2026-08-20 (`ls -d packages/cli/.bundle-meta` → no such file, on a `dist` holding five chunks, all three entry outputs, `index.d.ts` and all six plugin entries): clause 1 finds no gap, clause 2 does not evaluate, clause 3 holds, so the unit stays **`compared`**. `stale-bundle` still does not fire, but for the OTHER reason — `bundle-provenance-missing`, advisory, coverage `not-run` — while §3.5 **RUNS** and `stale-shipped-declarations` fires, NON-advisory, **exit 1 under `--strict`**, remedy `npm run bundle:cli` (§4's table), not the `unbuilt` row's. §8(i) already states this branch — "With the cache present the common case IS caught — but by §3.5, whose stated purpose is different, not by the clause written for it" — so until round 13 the design contradicted itself at two sites round 10 wrote in one pass, and this cell, the one an operator reads for "what fires on this tree", was the wrong one. §6 row 22's marker-absent twin pins it. `bundle-types.mjs:32-38` records the underlying incident `check-install-smoke.mjs` caught (`Cannot find module './exit-codes.js'` from an installed consumer), and §3.5 still keys on bytes for the reachable state its own section now names. Round 9 rewrote row 2 for clause 2 and left this row asserting pre-clause-2 behaviour. Named so nobody mutes it as noise.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Test fixtures under `src`                                                                                                                                                     | Input set restricted to `.ts` + `.json`; the other 15 of 18 non-`.ts` files under `src` are ignored.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Second-granularity mtimes                                                                                                                                                     | Compare strictly `>` on `mtimeMs`; equal is clean — and §3.3's clause 2 is the mirror, `>=`, so equal is clean there too, ⚠️ **asserted only since round 19 by §6 row 19's EQUAL-mtime twin (`C19-2`)**: every specification of the marker's mtime in §6 reads `at or after` or `newest`, so this row's own mitigation claim rested on nothing and a clause 2 spelled `>` turned the very pair below into a false positive on every completed build. ⚠️ **AND THIS CELL STATES ONE MITIGATION FOR FIVE THRESHOLDS AND NAMED AN ELEMENT FOR ONE (round 36, `C36-1`, standing rule 9 at the section that governs all five).** §3's Verdict is §6's false-negative bullet `input mtime exactly equal to output mtime`; §3.2's, §3.3's declaration-cache and §3.4's thresholds are §6 rows 5, 6 and 24's EQUAL-MTIME TWINS, asserted only since round 36 — before which a body spelling ANY of those three `>=` passed every element in this design and reported `stale-bundle`, `stale-declarations` or `stale-plugin-assets` against `packages/cli` at `check:all` member 0, exit **1** under `--strict`, on a build that had just completed, with a full rebuild or the ~5-minute `npm run bundle:types -- --force` as the printed remedy. ⚠️ **NONE of the three equalities is LIVE on this working copy, which is why those twins are the only elements there are** — re-derived read-only 2026-08-22 at `d0b706e`, `git status --porcelain` empty before and after and no import of `scripts/bundle-cli.mjs`: newest `packages/*/src/**.ts` **1787171860413.817** against `packages/cli/.dts-cache/index.d.ts` at **1787172303152.899**; newest of the six `PLUGIN_ASSET_ENTRIES` **1787049783306.418** against the newest beneath `packages/cli/dist/plugin` at **1787414591040.448**; and the newest of §3.2's three input members **1787171860413.817** against `bundleAt`, the MINIMUM over the five chunk-pattern files, at **1787414591004.189**. ⚠️ Ordering is asserted by `./mtime-propagation-probe.mjs`'s `ordering:` rows only for **writes more than a second apart** (`:74-75`, which calls `pastNextSecond()` between the two writes). The marker write at `bundle-cli.mjs:181` follows the plugin copy at `:178` by microseconds, which that probe does NOT cover — `>=` is precisely what makes the uncovered case safe (round 9).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Fresh clone, restored tarball, or a `dist/` left empty by `build:clean` or by a test's `mkdir`                                                                                | `unitState` returns `skipped` for all three (§3.3's unit-state table for the ABSENT case, whose literal `no dist/` that table has carried only since round 15 (OP15-2) — this cell asserted "each of the three now carries its own literal `reason`" for two rounds while the table it cites for the first case carried none; its reason-table rows 1 and 3 for the other two — three tree states, TWO reason-table rows, and each of the three now carries its own literal `reason`: round 13, OP13-2) — a unit with nothing proving a build ran is not stale. The cost of covering the third case is residual §8(h): `build:clean` is indistinguishable from never-built.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Toolchain bump marking all 19 units stale                                                                                                                                     | Deliberately NOT implemented: putting `package-lock.json` in every unit's input set would make an unrelated `npm i` fire `stale-unit` on all 19 units at line 5 of every subsequent `npm test`. ⚠️ **This cell read "block a push" for sixteen rounds and no run of this check blocks one (round 17, OP17-1).** See §8 residual (b).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Check run while `npm run build` is mid-flight                                                                                                                                 | Not mitigable; a transient the operator caused. Documented in the header. **Round 9: the manifestation is now `unbuilt packages/cli`**, and the window is wide — `build` is `tsc -b && bundle:types && bundle:cli`, so for the whole ~5-minute `bundle:types` step the entry outputs are newer than the marker. `pretest` makes a second terminal running `npm test` hit it, and the printed remedy is `rm -rf packages/cli/dist`, aimed at a directory the running build is about to write — the same hazard §3.1 names for the journal scratch, on a second path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A tree whose last build predates this change set (`.bundle-meta/` absent, `dist` complete) — **every developer's tree on day one, at every `npm test` until they build once** | Not a false positive — the check truthfully cannot verify provenance and says so. ⚠️ **But the cost is not provenance ALONE (round 10, C10-4):** §3.3's clause 2 is conditional on the marker ("**if** … exists"), so while it is absent C9-1's freshness oracle — the fix for the SEVENTH appearance of PASS-on-a-broken-tree — does not evaluate, and nothing reports that a unit-state clause was skipped: `skipped`/unit state stays UNIT-scoped (§2) while §4's coverage line is comparison-scoped, so it structurally cannot say it. On such a tree the only remaining oracle for `tsc -b` having overwritten a completed bundle is §3.5's byte comparison, which itself declines whenever `packages/cli/.dts-cache/index.d.ts` is absent. Accepted as a bounded residual naming that conjunction — §8(i), pinned by §6's false-negative battery. Reported as `bundle-provenance-missing`, **never `unbuilt`** (round 9, C9-3): the line says the CHECK is degraded, not that the tree is broken. Advisory: listed, exit **0** even under `--strict` (§4), and `pretest` carries no `--strict` (§5.1), so it warns without blocking `npm test`; remedy `npm run build`, never `rm -rf`. Measured 2026-08-20: `ls -d packages/cli/.bundle-meta` → no such file on a fully built tree (§6). Permanent and stateless — this tree cannot be told from a build interrupted between `bundle-cli.mjs:178` and `:181` (§8(i))                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Stale `.d.ts.map` lingering after `declarationMap: false`                                                                                                                     | Makes `dist` look _fresher_ — a false negative, not a false positive. Named for completeness. ⚠️ **AND it is now an ASSERTED verdict rather than a named one, being the baseline of §6 row 44's round-20 twin — the whitelist's only element (`C20-1`).** That twin's clean fixture is exactly this state — a `.d.ts.map` as the newest member of §3's filtered output set, with a `src` file newer than every `.js`/`.d.ts` beneath it — and it must report CLEAN, which is what fails an accept predicate whitelisting `.js`/`.d.ts` alone. Re-derived read-only 2026-08-21 at `9c247eb`: **three** units of this working copy (`packages/supervisor`, `packages/scheduler`, `packages/learning`) are in that state today, so the accepted false negative is live, and the row asserts it rather than a better one — §6's discipline for §8(i) and §8(p).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| §4's coverage line read as a failure on a clean tree (fresh clone, the marker-absent state §6 measures on this tree)                                                          | It cannot change the verdict: exit code unchanged (§4, §8(j)), no finding, no remedy-TABLE row — the table is keyed by finding KIND. ⚠️ **It does now carry a next action, and that is not a verdict change (round 14, OP14-1):** a non-`full` entry makes §4's REMEDY PLAN print on a run holding zero findings, step 5 alone, still exit 0, still no finding — except on the `nothing was checked` PASS this row's own fresh-clone case produces, which §4 carves out so research Q5's quiet clone stays quiet. Every entry names, in the reason its own section mandates and rendered by §4's stated renderer, either the absent artifact or — for the four entries §3.2's `unbuilt` suppression produces, where nothing is absent — `packages/cli unbuilt` (round 10, C10-1); a marker that exists and does not parse names itself, `.bundle-meta/metafile.json unreadable` (round 10, C10-3). No entry claims a migration window: C9-3 deleted that wording from the rule, and round 10 deleted it from the last two places it survived — §6's live rendering and this cell. The alternative is the bare PASS round 9 filed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| A `packages/cli/src` edit reporting BOTH `stale-unit` and `stale-bundle` — one fact, two kinds, two remedy rows                                                               | Neither firing is false; the DUPLICATE is (round 14, C14-5). §3.2's input side deliberately repeats `checkUnitFreshness`'s comparand, and the two differ only on the output side — min-over-chunks versus max-over-outputs — which is why §3.2 is kept as the tighter of the two rather than deduplicated (standing rule 8: dropping it is a false negative on a partial chunk refresh). The cost is bounded and paid in §4: step 1's fourth carve-out exempts `packages/cli` from `rm -rf` when its only kinds are these two, because `bundle-cli.mjs:113-119` wipes `dist` itself. §6 row 38 pins both halves. Named here so nobody mutes the second line as noise, and so nobody removes it as redundancy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

## 8. What this design does NOT establish

- **(a) Bootstrap — requirement 4, residual.** `packages/cli/package.json:22` declares
  `"crabgic": "dist/bin.js"`, so `crabgic doctor` is a compiled artifact and a stale
  `packages/cli/dist` runs a doctor registry that predates any check added to it. This
  design does not solve that; it _sidesteps_ it by living in `scripts/` (§1). No check
  can solve it from inside the tool, and `crabgic doctor` remains subject to it.
- **(b) Build program and toolchain — requirement 7, residual.** `scripts/bundle-cli.mjs`
  and `scripts/bundle-types.mjs` are read as INPUTS for one artifact each (§3.2, §3.3)
  and for nothing else — ⚠️ **and until round 15 (C15-3) no assertion anywhere in §6 touched
  either as a comparand member**, so the partial closure this bullet claims was a property of
  the DESIGN and of no implementation that could pass it. §6 rows 41 and 42 are the rows;
  each mutates one producer script's mtime forward and requires the kind that member exists to
  catch, with `stale-bundle` asserted NOT to arrive as `stale-unit` (the producers are in no
  unit's `src`). `bundle-cli.mjs` also produces `dist/plugin/**` (§3.4) and
  `dist/index.d.ts` (§3.5); both are compared against their own sources rather than
  against the producer, so editing it fires `stale-bundle` alone and not three findings. ⚠️ **That sentence is this bullet's own load-bearing claim and NOTHING asserted it for twenty-four rounds (round 25, `CF25-2`).** Row 42 pinned it for `bundle-types.mjs` with `SOLE kind`; row 41's verdict was `stale-bundle` and NOT `stale-unit`, which permits further kinds, so an implementation putting `bundle-cli.mjs` on §3.4's source side as well as §3.2's passed every row and rendered two kinds and two remedy rows from one producer edit — verbatim the "three findings" this sentence promises against. Row 41 now asserts the SOLE kind, on a fixture holding the six `PLUGIN_ASSET_ENTRIES` sources older than their shipped copies so §3.4 is REACHED rather than `not-run`, with that wiring as its second mutant. A `typescript` bump off `6.0.3` moves nothing the
  check reads, yet invalidates every `dist`. `.tsbuildinfo` is the only complete oracle
  and the research rejects it: no public interface describes its contents (three
  mentions in `typescript.d.ts`, all about the path) and it carries `version: "6.0.3"`,
  binding a parser to a compiler release.
- **(c) That the check DISCRIMINATES in CI.** It does FIRE there — `ci.yml:129` is
  `run: npm test` in the two-leg `test` matrix, so `pretest` runs **twice per push** —
  but `ci.yml:86` builds immediately before it, so it can only ever report clean.
  ⚠️ **`only ever` quantifies over CI and this bullet cited one workflow file for it
  (round 30, `CF30-2`).** It holds by §5's **THE CHECK HAS ONE CI OCCURRENCE**, whose
  measurement runs over `.github/workflows/` — all TEN files — rather than over `ci.yml`
  alone; this bullet names the rule and does not restate its evidence, which is how every
  other live site resting on that rule already carries it and how this one did not. An
  earlier draft said the check "cannot fire in CI", which is the opposite of the truth
  and would have left a CI failure with no documented owner (round 5).
- **(d) Content correctness, with one stated exception.** The check answers "was the
  build re-run since its inputs moved", never "is the output right" — a rebuild producing
  byte-identical output still refreshes mtimes and still reports clean. The exception is
  `packages/cli/dist/index.d.ts`, which §3.5 compares BYTE FOR BYTE against
  `.dts-cache/index.d.ts`; that establishes the shipped copy is the bytes
  `bundle-types.mjs` last produced, and nothing about whether that cache is itself
  current — which is §3.3's mtime question, under §7 row 1's bound. ⚠️ **BYTE FOR BYTE was
  asserted by NOTHING until round 34 (`C34-1`), and this bullet is the sentence that
  claimed it.** §3.5's Rule spells the comparison `size first, then content` and §2's
  declaration `size, then content`, and every §6 element in which the two files DIFFER also
  gave them DIFFERENT sizes, so a body doing the size step alone passed the whole battery —
  and establishes this bullet's exception for the LENGTH of the shipped copy and never for
  its CONTENT. §6 row 22's TWIN is the element: both declaration files pinned byte-identical
  at 28 bytes, rewritten to a barrel of the SAME length, so the sizes are EQUAL exactly where
  the bytes differ. §8(e)'s round-19 amendment is this bullet's own shape one residual over.
- **(e) Behaviour off WSL2/ext4.** Mtime ordering is asserted by a probe on this
  filesystem only, and only for writes more than a second apart
  (`mtime-propagation-probe.mjs:74-75`). Where mtimes are coarse or absent the four mtime
  COMPARISONS under-report and do not over-report. ⚠️ **§3.3's clause 2 is the one
  exception, added in round 9, and it CAN over-report.** It asserts that a write at
  `bundle-cli.mjs:181` carries an mtime no earlier than every write that preceded it in the
  same process. `>=` makes coarse granularity safe — ⚠️ **asserted only since round 19, by §6 row 19's
  EQUAL-mtime twin (`C19-2`); until then every marker mtime in §6 was specified `at or after`
  or `newest`, so a clause 2 spelled `>` passed the whole battery and gave this bullet a
  SECOND way to over-report — every completed build on a coarse filesystem, not the clock
  case alone, which is the one this bullet named.** ⚠️ **AND THIS BULLET'S OTHER HALF —
  that the four STRICT comparisons under-report and do not over-report — was backed by an
  element at ONE of the four until round 36 (`C36-1`, `C19-2`'s own class one comparison
  over, standing rule 9):** §3's Verdict has §6's false-negative bullet
  `input mtime exactly equal to output mtime`, while §3.2's, §3.3's declaration-cache and
  §3.4's thresholds are §6 rows 5, 6 and 24's EQUAL-MTIME TWINS. Without them a body
  spelling any of the three `>=` over-reports on every build whose two sides land in one
  second — the coarse-filesystem state this bullet grants clause 2, at the three
  comparisons the sentence above exempts from it. A clock that steps BACKWARD
  mid-build — an NTP correction, a network filesystem with its own clock — leaves
  `mtime(marker) < mtime(output)` on a correctly built tree and reports
  `unbuilt packages/cli`. Accepted as a residual rather than mitigated: the alternative is a
  monotonic build counter, which is a persisted build artifact carrying the same staleness
  problem assumption 2 rejects.
- **(f) Anything about the other 8 `e2e/*` tsconfigs.** All 8 set `noEmit: true`
  (`grep -l '"noEmit": true' e2e/*/tsconfig.json e2e/matrix/*/tsconfig.json | wc -l` → 8),
  emit no `dist`, and are outside the 19-unit enumeration by construction. `e2e/matrix`
  has no top-level `tsconfig.json` at all. ⚠️ **They ARE handed to a compiler by a build
  program, so requirement 9's wording reaches them and §8(l) counts them among its 9
  (round 12).** `scripts/check-e2e-types.mjs:40` runs `npx tsc -p <config> --noEmit` for
  exactly `E2E_TYPECHECK_PROJECTS` (`:24-32`), which is these 8 — re-derived 2026-08-20,
  as is the fact that each declares `"extends": "../../tsconfig.base.json"` (or
  `../../../`), so §3 rule 3 does not reach them (it covers `<unit>/tsconfig.json` for the
  19 root references only) and rule 4 does not either (it reaches only configs whose chain
  resolves INTO a unit). With (l)'s root config that is **9 of 30** outside every input
  set, which is why §0's requirement 9 row and §3 rule 4 both state **21 of 30**.
  `noEmit` is the whole of the bound: a config that emits nothing produces nothing that
  can go stale, which is why they are excluded from every input set by design rather than
  by oversight. Measured here, cited from §8(l) and §3 input rule 4.
- **(g) That 1:1 stem mapping is permanent.** It holds for all 18 tsc units today. A
  future `allowJs` or a `.tsx` file would break it — which the orphan check would report
  as a finding rather than silently mis-handle, and which the fixtures in §6 do not
  cover. ⚠️ **An emitted `.json` was listed here as a THIRD such case and it is NOT one
  (round 20, `C20-1`).** §3's output set is a WHITELIST of `.js`, `.d.ts`, `.js.map` and
  `.d.ts.map`, so a `dist/**.json` — which `resolveJsonModule: true`
  (`tsconfig.base.json:10`) makes reachable the moment a compiled `.ts` imports one of
  the 3 `src/**.json` §3's rule 2 counts as inputs — enters neither the mtime set nor
  §3.1's stem set, and the orphan check reports NOTHING for it. This bullet mandated the
  opposite for that input while §6 asserted neither reading, so both were live and the
  one an implementer builds the predicate from was the whitelist-free reading this bullet
  implies — `CF13-4`'s shape at the output filter, and a manufactured `orphan-output`
  whose printed remedy (`rm -rf <unit>/dist` then `npm run build`) cannot clear it,
  because the next `tsc -b` re-emits the file. §6 row 44's round-20 twin pins the
  whitelist reading — ZERO `orphan-output` on a `dist/**.json` at its baseline, with the
  whitelist-free stem predicate as the mutant. What is residual is the SILENCE on the
  other side: a `dist/**.json` whose `src` counterpart is DELETED is invisible to §3.1 by
  construction, which is §8(m)'s bound at a second artifact — bounded the same way, an
  EXTRA file rather than a stale one, cleared by the `rm -rf <unit>/dist` plus
  `npm run build` §4 prints for every neighbouring kind.
- **(h) That a `build:clean`ed unit is distinguishable from a never-built one.**
  `tsc -b --clean` deletes `.tsbuildinfo` along with the outputs, so a `--clean`ed unit
  and one nobody has ever built are byte-identical, and §3.3's reason table gives both the
  same `skipped`. Accepted rather than papered over, and bounded rather than open: §3.3
  records what `--clean` actually removes **from `packages/cli`** (the `packages/cli/`
  SUBSET of `npx tsc -b packages/cli --clean --dry` → exactly five deletions, nothing
  under `.bundle-meta/`, re-measured 2026-08-20; the whole command lists **4,781** across
  17 packages and the root `build:clean` **5,051** across all 19, because `--clean` cleans
  the reference graph — round 12, C11-2), so the cost is one verdict on one tree state and
  not a hole of unknown size. It is also why the
  verdict is a three-valued `unitState` and not a boolean (§2): a boolean must fold the
  empty tree into `stale`, which is noise on every fresh clone (CR6-2), or into `clean`,
  which is `PASS` on a tree with zero build output — round 3's `CR-2`, recorded there as
  "the worst available answer for the incident this check is named for".

- **(i) WHICH absence a missing marker is.** The check holds no state, so a tree built
  before this check landed and a build interrupted between `bundle-cli.mjs:178` and `:181`
  are byte-identical: the marker never existed in the first and existed-then-was-cleared in
  the second, and nothing on disk distinguishes them. Both report the advisory
  `bundle-provenance-missing`; requirement 3's comparison runs for neither, and no
  `unbuilt` verdict is inferred from the marker (round 9, C9-3). ⚠️ **And a SECOND cost,
  stated in round 10 (C10-4): while the marker is absent, §3.3's clause 2 does not
  evaluate at all.** Its predicate is "**if** the marker FILE exists" — tested on the path, never by a
  successful parse (round 14, C14-1: an unparseable marker STILL evaluates this clause, so
  only ABSENCE makes it inert, and this residual is bounded to the absent case alone)
  — so C9-1's freshness
  oracle is inert for exactly that window, and the coverage line cannot report it — the
  line is comparison-scoped while a clause of unit state is not (§2). The residual is the
  CONJUNCTION, named rather than left implicit: marker absent AND
  `packages/cli/.dts-cache/index.d.ts` absent, where §3.5's byte comparison also declines
  and a `tsc -b` that rewrote `dist/bin.js` over a completed bundle reports **PASS, exit 0
  even under `--strict`**. With the cache present the common case IS caught — but by §3.5,
  whose stated purpose is different, not by the clause written for it. Do NOT close it by
  making an absent marker `unbuilt` again: that is C9-3/O9-2, and it prints
  `rm -rf packages/cli/dist` to every developer on day one. Bounded rather than open:
  the marker is written last, so every earlier step is proven by §3.3's artifact set, and
  the second window is the milliseconds between two adjacent statements. ⚠️ **A THIRD
  cost, stated in round 12 (C11-6): while the marker is absent, clause 1 cannot verify the
  chunk SET.** Its membership is `metafile.outputs`-derived only when there IS a metafile;
  without one the clause falls back to "at least one file matching the pattern", so a
  `dist` holding one chunk of five passes clause 1, passes clause 3, and passes clause 2
  (which does not evaluate at all here — the first cost above), and the check reports
  **PASS, exit 0 even under `--strict`** on a `bin.crabgic` that dies with
  `ERR_MODULE_NOT_FOUND` (measured import graph, §3.3 clause 1). Bounded the same way as
  the other two: no repo script produces the state, and the first completed build both
  writes the marker and rewrites the whole chunk set. Accepted rather than closed by
  hardcoding a count, which would be a rule keyed on today's import graph. It self-clears —
  the first completed build writes the marker, and the wipe step clears it thereafter, so
  the state recurs only on a genuinely interrupted build. Accepted rather than papered
  over, in the shape §3.3 uses for `build:clean` at unit granularity. Round 8 tried to
  distinguish the two from the filesystem and produced the contradictory pair round 9 filed
  as CF9-2. ⚠️ **Round 10 merged two bullets that stated this same residual twice, and
  relabelled the tail of this section (i)/(j)/(k):** five bullets carried the label `(h)`,
  so all five citations from §4, §6, §7 and §9 resolved to whichever a reader hit first.
- **(j) That a degraded run is BLOCKED — only that it is stated, and (since round 14)
  that it carries a next action.** §4's coverage line
  reports; it does not gate. A `not-run` bundle comparison — requirement 3's, the
  founding incident's own — exits **0** under `--strict`, so `check:all` passes on a
  tree where that comparison never ran — asserted by §6 row 39's exit-code conjunct since
  round 27 (`OP27-1`) and by nothing before it, every other `exitCodeFor` assertion in §6
  reading a tree that carries findings. ⚠️ **This bullet said only "not blocked" for four
  rounds and was read as "nothing further is owed" (round 14, OP14-1):** a degraded run
  holding zero findings printed a `PASS` line, a coverage line and nothing else, so the
  operator learned what had not run and never how to make it run — on the tree §4's own
  recipe step 1 creates. §4's **A DEGRADED RUN CARRIES A NEXT ACTION** closes that half.
  What remains residual here is the ENFORCEMENT alone: the exit code is still unchanged,
  and a plan that scrolls away under `pretest` (§5.1) is still a plan nobody read. Enforcing it was rejected here as a scope
  expansion, and `check-citation-runs.mjs:274-288` is the shape to copy if it is ever
  wanted: FAIL only when NOTHING was verified, never on a partial. The state is live —
  `packages/cli/.bundle-meta/` does not exist until this change set lands (§6).
- **(k) That the bundler edits RUN in the asserted order.** §6 pins TEXT order inside
  `bundle-cli.mjs`'s single `main()` — clear before `await build({`, `mkdir` after the clear
  and before the write (round 22, `CF22-1`), write after the
  plugin-copy loop. A write hoisted into a helper invoked earlier, or guarded by a
  condition, satisfies the assertion and still breaks the oracle. ⚠️ **The same bound now
  covers round 10's two importability edits (C10-2):** §6 reads that `:89` says
  `export const` and that `await main()` sits behind the ino guard; it does not establish
  that importing `scripts/bundle-cli.mjs` runs nothing. A top-level side effect added
  ABOVE the guard satisfies both fragments and re-opens the rebuild round 3 disclosed —
  from inside the check that observes `packages/cli/dist`. Text order is chosen
  because it needs no build; the honest claim is "the source says so", never "the process
  does". Stated in round 9, when the previous form of this assertion was measured to pass
  with no write in the file at all — a fix that replaced one overclaim with another would
  be the eleventh partly-true completion claim in this record. ⚠️ **Round 15 (CF15-4) put
  the design's OTHER repo-internal import under this same bound:** §6 now reads that
  `walk.mjs`'s `bundle-cli.mjs` import statement names `PLUGIN_ASSET_ENTRIES`, with the
  duplicated literal as the mutant — and it did not read the STATEMENT until round 29
  (`CF29-2`) anchored that regex, the unanchored span having accepted a file whose comment
  carries the substring `import` AHEAD of the const's name while the import itself names
  some other binding, which made this bullet's residual an understatement of the hole
  rather than the whole of it. It does not establish that the imported binding is the
  one the walk USES — a `walk.mjs` holding both the import and a literal it prefers
  satisfies the read. The same "the source says so, never the process does" claim, at the
  mandate §3.3's clause 1 had carried with no surface at all for fourteen rounds.
  ⚠️ **Round 20 (`CF20-1`) adds the bound the TEXT of that order is read WITH.** The THREE
  placement anchors are regexes over the CALL — callee plus one literal, a negated class
  between (`MKDIR` since round 22, `CF22-1`) — so `CLEAR` and `WRITE` hold across
  `npm run format`'s wrapping and across a `const metaDir` factoring of the write, all three
  SOURCE FORMS re-derived 2026-08-21: the one-line, the wrapped and the factored, and NOT
  the anchors, of which there were two when that was measured. `MKDIR`'s conforming line is
  **67** characters at this file's 2-space indent (re-derived read-only 2026-08-21 at
  `5fef126`), 33 short of `printWidth: 100`, so the formatter never wraps it. The clear's
  anchor and the `mkdir`'s do NOT hold for a call routed through a module-level `META_DIR`
  const, which would then name no path — the write's does, its own literal being the
  FILENAME; §1 mandates the literal INSIDE the `rm(` call and INSIDE the `mkdir(` call for
  exactly that reason, and this bullet is where the mandate's cost is recorded rather than
  left implicit. Text order remains "the
  source says so, never the process does"; what round 20 removes is the narrower failure in
  which the source DOES say so and the assertion says otherwise. ⚠️ **Round 21 (`C21-2`) put
  the SIXTH edit under this same bound; round 22 (`CF22-1`) TOOK IT OUT of it and leaves a
  narrower residual here.** Round 21 gave the `mkdir` of `packages/cli/.bundle-meta/` no
  source property at all, on the reasoning that a mutant "could only restate the absence it
  asserts against" — the reasoning `CF20-1` applied to the `.gitignore` read. That holds for
  an EXISTENCE assertion and fails for an ORDER one, whose mutant is a HOIST: put the `mkdir`
  above the wipe-step clear and the clear deletes it, `writeFile` throws the same `ENOENT` on
  a tree carrying all six edits, and this bullet's round-21 form recorded only the mkdir's
  ABSENCE, so the misordered tree was named in no section of this design. §6 now carries a
  `MKDIR` anchor and asserts `search(CLEAR) < search(MKDIR)` and
  `search(MKDIR) < search(WRITE)`, each with a mutant that flips it alone. THREE things it
  still does not establish. The order is TEXT order — "the source says so, never the process
  does", as for the other four properties. §1's stronger "the pair placed together after the
  plugin-copy loop" is NOT asserted: an implementation putting the `mkdir` immediately after
  the clear violates that letter, builds correctly and passes — deliberately, because an
  element failing a correct implementation is the defect `CF20-1` and `OP16-3` file.
  ⚠️ **Round 28 (`CF28-1`) records what that licence COSTS, at the clause that grants it, and
  the cost is a PROOF and never the pin.** On any conforming source whose `mkdir` precedes the
  plugin-copy loop — the licensed placement just above, and equally one between `build()` and
  the loop — round 27's clear-sink mutant lands after that `mkdir` as well as after `build()`,
  so it flips `search(CLEAR) < search(MKDIR)` too and `search(CLEAR) < indexOf(BUILD)` is
  DELETABLE there with all SEVEN mutants still flipping. No relocation of the clear isolates
  it: an isolating destination must fall between `build()` and a `mkdir` above `build()`, and
  §6 anchors that sink on the two statements no §1 edit moves. The conjunct is still ASSERTED
  on that form and still fails a bundler whose clear sits after `build()`; what is NOT
  established there is its non-vacuity, which §6's mutant-coverage note carries for §1's
  prescribed placement alone — an implementer re-deriving on the licensed form must therefore
  not drop it. Re-derived read-only 2026-08-22 at `e70c9bb` over three `mkdir` positions. This
  widens what this same item already licenses and is not a fourth subject: the count above
  stays THREE. And the
  anchor needs the `".bundle-meta"` literal INSIDE the `mkdir(` call, which is why §1
  mandates it there, at the same cost this bullet already records for the clear.
- **(l) That the root `tsconfig.json` OR the 8 `e2e/*` `noEmit` configs are in any unit's
  input set — requirement 9, residual. NINE files, not one (round 12, C11-1).** `build` is `tsc -b && npm run bundle:types && npm run bundle:cli`
  (`package.json:15`), so a build program DOES hand the root config to a compiler, and it
  is in **no** unit's input set: §3 input rule 3 reaches it only by an upward `extends`
  walk and it declares none; rule 4 reaches only configs whose own chain resolves INTO a
  unit, and this one has `"files": []` plus 19 `references` and nothing else (re-derived
  2026-08-20 — `Object.keys` on the parsed file returns exactly `files, references`).
  ⚠️ **And the root config is not alone — `29` was the size of `enumerateTsconfigs`'
  RETURN, never the size of the covered set (round 12).** Re-derived 2026-08-20 by running
  §3's rules 3 and 4 over all 30 tracked configs: **21** are covered (the
  19 unit `tsconfig.json` through rule 3's FIRST member — which nothing in §6 exercised
  until round 18's twin on row 2 (`CF18-1`), an ancestors-only walk leaving all 19 outside
  every input set and this bullet's own count at **2 of 30** — and `tsconfig.base.json`
  through rule 3's upward
  chain — 20 in some unit's INPUT SET — plus `packages/cli/tsconfig.dts.json` through
  **§3.3's declaration-cache comparand**, rule 4 having been narrowed in round 14 (C14-3) to
  configs whose RESOLVED `outDir` lands in the unit's COMPARED output set and made
  EVALUABLE in round 24 (`C24-1`) by clause (iii), `composite` resolved from the same
  chain — which is the clause that excludes `packages/cli/tsconfig.dts.json`, whose
  resolved `outDir` IS `packages/cli/dist`, so rule 4 has no member today) and
  **9** do not — the root
  config plus the **8 `e2e/*` configs**, each of which declares
  `"extends": "../../tsconfig.base.json"` (or `../../../`), resolving to the root-level
  base and never INTO a unit, and none of which an upward walk from the 19 units reaches.
  A build program DOES hand those 8 to a compiler — `scripts/check-e2e-types.mjs:40` runs
  `npx tsc -p <config> --noEmit` for exactly `E2E_TYPECHECK_PROJECTS` (`:24-32`) — so
  requirement 9's own wording reaches them. They are bounded by **§8(f)**'s
  already-measured reason: all 8 set `noEmit: true`, so they emit no output that can go
  stale, and omitting them from the COUNT rather than stating that bound was standing
  rule 4 at the site of round 11's fix. Requirement 9 is therefore discharged for **21 of
  the 30** — the count against THIS bullet's own definition of discharge, **widened in round
  14 (C14-3) from "membership of some unit's input set" to "read by some comparison"**,
  because rule 4 no longer attaches `packages/cli/tsconfig.dts.json` and §3.3's comparand
  does. Under the old wording the same coverage reads 20 of 30, which is an arithmetic
  moving to follow an attribution change rather than a measurement — C11-1's own class of
  defect, avoided here by moving the DEFINITION and stating that it moved. Still NOT §3 rule
  4's 27 + 2 = 29 census population, which rule 4
  now states apart. The root config specifically is bounded rather than open, for two
  measured reasons: it carries **no `compilerOptions`**, so it contributes nothing to any unit's
  emitted output; and the one thing it does control — WHICH units exist — the check
  re-reads on every run, because §3 enumerates from this exact file
  (`enumerateRootReferences`, requirement 5), so a reference added or removed changes the
  unit list immediately rather than leaving a stale unit uncompared. Round 2 dispositioned
  this as `CF-B`, "accepted as a stated limit", and named §8 as where it would land; it
  reached no section for ten rounds (round 11). ⚠️ **A THIRD family is bounded here from
  round 24 (`C24-1`), and it adds NO FILE to the nine:** rule 4's clause (iii) admits only
  configs whose resolved `composite` is `true`, so a future NON-composite variant that a
  build program hands to a compiler AND that emits into a unit's compared output set would
  be in no input set either. Today there is exactly ONE non-composite descendant config —
  `packages/cli/tsconfig.dts.json`, and re-derived read-only at `8fdf3ed` it is also the
  only config of the 30 whose chain resolves into a unit at all — and it is among the **21
  covered**, through §3.3's comparand, so this bullet's **9** and its **21 of 30** are
  unmoved and neither needs re-derivation. The bound is the price of making clause (ii)
  decidable: `outDir` alone cannot separate a config a compiler EMITS from one a generator
  only READS, which is the contradiction `C24-1` measured between §6 rows 3 and 47.
  ⚠️ **"The price of making clause (ii) decidable" is NOT "in place of clause (ii)", and
  round 25 measured that it was readable that way (`CF25-1`).** Clause (iii) is a THIRD
  conjunct and (ii) stays mandated: a body evaluating (i) and (iii) alone attaches a config
  whose RESOLVED `outDir` lands OUTSIDE the unit's compared output set, and until round 25
  every element in this design SATISFIED clause (ii), so nothing could tell that body from
  a correct one. §6 row 47's ROUND-25 TWIN is clause (ii)'s failing-side element, and from
  round 34 (`CF34-1`) that twin's SECOND config is clause (i)'s — the same argument at the one
  clause a body may DROP outright, testing (ii) and (iii) alone. Neither moves anything here:
  BOTH configs are SYNTHETIC, so this bullet's **9**, its **21 of 30** and its
  "adds NO FILE to the nine" are all unmoved and none needs re-derivation.
- **(m) A DELETED plugin asset source — requirement 2, residual.** Delete
  `packages/plugin/skills/<x>/SKILL.md` and do not rebuild:
  `packages/cli/dist/plugin/skills/<x>/` survives and nothing reports it. Three sections
  each miss it for a different stated reason — §3.1 exempts `packages/cli` from orphan
  detection by construction; §3.3's clause 1 checks the six `PLUGIN_ASSET_ENTRIES` for
  top-level PRESENCE, not their contents — the same bound **§8(p)** states at the opposite
  tree state, an interrupted `cp` leaving an entry present-but-PARTIAL, whose cost is a
  MISSING shipped file rather than an extra one (round 15, C15-1); and §3.4 compares mtimes only, and a deletion
  raises nothing on the source side (whether a directory's own mtime counts is left to
  `newestOfEntries`' walk rather than settled here, because neither reading makes this
  comparison see it). `check:marketplace-pin` does not close it either: it digests the
  SOURCE (§3.4). Bounded: `bundle-cli.mjs:113-119` wipes `dist` — `KEEP` is `.tsbuildinfo`
  alone, re-derived 2026-08-20 at `:113` — so `dist/plugin` is removed and re-copied by
  the next completed `bundle:cli`, and the surviving file is an EXTRA rather than a stale
  one. Round 2 dispositioned this as `C-C`, "accepted as a stated limit"; like (l) it
  reached no section for ten rounds (round 11).
- **(n) That the check runs on a production-only install.** Round 10's C10-2 fix gives the
  check its SECOND repo-internal import, `scripts/bundle-cli.mjs`, whose `:59` is
  `import { build } from "esbuild"`, so `esbuild` must resolve at import time. ⚠️ **The
  FIRST is `scripts/repo-census.mjs` (requirement 11, §2), and it sits outside this
  residual only because of a measured fact this bullet has to state (round 13, CF13-1):**
  re-derived 2026-08-20, `grep -n "^import" scripts/repo-census.mjs` returns `node:fs`,
  `node:path`, `node:child_process` and `node:url` — builtins only. So this bound ranges
  over TWO modules and holds for one of them by luck; a dependency added to
  `repo-census.mjs` widens it, and `scripts/repo-census.mjs` is a file whose neighbourhood
  this change set already cites (`:290` in §2, `:382` in §1). Re-derived
  2026-08-20: it is a root `devDependency` (`package.json:61`, `0.28.1`), which `npm ci`
  installs, so `ci.yml:129`'s `test` job — the check's ONE CI occurrence (§5, round 25, `CF25-3`) — and every local invocation resolve it; an `--omit=dev`
  install does not. ⚠️ **What happens there depends on §1's IMPORT RULE, and the exit code
  this bullet stated was wrong for one round (round 12, OP12-1).** Under the mandated
  `await import(...)` at the entry module the throw lands in `check-stale-dist.mjs`'s catch
  and §4's internal-error row holds: `WARN`/exit **0** by default, `ERROR`/exit **2** under
  `--strict`. Under a STATIC entry import the resolve happens before the catch exists —
  measured 2026-08-20 (node v24.18.0, npm 11.16.0, nothing else running but the shell):
  `ERR_MODULE_NOT_FOUND`, a ten-frame node stack, no `check-stale-dist: ` line, and exit
  **1** with `--strict` AND without, which under `pretest` blocks `npm test` entirely —
  round 1's `O-2`. §6 row **31** proves the mandated form; without it this residual is a
  claim nothing tests. ⚠️ **And the reachability is wider than `--omit=dev`**: any
  incomplete or pruned `node_modules`, and any load-time throw anywhere in the six-module
  graph, lands in the same uncatchable window. Accepted rather than papered
  over, and bounded: no workflow
  under `.github/` and no root script installs with `--omit=dev` today (re-derived
  2026-08-20). §1 states the alternative that removes the residual entirely: a
  builtins-only `scripts/plugin-assets.mjs` both files import. ⚠️ **"Entirely" is true
  only while `repo-census.mjs`'s own import list stays builtins-only** — re-derived above,
  and the second half of this bound rather than a separate fact (round 13, CF13-1).
- **(o) That this check's one glyph DEGRADES — round 14, OP14-5, advisory.** §4 mandates
  `check-stale-dist: ⚠️ coverage: ` verbatim, and §6 rows 16, 17 and 26 make the prefix,
  the label and the asserted entry ONE contiguous substring, so no implementation may
  resolve a presentation profile for it. ⚠️ **That makes this check the FIRST `check:all`
  member to write a glyph to stdout — evidenced by this residual's own census below.**
  Re-derived 2026-08-21 with
  `grep -nP '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}\x{FE0F}]'` across all **14** member
  scripts: **7** hits in **4** files — `check-marketplace-pin-digest.mjs:147,155`,
  `check-support-window-freshness.mjs:40,110,303`, `generate-criteria-baseline.mjs:92`
  and `check-install-smoke.mjs:100` — and **every one is inside a COMMENT**. Zero of the
  14 write a glyph to any stream. This check would write one into an `&&`-chained stream
  of 14 ASCII-only neighbours and into CI logs (`ci.yml:129` fires `pretest` twice per
  push, non-TTY), on the one line §5.1 calls "the ONLY notice a degraded run ever gives".
  `docs/presentation-policy.md` §"Glyph profile" is this repository's settled authority
  and rules that a non-TTY resolves to `text` and that `CRABGIC_ASCII=1` forces `ascii`;
  `scripts/` cannot import `PresentationPolicy` (bootstrap, §8(a)), so the deviation is
  STRUCTURAL rather than an oversight, and rows 16, 17 and 26 forbid an implementation
  from honouring the policy even where it could. ⚠️ **This is a RENDERING residual, not a
  wrong verdict:** `⚠️` IS that policy's sanctioned warn glyph, so what is missing is the
  DEGRADATION and never the choice. Bounded: the glyph is decoration on a line whose text
  already names the comparison, its status and its reason, so a terminal rendering tofu
  loses no fact and no verdict changes. Closing it needs `report.mjs` to resolve a
  two-value profile from `process.stdout.isTTY` plus `CRABGIC_ASCII` and emit
  `WARN coverage:` in the `text` profile, with rows 16, 17 and 26 asserting whichever form
  the battery's own environment produces — a second bootstrap-scoped element this change
  set does not own, which is why it is stated here rather than taken.
- **(p) That a plugin asset entry copied PART-WAY is seen by anything — round 15, C15-1.**
  §3.3's clause 1 tests the six `PLUGIN_ASSET_ENTRIES` for the existence of their TOP-LEVEL
  paths under `dist/plugin` and never walks their contents, while `bundle-cli.mjs:177-179`
  copies each with ONE recursive `cp` — so an interrupt inside a single `cp` leaves all six
  paths present with one of them empty or truncated. Each of the four candidate detectors
  fails for its own stated reason: clause 1 by construction (presence of a path, not of a
  tree); §3.4 because `cp` writes the destination at copy time, so `newest(dist/plugin)` is
  `now` and the source side can never exceed it; clause 2 because `dist/plugin/**` is outside
  §3's filtered comparand, and on the reachable form of this tree the wipe-step clear has
  removed the marker and the write never ran, so it does not evaluate at all; and §3.1,
  which exempts `packages/cli` from orphan detection by construction. The verdict is
  `compared` plus exactly one advisory `bundle-provenance-missing`, **exit 0 even under
  `--strict`**, at `check:all` member 0 — so `check:tarball` (member 12) and
  `check:install-smoke` (member 13) run afterwards against the precondition §5 says this
  ordering names. Neither closes it: `check:marketplace-pin` digests the SOURCE (§3.4), and
  `check-install-smoke.mjs:255` fails only on `Cannot find module`, `unexpected error` or
  `ERR_MODULE_NOT_FOUND` — re-derived 2026-08-21 — none of which an empty `skills/`
  produces. ⚠️ **Distinct from §8(m), which is the same clause-1 bound at the OPPOSITE tree
  state**: there the source is deleted and the shipped copy is an EXTRA; here the source is
  intact and the shipped copy is INCOMPLETE, which is the class `bundle-cli.mjs:165-169`
  records this repository shipping in 1.0.0 (`crabgic doctor` dying in a real consuming
  repo). ⚠️ **The finding's own citations were re-derived and two were off:** the 1.0.0
  record is `:165-169`, not `:163-168`, and the install-smoke predicate is at `:255` —
  standing rule 2, on a reviewer's line anchors. Bounded and self-clearing: the next
  completed `bundle:cli` wipes `dist` (`KEEP` is `.tsbuildinfo` alone, `:113`) and re-copies
  all six, so the state recurs only on a genuinely interrupted copy. Accepted rather than
  closed by making §3.4's dist side per-entry — that trades a stated residual for a new
  comparand shape, a new line, a new reason and a new row set, and standing rule 8 has fired
  on every widening in this design; pinned instead by §6's false-negative battery, which
  asserts this verdict rather than a better one.

## 9. Review rounds

**Round 1 (2026-08-20) — `contract-fit` / `correctness` / `operability`, three
`revise`, seven findings, all re-derived by the manager and all `fixed`.** The lens
verified every count and line anchor in the design and found them exact, then attacked
the algorithm — which is where all seven live.

**The three that mattered share one root cause: the design specified the happy path's
remedy and never the degraded tree's.**

- **C-1, high.** The skip rule was per-unit. `checkBundleFreshness` and
  `checkDeclarationCache` stat unconditionally, so on a fresh clone — or after **this
  design's own documented `rm -rf packages/cli/dist`** — the check throws.
- **O-2, medium-high.** That throw exits **2**, and on npm 11.16.0 a `pretest` exiting
  2 blocks `npm test` exactly as exit 1 does. Re-derived: `TEST RAN` never prints. So
  §5.1's "non-strict, therefore it cannot block testing" argument did not cover the
  check's own errors, and C-1 made that path reachable from the printed advice.
  **Following the design would have left an operator unable to run the suite.**
- **O-1, high.** `npm run build` was the single printed remedy and cannot clear two of
  the four kinds. Re-derived on a composite fixture with the pinned `typescript@6.0.3`:
  delete a source, run `tsc -b`, and the orphaned `.js` and `.d.ts` **survive** — tsc
  never removes them. And `bundle-types.mjs:70` skips regeneration whenever its cache
  is newer than a predicate that stats no `.json` and not itself, so the two inputs
  §3.3 adds _specifically_ to catch staleness produce a finding `npm run build` will
  never clear. At 3am the operator runs the remedy, the check fires again identically,
  and under `--strict` the push stays blocked with no path stated.

**C-2, medium — a false NEGATIVE in the one comparison requirement 3 exists for.**
`bundleAt` keyed on `dist/{bin,index,bin/supervisord}.js`, but `packages/cli/src` has
all three sources and `tsc -b` writes exactly those paths. Verified. So
`npm run typecheck` alone resets the oracle and the bundle reports clean although
`bundle:cli` never ran. Now keyed on an artifact only esbuild produces.

**CF-1 / C-3, medium — the closure is a superset of the inlined set.** Verified:
`@crabgic/testkit` is a `devDependency` of `packages/cli`, not a dependency, and
**zero** testkit runtime symbols appear in `packages/cli/dist/*.js`. Editing testkit
and running `typecheck` fired `stale-bundle` against a bundle containing none of that
code. Now the closure minus dev-only edges, with the remaining over-approximation
stated.

**CF-2, medium — excluding `dist/plugin/**` closed blind spot 1 and opened a new
hole.** Those assets are then in no input set, and `check:marketplace-pin` digests the
source rather than the shipped copy. A fifth comparison covers it.

**O-3, low** — `stale-unit ×19` from one `tsconfig.base.json` edit is now one grouped
line, not nineteen printed above 83 vitest failures.

**O-4, advisory** — the `check:all` hit count had drifted 5 → **6**. This change set's
own standing rule 3, caught on the design one day after the research stage closed on it.

ℹ️ **What the lens checked and could not break**, recorded so it is not re-run: `src`
`.json` emission, `.d.ts`/`.js.map` stem collisions, JSONC tsconfigs (8 carry comments;
all 30 parse under raw `JSON.parse` today), equal mtimes, and renames or deletes on the
source side. It also re-derived §6's live claim with the design's own algorithm and got
exactly the four `stale-unit` findings the design predicted, and timed the walk at
**0.23-0.24 s** across six warm runs — a spread of 0.01 s, reported per standing rule 3.

**Round 2 (2026-08-20) — three `revise`, nine findings. Two refute round 1's own
fixes, and one of those was refuted by the exact rule this change set keeps
re-learning.**

**C-A, high — the CF-1 fix rested on a search narrower than its claim.** Round 1
asserted "zero testkit runtime symbols appear anywhere in `packages/cli/dist/*.js`"
and built a rule on it. The search was for the SPECIFIER `@crabgic/testkit`, which
esbuild erases when it inlines — it could only ever return nothing. Re-measured by
definition: **11** testkit identifiers in `chunk-I6JBP7DT.js`. And
`packages/cli` declares **zero** `@crabgic/*` `dependencies`, so "minus dev-only edges"
drops `renderer` (also inlined) or, read transitively, all 16 — a **vacuous**
comparison, in the one place requirement 3 exists for. Re-keyed on
`result.metafile.inputs`, which `bundle-cli.mjs` already produces.

**C-B / O-B, high — `npm run build` does not clear the most common finding, and the
investigation changed a design decision.** `tsc -b` re-emits from `.tsbuildinfo` content
versions, so a touched-but-unchanged source produces no compiler output. A first
measurement said the remedy worked; per-file measurement showed only `.tsbuildinfo` had
moved, because it lives inside `dist`. **That decided a question the design had left
open: `.tsbuildinfo` is excluded from the output set**, since including it would let
every `stale-unit` self-clear on the next `tsc -b` with nothing rebuilt.

⚠️ Recorded because of the near-miss: the whole-directory measurement said "cleared" and
would have closed the finding. Only asking which FILE moved reversed it. **A measurement
at the wrong granularity is as wrong as no measurement.**

**O-C, medium — the printed remedy could fail with an unrelated error.**
`npm run bundle:cli` throws when `.dts-cache` is absent (`bundle-cli.mjs:146-151`), and
its own error text says that happens "only when `bundle:cli` is invoked on its own" —
precisely what the design prescribed. Now `npm run build`, which orders `bundle:types`
first.

**CF-A, medium — round 1's fifth comparison landed in prose only.**
`stale-plugin-assets` appeared once in 528 lines, absent from the `kind` union, the
exports, the file table, the remedy table, the non-vacuity battery and the traceability
matrix. An implementer building from §2 would have shipped four comparisons. And
`exitCodeFor` still declared `0 | 1` while §4 required exit 2. Both fixed — the same
"the fix landed in one place" fault the research record hit four times.

**Accepted as stated limits rather than closed — and round 11 finally put two of them in §8, ten rounds after this sentence named it.** CF-B is now **§8(l)** and C-C is **§8(m)**; the other two items DID land at the time — C-D in §3.3's chunk-predicate bound ("an artifact esbuild produces and no source in this repository would name") and O-A in §5.1's scroll-away limit — which is what makes the two misses falsifiable rather than a matter of taste. CF-B — the root `tsconfig.json`
is in no input set (it carries no `compilerOptions`, only `files: []` and
`references`, so the impact is bounded); C-C — deleting a plugin asset source leaves the
shipped copy until the next completed `bundle:cli`; C-D — the chunk predicate is named
as `/-[A-Z0-9]{8}\.js$/` with `metafile.outputs` as the exact form; O-A — the `pretest`
warning prints ~199 lines above vitest's summary, so a reporter re-emit is the reliable
form and the limit is stated.

ℹ️ **What round 2 attacked and could not break**, so it is not re-run: §3.4's
"can only mask, never manufacture a false positive" — verified, `cpSync` resets mtimes
and no repo script writes into the asset sources; the hashed chunk exists so the
fallback is not live; the 0-orphan bijection; §6's live four-`stale-unit` prediction,
re-derived with an independent implementation; §8(g); and the walk timed at 0.06 s over
six runs.

**Round 3 (2026-08-20) — three `revise`, nine findings, all re-derived and all
dispositioned. Two are blocking, and one of them would have shipped a defect this
repository already has a gate against.**

**CR-1, high — the round-2 metafile fix would have published a 404 kB file.** The fix
put `result.metafile` at `packages/cli/dist/.bundle-meta.json`. `packages/cli`'s `files`
is `["dist", "!dist/**/*.test.*", "!dist/**/test-support/**", "!dist/.tsbuildinfo"]`, so
anything new under `dist` ships by default. Measured: **404,198 bytes, 758 inputs**,
including a `node_modules/**` tail.

⚠️ **This repository already banned exactly this file class, for exactly this reason.**
`check-published-tarball.mjs:41-45` excludes `.tsbuildinfo` because it is "the one file
that differs between two builds of identical sources in different environments, so
shipping it makes the published artifact non-reproducible — directly undermining
roadmap/23's reproducible-build criterion. **Shipped in 1.0.0 through 1.1.1.**" The
metafile is twice the size with the same defect, and no `files` negation or
`FORBIDDEN_PATTERNS` rule would have caught it — `check:tarball` would have **passed**.
Moved to `packages/cli/.bundle-meta/`, beside `.dts-cache`, which is the shape this
repository already uses for a build artifact that must not ship.

**CR-2, high — the skip rule's predicate was directory existence, and the empty state is
reachable from a published script.** Measured: `tsc -b --clean` leaves `dist` existing
and **empty**, and `build:clean` is a root script. A rule keyed on absence does not fire,
so the comparison would run with nothing to compare. Reporting `clean` there prints PASS
on a tree with **zero build output** — the worst available answer for the incident this
check is named for. Re-keyed on qualifying compiler outputs, with `unbuilt` as its own
verdict, never folded into `clean`. Round 2's `.tsbuildinfo` exclusion makes the state
reachable a second way, via `bundle-cli.mjs:113-119`.

**CF-1, medium — "both fixed" was again only half true.** Round 2's entry claimed
`stale-plugin-assets` had been propagated to the kind union, the exports, the file table,
the remedy table, the battery and the traceability matrix. Measured: it reached the kind
union and nothing else, and only `exitCodeFor` of the "both" was done. **This is the
second consecutive round in which a completion claim in this record was overstated**, and
the rule the research stage wrote down for exactly this — _a disposition is complete only
when every site is re-measured_ — was not applied to the disposition itself.

**CR-3, CF-2, OP-1, OP-2, OP-3, OP-4** — a missing metafile silenced requirement 3's
comparison on a fully built tree, so it now reports `bundle-provenance-missing`; the
metafile filter is stated (**758** inputs across **17** `packages/*`, so `inputs` is not
a unit list); `check:stale-dist` had two contradictory definitions, one of which made an
ad-hoc run punitive and passed `--strict --strict`; the fifth kind had no remedy row and
its only stated remedy was the command round 2 had just banned; `stale-declarations`
prints a **~5 minute** rebuild and now says so; and §5.1's "immediately above the
failures" is corrected to the measured **~199 lines** with the reporter re-emit named.
⚠️ **That last disposition was partly true, and round 12 (OP12-3) is the eleventh of its
kind in this record.** The correction reached the paragraph that STATES the measurement;
the sentence that RESTS on it — the closing argument for report-and-continue — still read
"immediately above the failures" nine rounds later. Round 2's `O-A` and round 3's `OP-4`
both dispositioned it `fixed`, and both verifications read the measuring paragraph rather
than the argument. `grep -n "immediately above"` over the whole file returned two hits,
one of them live in §5.1.

ℹ️ **The reviewer disclosed that it mutated the working tree** — a probe imported
`bundle-cli.mjs`, whose top-level `await main()` rebuilt `packages/cli/dist`. It checked
and reported that all five content-hashed chunk names were unchanged, so only mtimes
moved, and re-derived §6's live claim afterwards to confirm it still holds. Recorded
because disclosing a side effect and re-deriving past it is what makes the rest of the
report usable.

**Round 4 (2026-08-20) — three `revise`, nine findings, all re-derived and
dispositioned. One is the same failure for the third time, and it survived twice
because each fix keyed on a proxy.**

**C-R4-1, high — after `npm run build:clean`, `packages/cli` reports CLEAN with no
`bin.js`.** Measured with `tsc -b packages/cli --clean --dry`: `--clean` removes tsc's
outputs and the **five hashed esbuild chunks survive**. So the unit holds fresh
qualifying outputs, is not `unbuilt`, compares clean, and `bundleAt` reads a chunk that
is still there — while `dist/bin.js`, the published `bin.crabgic` entry point, does not
exist.

⚠️ **This is the third appearance of one failure — `clean` on a tree with no usable
build output.** CR-2 keyed the rule on directory existence; round 3 re-keyed it on
output presence; both are proxies. It is now keyed on the artifact anyone actually runs:
`packages/cli` requires its three entry outputs to EXIST, whatever the chunks say.

**C-R4-2, medium-high — `checkPluginAssets` was outside the skip rule**, so a fresh
clone or the design's own `rm -rf packages/cli/dist` would throw or emit
`stale-plugin-assets` against a tree that was never built. Round 1's C-1 recurring on the
comparison round 1's own fix introduced. All three cli-scoped comparisons now skip.

**C-R4-3, medium — the `unbuilt` remedy was wrong on the path §3.3 itself names.**
Measured: delete outputs but keep `.tsbuildinfo` — exactly `bundle-cli.mjs:113-119` — and
`tsc -b` re-emits nothing, twice over. `npm run build` clears it for `packages/cli` only
by accident, because `bundle:cli` rewrites the chunks, which does not generalise to the
other 18 units.

**CF-R4-3 — and one of this design's own claims was overstated in the safe direction.**
§3.2 called the reference closure "a SUPERSET of the inlined set". Measured: the metafile
filter yields **16** units, member for member identical to the closure, and `cli` never
matches because it enters only via `src/`. The sets are EQUAL today and the "minus `cli`"
step is a no-op. The metafile remains the rule — the closure's agreement is a fact about
today's import graph, not a property of it — but the record now says so.

**CF-R4-1, CF-R4-2, OP-R4-1, OP-R4-2, OP-R4-3** — §3.4 still printed the remedy round 2
banned; three of seven kinds had no non-vacuity row, so an implementer could have shipped
`stale-plugin-assets` hardcoded never to fire (the exact defect class this change set
exists for); `unbuilt` and `bundle-provenance-missing` had no line shape and would have
printed `undefined undefined NaN` (round 4 wrote "eighteen times after `build:clean`"; round 12 re-derived that state as **18 `skipped` units and ONE `unbuilt packages/cli`**, so the count and the unit were wrong at the round that filed them — the finding stands, the example did not); seven remedies had
no ORDER, and running them as listed wastes a full rebuild or undoes itself; and §5's
consequence list omitted `ci.yml:129`, the only place CI runs `npm test` and therefore
`pretest`.

ℹ️ **The lens disclosed two things it could not control** — the design file changed
under it mid-review, and `packages/cli/dist` was rebuilt at 09:43 by something that was
not its own probes, which it verified by pinning the artifact's md5 and by keeping every
probe non-mutating (`--dry`, and `esbuild` with `write: false` rather than importing
`bundle-cli.mjs`, which is how round 3 caused a rebuild). Recorded because a review whose
own side effects are stated is the only kind whose negatives mean anything.

**Round 5 (2026-08-20) — three `revise`, seven findings, four of them high. Two of
round 4's five claimed fixes did not land, and round 4's own new element failed on
contact.**

**C-R5-1, high — the test suite writes `.js` into a `dist`, so §3.1's "clean bijection"
was conditionally false.** `packages/journal/src/crash-fixtures/prepare-runtime.ts:25`
sets `SCRATCH_ROOT` to `packages/journal/dist` and `:110` `mkdtemp`s
`eo-crash-fixture-*` there, transpiling sources into it. Those `.js` have no `src`
counterpart, so they are orphans by this design's own rule — **40** measured, with two
fixture directories observed being reaped mid-review. The suite SIGKILLs its children, so
`cleanup()` cannot run when the harness dies.

⚠️ **It bites this design three ways at once**: `pretest` runs at the START of the very
`npm test` whose interrupted predecessor left the residue; the printed remedy would tell
the operator to `rm -rf` a directory a concurrent run is writing into; and §6's live
smoke test calls the check from inside vitest, so it can observe the orphans or `ENOENT`
on a directory reaped between `readdirSync` and `statSync`. Scratch is excluded by name,
and §3.1's count is restated as a measurement whose subject a test run mutates.

**C-R5-2, high — round 4's ordered recipe could not run in its own order.** Step 1
deleted every `<unit>/dist`; step 2 was `bundle:types -- --force`, and
`packages/cli/tsconfig.dts.json` declares `"references": []`, `"composite": false`, with
no `paths` anywhere — so `@crabgic/*` resolves through the workspace symlink to
`exports["."].types === "./dist/index.d.ts"`, exactly what step 1 deleted. Measured on a
fixture with the same generator: dependency `dist` present → inlines the type, exit 0;
deleted → `TS2307`, exit 1, no output. `npm run build` now comes second, and a fourth
step lifts the regenerated cache into `dist/index.d.ts`.

**C-R5-3, medium — "no build output" reached two OPPOSITE verdicts, and the design
directed operators onto the silent one.** `tsc -b --clean` leaves `dist` present →
`unbuilt`, a finding. The design's own `rm -rf` remedy leaves it absent → `skipped` →
**PASS, exit 0** on a tree with no CLI. The `PASS` line now names skipped units.

**CF-R5-1, high — §8(c) asserted the opposite of the truth.** It said the check "cannot
fire in CI". `ci.yml:129` is `run: npm test` in the two-leg `test` matrix, so with
`pretest` it fires **twice per push**; `ci.yml:86` builds immediately before, so it
cannot DISCRIMINATE there. The distinction matters because an unclassified throw would
redden both legs with no documented owner. Both §5 and §8(c) corrected, and the
internal-error row now covers **any** throw rather than malformed config only.

**CF-R5-2, high — §3.4 still printed the banned remedy, four lines after banning it, for
the FOURTH consecutive round.**

⚠️ **And this time it survived a disposition because the verification grep was
single-line and the sentence wrapped.** The trailing `Remedy:` and
`` `npm run bundle:cli`. `` sat on consecutive lines; a one-line pattern reported it
gone. **Standing rule 1 — a search narrower than its claim — applied to the verification
of a finding rather than to the finding itself.** That is the fourth consecutive round
with a partly-true completion claim, and the first where the cause was the check on the
claim rather than the claim.

**CF-R5-3, medium — round 4's two behavioural fixes had no non-vacuity row**, because
the battery is keyed by KIND and both are refinements _within_ a kind. An implementer who
never wrote the `packages/cli` entry-output rule would have passed all ten rows. Two rows
added: rm one entry output from an otherwise-complete cli `dist` → `unbuilt`; and
`packages/cli/dist` absent → `checkPluginAssets` returns `skipped`, neither finding nor
throw.

ℹ️ **What round 5 attacked and could not break**: the `unbuilt` remedy on both paths
§3.3 names (`--clean` removes `.tsbuildinfo` too, so path (a) clears under plain
`npm run build`); the entry-output rule against a fresh clone (row 1 precedes, so a fresh
clone is silent); every other unit's `dist` for foreign files (`packages/journal` is the
only one); `--clean` on `packages/cli` (exactly five deletions **in the `packages/cli/` subset** —
round 12 re-derived the whole command at 4,781 across 17 packages, and the root
`build:clean` at 5,051 across 19 — chunks and `plugin/**` survive); §6's four-`stale-unit` live claim, re-derived independently; and every count and
anchor. Walk timing 0.20-0.28 s over six runs, spread 0.08 s — wider than round 2's 0.06,
and the lens noted its machine was concurrently running vitest.

**Round 6 (2026-08-20) — three `revise`, nine findings, three high. The same failure
appeared for the FOURTH time, and the diagnosis is now exact.**

**CR6-3, high — `PASS` on a tree with no CLI, again, because the entry-output rule is
also a `tsc`-writable proxy.** The **`packages/cli/` subset** of
`tsc -b packages/cli --clean --dry` is exactly the three entry outputs plus `index.d.ts`
and `.tsbuildinfo` (round 12 re-scoped this: the whole command lists 4,781 across 17
packages) — so `tsc -b` alone satisfies round 4's rule. Interrupt `npm run build` during the **~5 minute** `bundle:types` step
and the tree has all three entries, no chunks, no `dist/plugin`, and a stale
`.bundle-meta/` that `rm -rf dist` never touched. Reported PASS, exit 0, on a tree with
no `crabgic` binary.

⚠️ **The four proxies are the whole lesson**, and they are recorded in order because
each looked sufficient at the time:

| round | keyed on                         | writable by `tsc -b` alone |
| ----- | -------------------------------- | -------------------------- |
| CR-2  | directory existence              | yes                        |
| 3     | output presence                  | yes                        |
| 4     | entry-output presence            | **yes**                    |
| 6     | an **esbuild-only** hashed chunk | no                         |

Three successive fixes chose something the wrong tool also produces. The rule is now
keyed on the one artifact only the bundler can make.

**CR6-1, high — round 5's scratch exclusion named ONE of two fixture families.**
`packages/journal/src/lease-fixtures/prepare-runtime.ts:60,:91` does exactly what the
crash family does — `SCRATCH_ROOT = packages/journal/dist`, `mkdtemp` into it, transpile
`.ts` to `.js`. A repo-wide `mkdtemp` sweep excluding `tmpdir()` bases returns exactly
these two. The exclusion is now `dist/eo-*-fixture-*/**`, which covers both and any third
following the convention.

**CR6-2, high — and that exclusion CREATED a false positive.** Both families call
`mkdir(SCRATCH_ROOT, { recursive: true })` and their `cleanup()` removes only the mkdtemp
directory, so `packages/journal/dist` persists **empty** after the first `npm test` on a
tree that was never built. With every remaining entry excluded, the next `pretest` would
report `unbuilt packages/journal` and print `rm -rf`. Round 5 would have turned a silent
wrong answer into a loud one. `unbuilt` now requires at least one non-excluded entry.

**CF6-1, high — and the exclusion had landed in ONE section.** §3's authoritative
_Per-unit output set_ had three `minus:` bullets and no scratch bullet; the exclusion sat
only in §3.1's prose. An implementer building `walk.mjs` from §2 and the output set from
§3 — the two places that define them — would have shipped no exclusion at all. It is in
the output set now.

**CF6-2 — the sixth consecutive partly-true completion claim.** Round 4's `C-R4-3` was
filed against §3.3's restated `unbuilt` remedy and dispositioned `fixed`; two rounds
later §3.3 still carried it. §3.3 now cites §4 rather than restating.

**OP6-1, OP6-2** — §4 banned `npm run bundle:cli` and then prescribed it as recipe step 4. The lens traced it and found the step genuinely **safe** — the ban's cause is
`.dts-cache` absence, and step 3 has just written that cache — so the fix was one clause
saying why, not a reorder. And the grouped `stale-unit ×N` line named only a count, which
makes step 1 unrunnable: the operator cannot `rm -rf` units nobody listed. It names them
now, matching what the success path already did.

ℹ️ **Six rounds, fifty findings, and a pattern worth stating plainly.** In four of six
rounds a fix in this design carried a defect, and three of those were caught only because
the fix was RUN rather than reasoned about. The design is 1000+ lines, seven finding
kinds, five comparisons — and the recurring failure has been a rule keyed on a proxy for
the thing it means. That is a property of the FULL design the owner selected, not of any
one round, and it is recorded here so the design-gate decision can weigh it.

**Round 7 (2026-08-20) — three `revise`, ten findings, three high. One is a REGRESSION
this design introduced in round 6, and one finally names the right oracle.**

**CR-1, high — round 6's `unbuilt` precondition REVERTED round 3's `CR-2` fix.** Round 6
required "at least one non-excluded entry" so that a test-created empty
`packages/journal/dist` would not report `unbuilt`. But `tsc -b --clean` deletes files and
leaves the directory, so after `npm run build:clean` **all 19 units hold zero entries**,
therefore zero non-excluded entries, therefore not `unbuilt`, therefore **clean** — the
exact state round 3 called "the worst available answer for the incident this check is
named for".

⚠️ **A count cannot carry the distinction, because a never-built tree and a `--clean`ed
tree both hold zero entries.** The rule is now keyed on WHY the directory is empty:
`.tsbuildinfo` alone is positive proof the unit _was_ built, so it is `unbuilt`; only
scratch means a test `mkdir`ed it and nothing was ever built, so it is `skipped`.

**CR-2, high — the chunk proves esbuild ran, not that the bundle completed, and esbuild
is step 2 of 5.** Verified order in `bundle-cli.mjs`: `:121` `build()` writes the chunks,
`:147` throws if `.dts-cache` is missing, `:153` copies the declarations, `:178` copies
the six plugin entries, `:181` reads the metafile. A fresh clone has no `.dts-cache`, so
`npm run bundle:cli` throws at `:147` — leaving chunks and entries present, and **no
declarations and no plugin assets**. Every comparison passes or skips, and the check
reports PASS on a `packages/cli` that would publish incomplete.

**So the oracle is the LAST artifact `bundle-cli.mjs` writes**, and the design now
requires the metafile write to be placed after the plugin copy. That is the fifth form of
this rule, and the reason the first four failed is identical each time: **they proved
something upstream of the thing that matters.**

| #   | oracle                        | proved                     |
| --- | ----------------------------- | -------------------------- |
| 1   | directory exists              | nothing                    |
| 2   | any output present            | `tsc` ran                  |
| 3   | entry outputs present         | `tsc` ran                  |
| 4   | an esbuild-only chunk         | esbuild ran — step 2 of 5  |
| 5   | **the last artifact written** | **the whole sequence ran** |

**CR-3, medium — mtime cannot see a partial plugin copy.** Interrupt the six-entry loop
after `agents/` and `dist/plugin` exists with a copy-time mtime, so the comparison reports
clean while the shipped CLI has no skills. Subsumed by the completion-marker rule, and
stated anyway.

**CF-1, high — §3.3 contradicted itself in two lines**, and §6's row 9 mandated the
refuted behaviour: an implementer who passed row 9 had not implemented the round-6 rule,
and one who implemented it failed row 9.

**CF-2, CF-3 — the wiring assertions copy one third of the shape they cite**, and the two
edits to EXISTING files are in no scope list. `pretest` — which §5.1 calls the ONLY
trigger that can fire on a stale tree — has no assertion anywhere; an implementer could
omit it, pass all 12 battery rows and the wiring test, and ship a check that never runs
automatically. Likewise `bundle-cli.mjs`'s metafile write and `.gitignore`'s entry exist
only in prose, so an implementer builds the six new files, passes everything, and leaves
requirement 3's comparison **permanently muted** — the vacuity class this change set
exists for.

**OP-1, OP-2 — `orphan-output` also printed `undefined … NaN`** (three kinds without a
delta, not two), and the `unbuilt` line said "holds no compiler output" in precisely the
case where `dist` holds four files. Round 6 changed the rule and not the sentence.

ℹ️ **Seven rounds, sixty findings.** Rounds 6 and 7 each produced a materially better
rule rather than another patch — the proxy diagnosis, then the completion-marker oracle —
but round 6's own fix also reverted round 3's. **A fix that narrows one failure can
re-open another when both are governed by the same predicate**, and neither round noticed
because each tested its own scenario and not the other's. That is the argument for the
battery rows CF-1 exposes as contradictory, and for treating this design's size as a
finding in its own right at the gate.

**Round 8 (2026-08-20) — three `revise`, fifteen findings, four high. Standing rule 8
fired on BOTH of round 7's fixes, exactly as it predicts.**

**C8-2, high — the completion marker was never invalidated, so it proved that SOME build
once finished.** `bundle-cli.mjs:63` sets `OUT_DIR` to `dist`, and the wipe at `:113`
iterates that directory only — so `packages/cli/.bundle-meta/`, a **sibling** of `dist`,
survives both the wipe and the design's own `rm -rf packages/cli/dist`. Round 6 had
already recorded "a stale `.bundle-meta/` that `rm -rf dist` never touched", while the
marker was not yet the oracle; round 7 promoted it without changing that. On a
previously-built tree, `rm -rf packages/cli/dist && npx tsc -b packages/cli` then yields
three entries, no chunks, no `dist/plugin`, a surviving marker — and **PASS, exit 0**.
Sixth appearance of the founding failure, introduced by the fix for the fifth. The marker
is now **cleared in the wipe step** and written after the plugin copy, so it marks the
build it belongs to.

**C8-1, high — the reason table could not see the reason.** Round 7 made an empty `dist`
`unbuilt` on the reasoning "it was built once and the outputs are gone". But both journal
fixture families `mkdir(SCRATCH_ROOT)` and then `rm(dir, …)` — the **mkdtemp directory,
not the root** — so a fresh clone that has run `npm test` once has an EMPTY
`packages/journal/dist`, and the next `pretest` reports `unbuilt` on a tree nobody built.
CR6-2, re-opened by the fix that replaced CR6-2's fix.

⚠️ **And nothing at unit granularity distinguishes the two states** — `tsc -b --clean`
deletes `.tsbuildinfo` too, so a `--clean`ed unit and a never-built one are byte-identical.
The empty case is therefore `skipped`, and `build:clean` is **accepted as indistinguishable
at unit granularity** and stated as a residual. It is not undetectable overall:
`packages/cli`'s marker lives outside `dist` and is now cleared per build, so a `--clean`ed
tree still reports `unbuilt packages/cli`.

**CF8-1, high — and this is the ninth consecutive partly-true completion claim, inside a
fix written to end that pattern.** Round 8's own §1 addition said "§6 asserts both by
reading the files". §6's entire wiring block was one `toContain` line. The assertions are
in §6 now — `toBe` on both scripts, a source-order check that the metafile write follows
the plugin copy, and a `.gitignore` read — in the three-part shape
`check-support-window-freshness.test.mjs:530` uses, which §6 had been citing while copying
one third of it.

**CF8-2 — the same rule change broke two battery rows nobody looked at.** Row 10's single
mutation mandated one kind while §3.3 mandated two, and row 11's "clean" fixture had no
metafile, so it was already `unbuilt` and the mutation flipped nothing — **vacuous**, the
defect class this change set exists for. Both fixed, and §3.3 now states precedence:
`unbuilt` wins, because it names the tree's state while `bundle-provenance-missing` would
say the check is degraded when the bundle is.

**CF8-3, CF8-4, CF8-5, CF8-6, CF8-7, C8-4, O8-1..O8-3** — `Unit.built: boolean` could not
carry a three-way verdict and is now `state`; §3's authoritative skip bullet still stated
the round-1 rule while the rule it consumes had moved four times 220 lines away; `pretest`
had **no assertion anywhere** despite §5.1 calling it the only trigger that can fire on a
stale tree; and §6's live prediction omitted `unbuilt packages/cli`, which is also the
**day-one migration** — every developer would see it on a correctly built tree until the
first build writes the marker, so a _missing_ marker on an otherwise-complete `dist`
reports `bundle-provenance-missing` with a migration note instead.

**C9-2, high — §3 excluded a second artifact and pointed at a section that never read
it.** `dist/index.d.ts` was excluded from `packages/cli`'s output set with the pointer
"handled as its own artifact in §3.3", while §3.3 compares against
`.dts-cache/index.d.ts` only — **no comparison in the design read the shipped copy at
all**. Re-derived from §4's own recipe: run step 3 (`bundle:types -- --force`) and stop,
and the cache is fresh, `stale-declarations` clears, the marker and `bundleAt` are
untouched, and the check prints PASS with the published `types` entry stale. CF-2's shape
at the second excluded artifact. Closed by §3.5's sixth comparison — and keyed on BYTES,
not mtime, because `bundle-types.mjs:32-38` records that `tsc -b` clobbers that exact file
with a barrel AND refreshes its mtime, which `check-install-smoke.mjs` caught as a real
shipped defect. The proposed mtime predicate would have been the fifth tsc-writable proxy
in this design, introduced by the fix for the sixth recurrence.

**Round 9 (2026-08-20) — CF9-2, high.** §6's battery row 10 and §6's own day-one
migration rule mandated OPPOSITE verdicts for one tree state, 44 lines apart, and the
migration rule's escape clause ("once the marker has existed and gone — which the
per-build clear makes observable") is unimplementable: the check sees one filesystem
state, not a history. Round 8's precedence rule then made `unbuilt` win in every reachable
case, so `bundle-provenance-missing` had **zero** non-vacuity rows and no reachable state
— a kind hardcoded never to fire, created by the fix that removed its only row. Resolved
by moving completeness off the marker onto the artifact set (`:121` chunk + 3 entries,
`:153` `dist/index.d.ts`, `:178` six plugin entries) and leaving the marker as §3.2's
provenance record: the two kinds are now disjoint by construction and need no precedence.

⚠️ **Round 8's disposition said "§3.3 now states precedence"; the text went into §3.2**
(`grep -n precedence` → row 10's citation and `:328` only), so row 10 cited a rule its own
section did not contain — the tenth consecutive partly-true completion claim, and the
third whose cause was the cross-reference rather than the rule.

**Round 9 (2026-08-20) — `C9-1`, high: the SEVENTH appearance of `PASS` on a broken tree,
and the first where the fix could not run at all.** Round 8 invalidated the completion
marker by clearing it in `bundle-cli.mjs`'s wipe step. But `build` is
`tsc -b && npm run bundle:types && npm run bundle:cli` (`package.json:15`), so an interrupt
during the ~5-minute `bundle:types` step means `bundle-cli.mjs` never executes and the clear
never fires — reached from this design's own printed remedy. Measured:
`rm -rf packages/cli/dist` then `tsc -b packages/cli` writes exactly `dist/bin.js`,
`dist/index.js`, `dist/index.d.ts`, `dist/bin/supervisord.js` and `.tsbuildinfo`; the
previous build's marker survives beside `dist`; no chunks, no `dist/plugin` — **PASS,
exit 0**.

⚠️ **The lesson is new, and it is not "another proxy".** Forms 1-4 failed by keying on
something the wrong tool also writes. Form 5 keyed on the right artifact and failed because
its INVALIDATION lived inside the tool that did not run. The marker is now a **freshness**
oracle — present AND no older than the newest qualifying compiler output under
`packages/cli/dist` — and the write-after-copy placement is what makes that invariant true
after every completed bundle and false in every failure class.

⚠️ **Standing rule 8 fired three times while fixing it.** Reading the comparand as raw
`dist/**` would (i) re-open §7 row 1 — measured on a composite fixture with the pinned
`typescript@6.0.3`, `touch src/a.ts && tsc -b` moves `dist/.tsbuildinfo` and no compiler
output — and (ii) vacate §6 row 7, whose entire mutation is touching
`dist/plugin/.mcp.json` to now. And replacing round 4's entry-output clause with freshness
would re-open `C-R4-1`, because a deletion LOWERS the newest output mtime and makes the
marker look more current. The comparand is §3's filtered output set, and the rule is a
conjunction of three clauses, none of which replaces another.

ℹ️ **One live inconsistency was found by the site walk rather than by a lens**: §2's `Unit`
comment still declared `built: boolean` two lines above the round-8 note stating it is "NOT
a boolean" — `CF8-3`'s fix reached the note and not the type. The tenth partly-true
completion claim in this record, and the first caught by walking every site a finding
governs before writing any of them.

### Owner ruling, 2026-08-20 — keep the full design

⚠️ **The counts below are the ones put to the owner AT ROUND 8's CLOSE and are not restated
as current (round 12).** Rounds 9, 10, 11 and 12 followed the ruling and are logged above and
below this subsection; the ruling itself — keep the full design and keep iterating — is
unchanged by them. The manager put the accumulated evidence to the owner: eight rounds, seventy-five
findings, one failure recurring six times with each fix introducing the next, and nine
consecutive partly-true completion claims. The alternatives offered were narrowing to the
single bundle-freshness comparison, going to the gate as-is, or splitting the work.

**The owner ruled: keep the full design and keep iterating.** Recorded here rather than
argued again — the concern was raised with its evidence and reaffirmed, which makes it the
owner's call. What the rounds have produced is real: five successive oracles ending in one
that is right for a stated reason, and a design that now names its own residual limits
rather than discovering them.

**Round 9 (2026-08-20) — C9-3/O9-2, medium — the day-one migration rule needed a history
the check does not have.** Round 8 made a missing marker report `bundle-provenance-missing`
"until the first build after adoption writes it", but the check holds no state. Measured
2026-08-20: `packages/cli/.bundle-meta` does not exist while `dist` holds all three entry
outputs, `index.d.ts`, the six plugin entries and five chunks — so this tree is already in
that state, permanently indistinguishable from a build interrupted between `:178` and
`:181`. And §4, authoritative for output, carried no migration wording, so an implementer
following battery row 10 would print `unbuilt packages/cli` with the remedy
`rm -rf packages/cli/dist` to every developer on day one. The temporal qualifier is dropped:
an absent marker is `bundle-provenance-missing`, advisory, unconditionally; `unbuilt` comes
from C9-1's freshness rule.

⚠️ **Standing rule 8 fired again, and is answered in the fix.** Removing the marker from
the `unbuilt` predicate re-opens round 7's CR-2 unless that rule keys on `dist/index.d.ts`
(`:153`) and `dist/plugin/**` (`:178`) — the artifacts the `:147` throw leaves absent.
Sites changed: §1's why-cell and battery count; §2's `checkBundleFreshness` signature,
`readMetafile` and `ADVISORY_KINDS`; §3.2's absence bullet (precedence deleted); §3.3's
rule, "final form" summary, proxy chain, entry-output referent, `--clean` reason and BOTH
skip-rule sentences; §3.4's "subsumed by" clause; §4's line shape, remedy row and exit
table; §6's rows 10-11, new row 13 and the day-one paragraph; §7's new row; §8(i).

**Round 9 (2026-08-20) — CF9-4, high: the source-order assertion round 8 added to END
the vacuity pattern was itself vacuous, measured.** §6 read
`bundler.indexOf(".bundle-meta", bundler.indexOf("PLUGIN_ASSET_ENTRIES"))`
`.toBeGreaterThan(bundler.indexOf("PLUGIN_ASSET_ENTRIES"))`. `indexOf(needle, from)`
returns `>= from` or `-1`, so the comparison can only restate "found something"; and the
anchor is `PLUGIN_ASSET_ENTRIES`'s **declaration** at `bundle-cli.mjs:89`, not the copy
loop at `:177`. Round 8's own wipe-step clear at `:113` supplies the passing occurrence.
Measured on mutated copies of the real bundler:

- write placed right after `build()`, **36 lines BEFORE** the copy loop: **passes**;
- write **ABSENT ENTIRELY**, only the round-8 clear present: **passes**, and
  `expect(bundler).toContain(".bundle-meta")` passes alongside it.

So both bundler assertions were satisfied by a tree in which requirement 3's comparison
is **permanently muted** — verbatim the failure §1's existing-file table was added to
prevent, and the tenth consecutive partly-true completion claim. CF8-1's citation was
wrong too: `check-support-window-freshness.test.mjs:530` asserts `package.json`, so §6
was copying a shape that does not read a file at all.

**Fixed** with two DISTINCT fragments (the clear and the write are indistinguishable by
path alone), two-`indexOf`-from-zero comparisons in `run-e2e-suites.test.mjs:31`'s shape,
and three mutants that must FLIP the predicate — §6's own battery discipline, applied to
the wiring assertions for the first time. §0, §1, §3.2, §3.3 and §3.4 were reconciled in
the same pass, and §8(k) now states what source-order text still does not establish.

**Round 9 (2026-08-20) — CF9-1, high: the struct still declared the boolean its own
footnote refutes.** §2 read `built: boolean` eighteen lines above its own note asserting
`state ∈ compared | unbuilt | skipped`, "never a boolean" — and round 8's entry above
claims that field is "now `state`". The ℹ️ note above records how it was found: the site
walk, not a lens, and the tenth partly-true completion claim in this record. The
disposition is that the field is REMOVED, not renamed. A `Unit` carries no state flag of
any kind, because `buildUnits` never reads `dist/` and so cannot know one; the verdict is
`unitState()`'s alone, computed once by `checkStaleDist` (§2). §3's skip rule now states
WHICH comparisons each of the three states runs, because "which comparisons run" is the
thing `built ? compare : skip` gets wrong. Three further sites carried the old predicate:
§3.3's first table said an existing-but-empty `dist` is `unbuilt` while its own reason
table says `skipped`; §4's `unbuilt` line shape hard-coded a sentence that is false for
the cases rounds 6 and 8 added, and now prints `unitState()`'s reason verbatim; and §6's
battery exercised one of the three states, so a boolean implementation passed every row.
§6 now keeps the three apart with one fixture per state (§2), and the cost of the third
state is recorded as residual §8(h).

**Round 9 (2026-08-20) — CF9-3, medium: the wiring assertions could not see the wiring.**
§6 pinned `check:all` membership with `toContain`, which pins neither the member's
POSITION nor the `--strict` on the chained form. §5 requires index **0** — ahead of
`check:tarball` (member 12) and `check:install-smoke` (member 13), citing defect
`25-install-smoke-depends-on-local-dist-state` — so an implementer appending it last with
no flag passed every assertion and shipped a check that runs after the two members it was
ordered before and exits **0** on a stale tree: every assertion green, the wiring's entire
value gone. Now pinned by INDEX and by equality on the full member string (§5). And
`package.json` joins §1's existing-file table, which had listed two of the three files
this change set must edit while its three script edits lived only in §5/§5.1 — the same
shape as CF9-4's finding one paragraph above, at the third file.

**Round 9 (2026-08-20) — O9-1, medium: the coverage the check computed was never
printed.** `comparisonsSkipped` appeared exactly ONCE in this design — §2's signature —
had no line shape in §4 and no battery row in §6, so on a tree whose bundle provenance was
missing the operator read `PASS — 19 units compared, 0 skipped` while requirement 3's
comparison had not run at all. A count also cannot say WHICH comparison declined, nor
carry the reason §4 prints. Disposition: replaced by §2's
`ComparisonCoverage = { comparison, status, reason }`, returned by all four cli-scoped
comparisons; §4 gains the coverage line, printed on BOTH the PASS path and the findings
path; §6 gains the rendered-text rows, which assert `formatFindings`' output because a row
reading `result.coverage` goes green on the exact defect it exists to catch. `skipped`
stays UNIT-scoped: naming skipped UNITS and not skipped COMPARISONS is round 5's own fix
one level up.

**Round 10 (2026-08-20) — four findings, all re-derived and all `fixed`, cited by
identifier from §0, §1, §2, §3.2, §3.3, §3.4, §3.5, §4, §6, §7 and §8.** Recorded here in
round 12 (C11-7/CF12-2), because rounds 10 and 11 dispositioned at the sites they govern
and left §9 without an entry — so "all dispositioned" was true but uncheckable from the
record the gate reads. Measured before writing this: six IDs (`C10-1`, `C10-2`, `C10-3`,
`C10-4`, `O9-1`, `OP11-2`) carried **37** citations in the body against a §9 that stopped
at round 9.

- **C10-1, high — a `not-run` cause with no reason this design could spell.** All four
  cli-scoped comparisons are suppressed when `unitState("packages/cli").state ===
"unbuilt"` (§3.2), and on that tree NOTHING is absent, so a coverage entry keyed on
  absence had nothing to name. The tree is the most routine there is — §7's
  `npm run typecheck` row — and it printed one `unbuilt` finding and no coverage line at
  all, which §4's recipe step 5 reads as "all six ran". `not-run` now has exactly TWO
  mandated causes, the second carrying the verbatim reason `packages/cli unbuilt`,
  evaluated BEFORE any comparison inspects its own comparand.
- **C10-2, high — the design's one repo-internal import could not be taken.** §3.3's
  clause 1 and §3.4's source side both read `PLUGIN_ASSET_ENTRIES` from
  `scripts/bundle-cli.mjs`, but `:89` is a bare `const` (re-derived: `grep -n "^export"`
  returns `:71` `EXTERNAL_DEPENDENCIES` and nothing else), and the file's last statement is
  `await main()`, so the only import that DID resolve would rebuild `packages/cli/dist`
  from inside the check that observes it. Two further edits — `export` the const, and put
  `await main()` behind the ino entry-point guard `scripts/repo-census.mjs:382` uses —
  raising the file to **five** edits, of which §6 asserts **four source properties**. The
  import also resolves `esbuild` at load time: residual §8(n).
- **C10-3, medium — `readMetafile`'s `object | undefined` signature was an invitation to
  throw.** The marker is 404,198 bytes over 758 inputs written by a plain `writeFile`, so
  a truncated one is reachable on a tree where every artifact is complete. Absent OR
  unparseable ⇒ `undefined`, never a throw, with a second mandated reason
  `.bundle-meta/metafile.json unreadable`.
- **C10-4, medium — §3.3's clause 2 is conditional on the marker, and the design said so
  nowhere.** C9-1's freshness oracle is inert for exactly the window in which the marker
  is absent. Stated as the CONJUNCTION residual in §8(i) and pinned by a false-negative
  battery row that asserts the accepted verdict rather than a better one.

Round 10 also deleted `reduced` as unreachable (§2, §3.2), relabelled §8's tail
(i)/(j)/(k) after five bullets carried the label `(h)`, and appended battery rows 24-25.
⚠️ **And round 10's own bulk apply wrote editor directives into the design as document
text** — the finding that produced the standing lesson that an apply step needs a guard on
WHAT it inserts, not only on WHERE.

**Round 11 (2026-08-20) — 60 consolidated edits applied; `OP11-2`, high, plus four
author-side site walks, all `fixed`.** `OP11-2`: §4's internal-error row had no owner in
§2 and no row in §6 — `exitCodeFor` takes a `result` and a throw yields none, `kind` has no
error member, and §1 gave `check-stale-dist.mjs` "arg parsing, CLI" and no catch. Closed
by `formatInternalError(err, { strict })` in `report.mjs`, called from that file's
try/catch, with both line texts written out and §6 row 27 exercising it. The round's four
other subjects: the preamble's intra-document citation rule, after
`grep -on '§[0-9][0-9.]*:[0-9]'` returned SIX anchors all resolving to the wrong text;
§8 gained **(l)** and **(m)**, the two round-2 dispositions that had reached no section for
ten rounds, and §0's requirement 2 and 9 rows gained the pointers that stop them claiming
unqualified discharge over a measured gap; §3 rule 4 gained its tsconfig arithmetic where
the input sets are DEFINED; §2's delta-less kinds are NAMED rather than counted; and §5's
self-referential `run check:all` grep was re-scoped. Battery rows 26-30 were appended.

⚠️ **Round 11's first apply CORRUPTED this document and the corruption was caught, not
shipped.** One edit's replacement text contained a dollar sign immediately followed by an
apostrophe, which JavaScript's `String.replace` expands to "everything after the match" —
it spliced the rest of the file back in and doubled it to **4499** lines. Reverted and
re-applied with a function replacement plus a line-delta guard. ⚠️ **The hazard is still
live in the document, and round 12 re-derived it:** three lines carry that sequence inside
backticked shell commands and one carries a dollar-backtick, so every future apply
touching them must use a function replacement — now a standing rule in this file's
preamble. Round 12 checked the CURRENT document for residual damage of that class — no
duplicated headings, no duplicated long line, no placeholder standing in as document text
— and found none. ⚠️ **Two of those four clauses did not hold as written (round 13,
CF13-6/OP13-5).** The line count it published, **2574**, was the count at the PREVIOUS
commit: `git show 46a1d5c:<file> | wc -l` → 2574 against `git show c18ebff:<file> | wc -l`
→ 3092, so round 12's own commit was **518** lines longer than the figure it offered as a
re-derivation of the current document — `C11-4`'s pattern a fourth time, in the baseline of
the doubling detector itself, and the one clause of the four that would have caught round
11's own doubling. And the placeholder clause missed §3.3's `indistinguishable — see below`
reason cell (OP13-2). The count is deleted rather than corrected, per `C11-4`; the other
three clauses reproduce.

**Round 12 (2026-08-20) — `contract-fit` / `correctness` / `operability`, seventeen
findings: `CF12-1`, `CF12-2`, `CF12-3`, `CF12-4`, `C11-1`, `C11-2`, `C11-3`, `C11-4`,
`C11-5`, `C11-6`, `C11-7`, `OP12-1`, `OP12-2`, `OP12-3`, `OP12-4`, `OP12-5`, `OP12-6`.**

- **`CF12-1`** — §0's requirement 9 row carried an unescaped `|` inside a code span, so
  GFM truncated the cell and dropped the whole bounded-residual statement round 11 had
  just added. The preamble gains an escape-aware row census; the naive form reports a
  correctly escaped row as malformed and so can never return nothing.
- **`CF12-2`** — the round-11 citation rule was enforced only against `§N:line` anchors, so
  a NAME or an ID that resolves to nothing passed it. Two more preamble passes, both
  scoped below `## 0.`; four `PASS-line rule` citations renamed; this section's missing
  entries written.
- **`CF12-3` / `C11-1`** — requirement 9's numerator was the enumeration's return size
  (29), not the covered set (21); nine configs are uncovered, not one.
- **`CF12-4`** — §1's five `bundle-cli.mjs` edits were summarised as four, and §0 and §3.2
  still said two.
- **`C11-2`** — the `--clean` residual was bounded by a scoped measurement read as
  unscoped: `npx tsc -b packages/cli --clean --dry` lists **4,781** deletions across 17
  packages (the `packages/cli/` subset is the five named), and the root `build:clean`
  **5,051** across all 19 units. `build:clean` is also caught by clause 1, not clause 3
  alone.
- **`C11-3`** — §4's `eighteen times over` example described a state that yields 18
  `skipped` and ONE `unbuilt`, and the line printed there blames a step the operator never
  ran; §3.3's cli reason table gains the `--clean` signature's own reason.
- **`C11-4`** — the unfiltered `check:all` figure was wrong at the commit that wrote it
  for the third time (measured: filtered 6, unfiltered 10, design said 9) and is deleted
  rather than corrected.
- **`C11-5`** — `--json` plus an internal error emitted a human line onto the JSON stream:
  the failure `bundle-types.mjs:63-68` records this repository shipping.
- **`C11-6`, advisory** — clause 1's chunk member was an at-least-one test over a set §3.2
  takes a minimum across. Measured: `bin.js` → `chunk-FRJGAF5Y.js` → three more chunks, so
  deleting three of four leaves a PASS on a `bin.crabgic` that dies with
  `ERR_MODULE_NOT_FOUND` — the eighth appearance of the founding failure.
- **`C11-7`, advisory** — this section, which had no entry for rounds 10 or 11.
- **`OP12-1`** — §4's internal-error row cannot see a LOAD-TIME throw: a static ESM graph
  is linked before the entry module's first statement runs, so the catch is not on the
  stack. Measured on this design's own shape: `ERR_MODULE_NOT_FOUND`, a ten-frame stack,
  no prefixed line, exit **1** with `--strict` AND without — under `pretest` a total block
  on `npm test`, round 1's `O-2` re-opened by omission. §1 gains an IMPORT RULE.
- **`OP12-2`** — §5.1's honest bound named ONE bypass; npm's pre-hook is name-exact and
  this repository declares four `test:*` siblings, so the measured set is **five**, and
  `test:watch` is the founding incident's own context.
- **`OP12-3`** — §5.1's closing argument still read "immediately above the failures" 25
  lines below the paragraph measuring it at ~199 lines; rounds 2 and 3 both dispositioned
  it `fixed` and both verifications read the measuring paragraph rather than the argument.
- **`OP12-4`** — §4's ordered remedy plan had no owner in §1 or §2, no position in the
  prefix rule and no battery row for twelve rounds.
- **`OP12-5`** — the internal-error line carried no locus (`JSON.parse` messages name no
  file) and told the operator nothing about the re-run or the 14 members that did not run.
- **`OP12-6`** — the `String.replace` hazard is a property of the apply step, now stated
  as document text rather than as one round's remedy.

Battery rows **31-34** were appended, nothing renumbered.

**Round 13 (2026-08-20) — `contract-fit` / `correctness` / `operability`, sixteen findings
consolidated across the three lenses: `CF13-1`, `CF13-2`, `CF13-3`, `CF13-4`, `CF13-5`,
`CF13-6`, `CF13-7`, `C13-1`, `C13-2`, `C13-3`, `C13-4`, `OP13-1`, `OP13-2`, `OP13-3`,
`OP13-4`, `OP13-5`. Entered here in the SAME round that filed them, which is the
preamble's own standing requirement and the thing rounds 10 and 11 did not do.** Six of
the sixteen are round 12's own new reproducible checks failing on their first writing,
which is now true of every check this document has added.

- **`C13-1`, high — the founding failure's NINTH appearance, at the clause added to close
  its seventh.** §3.3's cli reason table partitioned on (members present) × (marker
  present) and carried no row for a marker that is present but OLDER than the newest
  §3-filtered output — the state clause 2 exists for, and §6 row 19's own fixture. §2 and
  §3 both named that table as `unitState`'s authority, so an implementer built from it
  returns `compared` there; every comparison then runs clean and the check prints **PASS,
  exit 0 even under `--strict`** on a `packages/cli` that can neither publish nor run its
  own `bin.crabgic`. §2 reinforced the wrong branch three ways in one paragraph:
  `unitState` documented as reading "PRESENCE facts, not mtimes"; a two-outcome branch
  list with nothing for gaps-empty-and-marker-stale; and `cliArtifactGaps(cwd)` against
  the two-argument signature round 12's `C11-6` introduced and did not carry to its
  caller. Fixed at six sites: §2's signature comment, its `unitState` prose and its
  outcome list, §3's Skip rule, §3.3's cli table (one row split into two) and §4's
  clause-2 line.
- **`CF13-1`, medium — the design has TWO repo-internal imports and §1 said "exactly
  one", and the second one IS requirement 11.** §2 mandates that `units.mjs` import
  `enumerateRootReferences` and `enumerateTsconfigs` from `scripts/repo-census.mjs`, and
  §0's requirement 11 row discharges the requirement with exactly that. An implementer who
  honoured §1's count satisfied it by re-deriving the unit list — verbatim what
  requirement 11 forbids — and passed every §6 row, because nothing in §6 reads that
  import list. The dependency claim survived only by luck: re-derived 2026-08-20,
  `repo-census.mjs` imports `node:fs`, `node:path`, `node:child_process`, `node:url` and
  nothing else, so §8(n)'s `esbuild` bound is over two modules and holds for one of them
  as a fact about today's file. §1, §0's requirement 11 row, §2 and §8(n) at three points
  now say so.
- **`C13-2`, medium — a SUBSUMPTION stated unconditionally when the clause doing the
  subsuming is conditional.** §7 row 4 said clause 2 subsumes the tsc-barrel row, so
  neither `stale-shipped-declarations` nor `stale-bundle` fires. Clause 2 is
  marker-conditional, and the marker is absent on every tree until the first build after
  this lands — measured 2026-08-20. There the unit stays `compared`, §3.5 RUNS, and
  `stale-shipped-declarations` fires NON-advisory at exit **1** under `--strict`, with a
  remedy the cell denies. §8(i) already stated that branch, so the design contradicted
  itself at two sites round 10 wrote in one pass, and the site an operator reads was the
  wrong one. §4's line for the kind also printed a diagnosis false in that branch ("the
  cached declarations were never copied in") — the OP-2 failure §4 files two bullets below
  its own line list — and is now cause-neutral. §6 row 22 gains the marker-absent twin in
  row 33's shape, so no row is renumbered.
- **`CF13-4`, medium — §4 twice cited a precedence rule §3.2 and §3.3 abolished in round 9.** Both sections state the opposite in terms, so the citation resolved to its own
  denial; the rule §4 means is §3.2's `unbuilt` suppression (round 10, `C10-1`). Round 9's
  own site list claims it reached "§3.2's absence bullet (precedence deleted)" and "§4's
  line shape" — the eleventh partly-true completion claim, and `CF9-2`'s shape at a site
  round 9 recorded as done. The same line was also a splice: a full stop followed by an
  em-dash continuation whose anchor sat four lines above, the round-9 paragraph having been
  inserted between them — round 10's "guard WHAT an apply inserts" at a live site. Fixed in
  §4, §3.2's rule NAMED so a citation to it can resolve, and the word swept out of §3.5's
  paragraph heading and §6's row 10, so every surviving occurrence in §0-§8 is a negation.
- **`OP13-1`, high — the findings path names no skipped unit anywhere in human output.**
  §4's exit table prints the `PASS` line on the no-findings and advisory-only rows only,
  and the prefix rule says "the `PASS` line when there is one", so on any run holding ≥1
  non-advisory finding the skipped units survive only in `states` and `--json`. Three
  sites justified emitting no coverage entry by pointing at that slot — §3's coverage
  bullet, §3.1's orphan bullet, and §6's false-negative battery, whose fixture holds no
  findings at all. §4's own worked example is the counterexample: after
  `npm run build:clean`, **18** `skipped` units and exactly ONE `unbuilt` finding, and the
  operator is told about one package. Round 9's `O9-1` fix made the coverage line dual-path
  and then rested the unit table on a line the other path never prints — `O9-1` one level
  over, produced by `O9-1`'s own fix. Fixed by giving the units-compared/skipped summary
  the same "both paths, never one" rule; §4's rule renamed **SKIPPED UNITS ARE NAMED ON
  BOTH PATHS** with all four citations repointed; §6 row **35** is the mutant-proved row.
- **`OP13-2`, high — an editor's cross-reference standing in as an operator-facing reason
  string.** §3.3's unit-state reason table gave an existing-but-empty `dist/` the whole
  reason `indistinguishable — see below`, while §4 prints `unitState()`'s `reason` VERBATIM
  and §6 row 30 forbids the reporter inventing one — so the design's own rules route a
  pointer into a design file onto `pretest` line **5** of 204. Round 10's lesson at a new
  site, one promotion further out. The cell now carries a printable reason. ⚠️ **The row
  SPLIT the finding proposed was rejected in consolidation**: the reason table's domain is
  "what `dist` holds", the ABSENT-`dist/` case is decided by §3.3's unit-state table one
  table above, and adding a row for it would duplicate that verdict — `CF9-2`'s shape — and
  renumber a table §6 rows 13 and 14 and §7 cite by row number. §4's "three `skipped` rows"
  is corrected instead: three STATES, two of them reason-table rows.
- **`OP13-3`, medium — "grouped" stated for every kind, shaped for one.** Four kinds carry
  per-finding text in their §4 line and grouping discards it. Two `unbuilt` findings with
  reasons from DIFFERENT tables is a state §4 itself names as reachable, and no battery row
  renders two `unbuilt` findings at once, so a grouping formatter passed the whole battery.
  §4 now partitions the eight kinds; §6 row **36** is the row. The site walk also found
  §6's prefix-failure count reading "seven" against a group of **twelve** rows one sentence
  above it — round 12 appended 31-34 without touching the figure — so the count is replaced
  by the SET, per `C11-4`, rather than by another number.
- **`CF13-5`, low — the registry that exists to protect §6's row numbers was wrong in both
  directions.** It asserted "§3.3's clause 1 cites 33" while `grep -n "row 33"` returned
  nothing anywhere in the file, and omitted §1 and §2 from rows 31 and 34. §3.3's clause 1
  now carries the citation, in the shape §4 uses for rows 32 and 34, and the bullet quotes
  a `grep` rather than restating it.
- **`C13-4`, advisory — "the ONLY row" was false by one from the round that wrote it.**
  §6 row 19 claimed to be the only row a presence-only marker implementation fails. Row 26
  starts from row 19's own end state and requires four `not run (packages/cli unbuilt)`
  coverage entries, which such an implementation renders as four `full` runs — so it fails
  too. Corrected to name both, which also states the battery's coverage of exactly the gap
  `C13-1` names, the reassurance a reader would otherwise take from the exclusivity claim.
- **`CF13-2` / `C13-3` / `OP13-4`, medium — pass 1's regex could not see round 2's entire
  slate, filed independently by all three lenses.** The numeric-suffix form misses `C-A`
  through `O-C`: **19** such citations are live below `## 0.`, five of them in the body
  above §9 and one of those inside §0's requirement rows — every figure in this bullet
  measured against the document round 12 left, `git show c18ebff:<file>`, because this
  round's own apply invalidates a bare count in the commit that writes it. Mutant: strip four of them from
  §9 alone and the pass prints nothing while the condition it claims to enforce is
  violated. Widened to `([0-9]+|[A-Z])` — 82 IDs became **91** there, still returning nothing and
  four `DANGLING` lines on the mutant, and nothing on this document after this round's
  apply. `[0-9A-Z]+` was measured against the same
  document, matches the identical 91, and was rejected in consolidation: it would also
  match a hyphenated ALL-CAPS token and dangle forever, which is a pass that cannot return
  nothing.
- **`CF13-3` / `OP13-4`, medium — pass 2 inspects 5 citations out of 278.** It matches the
  BOLD form only, while the unbolded form occurs **272** times below `## 0.`; renaming
  `IMPORT RULE` inside §1 alone leaves five dangling citations and pass 2 silent. The
  greedy widening the finding proposed was measured first and rejected: with `grep -oE "§[0-9](\.[0-9])?'s [^,;.)]+"` it yields 237 extractions and **193**
  UNRESOLVED lines on the unmodified document, so it can never return nothing — round 11's
  lesson at a third site. (The finding reported 184; re-derived at 193, standing rule 2 on
  a reviewer's own figure. The conclusion is unchanged and the number now travels with the
  regex that produced it.) Closed instead by narrowing the CONVENTION to what a machine can
  resolve (a cross-section rule name is BOLD or ALL CAPS) and adding pass 3 over the
  ALL-CAPS form, which returns nothing here and flips on the mutant. ⚠️ **Consolidation
  found a THIRD narrowness no lens named: `grep` is line-oriented, and one live bold
  citation below `## 0.` WRAPS across a newline** — §2's `ComparisonCoverage` comment — so
  the prefix occurs ONE time more below `## 0.` than the full pattern matches. The
  DIFFERENCE is the reproducible form, not either count, because both move with every
  citation a round adds; re-deriving both and subtracting is what makes the gap visible. Both residuals are
  stated in the guard: a citation naming no rule at all is seen by no pass, which is how
  `CF13-4` survived, and a bold citation that wraps is seen by no pass either.
- **`CF13-6` / `OP13-5`, low — the corruption guard was baselined at 2574 lines against a
  3092-line file**, the count having been taken before round 12's own 60 edits:
  `git show 46a1d5c:<file> | wc -l` → 2574, `git show c18ebff:<file> | wc -l` → 3092. It is
  the sub-claim that would have caught round 11's doubling. ⚠️ **The proposed remedy of
  writing the CURRENT count was rejected in consolidation** — this round's own apply makes
  any literal written here stale in the same commit that writes it, which is verbatim the
  defect being filed. Deleted instead, per `C11-4`; if a count is ever restored it carries
  its `git show <sha>` command. The other three clauses re-derive clean and carry no
  number, except that the placeholder clause was itself false until `OP13-2` landed.
- **`CF13-7`, low — §5's `check:all` census split 9 files as "4 prose comments, 4 test
  assertions"; re-derived, it is 6 and 2.** Both assertion files use the `toContain` shape
  §1 calls "membership only, blind to position and to flags", so the figure overstated by
  2x how much of `check:all`'s membership any test pins, in the section arguing that §6
  must pin member 0 by index and equality — the fourth recurrence of round 1's `O-4`, in
  the block round 12 had just rewritten for that exact reason. The nine files are now
  enumerated, so a fifth drift is detectable rather than merely likely.

ℹ️ **What round 13 re-derived and could not break:** the anchor grep (nothing below
`## 0.`, exactly its two quoted counterexamples over the whole file), the escape-aware
row-width census (nothing; the naive form still flags §0's requirement 9 row, which is the
document's own account), pass 2 as scoped (nothing on its five occurrences), pass 3 as
newly written (nothing, over §1's IMPORT RULE), the four `String.replace` special
sequences (still exactly four — three dollar-apostrophes and one dollar-backtick), the
three structural clauses of the corruption guard, and the widened pass 1 against the whole
91-ID set. No working-tree mutation: every probe read files as text or ran on a copy under
the scratchpad, and `scripts/bundle-cli.mjs` was never imported.

Battery rows **35** and **36** were appended, nothing renumbered.

**Round 14 (2026-08-21) — `contract-fit` / `correctness` / `operability`, fourteen
findings consolidated across the three lenses: `CF14-1`, `CF14-2`, `CF14-3`, `CF14-4`,
`C14-1`, `C14-2`, `C14-3`, `C14-4`, `C14-5`, `OP14-1`, `OP14-2`, `OP14-3`, `OP14-4`,
`OP14-5`. Entered here in the SAME round that filed them, which is the preamble's own
standing requirement. TWELVE of the fourteen are SUBSTANCE — the algorithm, its output,
its remedies and its battery — and only two touch this document's own self-checks, which
reverses rounds 11-13's trend and is what this stage was waiting on.**

- **`C14-1`, high — the founding failure's TENTH appearance, produced by the fix for its
  ninth.** §2's `cliArtifactGaps` branch list partitions the marker on `readMetafile`'s
  RETURN; §3.3's clause 2 and its cli reason table partition on the marker FILE.
  `readMetafile` is `undefined` for absent AND for unparseable (round 10, `C10-3`), so on
  a present-but-truncated marker §2 says `compared` plus one advisory at exit **0 even
  under `--strict`** while §3.3 says `unbuilt` at exit 1 — opposite verdicts and opposite
  exit codes on one reachable tree, on a `packages/cli` this section's own stated-cost
  paragraph calls one that "cannot publish and cannot run its own `bin.crabgic`". §3.3's
  table also promised "bundle comparison runs" for an input where it provably cannot.
  Fixed by keying every branch on the FILE, adding §2's missing FOURTH branch (three
  branches for four inputs is what let it stand), splitting the NOT-OLDER row on
  parseability — the ONE row in that table that does — and repointing §3.3's clause-1
  fallback, §3.2's unreadable-marker bullet, §8(i), §0's requirement 3 row and §1's
  `compare.mjs` cell. §6 row **37** is the row: row 19's marker parses and the
  false-negative battery's truncated marker is NEWEST, so no existing row discriminates.
- **`CF14-1`, high — §7 row 1 prescribed a remedy §4 measures as unable to clear the
  finding, and §4 names §7 row 1 by number while measuring it.** The cell read "Remedy is
  one `npm run build`" for the false positive the owner ACCEPTED; §4's table gives
  `stale-unit` the remedy `rm -rf <unit>/dist` **then** `npm run build`, and §4's own
  paragraph says in terms that a bare `npm run build` leaves the check firing "again,
  identically, forever … exactly §7 row 1's top false-positive scenario". Round 1's `O-1`
  fixed this at §4 and the disposition never reached §7 row 1 — standing rule 9, thirteen
  rounds running, at the one site that states the COST of the false positive that was
  accepted. Fixed by citing §4's table rather than restating a second remedy, and by
  recording the ×N cost §6 row 2's own mutation mandates. ⚠️ **The finding's freshly
  minted fixture timestamps were REJECTED in consolidation (standing rule 2):** they were
  not re-derivable inside this review's no-mutation envelope, and §4 already carries the
  same measurement, published and re-derived by earlier rounds.
- **`CF14-2`, medium — requirement 5's discharge rested on a fact nothing asserted, and
  the implementation it forbids passes every assertion in this design.**
  `enumerateWorkspaces` (`repo-census.mjs:270`) sits beside `enumerateRootReferences`
  (`:286`) and returns a STRICT SUBSET: re-derived 2026-08-21 by calling both on the live
  tree, 18 against 19, set difference exactly `e2e/report`. So a `units.mjs` importing it
  satisfied requirement 11's letter, passed §6's live smoke test (a MEMBERSHIP check,
  true of any subset) and every synthetic battery row, and never walked `e2e/report`,
  which holds **41** files under `dist` from **10** `.ts` sources. Blind spot 5, shipped,
  every assertion green. Fixed BOTH ways: the smoke test asserts EQUALITY, both sides
  live-derived so nothing rots; and §6's wiring block gains a mutant-proved read of
  `units.mjs`'s `repo-census.mjs` import STATEMENT. ⚠️ **The whole-file form of that
  predicate was rejected in consolidation:** a conforming `units.mjs` will very likely
  name `enumerateWorkspaces` in a comment explaining why it is not called, and a negated
  whole-file `includes` would then fail correct code — a search wider than its claim, at
  the assertion whose subject is scope. ⚠️ **Standing rule 9 fired on the fix itself:**
  §1's prose and §0's requirement 11 row both stated the ABSENCE of that assertion as a
  live fact, so both are repointed in the same pass.
- **`C14-3`, medium — §3 input rule 4 mandated a finding that is a false statement about
  the compared set.** `packages/cli/tsconfig.dts.json` sets `"composite": false` with
  `"references": []`, and its only tracked non-doc consumer is `bundle-types.mjs:84`,
  which hands it to `dts-bundle-generator --project` writing `.dts-cache/`; `tsc -b`
  reads `packages/cli/tsconfig.json` and never this file. Its only artifact under `dist`
  is `dist/index.d.ts` (`bundle-cli.mjs:153`) — §3's ONE exclusion from the mtime side.
  So rule 4 attached it to a comparison whose output side cannot move for it: `touch` it
  and the check reports `stale-unit packages/cli` and sends the operator to
  `rm -rf packages/cli/dist` plus a 19-unit rebuild, manufacturing a second kind that
  also defeats §4 step 2's carve-out. Rule 4 is narrowed to configs that emit into the
  unit's **COMPARED** output set — the raw-`dist` wording the finding proposed would have
  left the defect standing, since this config does reach `dist/index.d.ts` — and
  requirement 9's discharge for it moves to §3.3's comparand, which genuinely reads it.
  The count stays **21 of 30**; §8(l)'s DEFINITION of discharge moves with it, stated
  rather than letting the arithmetic drift to 20 (`C11-1`'s class). Rule 4 has no member
  today, which is written rather than hidden. §6 row 3 now mandates `stale-declarations`,
  and its fixture cell gains the cache clause without which the row would be vacuous.
- **`C14-5`, medium — a deliberate duplicate no section stated.** §3.2's input side
  repeats `checkUnitFreshness`'s comparand, and §3's skip-rule table exempts
  `packages/cli` from nothing, so `touch packages/cli/src/index.ts` reports BOTH
  `stale-unit packages/cli` and `stale-bundle packages/cli` from one fact, with two
  different remedy rows. §8(b) reasons about exactly this principle at the neighbouring
  site and the reasoning was never applied here. ⚠️ **Standing rule 8: the overlap is
  KEPT** — §3.2 is strictly tighter (min over chunks against max over outputs), so
  removing it trades one duplicate for a false negative on a partial chunk refresh. What
  changes is that it is stated where it is created (§3.2), carried as a §7 row so nobody
  mutes the second line, and paid for by §4 step 1's FOURTH carve-out: `packages/cli` is
  exempt from `rm -rf` when its only kinds are those two, because `bundle-cli.mjs:113-119`
  wipes `dist` itself — round 4's `C-R4-3` recorded that as an accident, and it is an
  accident only for the other 18 units. §6 row **38** pins both halves.
- **`C14-2`, medium — the `stale-declarations` remedy row made a universal claim from a
  two-input measurement.** "`npm run build` alone CANNOT clear this" was true for the two
  inputs §3.3 ADDS to the comparand and false for the thousands of `packages/*/src/**.ts`
  that dominate the kind: `bundle-types.mjs:70` skips only while the cache DOMINATES
  `newestSourceMtime()` (`:43-61`), which walks exactly that set, so a source-side trigger
  regenerates without `--force`. Round 1's `O-1` measured the two narrowly and this row
  generalised them. Fixed at the table. ⚠️ **The proposed step-3 carve-out was REJECTED in
  consolidation, and the rejection is recorded in the cell:** conditioning step 3 on
  `newerInput` breaks the run §4's own step-2 carve-out creates — a sole
  `stale-declarations` with a `packages/*/src` trigger is reachable (edit
  `packages/perf/src`, outside §3.2's inlined 16 and outside `packages/cli`'s closure,
  then `npm run typecheck`), and there BOTH steps would be skipped and the plan would
  emit no build step at all — and it makes §6 row 34's asserted step 4 unreachable on row
  34's own fixture. Standing rule 8 on the fix, twice, and the proposed battery row for it
  was vacuous besides: its fixture is the sole-kind case, where the correct plan also
  emits step 3. ⚠️ **BOTH of those trailing claims are FALSE, and round 34 (`OP34-1`)
  built the row the second one rejected.** Row 34's main half asserts no step at all —
  round 25's own `OP25-1` says so of that verdict cell — so the step-4 consequence named
  an assertion §6 does not carry; and the vacuity argument holds only of a
  PRODUCER-triggered sole-kind fixture, which is the one this round happened to have.
  On a sole-kind fixture whose trigger IS a `packages/*/src/**.ts` — the state this very
  entry measures as reachable, and §6 row 43's own end state since round 15 — the
  conditioned body emits NO step 3 while a correct plan does. So the rejected carve-out
  was falsifiable all along and this rule went twenty rounds with no element. §6 row 43's
  CLI twin's PLAN conjunct is it.
- **`OP14-1`, medium — the ordered remedy plan is gated on FINDINGS while degradation is
  carried by COVERAGE, so this design's own recipe step 1 prints a page with no next
  action.** `rm -rf packages/cli/dist` on an otherwise complete tree makes `packages/cli`
  `skipped`, leaves the other 18 clean, and yields ZERO findings with THREE `not-run`
  entries — requirement 3's among them. The plan is the ONLY element that prints recipe
  step 5 (re-derived: `grep -n "re-run"` returns step 5 and the internal-error line as the
  only re-run instructions anywhere), and §4's coverage bullet said the line "carries no
  remedy", so the whole human output is a `PASS` line, a coverage line and exit 0. A
  second reach needs no aborted recipe: `rm -rf packages/cli/.dts-cache` leaves the unit
  `compared`, two comparisons `not-run`, zero findings, and `npm run build` named by no
  remedy-table row. §8(j) stated only that a degraded run is not BLOCKED, and that was
  read as "nothing further is owed" for four rounds. Gated on "≥1 finding OR ≥1 non-`full`
  entry" and NAMED **A DEGRADED RUN CARRIES A NEXT ACTION**. ⚠️ **Standing rule 8:** the
  widened gate would print `npm run build` on every fresh clone and in both `meta-checks`
  legs, so the `nothing was checked` PASS is carved out explicitly at §4, §5 and §2. §6
  row **39** is the row: no existing row pairs a zero-finding fixture with a non-`full`
  entry, so every row before it passes a formatter that keeps the findings gate.
- **`OP14-2`, medium — the skipped summary was unbounded where §4 bounds every finding
  line.** §4 fixes a grouped shape for the four delta-carrying kinds and left this summary
  at "`unitState()`'s reasons printed the same way", with one worked example carrying ONE
  skipped unit. On §4's own `npm run build:clean` example — **18** `skipped` and one
  `unbuilt` — every skipped unit takes the same reason. Re-derived 2026-08-21 against the
  **19** root `references` and that literal **111**-character string: the per-unit form is
  **2,433** characters, **31** wrapped rows at 80 columns and **21** at 120, at line 5 of
  §5.1's measured 204 under `pretest`; grouped it is **512** and **7**. Round 1's `O-3` at
  finding granularity, reinstated one level up by `OP13-1`'s own fix. ⚠️ **The two lenses
  mandated CONTRADICTORY shapes here and were merged:** the accepted form is one clause
  per distinct reason written `<units>: <reason>`, which with a single unit renders
  `packages/cli: no dist/` — byte-identical to §4's existing PASS-line literal and to §2's
  `states` comment, so it generalises the shape and needs no fan-out. Row 35's fixture
  gains a ≥2-units-one-reason clause and the per-unit form as a second mutant.
- **`CF14-3`, medium — round 13's own new worked example was wrong on the count and on the
  unit for either tree its paragraph names, and named a unit/reason pair `unitState()`
  cannot produce.** It illustrated the findings-path summary as
  `check-stale-dist: 18 skipped (packages/cli: no dist/)`, called it "the `PASS` line's
  slot verbatim", and then offered a second shape. On the `rm -rf packages/cli/dist` tree
  the same rule gives the line as `18 units compared, 1 skipped` — and that tree carries
  no finding, so it never takes this path. On the `npm run build:clean` tree the sentence
  goes on to name, §4's own `C11-3` paragraph measures **18 `skipped` and exactly ONE
  `unbuilt` — `packages/cli`** — so `packages/cli` is a FINDING there and the 18 carry
  §3.3's reason-table row-1 string, never `no dist/`, which the unit-state table reserves
  for the ABSENT case. An implementer copying it emitted, on the only path an operator
  reads because something is already wrong, a count off by 17, the one unit that is not
  skipped, and a reason no `unitState()` branch returns — `OP13-2`'s defect class at the
  site `OP13-1` created, in the same round. Fixed together with `OP14-2` above.
- **`OP14-3`, low — the third carve-out was written at the step it SELECTS and not at the
  step it SUPPRESSES.** Step 2 was unconditional except for a sole-`stale-declarations`
  run, while step 4 carried "if `stale-shipped-declarations` fired **alone**, this step is
  the whole remedy". A plan assembled by walking steps 1-5 therefore printed
  `npm run build` and then printed step 4 saying steps 1-2 were unnecessary — one plan
  contradicting itself, two implementations a full `tsc -b` apart, and row 34's fixture
  discriminates neither. Step 4's reason was also false as written: step 1 is already
  conditioned on kinds that did not fire, so "steps 1-2 would delete" named a consequence
  that cannot occur. Written at step 2 in step 1's own shape, step 4's reason narrowed,
  and row 34 gains the sole-kind twin in row 33's shape.
- **`C14-4`, low — a not-run reason producible by no reachable tree.** §3.4 offered
  `packages/cli/dist/plugin` absent as a coverage reason. This comparison runs only on a
  `compared` `packages/cli`, whose clause 1 requires all six `PLUGIN_ASSET_ENTRIES` under
  `dist/plugin` — so whenever it is reached the directory exists, and whenever it is
  absent the unit is `unbuilt` and §3.2's suppression, evaluated first, makes the reason
  `packages/cli unbuilt`. The class round 10 deleted `reduced` for, left behind at a
  reason string when entry presence moved into clause 1, and §4's own rule ("A battery row
  asserting any other spelling fails an implementation that follows the sections") makes
  a surviving unreachable reason an invitation to write the row §6 row 16 already had to
  replace once. Deleted from §3.4 and from §4's verbatim list.
- **`OP14-4`, low — the internal-error `ERROR` line asserted `check:all` unconditionally.**
  `--strict` is not evidence of `check:all`: §5 prints `npm run check:stale-dist --
--strict` as the chain member and §5.1 confirms the script's own definition carries no
  flag, so an operator who copies that command and hits a throw was told other members had
  been blocked when nothing was invoked and nothing was blocked — a wrong diagnosis at
  exit **2**, which is the class §4 itself calls "what the founding incident was", and the
  check cannot observe its caller. The sentence is now conditional and set-valued. ⚠️ **The
  finding's arithmetic half was REJECTED (standing rule 2):** re-derived 2026-08-21,
  `check:all` has 14 members with `check:tarball` at 12 and `check:install-smoke` at 13,
  and §5 chains this check FIRST "making it the **15th**", so `member 0 of 15 … the
remaining 14` is CORRECT for the state this design creates and the drift would need a
  sixteenth member. The count is dropped as drift-proofing per `C11-4`, not as a
  correction, and §5's sentence resting on it is repointed in the same pass. §6 row 27
  pins the shape and the absence of an `of <N>` count.
- **`OP14-5`, advisory — the `⚠️` in the coverage line is the FIRST glyph any `check:all`
  member writes to stdout, and it can never degrade.** §4 mandates the label verbatim and
  §6 rows 16, 17 and 26 make prefix, label and entry ONE contiguous substring, so no
  implementation may resolve a presentation profile. Re-derived 2026-08-21 with
  `grep -nP` for the emoji ranges across all 14 member scripts: **7** hits in **4** files
  (`check-marketplace-pin-digest.mjs:147,155`,
  `check-support-window-freshness.mjs:40,110,303`, `generate-criteria-baseline.mjs:92`,
  `check-install-smoke.mjs:100`) and **every one inside a COMMENT** — zero write a glyph to
  any stream. §4's zero-exceptions measurement was over PREFIXES and this one was never
  run: standing rule 7 at the site of §4's own cited precedent.
  `docs/presentation-policy.md` §"Glyph profile" rules that a non-TTY resolves to `text`
  and `CRABGIC_ASCII=1` to `ascii`, and `scripts/` cannot import `PresentationPolicy`
  (§8(a)), so the deviation is structural. `⚠️` is that policy's own warn glyph, so the
  gap is the missing DEGRADATION and not the choice; stated as residual **§8(o)**.
- **`CF14-4`, low — the registry that exists to protect §6's row numbers restated a SUBSET
  of what its own quoted command returns.** Re-derived at a named sha, per this file's own
  convention — `git show f3aed16:<file>` — the command returns **36** hits and **19**
  distinct rows outside §6. Round 13's half names four, the by-hand sentence eleven and
  the tail two, so rows **10, 11, 15, 16, 19, 20, 26, 27 and 30** — NINE — were cited from
  outside §6 and named in NEITHER half; renumbering any of them breaks a link the registry
  says does not exist, verbatim `CF13-5` one round earlier, at the fix for it. ⚠️ **The
  finding's own list was wrong in both directions and its post-apply figures were rejected
  (standing rules 1 and 2):** it named 22, 23 and 35, all of which the bullet already
  carries, missed 10 and 11, and re-derived below §9 only — narrower than the command it
  quotes, on a reviewer verifying a check whose whole subject is scope. Fixed per `C11-4`
  by replacing the enumeration with the command, the sha, and the rule: **re-run it before
  renumbering §6, and never restate a subset of its output** — including for this round's
  own appended rows.

ℹ️ **What round 14 re-derived and could not break:** the four preamble checks; §3.1, §3.5,
§5.1 and §8(a)-(h)/(k)/(m)/(n) against all fourteen findings; §4's exit-code table, whose
existing rows already carry both new marker branches; the `.tsbuildinfo` exclusions; and
§6's bundler source-property assertions, which are invariant under every change here.
⚠️ **Four proposed edits were rejected outright in consolidation** — C14-2's step-3
carve-out and its battery row, the correctness lens's row-append ANCHOR (it sat after a
blank line, so rows 37-39 would have started a second table with row 37 as its header and
no delimiter row: round 10's "guard WHAT an apply inserts" at a live proposal, and one the
escape-aware row census provably cannot see — ⚠️ **and round 15 (OP15-4/CF15-5) found the SAME structure SHIPPED by this round's own apply at a different site:** `git diff f3aed16 dbe9850` shows the blank line and §7's `C14-5` row added together, so the one §7 row that exists so nobody mutes the deliberate duplicate was the one §7 row a reader scanning the risk table could not see. Round 10's "guard WHAT an apply inserts" at a live edit rather than at a proposal, and the preamble's fifth structural clause is what now sees it), and the operability lens's addition of a
tenth hand-written entry to the registry CF14-4 files against. **No working-tree mutation:**
`scripts/bundle-cli.mjs` was never imported, every probe read files as text or called
`repo-census.mjs`'s pure enumerators, and `git status --porcelain` is empty at `f3aed16`.

Battery rows **37**, **38** and **39** were appended, nothing renumbered.

**Round 15 (2026-08-21) — `contract-fit` / `correctness` / `operability`, fourteen finding
IDs consolidated across the three lenses: `CF15-1`, `CF15-2`, `CF15-3`, `CF15-4`, `CF15-5`,
`C15-1`, `C15-2`, `C15-3`, `C15-4`, `OP15-1`, `OP15-2`, `OP15-3`, `OP15-4`, `OP15-5` —
`CF15-5` and `OP15-4` being ONE defect filed independently by two lenses. Entered here in
the SAME round that filed them, which is the preamble's own standing requirement. THIRTEEN
of the fourteen are SUBSTANCE, and all of them sit in the OUTPUT and TRACEABILITY layer
round 14 identified as where this stage still was — §0, §1, §2, §3.2, §3.3, §3.4, §4, §6,
§7, §8. §3, §3.1, §3.5 and the three-clause conjunction survived a second full walk with no
change to their substance. TWO findings were introduced by an edit that changed no words of
the design: one by the FORMATTER, one by a BLANK LINE.**

- **`CF15-1`, high — the FORMATTER silently vacated round 14's own new assertion.** §6's
  census-import predicate read `[^;]` UNDERSCORE `from` BACKSLASH-s UNDERSCORE where the
  quantifiers belong. Re-derived by running it as spelled against ONE bare conforming statement with no comment
  above it —
  `import { enumerateRootReferences, enumerateTsconfigs } from "../repo-census.mjs";` — the
  scope named here rather than left as "the conforming statement", round 29 (`CF29-2`)
  having found that one-source basis narrower than the claim it carried, which quantifies
  over every conforming `units.mjs` including the commented one the same paragraph
  predicts:
  `censusImport()` returns the empty string, so `enumeratesReferences()` is false, the
  `toBe(true)` line FAILS a correct implementation, and the mutant line passes for EVERY
  input — vacuous, the class this change set exists for, at requirement 5's and requirement
  11's only source-level discharge. Cause re-derived rather than inferred: `git show
934c547`, whose message reads "whitespace only, in 47 table hunks", carries exactly TWO
  non-whitespace changes and both are prettier normalising star-form emphasis to the
  underscore form — one harmless, in §3.4's prose, and this one, inside a regex. ⚠️ **The
  finding claimed ONE and it is TWO (standing rule 2, on a reviewer's own measurement);
  the conclusion is unchanged and stronger, because the pair is what shows the mechanism
  cannot tell prose from a regex.** The wiring block is a single-backtick span crossing
  blank lines, so the star pair parsed as emphasis; `prettier --check` then reported the
  file clean, making the mangled form the stable one. Fixed with quantifiers that form no
  emphasis pair — `[^;]+from` BACKSLASH-s`+`, measured a no-op over the whole file — rather
  than by restoring the stars, which prettier is MEASURED to re-mangle on the next run, and
  rather than by the ```js fence the finding proposed, which prettier REFORMATS (a plain
  fence is a no-op but costs every inner code span its rendering). The preamble gains the
  check — and the `diff -w` form the finding proposed for it was itself measured failing on
  a correctly-formatted candidate, because re-padding a table changes its delimiter row's
  DASH count and a dash is not whitespace, so the guard normalises table rows explicitly.
- **`CF15-2`, medium — the same predicate pinned ONE of the two calls requirement 11 is
  discharged by.** §0's requirement 11 row, §1 and §6's own comment all say the import is
  `enumerateRootReferences` PLUS `enumerateTsconfigs`; the predicate tested the first
  alone. Re-derived with the corrected regex, so this survives `CF15-1`'s fix: a
  `units.mjs` importing one name and hand-rolling a `readdirSync` config walk returns
  true, passes the live smoke test (the unit SET is identical), and passes every synthetic
  row — §3 input rule 4 has no member on today's tree (round 14, `C14-3`) and row 3's
  `stale-declarations` comes from §3.3's named comparand, so no fixture discriminates. That
  hand-rolled walk is verbatim what requirement 11 forbids, at the call feeding rule 4 and
  §0's requirement 9 arithmetic. Widened to both names with a second mutant that RENAMES
  rather than deletes, and whose replacement shares no substring with any of the three
  names, so it flips on the missing name and never on the negated one. Standing rule 9:
  §0's requirement 11 row, §0's requirement 5 row and §1's paragraph all stated the closure
  as complete and are repointed in the same pass — and requirement 5's row carried "all 36
  synthetic battery rows" against §6's own "all 39" for the identical claim at the same
  round, so BOTH are replaced by the set-valued form per `C11-4` rather than by another
  number this round's own appends invalidate.
- **`CF15-3`, low — §4's internal-error owner sentence cited §2 for a two-field call.**
  It read `formatInternalError(err, { strict })` while §2 declares
  `formatInternalError(err, { strict, json })` and §1's cell passes "the `--json` it
  parsed". An implementer building the row from §4 — the section §4 calls authoritative
  for output, and the row's owner since round 11 — leaves `json` undefined and writes the
  human line onto the JSON stream: verbatim round 12's `C11-5`, whose fix reached §1's
  cell, §2's signature and §4's two other mentions and not §4's own owner sentence.
  `CF13-4`'s shape — §4 citing §2 for a rule §2 does not carry. One edit; `grep` confirms
  the only other two-field call in the file is §9's round-11 entry, correct as history.
- **`CF15-4`, advisory — §3.3's clause 1 mandated an import nothing read.** "Never a
  literal duplicated into `walk.mjs`" (round 10, `C10-2`) had no assertion anywhere:
  `grep -n "walk.mjs"` returned nine hits, none inside §6. A `walk.mjs` carrying its own
  six-element literal passes every row (synthetic trees), the false-negative battery, the
  four `bundle-cli.mjs` source properties (they read the BUNDLER, which still exports the
  const), the `package.json` and `.gitignore` reads and the live smoke test. The drift is
  one commit away and measured: `PLUGIN_ASSET_ENTRIES` at `bundle-cli.mjs:89` holds
  `agents`, `hooks`, `skills`, `statusline`, `.mcp.json` and `.claude-plugin`, while
  `packages/plugin/` also holds `workflows` (re-derived 2026-08-21), after which a seventh
  asset ships with its absence not a clause-1 gap — quietly narrowing §0's requirement 1
  row. `CF14-2`'s shape at the design's other repo-internal import. Closed with the same
  one-predicate mutant-proved read, and §8(k)'s "the source says so, never the process
  does" bound extended to cover it.
- **`CF15-5` / `OP15-4`, medium — round 14's own new §7 row is not in §7's table.**
  `C14-5` added the `stale-unit` + `stale-bundle` duplicate as a §7 row "so nobody mutes
  the second line", and a BLANK LINE sits between it and the table. GFM ends a table at the
  first blank line and a following pipe line begins a NEW table whose first row is its
  header — with no delimiter row it is not a table at all, so the row renders as a
  paragraph of literal pipes, detached from the twelve rows above it. Verbatim the defect
  round 14 REJECTED the correctness lens's row-append anchor for, committed in the same
  round at the other table; `git diff f3aed16 dbe9850` shows the blank line and the row
  added together. Re-derived with this repository's prettier 3.9.5, which normalises every
  table it parses: the row is left at its raw **802** characters while every §7 row it
  should join is padded to **2,619**; delete the blank line and prettier pads it to 2,619
  like the rest. ⚠️ **One lens reported 813 and it is 802 (standing rule 2).** ⚠️ **The
  escape-aware row census provably cannot see it** — a blank line resets its header, so
  the orphan becomes its own one-row header and matches itself — which is the third
  residual of that check and is stated with the other two. The whole-document sweep for
  the shape is the reproducible form, and the preamble gains it as a fifth structural
  clause — promoted in round 16 (`OP16-1`) from a post-hoc re-derivation to a gate on the
  apply. ⚠️ **"returns exactly this one site" stood here and did not survive this round's
  own apply (round 16, `CF16-2`/`OP16-2`).** It was measured on the PRE-apply document: at
  the round-14 commit the sweep returns one line, and at `db857c3` — the commit this bullet
  was published in — it returns **two**, this round's own appended rows 40-43 being the
  second, while this round's prescribed deletion of the first never landed. The diff across
  this commit carries no change to that §7 row, still **802** characters against §7's
  padded **2,619**. `C11-4`'s class, in the sentence introducing the detector for it.
- **`C15-1`, medium — clause 1 answers round 7's `CR-3` BETWEEN entries, and §3.4 claimed
  "on every tree".** `bundle-cli.mjs:177-179` copies each of the six
  `PLUGIN_ASSET_ENTRIES` with one recursive `cp`, so an interrupt INSIDE one leaves that
  entry's top-level path present and its contents partial — a state clause 1 cannot see
  (it tests six paths for existence and never walks them), §3.4 cannot see (`cp` writes the
  destination at copy time, so the dist side is `now`), and clause 2 cannot see twice over
  (`dist/plugin/**` is outside §3's filtered comparand, and the marker was cleared in the
  wipe step and never written). Verdict `compared` plus one advisory, **exit 0 even under
  `--strict`**, at `check:all` member 0 — so `check:tarball` and `check:install-smoke` then
  run against the precondition §5 says this ordering names, on a `packages/cli` whose
  tarball ships an empty `skills/`. Closed as a STATED residual **§8(p)** rather than by
  making §3.4 per-entry, with §3.3's clause 1 and §0's requirement 1 row carrying the bound
  and §6's false-negative battery asserting the accepted verdict. ⚠️ **Filing it in §8(i)
  as the finding proposed was REJECTED in consolidation:** §8(i)'s three costs are all
  conditional on the marker being ABSENT, and this gap holds with the marker PRESENT too —
  a manual delete under `dist/plugin/skills` moves nothing clause 2 reads — so §8(i) would
  have stated the residual narrower than it is. ⚠️ **Two of the finding's line anchors were
  re-derived and corrected (standing rule 2):** the 1.0.0 record is
  `bundle-cli.mjs:165-169`, not `:163-168`, and `check-install-smoke.mjs`'s predicate is at
  `:255`.
- **`C15-2`, medium — §3.2's input side had no absence rule for SIXTEEN of its members, and
  §3.3's skip rule foreclosed one by scoping every side to `packages/cli`.** That section
  stated definedness for the OUTPUT side only ("`bundleAt` is always defined here"), while
  `newestUnder` returns `undefined` for an absent or empty `dist` (§2). Reached from §4's
  own recipe: step 1's FOURTH carve-out exempts `packages/cli`, so another unit's `dist` is
  deleted while `packages/cli` stays complete with a fresh marker, and the next run — or
  `npm test`, `pretest` being the primary trigger — reaches `checkBundleFreshness` on a
  `compared` cli with one member `undefined`. Two defensible readings two exit codes apart,
  verbatim `C10-3`'s framing: drop the member and render coverage `full` (a comparison over
  15 of 16, which §4's recipe step 5 licenses an operator to read as covered), or read
  `.mtimeMs` off `undefined` and reach §4's internal-error row at exit **2** from member 0.
  Ruled: coverage `not-run`, reason the offenders' `dist` paths comma-joined and suffixed
  ` absent`. ⚠️ **The finding's precedence line named only the two marker reasons and was
  WIDENED in consolidation:** on a fresh clone and in both `meta-checks` legs
  `packages/cli/dist` is absent TOO, so without ranking `packages/cli/dist absent` above
  this reason the commonest tree in the design would have gained the second spelling this
  finding exists to remove. ⚠️ **And standing rule 8 fired on the fix, caught in
  consolidation rather than a round later:** an absence rule makes `bundle-freshness`
  `not-run` on any fixture short of its own inlined set, which would have vacated rows 5,
  38 and 41 — baseline `not-run`, mutation flips nothing, CF8-2's defect at three rows at
  once. §6 gains the sibling fixture rule, in the shape of its existing complete-cli-build
  rule, with row 40 as the one deliberate exception. Sites: §3.2's rule and its precedence
  line, §3.3's skip-rule enumeration, §4's verbatim reason list, §4's degraded-run producer
  sentence, §6's fixture rule, §6 row **40**.
- **`C15-3`, medium — requirement 7's partial discharge rested on two comparand members that
  NO assertion in this design exercised.** Re-derived 2026-08-21 over §6's then-39 rows, the
  false-negative battery, the wiring assertions and the live smoke test: `bundle-types.mjs`
  appeared in §6 in one citation and no mutation, and every `bundle-cli.mjs` hit was a
  citation or a fixture module copy. An implementation whose `stale-bundle` input side was
  the 16 dists plus `packages/cli/src`, and whose declaration-cache comparand was
  `packages/*/src` plus `tsconfig.dts.json`, passed EVERYTHING and shipped both members
  hardcoded never to fire — `CF14-2`'s shape at two more members. §6 rows **41** and **42**
  close it; §0's requirement 7 row and §8(b) stop claiming an unasserted closure. Row 42
  also supplies the sole-`stale-declarations` fixture §4 step 2's carve-out has never had.
  ⚠️ **The finding's `PRODUCER_INPUTS` rule was AMENDED in consolidation, because as
  proposed it was unimplementable and would have made both rows vacuous by construction:**
  neither `checkBundleFreshness(units, metafile)` nor `checkDeclarationCache(units)` takes
  a `cwd`, so the rule is written as recovering it from the `packages/cli` `Unit` each
  already holds — the same root that unit's `.dts-cache/index.d.ts` comparand is resolved
  from — and no signature changes arity. Both rows run the check against the fixture as its
  `cwd` and say so, in row 31's shape.
- **`C15-4`, low — §3's skip-rule table said a `skipped` unit yields "no finding" where a
  cli-scoped comparison still can.** `rm -rf packages/cli/dist` — §4's recipe step 1 —
  leaves `.dts-cache/` beside it, so `checkDeclarationCache` RUNS, `full`, exactly as §4's
  recipe step 5 states, and against a cache older than `packages/*/src` it reports
  `stale-declarations packages/cli` on a unit the same run names as `skipped`. Only
  `unbuilt` suppresses the four (§3.2); `skipped` suppresses the two PER-UNIT comparisons
  that table's column names and nothing else. An implementer honouring the table as written
  suppresses the finding and ships a false negative for requirement 8's kind on the tree
  this design's own remedy creates. Scoped at §3's table, stated at §3.3's skip rule, and
  pinned by §6 row **43** — a verdict row whose fixture deliberately does NOT report clean,
  because the finding must already exist for the mutation to be able to suppress it, and
  the row says so rather than leaving the next round to file `CF8-2` against it.
- **`OP15-1`, medium — the design's ONE rendered `stale-unit` line printed basenames, and
  the remedy built from it is a silent no-op.** It read
  `stale-unit ×4 — contracts, engine-claude, gates, plugin` while `enumerateRootReferences`
  returns path-form dirs — re-derived 2026-08-21 on the live tree, 19 of them,
  `packages/contracts` … `e2e/report`. §4's recipe step 1 concatenates that token into
  `rm -rf <unit>/dist`, and none of `contracts/`, `gates/`, `plugin/`, `engine-claude/` or
  `report/` exists at the repo root (verified), so the command exits **0** having deleted
  nothing, step 2 re-emits nothing for a touched-but-unchanged source, and step 5's re-run
  reports the identical four findings — this section's own "the check fires again,
  identically, forever", and `CF14-1`'s class one round later. No rule licenses a short
  form (`grep -c -i 'basename|short name|short form'` → **0**). Ruled as **A UNIT IS ALWAYS
  PRINTED AS ITS `Unit.dir`**, and §6 row 28's `<unit>` placeholder — which any non-empty
  token satisfies — is tightened to EQUALITY on `dir`.
- **`OP15-2`, medium — the most frequently printed `reason` in the design was spelled in no
  §3 table.** §4 prints `unitState()`'s `reason` VERBATIM and §6 row 30 forbids the
  reporter inventing one, while `grep -n 'no dist/'` returned §2, §4 and §9 only — §3.3's
  unit-state table decided the ABSENT-`dist/` verdict and carried no string for it. That is
  the reason printed for all 19 units on a fresh clone, in both `meta-checks` legs, and for
  `packages/cli` after §4's own recipe step 1, and an implementer returning `dist/ absent`
  passed every row, the false-negative battery, the wiring assertions and the smoke test.
  `OP13-2`'s defect class at the one `skipped` state that round's fix did not reach. The
  literal lands in the unit-state table's own verdict cell — a row in the reason table
  would duplicate the verdict and renumber a table §6 rows 13-14 and §7 cite, which round
  13 rejected once already — and §6 row 30 gains a FOURTH fixture unit asserting it. §7's
  fresh-clone row, which claimed all three `skipped` states already carried their own
  literal, is corrected in the same pass.
- **`OP15-3`, medium — §4's opening sentence promises a line shape no kind renders, and the
  collapse had no rule at N=1.** "One line per finding naming the unit, the newest input,
  the older output and the delta" is false of all eight kinds: four are delta-less by this
  section's own rule and four collapse to `<kind> ×N — <units>`, which has nowhere to put
  any of the three. So `newerInput`, `olderOutput` and `deltaMs` are computed and rendered
  on NO human path — `O9-1`'s defect class at finding granularity — and they are now stated
  `--json`-only at §2's `Finding` and at §4. And because no threshold stood at the collapse
  and no row rendered a delta-carrying kind at N=1, `stale-unit ×1 — packages/contracts`
  and the generic comparison shape BOTH passed the whole battery, for the commonest kind on
  the commonest tree, in a section that calls two shapes for one slot a defect in terms.
  Ruled: one shape at every N, pinned by row 28's N=1 twin — a twin rather than a new row,
  so nothing is renumbered.
- **`OP15-5`, medium — the `nothing was checked` PASS is a second shape for the summary §4
  says has ONE.** `CF14-3`'s "ONE shape and never two" failure at the other slot of the
  same summary, one round later: the rule mandates
  `<N> units compared, <M> skipped (<clause>, <clause>, …)` while this literal prints no
  parenthetical, and on the tree it describes there IS a clause — all 19 units `skipped`,
  all sharing one reason. Re-derived 2026-08-21 against the live 19 root `references` and
  the `no dist/` literal: the shape-conforming line is **451** characters, **6** terminal
  rows at 80 columns and **4** at 120, against **74** and **1**. Two mandated renderings for
  the quietest and most frequently reached tree this design has, and §6 row 39's negative
  twin runs that exact fixture while asserting only that the plan does not print — so no
  row discriminated them. Suppressed rather than printed, stated at §4's literal, at §4's
  rule, at §5's CI bullet and at §2's `formatFindings`, and asserted by extending row 39's
  negative twin to the rendered `PASS` line.

ℹ️ **What round 15 re-derived and could not break:** §3, §3.1, §3.5 and §3.3's three-clause
conjunction against all fourteen IDs; §4's exit-code table, prefix rule, per-kind line
shapes, remedy table and ordered recipe steps 1-5, walked row by row for the `CF14-1` defect
shape — every remedy re-checked against the finding it is printed for, and `stale-unit`'s
remedy is correct as a COMMAND, what was wrong being the ARGUMENT the printed line hands it
(`OP15-1`); §5 and §5.1; §7's other twelve rows; §6's rows 1-39, its false-negative battery
and its bundler source-property assertions; and §8(a)-(j), (l)-(o). The four preamble passes,
the escape-aware row census, the `String.replace` census and the new formatter check all
return nothing on the fully edited document. ⚠️ **The new orphan-row sweep did NOT, and
this sentence was false in the commit that published it (round 16,
`CF16-2`/`OP16-2`/`CF16-1`/`OP16-1`):** at `db857c3` it returns **2541** and **2838** —
§7's `C14-5` row, whose fix this round dispositioned and never applied, and a SECOND
orphan this round's own apply created at §6's battery, rows 40-43. The one check whose
whole subject is an append is the one check this round's own append broke. ⚠️ **Two live row COUNTS
were replaced rather than corrected, per `C11-4`:** §0's requirement 5 row read **36** and
§6's wiring comment **39** for the SAME claim at the same round, so the pair was already
inconsistent before this round appended anything. **No working-tree mutation:**
`scripts/bundle-cli.mjs` was never imported, every probe read files as text or ran on a copy
under a scratchpad, and `git status --porcelain` is empty at `934c547`.

Battery rows **40**, **41**, **42** and **43** were appended BELOW A BLANK LINE, so not one
of the four was on the page until round 16 deleted it; nothing renumbered.

**Round 16 (2026-08-21) — `contract-fit` / `correctness` / `operability`, nine finding IDs
over SEVEN distinct defects: `CF16-1`/`OP16-1`, `CF16-2`/`OP16-2`, `CF16-3`, `C16-1`,
`C16-2`, `OP16-3`, `OP16-4`. Entered here in the SAME round that filed them, which is the
preamble's own standing requirement. TWO of the seven are ONE defect at two tables — a
row appended below a blank line — and the round that filed that defect against §7 is the
round that shipped it again at §6 and never applied the fix at §7. THREE more are one
shape at three different rules: a mandate carried by §0, §3, §3.1 and §3.2 and exercised
by NOTHING in §6, so the implementation each rule exists to forbid passes the whole suite.**

- **`CF16-1` / `OP16-1`, high — §6's own battery rows 40-43 were not in §6's battery
  table.** Round 15 appended them below the blank line at the table's foot. GFM ends a
  table at the first blank line and a following pipe line begins a NEW table whose first
  line is its header — with no delimiter row it is not a table at all — so all four
  rendered as a paragraph of literal pipes. Re-derived at `db857c3` with no working-tree
  mutation: the preamble's own fifth structural clause returns **2541** with **2540**
  blank; this repository's prettier 3.9.5 leaves rows 40-43 at their raw **1307**, **875**,
  **1017** and **1276** characters while rows 36-39 sit padded at **2500**; delete the
  blank line, re-run prettier, and rows 39-43 all pad to a uniform **2597**, which is the
  confirmation they are parsed as rows rather than as the paragraph they were. The
  consequence is that THREE requirement closures this design asserts rested on elements a
  reader of the rendered design could not see: requirement 7's partial discharge ("§6 rows
  41 and 42 close it", `C15-3`), requirement 3's absence rule (row 40, `C15-2`) and
  requirement 8's `skipped`-unit false negative (row 43, `C15-4`) — three comparand
  members whose only assertions were off the page, while every other §6 row still passed.
  ⚠️ **Round 14 REJECTED this exact anchor as a PROPOSAL and round 15 committed it here,
  in the round that filed it against §7** — round 10's "guard WHAT an apply inserts, not
  only WHERE" at its third site. The append rule is written at §6 and in the preamble
  rather than left as this round's remedy, per `OP12-6`'s own disposition, and the sweep is
  promoted to a GATE ON THE APPLY beside the formatter check.
- **`CF16-2` / `OP16-2`, medium — round 15's fix for the SAME defect at §7 was
  dispositioned `fixed` and never applied, and the guard then published a false absence
  claim.** The blank line at 2837 is still there and §7's `C14-5` row is still detached
  from the risk table: the diff across round 15's commit carries no change to it, and it is
  still **802** characters raw against §7's twelve padded **2,619** rows. Re-derived across
  three shas — round-13 commit → nothing, round-14 commit → **2620**, `db857c3` →
  **2541 and 2838** — so the sweep's own published output, "returns nothing thereafter",
  was false in the commit that wrote it, and round 15's "returns exactly this one site" was
  measured on the pre-apply document. That row is the only place this design tells a reader
  not to mute the deliberate `stale-unit` + `stale-bundle` duplicate `C14-5` keeps on
  purpose, and it was detached from the risk table a reader scans. This is the first
  partly-true completion claim in this record whose subject is the published output of one
  of the document's own reproducible checks — standing rule 2 applied to a guard rather
  than to a finding — so the clause now carries the command WITH the sha it was run at, in
  the shape `CF13-6` already forced on the line-count clause, and a stale result is
  distinguishable from damage.
- **`C16-1`, high — §3's and §3.1's `dist/eo-*-fixture-*/**` exclusion was asserted in
  NEITHER of the two sets it governs.** Re-derived over rows 1-43, the false-negative
  battery, the wiring assertions and the live smoke test: the only row whose fixture holds
  a scratch directory is 14, with 15 and 30 reusing its end state, and row 14 never
  required that directory to CONTAIN a `.js` — with an empty one its baseline and its end
  state read the same under an implementation that excludes scratch and one that does not.
  `grep -n stemsUnder` returned exactly two lines, both in §2, and nothing in §6 — measured at
  `git show db857c3:<file>`, the document this round READ, and false in `badf45a`, the commit
  that PRINTS it, where §6 rows 14 and 44 already carry the name; the sha travels with the
  figure, and `CF17-2` reached the two sibling claims of exactly this shape and never this
  third one, which is standing rule 9 (round 29, `C29-1`). Measured read-only
  on this working copy 2026-08-21, no tree mutation:
  `packages/journal/dist/eo-crash-fixture-6dP02V` is live NOW and holds **20** `.js` with
  no `src` counterpart. A `stemsUnder` missing the exclusion therefore reports
  **20 `orphan-output packages/journal`** findings — a kind §4 does not collapse, so twenty
  printed lines — exit **1** under `--strict` at `check:all` member 0, printing
  `rm -rf packages/journal/dist` at the start of the very `npm test` whose interrupted
  predecessor left the residue, which is round 5's own named hazard; and an
  `isCompilerOutput` missing it reads that unit's newest output as **1787308024** rather
  than **1787133002**, a **2.03-day** forward shift masking any `stale-unit` in the window.
  Closed at row **14**'s fixture, whose BASELINE now pins the stem side and whose END STATE
  pins the unit-state side, and at new row **44**, whose MUTATION pins the freshness side
  that row 14 never reaches. Sites: §0's requirement 1 and 2 rows, §3's scratch bullet,
  §3.1, §6 rows 14 and 44, §6's rendered-text group and registry bullets, §1's battery
  count.
- **`C16-2`, medium — §3.2's central rule was discharged by nothing.** "The inlined set is
  therefore derived from esbuild's own metafile, never from the package.json graph" is what
  round 1's `CF-1`/`C-3` and round 2's `C-A` cost two rounds to establish, and re-derived
  at `git show db857c3:<file>` — the document round 16 READ —
  `inlinedUnitsFromMetafile` occurred **three** times in this design with none inside §6;
  the sha travels with the figure because round 16's own apply made the bare present-tense
  form false in `badf45a`, where it is six with one in §6 (round 17, `CF17-2`). A `checkBundleFreshness` whose inlined set is the transitive closure of
  `packages/cli/tsconfig.json`'s references — re-derived read-only 2026-08-21, **16**
  direct references — passes every battery row, the false-negative battery, the four
  bundler source properties, the `units.mjs` and `walk.mjs` import reads and the live smoke
  test, which asserts the UNIT set and never the INLINED set. It passes because every
  cli-scoped fixture makes the two derivations COINCIDE by construction, and neither the
  marker-`outputs` rule nor round 15's inlined-`dist` rule forces them apart at any row.
  Wrong result: esbuild erases type-only imports and tree-shakes unused runtime surface
  while `tsc` project references do neither, so a unit referenced for types alone is in the
  closure and not in `inputs`, and editing it fires `stale-bundle packages/cli` against a
  bundle containing none of its code — verbatim round 1's `CF-1`. Closed by row **45**, in
  rows 40 and 41's direct-call shape, with BOTH directions because half (i) alone is
  satisfied by an implementation that never fires. Sites: §0's requirement 3 row, §3.2's
  derivation paragraph, §6 row 45, §6's cli-scoped-fixture list, its inlined-`dist` fixture
  rule, its rendered-text group and registry bullets, §1's battery count.
- **`CF16-3`, medium — requirement 6's SOLE non-vacuity element was satisfied at N=1.** §6
  row 2 read fixture `dist newer than src` — the same three words as row 1's, so nothing
  required a second unit — mutation `touch tsconfig.base.json forward`, verdict
  `stale-unit ×N`. The implementation that passes is the ordinary memoisation shape: one
  shared `extends`-chain resolver serving all 19 units with a MODULE-LEVEL visited set adds
  `tsconfig.base.json` to the FIRST unit walked and skips it for the other 18, renders
  `stale-unit ×1`, and `×N` accepts it. Nothing else discriminates — measured at `git show db857c3:<file>`,
  `extends` occurred **zero** times anywhere in §6 — the sha travels with the figure,
  because round 16's own apply made it three (round 17, `CF17-2`) — row 1 is src-side,
  row 3's trigger is
  `tsconfig.dts.json` and its verdict is `stale-declarations` (`C14-3`), rows 28 and 34 are
  the only other multi-unit `stale-unit` fixtures and both are src-triggered, and the live
  smoke test asserts the unit SET, which is unaffected because the 18 under-covered units
  are still reported, merely reported clean. `CF14-2`'s "a MEMBERSHIP check, true of any
  subset" and `OP15-1`'s placeholder, at the row §0's requirement 6 cites as its only
  element — and §7 row 1's owner-accepted cost is stated "at §6 row 2's scale", so what the
  owner was asked to accept rested on a row that could not tell ×1 from ×19. §3 input rule
  3 is CORRECT and says so in terms ("Chain length is 2 for all 19 today"): this is the
  battery failing to assert what the section states. Fixed at the row with a TWO-unit
  fixture and EQUALITY on both units' `Unit.dir`, with §0's requirement 6 row and §7 row 1
  repointed in the same pass (standing rule 9). ⚠️ **The finding's RENDERED-line form was
  AMENDED in consolidation:** asserting the rendered grouped line would move row 2 into the
  rendered-text group and churn §6's three group enumerations while duplicating row 28's
  subject, which `OP15-1` already pins to `dir` by equality — so a basename-printing
  reporter fails row 28 and needs no second row. Row 2 asserts the RETURN value because its
  subject is §3's input rule 3 reaching every unit and not the reporter, and the split is
  written into the cell and into the group bullet so a later round does not file it as an
  inconsistency. ⚠️ **And no mutant swap is introduced:** the wrong implementation FAILS
  row 2 on row 2's own fixture, which is stronger than a mutant because it needs no
  injection point.
- **`OP16-3`, medium — row 39's negative twin asserted a literal that FAILS a correct
  implementation.** `OP15-5` made the twin assert the rendered `PASS` line "byte-equal to
  §4's literal", and that literal is
  `check-stale-dist: PASS — 0 units compared, 19 skipped; nothing was checked`, whose **19**
  is this repository's LIVE root-reference count — re-derived 2026-08-21 by calling
  `enumerateRootReferences()`, 19 path-form dirs, `e2e/report` … `packages/testkit`. Every
  fixture in §6 is a synthetic tree under `mkdtempSync(tmpdir())`, for the stated reason
  that asserting against the real repository makes a test restate today's file list, and no
  rule gives the `meta-checks` fixture 19 units — so on any fixture with N other than 19 a
  CORRECT implementation renders `N skipped` and the assertion fails. `CF15-1`'s class and
  rows 16 and 17's, at the twin `OP15-5` filed to END the state in which the two mandated
  renderings of the quietest and most frequently reached tree here are discriminated by
  nothing. Both escapes are defects: pinning 19 into the fixture rots on the 20th workspace
  (`C11-4`), and weakening to a `toContain` of the trailing sentence passes BOTH renderings
  and vacates the twin. Fixed in row 35's own wording — the SHAPE, both counts derived from
  this fixture, no parenthetical — plus the mutant that makes it non-vacuous, a formatter
  rendering the clause list on this fixture, the 451-character line `OP15-5` measured. §4
  now states that the exemption is a SHAPE and that its `0` and `19` are a live-tree
  illustration, with §5's `meta-checks` bullet explicitly carved out, since that bullet uses
  the same literal as a PREDICTION about this repository and is correct.
- **`OP16-4`, low — the coverage line's SUPPRESSION is stated in one bullet and asserted by
  no row.** "It lists only entries whose status is not `full`, so a fully covered run prints
  nothing extra" is the only statement anywhere that the line is withheld — `grep -n`
  returns exactly that one hit — while §4's prefix rule enumerates "the coverage line"
  unconditionally among the lines that must carry the prefix and fixes its position, having
  conditioned the `PASS` line and the summary in the same sentence. Every row that READS
  the coverage line — 16, 17, 18, 26 and all three of row 39's twins — runs a fixture
  holding at least one non-`full` entry, and the two rows whose fixtures are fully covered
  assert the plan (34) and the two kinds plus the plan half (38). So a `formatFindings`
  emitting the line unconditionally as prefix + `⚠️ coverage: ` + the joined non-`full`
  entries — the empty string when all four are `full` — passes every row, the
  false-negative battery, the wiring assertions and the live smoke test, while printing a
  bare `check-stale-dist: ⚠️ coverage: ` on every green run: the steady state after the
  first post-adoption build, on every local `npm test` against a built tree, and in both
  `test` legs of `ci.yml:129`. At line 5 of §5.1's measured 204 a `⚠️` with nothing after it
  on a healthy tree is §7's own coverage-line false-positive read made unconditional, on a
  glyph §8(o) records cannot degrade. Fixed at §6 row **38** — chosen over row 34 because
  §6's `C15-2` sibling rule binds row 38's fixture to hold its whole inlined set, which is
  what makes `bundle-freshness` provably `full` there, while row 34's is not pinned and the
  assertion could be vacuous — and the condition is written into §4's prefix rule and its
  order sentence, into §1's `report.mjs` cell and into §2's `formatFindings` comment, the
  four sites an implementer builds the line from.

ℹ️ **What round 16 re-derived and could not break:** §3, §3.1, §3.2, §3.3, §3.4 and §3.5
walked against all nine IDs with no change to their substance beyond the three non-vacuity
pointers — a THIRD consecutive round in which the algorithm and the six comparisons
survived, and in all three cases the defect was §6 failing to assert what §3 already states
correctly; §4's exit-code table, per-kind line shapes, remedy table and ordered recipe steps
1-5; §5 and §5.1, including the `meta-checks` bullet's `0 units compared, 19 skipped`, which
is a LIVE-repo prediction and therefore correct; §0's eleven requirement rows; §7's twelve
other rows as TEXT; §8(a)-(p), none of which gains a residual, because `CF16-1` and `CF16-2`
are damage repaired rather than accepted and the other five are assertions strengthened
rather than limits conceded; and §6's rows 1, 3-13, 15-37 and 39-43, its false-negative
battery, its wiring assertions — the census-import predicate's round-15 plus-quantifier form
re-read and confirmed to match a conforming import — and its live smoke test, row by row for
a wrong implementation that passes. The five preamble structural clauses, the escape-aware
row census, the `String.replace` census and the formatter check were run on the fully edited
candidate: the sweep returns nothing, the census returns nothing, prettier's second run over
the candidate is byte-identical, and the `see above`/`see below` sweep below `## 0.` returns
only backticked quotations of `OP13-2`'s fixed cell as history, so round 10's lesson holds at
every live site. **No working-tree mutation:** `scripts/bundle-cli.mjs` was never imported,
every probe read files as text or called `repo-census.mjs`'s pure enumerators, both prettier
runs were on scratchpad copies, and `git status --porcelain` is empty at `db857c3`.
⚠️ **One citation-hygiene note, not filed as a finding:** `f3aed16` and `dbe9850`, cited by
rounds 13-15, are no longer ancestors of this branch after a rewrite; their on-branch
equivalents are byte-identical for this file, and round 16's own figures are quoted against
`db857c3` and `fe892ae` so they reproduce from the branch.

Battery rows **44** and **45** were appended, nothing renumbered; rows **40**-**43** rejoined
the table they had been appended below.

**Round 17 (2026-08-21) — `correctness` / `contract-fit` / `operability`, three `revise`,
eight finding IDs, all re-derived read-only at `badf45a` on a clean tree and all
dispositioned. FIVE are SUBSTANCE and three are this document's own bookkeeping. Entered
here in the SAME round that filed them, which is the preamble's own standing requirement.
§3.1 and §3.3's verdicts survive a FOURTH consecutive walk unchanged; what moves in §3.2,
§3.3 and §3.4 is only WHEN the four cli-scoped comparisons decline.**

- **`C17-1`, high — the founding failure's ELEVENTH appearance, and the first reachable
  with two published root scripts and no manual `rm`.** §3.2's Rule asserted "`bundleAt` is
  always defined here: this comparison runs only on a `compared` `packages/cli`, and §3.3's
  clause 1 requires a chunk to exist". Both halves died with `C15-4`: `skipped` suppresses
  only `checkUnitFreshness` and `checkOrphans`, and §3.3's conjunction is "one further step
  INSIDE the `compared` branch — never before it". A fresh clone, `npm run typecheck`,
  `npm run build:clean` leaves `packages/cli/dist` PRESENT and outputless — re-derived
  read-only at `badf45a`, `./node_modules/.bin/tsc -b packages/cli --clean --dry` under the
  pinned typescript 6.0.3 lists **4,781** file paths, **five** under `packages/cli/`
  (`.tsbuildinfo` among them) and **zero** directory entries — so the unit is `skipped`,
  §3.3's skip rule does not fire because its target was the DIRECTORY, and all four
  cli-scoped comparisons run. `bundleAt` is then the minimum over an EMPTY chunk set:
  reading `.mtimeMs` off `undefined` reaches §4's internal-error row at exit **2** from
  `check:all` member 0, and guarding it renders `bundle-freshness` `full` over a comparison
  that compared nothing — verbatim `C10-3`'s and `C15-2`'s two readings two exit codes
  apart, at the one comparison requirement 3 exists for. `checkPluginAssets` reaches an
  `undefined` comparand with NO admissible reason at all, because round 14's `C14-4` deleted
  `packages/cli/dist/plugin absent` on exactly the argument `C15-4` falsified one round
  later — standing rule 8 on `C14-4`'s own fix. Closed by re-keying §3.3's **SKIP RULE —
  ALL FOUR cli-SCOPED COMPARISONS** off directory existence and onto each comparison's OWN
  comparand, stated per comparison; by §3.2's new `bundleAt === undefined` rule with the
  mandated reason `packages/cli/dist holds no esbuild chunk` ranked into its REASON
  PRECEDENCE; by restoring §3.4's deleted spelling with a precedence of its own; and by
  keying the advisory's silence on the unit's STATE rather than on one directory, at §3.2
  and §2, which is what §4's line list and §6's false-negative battery already said.
  ⚠️ **`reduced` is NOT reinstated** — the answer is `not-run`, a status the enum already
  has, and §0, §2 and §3.2 all carry the qualifier that "chunkless ⇒ `unbuilt`" holds only
  where clause 1 is REACHED. ⚠️ **Standing rule 8 at the fix:** the re-keyed skip rule
  vacates no existing row, because §6's complete-cli-build rule binds every other cli-scoped
  fixture to hold a chunk, and both restored precedences leave rows 12, 20 and 26 on the
  spellings they already assert. Sites: §0's requirement 3 row, §1's battery count, §2's
  `checkBundleFreshness` and `reduced` comments, §3.2's Rule, its no-fallback paragraph, its
  `C15-2` paragraph, its structural-silence bullet and its REASON PRECEDENCE, §3.3's two
  skip-rule sentences, §3.4's coverage paragraph, §4's verbatim reason list, and §6's new
  row **46** with its exception, group and registry bullets.
- **`C17-2`, medium — §3.5 cited §3.3 as the authority for the one rule §3.3 exists to
  deny.** Its closing clause read "the comparison runs only on a `packages/cli` whose state
  is `compared`: `unbuilt` and `skipped` win, for §3.3's stated reason", while §3.3's skip
  rule and §3's table both state that only `unbuilt` suppresses these four. `CF13-4`'s
  shape, at the section `C15-4` did not walk. An implementer building the four from that
  sentence gates them on `state === "compared"` — verbatim the mutant §6 row 43 exists to
  kill — and fails row 43 on row 43's own fixture at exit 1 under `--strict`, while one
  building from §3.3 passes it; both sentences were live. ⚠️ **The reason no row caught it
  is coincidence and is now written down:** on every reachable `skipped` tree §3.5's own
  `dist/index.d.ts` side is absent too, so its coverage entry is identical either way.
- **`C17-3`, low — one state, two mandated spellings, on the commonest and quietest tree
  this design has.** §3.5's coverage rule named "the absent side" in the singular with no
  rule for BOTH absent, which is a fresh clone and both `meta-checks` legs — nothing built,
  `.dts-cache/` gitignored at `.gitignore:47` and never created by `npm ci`. §4's own
  verbatim-reason rule makes two spellings for one state a defect in terms, and nothing
  discriminated them: §6 row 23 pins the cache-PRESENT case alone and §5's `meta-checks`
  bullet names no reason. This is the gap `C15-2`'s consolidation widened §3.2's precedence
  line for, applied at §3.2 and never carried to §3.5 — standing rule 9. Fixed by a one-line
  precedence at §3.5, repointed at §0's requirement 8 row, and pinned by a both-absent twin
  on §6 row 23 in row 33's shape so nothing is renumbered.
- **`OP17-1`, medium — EIGHT sites priced this check's findings against a blocked push, and
  §5 of this same document measures that no run of it can block one.** `OP12-3`'s shape a
  twelfth time: the paragraph that MEASURES the reach was corrected long ago and every
  sentence RESTING on it was not. Re-derived read-only at `badf45a`, clean tree:
  `git config core.hooksPath` → `/home/eimi/.codex/git-hooks`, whose `pre-push` loops
  `for script_name in lint typecheck build test` and never `check:all`;
  `grep -rn "check:all" .github/` returns exactly two hits, both COMMENTS (`ci.yml:263`,
  `ci.yml:385`), and no `run:` step anywhere; and
  `git ls-files -z | xargs -0 grep -ln "check:all" | grep -v ^docs/` returns the **9** files
  §5 already enumerates, with no invocation among them. So
  `npm run check:stale-dist -- --strict` executes in NO automatic context, and the
  non-strict path is blind on a push too, because that hook runs `build` immediately before
  `test` — §5's own third bullet in terms. Two wrong results followed: §7 row 1's
  owner-accepted false positive was priced at up to 19 `rm -rf` plus a full rebuild as the
  cost of unblocking a push that is never blocked, which materially changes what was
  accepted; and exit 2's whole warrant — for a convention `grep -rn "exit(2)" scripts/`
  re-derives at **zero** precedent in this repository — was "when the check gates a push".
  Repointed onto §5's measured bound in ONE pass (standing rule 9), at all eight sites and
  not the five the finding named: §3.2's closure paragraph, §4's `stale-declarations` remedy
  row, §4's two `npm run build`-cannot-clear paragraphs, §4's report-by-default rationale,
  §4's exit-2 warrant, §7 row 1, and §7's toolchain row. ⚠️ **The exit-2 convention is
  RESTATED, not withdrawn:** member 0's non-zero exit stops the other 14 before any of them
  prints, so attribution is load-bearing on the one chain a human runs deliberately.
  ⚠️ **Standing rule 8, checked at the fix:** §5.1's non-strict `pretest` argument rests on
  "a strict `pretest` turns any false positive into a total block on local testing", which
  is measured, push-free and untouched; and §5.1's quotation of §4's "an ad-hoc local run
  stays non-punitive, and only the chained invocation fails" is preserved verbatim, so no
  citation dangles. ⚠️ **§9's round-1 `O-1` bullet KEEPS the sentence** as the history of
  what that round filed.
- **`OP17-2`, low — §4's zero-finding plan named THREE of the four `not-run` reasons
  reachable on the tree the rule is written for.** The enumeration gave `npm run build` for
  `packages/cli/dist absent`, `packages/cli/.dts-cache/index.d.ts absent` and an inlined
  unit's `<unit>/dist absent`, and omitted `packages/cli/dist/index.d.ts absent` — §3.5's
  own mandated reason, and the third of the three entries this same paragraph counts on the
  tree its own step 1 creates. An implementer building the renderer from that sentence emits
  a plan naming two of three degraded entries' artifacts and nothing for the published
  `types` entry, contradicting §6 row 39 and step 5's own general wording 70 lines below.
  `C14-4`'s mirror, one round-14 fix over. Fixed per `C11-4` by replacing the enumeration
  with the RULE rather than extending it by one — which is also what keeps it correct under
  `C17-1`'s two new spellings — with the three spellings NOT reachable at zero findings
  named as the bound. §6 row 39 already states the rule form and is unchanged.
- **`CF17-1`, medium — requirement 9's DOWNWARD half was discharged by nothing, and §0
  cited the one row that asserts its NON-firing.** §0's requirement 9 row discharged "incl.
  downward `extends`" with "§3 input rule 4 + §3.3. Fixture row 3, §6"; §3 rule 4 keeps the
  mandate live in terms ("a future config that extends into a unit AND emits into its
  compared outputs must still be covered") and §2 declares `descendantConfigs` as its
  element. Re-derived read-only at `badf45a`: that name occurs **once** in the whole design,
  at §2's declaration, and `downward`/`DOWNWARD` **twice**, at §0's requirement 9 row and
  that same line — none of the three anywhere in §6. So an input set built from rules 1-3
  alone passed every battery row, the false-negative battery, the wiring assertions and the
  live smoke test: row **3** is the only row whose trigger is a descendant config and since
  `C14-3` it mandates `stale-declarations` and explicitly **NOT** `stale-unit`, so the
  omitting implementation PASSES it; row 2 walks rule 3's UPWARD chain; the census-import
  predicate matches the import STATEMENT; and the live tree cannot discriminate, rule 4
  having **0** members there. Row **47** closes it, on a SYNTHETIC fixture, so §3's 21-of-30
  arithmetic and §8(l) are unaffected. ⚠️ **Standing rule 9 on the fix: §3's input-set rules
  are the section, and rule 2 was in the identical state.** Every src-triggered row moved a
  `*.ts`, so an input walk filtered to `*.ts` shipped rule 2 hardcoded never to fire against
  the **3** members it has on the live tree —
  `packages/connectors-jira/src/testkit/fixtures/read-scenario.cassette.json`, its
  `write-scenario` sibling, and `packages/engine-claude/src/live/fixtures/live-verdicts.json`,
  re-derived read-only at `badf45a` — while the false-negative battery's
  `.info`/`.snap`/`.mjs` bullet is the EXCLUSION side, which that same walk also passes.
  Row **48**.
- **`CF17-2`, low — three present-tense absence counts, all FALSE in the commit that
  published them, falsified by that commit's own apply.** §6 row 2's "`extends` occurs
  **zero** times anywhere in §6", §3.2's and §6 row 45's "`inlinedUnitsFromMetafile` occurs
  **three** times in this design and none of them in §6", and §9's round-16 entries
  repeating both. Re-derived at both shas: at `db857c3`, the document round 16 READ,
  `extends` in §6 is **0** and `inlinedUnitsFromMetafile` is **3** with **0** in §6 — both
  exactly right; at `badf45a`, the commit that PRINTS them, `extends` in §6 is **3**, all
  three inside row 2's own new cell, and `inlinedUnitsFromMetafile` is **6** with **1**
  inside §6, at row 45's own new cell. `C11-4`'s class — and `CF16-2` had forced this very
  remedy ONE BULLET AWAY, giving the preamble's fifth structural clause the sha it was run
  at, so the pattern was fixed in one place and reproduced in four others in the same
  commit. Each figure now states the SET at the sha it was measured at, and the historical
  figure keeps its sha. ⚠️ **A SET OF ROW NUMBERS IS NOT SCALE-FREE EITHER, and this
  remedy's own claim that it "survives the next append" was falsified by the next append
  (round 29, `C29-1`):** `extends` in §6 went from row 2 alone to rows 2, 47 and 48 at
  `896dd84`, the commit IMMEDIATELY after this one and by rows this same change set added,
  and to rows 2, 3, 47, 48 and 49 by `5fef126` — false in every one of the TWELVE commits
  from `896dd84` through `c672565`; `inlinedUnitsFromMetafile` went from row 45 alone to
  rows 42 and 45 at `3095116` and to rows 40, 42 and 45 at `2c203a0`. So the rule is: a
  document-wide occurrence claim carries the sha it was run at, or it names the PROPERTY
  that distinguishes the element — an assertion against prose, a multi-unit verdict against
  a single-unit one — and never the occurrence set, which every appending round
  re-invalidates (`C11-4`'s class at a set rather than at a count). ⚠️ **And this finding's
  own scope was narrower than the defect it names:** a THIRD claim of exactly this shape —
  round 16's `grep -n stemsUnder` reading, live at §0's requirement 2 row, at §3.1 and inside
  `C16-1`'s entry above — was false in `badf45a` too, §6 rows 14 and 44 already carrying the
  name there, and stood through all THIRTEEN commits from `badf45a` to `c672565` because
  this bullet enumerated the sites it had found rather than the claims of its own shape
  (standing rule 9, round 29). ⚠️ **And §6's rendered-text group bullet restated a
  SUBSET of itself:** "Rows 12, 23 and 40 are deliberately NOT in this group: their mutation
  column CALLS the comparison directly", while the same bullet says one sentence above that
  round 16's row **45** calls `checkBundleFreshness` directly — `CF14-4`'s "never restate a
  subset" at this section's own bookkeeping. Replaced by the RULE it encodes.
- **`CF17-3`, low — one fixture with two opposite mandates, and the site an implementer
  builds from was the wrong one.** §6's complete-cli-build rule listed row **43** among the
  deliberate exceptions ("their whole subject is an absent artifact") and omitted it from
  its own conforming list, while row 43's clean-fixture cell reads `cli COMPLETE (as row
11)` and its MUTATION is `rm -rf packages/cli/dist`. The absence is row 43's END STATE,
  not its baseline — unlike rows 12 and 23, whose clean fixtures DO carry it — and the
  rule's own `except where the row itself deletes or moves it` clause already covers a
  mutation. `CF13-4`'s shape. Row 43 moved into the conforming list; rows 12, 23 and round
  17's new row **46** are the exceptions, and the justification then reads correctly of all
  three without amendment.

ℹ️ **What round 17 re-derived and could not break:** §3, §3.1 and §3.3's three-clause
conjunction, its unit-state table, its cli reason table and its four-proxy chain, walked
against all eight IDs with no change to any unit-state VERDICT; §4's exit-code table, prefix
rule, per-kind line shapes, grouping rule, remedy table and ordered recipe steps 1-5, walked
row by row for the `CF14-1` shape, every remedy re-checked against the finding it is printed
for and every one able to clear it; §5 and §5.1 as written, which are the measurements
`OP17-1` and `OP17-2` repoint onto; §0's other eight requirement rows; §7's other eleven
rows; §6's rows 1, 3-45, its false-negative battery, its wiring assertions and its live
smoke test; and §8(a)-(p), none of which gains a residual, because `C17-1` and `C17-2` are
damage repaired, `C17-3` and `OP17-2` are a spelling and a rule stated rather than limits
conceded, and `CF17-1` adds assertions. **No working-tree mutation:**
`scripts/bundle-cli.mjs` was never imported, every probe read files as text or ran
`git`/`grep`/`tsc --dry` read-only, and `git status --porcelain` is empty at `badf45a`.

Battery rows **46**, **47** and **48** were appended BELOW ROW 45 per §6's APPEND RULE,
nothing renumbered; prettier and the preamble's five structural clauses — the orphan-row
sweep among them — are re-run on the candidate before the commit, and their results are
reported with the sha they were run at.

**Round 18 (2026-08-21) — `contract-fit` / `correctness` / `operability`, run as a COVERAGE
MATRIX rather than as a sweep: every testable obligation enumerated from the text, then the
§6 element that makes each one falsifiable named, with UNCOVERED recorded wherever the only
candidate would also pass a plausible wrong implementation. Sixteen finding IDs survived
consolidation, all re-derived read-only at `896dd84` on a clean tree and all dispositioned:
`CF18-1`, `CF18-3`, `C18-1` … `C18-7`, `OP18-1` … `OP18-7`. Entered here in the SAME round
that filed them, which is the preamble's own standing requirement. Every one has ONE shape:
a rule §0, §2, §3 or §4 states correctly, whose only candidate element in §6 is satisfied by
the very implementation the rule forbids. NO battery row is appended and nothing is
renumbered — every fix is a twin in row 33's shape or an extension of a row's existing
assertions — so §1's **48** stays correct and §6's registry needs no re-run.**

- **`CF18-1`, medium — §3 input rule 3 has TWO members and §6 exercised only the SECOND.**
  Requirement 6's own subject line reads "each unit's `tsconfig.json` + its `extends` chain"
  and its cell discharged both with "Fixture row 2", whose mutation is the shared ANCESTOR
  `tsconfig.base.json`. Re-derived over §6's whole mutation column: NO row moves a unit's
  own `tsconfig.json`, so an input set built from the resolved ancestors alone — equivalently
  `configChain` read as excluding its own argument — passed everything, left all **19** unit
  configs outside every input set, and printed PASS at exit 0 even under `--strict` on a
  `packages/gates/dist` compiled under the old `compilerOptions`. §3's "21 of the 30" reads
  **2 of 30** under it. Closed by a TWIN on row 2, with that row's baseline strengthened —
  its clean cell constrained only `src` against `dist`, so under a correct rule 3 the
  baseline was already `stale-unit ×2` and BOTH halves were vacuous by construction
  (`CF8-2`'s shape).
- **`C18-1`, high — the walkers' RECURSION is mandated by §3 and §2 and was required by no
  fixture.** No battery row required either walked side to hold a member below depth 0 whose
  depth changes a verdict, so a `readdirSync` omitting `recursive: true` passed every row.
  Re-derived read-only: `packages/contracts/src` holds **141** nested `.ts` against **1**
  flat, and across the 19 root references **933** nested `.js` against **332** flat outside
  `dist/plugin/` and the scratch dirs. ⚠️ **The finding claimed "every `src`/`dist` path token
  in §6 sits at depth 0" and that is FALSE — `dist/bin/supervisord.js` is depth 1 on every
  cli-complete fixture; it is EXISTENCE-checked by §3.3's clause 3 and never required to be
  the newest output, so it does not read the recursion. The claim was narrowed in
  consolidation to what is verifiable (standing rule 1).** Closed at row 1 (mtime side, both
  walk directions) and row 4 (stem side), each failing a shallow walk on its own fixture.
- **`OP18-1`, high — §4's exit-table DEFAULT column was asserted by nothing.** `exitCodeFor`
  occurs in §6 exactly twice, at row 21 and at the false-negative battery's truncated-marker
  bullet, and BOTH pass `strict: true`; the literal `strict: false` occurs **zero** times in
  the whole file. So an `exitCodeFor` that IGNORES its `strict` argument passed every element
  in §6. `pretest` carries no flag and §6 predicts FOUR `stale-unit` findings on this working
  copy, so that implementation blocks every developer's `npm test` from the first run —
  round 1's `O-2` reached through the exit code rather than through a throw. Closed by
  extending row 21 from two assertions to four on the two fixtures it already holds.
- **`C18-6`, medium — `dist/index.d.ts`'s exclusion from `packages/cli`'s mtime side was
  reached by no element, and both citations for it pointed at the neighbouring exclusion's
  row.** Three of §3's four output-set minus-bullets have their own row; this one cited row
  7, whose mutation is `dist/plugin/.mcp.json` and whose verdict cell says the plugin
  exclusion is what it tests — `CF13-4`'s shape. Re-derived: ZERO rows move that artifact's
  mtime (row 32 deletes it, row 22 licenses `mtime UNCHANGED or NEWER` on a byte-keyed
  verdict). ⚠️ **The exposure is BOUNDED and the bound re-derived rather than taken from the
  finding (standing rule 2):** `packages/cli/dist/index.d.ts` at `1787319590020415462`
  against the newest esbuild output `dist/index.js` at `1787319590009315482` — **11.10 ms**,
  equal at second granularity. Closed by a TWIN on row 7 moving that artifact instead, which
  must still report `stale-unit packages/cli` and explicitly NOT `unbuilt` and NOT clean.
  ⚠️ **`CF18-2` filed the identical obligation with a different remedy — a fixture reordering
  on row 38 — and was rejected in consolidation: row 38 already carries four assertions and
  gains a fifth this round, and coupling all five to an mtime window between two artifacts is
  strictly more fragile than a twin that flips on its own fixture. Its evidence is carried
  here; only its remedy is dropped.**
- **`C18-2`, medium — §3.2's REASON PRECEDENCE is a five-rank total order and only one
  adjacent pair was pinned.** Row 46's twins pin 3 over 4; nothing pinned 2 over 4, the pair
  this line's own rationale names ("on a fresh clone and in both `meta-checks` legs EVERY
  unit is bare, so without this line that tree has two admissible spellings"). Row 16 and row
  39's main half delete `packages/cli/dist` while `.bundle-meta/` survives beside it, so rank
  4 is unreachable there. Closed on row 39's NEGATIVE twin, which already runs that exact
  tree, now asserting all four coverage reasons verbatim; its mutant is an implementation
  reading the MARKER first. ⚠️ **The finding named rank 5 as the unpinned rank and a
  410-character reason as the wrong output, and BOTH were rejected in consolidation:** rank
  5's cause is derived from `inlinedUnitsFromMetafile(metafile)`, and that fixture has no
  `.bundle-meta`, so the set is not derivable and the named mutant cannot produce that reason
  there — it would have passed. Rank 5 against ranks 2-4 is recorded at §3.2 as a residual no
  fixture reaches, rather than claimed as discharged.
- **`C18-3`, medium — §3.5's cache-absent-only spelling was produced by no assertion.** Rows
  23 and 26 and row 23's round-17 twin assert `packages/cli/dist/index.d.ts absent` or
  `packages/cli unbuilt`, so a `checkShippedDeclarations` hardcoding the first for BOTH
  absent sides passed every row while §4's recipe step 5 named an artifact that EXISTS.
  Closed at row 17, whose mutation is already `rm -rf packages/cli/.dts-cache` — the only
  fixture in §6 that leaves the cache absent and the shipped copy present.
- **`C18-4`, medium — §3.3's comparand says "and its chain" and only the LEAF was
  exercised.** Row 3 mutates `tsconfig.dts.json`; rows 6 and 42 cover the other two members;
  the chain had none, and `bundle-types.mjs:43-61` stats no `.json` at all, so a
  `tsconfig.base.json` edit invalidated the cache from neither inside nor outside — verbatim
  what `C15-3` filed for the two members beside it. Closed by a TWIN on row 3 moving the
  fixture's own `tsconfig.base.json`; the mutation was restated in consolidation as "forward
  past the newest output under EVERY unit's `dist`", because "past `.dts-cache/index.d.ts`"
  alone does not force the `stale-unit` half the twin asserts.
- **`C18-5`, medium — §3 mandates TWO wrapped reads and cited one row for both.** Row 27
  mutates a UNIT config, read inside `units.mjs`; `enumerateRootReferences` `readFileSync`s
  and `JSON.parse`s the ROOT `tsconfig.json`, intact on that fixture — so a `units.mjs`
  wrapping its own reads and calling the census UNWRAPPED passed rows 27 and 31 alike and, on
  a malformed root config, printed a `JSON.parse` message with no locus at exit **2** from
  `check:all` member 0. Closed by a TWIN on row 27 whose ROOT config is the malformed file.
- **`C18-7`, medium — `bundleAt` is mandated as the MINIMUM and no fixture gave two chunks
  different mtimes.** Rows 25 and 33 exercise chunk MEMBERSHIP by deletion, row 38's mutation
  moves past everything so min and max agree, row 46's chunk set is empty — and §2 declares
  no minimum helper at all, so `newestUnder(packages/cli/dist, isChunk)` was the path of
  least resistance and passed everything at the one comparison requirement 3 exists for.
  Closed by giving row 5 two chunks and a mutation strictly between them.
- **`OP18-2`, medium — the N=1 collapse was fixed for one of the four kinds it governs.**
  `stale-bundle ×`, `stale-declarations ×` and `stale-plugin-assets ×` occur **zero** times
  in this design, and each of those kinds is structurally N=1 — one `packages/cli`-scoped
  comparison apiece — which is exactly the N at which round 15's own mutant fires. Standing
  rule 9 at `OP15-3`'s fix. Closed at row 38, whose fixture already fires `stale-bundle`
  beside `stale-unit`.
- **`OP18-3`, medium — two of §4's FOUR carve-outs had a fixture and no assertion.** Row 21's
  fixture IS the sole-`bundle-provenance-missing` run and asserted `exitCodeFor` only; row
  42's IS the sole-`stale-declarations` run and asserted the KIND only, `C15-3` having
  recorded it verbatim as the fixture that carve-out never had. Supplying a fixture is not
  asserting it. Closed by two twins in row 33's shape. ⚠️ **Row 21's mutant was corrected in
  consolidation:** the finding named a step-walking formatter, and §4 step 4 states in terms
  that step 1's own condition already selects nothing on that tree, so that mutant would have
  passed; the mutant is now a formatter whose step-1 trigger is the finding SET, which §4's
  remedy table invites by mapping this kind to a rebuild.
- **`OP18-4`, medium — the zero-finding producer rule was written over ABSENCE spellings and
  round 17's own new reason is not one.** Re-derived over §4's verbatim list: NINE mandated
  reasons, five absence spellings the rule reaches, three named in the bound, and
  `packages/cli/dist holds no esbuild chunk` (`C17-1`) in NEITHER. It is reachable at zero
  findings on round 17's own new row 46 — a DIRECT-CALL row, so the reporter never runs
  there. `OP17-2`'s defect at the spelling the same round created, in the same commit.
  Stated over REASONS at both sites, with row 46 gaining a rendered plan twin whose fixture
  gains row 3's cache clause, without which the zero-finding premise is not established.
- **`OP18-5`, low — the `bundle-provenance-missing` line's SECOND variant had no row.** §6
  renders that kind at row 29 alone, whose fixture is row 21's, marker ABSENT, while
  `unreadable` occurs in §6 exactly once, in the truncated-marker bullet, which never reads
  the rendered line. So a `formatFindings` carrying ONE hard-coded line spelling `absent`
  passed everything and told the operator a file was absent while it sat on disk. Round 12's
  `C11-3` gave the `unbuilt` kind's second variant row 32 for exactly this reason. Closed at
  row 29's fifth fixture, which is the truncated-marker tree again.
- **`OP18-6`, low — §4's "the WARN line carries neither" was asserted by nothing.** Rows 27
  and 31 assert `contains` of the prefix and forbid no extra text, so a `formatInternalError`
  building ONE string and varying only the `WARN`/`ERROR` token passed both — printing, on
  the one path that is never chained, a member-0 sentence guaranteed FALSE. `OP14-4`'s own
  defect at the twin line its fix did not reach.
- **`OP18-7`, advisory — nothing read `--json` on a run that COMPLETES.** §6's only `--json`
  invocations are row 27's, all on a THROWING fixture whose asserted object §4 states carries
  no `coverage` key, so a CLI emitting `{ findings, states }` and dropping `coverage` passed
  everything while the machine path lost the only signal that requirement 3's comparison did
  not run — `O9-1`'s class one path over, at the bullet claiming the two paths cannot drift.
  Closed by a fourth invocation on row 39.
- **`CF18-3`, low — one reason, three renderings, all byte-identical at N=1.** §6 row 40's
  mutation deletes exactly ONE inlined `dist`, and §6's `C15-2` sibling-fixture rule binds
  every OTHER row asserting `stale-bundle` or a `full` `bundle-freshness` entry to hold
  qualifying output under every inlined member — so by RULE no row could reach a second
  offender, and `paths.map((p) => p + " absent").join(", ")` and `paths[0] + " absent"` both
  passed. The two-offender tree is this design's own: §4 recipe step 1's FOURTH carve-out
  exempts `packages/cli`. Closed by a TWIN on row 40 at two offenders, with the JOIN ORDER
  now stated at §3.2 (`buildUnits`' own order, which `enumerateRootReferences` sorts), since
  at N=2 the order is a second free variable.

ℹ️ **What round 18 re-derived and could not break:** §3.1's stem bijection and its
`packages/cli` exemption; §3.2's consumer set, metafile derivation, DISJOINTNESS RULE,
structural-silence bullet and deleted `reduced` fallback; §3.3's three-clause conjunction,
its unit-state and cli reason tables and its four-proxy chain; §3.4 in full; §3.5's byte
keying and its `C17-3` precedence; §4's exit-code table, prefix rule, per-kind line shapes,
grouping rule, remedy table and recipe steps 1-5; §5's `check:all` census; §7's thirteen
rows, row 1's owner-accepted cost still priced at §6 row 2's UNCHANGED original half; and
§8(a)-(p), none of which gains a residual, because every fix is an assertion added rather
than a limit conceded — the single exception being §3.2's rank-5 residual, which is a gap
STATED rather than a cost accepted. The `see above`/`see below` sweep below `## 0.` returns
only backticked quotations of `OP13-2`'s fixed cell as history, so round 10's "guard WHAT an
apply inserts" holds at every live site and no placeholder or editor directive stands in as
document text anywhere. **No working-tree mutation:** `scripts/bundle-cli.mjs` was never
imported, every probe read files as text or `stat`ed them, and `git status --porcelain` is
empty at `896dd84`. Prettier and the preamble's five structural clauses are re-run on the
candidate before the commit, with their results reported against the sha they were run at.

**Round 19 (2026-08-21) — `contract-fit` / `correctness` / `operability`, run as the SAME
COVERAGE MATRIX as round 18 and enumerated INDEPENDENTLY of round 18's list rather than as a
check that its fixes landed: obligations re-derived from the document as round 18 left it, in
each lens's own order, with UNCOVERED recorded wherever the only candidate element would also
pass a plausible wrong implementation. FOUR finding IDs survived consolidation, all re-derived
read-only at `7dfa1ba` on a clean tree and all dispositioned `fixed`: `CF19-1`, `C19-1`,
`C19-2`, `OP19-1`. Entered here in the SAME round that filed them, which is the preamble's own
standing requirement. Every one has round 18's shape at a site round 18 did not reach — a rule
§0, §2, §3 or §4 states correctly whose only candidate element in §6 is satisfied by the very
implementation the rule forbids. NO battery row is appended and nothing is renumbered — every
fix is a TWIN in row 33's shape — so §1's **48** stays correct and §6's registry needs no
re-run.**

- **`CF19-1`, medium — `exitCodeFor` honours its argument and nothing read what the CLI
  PASSES it.** Round 18's `OP18-1` closed the FUNCTION at row 21's four DIRECT calls; the
  WIRING stayed open. Re-derived at `7dfa1ba`: rows 27 and 31 are the only §6 elements that
  run `check-stale-dist.mjs` as a process, and neither runs it on a tree carrying a finding —
  row 27's fixture is a malformed config, every exit code row 31 asserts is its LOAD-TIME
  mutant's, and row 31's clean baseline is a ZERO-finding tree where exit 0 is correct under
  either `strict`. So a success path spelled `process.exit(exitCodeFor(result, { strict: true }))`,
  or equivalently `process.exit(result.findings.length ? 1 : 0)`, passed every element in this
  design while `pretest` — `node scripts/check-stale-dist.mjs`, no flag — exited **1** on the
  four `stale-unit` findings §6 predicts for this working copy, which §5.1's measured table
  makes PRETEST FAILED with `test` never running: round 1's `O-2` reached through the CLI's
  ARGUMENT rather than through the function or a throw, and §4's DEFAULT column void with
  every assertion green. Closed by a CLI TWIN on row **43**, the one fixture in §6 that
  carries a non-advisory finding on a tree whose CLI can be run: the flagless process at exit
  **0** with the line still rendered, and `--strict` at exit **1** on the same fixture — the
  PAIR, since a flagless half alone is satisfied by a CLI that always exits 0. ⚠️ **Two of the
  finding's own claims were corrected in consolidation (standing rules 1 and 2):** "BOTH of
  their fixtures THROW" is false of row 31's clean baseline, which reports clean, and is
  narrowed at every site to what is verifiable; and the drafted literal
  `check-stale-dist: stale-declarations packages/cli` would have FAILED a correct
  implementation, `stale-declarations` being a delta-carrying kind that collapses at every N
  including N=1 (`OP15-3`, `OP18-2`) — the twin pins `stale-declarations ×1 — packages/cli`,
  `OP16-3`'s class caught before the row was written. Sites: §0's requirement 10 row, §1's
  `check-stale-dist.mjs` cell (which mandates the parsed flag at the success-path call, the
  specification gap that let the argument be a literal), §2's `exitCodeFor` comment, §4's
  DEFAULT-column paragraph, §5.1's report-and-continue argument, §6's preamble, group,
  mutant-formatter and prefix enumerations, §6 rows 21 and 43, §6's `pretest` wiring comment
  and §7 row 1, whose owner-accepted cost is priced on the check reprinting rather than
  blocking.
- **`C19-1`, medium — §3.2's REASON PRECEDENCE rank 1 was in neither the pinned list nor the
  residual, and the rank it puts FIRST was asserted only where every competitor is
  unreachable.** Re-derived read-only at `7dfa1ba`: `grep -n 'packages/cli unbuilt'` returns
  three hits inside §6, two of them prose inside rows 17 and 19, so the only ASSERTION is row
  26 — whose own cell reads "NOTHING is absent on this fixture". That is verbatim the argument
  §3.2 already makes about row 16 for rank 2 ("evidence for the spelling and never for the
  ordering") and did not make about row 26. An implementation resolving each entry's reason
  from its own comparand and falling back to `packages/cli unbuilt` only when nothing is
  absent — exactly how round 10's `C10-1` introduced the reason — passed every element in §6.
  The state is not hypothetical: `.bundle-meta/` is absent on every tree until the first
  post-adoption build, so ranks 1 and 4 co-occur on EVERY `unbuilt` `packages/cli` today, at
  `check:all` member 0, telling the operator a bookkeeping file is missing when the bundle is
  broken — the one-state-two-spellings class §4's verbatim-reason rule calls a defect in
  terms. Closed by a TWIN on row **26** whose fixture removes `.bundle-meta/`, every
  chunk-pattern file, `dist/plugin/`, `dist/index.d.ts` and `.dts-cache/index.d.ts`, so all
  four entries have a competing absence spelling live and all four must still read
  `packages/cli unbuilt`. ⚠️ **Row 26's MAIN half is UNCHANGED (standing rule 8):** it is the
  only element pinning that the suppression emits an entry at all. Consolidation also settled
  that ranks 1 and 2 cannot CO-OCCUR — an absent `packages/cli/dist` is `skipped`, never
  `unbuilt` — so that pair is recorded as unreachable rather than left as a second residual.
  Sites: §0's requirement 8 row, §2's `status` comment, §3's coverage bullet, §3.2's
  structural-silence bullet and its REASON-and-RANK paragraph (whose residual now reads over
  ranks 1-4), §3.3's **SKIP RULE — ALL FOUR cli-SCOPED COMPARISONS**, §3.4's and §3.5's own
  precedence lines — both of which named an element for their lower ranks and none for rank 1
  — and §4's verbatim reason list at two points.
- **`C19-2`, low — §3.3's clause 2 mandates `>=` and the EQUAL case was a free variable in
  every fixture.** Re-derived read-only at `7dfa1ba` over §6's whole fixture column: every
  specification of the marker's mtime reads `at or after` or `newest` — the complete-cli-build
  rule, the truncated-marker bullet, and rows 5, 16, 19, 26, 32, 33, 37, 38, 39, 40, 41, 42,
  45 and 46 — and the battery's ONE equal-mtime element, the false-negative bullet's `input
mtime exactly equal to output mtime`, is §3's input-versus-output Verdict, which the marker
  is not an input to. So a fixture author writing the marker strictly after the newest output
  — the natural choice, `:181` running last — left a clause 2 spelled `>` passing every
  element in this design, while on `packages/cli` COMPLETE with `mtime(marker) == mtime(newest
§3-filtered output)` — what `:178` → `:181` produces on any second-granularity filesystem,
  the exact state §7's `Second-granularity mtimes` row claims as mitigated — it returns
  `unbuilt packages/cli` and prints `rm -rf packages/cli/dist` plus a ~5-minute rebuild at
  `check:all` member 0, exit **1** under `--strict`, on a correctly completed build. §8(e)
  names exactly one way clause 2 can over-report; under `>` there is a second. Closed by a
  TWIN on row **19** at EXACT equality, whose mutant is clause 2 spelled `>` and which needs
  no injection point. ⚠️ **The finding's `all four cli-scoped entries full` clause was REJECTED
  in consolidation (`OP16-3`'s class):** row 19's clean fixture is bound by neither §6's
  `C15-2` sibling-fixture rule nor row 3's cache clause, so three of the four entries are free
  variables there and that assertion would have FAILED a correct implementation. The twin
  asserts the unit STATE alone — `compared`, not `unbuilt`, no `unbuilt packages/cli` finding
  — which is what the boundary needs and adds no fixture clause. Sites: §3.3's clause 2, §6
  row 19, §6's complete-cli-build rule (`at or after` recorded as a LICENCE with this one row
  taking its boundary, so the pair is not one fixture with two mandates — `CF17-3`'s shape),
  §7's `Second-granularity mtimes` row and §8(e). ⚠️ **Row 19's main half keeps `marker
newest` and its forward mutation, and the rule keeps `at or after` as the general licence
  (standing rule 8).**
- **`OP19-1`, medium — the `unbuilt` kind has TWO mandated sentences and only one was ever
  rendered.** §3.3's cli reason table gives `packages/cli` two `unbuilt` reasons —
  `bundle:cli did not finish` for any missing member of the artifact set, and
  `tsc -b --clean removed the compiler outputs from a completed bundle` for the `--clean`
  signature — and §4 prints `unitState()`'s reason VERBATIM. Machine-checked over every
  cli-`unbuilt` row (11, 19, 20, 25, 32, 33, 36, 37, 46) at `7dfa1ba`: row **32** is the only
  one pinning a reason SENTENCE by equality, and it pins the `--clean` one.
  `bundle:cli did not finish` occurs **5** times in this design — §3 twice, §4 twice, §6 ONCE
  — and its single §6 occurrence is row 32's MUTANT, which that row requires to FAIL. Row 30
  does not reach it either: its fixtures are rows 9, 13 and 14's end states, whose one
  `unbuilt` unit is not `packages/cli`. So a `unitState` collapsing the table's first two rows
  onto the `--clean` sentence plus the gap list passed all nine rows, the false-negative
  battery, the wiring assertions and the live smoke test; on row 20's own end state
  (`rm -rf packages/cli/dist/plugin/skills` — an `npm run build` interrupted in the ~5-minute
  `bundle:types` step, or `bundle-cli.mjs` throwing at `:147` on a fresh clone) it tells the
  operator a command they never ran deleted their outputs, at exit **1** under `--strict` from
  `check:all` member 0. That is §4's own OP-2 wrong-diagnosis class, `C11-3` with its two
  sentences swapped at the row `C11-3`'s fix created — standing rule 8 on that fix — and
  `OP18-5`'s shape at a second kind. Closed by a RENDERED TWIN on row **20** asserting the
  line by equality, its mutant the mirror of row 32's so neither collapse survives.
  ⚠️ **The finding's drafted gap literal `missing: dist/plugin` was corrected to
  `dist/plugin/skills`** — §4's own worked example, §3.3 clause 1's top-level-path test and
  `PLUGIN_ASSET_ENTRIES` all give the entry path, and the shorter spelling would have failed a
  correct implementation. Sites: §3.3's reason table, §4's line list, §2's `cliArtifactGaps`
  comment and §6's preamble, group, mutant-formatter and prefix enumerations, per standing
  rule 9.

ℹ️ **What round 19 re-derived and could not break**, recorded so a further round is not spent
on it: §3's four input rules and its output filter, rule 3's OWN-config member and row 2's
round-18 twin included; §3.1 in full; §3.2's consumer set, metafile derivation, DISJOINTNESS
RULE, `bundleAt` minimum rule, chunkless `not-run` reason and deleted `reduced` fallback;
§3.3's three-clause conjunction, its unit-state table, its four-proxy chain and its
`build:clean` indistinguishability paragraph — §2's branch list and that table state the
clause-2 boundary as `OLDER`/`NOT older`, which is boundary-exact prose and needed no operator
pointer; §3.4 and §3.5 apart from their rank-1 clauses, §3.5's per-spelling rows (`C18-3`)
being the precedent `OP19-1` follows; §4's exit-code table, prefix rule, per-kind line shapes
(the clause-2 `unbuilt` line reads "is NEWER than", correct under `>=`), grouping rule, remedy
table — every remedy re-checked against the finding it is printed for and every one able to
clear it — and ordered recipe steps 1-5 with all four carve-outs; §5 and §5.1 as measured,
whose `meta-checks` bullet runs a tree where `packages/cli` is `skipped` and rank 1 is not
live; §0's other nine requirement rows; §7's twelve other rows — row 2 describes a tree on
which nothing IS absent and is true as written; §6's rows 1-18, 22-25, 27-42 and 44-48, its
false-negative battery, its wiring assertions, its live smoke test, its APPEND RULE and its
registry bullet (no row appended, so the sha-pinned derivation stands); and §8(a)-(p), none of
which gains a residual, because `C19-1` NARROWS §3.2's existing rank residual rather than
adding one and the other three are assertions added rather than limits conceded.
⚠️ **Rows 19 and 37 were considered for `OP19-1`'s tightening and deliberately NOT changed.**
Their verdict cells read `reason naming dist/bin.js` and `carrying the clause-2 reason`, and
the clause-2 row has exactly ONE mandated spelling, so a collapse onto either other sentence
fails row 19's cell as written. The asymmetry that made row 20 vacuous is that BOTH
artifact-set sentences carry a gap list, so `gaps naming dist/plugin` could not tell them
apart; and moving row 37 into the rendered-text group would falsify the four sentences that
cite it as the verdict-row exemplar. The `see above`/`see below` sweep below `## 0.` was
re-run and returns only backticked quotations of `OP13-2`'s fixed cell used as history, and a
sweep for `TODO`/`TBD`/`FIXME`/`<insert`/placeholder text returns no live site, so round 10's
"guard WHAT an apply inserts" holds everywhere. **No working-tree mutation:**
`scripts/bundle-cli.mjs` was never imported, every probe read files as text or ran `git` and
`grep` read-only, and `git status --porcelain` is empty at `7dfa1ba`. Prettier and the
preamble's five structural clauses are re-run on the candidate before the commit, with their
results reported against the sha they were run at.

**Round 20 (2026-08-21) — `contract-fit` / `correctness` / `operability`, run as the SAME
COVERAGE MATRIX as rounds 18 and 19 and enumerated INDEPENDENTLY of round 19's list rather
than as a check that its fixes landed: obligations re-derived from the document as round 19
left it, in each lens's own order, with UNCOVERED recorded wherever the only candidate
element would also pass a plausible wrong implementation. FOUR finding IDs survived
consolidation, all re-derived read-only at `9c247eb` on a clean tree and all dispositioned
`fixed`: `CF20-1`, `C20-1`, `C20-2`, `OP20-1`. Entered here in the SAME round that filed
them, which is the preamble's own standing requirement. NO battery row is appended and
nothing is renumbered — every fix is a TWIN in row 33's shape — so §1's **48** stays correct
and §6's registry needs no re-run.**

- **`CF20-1`, medium — requirement 3's only wiring surface asserted a contiguous source
  substring, and this repository's own format gate rewrites it.** §6's `WRITE` was
  `writeFile(join(CLI_ROOT, ".bundle-meta", "metafile.json")`. Re-derived read-only at
  `9c247eb`: `.prettierrc.json` sets `printWidth: 100`, `.prettierignore` does not cover
  `scripts/`, `npm run format` is `prettier --check .`, and the shortest conforming
  one-line write at the wipe block's 2-space indent is exactly **100** characters — at the
  width, zero margin. Run through this repository's prettier 3.9.5 over scratchpad copies,
  that form survives `--check` while the same call one argument longer is rewritten across
  four lines, on which `src.includes(WRITE)` is **false** with all five §1 edits landed and
  correctly ordered: `OP16-3`'s class, an assertion that fails a correct implementation, at
  the one surface §0's requirement 3 calls "the only thing that establishes" it. A second,
  formatter-independent reach is ordinary factoring — `writeFile` cannot create
  `.bundle-meta/`, so a `mkdir` and a `metaDir` const come with it. And the block THREW
  rather than failing readably: `lineWith(WRITE)` is `undefined` when no single line holds
  the anchor, so `lineWith(WRITE).trim()` in the third mutant raised `TypeError: Cannot
read properties of undefined (reading 'trim')` — measured on both the wrapped and the
  factored source. §6 had already learned this at its two OTHER source reads, whose
  newline-spanning predicates match a wrapped import, and did not carry it here (standing
  rules 7 and 9). The `CLEAR` half had 23 columns of margin at **77** characters,
  re-derived rather than assumed, so the exposure was bounded to the write plus any later
  edit to the clear.
  **Fixed** by re-keying both halves to callee-plus-literal regexes over the raw source,
  which keeps the clear and the write distinguishable by CALLEE so `CF9-4` stays closed; by
  taking order with `search()` on the regex halves and `indexOf` on the literal ones; and by
  replacing `lineWith` with a statement-level splitter, well-defined here because
  `.prettierrc.json` sets `semi: true`, so no mutant can throw. Quantifiers are pluses and
  not stars, per `CF15-1`'s rule for this span. Standing rule 8, checked AT the fix and
  re-derived 2026-08-21: the predicate is TRUE on the one-line, prettier-wrapped and
  `metaDir`-factored conforming sources, all three mutants FLIP it on all three, and it is
  FALSE both on a bundler carrying round 8's clear with no write at all and on the unedited
  bundler.
  ⚠️ **The same finding caught a second, older overclaim.** §1's "FIVE source properties,
  each proved non-vacuous by a mutated copy of the source" is false of the fifth: the
  `.gitignore` read is a bare `toContain` and never had a mutant. Stated apart — four are
  mutant-proved, the fifth's only failure mode is the absence it asserts against — rather
  than adding a mutant that could only restate the assertion.
- **`C20-1`, high — §3's output filter is a four-extension WHITELIST before it is a list of
  exclusions, and only the exclusions had elements.** Every fixture §0's requirement 1 row
  cites asserts a minus-bullet; §6 row 48 CITES the four extensions and its fixture holds
  no `.map` and no `dist/**.json`; row 22's twin writes `dist/index.d.ts.map` in the SAME
  emit as `dist/index.js`, so it moves no verdict either way. Both wrong readings therefore
  passed the whole battery, and they are two verdicts apart on this repository's own tree.
  Re-derived read-only at `9c247eb` with an independent walker: under the CORRECT filter
  this working copy carries **FOUR** `stale-unit` — `packages/contracts`,
  `packages/engine-claude`, `packages/gates`, `packages/plugin`, exactly §6's live
  prediction — while a whitelist of `.js`/`.d.ts` alone carrying all four minus-bullets and
  `recursive: true` reports **SEVEN**, adding `packages/supervisor`, `packages/scheduler`
  and `packages/learning`, whose newest filtered output is a `.d.ts.map`
  (1787133004.80 / 1787133015.08 / 1787133016.63) with the newest `src` input strictly
  between it and the newest `.js`/`.d.ts` — at `check:all` member 0, exit **1** under
  `--strict`, and under flagless `pretest`. The reciprocal reading, no whitelist at all,
  takes a `dist/**.json` as an output: `resolveJsonModule: true` (`tsconfig.base.json:10`)
  makes that reachable the moment a compiled `.ts` imports one of the 3 `src/**.json` rule 2
  counts as inputs, and §3.1's stem walk then reports an `orphan-output` whose printed
  remedy re-emits the file. ⚠️ **§8(g) mandated that second reading in terms** — "an emitted
  `.json` would break [the bijection] — which the orphan check would report as a finding" —
  so the design contradicted its own filter at the site an implementer builds the predicate
  from, `CF13-4`'s shape at the output set. Closed by a TWIN on row **44** — the same
  predicate's mirror, its subject the exclusion side and the twin's the inclusion side, on
  a fixture already in the newest-file-under-`dist` shape both halves need — carrying a
  `.d.ts.map` at an EXISTING 1:1 stem as the newest file under `dist` and a
  `dist/build-info.json` older than everything with no `src` counterpart. Two mutants, one
  per walked side, both failing on that fixture: the too-narrow whitelist at the MTIME side,
  the absent whitelist at the STEM side. ⚠️ **The twin's `.d.ts.map` was moved onto a shared
  stem in consolidation:** at a stem no `src` file carries it is itself an `orphan-output`
  under the correct filter, so the baseline the row asserts CLEAN would already have been
  failing and the mutation would have flipped nothing — `CF8-2`'s vacuity, caught before the
  row was written. §7's `.d.ts.map` row becomes an ASSERTED accepted false negative rather
  than a named one, three units of this working copy being in that state today. ⚠️ **The
  finding's "ONE predicate is the accept argument for `newestUnder` AND for `stemsUnder`"
  clause was REJECTED in consolidation (standing rule 8):** row 44's own cell rules that
  "neither row subsumes the other, because a `stemsUnder` accept-predicate and an
  `isCompilerOutput` are two places one exclusion has to land", and merging them would
  vacate that sentence and make row 14's stem half redundant by construction. The whitelist
  is stated as governing both sides without merging them, which the two-mutant twin already
  proves.
- **`C20-2`, medium — §3.3's comparand member 1 is `packages/*/src` and its SCOPE was
  asserted by nothing for nineteen rounds.** Members 2, 3 and 4 each carry their
  non-vacuity row in that list (rows 3's twin, 42); member 1 carried none, and §2 hands
  `checkDeclarationCache(units)` no `cwd`, so `units.map(u => u.srcDir)` is the only
  source-side collection the signature offers — while §3's Units step says in terms that
  those 19 exceed `workspaces` (18) by exactly `e2e/report`, whose `src` holds **10** `.ts`
  (re-derived 2026-08-21, against 18 `packages/*` dirs). Nothing discriminated the two
  readings: EVERY fixture unit in §6 is under `packages/` — re-derived read-only at
  `9c247eb` over the battery's own line range, the only four `e2e/report` hits being prose
  about `enumerateWorkspaces`, row 39's PASS-line literal and the wiring block. The wrong
  reading fires `stale-declarations packages/cli` on a `touch e2e/report/src/<any>.ts` at
  `check:all` member 0, exit **1** under `--strict`, on a cache no byte of that unit can
  reach — and §4's remedy row cannot clear it: `bundle-types.mjs`'s `newestSourceMtime()`
  (`:43-61`, re-read 2026-08-21 as `for (const pkg of readdirSync(PACKAGES_DIR, …))`) never
  leaves `packages/`, so `:70` skips regeneration, the build runs, the cache is untouched
  and the check fires again identically — `CF14-1`'s class at the comparand. Closed by a
  SCOPE TWIN on row **42**, the one fixture in §6 already specified as cwd-resolved,
  `compared`-cli, marker-newest with a clean cache, carrying one unit outside `packages/`
  and asserting NO `stale-declarations` anywhere with `declaration-cache` coverage `full`;
  its mutant needs no injection point. ⚠️ **That closure is member 1's DIRECTORY half and
  round 30 (`C30-1`) records that it is not the whole member:** the member is
  `every *.ts under every packages/*/src`, and the twin as this round wrote it discriminates
  `packages/*` from the 19 units and NOTHING about the EXTENSION filter — while the three
  sentences this round repointed onto it (§3.3's, §2's `checkDeclarationCache` comment's and
  §4's remedy row's) each quantify over both halves. The twin now carries the second half
  too, and this entry is narrowed rather than rewritten because the round-20 measurement
  itself stands. Row 42 now carries TWO twins, so §6's group bullet
  gains the rule that a twin's membership is read off what IT asserts and never off its
  ROW's number.
- **`OP20-1`, medium — §4's grouping rule governs FOUR kinds and the fourth was rendered by
  NOTHING.** Round 18's `OP18-2` named §6 row 38 as the fix for a THREE-kind gap and closed
  one of the three; round 19 closed a second incidentally (row 43's CLI twin, `CF19-1`,
  whose own subject was the exit code). Re-derived read-only at `9c247eb`:
  `grep -on 'stale-plugin-assets ×'` returns **three** hits and every one is PROSE — §4's
  grouping rule and rows 28 and 38's own re-derivations of `OP18-2` — with no assertion
  among them; the kind's only §6 rows are **8** and **24**, and both assert the RETURN
  value. So a `formatFindings` whose grouped-kind set omits one member and falls through to
  §4's generic comparison shape for the fourth passed rows 8 and 24, row 28 and its N=1
  twin, row 38, row 43's CLI twin, row 29, row 36, the remedy-plan rows, the false-negative
  battery, the wiring assertions and the live smoke test. It is NOT row 28's N=1 twin's mutant,
  which collapses at N≥2 for EVERY kind and which those three rendered rows already kill:
  one rule, four kinds, and standing rule 9 at `OP18-2`'s own fix. Wrong output on row 8's
  own end state — `packages/plugin/skills/**` edited without a rebuild, an ordinary state in
  this repository:
  `check-stale-dist: stale-plugin-assets packages/cli <newerInput> <olderOutput> <deltaMs>`
  instead of `check-stale-dist: stale-plugin-assets ×1 — packages/cli`, which renders on a
  human path the three fields §4 states in terms are `--json`-only (round 15, `OP15-3`), at
  `check:all` member 0, exit **1** under `--strict`, and at line 5 of §5.1's measured 204
  under `pretest`; §4 calls two shapes for one slot a defect in terms. Closed by a RENDERED
  TWIN on row **8** — the host rather than row 24, whose fixture is the FILE-member case
  (`CF17-3`'s shape), and already in §6's complete-cli-build list so `checkPluginAssets`
  runs — asserting the line by EQUALITY, with the omitting-SET formatter as its mutant,
  which needs no injection point because it fails on that row's own fixture. ⚠️ **The
  closure is stated as the SET of grouped literals asserted by equality and never as a count
  or as one row (`C11-4`), because naming ONE row for a three-kind gap is exactly what let
  this stand for two rounds.** ⚠️ **All four literals were given §6's mandatory
  `check-stale-dist: ` prefix in consolidation:** the drafts quoted row 28's two without it,
  while that row's cell pins `check-stale-dist: stale-unit ×2 — <unit>, <unit>` and §6's own
  group rule reads "every assertion in this group includes §4's `check-stale-dist: `
  prefix" — a SET statement false of the rows it enumerates is `C11-4`'s defect inside
  `C11-4`'s remedy.

Sites changed: §0's requirement 1 and requirement 3 rows; §1's `scripts/bundle-cli.mjs`
existing-file cell (anchors re-keyed, plus the literal-inside-the-`rm(`-call mandate) and
§1's closing source-reads paragraph (the `indexOf`/`search()` split and the four-not-five
count); §2's `isCompilerOutput`, `checkDeclarationCache` and `formatFindings` declarations;
§3's output-set sentence; §3.1's stem comparison; §3.3's comparand member 1; §4's grouping
rule and its `stale-declarations` remedy row; §6's wiring preamble sentence citing
`run-e2e-suites.test.mjs:31`, its `CLEAR`/`WRITE` comment and consts, its COPY/BUILD
uniqueness comment and its `placedCorrectly`/`lineWith` block; §6's preamble battery, group,
mutant-formatter and prefix enumerations; §6 rows 8, 28, 38, 42, 44 and 48; §7's
`.d.ts.map` row; §8(g) and §8(k).

ℹ️ **What round 20 re-derived and could not break:** §0's other nine requirement rows; §1's
new-file table, its IMPORT RULE and its battery count of **48**, which three twins leave
correct; §2's remaining signatures, its kind union and its delta-less naming; §3's four
input rules, its skip-rule table, its 21-of-30 arithmetic and its four minus-bullets, whose
own-row sentence enumerates the BLACKLIST and stays exactly true; §3.2 in full — its
consumer set, metafile derivation, `bundleAt` minimum, DISJOINTNESS RULE and REASON
PRECEDENCE; §3.3's three-clause conjunction, both reason tables, its four-proxy chain and
its SKIP RULE; §3.4 and §3.5 in full; §4's exit-code table, prefix rule, per-kind line
shapes, its other seven remedy rows and ordered recipe steps 1-5 with all four carve-outs;
§5 and §5.1; §6's rows 1-7, 9-27, 29-37, 39-41, 43 and 45-47, its complete-cli-build rule
and its three deliberate exceptions, its `C15-2` sibling-fixture rule — row 44's twin is not
cli-scoped, row 42's added unit is outside `packages/` so `inlinedUnitsFromMetafile`'s
`^packages/([^/]+)/dist/` filter can never name it, and row 8's twin asserts no bundle
coverage — its APPEND RULE, its sha-pinned registry bullet, its false-negative battery, its
wiring assertions and its live smoke test; §7's twelve other rows; and §8(a)-(f) and
(h)-(p), none of which gains a residual, `C20-1` correcting (g) and `CF20-1` narrowing (k)
rather than conceding a limit. The `see above`/`see below` sweep below `## 0.` returns only
backticked quotations of `OP13-2`'s fixed cell used as history, and a sweep for
`TODO`/`TBD`/`FIXME`/`<insert`/placeholder text returns no live site, so round 10's "guard
WHAT an apply inserts" holds everywhere; the `String.replace` census reproduces at **four**
hits, three dollar-apostrophes and one dollar-backtick, and no replacement text this round
spans one. **No working-tree mutation:** `scripts/bundle-cli.mjs` was never imported, every
probe read files as text or `stat`ed them, prettier ran only over scratchpad copies, and
`git status --porcelain` is empty at `9c247eb`. Prettier and the preamble's five structural
clauses are re-run on the candidate before the commit, with their results reported against
the sha they were run at.

**Round 21 (2026-08-21) — `contract-fit` / `correctness` / `operability`, run as the SAME
COVERAGE MATRIX as rounds 18, 19 and 20 and enumerated INDEPENDENTLY of round 20's list
rather than as a check that its fixes landed: obligations re-derived from the document as
round 20 left it, in each lens's own order, with UNCOVERED recorded wherever the only
candidate element would also pass a plausible wrong implementation AND — round 20's own new
disqualifier — wherever the only candidate would FAIL a correct one. The consolidator
re-derived the matrix a fourth time and independently of all three lenses: **137**
obligations, **6** UNCOVERED, and the six are the same six the lenses reached in three
different orders. SIX finding IDs survived consolidation, all re-derived read-only at
`3095116` on a clean tree and all dispositioned `fixed`: `C21-1`, `C21-2`, `CF21-1`,
`CF21-2`, `OP21-1`, `OP21-2`. Entered here in the SAME round that filed them, which is the
preamble's own standing requirement. ⚠️ **ONE battery row IS appended — row 49 — so §1's
live count moves from 48 to 49 and §6's registry bullet records the append; nothing is
renumbered, and every other fix is a second mutant, a twin or a further clause on a row that
already exists. Two lenses wrote "no row is appended, so §1's 48 stays correct" and
consolidation refuted it, which is standing rule 1 applied to a round's own bookkeeping.**

- **`C21-2`, medium — §1's blessed edits produce a bundler whose FIRST build throws, because
  the `mkdir` of `.bundle-meta/` is mandated nowhere and asserted by nothing.** §1's cell
  named the `mkdir` once, as a CONSEQUENCE of the `metaDir` factoring an implementer "needs
  anyway", while its own authoritative scope list read **FIVE edits** and §0's requirement 3
  row enumerated the same five. Re-derived read-only at `3095116`:
  `grep -c bundle-meta scripts/bundle-cli.mjs` returns **0**, `packages/cli/.bundle-meta`
  does not exist, and the directory is gitignored by this change set's own `.gitignore`
  edit, so it is absent on the FIRST build of every tree and the wipe-step clear removes it
  again on every build after that — factored or not. Land exactly the five and `writeFile`
  throws `ENOENT`. `build` is `tsc -b && npm run bundle:types && npm run bundle:cli`
  (`package.json:15`), so `npm run build` then fails at its last step on every tree forever —
  and `npm run build` is the remedy §4's table prints for four kinds and the command its
  recipe steps 2 and 5 name, so every printed remedy in this design would terminate in a
  failing command: round 1's `O-2` class at the build program rather than at the test hook.
  NOTHING in §6 can see it — the four bundler properties are TEXT reads (§8(k) already
  records that they do not establish that the edits RUN), every battery fixture uses row
  31's stub `bundle-cli.mjs`, and the live smoke test never invokes the bundler — so the
  mandate is written where the edit is prescribed, unconditionally and as an edit in its own
  right, in the shape of `:176`'s own `await mkdir(pluginOut, { recursive: true })` copied
  WHOLE (standing rule 7). It carries no source property, stated apart rather than papered
  over, which is `CF20-1`'s own discipline for the `.gitignore` read. ⚠️ **Standing rule 9
  reached THREE sites no lens named:** §0's requirement 3 row carries the count TWICE and
  the fan-out fixed one, §2's `formatInternalError` comment cited "§1's fourth edit" and
  §6's `EXPORTED` const cited "§1's 4th edit" — both stranded by the renumbering, and both
  repointed onto the edit's NAME rather than onto a number, per `C11-4`.
- **`C21-1`, low — §3's Units step mandates a per-unit `rootDir`/`outDir` reading and nothing
  exercised it for twenty rounds, while §3's own rules spelled the literals it says the
  design does not assume.** Re-derived read-only at `3095116` by parsing all 19 root
  references: `rootDir === "./src" && outDir === "./dist"` for **19 of 19**, exception set
  EMPTY — and §6 holds no non-`src`/`dist` fixture, the battery's only `outDir` being row
  47's `"./dist"`. So a `buildUnits` hardcoding `join(cwd, dir, "src")` and
  `join(cwd, dir, "dist")` passed every row and every twin, the false-negative battery, the
  four bundler properties, both import reads and the live smoke test, which asserts the unit
  SET and the coverage SHAPE. Add one unit emitting to `build/` and that body stats an
  absent `dist`, the unit is `skipped` with reason `no dist/`, both per-unit comparisons are
  suppressed for it forever, and the run prints PASS at exit 0 even under `--strict` — the
  founding failure at the two primitives every comparison is built on. Verbatim input rule
  4's state until `CF17-1`, closed the same way: a SYNTHETIC row, so §3's 21-of-30
  arithmetic, §8(f) and §8(l) are untouched. Row **49** is the element and its BASELINE is
  the load-bearing half, because under the mutant the baseline is already `skipped`
  (`CF8-2`, read the other way). ⚠️ **Standing rule 8, checked at the fix, at two sites:**
  §2's `checkDeclarationCache` comment keeps `packages/*/src` and its "never
  `units.map(u => u.srcDir)`" (`C20-2`), and §3.2's `^packages/([^/]+)/dist/` filter STAYS
  literal with its bound stated, because re-spelling it against `distDir` would key it on a
  config the metafile never saw and re-open `C16-2`.
- **`OP21-1`, medium — the design forbids a per-kind remedy line in terms, in two sections,
  and no element read either one.** §6 row 34's single mutation is a CONJUNCTION — per-kind
  text AND no plan — and its verdict is the plan's presence, steps, order and position, all
  of which a `formatFindings` rendering the per-kind remedy AND the correct plan satisfies;
  rows 21, 38, 39 and 42 assert their plans and never the absence of other remedy text.
  Re-derived read-only at `3095116` over §6: `remedy` occurs **20** times, every one about
  the PLAN, and `Finding.remedy` occurs **zero** times. The reading is the one §2 requires:
  `remedy` is a REQUIRED member with only "the three comparison fields" marked `--json`-only
  (`OP15-3`), while §4's column was headed "remedy printed". ⚠️ **The same rule is
  unasserted at a SECOND surface, found by the matrix rather than by the finding:** §4's
  coverage line "carries no remedy of its own" while rows 16, 17, 18 and 26 pin a contiguous
  SUBSTRING and row 38 pins only the label's ABSENCE on an all-`full` fixture. Fixed by a
  second mutant at row 34 with a negative clause scoped to the lines BEFORE the plan's first
  line, by the same clause on row 18's coverage line, and by stating the field's one surface
  at §2 and the table's role at §4. Both clauses are provably safe on their own fixtures,
  which is `OP16-3`'s disqualifier applied before the clause was written.
- **`OP21-2`, medium — the `PASS` line's ORDINARY rendering is asserted by nothing, so two
  wrong implementations pass every element in this design.** §6's only `PASS —` assertion is
  row 39's NEGATIVE twin, the `nothing was checked` carve-out with NO parenthetical; row 35
  is the findings path; the false-negative bullet asserts only that a skipped unit is
  **named**; row 18's fixture holds ZERO skipped units. (i) A `PASS` line reading
  `1 skipped (packages/cli)` with `unitState()`'s reason dropped passes all of those, and on
  §4's own recipe step-1 tree the operator cannot tell which of the THREE `skipped` states
  they are in. (ii) One that emits the standalone summary BESIDE the `PASS` line prints the
  summary twice on every green run and re-emits `OP15-5`'s suppressed 451-character clause
  list on every fresh clone and in both `meta-checks` legs. ⚠️ **And §3.3's own cell for the
  literal claimed a printing site the same round's carve-out had suppressed.** Fixed by
  extending row 39's MAIN half — no new row, no new fixture — with the `PASS` line by
  EQUALITY in §4's full shape, counts derived from that fixture and never from §4's live
  `18` (`OP16-3`), plus an exactly-once assertion and two mutants, and by repointing §4's
  prefix rule, its coverage bullet, its both-slots clause and its worked literal, §2's
  `states` and `formatFindings` comments, §3.3's unit-state row, §5's `meta-checks` bullet,
  §6's group and false-negative bullets and §1's `report.mjs` row in the same pass.
- **`CF21-1`, medium — round 20's own apply spliced a new item BETWEEN the two conjuncts of
  another row's predicate, and the conjunct then FAILED a correct implementation.** §6's
  prefix enumeration carried row 38 as asserting both the grouped `stale-bundle ×1` line and
  the ABSENCE of any `check-stale-dist: ⚠️ coverage: ` line (`OP16-4`), with the appositive
  "its fixture being the one in this group whose four coverage entries are all `full`".
  Round 20 inserted row 8's item between the two. Verified by diff, read-only: at `9c247eb`,
  the document round 20 READ, the contiguous form matches **1**; at `3095116` it matches
  **0**. The ABSENCE conjunct and its appositive therefore re-pointed onto row 8's twin,
  which is bound by NEITHER the `C15-2` sibling-fixture rule — round 20's own entry exempts
  it in terms, "row 8's twin asserts no bundle coverage" — NOR the complete-cli-build cache
  clause, so a correct `formatFindings` renders a coverage line on its fixture and the
  re-pointed assertion fails: `OP16-3`'s and `CF20-1`'s class in the round after `CF20-1`
  filed it. Reciprocally, row 38 is the SOLE element pinning the suppression and the
  enumeration stopped naming it — a SET statement false of the rows it enumerates, which is
  `C11-4`'s defect inside `C11-4`'s remedy one sentence away. The operability lens PARKED
  the same ambiguity independently rather than filing it. Fixed by restoring the conjunct to
  row 38, naming ROW 38 in the appositive, moving row 8's item after it, and writing the
  rule into §6's group bullet: **A PREDICATE IS NEVER SPLIT BY ANOTHER ROW**. Rows 8 and 38
  state the split correctly in their own cells and are UNCHANGED (standing rule 8).
- **`CF21-2`, low — §6's live smoke test sentence has been split by three paragraphs since
  round 9, and the three sites resting on it resolved on the page to a list of ONE unit.**
  "the algorithm above returns four `stale-unit` findings (`packages/contracts`," and its
  continuation had three whole paragraphs and four blank lines between them. GFM ends a
  paragraph at a blank line, so the rendered page showed a paragraph ending in a comma and,
  33 lines later, an orphan paragraph opening with a bare backticked name and a close-paren.
  §0's requirement 1 row, §3's input rule 1 and §3's output-set bullet all cite that
  paragraph as the authority for the number that discriminates §3's four-extension whitelist
  from `C20-1`'s mutant — FOUR against SEVEN. NONE of the document's guards saw it: the
  anchor grep and passes 1-3 read citations, the row census and the orphan-row sweep read
  tables, the formatter check reads prettier's diff, and the placeholder sweep reads tokens.
  Fixed by closing the sentence where it opens and deleting the orphan fragment; the three
  citing sites resolve correctly then and are UNCHANGED. The preamble gains the detector as
  a SIXTH structural clause and a GATE ON THE APPLY beside the orphan-row sweep — **NO
  SENTENCE SPLIT BY A PARAGRAPH** — measured read-only in all three directions on scratchpad
  copies: **one** hit at `3095116` (body-relative 3125, file line 3367), nothing on the
  fixed candidate, and the spliced site printed on a control. The sha travels with the
  result, per `CF16-2`.

Sites changed: §0's requirement 3 row at both of its counts; §1's `bundle-cli.mjs` cell (the
import clause, the `mkdir` mandate, the factoring clause, both ordinals, the scope-list tail
and the edits-versus-properties sentence), its `report.mjs` row and its battery count; §2's
`buildUnits`, its `Finding` note, its `states` comment, three clauses of its
`formatFindings` comment and its `formatInternalError` comment; §3's Units step, input rules
1 and 2 and its per-unit output set; §3.1's stem comparison; §3.2's metafile-filter bound
and its build-program paragraph; §3.3's unit-state ABSENT-`dist/` row; §4's grouping
lead-in, its remedy-table head, its remedy-plan owner paragraph, its coverage-line bullet at
two clauses, its prefix rule's summary clause and two clauses of **SKIPPED UNITS ARE NAMED
ON BOTH PATHS**; §5's `meta-checks` bullet; §6's group and false-negative bullets, its
registry bullet, its wiring comments, its `EXPORTED` const, rows 18, 34 and 39, and new row
**49**; §8(k); and the preamble.

ℹ️ **What round 21 re-derived and could not break:** §0's other ten requirement rows as
TEXT; §1's new-file table, its IMPORT RULE and its two-repo-internal-imports paragraph, no
import being added or removed — `mkdir` is already in `bundle-cli.mjs`'s own
`node:fs/promises` import at `:55`, re-derived; §2's remaining signatures, its kind union
and its `checkDeclarationCache` comment, kept verbatim against `C20-2`; §3's input rules 3
and 4, its skip-rule table, its 21-of-30 arithmetic and its four minus-bullets; §3.1's
scratch exclusion and `packages/cli` exemption; §3.2's consumer set, `bundleAt` minimum,
DISJOINTNESS RULE, REASON PRECEDENCE and rank-5 residual; §3.3's three-clause conjunction,
both reason tables, its four-proxy chain, its cli-scoped literals — which come from
`bundle-cli.mjs:63`'s own `OUT_DIR` and must NOT be re-spelled — and its SKIP RULE; §3.4 and
§3.5 in full; §4's exit-code table, per-kind line shapes, its other seven remedy rows and
ordered recipe steps 1-5 with all four carve-outs, every remedy re-checked against the
finding it is printed for in the `CF14-1` shape and every one able to clear it ONCE the
`mkdir` lands; §5 and §5.1; §6's rows 1-17, 19-33, 35-38 and 40-48, its complete-cli-build
rule and its three exceptions, its `C15-2` sibling-fixture rule — row 49 is not cli-scoped
and asserts neither `stale-bundle` nor a `full` bundle-freshness entry, so that rule does
not quantify over it — its APPEND RULE, its `CLEAR`/`WRITE` regexes and `placedCorrectly`
block, which the `mkdir` cannot confuse because it uses a different CALLEE and `[^;]+`
cannot cross a statement boundary under `semi: true`, its false-negative battery and its
live smoke test; §7's thirteen rows; and §8(a)-(j) and (l)-(p), none of which gains a
residual, `C21-2` and `C21-1` adding a mandated edit and an assertion and the other four
being damage repaired rather than limits conceded. The `see above`/`see below` and
`TODO`/`TBD`/`FIXME`/`<insert`/placeholder sweeps below `## 0.` return only backticked
quotations of `OP13-2`'s fixed cell used as history, so round 10's "guard WHAT an apply
inserts" holds at every live site. **No working-tree mutation:** `scripts/bundle-cli.mjs`
was never imported, every probe read files as text or ran `git`, `grep`, `awk` and
`JSON.parse` read-only, every write went to the scratchpad, and `git status --porcelain` is
empty at `3095116` before and after. Prettier and the preamble's structural clauses — the
new sentence-split detector among them — are re-run on the candidate before the commit,
with their results reported against the sha they were run at.

Battery row **49** was appended BELOW ROW 48 per §6's APPEND RULE, nothing renumbered.

**Round 22 (2026-08-21) — `contract-fit` / `correctness` / `operability`, run as the SAME
COVERAGE MATRIX as rounds 18-21 and enumerated INDEPENDENTLY of round 21's list. FIVE
finding IDs survived consolidation, re-derived read-only at `5fef126` on a clean tree and
all dispositioned `fixed`: `CF22-1`, `C22-1`, `OP22-1`, `OP22-2`, `OP22-3`. NO battery row
is appended and nothing is renumbered, so §1's **49** stays correct — every fix is a
conjunct, an invocation or a clause added to an element that already exists.**

- **`CF22-1`, medium — round 21's `mkdir` was given no source property, on a reason that is
  true of an EXISTENCE assertion and false of an ORDER one.** Round 21 (`C21-2`) added the
  `mkdir` of `packages/cli/.bundle-meta/` as §1's sixth bundler edit and declined a mutant
  because one "could only restate the absence it asserts against" — `CF20-1`'s reasoning for
  the `.gitignore` read. An ORDER mutant is a HOIST, not a deletion: put the `mkdir` above
  the wipe-step clear and the clear deletes the directory it just made, `writeFile` throws
  the same `ENOENT` `C21-2` measured, and the tree carries all six edits — a state named in
  no section of this design. §6 now carries a `MKDIR` anchor beside `CLEAR` and `WRITE` and
  two order conjuncts, each with a mutant that flips it ALONE. ⚠️ **Two corrections the
  finding did not carry, both load-bearing.** First, `search(MKDIR) < search(WRITE)` alone
  does not catch the failure — a `mkdir` hoisted to the top of `main()` is still before the
  write — so the load-bearing conjunct is `search(CLEAR) < search(MKDIR)`. Second, the
  anchor needs the literal INSIDE the `mkdir(` call or it FAILS the `const metaDir`
  factoring §1 blesses (`OP16-3`'s class), so §1's ONE mandate becomes TWO. Round 21's
  own count of what §6 asserts moved from four properties to five at seven sites, and its
  "only the clear/write pair is ORDER-sensitive" was a claim its own same-round insertion
  falsified.
- **`C22-1`, medium — the `unreadable` COVERAGE spelling was mandated in §3.2 and in §4's
  verbatim reason list and asserted by nothing.** Every OTHER spelling in that list carries
  an element named beside it — rows 10, 12, 16, 17, 23 and its twin, 26 and its round-19
  twin, 40 and its round-18 twin, 46 and its twins, and row 39's negative twin — and this
  one carried none. Re-derived read-only at `5fef126` over §6's own line range: `unreadable`
  occurs TWICE, at row 29's rendered FINDING line and at the false-negative battery's
  truncated-marker bullet, whose four conjuncts are one advisory, the FINDING's reason,
  `exitCodeFor` under `strict: true` and no throw — never `result.coverage`. No row reaches
  the state on the coverage side: rows 10 and 21 and row 39's negative twin hold the marker
  ABSENT, row 16 holds it present and parseable so its entry takes rank 2, and row 37's
  mutation makes the unit `unbuilt` so rank 1 spells all four `packages/cli unbuilt`. So a
  `checkBundleFreshness` keying the COVERAGE reason on its ARGUMENT while distinguishing the
  two states for the FINDING — which row 29's fifth fixture forces — passed every element in
  this design and printed both spellings for one state on one page, telling the operator a
  file was absent while it sat on disk: `OP18-5`'s own diagnosis at the sibling surface,
  four rounds after `OP18-5` closed it at the finding line. ⚠️ **§2's own signature endorses
  that implementation**, since `checkBundleFreshness(units, metafile)` receives only
  `readMetafile`'s return and §2 says in terms that the return "collapses two states §3.3
  partitions on" — so the rule is written where the branch order is prescribed: the spelling
  is read off the marker PATH, ONE existence test feeding the finding and the coverage entry
  alike, in the shape §2's `unitState` already carries for clause 2 (`C14-1`).
- **`OP22-1`, medium — `remedy`, `newerInput`, `olderOutput` and `deltaMs` are declared
  `--json`-only and were read on NEITHER path.** `--json` is exercised in §6 at row 27,
  whose three invocations run a THROWING fixture whose object carries no `findings` key at
  all, and at row 39's fourth invocation, which deep-equals `coverage` on a ZERO-finding
  fixture. So a serialiser projecting each finding to `{ kind, unit }` — or emitting
  `{ states, coverage }` and dropping `findings` — passed every row, the false-negative
  battery, the wiring assertions and the live smoke test, `OP18-7`'s own defect at the
  sibling key of the same object. Row 43's CLI twin is the only §6 element that runs the CLI
  as a process on a findings-carrying tree, so it takes a THIRD invocation, deep-equal
  against the run's OWN return value and never against literals, pinning no mtime.
- **`OP22-2`, low — §6 row 34's mutant (a) assertion FAILED a correct implementation.** It
  positions the remedy plan "before the coverage line" on a fixture whose four entries a
  legal build leaves all `full`, and `OP16-4` made that line CONDITIONAL in the very round
  that recorded row 34's fixture as fully covered and `bundle-freshness` there as "not
  pinned". An implementer writing that clause as an index comparison gets `-1` and the row
  fails a conforming reporter — `OP16-3`'s class. Narrowed to the AFTER-findings half, with
  the plan-before-coverage ORDER re-attributed at §4 to row 39, whose fixture carries a
  non-`full` entry by construction, so a later round cannot re-point it back.
- **`OP22-3`, low — one live figure stated two ways.** §6 row 8's twin cell read "§1's
  battery count of 48 stays correct" while §1 has read **49** since round 21 appended a row,
  so the citation resolved to a refutation. Reworded to the property the twin actually has —
  it adds no row — and the rule is committed beside §6's APPEND RULE so the next twin cell
  cannot repeat it, per `OP12-6`.

⚠️ **FOUR pieces of APPLY DAMAGE were found by the site walk rather than by any lens, three
of them older than round 21.** (1) §2's `readMetafile` comment opened "absent OR
UNPARSEABLE ⇒ `undefined`, and `undefined` ⇒ a `bundle-provenance-missing`" and its second
half — "finding with the reason its case mandates (§3.2)" — sat FIVE LINES BELOW it,
orphaned under round 14's inserted block and reading as a fragment after "whether the
bundle COMPARISON can run". Present at `badf45a`, `7dfa1ba`, `3095116` and `5fef126`, and
the orphaned half is exactly the clause `C22-1` needs an implementer to read. Rejoined. (2)
§6's complete-cli-build rule enumerated "29's two cli fixtures" while row 29 has held THREE
since round 18 (`OP18-5`) — the third being the truncated-marker tree, so the site an
implementer reads to BUILD it licensed a baseline short of a member, which would vacate the
FINDING-line assertion `C22-1`'s standing-rule-8 check relies on (`CF13-4`'s shape). (3)
§6's non-vacuity preamble named a mutant "the third" and counted "all three mutants", both
re-pointed by this round's own insertions — repaired by naming the mutant by its
DESTINATION, since round 22 also WIDENS it from the write alone to the PAIR. (4) §0's
requirement 3 row and §3.3's clause-2 paragraph each carried a bundler placement count
("the clear/write pair in the right order", "Both placements are pinned by §6") that this
round's own insertion falsifies one clause later; both are repaired in the same pass, which
is standing rule 9 checked AT the fix.

⚠️ **The general form of (1) is A SENTENCE IS NEVER SPLIT BY AN INSERTED BLOCK, stated as
document text per round 21, and NO detector is added for it.** Round 21's clause keys on a
line ending in a COMMA whose successor is BLANK, while this half ends in a backticked kind
name with a non-blank successor. The obvious replacement — a line opening lowercase whose
predecessor ends in a full stop — was measured before it was proposed and rejected on the
measurement. Run below the `## 0.` heading as `awk` over the file, stripping a leading
comment marker and indent from each line and printing every line index whose predecessor
ends in a full stop and whose own first character is lowercase, excluding table rows, list
items, headings and blockquotes, it returns at `5fef126` **33** lines of which exactly ONE
is the defect — the other 32 are signature declarations and lowercase-opening comment
annotations this document writes by convention. A guard that reports 32 correct sites as
damage can never return nothing, which is `CF16-1`'s defect and the preamble's own test for
whether a check is a check. Standing rule 5 at the guard rather than at the rule: an
unmeasured proxy is worse than a stated gap, so the gap is stated.

ℹ️ **One count is deliberately NOT swept, and the ruling is recorded so a later round finds
it stated rather than re-derived.** §3's `C21-1` narrative reads "passed all 48 rows and
every twin, the false-negative battery, the four `bundle-cli.mjs` source properties". Round
21 wrote that whole sentence in the commit that appended row **49**, one sentence below
"§6 row 49 is the non-vacuity row" — so its figures are pinned to the state BEFORE row 49
and before this round's fifth property, and both are correct as history. Moving one of the
two would leave a sentence true of no state of this document, which is the split-count
damage this round exists to remove. The three OTHER sites carrying that property count —
§3.2's `C16-2` narrative, §6's `walk.mjs` import comment and §6 row 45 — are SET-shaped
("every battery row"), carry no epoch-pinned sibling figure and DO move to five; row 45's
reads `four bundler source properties`, a spelling the contract-fit lens's own grep could
not see, which is standing rule 1 at the fan-out.

Sites changed: §0's requirement 3 row (both order claims); §1's `bundle-cli.mjs` cell (the
`mkdir` source-property clause, the anchor mandate, the edits-versus-properties tail), its
source-reading paragraph and its `compare.mjs` row; §2's `readMetafile`, `checkBundleFreshness`
and `Finding` comments; §3's Comparison coverage paragraph; §3.2's structural-silence bullet,
its `C16-2` narrative and its build-program paragraph; §3.3's clause-2 placement paragraph;
§4's remedy table, its verbatim reason list, its `--json`-only paragraph and its ordered
recipe's plan-position sentence; §6's preamble (two enumerations of row 43's twin), its
complete-cli-build rule, its row-number registry bullet, rows 8, 29, 34, 43 and 45, its
`walk.mjs` import comment, its wiring-assertion comments, anchors, census, `placedCorrectly`
and mutant block, and its false-negative battery's truncated-marker bullet; §8(k); and this
entry.

ℹ️ **What round 22 re-derived and could not break:** §3.1, §3.3's three-clause conjunction
and both reason tables — whose rows spell a UNIT verdict and a FINDING reason and never a
coverage reason, so the new marker-path rule reaches none of them — §3.4, §3.5, §5, §5.1 and
§7 in full; §3.2's `bundleAt` minimum Rule, DISJOINTNESS RULE, `C15-2` absence rule and
REASON PRECEDENCE, whose rank 4 already carries both marker spellings and is the only live
rank on the truncated-marker tree, so the new coverage conjunct cannot fail a correct
implementation; §4's exit-code table, per-kind line shapes, grouping rule, prefix ORDER rule,
the other seven remedy rows, the ordered recipe steps 1-5 with all four carve-outs, and
**A DEGRADED RUN CARRIES A NEXT ACTION**, whose bound over REASONS already excludes both
marker spellings from the zero-finding producer rule because each carries a finding; §6's
`C15-2` sibling-fixture rule, its APPEND RULE and its DERIVED outside-citer command, none
touched by a round that appends no row; and §8(a)-(j) and (l)-(p), none of which gains a
residual — `CF22-1` NARROWS (k) by converting a residual into an assertion, and the other
four findings add elements or repair damage rather than concede limits. Re-derived at
`5fef126`: the orphan-row sweep returns **0**, the escape-aware pipe census **0**, the
comma-then-blank sentence-split sweep **0**, the anchor grep **0**, passes 1, 2 and 3 each
nothing, and the `see above`/`see below` sweep only the backticked quotations of `OP13-2`'s
fixed cell used as history — round 21's result, re-run rather than carried. Bundler facts
re-derived read-only at `5fef126`: `grep -n mkdir scripts/bundle-cli.mjs` returns `:55` and
`:176` only, `grep -c bundle-meta scripts/bundle-cli.mjs` returns **0**, all three anchors
match ZERO times, and the conforming `mkdir` line is **67** characters. **No working-tree
mutation:** `scripts/bundle-cli.mjs` was never imported, every probe read files as text or
ran `git`, `grep`, `sed`, `awk` and `python3` read-only, every write went to the scratchpad,
and `git status --porcelain` is empty at `5fef126` before and after.

**Round 23 (2026-08-21) — `contract-fit` / `correctness` / `operability`, run as the SAME
COVERAGE MATRIX as rounds 18-22 and enumerated INDEPENDENTLY of round 22's list. The
`contract-fit` lens returned `approve` with no edit. THREE finding IDs survived
consolidation, all re-derived read-only at `53996f5` on a clean tree and all dispositioned
`fixed`: `C23-1`, `C23-2`, `OP23-1`. NO battery row is appended and nothing is renumbered,
so §1's **49** stays correct — every fix is a TWIN in row 33's shape, added to an element
that already exists. All three have rounds 18-22's shape at a site those rounds did not
reach: a rule §0, §2, §3 or §4 states correctly whose only candidate element in §6 is
satisfied by the very implementation the rule forbids.**

- **`C23-1`, high — `Unit.kind` decides whether requirement 2 runs for a unit AT ALL, and
  its derivation was stated in NO section for twenty-two rounds.** `C21-1` closed
  `srcDir`/`distDir` at §2's `buildUnits` and at §3's Units step one round earlier and
  stopped at two fields of the same struct's three. Re-derived read-only at `53996f5`:
  `kind: "tsc"` occurs exactly TWICE in this design — the declaration and §3.1's scope
  predicate — `bundled` once as this field's value, and `Unit.references` and
  `Unit.configs` occur NOWHERE outside the struct, so the reference closure is the one
  other thing `references` could be for. A `kind` read off `packages/cli`'s closure marks
  **17 of 19** `"bundled"`, runs `checkOrphans` on `packages/perf` and `e2e/report` ALONE,
  and leaves requirement 2 INERT for the other 16 — `packages/contracts`, `packages/gates`,
  `packages/journal`, `packages/plugin` and twelve more — at PASS, exit **0 even under
  `--strict`**, on a tree carrying an orphan, at `check:all` member 0 and under flagless
  `pretest`. Re-derived by parsing the live tsconfigs: root references 19, `packages/cli`
  direct references 16, transitive closure 16, complement exactly those two units. NO
  element in §6 discriminates it, and the reason is exact rather than enumerated: no
  fixture in this design builds a `packages/cli` beside a unit its own
  `packages/cli/tsconfig.json` references AND carries an orphan there — rows 4, 14, 44, 48
  and 49 reach the stem side on trees holding no `packages/cli` at all, rows 40, 45 and 46
  call comparisons directly, row 42's scope twin asserts NO `stale-declarations` and reads
  no orphan, and the live smoke test asserts the unit SET and the coverage SHAPE and
  explicitly not the finding set, on a tree re-derived at `53996f5` as holding **0**
  non-scratch orphans across all 18 tsc units. Closed by a TWIN on row **4** whose fixture
  is cli-COMPLETE and whose `packages/cli/tsconfig.json` references the orphan-carrying
  unit, with two mutants — the closure derivation and `kind: "tsc"` for all 19 — each
  failing on the twin's own fixture. It is the first element anywhere to assert the
  `packages/cli` exemption POSITIVELY; §3.1 has asserted it in prose since round 1 and no
  element read it. ⚠️ **Standing rule 9 reached a site the finding did not name:** §6's
  complete-cli-build rule quantifies over that twin's fixture, and without naming it there
  a fixture author is licensed to build a cli baseline short of a member, which makes the
  baseline `unbuilt`, suppresses `checkOrphans` for `packages/cli` under EVERY
  implementation and vacates the twin's ZERO half — `CF8-2` at the element added to close
  the finding. It is named there as a FIXTURE and never as a row, in 29's own precedent,
  because row 4's MAIN half builds no `packages/cli` and naming the ROW would produce a SET
  statement false of the half it enumerates (`CF21-1`/`OP16-3`'s class). ⚠️ **The
  derivation is written as a DESIGN-TIME fact about `bundle-cli.mjs:63`'s
  `CLI_ROOT`/`OUT_DIR` and explicitly NOT as an import:** a `units.mjs` importing them would
  be a THIRD repo-internal import and would falsify §1's exactly-two count and widen §8(n).
- **`C23-2`, medium — §3.2's FIRST `stale-bundle` input member was the one comparand member
  in that list with no pointer to the rule defining its set, and §3's output filter had no
  element at that consumer.** Its sibling one line down carries `(§3 rules 1-2)`, and §3's
  own "also the comparand for" sentence named §3.3's marker clause as the filtered set's
  only further consumer — `CF6-1`'s shape, one exclusion landing in one section, at the
  filter's THIRD reader, and `C16-1`'s site list stopping one walked side short. §3.2 does
  state the predicate for these members twelve lines below the bullet; what had no element
  is the bullet. Re-derived read-only at `53996f5` over §6: NO fixture places a
  `.tsbuildinfo` or an `eo-*-fixture-*/` directory under the `dist` of a unit its own
  marker names as inlined — rows 5, 40, 41 and 45 give their inlined units a bare
  `dist/index.js`; rows 14 and 44, the two carrying scratch, are not cli-scoped; and rows
  32, 36 and 46 hold their `.tsbuildinfo` under `packages/cli/dist`, which is not inlined,
  on trees where §3.2's rank-1 or `bundleAt === undefined` branch suppresses the comparison
  outright. So an unfiltered member-(i) walk reported `stale-bundle packages/cli` on every
  tree whose `npm test` ran after its last `bundle:cli`: `packages/journal` is one of the 16
  AND the one unit this suite writes scratch into, its newest scratch `.js` at mtimeMs
  **1787308024588.285** against the newest FILTERED output across the 16 at
  **1787156422460.689**, a **1.75-day** forward shift, at `check:all` member 0 and at line 5
  of §5.1's measured 204 under flagless `pretest`. Closed by a TWIN on row **5**, baseline
  only, carrying both excluded files under the same inlined unit's `dist` and asserting
  CLEAN with `bundle-freshness` `full`. ⚠️ **The finding's per-excluded-file mutant pair was
  AMENDED in consolidation:** dropping the `.tsbuildinfo` minus-bullet ALONE flips nothing,
  the four-extension whitelist already rejecting that file by extension, so a cell claiming
  one mutant per file would have asserted a mutation that cannot fail — `CF8-2` read the
  other way, caught before the row was written. The two mutants are a walk with NO accept
  predicate and a walk carrying the whitelist without the scratch minus-bullet. ⚠️ **And the
  cost is NOT live on this working copy, which the cell states rather than hides:** here
  `bundleAt`, the MIN over the five chunks, is **1787338130243.025**, ahead of both the
  scratch file and the newest inlined `.tsbuildinfo` (**1787158151980.000**), so both
  mutants report clean here — which is exactly why the live smoke test cannot see either.
  ⚠️ **The finding's "1.76-day" was re-derived at 1.7547 and corrected to 1.75** (standing
  rules 2 and 3).
- **`OP23-1`, high — `exitCodeFor`'s partition was closed by `strict` in both directions and
  by KIND in neither, and the open case is this repository's own tree.** `OP18-1` closed
  §4's exit-table DEFAULT column and `CF19-1` closed the CLI's ARGUMENT; neither reached the
  MIXED finding set. Row **21**'s mutation cell names "this row's advisory-only fixture AND
  the non-advisory fixture the verdict cell names", so whether that second fixture ALSO
  holds an advisory is a FREE VARIABLE, and §4's third exit-table row applied to a set
  holding BOTH kinds was asserted by nothing. `some((f) => ADVISORY_KINDS.has(f.kind))`
  written where §2's rule needs `every((f) => ADVISORY_KINDS.has(f.kind))` as the exit-0
  test under `--strict` — "an advisory is PRESENT" read for "the findings are ONLY
  advisory" — differs from a correct implementation on the MIXED set and on NOTHING else,
  which is why it passed row 21's four calls, the false-negative battery's truncated-marker
  bullet, its `C10-4` conjunction and its §8(p) partial-copy bullet (each carrying exactly
  ONE advisory and asserting exit 0), row **43**'s CLI twin (whose end state carries exactly
  one NON-advisory `stale-declarations`), rows **27** and **31** (a throw and a zero-finding
  baseline), every mutant-formatter row — none of which CALLS `exitCodeFor`; re-derived at
  `53996f5`, the name occurs in §6 at rows 21, 29 and 43 and in the false-negative battery,
  and row 29's occurrence is a CITATION — and the live smoke test, which asserts the unit
  SET and the coverage shape and explicitly no exit code. Re-derived read-only at
  `53996f5`: `ls -d packages/cli/.bundle-meta` returns no such file while `packages/cli/dist`
  holds `bin.js`, `index.js`, `index.d.ts`, `bin/`, `plugin/` and five chunk-pattern files,
  so this working copy carries the one advisory §3.4 and §7 measure beside the four
  `stale-unit` findings §6 predicts — the mixed state, on day one, on every developer's
  tree, and permanently afterwards on any tree whose marker is deleted, truncated or lost
  to an interrupted `bundle-cli.mjs`. Under that mutant `check:all` member 0 exits **0**
  under `--strict` on a stale `dist` and the other 14 members run, which §4 calls the worst
  available answer for the incident this check is named for. Row 21 now carries an
  EXIT-CODE MIX TWIN in row 33's shape; its mutant needs no injection point, FAILING on the
  twin's own fixture, and it cannot fail a correct implementation because §4's third
  exit-table row and §2's "some finding's kind is NOT advisory" both mandate **1** there in
  terms. ⚠️ **The finding's drafted predicate named `every((f) => !ADVISORY_KINDS.has(f.kind))`
  as the correct spelling and it was REJECTED in consolidation:** that reads "the findings
  are ONLY NON-advisory", which returns **1** on an advisory-only tree and would have
  re-opened §3.4's, §7's day-one row's and the false-negative battery's exit-0 verdicts
  (`C10-4`, `C15-1`'s §8(p)) at the site an implementer builds the function from — a wrong
  predicate written into the design as its own mandate, `C13-1`'s class. ⚠️ **The twin's
  fixture also gained two clauses the finding did not carry**, without which "exactly one
  non-advisory" is not established: 1:1 stems on the added unit, and
  `.dts-cache/index.d.ts` newer than every member of §3.3's comparand (row 3's own clause).
  The exact mirror of `OP18-1`, five rounds on, at the sibling half of one function.

Sites changed: §2's `buildUnits` comment and the END of its `exitCodeFor` comment; §3's
Units step (a new paragraph BELOW the `C21-1` block, so that block's "this sentence" keeps
its referent) and its "also the comparand" sentence; §3.1's scope predicate; §3.2's
`stale-bundle` input member (i); §0's requirement **1** and requirement **2** rows; the END
of §4's advisory-row paragraph (below the `CF19-1` sentence, so its "That closed the
FUNCTION" keeps its referent); §6 rows **4**, **5** and **21** (three twins), §6's
complete-cli-build rule and §6's `C20-2` twin-disambiguation rule, which now names 21
beside 42; and this entry.

ℹ️ **What round 23 re-derived and could not break:** §0's other nine requirement rows —
requirement 3's is deliberately unchanged, member (i)'s comparand being a requirement-1
obligation registered in requirement 1's row, and putting it in both would give one mandate
two spellings (`CF13-4`) and would risk that cell's SIX/THREE/FIVE figures, which round 22
had just repaired; §1's two file tables, its IMPORT RULE, its two-repo-internal-imports
paragraph, its SIX bundler edits with the three-edit order chain and its FIVE source
properties, none of which a twin touches and none of which gains an edit or an anchor,
`bundle-cli.mjs:63` being read as a design-time fact; §2's other signatures, the
eight-member `kind` union, `ADVISORY_KINDS` itself, `isCompilerOutput`'s "BOTH walked
sides" — member (i) is a second CONSUMER of the mtime set and not a third accept predicate,
so that count is checked and deliberately NOT moved — and `checkBundleFreshness`'s claim
that row 5's two-chunk fixture is the only element failing the MAXIMUM mutant, re-checked
against row 5's new twin and still true; §3's four input rules, its four minus-bullets and
four-extension whitelist, its skip-rule table, its 21-of-30 arithmetic, its `27 + 2 = 29`
figure and its `933`/`332` and `2.03-day` figures; §3.1 apart from its scope predicate, its
`0 orphaned outputs` measurement and its "Four entries, never six" sentence standing;
§3.2's consumer set of **16**, its metafile derivation, `bundleAt` MINIMUM Rule,
DISJOINTNESS RULE, `C15-2` absence rule, FIVE REASON RANKS and rank-5 residual — the input
list stays at THREE members, the count §3.3's cli reason table states in terms; §3.3 in
full, including that table and the SKIP RULE; §3.4 and §3.5 in full, their comparands being
`packages/cli`-only minus-bullets member (i) can never reach; §4's exit-code table (FOUR
condition rows), its eight per-kind line shapes, its grouping and prefix ORDER rules, its
eight remedy rows, its ordered recipe with all four carve-outs, SKIPPED UNITS ARE NAMED ON
BOTH PATHS and A DEGRADED RUN CARRIES A NEXT ACTION — no kind, reason spelling, coverage
status or exit code is added and every rendered surface is byte-identical; §5 and §5.1,
whose measurements this round CITES without restating; §6's preamble, its four
rendered-text group enumerations (all three new twins read RETURN VALUES, so `C20-2` puts
them in none, exactly as it does row 42's SCOPE twin and row 44's round-20 twin), its
mutant-formatter list, its prefix enumeration, its `C15-2` sibling-fixture rule with row 40
its one exception and its row-45-conforms note, its APPEND RULE, its wiring assertions with
all eight `placedCorrectly` operands and six mutants, its false-negative battery and its
live smoke test; §7's thirteen rows; and §8(a)-(p), none of which gains a residual — all
three fixes add elements rather than concede limits. Re-derived read-only at `53996f5`: the
DERIVED outside-citer command returns **188** hits over **44** distinct rows, and rows 4, 5
and 21 are ALREADY members, so this round creates no renumbering link the registry does not
record; the hit COUNT moves and the registry pins the SET, not the count, and round 22's
sha-pinned **36**/**19** figure at `git show f3aed16:<file>` is history and untouched. The
`see above`/`see below` sweep below `## 0.` returns only the backticked quotations of
`OP13-2`'s fixed cell used as history, and the `TODO`/`TBD`/`FIXME`/`<insert`/placeholder
sweep only §9's own descriptions of that sweep and §4's `<unit>` line-shape discussion — no
live site, so round 10's "guard what an apply inserts" holds. ⚠️ **One structural
observation is recorded and deliberately NOT acted on:** §3's `minus:` list is interrupted
after its FIRST bullet by the "also the comparand" paragraph this round edits, so GFM
renders the four minus-bullets as one bullet, a paragraph, then three. No content is off
the page — unlike `CF16-1`'s table rows — and moving the paragraph would vacate `CF6-1`'s
own ruling that it be stated in the section that DEFINES the output set, so it is left as
written. **No working-tree mutation:** `scripts/bundle-cli.mjs` was never imported, every
probe read files as text or ran `git`, `ls`, `grep`, `sed` and `python3` read-only, every
write went to the scratchpad, and `git status --porcelain` is empty at `53996f5` before and
after.

**Round 24 (2026-08-21) — `contract-fit` / `correctness` / `operability`, run as the SAME
COVERAGE MATRIX as rounds 18-23 and enumerated INDEPENDENTLY of round 23's list. The
`contract-fit` lens returned `approve` with no edit for the SECOND round running, over 91
obligations. THREE finding IDs survived consolidation — `C24-1`, `OP24-1`, `OP24-2` — all
re-derived read-only at `8fdf3ed` on a clean tree and all dispositioned `fixed`.**
`C24-1`: input rule 4's round-14 narrowing named a criterion no implementation can
evaluate. `descendantConfigs(unitDir, allConfigs)` reads a config's own `extends` chain and
nothing else, and `packages/cli/tsconfig.dts.json`'s RESOLVED `outDir` inherits
`"./dist"` from `packages/cli/tsconfig.json`, which EQUALS that unit's `distDir` — so
"emits into the COMPARED output set" makes it a rule-4 member and fires `stale-unit
packages/cli`, the exact harm `C14-3` filed. The rule now states THREE clauses, and clause
(iii), RESOLVED `composite` is `true`, is the only one that separates it from §6 row 47's
`packages/A/tsconfig.emit.json`, which is identical on the other two. Rule 4's live member
count is still ZERO, now for an evaluable reason, so the 21-of-30 arithmetic, the 20/9
split, §0's `29 PATHS` and §8(l)'s NINE are all unmoved — re-derived, not restated. Clause
(iii)'s cost is stated at §8(l) rather than left to be discovered: a future NON-composite
variant handed to a compiler that emits into a compared output set would be in no input
set, and has no member today. `OP24-1` and `OP24-2` concern §4's prefix/ORDER rule,
§6 row 18's verdict cell, `formatFindings`' comment and row 43's CLI twin.
**Round 24's numbers:** 281 obligations enumerated across the three lenses, 3 uncovered —
the fourth consecutive round at exactly 3, against 67/10 at round 18, while the enumeration
grew from 184 to 281. **No working-tree mutation:** every probe read files as text or ran
`git`, `node -e` over `scripts/repo-census.mjs`, `grep`, `sed` and `python3` read-only,
every write went to the scratchpad, and `git status --porcelain` is empty at `8fdf3ed`
before and after.

**Round 25 (2026-08-21) — `contract-fit` / `correctness` / `operability`, run as the SAME
COVERAGE MATRIX as rounds 18-24 and enumerated INDEPENDENTLY of round 24's list. FIVE
distinct uncovered obligations survived consolidation — `CF25-1` (which `correctness` found
independently and filed as `C25-1`; ONE defect, counted ONCE), `CF25-2`, `CF25-3`, `OP25-1`
and `OP25-2` — all re-derived read-only at `7837d25` on a clean tree and all dispositioned
`fixed`.** `contract-fit` had returned `approve` with zero uncovered for the two preceding
rounds; what broke that run is that the matrix was walked CLAUSE BY CLAUSE and STEP BY STEP
rather than rule by rule and section by section.

- **`CF25-1` (= `C25-1`), medium — round 24's own new three-clause rule had an element for
  two clauses and none for the third.** Clause (iii) got two paired fixture pins in round 24
  (§6 row 47's INHERITED-TRUE, §6 row 3's explicit `false`); clause (ii) got none.
  Re-derived read-only at `7837d25`: `clause (ii)` occurs **twice** in the 7009-line
  document — §3's rule-4 paragraph and §8(l)'s price-of-decidability sentence, both prose
  — against **eleven** occurrences of
  `clause (iii)`, and in §6 `clause (ii)` occurs **ZERO** times. ⚠️ **The finding reported
  ONE occurrence and it is TWO (standing rule 2, on a reviewer's own measurement); the
  conclusion is unchanged, both hits being prose and neither an element.** Every rule-4
  config in §6 puts its `outDir` INSIDE the unit's compared output set — row 47's
  `"outDir": "./dist"`, row 3's INHERITED `./dist` — so a `descendantConfigs` implementing
  clauses (i) and (iii) ALONE, with no `outDir` test at all, attaches row 47's config,
  declines row 3's, and passes BOTH rows plus the false-negative battery, the wiring
  assertions and the live smoke test, rule 4 having 0 members on the live tree under BOTH
  readings. Its harm is `C14-3`'s at the clause written to prevent it: a future
  `<unit>/tsconfig.*.json` inheriting `composite: true` with `"outDir": "./dist-test"` — or
  a `packages/gates/tsconfig.bench.json` with `"outDir": "./bench-out"` — enters that unit's
  input set and a `touch` reports `stale-unit <unit>` at `check:all` member 0, exit **1**
  under `--strict`, printing `rm -rf <unit>/dist` plus a 19-unit rebuild for a config whose
  output side provably cannot move for it. Closed by a TWIN on row 47 in row 33's shape: a
  second variant config `packages/A/tsconfig.emit-elsewhere.json`, `composite`
  INHERITED-TRUE and `"outDir": "./lib"` with no `packages/A/lib/` written, verdict CLEAN at
  BASELINE and at the END state, mutant = the (i)+(iii) body, failing on the twin's own
  fixture with no injection point. `isVariantTsconfig("tsconfig.emit-elsewhere.json")`
  returns true (`repo-census.mjs:75-79`, re-derived read-only at `7837d25`). Sites: §0's
  requirement 9 row, §3's rule 4 clause statement and its row-47 testability paragraph, §2's
  `descendantConfigs` comment, §6 row 47's clean cell and its verdict cell, §8(l), and §3's
  Units step. ⚠️ **The last two are the sites the `contract-fit` proposal did not name and
  `correctness` did, and one of them would have been made FALSE by the fix (standing rule
  9):** §3's Units step carried `C21-1`'s measurement as "every fixture in §6 is a
  `src`/`dist` tree, row 47's `tsconfig.emit.json` included, whose `outDir` is `./dist`" — a
  SUBSET of row 47's variant configs the moment the twin lands, `CF14-4`'s class — repaired
  by naming both and by stating why a VARIANT's `outDir` is not a unit layout at all, which
  is what keeps `C21-1`'s own mutant argument and its `3095116` **19 of 19** figure intact;
  and §8(l)'s "the price of making clause (ii) decidable" is the sentence the misreading
  comes from, now qualified rather than left as licence to drop the clause.
- **`CF25-2`, low — requirement 7's `an input to ONE artifact` was asserted for one producer
  of the two, and not for the one it can be false of.** Row 42 pins `stale-declarations` as
  the run's SOLE kind for `bundle-types.mjs`; row 41's verdict for `bundle-cli.mjs` was
  `stale-bundle packages/cli` and **NOT** `stale-unit`, which permits any number of further
  kinds. Re-derived at `7837d25`: `SOLE kind` occurs exactly ONCE in §6, in row 42. So a
  `PRODUCER_INPUTS` consumed as "the producer is an input to every artifact it produces" —
  `bundle-cli.mjs` on §3.4's source side as well as §3.2's, the reading §8(b) names in terms
  when it says the design avoids "three findings" — renders two kinds and two remedy rows
  from one edit and passed every element. Rows 8 and 24 mutate plugin SOURCES; row 8's
  round-20 rendered twin runs ROW 8's end state; row 22 is byte-keyed. ⚠️ **`SOLE kind`
  alone would NOT have discriminated it:** row 41's fixture is row 5's, and the
  complete-cli-build rule binds the six plugin entries under `packages/cli/dist/plugin` and
  says nothing about `packages/plugin/` SOURCES, so §3.4 reported `not-run` and the extra
  kind could not fire — CF8-2's defect inside the fix. ⚠️ **And the fixture clause the
  finding proposed would have re-introduced it one bound over (round-8's class, caught at
  consolidation):** "the six sources OLDER than `bundleAt`" is strictly weaker than what
  §3.4 compares. The marker is written LAST, so `newest(dist/plugin)` is at or before
  `bundleAt` and a source older than `bundleAt` alone can still be NEWER than its shipped
  copy, firing `stale-plugin-assets` at BASELINE where the mutation then flips nothing. The
  clause landed as row 8's own `plugin assets fresh` shape copied WHOLE — every source
  OLDER than the newest file beneath `packages/cli/dist/plugin` (standing rule 7). Row 41's
  fixture also gains row 3's cache clause; its verdict gains the SOLE kind and the second
  mutant. Sites: §0's requirement 7 row, §2's `PRODUCER_INPUTS` comment, §8(b), §6 row 41.
- **`CF25-3`, medium — §5 predicted a per-push CI line for a job in which this member has no
  step, and 38 sites carry the attribution.** Re-derived read-only at `7837d25`, working
  tree clean before and after: `grep -n "npm test" .github/workflows/ci.yml` returns exactly
  ONE run line, `:129`, and `grep -nE '^  [a-z-]*:$'` returns EIGHT headers — `push:` 4,
  `lint:` 13, `typecheck:` 30, `test:` 49, `commitlint:` 196, `engine-pin-lint:` 233,
  `meta-checks:` 246, `packaging:` 403 — putting `:129` and `:86`'s `npm run build` both
  inside `test:`; `meta-checks` (:246) and `packaging` (:403) run `npm ci` then individual
  `npm run check:*` steps and never `npm test`, so `pretest` cannot fire in either.
  `git ls-files -z | xargs -0 grep -ln "run check:all" | grep -v stale-dist-check-design`
  returns **6** files, ALL `docs/evidence` prose, so adding a member to `check:all` adds no
  CI step, and §1's existing-file table has exactly three rows and no `ci.yml`. §5's own
  closing paragraph refuses a CI step, §6's wiring block says so too, and §5.1's opening
  measured it in terms — "they were told that `check:all` runs in CI and in every
  pre-push. It runs in neither" — so the bullet's premise was refused by its own section
  three paragraphs later, by its own subsection eighteen lines later, and head-on by §8(c).
  ⚠️ **And ``both `meta-checks` legs`` was false a second way:** `meta-checks` is
  `runs-on: ubuntu-latest` with no `strategy`/`matrix` — ONE leg; the two legs belong to
  `test:`, where `ci.yml:86` builds first. ⚠️ **Round 16 (`OP16-3`) inspected this bullet
  and blessed it as "a LIVE-repo prediction and therefore correct"** — it checked 19 against
  the live root-reference count and never asked whether the check runs in that job: standing
  rule 1, a search narrower than its claim, applied to a blessing. The BEHAVIOUR rules
  survive, the tree state being real on a fresh clone (`npm ci` then `npm test` via
  `pretest`), which is what §6 row 39's negative twin fixtures; what is false is the CI
  attribution, and it was the stated justification for §4's `nothing was checked` carve-out,
  for `OP15-5`'s 451-character suppression and for `OP21-2`'s "exactly one of its two
  positions per run". Fixed by naming §5's **THE CHECK HAS ONE CI OCCURRENCE** and
  deleting the CI half wherever `on a fresh clone` already stood beside it. ⚠️ **The finding
  counted 17 occurrences of ``both `meta-checks` legs`` and there are 15 (standing rule 2),
  so the EDIT-SITE SET travels here rather than a figure (`C11-4`):** §0 (×2), §2, §3.2 (×3), §3.3, §3.5 (×2), §4 (×6), §5 (×6), §6 (×5),
  §7 (×2), §8(n) — stated as SECTIONS, never line numbers, because the preamble's
  anchor rule binds this section too and round 25's first apply wrote eleven line
  anchors here before this correction — which reached the EDIT-SITE SET alone and left TEN
  standing in this same bullet and FOUR more elsewhere in this entry, all fourteen deleted
  in round 26 (`CF26-2`) —
  **29** edits, §6 row 39's THIRD occurrence spelled `both CI legs`, which a `meta-checks`
  grep does not return. Re-derived at `7837d25`: `meta-checks` occurs **38** times in the
  document, **25** of them live and **13** in this section; ``both `meta-checks` legs``
  occurs **15** times, **10** live. EVERY `meta-checks` occurrence inside this §9 round log
  stands as written, this section being a record of what each round said, and is read under
  this entry — stated as the SET and never as a list of members, a list being a SUBSET a
  later round then reconciles against (`C11-4`, `CF14-4`); the **13**-in-this-section figure
  one sentence above is the whole statement of that population.
- **`OP25-1`, high — of the FIVE steps of §4's one ordered recipe, four were rendered by an
  element and step 1 — the FIRST, and the only one that makes step 2's `npm run build` able
  to clear the commonest kind — by none.** Re-derived over §6: every positive rendered-plan
  step assertion is row 21's PLAN TWIN (`step 2 and step 5, and NO step 1`), row 34's
  SOLE-KIND TWIN (`step 4 and step 5 and NO step 2`), row 39 (`step 5 as its only step`),
  row 42's PLAN TWIN (`step 3, step 4 and step 5, and NO step 2`) and row 46's PLAN TWIN
  (`step 5 as its only step`); every remaining step-1 and `rm -rf` occurrence in that
  section is a mutation column, a fixture description, prose, or a NEGATIVE (row 21's `NO
step 1`, row 38's "does NOT prescribe `rm -rf packages/cli/dist`"). FOUR elements demanded
  step 1's ABSENCE and none its presence, so a plan assembler with NO step-1 branch passed
  rows 21, 34, 38, 39, 42 and 46 and reduced this document's own live prediction — four
  `stale-unit` findings — to `npm run build` then step 5, which §4's pinned-`typescript@6.0.3`
  measurement says re-emits nothing for a touched-but-unchanged source: `CF14-1`'s class
  reached through the step §4 added to prevent it. A second survivor emits step 1 for the
  FIRST selected unit only, byte-identical at N=1 and reached by no fixture at N≥2 —
  `CF18-3`'s N=1 argument at the plan. Row 21's PLAN TWIN had asserted in terms that "rows
  34, 38 and 39 assert plans on fixtures where step 1 is correctly SELECTED", and row 34's
  own cell names no step at all: a property of the FIXTURE credited as an ASSERTION, which
  is round 10's lesson at a citation. §6 row **28** gains a PLAN TWIN — its clean fixture is
  `stale-unit` on ≥2 units and is outside the cli-scoped list, so it is the ONE fixture in
  the battery where step 1 selects more than one unit and step 1's FOURTH carve-out cannot
  exempt it.
- **`OP25-2`, medium — §4's units-compared/skipped summary is "ONE shape and never two" with
  exactly one exemption, and that rule fixed NO rendering for `M = 0`** — the STEADY STATE,
  and the whole human output of every green run at `check:all` member 0, in both `test` legs
  of `ci.yml:129` and at line 5 of §5.1's measured 204 under `pretest`. A direct
  transcription prints `0 skipped ()` (`OP16-4`'s class at the sibling line); dropping the
  parenthetical unlicensed is a second shape under a rule that says ONE. Re-derived: row
  39's MAIN half pins that line at M=1, row 35 at M≥2, row 39's NEGATIVE twin the `nothing
was checked` carve-out, and rows 34 and 38 run `M = 0` fixtures asserting no summary text
  — while §4's own `PASS — 19 units compared, 0 skipped` literal in the coverage paragraph
  is the paren-less form already on the page. Ruled: the parenthetical is OMITTED at an
  EMPTY clause list, which is an empty list not rendered and NOT a non-empty one suppressed,
  so **THE CLAUSE LIST IS SUPPRESSED ON THIS LINE AND ON NOTHING ELSE**, §2's one carve-out
  and §5's own citation of that one exemption all stand unedited (standing rule 8). §6 row
  **18** — zero skipped units, a rendered `PASS` line, already in the prefix enumeration —
  pins the `M = 0` text by EQUALITY with `<N>` off its own fixture (`OP16-3`).

**Counts and enumerations moved, with their sites:** §4's non-vacuity plan-row enumeration
and its §2 mirror,
non-vacuity plan rows **5 → 6** (21, 28, 34, 38, 39, 42), and §2's "Those **five** rows" →
"Those **six** rows" one clause later; §2's and §6 row 34's `OP21-1` presence list **4 → 5**;
§6's plan-first-line prefix enumeration **3 → 4** (21, 28, 42, 46); §6's `C20-2` two-twin row
list **2 → 3** (42, 21, 28), with the five "row 28's twin's" citations re-pointed to "row
28's **N=1** twin's" at §2, §6 row 8's RENDERED TWIN, §6 row 38 (twice) and §9's
round-20 entry — re-derived at `7837d25`, `row 28's twin` occurs exactly **5** times.
**UNCHANGED and deliberately not restated:** §1's `scripts/check-stale-dist.test.mjs` **49**
battery rows — all three new elements are TWINS and none adds a row (`OP22-3`), so §1
and §6's append-registry bullet both stay put; rule 4's live member count at
**ZERO**; §3's **21 of 30**, its **20**/**9** split, §0's **29 PATHS** and §8(l)'s **NINE**,
every new config being SYNTHETIC; `check:all`'s member figure (**14** today, 15 after,
re-derived at `7837d25`); `OP15-5`'s **451** characters; the `0 units compared, 19 skipped`
literal; §4's FOUR carve-outs and its ONE summary exemption; §6's rendered-text group and
mutant-formatter lists, which already carry rows 18 and 28 by number; §6's "TWIN halves of
rows 8, 20, 21, 42, 43 and 46" list, row 28's main half being rendered text; and §6's
sha-pinned outside-citer registry — re-derived read-only at `7837d25`,
`grep -onE '§6 rows? [0-9]+'` outside §6 returns **206** hits across **44** distinct rows
and BOTH 18 and 28 are already members, so this round creates no citation the registry does
not record, and the bullet pins the SET at `f3aed16` rather than the live count.
**Round 25's numbers:** `contract-fit` enumerated **96** obligations and returned **3**
uncovered; `correctness` returned **1**; `operability` returned **2**; consolidation merged
`C25-1` into `CF25-1` as ONE defect found twice, leaving **FIVE** distinct uncovered
obligations, all dispositioned `fixed`. Per-lens enumeration totals for `correctness` and
`operability` are NOT stated, this entry having not derived them — a figure nobody
re-derived is `C11-4`'s class. **No working-tree mutation:** `scripts/bundle-cli.mjs` was
never imported, every probe read files as text or ran `git`, `grep`, `sed` and `python3`
read-only, every write went to the scratchpad, and `git status --porcelain` is empty at
`7837d25` before and after.

**Round 26 (2026-08-22) — `contract-fit` / `correctness` / `operability`, run as the SAME
COVERAGE MATRIX as rounds 18-25 and enumerated INDEPENDENTLY of round 25's list rather than
as a check that its fixes landed. SIX distinct uncovered obligations survived consolidation
— `CF26-1`, `CF26-2` (which `operability` found independently and filed as `OP26-2`; ONE
defect, counted ONCE, in round 25's own precedent for `CF25-1`), `CF26-3`, `C26-1`, `C26-2`
and `OP26-1` — all re-derived read-only at `e99b066` on a clean tree and all dispositioned
`fixed`. NO battery row is appended, nothing is renumbered and NO twin is added, so §1's
**49** stays correct (`OP22-3`) and §6's sha-pinned outside-citer registry needs no re-run;
no kind, reason spelling, coverage status or exit code is added.** ⚠️ **This round carried
one ADDITIONAL standing rule and every fix is logged against it — PREFER A FIX THAT CREATES
NO NEW OBLIGATION: delete a clause the repository cannot reach, else narrow a sentence that
already exists, else strengthen an EXISTING element, and only then add.** Rounds 18-25
measured that FIXES CREATE OBLIGATIONS — round 24 replaced a one-clause rule with a
three-clause rule and round 25 found clause (ii) had no element — so an added clause with no
named element is the defect that pattern keeps producing. **FIVE of this round's edits are
pure DELETIONS and every other one is a NARROWING of a sentence already on the page or a
clause on an element that already exists; NOTHING is added that mandates new behaviour, and
exactly ONE count moves — §6 row 47's twin's mutant enumeration, ONE → TWO, stated as the
SET of edit shapes rather than as an edit tally, a tally being a figure the next apply
invalidates (`C11-4`).**

- **`CF26-1`, medium — clause (ii)'s only element stated its discriminating property in RAW
  terms, so the reading round 14 REFUSED also passed it.** §3 input rule 4's clause (ii) is
  worded against the unit's COMPARED output set and explicitly NOT `<unit>/dist` raw, `C14-3`
  recording that the raw-`dist` wording the finding proposed would have left the defect
  standing — but round 25's twin failed the clause with `"outDir": "./lib"`, which lies
  outside `packages/A/dist` under BOTH readings. So a `descendantConfigs` evaluating clause
  (ii) as RAW path containment declined the twin's config, passed its CLEAN-in-both-states
  verdict, passed row 47's MAIN half and both halves of row 3, and could not be seen on the
  live tree, rule 4 having ZERO members there under BOTH readings. That body is the path of
  least resistance: an `outDir` is a DIRECTORY, while the compared output set is a set of
  FILES that need not exist yet. §3's own rule-4 harm sentence already offered
  `"outDir": "./dist/plugin"` as the inside-raw case and then asserted "Row 47's twin holds
  exactly that config" — FALSE while the twin held `./lib`, and TRUE after this fix, which is
  the defect stated by the document about itself. Closed by STRENGTHENING THE EXISTING TWIN
  and adding nothing: `packages/A/tsconfig.emit-elsewhere.json`'s `outDir` moves from `./lib`
  to `./dist/eo-elsewhere-fixture-1`, still a directory the fixture NEVER creates, now INSIDE
  `packages/A/dist` RAW and OUTSIDE the COMPARED set through §3's `dist/eo-*-fixture-*/**`
  minus-bullet — which is in BOTH families, is NOT cli-scoped and reaches every unit, rows 14
  and 44 being its non-cli rows. ONE element, BOTH wrong bodies, on the twin's own fixture
  with no injection point. **COUNTS THIS ROUND CHANGES HERE, with their site — exactly one:**
  the twin's mutant enumeration in §6 row 47's verdict cell, **ONE mutant → TWO**, and that
  cell is the only site of it — re-derived read-only at `e99b066`,
  `awk '/^## 6\./,/^## 7\./' <file> | grep -onE '(ONE|TWO|THREE|FOUR|FIVE|[0-9]+) mutants?'`
  — pattern-scoped, never line-keyed, for the reason `CF26-2` below gives — returns **10**
  hits, EIGHT of them other rows' own cells and TWO inside §6's
  wiring-assertion comment blocks, and not one an aggregate over the battery. ⚠️ **The
  proposal said all ten are single rows' own cells; re-derived it is eight and two (standing
  rule 2, on a reviewer's own measurement), and the conclusion is unchanged.** `OP16-3`'s and
  `CF20-1`'s disqualifier re-checked AT the fix: under all three clauses the config is in no
  input set, so both of the twin's states stay clean and no correct implementation fails it.
  Standing rule 8, checked the other way: `packages/A/dist/eo-elsewhere-fixture-1/` is never
  created, so §3's output set, §3.1's stem bijection, rows 14 and 44's scratch elements and
  row 47's MAIN half are untouched. SITES, all moved together per standing rule 9: §2's
  `descendantConfigs` comment, §3's Units step inside `C21-1`'s **19 of 19** sentence, §3's
  rule-4 clause-(ii) element paragraph, and §6 row 47's twin at all three of its cells —
  fixture, verdict and the standing-rule-8 sentence. ⚠️ **§3's
  `packages/gates/tsconfig.bench.json` illustration KEEPS `"outDir": "./bench-out"`
  deliberately**: it illustrates the (i)+(iii) body, which is still a wrong body, and an
  OUTSIDE-raw example standing beside §3's own INSIDE-raw one covers both readings where a
  single re-pointed example would cover one. UNCHANGED and deliberately not restated: §1's
  **49**; rule 4's live member count **ZERO**; §3's **21 of 30**, its **20**/**9** split,
  §0's `29 PATHS` and §8(l)'s **NINE**, the config being SYNTHETIC and its `outDir` never
  written on any disk; §0's requirement 9 row, which names clause (ii)'s failing side without
  naming the directory; §3's Units step's **19 of 19** and its EMPTY exception set, that
  measurement being over LIVE unit `tsconfig.json` files and a variant's `outDir` still not
  being a unit layout. §9's round-25 entry keeps `"./lib"` as the record of what round 25
  said and is superseded by this bullet.
- **`CF26-2` (= `OP26-2`), medium — the bullet that says its sites are "stated as SECTIONS,
  never line numbers" carried FOURTEEN intra-document line references, and not one of them
  resolves.** §9's round-25 entry: TEN in the `CF25-3` bullet itself — its `both CI legs`
  anchor plus its nine `meta-checks` HISTORY anchors — TWO in the `CF25-1` bullet, both on
  its `clause (ii)` measurement, and TWO in the closing counts paragraph, one on §6's
  append-registry bullet and one written as a section plus the word `line` with no colon at
  all. ⚠️ **Not one of the fourteen is REPRODUCED here, the numbers being the defect:** each
  is named by the sentence it stood in, which is the same remedy applied to this entry that
  the fix applies to that one. Re-derived
  read-only in both directions at `e99b066`, `git status --porcelain` empty before and after
  and `scripts/bundle-cli.mjs` never imported: all fourteen resolved at `7837d25` (7009
  lines) and NONE resolves at `e99b066` (7306 lines), round 25's own apply having added 297
  lines and moved every line they named. A reader following any of the fourteen lands on
  unrelated text, which is verbatim the preamble's founding measurement — six anchors, all
  six resolving to the wrong text — reproduced inside the section whose own bullet forbids
  them, and `CF16-2`'s defect at fourteen sites at once. ⚠️ **ALL FOURTEEN were invisible to
  EVERY check the preamble runs:** the anchor grep, passes 1, 2 and 3, the escape-aware row
  census, the orphan-row sweep, the split-sentence sweep and the placeholder sweep were all
  re-run at `e99b066` and all returned nothing, because `grep -on '§[0-9][0-9.]*:[0-9]'`
  matches the section-sign-plus-colon form ONLY and these are bare. FIXED BY DELETION at all
  fourteen sites, the fix that creates no obligation: each is replaced by the SECTION-and-name
  form the same entry already uses, and the nine history anchors go by `CF26-3`'s remedy
  below. ⚠️ **The anchor grep is NOT widened, and that is a ruling rather than an omission,
  both candidate widenings having been MEASURED (standing rule 5, round 22's own precedent
  for the sentence-split guard).** The bare three-or-four-digit form returns **128** hits
  below `## 0.`, of which **13** are these anchors and **115** are LEGITIMATE line citations
  INTO OTHER FILES — §1's `scripts/bundle-cli.mjs` cell alone carries `:55`, `:121`, `:176`,
  `:178` and `:181` — and the paragraph beside it rules that those stay exact, so it can
  never return nothing: round 11's own test for whether a check is a check, at a fifth site.
  The four-digit-only form returns exactly the thirteen today and is refused for keying on
  digit COUNT as a stand-in for "into this document" — blind to the two bare three-digit
  intra-document anchors the guard quotes as its own counterexamples, `:710` and `:724`, and
  blind past 9999 lines on a file already at **7306** and growing about 290 a round. The
  residual is STATED in the preamble beside the rule it bounds, at ONE host and not two, and
  the rule stays enforced by READING. ⚠️ **`operability` filed the same defect independently
  and reported THIRTEEN, `contract-fit` FOURTEEN; both are right about different
  populations — thirteen bare colon-plus-digit anchors, plus a fourteenth written as a
  section and the word `line` —
  and `operability`'s proposed widening figure of 168 reproduces under no spelling of its
  own regex (the bare two-or-three-digit sweep returns 380), so it is not shipped.** ⚠️ **The
  same bullet's "round 25's first apply wrote eleven line anchors here BEFORE THIS
  CORRECTION" was a completion claim only partly true** — the correction reached the
  EDIT-SITE SET and left ten anchors standing in that same bullet and four more elsewhere in
  the entry, the tenth consecutive round to find one (standing rule 9); that clause is
  NARROWED to say so rather than deleted, being the record of what round 25 did. **NO COUNT
  CHANGES and no enumeration lengthens:** the EDIT-SITE SET, the **29** edits and the
  **38**/**25**/**13**/**15**/**10** figures all stand exactly as written, and only the
  anchors go.
- **`CF26-3`, low — a nine-member list stood for a population the same sentence measures at
  thirteen.** §9's round-25 entry, `CF25-3` bullet, states "`meta-checks` occurs **38** times
  in the document, **25** of them live and **13** in this section" and then names NINE §9
  sites as "the HISTORY sites here". Re-derived read-only at `7837d25`, the sha the entry
  itself names, `awk '/^## 9\./,0' <file> | grep -c meta-checks` returns **13** against the
  list's NINE. The FOUR omitted are named by their own text rather than by line, this bullet
  being where that lesson is written: round **14**'s widened-gate sentence, "a widened gate
  would print `npm run build` on every fresh clone and in both `meta-checks`", whose `legs`
  WRAPS to the next line so a single-line grep for the full phrase never returned it; round
  **15**'s "on a fresh clone and in both `meta-checks` legs"; round **15**'s "the reason
  printed for all 19 units on a fresh clone, in both `meta-checks` legs"; and round **16**'s
  "rule gives the `meta-checks` fixture 19 units". THREE of the four carry the very CI
  attribution the bullet is cataloguing. The nine are not the phrase's §9 set either:
  ``both `meta-checks` legs`` occurs on exactly FIVE §9 lines at that sha and only THREE of
  them are in the list. So the nine match NEITHER population the sentence measures,
  and a later round reconciling §9's history against the list finds three false-attribution
  sites the list does not name — `CF14-4`'s "never restate a subset", inside the bullet that
  says the EDIT-SITE SET travels here rather than a figure (`C11-4`). Separable from
  `CF26-2`: written as section names the list would still be nine of thirteen. FIXED BY
  DELETION, and by the SAME deletion `CF26-2` makes: the list is replaced by the RULE that
  quantifies over the whole population — every `meta-checks` occurrence inside this §9 round
  log stands as written. No enumeration survives to drift, nothing is added, no count
  changes, and the **13**-in-this-section figure the same sentence already carries becomes
  the only statement of that population, which is `C11-4`'s own remedy stated as the SET.
- **`C26-1`, medium — `plugin-assets` is asserted `full` on fixtures this design never gave a
  plugin SOURCE tree, so the assertion FAILS a correct implementation and the mutant it
  exists to kill ships.** §6's complete-cli-build rule enumerates what every cli-scoped
  fixture holds — a chunk, the three entry outputs, `dist/index.d.ts`, the six plugin entries
  and the marker — every member of it under `packages/cli/`, and said NOTHING about the six
  `PLUGIN_ASSET_ENTRIES` SOURCES that §3.4's source side reads. Re-derived read-only at
  `e99b066`, `git status --porcelain` empty before and after, no import of
  `scripts/bundle-cli.mjs`: over the paragraph
  `awk '/Every cli-scoped row starts from a COMPLETE cli build/,/^$/'` delimits — 37 lines,
  the whole rule — `grep -c packages/plugin` returns **0**, and `grep -c 'SOURCES under'`
  over the whole document returns **1**, row 41's
  round-25 clause (`CF25-2`) — which round 25 wrote for precisely this reason and wrote at
  ONE row. On a fixture holding no sources §3.4's source side yields nothing and §3.3's
  **SKIP RULE — ALL FOUR cli-SCOPED COMPARISONS** declines, so `plugin-assets` reads
  `not-run` with NO admissible spelling, §3.4's three ranked reasons all naming the DIST
  side; row 41's own cell says so in terms — "without that clause §3.4's SOURCE side is
  absent, the comparison declines". Row **38**'s THIRD assertion (round 16, `OP16-4`) — the
  SOLE element for §4's coverage-line suppression — then fails conforming code, and
  reciprocally `OP16-4`'s mutant, a `formatFindings` emitting the label whenever it emits
  anything, is killed by nothing and ships a bare `check-stale-dist: ⚠️ coverage: ` on every
  green run at `check:all` member 0 and at line 5 of §5.1's measured 204 under `pretest`.
  ⚠️ **The dependent sites are stated as the SET the fix quantifies over and never as a list
  (`C11-4`), the proposal's own list having named FIVE and included row 19, which asserts no
  such thing:** every row whose verdict asserts a `full` `plugin-assets` entry, directly or
  through an `all four` or `the other three` aggregate — a `plugin-assets` grep returns
  neither aggregate, which is why the clause carries BOTH spellings (standing rule 1).
  **Fixed by ONE SIBLING CLAUSE on the complete-cli-build rule, keyed on what a row ASSERTS
  in the `C15-2` sibling-fixture rule's own shape** and deliberately NOT on that rule's
  cli-scoped row list. ⚠️ **The row-list form was proposed and is REFUSED, because it
  falsifies two prior rulings in the commit that lands it (standing rule 8):** §6 row 24's
  cell rests on "Row 8's mutation is `packages/plugin/skills/**`, a DIRECTORY member, and its
  fixture need not contain the file member at all", which is row 24's whole non-redundancy
  argument and is false once row 8 must hold `.mcp.json`; and round 19's `C19-2` REJECTED an
  `all four cli-scoped entries full` clause at row 19's twin "because three of the four
  entries are free variables there". The assertion-keyed form reaches rows 17, 22's round-13
  twin, 34, 38 and 41 and leaves rows 8, 19 and 24 outside. Row **41** is LEFT AS WRITTEN and
  the rule records that it GENERALISES that clause rather than replacing it (`CF17-3`'s
  shape). Two sites take a POINTER and not the mandate, being bullets rather than rows — the
  false-negative battery's `C10-4` conjunction and its §8(p) partial copy, whose markers are
  ABSENT (`CF6-2`'s remedy shape). ⚠️ **Standing rule 8, checked AT the fix:** the clause is
  a FIXTURE requirement and adds nothing to §3.3's clause-1 artifact set or to row 11's
  completeness enumeration, so a missing source is not an `unbuilt` trigger and no verdict
  moves; the bound is OLDER than the newest file beneath `packages/cli/dist/plugin` — row 8's
  own `plugin assets fresh` shape copied WHOLE (standing rule 7) — never older than
  `bundleAt`, the weaker form round 25 rejected, so no baseline gains `stale-plugin-assets`
  and no SOLE-kind assertion moves. ⚠️ **The absent-source state is REMOVED from the fixture
  space rather than SPECIFIED:** §2 leaves `newestOfEntries` undefined for an absent member —
  `undefined` and a `statSync` throw are both defensible — and ruling on it would be a NEW
  obligation needing its own element, while the state is unreachable on any real tree,
  re-derived read-only at `e99b066`: all six exist under `packages/plugin/`, five directories
  and `.mcp.json`. COUNTS THIS EDIT CHANGES: none is a number. Row 38's justification moves
  from three grounds to four and is restated as the SET (`C11-4`). Sites: §6's
  complete-cli-build rule, §6 row 38's third-assertion justification, and the two
  false-negative-battery bullets.
- **`C26-2`, low — §3.2 mandates the not-run reason's JOIN ORDER and its only element cannot
  see it, the mandated order and the forbidden one COINCIDING on the one fixture that reaches
  N=2.** §6 row 40's round-18 twin (`CF18-3`) is the battery's only two-offender element —
  §6's `C15-2` sibling-fixture rule forbids every other row asserting `stale-bundle` or a
  `full` `bundle-freshness` entry from reaching a second offender, and row 40 is its ONE
  stated exception — and it asserts EQUALITY on the whole reason string while pinning nothing
  about the marker's `inputs`. ⚠️ **The finding said the only §6 cell naming a marker's
  `inputs` is row 45 and there are TWO (standing rule 2):** re-derived read-only at
  `e99b066`, `` `inputs` `` occurs twice in §6, at row 4's round-23 twin and at row 45, and
  EACH names exactly ONE member — so the conclusion is unchanged and stronger, no element in
  this design reaching a second member at all. `enumerateRootReferences` SORTS
  (`scripts/repo-census.mjs:293`; the live root `tsconfig.json` parsed read-only returns 19
  sorted path-form dirs, `e2e/report` first and `packages/testkit` last), so
  `packages/contracts` precedes `packages/gates` under the mandated order and a fixture author
  writing the marker in that same order makes both bodies render the mandated string. The
  forbidden body is the path of least resistance — filter `inlinedUnitsFromMetafile(metafile)`,
  whose order is `metafile.inputs`' own insertion order — and on §4 recipe step 1's own
  two-offender tree it renders `packages/gates/dist, packages/contracts/dist absent`, one
  state in two spellings, which §4's verbatim-reason rule calls a defect in terms. `CF18-3`'s
  own shape one level in: the round that fixed the reason's RENDERING at N=2 stated the ORDER
  and left it a free variable in the one fixture that reaches N=2. **Fixed by ONE clause on
  that twin's fixture** — its marker naming `packages/gates/dist/index.js` BEFORE
  `packages/contracts/dist/index.js` — so an assertion that already exists discriminates,
  with §3.2's own sentence repointed onto it and given the source anchor its "sorted" claim
  never had (re-derived: `sorted` occurs at exactly TWO sites in this document, §2's
  `stemsUnder` declaration and that sentence, and §3's Units step does not state it).
  ⚠️ **NO mutant is added and the cell's TWO mutants stand:** the metafile-order body FAILS
  on the twin's own fixture once the orders are opposite and needs no injection point.
  ⚠️ **It cannot FAIL a correct implementation (`OP16-3`'s disqualifier):** a body joining in
  `buildUnits`' sorted order renders `packages/contracts/dist, packages/gates/dist absent`
  whatever the marker's key order is. ⚠️ **Standing rule 8:** row 40's single-offender MAIN
  half, its two mutants and its status as the `C15-2` rule's ONE exception are untouched; the
  complete-cli-build rule constrains the marker's `outputs` (round 12, `C11-6`) and never its
  `inputs`, so no cli-scoped fixture is affected. COUNTS THIS EDIT CHANGES: none.
- **`OP26-1`, medium — the `M = 0` rendering is ruled for BOTH summary slots and had an
  element for ONE.** §4's **SKIPPED UNITS ARE NAMED ON BOTH PATHS** rules the parenthetical
  omitted at an empty clause list "on this line and on the findings-path line alike, which is
  the general shape and not a second carve-out", and §2's `formatFindings` comment says
  "`<N> units compared, 0 skipped`, in EITHER slot" — while both named ONE element, "§6 row
  18 is the element", whose fixture is ADVISORY-ONLY and therefore the one state §4's exit
  table renders a `PASS` line and a finding together. The STANDALONE summary at `M = 0` was
  rendered by nothing, which row 18's own cell recorded: "rows 34 and 38 hold `M = 0`
  fixtures that assert no summary text at all". `OP25-2` closed `M = 0` for one of this
  rule's two slots — standing rule 9 at `OP25-2`'s own fix, one round later. Re-derived
  read-only at `e99b066`:
  `awk '/^## 6\./,/^## 7\./' <file> | grep -on 'units compared'` returns **seven**
  occurrences across **five** sites — §6's preamble bullet, row 18 (`PASS` line, `M = 0`),
  row 35 (findings path, M≥2), row 39's MAIN half (`PASS` line, M=1) and row 46 (prose).
  ⚠️ **The finding reported FIVE hits and it is seven over those same five sites (standing
  rule 2); the conclusion is unchanged.** So a `formatFindings` rendering the `PASS`-slot
  summary with the omission row 18 forces and the STANDALONE line from a second template —
  two slots being the implementation §4 and §2 both describe in terms — passed rows 18, 30,
  34, 35, 38, 39 and its twins and row 43's CLI twin, the false-negative battery (which
  asserts NAMING alone), the wiring assertions and the live smoke test, while printing
  `check-stale-dist: <N> units compared, 0 skipped ()` on every run holding a finding against
  a fully built tree. That tree is THIS working copy: re-derived read-only at `e99b066` by
  parsing the live root `tsconfig.json`, **19** units, **ZERO** without a `dist` and **ZERO**
  with an empty one, beside §6's live prediction of four `stale-unit` findings — so the
  findings path at `M = 0` is this repository's own day-one state, at `check:all` member 0,
  exit **1** under `--strict`, and at line 5 of §5.1's measured 204 under flagless `pretest`.
  `OP16-4`'s bare-container class at the sibling slot of the line `OP25-2` filed to remove
  it. Closed by a CLAUSE on §6 row **38** — no row, no twin, no new fixture — appended after
  that row's coverage-absence conjunct and never between that conjunct and its appositive
  (`CF21-1`). Row 38 hosts it rather than row 34 for round 16's own reason at the sibling
  clause: the `C15-2` sibling-fixture rule binds row 38's fixture to hold qualifying output
  under every inlined member and the complete-cli-build rule binds `packages/cli`, while row
  34's is not pinned. ⚠️ **The ONE new obligation this fix creates is written into the cell
  rather than inferred:** that fixture may hold no unit outside those two rules' reach, since
  a bare unit added later would make `M` 1 and the assertion would then FAIL a correct
  implementation (`OP16-3`'s class, settled before the clause was written). Sites: §4's
  element sentence, §2's `formatFindings` comment, §6's prefix enumeration at row 38's item
  and at row 18's parenthetical, §6 rows 18 and 38, and this entry. No count moves.

**Round 26's numbers:** SIX distinct uncovered obligations, all dispositioned `fixed`,
against FIVE at round 25 and 3-3-3-3 at rounds 21-24; `CF26-2` and `OP26-2` are ONE defect
found twice and counted once. Per-lens enumeration totals are NOT stated, this consolidation
having not derived them — a figure nobody re-derived is `C11-4`'s class, which is round 25's
own precedent. **Obligations created by this round's own fixes, each with the element or the
site that carries it:** exactly TWO — §6 row 47's twin now owes TWO mutants where it owed
one, BOTH named in that same verdict cell and both failing on the twin's own fixture with no
injection point; and §6 row 38's fixture may hold no unit outside the `C15-2` sibling-fixture
rule's and the complete-cli-build rule's reach, written into that row's own cell as a fixture
clause. Every other fix is a deletion or a narrowing, and a deleted anchor can never go
uncovered. **No working-tree mutation:** `scripts/bundle-cli.mjs` was never imported, every
probe read files as text or ran `git`, `grep`, `sed`, `awk` and `python3` read-only — the
`enumerateRootReferences` figures were re-derived by PARSING the root `tsconfig.json` rather
than by importing `scripts/repo-census.mjs` — every write went to the scratchpad, and
`git status --porcelain` is empty at `e99b066` before and after.

**Round 27 (2026-08-22) — `contract-fit` / `correctness` / `operability`, run as the SAME
COVERAGE MATRIX as rounds 18-26 and enumerated INDEPENDENTLY of round 26's list rather than
as a check that its fixes landed. THREE distinct uncovered obligations survived
consolidation — `CF27-1`, `C27-1`, `OP27-1` — all re-derived read-only at `2c203a0` on a
clean tree and all dispositioned `fixed`. NO clause, NO battery row, NO twin and NO fixture
is added, nothing is renumbered, and no kind, reason spelling, coverage status or exit code
is added — so §1's **49** stays correct (`OP22-3`) and §6's sha-pinned outside-citer
registry needs no re-run.** Round 26's DELETE-FIRST rule stayed in force and every edit is
logged against it: ONE fix per lens is policy step 3 (STRENGTHEN an existing element by
changing or extending a VALUE) and every other edit is policy step 2 (NARROW a sentence
already on the page). Step 4 is used once, for this entry.

**Enumerations, each derived at this consolidation rather than carried:** `placedCorrectly`'s
SEVEN conjuncts against the six mutants beside them — ONE uncovered; §3 rule 4's three
clauses on both sides against §6's rule-4 elements, rows 3 and 47's two halves — ONE
uncovered; §4's exit table's four conditions by two columns, EIGHT states, against every
`exitCodeFor` assertion in §6 — ONE uncovered. Per-lens document-wide totals are NOT stated,
this consolidation having not derived them, a figure nobody re-derived being `C11-4`'s class
(round 25's own precedent).

- **`CF27-1`, medium, scope SUBSTANCE — the FIRST of §6's five bundler source properties was
  proved non-vacuous by nothing.** `placedCorrectly`'s conjunct
  `src.search(CLEAR) < src.indexOf(BUILD)` (commented "cleared before the build — C8-2") is
  flipped by NONE of the six mutants beside it — the delete-CLEAR mutant makes
  `search(CLEAR)` **-1**, which SATISFIES it — so the conjunct-deleted predicate is
  behaviourally identical on every input §6 tests and the conjunct is DELETABLE with every
  mutant still flipping. That is this section's own definition of vacuous and verbatim the
  state round 22 (`CF22-1`) refused for round 7's conjunct one clause away. It is also
  `CF14-4`'s "never restate a subset" inside the mutant-coverage argument itself: that note
  paired conjunct to mutant for SIX of the SEVEN and omitted `CLEAR < BUILD` entirely. The
  body it forbids and nothing else catches is the clear moved OUT of the wipe step to sit
  with the other `.bundle-meta` statements, under which the marker no longer names the build
  it belongs to and a surviving metafile names some OTHER run's inlined unit set. **The
  exposure is bounded and the bound is stated rather than assumed:** round 9 (`C9-1`) demoted
  the clear to defence in depth, and a relocated clear still leaves the surviving marker
  OLDER than the chunks `:121` writes after the `:113` wipe, so §3.3's clause 2 reaches
  `unbuilt` on the interrupts that matter — an argument from those two sections' own text,
  never a measurement, and labelled as one. What was NOT bounded is the CLAIM: §0, §1 and
  §3.3 each assert the property mutant-proved.
- **Fixed by a SEVENTH expectation beside the six, and its DESTINATION is load-bearing.** The
  clear is deleted and re-inserted immediately before the plugin-copy loop. ⚠️ **The lens
  proposed re-inserting it beside the `mkdir`, and that would have FAILED A CORRECT
  IMPLEMENTATION:** §8(k) states in terms that "an implementation putting the `mkdir`
  immediately after the clear violates [§1's] letter, builds correctly and passes", and on
  that conforming source the proposed mutant leaves the clear before `build()`, the predicate
  TRUE and `toBe(false)` failing — `CF20-1`'s and `OP16-3`'s class, re-opening the residual
  §8(k) records. The copy loop cannot: `await build({` is `bundle-cli.mjs:121` and
  `for (const entry of PLUGIN_ASSET_ENTRIES)` is `:177` (re-derived read-only at `2c203a0`,
  `grep -cF` both **1**), both pre-existing statements no §1 edit moves, so the sunk clear
  lands after `build()` in EVERY conforming source. Standing rule 8 MEASURED at the fix
  against all THREE conforming source forms (one-line, prettier-wrapped, `metaDir`-factored):
  it flips `CLEAR<BUILD` ALONE, leaving `CLEAR.test`, `MKDIR.test`, `WRITE.test`,
  `CLEAR<MKDIR`, `MKDIR<WRITE` and `COPY<WRITE` TRUE, and all six existing mutants still flip.
  It is placed ABOVE round 22's three order mutants deliberately: appending below would
  re-point the note's "THE LAST MUTANT", the positional citation `C11-4` forbids, so this
  placement needs no repointing edit at all. The delete-CLEAR mutant is KEPT and is NOT
  replaced by it. **Deletion and narrowing were both refused first:** deleting the conjunct
  falsifies §0's ONE CHAIN sentence and §1's edit list and drops the only textual enforcement
  of round 8's `C8-2` placement; narrowing §1 converts a defect into a residual and leaves the
  conjunct deletable. The mutant-coverage note is rewritten to quantify over ALL SEVEN
  conjuncts as a SET, with the per-conjunct flip table re-derived at the apply.
- **`C27-1`, medium, scope SUBSTANCE — clause (ii)'s ACCEPTING side was pinned only at
  `distDir` ITSELF, so the EQUALITY reading passed every element in this design.** Rounds 25
  and 26 closed clause (ii)'s DECLINING side (row 47's twin) and clause (iii)'s both sides;
  nothing required clause (ii) to ACCEPT an `outDir` strictly BELOW `distDir`. Both configs
  exercising the accepting side resolved to the unit's `distDir` — §6 row 47's
  `tsconfig.emit.json` declaring `"outDir": "./dist"`, §6 row 3's `tsconfig.dts.json`
  INHERITING it — so a `descendantConfigs` spelling clause (ii) as
  `resolve(unitDir, outDir) === unit.distDir` attached row 47's config and rendered its
  `stale-unit packages/A`, declined row 3's and the twin's, and passed BOTH halves of both
  rows, the false-negative battery, the wiring assertions and the live smoke test, which
  cannot discriminate: rule 4's live member count is ZERO under that body and under the
  correct one. Its harm is a FALSE NEGATIVE at the one clause rounds 25 and 26 spent two
  rounds making non-vacuous — a future `packages/gates/tsconfig.esm.json` inheriting
  `composite: true` with `"outDir": "./dist/esm"`, the ordinary dual-emit layout, is in NO
  input set, so a `touch` of it prints PASS, exit **0 even under `--strict`** at `check:all`
  member 0 and under flagless `pretest`, on a `dist` compiled under the old
  `compilerOptions`: the founding failure's own shape. Closed by STRENGTHENING THE EXISTING
  MAIN HALF and adding nothing — `packages/A/tsconfig.emit.json`'s `outDir` moves from
  `./dist` to `./dist/emit`, a STRICT subdirectory reached by NO minus-bullet of §3's output
  set and written on no disk, exactly as the twin's directory is. `OP16-3`'s and `CF20-1`'s
  disqualifier checked AT the fix: a CORRECT body ACCEPTS it, the compared output set being a
  set of FILES that need not exist yet (§3's own words). ⚠️ **Standing rule 8, checked in
  BOTH directions:** the twin's TWO mutants are untouched — the (i)+(iii)-alone body still
  attaches both configs and the raw-containment body still attaches the twin's,
  `./dist/emit` being inside `packages/A/dist` RAW under both — row 3's pairing survives
  because both configs still SATISFY (ii) and disagree on (iii) ALONE, and
  `packages/A/dist/emit/` is never created, so §3's output set, §3.1's stem bijection, rows
  14 and 44's `eo-*-fixture-*` elements and row 47's own `stale-unit packages/A` equality are
  all unmoved. ⚠️ **The proposal also asked to DELETE §3's `"outDir": "./dist/plugin"` harm
  example, and deleting it would have RE-OPENED `CF26-1`:** that example is the INSIDE-raw
  case the very next sentence's "Row 47's twin holds exactly that config" resolves against,
  and the surviving `./dist-test` example is OUTSIDE raw — verbatim the falsity round 26
  repaired. The example is NARROWED instead, to the twin's own
  `"./dist/eo-elsewhere-fixture-1"` with the `dist/eo-*-fixture-*/**` minus-bullet that
  decides it named, which repairs what the finding correctly measured — `dist/plugin/**` is a
  `packages/cli`-ONLY minus-bullet, so the sentence was FALSE for the other 18 units — and
  makes that citation EXACT rather than shape-true. ⚠️ **Two of the proposal's own
  measurements were wrong and are corrected here (standing rule 2):** its §6 census was
  LINE-keyed, and re-derived read-only at `2c203a0`,
  `awk '/^## 6\./,/^## 7\./' <file> | grep -oE "clause \(ii\)|clause \(iii\)|descendantConfigs" | sort | uniq -c`
  returns `clause (ii)` **6**, `clause (iii)` **4** and `descendantConfigs` **6** against the
  reported ONE and ZERO — the conclusion is unchanged, none of the six being an element for
  the accepting side; and its "exactly one site restates the main half's mutant count" was
  TWO, the second being that row's TWIN standing-rule-8 sentence. The `outDir` census IS
  confirmed: `./build` **1** (row 49's unit layout), `./dist` **2** (rows 3 and 47) and
  `./dist/eo-elsewhere-fixture-1` **1**, so no accepting-side `outDir` lay below `distDir`
  anywhere in the battery.
- **`OP27-1`, high, scope SUBSTANCE — §4's exit-table FIRST row had no element in its
  `--strict` column, and the three sentences that reason about the empty finding set all
  asserted the opposite.** `OP18-1` closed that table's DEFAULT column, `CF19-1` closed the
  CLI's ARGUMENT and `OP23-1` closed the MIXED set; none of the three reached the EMPTY set
  under `--strict` — the steady state of every correctly built tree. Every exit-code
  assertion in §6, walked row by row: row 21's four direct calls and its EXIT-CODE MIX TWIN,
  the false-negative battery's truncated-marker, `C10-4` and §8(p) bullets and row 43's CLI
  twin all carry ≥1 finding; rows 27 and 31 take their exit from §1's catch; row 31's clean
  baseline is the one zero-finding PROCESS run and is FLAGLESS; row **39** is the battery's
  only `ZERO findings` fixture and asserted rendered text, three twins and a `--json`
  invocation and NO exit code; and the live smoke test asserts the unit SET and the coverage
  shape and explicitly no exit code. Re-derived read-only at `2c203a0` (node v24.18.0,
  nothing else running but the shell) with `ADVISORY_KINDS = {bundle-provenance-missing}`,
  `correct = strict ? (f.every(isAdvisory) ? 0 : 1) : 0`,
  `survivor = strict ? (f.length > 0 && f.every(isAdvisory) ? 0 : 1) : 0` and round 23's own
  named mutant `r23 = strict ? (f.some(isAdvisory) ? 0 : 1) : 0`:

| finding set       | `--strict` | correct | survivor | round 23's `some` mutant |
| ----------------- | ---------- | ------- | -------- | ------------------------ |
| empty             | yes        | **0**   | **1**    | **1**                    |
| empty             | no         | 0       | 0        | 0                        |
| advisory-only     | yes        | 0       | 0        | 0                        |
| advisory-only     | no         | 0       | 0        | 0                        |
| non-advisory-only | yes        | 1       | 1        | 1                        |
| non-advisory-only | no         | 0       | 0        | 0                        |
| mixed             | yes        | 1       | 1        | **0**                    |
| mixed             | no         | 0       | 0        | 0                        |

The survivor is the ordinary transcription of §4's table — rows 1 and 2 both yield 0, so
they get merged, and `length > 0 && every(isAdvisory)` is the wrong merge of row 2's own
words ("only advisory findings") while `every(isAdvisory)` alone is the right one:
`OP23-1`'s guarded-quantifier family one table row up. It agrees with a correct
implementation on every state §6 asserts and differs ONLY on empty-plus-strict, where §5
chains this check as `check:all` member 0 verbatim `npm run check:stale-dist -- --strict`.
Every CLEAN, fully built tree exits **1** there, permanently, and §4's own warrant says a
non-zero exit at index 0 stops the other 14 before any of them prints — `OP18-1`'s failure
INVERTED: not PASS on a broken tree but FAIL on a correct one, forever. ⚠️ **And the
design's own reasoning is the evidence that the case was never checked:** the table above
shows round 23's named mutant returning **1** on the empty set where a correct
implementation returns **0**, so §2's comment, §4's advisory paragraph and row 21's MIX
TWIN cell were all three false where they said it "agrees on zero findings". That mutant is
killed by the MIX TWIN and cannot ship; what did not survive is the claim printed beside
it. Closed by STRENGTHENING THE EXISTING ELEMENT and adding nothing: §6 row 39 — already
bound by the complete-cli-build rule, already running `checkStaleDist`, so a `result` is in
hand — gains ONE conjunct, `exitCodeFor(result, { strict: true })` and
`exitCodeFor(result, { strict: false })` both **0**. It cannot fail a correct implementation
(§4's first exit-table row mandates 0 in both columns, §4 states that coverage never changes
the exit code, and this fixture's `not-run` coverage entries are its whole degradation —
`OP16-3`'s disqualifier), it needs no injection point, and ONE conjunct kills TWO wrong
bodies: the survivor, and round 23's `some` mutant. ⚠️ **Standing rule 8 checked the other
way: it does NOT vacate row 21's MIX TWIN** — a body returning 0 on the mixed set while
correct everywhere else (`some(isAdvisory) ? 0 : (length ? 1 : 0)`) is killed by that twin
alone and by row 39 not at all. Row 39's plan assertion, its rendered `PASS` line by
equality, its two twins, its negative twin's four coverage reasons and its `--json`
invocation are untouched. ⚠️ **The proposal's "this fixture's three `not-run` entries" is
dropped rather than carried (standing rule 3):** that count is derived nowhere in this
design — the row's own cell says `≥1 non-full coverage entry` and its NEGATIVE twin's
meta-checks fixture is the one carrying four — so the un-counted form is written instead.

- **COUNTS THIS ROUND CHANGES, with their sites — exactly THREE, at FIVE sites.** §6's
  `placedCorrectly` mutant expectations, SIX → SEVEN, at the expectation block and at the
  "four of the six mutants are unmeasured" sentence of its own preamble, where the
  unmeasured-by-round-20 subset also moves FOUR → FIVE. §6 row 47's MAIN half mutant
  enumeration, ONE → TWO, at that row's verdict cell and at its TWIN's standing-rule-8
  sentence, and with it the §6 mutant-count census output **10 → 11**, where row 47's twin
  cell PINS its **10** at `e99b066` and is LEFT AS WRITTEN, this file's convention for a
  sha-pinned figure, its claim being scoped to THIS TWIN's count. `OP27-1` moves no number at
  all. **Checked and deliberately NOT moved:** §1's **49** battery rows and its `~680`
  estimate, no row being added and the appended code lines sitting inside that tilde; §1's
  "SIX … FIVE" and §0's "SIX … THREE … FIVE", which become TRUE rather than moving; §6's "SIX
  of the bundler predicate's EIGHT order operands", no conjunct being added; rule 4's live
  member count **ZERO**, `./dist/emit` being SYNTHETIC and never written; §3's **21 of 30**,
  its **20**/**9** split, §0's `29 PATHS` and §8(l)'s **NINE**; §3's Units step's **19 of 19**
  and its EMPTY exception set, that measurement being over LIVE unit `tsconfig.json` files;
  and §4's FOUR exit-table rows, its four carve-outs and its nine mandated reasons — what was
  missing there was the element, not the rule. Round 23's "six mutants" and §9's rounds 23-26
  entries are the record of what those rounds said and are untouched, superseded by this one,
  in round 26's own treatment of round 25's `"./lib"`.
- **SITES, all moved together per standing rule 9.** `CF27-1`: §6's `placedCorrectly` mutant
  block, its preamble count and its mutant-coverage note; §1's five-properties sentence; §0's
  requirement 3 row; §3.3's clause-2 paragraph, whose "both are pinned by §6's mutant-proved
  source-order assertion" is the argument that RESTS on the conjunct. `C27-1`: §0's
  requirement 9 row (the MAIN half's ROLE, and a quotation of cell text this round rewrites);
  §2's `descendantConfigs` comment (the deciding word, the rows-3-and-47 pairing and the
  FIFTH wrong body); §3's rule-4 clause-(ii) statement, its harm example, its
  clause-(ii)-element ⚠️ block, its clause-(iii) decidability paragraph and its element
  paragraph; §3's Units step inside `C21-1`'s **19 of 19** sentence; and §6 row 47's fixture,
  pairing, verdict, twin-opening and standing-rule-8 cells. `OP27-1`: §2's `exitCodeFor`
  comment; the END of §4's advisory-row paragraph, above "The table itself is unchanged" so
  that sentence keeps its referent; §6's rendered-text group bullet, narrowed onto the
  SUBJECT rule it already states; §6 row 21's EXIT-CODE MIX TWIN cell AND its "no
  mutant-formatter row CALLS `exitCodeFor`" enumeration, which row 39's conjunct makes a
  subset at the cell the fix itself creates; §6 row 39; §6 row 43's present-tense direct-call
  enumeration, the same class at the sibling row; and §8(j).

**Round 27's numbers:** THREE distinct uncovered obligations, all dispositioned `fixed`,
against SIX at round 26 and FIVE at round 25. **Obligations created by this round's own
fixes, each with the element that carries it:** exactly TWO — §6 row 47's MAIN half now owes
TWO mutants where it owed one, BOTH named in that same verdict cell and both failing on the
row's own fixture with no injection point; and §6 row 39 now owes an exit code under both
`strict` values, written into that row's own verdict cell, failing on that row's own fixture
with no injection point and unable to fail a correct implementation. `CF27-1`'s fix extends a
value list and creates none. Every other edit is a narrowing of a sentence already on the
page, and a narrowed sentence can never go uncovered. **No working-tree mutation:**
`scripts/bundle-cli.mjs` was never imported, every probe ran `git`, `grep`, `awk`, `sed` or
`python3` read-only over files as text, the exit-code table above was re-derived by `node` on
a scratchpad module importing nothing from this repository, every write went to the
scratchpad, and `git status --porcelain` is empty at `2c203a0` before and after.

**Round 28 (2026-08-22) — `contract-fit` / `correctness` / `operability`, run as the SAME
COVERAGE MATRIX as rounds 18-27 and enumerated INDEPENDENTLY of round 27's list rather than
as a check that its fixes landed. THREE distinct uncovered obligations survived
consolidation — `CF28-1`, `C28-1`, `OP28-1`, one per lens and no two the same defect — all
re-derived read-only at `e70c9bb` on a clean tree and all dispositioned `fixed`. NO battery
row, NO twin, NO fixture row and NO invocation is added, nothing is renumbered, and no kind,
reason spelling, coverage status or exit code is added — so §1's **49** stays correct
(`OP22-3`) and §6's sha-pinned outside-citer registry needs no re-run.** Round 26's
DELETE-FIRST rule stayed in force and every edit is logged against it: `C28-1`'s and
`OP28-1`'s fixes are policy step 3 (STRENGTHEN an existing element by a changed VALUE — a
seventh path on row 8's clean fixture and a SECOND MUTANT on the half that already hosts that
comparison; an exit code on each of two `--json` invocations that already existed), `CF28-1`'s
is policy step 2 throughout (NARROW every sentence that rests on the claim, and convert the
enumeration that hid it into a rule over a SET), and every remaining edit is step 2. Step 4 is
used once, for this entry.

**Enumerations, each derived at this consolidation rather than carried:** `placedCorrectly`'s
SEVEN conjuncts, re-derived against the seven mutants beside them on THREE `mkdir` placements
rather than one — ONE uncovered; §3.4's source-side mandate's TWO halves, the SCOPE and the
MIXED TYPE, against every element that reads `checkPluginAssets` (rows 8 and 24, row 8's
rendered twin, the false-negative battery's `C10-4` conjunction and its §8(p) bullet, the
`walk.mjs` import assertion and the live smoke test) — ONE uncovered; and §4's internal-error
paragraph's THREE mandates on the `--json` throw path against every `--json` INVOCATION in §6,
of which `--json` occurs **16** times and reaches an invocation at exactly FOUR sites — ONE
uncovered. Per-lens document-wide totals are NOT stated, this consolidation having not derived
them, a figure nobody re-derived being `C11-4`'s class (round 25's own precedent).

- **`CF28-1`, low, scope SUBSTANCE — round 27's ALONE claim was measured on ONE `mkdir`
  placement, and this design licenses others.** `CF27-1` closed the clear-before-`build()`
  conjunct with a seventh mutant sinking the clear to immediately before the plugin-copy loop,
  recorded as flipping `CLEAR<BUILD` ALONE "against all THREE conforming source forms". Those
  three vary the WRITE's FORMATTING only — one-line, prettier-wrapped, `metaDir`-factored —
  and all three carry §1's prescribed `mkdir`-after-the-copy-loop placement. §8(k) licenses
  another in terms: "an implementation putting the `mkdir` immediately after the clear violates
  that letter, builds correctly and passes — deliberately". On that source the sunk clear lands
  after the `mkdir` as well as after `build()`, so it flips `CLEAR<MKDIR` too and `CLEAR<BUILD`
  is DELETABLE with all SEVEN mutants still flipping — §6's own definition of vacuous, at the
  conjunct round 27 filed for, un-closed on a form this design says passes. **Re-derived
  read-only 2026-08-22 at `e70c9bb`**, each placement built as a scratchpad copy with
  `scripts/bundle-cli.mjs` read as text and NEVER imported (round 3's disclosure), node
  v24.18.0, nothing running but the shell. The probe is the deletability question asked per
  conjunct — delete it from the predicate, then ask whether all SEVEN mutants still fail — and
  never a call to `placedCorrectly`, which takes ONE argument: `mkdir` AFTER the copy loop,
  §1's prescription, the sink leaves `CLEAR<BUILD` false ALONE and the answer is NO, round
  27's claim reproducing exactly; `mkdir` immediately after the wipe-step clear, §8(k)'s
  licensed form, the sink leaves `CLEAR<BUILD` and `CLEAR<MKDIR` false and the answer is YES;
  `mkdir` between `build()` and the copy loop, a third conforming position the finding did not
  name, identical to the licensed form. ⚠️ **So the boundary is not §1-versus-§8(k), it is
  whether the `mkdir` follows the SINK DESTINATION**, and stating it that way is what makes the
  next `mkdir` position self-classifying instead of owing a fourth measurement. The reachable
  wrong implementation: an implementer on a licensed placement writes the battery, re-derives
  §6's own set rule, finds `CLEAR<BUILD` deletable and drops it as redundant; a later move of
  the `mkdir` down to §1's position then leaves a bundler whose clear sits AFTER `build()`
  passing all seven — round 8's `C8-2`, a surviving metafile naming some OTHER run's inlined
  unit set, which is requirement 3's provenance oracle. Deleting the conjunct was refused for
  round 27's own reason — it falsifies §0's ONE CHAIN sentence and §1's edit list — and
  relocating the mutant cannot close it: an isolating destination needs `BUILD < P < MKDIR`,
  which does not exist where the `mkdir` precedes `build()`. The conjunct stays ASSERTED on
  every conforming form and still fails a bundler whose clear sits after `build()`; what is
  placement-dependent is the PROOF, never the pin. ⚠️ **And the note's closing clause was
  refuted by its own derivation four lines above:** it read "`CLEAR.test`/`WRITE.test` …
  whose deletion mutants have never flipped alone either", while a deleted clear leaves
  `search(CLEAR)` at -1, which SATISFIES both order conjuncts — measured on all three
  placements, delete-CLEAR flips `CLEAR.test` ALONE and delete-WRITE flips `WRITE.test`,
  `MKDIR<WRITE` and `COPY<WRITE` together — so the clause is narrowed to `WRITE.test`, which
  it is true of. **SITES, all moved together per standing rule 9:** §6's
  three-conforming-sources sentence, converted from a list to the quantified conforming set;
  §6's pair-hoist rationale, whose "hoisting the write ALONE flips that conjunct as well" is
  measured FALSE wherever the `mkdir` precedes the loop; §6's mutant-coverage note, at the
  ALONE clause and at the `CLEAR.test` clause; §1's five-properties sentence; §0's requirement
  3 row; §3.3's clause-2 paragraph, the argument that RESTS on the conjunct; and §8(k), where
  the licence is granted and where the cost is now recorded — inside its item (2), so "THREE
  things it still does not establish" does not move.
- **`C28-1`, medium, scope SUBSTANCE — §3.4's source side is mandated over the SIX ENTRIES and
  was asserted only over their TYPES, so the body that walks the PACKAGE passed every element
  in this design.** §2's `newestOfEntries` declaration and §6 row 24 discharge the MIXED-TYPE
  half alone: their stated wrong body is a `newestUnder` applied PER ENTRY, which throws
  `ENOTDIR` on the `.mcp.json` member. A `checkPluginAssets(cwd)` whose source side is
  `newestUnder(join(cwd, "packages/plugin"))` never hands that member to `readdirSync`, never
  throws, and is reached by neither — one call, no mixed-type handling, and `cwd` is the only
  argument §2's signature offers. Rows 8 and 24 both mutate a path INSIDE the six, so both flip
  under the wrong body; the `C10-4` and §8(p) bullets assert `plugin-assets` `full` on fixtures
  where the two readings are equal by construction; `importsAssetEntries(walk)` still passes,
  §3.3's clause 1 needing that same import for its own presence check; and the live smoke test
  asserts the unit SET and the coverage SHAPE and explicitly not the finding set. **The wrong
  result is on this working copy's own shape.** `packages/plugin` holds `src/`, `dist/`,
  `tsconfig.json`, `package.json` and `workflows/` beside the six — five non-members. Measured
  read-only at `e70c9bb`, `git status --porcelain` empty before and after, by a `node` walker
  importing nothing from this repository: `newestOfEntries(six)` = **1787049783306**
  (`packages/plugin/.claude-plugin/marketplace.json`, 2026-08-18T10:43:03Z);
  `newestUnder(packages/plugin)` = **1787158151980**, carried by
  **`packages/plugin/dist/.tsbuildinfo`** (2026-08-19T16:49:11Z);
  `newest(packages/cli/dist/plugin)` = **1787381218597** (2026-08-22T06:46:58Z). BOTH readings
  are quiet today, which is why the live smoke test cannot discriminate either — but the newest
  non-member is the one file §3's `.tsbuildinfo` minus-bullet measures as moving on a
  `touch src/a.ts && tsc -b` that emits nothing, §7 row 1's own scenario. So `npm run typecheck`
  after any edit under `packages/plugin/src`, or a bare `touch` of
  `packages/plugin/workflows/stage-loop.mjs` — a tracked file, one of TWO there today
  (re-derived read-only at `e70c9bb`), copied nowhere by `bundle-cli.mjs` and reached by no
  input rule — puts the wrong body's source side after the last `bundle:cli` copy and prints
  `stale-plugin-assets packages/cli` with §4's `npm run build` remedy at `check:all` member 0,
  exit **1** under `--strict` and under flagless `pretest`, on a tree where not one of the six
  sources changed and the shipped copy provably cannot differ from them. ⚠️ **§3's
  `.tsbuildinfo` minus-bullet does NOT mitigate it:** that bullet is scoped to a unit's OUTPUT
  set and §3.4's source side is not one, so the design's stated mitigation for §7 row 1's
  scenario is silent at exactly this surface. Fixed by STRENGTHENING THE EXISTING ELEMENT and
  adding nothing: §6 row 8 is the host by its own cell, its CLEAN fixture gains ONE path under
  `packages/plugin/` that is not one of the six — the live tree's own non-member, the file
  §3.3's clause 1 and §6's `walk.mjs` comment already name as the drift vector — with an mtime
  NEWER than the newest file beneath `packages/cli/dist/plugin`, and its RETURN-VALUE main half
  gains a SECOND MUTANT, the package-root body, which reports `stale-plugin-assets` at BASELINE
  and so FAILS on the row's own fixture with no injection point (rows 44, 47 and 48's
  discipline). ⚠️ **`OP16-3`'s and `CF20-1`'s disqualifier, checked before the clause was
  written:** `newestOfEntries(PLUGIN_ASSET_ENTRIES)` never reads that path, so a correct
  implementation keeps a clean baseline and a single finding at the END state. ⚠️ **Standing
  rule 8, checked at the fix, four ways:** the added path is outside every unit's `src` and
  `dist` and under no unit the fixture's root `tsconfig.json` references — row 41's and
  `C26-1`'s own wording, copied WHOLE (standing rule 7) — so no unit, no stem and no output-set
  member is introduced and §3.1 is untouched; it is spelled as a plain file under a NON-MEMBER
  DIRECTORY and never as `packages/plugin/dist/.tsbuildinfo`, which would make the fixture's
  `packages/plugin` a unit and move the row's unit counts; the row still asserts no `full`
  `plugin-assets` COVERAGE entry, so the complete-cli-build rule's plugin-SOURCE clause — keyed
  on what a row ASSERTS — still does not reach it and row 24's "its fixture need not contain the
  file member at all" survives verbatim, which is the collision round 26 refused the row-list
  form for; and the clause goes on row 8 ALONE, so row 8's RENDERED TWIN keeps its N=1 equality
  on `check-stale-dist: stale-plugin-assets ×1 — packages/cli` and no other row's fixture,
  mutant or verdict moves. **SITES:** §6 row 8's clean-fixture cell and both halves of its
  verdict cell — including the RENDERED TWIN's closing "the return-value main half is
  UNCHANGED", a present-tense claim about round 20's own append that this round's main-half
  clause falsifies (`C11-4`'s class at a state description); §6's `CF21-1` group bullet, whose
  "Rows 8 and 38 … are UNCHANGED" is the same present tense one bullet out; §6's
  complete-cli-build rule, where "row 8's own `plugin assets fresh` shape copied WHOLE" is
  narrowed to the bound over the SIX ENTRIES so a fixture author copying it does not copy the
  seventh path; §3.4's ⚠️ source-side clause and the tail of its mixed-type paragraph, which
  now name each half's element; and §2's `newestOfEntries` declaration comment, whose
  "`newestUnder(dir, …)` cannot do it" argument is about the PER-ENTRY application only and now
  says so.
- **`OP28-1`, medium, scope SUBSTANCE — the exit code on the `--json` internal-error path was
  mandated at TWO sites and asserted by NO element.** §1's `check-stale-dist.mjs` cell and §4's
  internal-error paragraph both mandate the catch's pair on the machine path — §4 in terms,
  "the exit code is unchanged (**0** non-strict, **2** strict)" — and re-derived read-only at
  `e70c9bb`, not one of §6's four `--json` invocations read an exit code, while
  `grep -c -- '--json --strict'` and `grep -c -- '--strict --json'` over the whole file each
  returned **zero**, so no element anywhere ran the two flags together. Two fixed-string
  searches and never one alternation, the escape an alternation needs rendering as a bare pipe
  once this file is read as markdown, so that the command a reader copies back would match
  neither spelling — standing rule 1 applied to the command an absence claim is reported with.
  Row 27's third invocation is explicitly flagless and asserts only that stdout `JSON.parse`s to
  `{ internalError, ran: false }` with the path inside `internalError` and neither human line;
  its `C18-5` twin's third asserted the ROOT path in that object; row 39's fourth deep-equals
  `coverage`; row 43's CLI twin's third deep-equals `findings` and `states`, and that row states
  the exclusion in terms — "the EXIT-CODE PAIR is what discriminates, and the `--json`
  invocation above stands beside it rather than in it". Row 27's own mutant, "no `try`/`catch`",
  is scored as failing both HUMAN halves. So a catch spelled
  `const out = formatInternalError(err, { strict, json }); if (json) { console.log(JSON.stringify(out)); process.exit(0) }`
  — the ordinary "machine consumers get a clean stream and a clean code" branch, and `CF19-1`'s
  own shape (a flag parsed for the formatter and then not threaded to the exit) moved from the
  success path to the catch — passed rows 27, its twin, 31, 39 and 43, every other element in
  §6, the false-negative battery, the wiring assertions and the live smoke test, while
  `npm run check:stale-dist -- --strict --json` on a malformed `tsconfig.json` exited **0**
  where §1 and §4 both mandate **2**: a consumer reads `ran: false` off a stream that reported
  success, verbatim §4's own warrant that "your dist is stale" and "the check broke" must not
  share an exit code, at a code this design introduces as a NEW convention (re-derived read-only
  at `e70c9bb`, `grep -rn "exit(2)" scripts/` returns **zero** hits today). Same class as
  `OP18-7`, `OP22-1` and `OP24-2`, each of which closed a latent `--json` clause on §4's own
  "stated now rather than after a consumer exists" reasoning; this was the last `--json` clause
  with no element, and the exit code is the ONLY thing that can differ between the two `--json`
  internal-error invocations, §4 mandating one object for both. Closed by STRENGTHENING two
  invocations that already exist: row 27's third gains exit **0**, and its `C18-5` twin's third
  becomes `--json --strict` and gains exit **2**. The PAIR is what discriminates, in row 43's
  CLI twin's own stated shape — a flagless half alone is satisfied by a catch that always exits
  **0** under `--json`, a strict half alone by one that always exits **2** — and each half has a
  mutant that flips it ALONE, which is round 27's quantified pairing rule applied at the
  conjunct as it is written rather than a round later. ⚠️ **Standing rule 8, checked AT the
  fix:** §4 mandates ONE object for both flag settings with no `strict`-varying member, so the
  twin's ROOT-path-inside-`internalError` assertion and row 27's `{ internalError, ran: false }`
  and neither-human-line assertions hold byte-identically under `--strict`; nothing either row
  already pins is vacated, and row 27's `no try/catch` mutant still fails both human halves.
  **SITES:** §4's internal-error `--json` paragraph, re-pointed off "asserts all three
  invocations, including that the `--json` one parses" and stated as a SET RULE over `--json`
  invocations on THROWING fixtures rather than as a list of the two that carry it — round 27's
  structural lesson, scoped so it creates no obligation on row 39's or row 43's completing
  fixtures; §1's `check-stale-dist.mjs` cell, which gains the element pointer beside "exits
  **2** under `--strict`, **0** without" in the shape its own `CF19-1` clause already uses for
  the success path; §2's `formatInternalError` comment, which stated where exit **2** comes from
  and named an element for the IMPORT RULE and none for the code; §6 row 27's verdict cell at
  BOTH halves; and §6 row 43's CLI twin, whose "in row 27's invocation shape" citation now names
  two shapes and is narrowed to the MAIN half (`C20-2`'s rule).

**Round 28's numbers:** THREE distinct uncovered obligations, all dispositioned `fixed`,
against THREE at round 27, SIX at round 26 and FIVE at round 25 — the figures read off those
rounds' own entries above rather than carried. **Obligations created by this round's own
fixes, each with the element that carries it:** exactly THREE, and every one of them sits on a
fixture or an invocation that already existed — §6 row 8's main half now owes a SECOND MUTANT,
failing on that row's own fixture with no injection point; row 27's third invocation owes exit
**0**; and that row's `C18-5` twin's third owes exit **2**, both written into row 27's own
verdict cell, both failing on that row's own fixtures with no injection point, and none of the
three able to fail a correct implementation (`OP16-3`'s disqualifier, checked at each).
`CF28-1`'s fix creates none, being a narrowing throughout, and a narrowed sentence can never go
uncovered. **Counts:** none moved, at no site — §1's **49** battery rows and its `~680`
estimate, no row and no twin being added (`OP22-3`); §6's SEVEN conjuncts and SEVEN mutants and
its "SIX of the bundler predicate's EIGHT order operands"; §0's "SIX … THREE … FIVE" and §1's
"SIX source properties … **FIVE**", no source property or bundler edit being added; §8(k)'s
THREE, this round's residual going INSIDE its item (2); §3's **21 of 30**, its **20**/**9**
split, §0's `29 PATHS` and §8(l)'s **NINE**, no config being added; rule 4's live member count
**ZERO**; §3's Units step's **19 of 19**; §4's FOUR exit-table rows, its four carve-outs and
its nine mandated reasons; row 27's three invocations, its twin's three, row 39's four and row
43's CLI twin's three; and §6's sha-pinned outside-citer registry, nothing being renumbered and
rows 8, 24 and 27 already being members of the set its command returns. ⚠️ **The §6
mutant-count census was re-derived under §9's round-26 command at `e70c9bb` and this round's
apply adds ZERO hits to it** — row 8's clause is spelled `SECOND MUTANT`, in row 41's own shape
(standing rule 7), row 27's is `Mutant for the PAIR`, and `SEVEN mutants` matches no alternative
of that command's pattern — so row 47's twin's sha-pinned **10** at `e99b066` is unread by this
apply and is left as the measurement it is. **No working-tree mutation:**
`scripts/bundle-cli.mjs` was never imported, every probe ran `git`, `ls`, `grep`, `sed`,
`python3` or `node` read-only over files as text or `stat`ed them, the three `mkdir` placements
and the three mtime figures were derived by scratchpad modules importing nothing from this
repository, every write went to the scratchpad, and `git status --porcelain` is empty at
`e70c9bb` before and after.

**Round 29 (2026-08-22) — `contract-fit` / `correctness` / `operability`, run as the SAME
COVERAGE MATRIX as rounds 18-28 and enumerated INDEPENDENTLY of round 28's list rather than as
a check that its fixes landed. SEVEN distinct uncovered obligations survived consolidation —
`CF29-1`, `CF29-2`, `CF29-3`, `C29-1`, `OP29-1`, `OP29-2`, `OP29-3`, no two the same defect —
all re-derived read-only at `c672565` on a clean tree and all dispositioned `fixed`. NO battery
row, NO twin and NO fixture row is added, nothing is renumbered, and no kind, reason spelling,
coverage status or exit code is added — so §1's **49** stays correct (`OP22-3`) and §6's
sha-pinned outside-citer registry needs no re-run, rows 2, 26, 43 and 45 already being members
of the set its command returns.** Round 26's DELETE-FIRST rule stayed in force and every edit
is logged against it: `CF29-2`'s fix is policy step 3 (STRENGTHEN two existing elements by a
changed VALUE — an anchor on each of two regexes, no new element and no new row) with policy
step 2 on every sentence that rests on them; `CF29-1`'s, `C29-1`'s and `OP29-3`'s are policy
step 2 throughout; `OP29-1`'s is policy step 2 at four sites and step 3 at one; `OP29-2`'s is
step 3 at row 43's CLI twin and step 2 at every sentence resting on it; `CF29-3`'s is step 4,
taken only after 1-3 were checked and refused, and it adds exactly ONE element. Step 4 is used
a second time, for this entry.

**The class this round hunted, stated once:** every MEASUREMENT in this design whose SCOPE is
narrower than the CLAIM resting on it — the research record's STANDING RULE 1 applied to
measurements rather than to searches, which is what rounds 27 and 28 each found one level
apart. All seven findings are that class and no other: a re-derivation run over ONE source
standing for the conforming SET (`CF29-2`); a command run over TRACKED FILES standing for a
claim about HOOKS (`CF29-1`); a mutant battery derived per NAME standing for a predicate
quantified per CONJUNCT (`CF29-3`); three occurrence greps run at ONE commit standing for the
document as it now stands (`C29-1`); a count taken over the gaps-NAMED rows standing for a
three-row reason table (`OP29-1`); a rule scoped to THROWING fixtures standing for a sentence
quantified over EVERY path (`OP29-2`); and a `tsc -b` measurement standing for a three-step
build over all nineteen units (`OP29-3`).

- **`CF29-1`, medium, scope SUBSTANCE — the load-bearing claim was attributed ENTIRELY to a
  command that can produce ZERO evidence for half of it.** §5's measurement comment read "the
  load-bearing claim — that no workflow and no hook invokes `check:all` — is carried entirely
  by the 6", and the 6 come from `git ls-files -z | xargs -0 grep -n "run check:all"`.
  `git ls-files` enumerates files TRACKED IN THIS REPOSITORY and a git hook is never one.
  Re-derived read-only at `c672565`, `git status --porcelain` empty before and after:
  `git config core.hooksPath` → `/home/eimi/.codex/git-hooks`, a directory OUTSIDE the
  repository holding `pre-commit`, `pre-push` and `pre-push.bak`; `git ls-files | grep -i hook`
  returns **17** paths — 14 `.ts`/`.mjs` SOURCES (6 `.mjs` plus 8 `.ts`) and 3 `.json` — and
  `git ls-files | grep -iE '(^|/)(pre-commit|pre-push|commit-msg)$'` returns **0**, so not one
  of them is a hook SCRIPT (⚠️ the finding reported this population as "nine `.ts`/`.mjs`
  source files"; re-derived it is 17 and 14, standing rules 2 and 3 — the conclusion is
  unchanged and the command is written beside the figure). Add `npm run check:all` to that
  `pre-commit` and the command still returns exactly **6** while the sentence it is said to
  carry goes FALSE — and with it §7 row 1's owner-accepted pricing (up to 19 `rm -rf` plus a
  rebuild, accepted BECAUSE no push is blocked) and §4's exit-2 warrant, the eight sites
  `OP17-1` repointed onto "no run of it can block a push". The hook half is in fact carried by
  this same section's `core.hooksPath` bullet and by round 17's own probe, both re-derived
  unchanged today — `/home/eimi/.codex/git-hooks/pre-push:79` is
  `for script_name in lint typecheck build test; do`, `grep -c check:all` on it returns **0**,
  and `grep -c 'npm run'` on `pre-commit` returns **0** — and it is a PER-CLONE fact about this
  machine rather than a property of this repository, which is the second thing "entirely by the
  6" hid. Round 25's `CF25-3` re-derived the same 6 and drew only the WORKFLOW conclusion from
  it, which is the half the command supports. Fixed by splitting the attribution and adding no
  measurement. ⚠️ **`C11-4`'s drift is NOT re-triggered by the added text**, both commands
  re-derived at `c672565` and both unmoved at **6** and **9**: this design file is excluded from
  the first BY CONSTRUCTION (`grep -v stale-dist-check-design`) and filtered out of the second
  by `grep -v ^docs/`. **SITES:** §5's "Measured:" lead-in, which introduced BOTH commands as
  evidence for both halves, and §5's attribution comment. The eight `OP17-1` sites are checked
  and NOT moved: each cites §5's measured bound and §5's `core.hooksPath` bullet, both of which
  stand.
- **`CF29-2`, high, scope SUBSTANCE — the two assertions whose whole subject is SCOPE were
  themselves wider than their claim, and the regex failed in BOTH directions.** §6 says both
  reads are "SCOPED TO THE IMPORT STATEMENT, never to the whole file, and that is not
  cosmetic", predicting that a conforming `units.mjs` will NAME `enumerateWorkspaces` in a
  comment. The regex did not scope to the statement: `[^;]+` spans newlines, so the match
  begins at the EARLIEST `import` SUBSTRING in the semicolon-free span reaching the target
  `from`. ⚠️ **The MEASUREMENT AND ITS SCOPE, together, because stating one without the other
  is the defect this round is about:** re-derived read-only at `c672565`, node v24.18.0,
  `scripts/*` read as text and never imported, clean tree before and after, over SIX conforming
  sources chosen to span the property that discriminates them — whether a comment ahead of the
  statement carries the substring `import`, and whether it does so AHEAD of the name.
  **(a) It FAILED A CORRECT IMPLEMENTATION** on the THREE that carry `import` ahead of
  `enumerateWorkspaces`: a one-line comment above the statement, the same with a preceding
  `import { join } from "node:path";` (whose `;` does NOT protect it, the comment's own
  `import` starting a fresh span), and a JSDoc block of that shape — all three importing
  exactly the two mandated names and nothing else, all three returning `false` from
  `enumeratesReferences`, so `expect(enumeratesReferences(units)).toBe(true)` fails: verbatim
  the failure the paragraph above it was written to avoid, `OP16-3`'s and `CF20-1`'s
  disqualifier at the element that names it. The bare statement and the two comment forms
  carrying no such substring returned `true` even unanchored, which is why the PROPERTY and not
  the mere presence of a comment is what the fix writes down. **(b) It ADMITTED THE VACUITY IT
  EXISTS TO CATCH**, at the sibling predicate: `importsAssetEntries` returned TRUE on a
  `walk.mjs` whose comment reads `// We import the dep set here, never PLUGIN_ASSET_ENTRIES`
  above `import { EXTERNAL_DEPENDENCIES } from "../bundle-cli.mjs";` with the six-element
  literal declared below — §3.3 clause 1's forbidden implementation, green at every element in
  this design, `packages/plugin/workflows/` never firing `stale-plugin-assets`: `CF15-4`'s own
  diagnosis, re-opened by `CF15-4`'s own fix. **The scope gap entered at round 15's
  re-derivation**, recorded in `CF15-1` as run "against the conforming statement" — ONE bare
  statement with no comment above it, standing for a claim quantified over every conforming
  `units.mjs`. Fixed by anchoring both regexes to the line start —
  `/^import[^;]+from\s+["'][^"']*repo-census\.mjs["']/m` and the same for `bundle-cli.mjs` —
  which returns TRUE on all SIX conforming shapes, FALSE on (b)'s `walk.mjs`, and leaves both
  standing census mutants and the `LITERAL` mutant flipping, all re-derived. NARROW was refused
  as the remedy: restating the sentence as "the semicolon-free span before the import" is true
  and clears NEITHER half. ⚠️ **`CF15-1` checked AT the fix by MEASUREMENT and not by
  reasoning:** `./node_modules/.bin/prettier --check` 3.9.5 reports this file clean today AND
  reports the anchored candidate clean, and `prettier --write` on a scratchpad copy leaves both
  regex lines byte-identical — `^` and `m` introduce no `*`, so the emphasis mangling that
  vacated this same predicate in round 15 cannot re-open; `semi: true` (`.prettierrc.json`)
  bounds the span at the previous `;` and prettier never indents a top-level ESM import, so `^`
  cannot fail a formatted source. **SITES, all moved together per standing rule 9:** §6's
  `censusImport` and `bundlerImport` consts; §6's census SCOPED bullet, where the anchor's
  rationale and the six-source measurement basis are stated AT the measurement; §6's `walk.mjs`
  SCOPED sentence, which pointed at the census reason and needed the opposite DIRECTION of
  failure recorded; §1's two-repo-internal-imports paragraph, at both mentions of the read;
  §0's requirement 5 row and requirement 11 row; §3.3's clause 1; §8(k)'s round-15 sub-bullet,
  whose residual was an understatement of the hole rather than the whole of it; and §9's
  round-15 `CF15-1` entry, narrowed to the ONE-source scope it actually had.
- **`CF29-3`, low, scope SUBSTANCE — the census predicate has THREE conjuncts and TWO mutants,
  and the deletable one is requirement 5's whole prohibition.** §6's comment says "TWO mutants,
  one per name" and §0's requirement 11 row restates it. Re-derived read-only at `c672565` with
  `!censusImport(src).includes("enumerateWorkspaces")` REMOVED from the predicate: mutant 1
  (`enumerateRootReferences`→`enumerateWorkspaces`, global) → false, mutant 2
  (`enumerateTsconfigs`→`listConfigs`) → false. **Both still flip**, so the negated conjunct is
  DELETABLE — this section's own definition of vacuous (`CF27-1`'s set rule, applied per
  CONJUNCT rather than per NAME). Mutant 1 cannot isolate it because it REMOVES
  `enumerateRootReferences` in the same edit; mutant 2 never mentions the forbidden name. An
  implementer who drops the conjunct passes the whole battery, and a `units.mjs` importing all
  three names is then unseen at the ONLY source-level element requirement 5 has. Policies 1-3
  were checked and refused: DELETE loses the only element that sees the forbidden import at
  all; NARROW does not apply, the sentence being a count; STRENGTHENING mutant 1's VALUE to
  isolate the conjunct re-opens positive conjunct 1, which is standing rule 8 at the fix.
  **NEW OBLIGATION AND ITS ELEMENT:** a `units.mjs` importing BOTH mandated names AND
  `enumerateWorkspaces` must be REJECTED — one added expectation in the same wiring block, on
  the `units` read that already exists, with no injection point. ⚠️ **It is applied THROUGH
  `censusImport` and never to the whole file**, in `walk.replace(bundlerImport(walk), LITERAL)`'s
  own shape (standing rule 7): a bare `units.replace("enumerateTsconfigs", …)` lands on the
  FIRST occurrence, which in a `units.mjs` whose comment names that function above the import
  is OUTSIDE the statement, leaving the predicate TRUE and FAILING A CORRECT IMPLEMENTATION —
  measured, and the reason the naive form was refused. ⚠️ **NON-VACUITY, measured at
  `c672565`:** baseline true on every conforming shape; all three mutants false; and with the
  negated conjunct DELETED the third returns TRUE while 1 and 2 still return false, so the
  conjunct is no longer deletable. ⚠️ **`OP16-3`'s disqualifier checked AT the fix:** the
  scoped form returns FALSE on every conforming shape measured and TRUE on none.
  **SITES:** §6's census mutant comment and expectation block; §0's requirement 11 row, which
  carries the count; and §0's requirement 5 row, whose prohibition this conjunct is.
- **`C29-1`, medium, scope SUBSTANCE — three sole-element occurrence claims measure the
  document at ONE commit and quantify over the document as it now stands.** Each carries the
  "nothing else in the battery discriminates it" half of an element that is the only element its
  requirement has: (1) round 16's `grep -n stemsUnder`, live at §0's requirement 2 row, at §3.1
  and inside `C16-1`'s entry above; (2) `inlinedUnitsFromMetafile` "in §6 only at row 45", live
  at §3.2 and at row 45's own cell; (3) `extends` "in §6 only in this row", live at row 2. All
  five sites are the SET form `CF17-2` introduced in place of counts, on that bullet's claim
  that a SET "is scale-free and survives the next append". **It did not.** Re-derived read-only
  at `c672565`, each §6 slice parsed between the `## 6.` and `## 7.` headings at EVERY commit
  that touches this file rather than at a sample: `stemsUnder` in §6 is {} at `db857c3`,
  **{14,44}** at `badf45a` — so this one was false in the commit that PRINTED it, `CF17-2`'s own
  defect at the claim `CF17-2` did not reach — and {4,14,44} from `7dfa1ba` to `c672565`,
  THIRTEEN commits in all; `inlinedUnitsFromMetafile` is {45} at `badf45a`, {42,45} from
  `3095116`, {40,42,45} from `2c203a0`; `extends` is {2} at `badf45a`, {2,47,48} at `896dd84` —
  the commit immediately after the one that wrote the SET form, falsified by rows that same
  change set added — {2,3,47,48} at `7dfa1ba` and {2,3,47,48,49} from `5fef126`, false in all
  TWELVE commits from `896dd84` through `c672565`. The `stemsUnder` claim's other half is stale
  too: §2 carries FOUR lines naming it today against the two at `db857c3`. **The reachable wrong
  result is `CF17-2`'s own:** a reader auditing the coverage matrix runs the stated grep, gets
  three or five rows back, and cannot tell from the sentence which one is the element — so the
  audit that would catch a later round vacating the real element fails on the bookkeeping
  instead. In each case the substantive conclusion holds and is what the narrowing now says:
  row 45 alone ASSERTS §3.2's derivation of the inlined set from the metafile, row 40's mention
  sitting in its twin's fixture-construction clause about the marker's `inputs` ORDER (`C26-2`)
  and row 42's in a fixture-conformance argument about `C15-2`'s rule; row 2 alone pins a
  MULTI-unit `stale-unit` set by EQUALITY, rows 47 and 49 reading `EQUAL to the single Unit.dir
packages/A`, row 48 `EQUAL to that unit's Unit.dir`, and row 3's verdict being
  `stale-declarations` and explicitly NOT `stale-unit` (`C14-3`) with its round-18 twin
  quantifying over the units its fixture holds; and rows 4, 14 and 44 all landed at or after
  round 16. **SITES, all moved together per standing rule 9:** §0's requirement 2 row; §3.1's
  scratch-exclusion paragraph; `C16-1`'s entry above, the third site of that claim and the one
  `CF17-2` never reached; §3.2's derivation paragraph; §6 row 45's verdict cell; §6 row 2's
  verdict cell; and `CF17-2`'s own disposition, where the "survives the next append" licence is
  corrected to the rule — a document-wide occurrence claim carries the sha it was run at, or it
  names the PROPERTY that distinguishes the element, and never the occurrence set.
- **`OP29-1`, medium, scope SUBSTANCE — §4 licensed `packages/cli`'s gap list on EVERY
  `unbuilt` line, and one of §3.3's three cli reason rows has an EMPTY one.** §2's THIRD cli
  branch is `gaps.length === 0 && the marker FILE exists … and its mtime is OLDER than the
newest §3-filtered output`, so clause 2's reason is the whole of that line; §4's grouping
  paragraph said "plus `packages/cli`'s gap list" unqualified. An implementation with ONE
  composition point — `<reason>` + `"; missing: " + gaps.join(", ")` — prints a bare trailing
  container on §6 row 26's own fixture (row 19's end state), which §7 row 2 names as a CORRECT
  firing nobody may mute, at `check:all` member 0, exit **1** under `--strict` and at line 5 of
  §5.1's measured 204 under `pretest`, and it passes EVERY element: re-derived read-only at
  `c672565`, `tsc -b has overwritten the bundled output` occurs at §3.3's reason table and §4's
  line list and **zero** times inside §6, while all **FIVE** `missing:` occurrences inside §6
  carry NON-EMPTY gap lists. ⚠️ **The clause-2 fixture set was re-derived rather than carried,
  and the finding's own enumeration was wrong:** it named rows 19, 26, 37 and 46; row 46's
  fixture is `skipped` and NOT `unbuilt` by §3.3's reason table, so it is no clause-2 fixture at
  all. The set is rows **19**, **26** and **37** — 19 and 37 return-value rows round 19
  deliberately did not move into the rendered group, 26 the one already in it, with the coverage
  line as its only rendered assertion. FOURTH site of the bare-container class already guarded
  at three (`OP25-2`, `OP26-1`, `OP16-4`). ⚠️ **The design's own bookkeeping is why it was
  invisible:** §6's group bullet called row 20's line "the second of the two sentences §3.3's cli
  reason table mandates" — a count taken over the two gaps-NAMED rows standing in for a table
  that mandates THREE — so the enumeration of which cli `unbuilt` sentences are rendered could
  not see the one that was not. **SITES, all moved together per standing rule 9:** §4's grouping
  paragraph, NARROWED to the non-empty branch; §6's group bullet at BOTH the row-20-twin
  sentence and the per-row enumeration of what each row asserts; §2's `cliArtifactGaps` branch
  comment, whose "TWO of them" attributed a row-shape count to the table; and §6 row 26's MAIN
  half, STRENGTHENED with a whole-line SUFFIX conjunct in row 38's and row 27's negative-clause
  shape, killed by the unconditional composition and by nothing else, passing on row 20's
  RENDERED TWIN and row 32 whose gap lists are non-empty, invisible to rows 19 and 37 whose
  verdicts assert the clause-2 REASON by containment and not the rendered line, and provably
  unable to fail a correct implementation because §4's bullet ends that line exactly there
  (`OP16-3`'s disqualifier).
- **`OP29-2`, medium, scope SUBSTANCE — round 28 closed the `--json` exit code on the CATCH
  path and §4 quantifies over BOTH.** §6 row 43's CLI twin is the only element running the
  process on a COMPLETING tree carrying a finding, and its `--json` invocation asserted deep
  equality and no exit code at all. A success path spelled
  `if (json) { console.log(JSON.stringify(result)); process.exit(0) }` before
  `process.exit(exitCodeFor(result, { strict }))` — §1's own named "machine consumers get a
  clean stream and a clean code" branch, `CF19-1`'s shape at the sibling of the path `OP28-1`
  closed — passes all three of that twin's invocations, row 39's fourth, rows 27 and 31 and row
  27's `C18-5` twin (all THROWING, all inside round 28's rule), row 21's four direct calls and
  its EXIT-CODE MIX TWIN, the wiring assertions and the live smoke test — and exits **0** on
  `npm run check:stale-dist -- --strict --json` against a stale tree. Closed by STRENGTHENING
  the twin: exit **0** on its FLAGLESS `--json`, and a FOURTH invocation, `--json --strict`, at
  exit **1**, asserting the code and nothing else so the TWO live citations elsewhere to "its
  `--json` invocation" still name the third (`C20-2`'s rule discharged AT the fix; §4's
  `e70c9bb` figure and §9's round-28 entry already spell it "third"). Each half has a mutant
  that flips it ALONE, and neither can fail a correct implementation: §4's exit table gives
  **0**/**1** under the two `strict` values with no `--json` carve-out. ⚠️ **Round 28's stated
  reason for excluding row 39's fourth — "COMPLETING rather than throwing" — is corrected only
  as far as it is measurable:** that row's fixture holds ZERO findings, so **0** is correct under
  either reading and `OP27-1` already pins the pair, and §6's group bullet lists row 39 among the
  rows whose single mutation is a MUTANT FORMATTER. ⚠️ **Standing rule 8, checked AT the fix:**
  §4 mandates ONE serialisation for both settings with no `strict`-varying member, so the
  `findings` and `states` deep-equalities and the rendered grouped line hold byte-identically.
  **SITES:** §4's `--json`-is-unaffected sentence; §4's internal-error paragraph; §1's
  `check-stale-dist.mjs` cell, whose success-path clause mandates the ARGUMENT and said nothing
  about whether the call is REACHED; §2's `exitCodeFor` comment; §6 row 43's CLI twin at three
  places — its invocation set, its EXIT-CODE PAIR sentence and its closing standing-rule-8
  sentence; and ⚠️ **§6's rendered-text group bullet at TWO further sites the lens set did not
  reach**, where the same enumeration reads "NO flag, with `--strict` and … with `--json`" and
  "at THREE flag settings". Both are moved to the four-member SET in this same pass, without
  which the count would stand at three in the very section that carries the fourth — standing
  rule 9, found by this consolidation rather than by the lens.
- **`OP29-3`, low, scope SUBSTANCE — a `tsc -b` measurement over one unit class carried as a
  claim about a three-step build over all nineteen.** §4's remedy `unbuilt` row read "plain
  `npm run build` clears it only when `.tsbuildinfo` went with the outputs", measured over
  `tsc -b`. Re-derived read-only at `c672565`, `scripts/bundle-cli.mjs` read as TEXT and never
  imported: `package.json`'s `build` is `tsc -b && npm run bundle:types && npm run bundle:cli`,
  and `bundle-cli.mjs:113` is `const KEEP = new Set([".tsbuildinfo"]);` followed by an `rm` of
  every other `dist` entry and a full rewrite of every clause-1 member below — so plain
  `npm run build` clears `unbuilt packages/cli` in ALL THREE cli branches and the "only when" is
  false for it. §4 states the opposite fact correctly at step 1's fourth carve-out, four hundred
  lines below; the site an operator and an implementer read is the remedy table, which this
  section calls the ordered plan's SOURCE. Round 1's `O-1` generalised in this table a second
  time, after `C14-2` did it at the `stale-declarations` row. NARROWED to the 18 tsc-only units
  with the carve-out CITED rather than restated (`CF6-2`), and the cell says in terms that
  narrowing the justification does not widen that carve-out to this kind — the exemption is by
  KIND and by UNIT, `unbuilt packages/cli` keeps step 1, and the PRINTED plan is unchanged. NO
  element is owed and none is added. **SITES:** §4's remedy table `unbuilt` row alone.

**Round 29's numbers:** SEVEN distinct uncovered obligations, all dispositioned `fixed`,
against THREE at round 28, THREE at round 27, SIX at round 26 and FIVE at round 25 — the
figures read off those rounds' own entries above rather than carried. Per-lens document-wide
totals are NOT stated, this consolidation having not derived them, a figure nobody re-derived
being `C11-4`'s class. **Obligations created by this round's own fixes, each with the element
that carries it:** exactly THREE, and every one sits on a fixture that already existed — §6's
census predicate owes a THIRD mutant, written into that same wiring block on the `units` read
that already exists; §6 row 26's MAIN half owes a whole-line SUFFIX on the clause-2 `unbuilt`
line, failing on that row's own fixture; and §6 row 43's CLI twin owes an exit code on each of
its two `--json` invocations, on that twin's own end state. None needs an injection point, each
has a mutant flipping it ALONE, and none can fail a correct implementation (`OP16-3`'s
disqualifier, checked at each). `CF29-1`'s, `C29-1`'s and `OP29-3`'s fixes create none, being
narrowings throughout; `CF29-2`'s creates none, changing the VALUE of two regexes that already
carry their elements; and a narrowed sentence can never go uncovered. **Counts moved, with
their sites:** the census predicate's mutant count TWO → THREE (§6's wiring comment, §0's
requirement 11 row); §6's group bullet and §2's `cliArtifactGaps` branch from `TWO sentences
the table mandates` to `TWO of its THREE`, stated by row shape; row 43's CLI twin's `THREE
times` to the four-member flag SET (`OP22-3`'s form), at the twin and at §6's group bullet's
two further sites; and `stemsUnder` in §6 from NOTHING to rows 4, 14 and 44 (three sites),
`inlinedUnitsFromMetafile` from row 45 alone to rows 40, 42 and 45 with row 45 the only
assertion (two sites), and `extends` from row 2 alone to rows 2, 3, 47, 48 and 49 with row 2
the only multi-unit equality (one site). **Counts NOT moved:** §1's **49** battery rows and its
`~680` estimate, no row and no twin being added (`OP22-3`) and the appended code lines sitting
inside that tilde; §5's **6** and **9**, both re-derived at `c672565` and both unmoved; §6's
SEVEN `placedCorrectly` conjuncts and SEVEN mutants and its "SIX of the bundler predicate's
EIGHT order operands"; §0's "SIX … THREE … FIVE" and §1's "SIX source properties … **FIVE**";
§8(k)'s THREE, this round's §8 edit narrowing a sentence inside an existing subject; §3's
**21 of 30**, its **20**/**9** split, §0's `29 PATHS` and §8(l)'s **NINE**; rule 4's live member
count **ZERO**; §3's Units step's **19 of 19**; §4's FOUR exit-table rows, its four carve-outs,
its EIGHT remedy rows and its nine mandated reasons; and row 27's three invocations and its
twin's three. §4's `e70c9bb` four-`--json`-site figure and every earlier round's figures are
sha-pinned MEASUREMENTS and are left as the measurements they are. ⚠️ **The §6 mutant-count
census was re-derived under §9's round-26 command at `c672565`: 13 hits before this apply and
13 after** — the wiring block's single hit changes VALUE from `TWO mutants` to `THREE mutants`
(the distribution going from 12×`TWO`/1×`THREE` to 11×`TWO`/2×`THREE`), and no other edit in
this set writes a numeral immediately before `mutant`/`mutants` — so row 47's twin's sha-pinned
**10** at `e99b066` is unread by this apply and is left as the measurement it is. **No
working-tree mutation:** `scripts/bundle-cli.mjs` was never imported and was read as TEXT for
its KEEP set and its rewrite sequence; every probe ran `git`, `ls`, `grep`, `awk`, `sed`,
`python3` or `node` read-only over files as text; every §6 slice was re-derived from
`git show <sha>:<file>` piped through `awk` rather than from a checkout; the conforming
`units.mjs` shapes and the `walk.mjs` counterexample were built as scratchpad strings inside
modules importing nothing from this repository; the prettier proof ran `--check` on this file
and `--check`/`--write` on a SCRATCHPAD copy only; every write went to the scratchpad; and
`git status --porcelain` is empty at `c672565` before and after.

**Round 30 (2026-08-22) — `contract-fit` / `correctness` / `operability`, all three run as the
SAME COVERAGE MATRIX as rounds 18-29 and all three enumerated INDEPENDENTLY of round 29's list
rather than as a check that its fixes landed. `contract-fit` took the ELEVEN requirements of §0
and every wiring claim of §5, **54** testable obligations, THREE uncovered; `correctness` took
§2's exported signatures and §3-§3.5's comparison rules, **86**, ONE uncovered; `operability`
took §4's printed surface — its FOUR exit-table rows, its nine mandated reason sentences, its
printed line shapes, its `PASS` and coverage lines, its three internal-error surfaces (`WARN`,
`ERROR`, the `--json` object), its EIGHT remedy rows and ordered-recipe steps 1-5 with all four
carve-outs — plus §7's thirteen rows and §5.1's five bypass members and its 204/line-**5**
placement, **60**, THREE uncovered. Each obligation was read off the document as it now stands
and each matched to the §6 element a plausible wrong implementation would fail, with an element
a plausible WRONG implementation also passes counted as no coverage and an element that would
FAIL a CORRECT one counted as a defect of its own. SEVEN distinct uncovered obligations survived
consolidation — `CF30-1`, `CF30-2`, `CF30-3`, `C30-1`, `OP30-1`, `OP30-2`, `OP30-3`, no two the
same defect — all re-derived read-only at `187eb2d` on a clean tree and all dispositioned
`fixed`. NO battery row, NO twin and NO fixture row is
added, nothing is renumbered, and no kind, reason spelling, coverage status or exit code is
added — so §1's **49** stays correct (`OP22-3`) and §6's sha-pinned outside-citer registry
needs no re-run: NOTHING is renumbered, which is that registry's only trigger. `contract-fit`
cited no §6 row by number; `correctness` and `operability` add UNBOLDED `§6 row`/`§6 rows`
citations to §2, §3.3, §4 and this entry, which that registry's DERIVED command RETURNS rather
than restates (`CF13-5`, `CF14-4`) — so no subset of its output is written here, per its own
rule, and the citations are deliberately unbolded because the bolded form is the one that
command does not match.** Round 26's DELETE-FIRST rule stayed
in force and every edit is logged against it: `CF30-1`'s fix is policy step 2 (NARROW — the
universal replaced by the count and the job SET it is true of, no element added); `CF30-2`'s is
policy step 3 (STRENGTHEN an existing measurement by a changed VALUE — the grep's path argument
widened from one file to the directory, no new element and no new row); `CF30-2` also uses step
2 once more, at §8(c), where the rule is named instead of its evidence restated; `CF30-1` also
uses step 2 once more, at §5's consequence-list preamble, and its proposed step-4 ADD at §5's
closing refusal was REFUSED in consolidation, the precedent already being live in §5's CI
paragraph; `CF30-3`'s is policy step 2 at four sites; `C30-1`'s is policy step 3 (STRENGTHEN AN
EXISTING ELEMENT BY A CHANGED VALUE — one fixture clause and a SECOND MUTANT on a twin that
already exists, plus policy step 2 at §9's round-20 entry), taken after DELETE was refused (the
`.ts` filter is correct and mirrors `bundle-types.mjs:43-61`) and NARROW was refused (the
sentence is right; narrowing closes nothing); `OP30-1`'s and `OP30-2`'s are policy step 3 on
conjuncts and invocations that already exist, each with policy step 2 at the sites that govern
them (§4 and §3.3 for the first, §4 and §2 for the second); and `OP30-3`'s is policy step 2
alone, `C11-4`'s own remedy — state the SET, never the fraction — applied at the one site in §4
stating a remedy FRACTION. Step 4 is used once, for this entry.

**The class this round hunted, stated once:** round 29's class carried forward — every
MEASUREMENT whose SCOPE is narrower than the CLAIM resting on it — plus its mirror, a claim
stated more WIDELY than any evidence in the document reaches. All SEVEN findings are that class
and no other: a UNIVERSAL over fourteen members and two jobs backed by no measurement at all
and false of one member and of one job (`CF30-1`); a grep run over ONE of TEN workflow files
standing for a rule quantified over CI (`CF30-2`); a scratchpad run over TWO script names
standing for a bypass set of FIVE, one of whose members is a kind no npm measurement can reach
(`CF30-3`); an ELEMENT that discriminates the DIRECTORY half of a two-half comparand member
standing for the whole member (`C30-1`) — round 29's gap-in-ONE-CONJUNCT shape at an ELEMENT
rather than at a command, which is where this design's coverage claims live; a SUFFIX assertion
on one END of a four-part mandated line standing for the line (`OP30-1`); a set of conjuncts
reading a printed line's HEAD, its PATH, its exit codes and one NEGATIVE, standing for what §4
mandates that line must CARRY (`OP30-2`); and its mirror, a FRACTION stated more widely than any
kind set this document still has (`OP30-3`).

- **`CF30-1`, medium, scope SUBSTANCE — §5 asserted a universal about CI over all fourteen
  `check:all` members and named two jobs, and it is false of one member and of one job.** The
  sentence read "CI runs `check:all`'s EXISTING members as individual steps in two jobs
  (`ci.yml:246` `meta-checks`, `ci.yml:403` `packaging`)". No measurement in this design ever
  backed it: the two commands above it census the string `check:all` across tracked files and
  support only the conjunct that follows — "never `check:all` itself". Re-derived read-only
  2026-08-22 at `187eb2d`, `git status --porcelain` empty before and after: `grep -rn
"check:claim-scope" .github/` returns NOTHING, so `check:claim-scope` — `--strict` at
  `package.json:44`, and the very script §4 cites as this check's report-by-default precedent —
  is a `check:all` member with NO CI step anywhere; and `grep -n "npm run check:engine-pin"
.github/workflows/ci.yml` returns `:244`, which sits under `engine-pin-lint` (`:233`), a
  THIRD job. `meta-checks` holds TEN of the members and `packaging` two, so the true figure is
  THIRTEEN of fourteen across THREE jobs. ⚠️ **The third job's header is ENUMERATED by this
  same section's own CI re-derivation eight lines below** — `engine-pin-lint:` 233 is one of
  the EIGHT headers `CF25-3` listed — so the population was measured and the step-to-job
  mapping never was: standing rule 4 at the granularity of jobs versus steps, on a figure the
  measuring round printed itself. The consequence is a framing one and it points the design's
  way: a `check:all` member with no CI step is ALREADY normal here, which is what §5's closing
  refusal of a dedicated CI step needs and what the false universal denied. Fixed by NARROWING
  to the count and the job set, with the measurement written beside it. **SITES:** §5's CI
  paragraph, one sentence; and §5's consequence-list preamble, which enumerated the TWO
  named jobs while the paragraph above it names THREE, and now states the set it ranges
  over. §5's closing refusal is CHECKED and NOT touched: the precedent this bullet says it
  needs is already live in the CI paragraph above — `check:claim-scope` "is a `check:all`
  member with no CI step at all, which makes that state ALREADY this repository's normal
  case rather than something this design introduces" — so a second statement of it would be
  an ADD the DELETE-FIRST policy does not license, and the sentence resting on it is
  already true.
- **`CF30-2`, medium, scope SUBSTANCE — the rule quantified over CI and its measurement read
  one workflow file of ten.** §5's **THE CHECK HAS ONE CI OCCURRENCE** (round 25, `CF25-3`) is
  backed by `grep -n "npm test" .github/workflows/ci.yml`, and `.github/workflows/` holds TEN
  files. A `pretest` firing in any of the other NINE falsifies the rule, §8(c)'s "it can only
  ever report clean", and the fresh-clone re-attribution `CF25-3` itself made — and no spelling
  of a `ci.yml`-scoped command can return evidence about them, which is `CF29-1`'s shape one
  directory out. Re-derived read-only 2026-08-22 at `187eb2d`, `git status --porcelain` empty
  before and after, over the DIRECTORY: `grep -rn "npm test" .github/workflows/` returns exactly
  ONE `run:` line, `ci.yml:129`, its other hits being prose in `journal-crash-suite.yml` and
  `gates-conformance.yml`; `grep -rn "run: npm run test" .github/workflows/` returns
  `engine-live.yml:81` (`test:live`) and `release-e2e.yml:389` (`test:e2e:release-evidence`),
  which npm's NAME-EXACT pre-hook sends to `pretest:live` and `pretest:e2e:release-evidence`,
  neither declared; every remaining test-running step in that directory is either an
  `npx vitest run` — §5.1's own bypass set instantiated in CI, firing no pre-hook — or
  `jira-datacenter-smoke.yml:28`'s `docker/jira-datacenter/smoke-test.sh`, in which
  `grep -n npm` returns NOTHING, the ONE `run:` step there that is neither an `npm` call nor a
  `vitest` call; and the one `workflow_call` entry point, `publish.yml:64`
  `uses: ./.github/workflows/release-e2e.yml`, carries no `npm test` either. ⚠️ **That
  disjunction is stated because the first draft of this bullet wrote `every remaining test step
… is an npx vitest run`, which is this finding's own class inside this finding's own fix** —
  caught at the apply, `grep -rnE "^\s+run: (\./|bash |sh |docker|scripts/)"
.github/workflows/` returning that one line. ⚠️ **The rule is UNCHANGED — this is a measurement widened, never a verdict moved**,
  which is why the fix is policy step 3. ONE site resting on the rule is touched and only
  one: §8(c) restated the CI-wide universal on its own `ci.yml` citations instead of naming
  the rule, and now names it. Both live spellings were grepped over the WHOLE file —
  `THE CHECK HAS ONE CI OCCURRENCE` and `the check's ONE CI occurrence` — returning, outside
  §5 and outside this section, §0's requirement 8 row (twice on one line), §1, §3.2 (twice),
  §3.5, §4 (twice), §6 row 39 and §8(n): every one NAMES the rule and restates none of its
  evidence, so none is touched. The complementary search is what located §8(c) —
  `only ever report clean` returns THREE hits, §5's own measuring sentence, §5's `packaging`
  bullet (scoped to that one job) and §8(c) — leaving §8(c) as the single live site
  asserting the CI-wide universal off `ci.yml` line numbers alone. **SITES:** §5's CI
  re-derivation, one insertion; §8(c), one clause.
- **`CF30-3`, low, scope SUBSTANCE — a set of FIVE called `measured`, from a run over TWO of
  them.** §0's requirement 10 row read "Re-derived 2026-08-20 (npm 11.16.0): four sibling
  scripts bypass it … plus `npx vitest`, so the measured bypass set is **five**". §5.1's run
  declared `pretest`, `test`, `test:watch` and `test:e2e` and invoked `npm run test:watch` and
  `npm run test:e2e`. What it measured is npm's pre-hook NAME-EXACTNESS; `test:live` and
  `test:e2e:release-evidence` are members BY that property and not by a run, and `npx vitest`
  is a member by invoking no npm script at all — a KIND no npm pre-hook run can reach in either
  direction, so no widening of that scratchpad would ever have covered it. The bypass set and
  its **five** are unrefuted and are left as they are; what goes is the word attributing all
  five to one re-derivation. Verified read-only at `187eb2d` that the four siblings are
  `package.json:20-23` exactly as the row names them. Fixed by NARROWING the attribution at
  §0's row and by stating the run's scope AT the run in §5.1 — the round's own rule, applied to
  a measurement whose conclusion nobody doubts. **SITES:** §0's requirement 10 row, §5.1's
  Honest bound, §5.1's NAMED-future-element list and §6's wiring-assertion comment — the
  last two because the Honest bound's new sentence names FOUR npm-script bypass members two
  lines above two lists of THREE `pretest:*` names, so the extension both sites call a
  stated decision covers three of the four and the fifth member not at all (standing rule 8:
  one predicate, four sites).
- **`C30-1`, medium, scope SUBSTANCE — §3.3's comparand member 1 has TWO testable halves and
  `C20-2` closed one.** The member is
  `every *.ts under every packages/*/src (the set bundle-types.mjs:43-61 walks)` — a DIRECTORY
  set AND an EXTENSION filter — and the element `C20-2` added, row 42's SCOPE TWIN,
  discriminates `packages/*` from the 19 enumerated units and NOTHING about the extension,
  while §3.3's own sentence, §2's `checkDeclarationCache` comment and §4's `stale-declarations`
  remedy row each rest on it for the WHOLE member. `C28-1`'s shape at the sibling comparand.
  Two scope-CORRECT bodies get the filter wrong and neither is carelessness:
  `newestUnder(join(cwd, "packages", pkg, "src"))` with NO accept — §2 declares
  `newestUnder(dir, accept)` and §3.4's own dist side is specified as "a plain recursive walk" —
  and the same call carrying §2's `isCompilerInput`, which §3's input rule 2 makes TRUE of a
  `src/**.json`. The producer takes `.ts` alone: re-read as TEXT read-only 2026-08-22 at
  `187eb2d`, `bundle-types.mjs:43-61`'s `walk()` recurses, skips `node_modules`/`dist`, and
  reaches `statSync(full).mtimeMs` only inside `entry.name.endsWith(".ts")`. ⚠️ **THE
  MEASUREMENT AND ITS SCOPE TOGETHER:** §3's census of **18** non-`.ts` files across the 19
  `src/` trees re-derives unmoved under its own printed command, and ALL 18 sit under a
  `packages/*/src` — `e2e/report/src` holds **0**, same command split by directory, read-only
  2026-08-22, `git status --porcelain` empty before and after. So either body reports
  `stale-declarations packages/cli` for a
  `touch packages/gates/src/coverage/test-support/real-vitest-lcov.info` — tracked, and the
  newest non-`.ts` member of that set — at `check:all` member 0, exit **1** under `--strict`
  and at line 5 of §5.1's measured 204 under `pretest`; and §4's remedy row cannot clear it,
  `newestSourceMtime()` not seeing the file, so `:70` skips, the cache does not move and the
  check fires again identically — `CF14-1`'s class at the OTHER half of the member `C20-2`
  half-closed. ⚠️ **Not live on this working copy, and that is the point:** measured read-only
  2026-08-22, the newest non-`.ts` under `packages/*/src` is **1786978107051.44** against
  `packages/cli/.dts-cache/index.d.ts` at **1787172303152.90**, so all three bodies are quiet
  here today and the smoke test asserts the unit SET and the coverage SHAPE and explicitly not
  the finding set. ⚠️ **The §6 census is what makes the gap falsifiable rather than argued:**
  re-derived read-only 2026-08-22 over this file's `## 6.`-to-`## 7.` span, exactly TWO places
  in §6 put a non-`.ts` file under any `src` — row 48's `src/**.json`, on a row whose own cell
  opens **NOT a cli-scoped row**, and the false-negative battery's `.info`/`.snap`/`.mjs`
  bullet, which is a BULLET where the complete-cli-build rule quantifies over `Every cli-scoped
row` and its plugin-SOURCE clause over `EVERY ROW ASSERTING` (`C26-1`'s keying) — so neither
  can hold a `packages/cli/.dts-cache/index.d.ts`, every cache-bearing fixture in §6 holds `.ts`
  sources ALONE and all three bodies agree on every one. Fixed by policy step 3 at row 42's
  SCOPE TWIN: ONE fixture clause (`packages/cli/src/fixture.json`, newer than the cache and
  older than `bundleAt` — a `*.json` and not a `.snap`, because it is the ONE extension both
  wrong bodies accept, so one clause kills both) and ONE SECOND MUTANT in row 8's own spelling
  (standing rule 7). ⚠️ **The finding proposed a further `packages/B` UNIT and it was NARROWED
  in consolidation (standing rule 2):** one file under the fixture's existing `packages/cli/src`
  discriminates both bodies identically while dragging in none of the added unit's fan-out — no
  root-reference change, no `C15-2` inlined-set question, no §3.1 bijection question, no §3.2
  first-input-member question, and no edit to that twin's existing `the added unit is outside
packages/` conformance sentence. ⚠️ **`OP16-3`'s and `CF20-1`'s disqualifier, checked BEFORE
  the clause was written:** `bundleAt` is the minimum over the chunk set and every chunk is a
  `.js` under `packages/cli/dist` reached by no minus-bullet, so the file is older than this
  unit's newest output and no `stale-unit` fires; it IS §3.2's second input member and is older
  than `bundleAt`, so no `stale-bundle`; `packages/cli` carries `kind` `"bundled"` and §3.1
  exempts it structurally, so no `orphan-output`; and it adds no output, no stem and no unit.
  The clause also PINS an ordering this fixture otherwise left free — the cache below `bundleAt`
  — which is stated at the clause so the element is constructible rather than empty.
  ⚠️ **Standing rule 8, checked in both directions:** the twin's existing scope mutant still
  fires off `e2e/report/src/**.ts`, which this clause does not touch, and row 42's main half
  and its PLAN TWIN are unmoved, this twin asserting neither `stale-bundle` nor a `full`
  `bundle-freshness` entry. **SITES, all moved together per standing rule 9:** §3.3's comparand
  member 1 bullet; §2's `checkDeclarationCache` comment; §4's `stale-declarations` remedy row,
  whose "what makes the identity falsifiable" pointer named a two-coordinate identity and a
  one-coordinate element; row 42's SCOPE TWIN at its fixture, its mutant and its "Nothing else
  in §6 reaches it" cells; §9's round-20 `C20-2` entry, narrowed to the half it closed; and this
  entry. §0's requirement 8 row is CHECKED and NOT moved — its fixture list is the coverage/skip
  spellings (rows 6, 17, 22, 23, 29) and `C20-2` did not add row 42 to it either.
- **`OP30-1`, medium, scope SUBSTANCE — §4 and §3.3 mandate a four-part `unbuilt` line and §6
  pinned only its two ENDS.** §4's clause-2 bullet mandates
  `unbuilt packages/cli — dist/<file> (<mtime>) is NEWER than the last completed bundle (.bundle-meta/metafile.json, <mtime>); tsc -b has overwritten the bundled output`
  and closes "So the line names the offending file and both mtimes"; §3.3's cli reason table
  mandates the identical string as `unitState()`'s reason, which §4 prints VERBATIM. Re-derived
  read-only at `187eb2d`, `git status --porcelain` listing nothing but this file before and
  after: `grep -c -F 'is NEWER than the last completed bundle'` returns **3** over the whole
  file — §3.3's reason table, §4's line list and §7's `npm run typecheck` row — while
  `awk '/^## 6\./,/^## 7\./' | grep -c -F` the same string returns **ZERO**, so no element in §6
  SPELLS the reason. The two §6 rows that reach it name it by REFERENCE: row 19's verdict says
  "reason naming `dist/bin.js`", the offending FILE alone, and row 37's says "carrying the
  clause-2 reason", a which-clause-fired discrimination (`C14-1`) on a row its own cell calls "a
  verdict row, not a rendered-text one" — both kept OUT of the rendered group by round 19
  (`OP19-1`). Row 26's MAIN half is the only RENDERED assertion, added last round by `OP29-1` as
  a SUFFIX on `tsc -b has overwritten the bundled output`, its own cell saying in terms that no
  mtime FORMAT is pinned. So a clause-2 reason reading
  `dist/bin.js is stale; tsc -b has overwritten the bundled output` — the offending file named,
  the mandated literal at the end, NEITHER the marker path NOR either mtime — satisfies every
  reference and the SUFFIX alike, on §7 row 2's own tree: exit **1** under `--strict` at
  `check:all` member 0, and at line 5 of §5.1's measured 204 under flagless `pretest`, handing
  the operator an unverifiable one-clause assertion about a file `ls` shows present and recent —
  while §4's own warrant for the mtimes is that this is the state "nothing an operator can see
  with `ls` will reveal", at the one firing §7 says nobody may mute as noise. `OP19-1`'s and
  `OP18-5`'s class at the third of §3.3's three cli `unbuilt` sentences, one round after
  `OP29-1` reached that sentence's tail and stopped there. Fixed by STRENGTHENING an EXISTING
  conjunct (policy step 3; DELETE and NARROW were checked first and refused, deletion losing the
  line's only rendered assertion and narrowing §4 conceding a diagnosis §7 row 2 depends on):
  row 26's SUFFIX becomes the WHOLE-LINE anchored form on the SAME fixture, the two mtime slots
  left free so a correct implementation cannot fail it (`OP16-3`'s disqualifier) and the end
  anchor carried over unchanged so round 29's bare-container mutant is still killed (standing
  rule 8, checked at the fix). NO row, NO twin, NO fixture and NO mutant count is added.
  Standing rule 9 in the same pass: §4's bullet and §3.3's clause-2 row each now name WHICH part
  of the line each element pins. **SITES:** §6 row 26's MAIN half, §4's clause-2 line bullet,
  §3.3's cli reason-table clause-2 row.
- **`OP30-2`, medium, scope SUBSTANCE — the clause §4 rests the whole `WARN` line on was read by
  nothing.** §4's internal-error block mandates that BOTH printed lines carry
  `; the staleness check did NOT run, so this is not a verdict on your tree`, and §4's warrant
  for degrading an internal error to a warning at exit 0 is that clause — the row round 1's
  `O-2` produced and round 11's `OP11-2` gave an owner and a text. Re-derived read-only at
  `187eb2d`: `the staleness check did NOT run` and `not a verdict on your tree` each occur **2**
  times in the whole file, BOTH inside §4's own fenced block, and
  `awk '/^## 6\./,/^## 7\./' | grep -c -F` returns **ZERO** for each — the sentence is
  referenced in §6 neither as a literal nor descriptively. What rows 27, 31 and row 27's
  `C18-5` twin DO pin is the HEAD, the `<path>`, the exit codes, the `WARN` NEGATIVE clause and,
  on the `ERROR` halves, a SHAPE containing `if this ran as part of` and `member 0`. So a
  `formatInternalError` returning the head alone plus, under `strict`, the re-run and member-0
  sentences — the verdict clause dropped from BOTH lines — passed every element in §6, the
  false-negative battery, the wiring assertions and the live smoke test; row 27's own
  `no try/catch` and shared-string mutants are both blind to it, the first failing on the head
  and the second on the negative clause. Wrong result on the one path §4 says is never chained:
  a malformed `packages/*/tsconfig.json` under flagless `pretest` prints
  `check-stale-dist: WARN internal error — packages/gates/tsconfig.json: Expected double-quoted property name in JSON at position 7 (line 1 column 8)`
  at exit **0**, at line 5 of §5.1's measured 204 and ~199 lines above a GREEN vitest summary,
  with nothing on the page saying the check did not run — verbatim the "read the WARN as a
  verdict" failure the clause was introduced to prevent. `OP18-6`'s class at the same two lines
  one clause over: that round closed what the `WARN` line must NOT carry and left what BOTH
  lines MUST carry unread. Fixed by STRENGTHENING EXISTING invocations (policy step 3; NARROW
  refused because the sentence is the `WARN` line's whole purpose, DELETE because it would
  concede `O-2`'s remedy): rows 27 and 31 and row 27's `C18-5` twin each gain the containment
  conjunct on both human lines, §4 mandating it with no `strict`- or `json`-varying member so no
  correct implementation can fail it, and in the same conjunct the `ERROR` half's "plus the
  re-run sentence" gains its LITERAL — re-derived at **0** occurrences in §6 — so the clause its
  `WARN` twin forbids BY NAME is asserted by the same spelling on both sides; that half is a
  companion of this fix and is NOT claimed as separately uncovered. Standing rule 8, checked at
  the fix: the `WARN` negatives, both `--json` object assertions, the round-28 `--json` exit
  PAIR and row 31's static-import mutant are all untouched. NO row, NO twin, NO fixture and NO
  invocation is added. **SITES:** §6 rows 27 and 31 and row 27's `C18-5` twin; §4's
  internal-error paragraph; §2's `formatInternalError` comment.
- **`OP30-3`, low, scope SUBSTANCE — a remedy FRACTION whose denominator has had no referent
  since round 9.** §4's remedy lead-in read "`npm run build` was the single printed remedy and
  it is wrong for two of the four kinds", standing above §4's eight-row remedy table and read as
  its summary. Re-derived at `187eb2d`: §2's `kind` union has EIGHT members, so "the four kinds"
  names round 1's kind set, which this design has not had since round 9 added the eighth. Both
  halves of the fraction are false of the document as it stands. The paragraph MEASURES two —
  `orphan-output`, via the pinned `typescript@6.0.3` composite fixture, and `stale-declarations`,
  via `bundle-types.mjs:70` — while the very next ⚠️ paragraph chains a third off this sentence
  with the word "either" (`stale-unit`) and §4's own remedy table adds a fourth in the `unbuilt`
  row (`OP29-3`). So the sentence a reader takes as the table's summary puts six of eight kinds
  on a bare `npm run build`. `C11-4`'s class living in §0-§8, at the one site in this section
  stating a remedy FRACTION rather than a per-row remedy — and `two of the four` occurs exactly
  **2** times in the whole file on one line each, this one and §3.3's, whose subject is the four
  cli-scoped comparisons and not a remedy, so this is a single site and not a set a later round
  must reconcile. This is an over-scoped CLAIM rather than an uncovered obligation and is
  counted as neither of the two above. Fixed by NARROWING — policy step 2, the fix that creates
  no obligation, and `C11-4`'s own remedy applied where it had not been: the lead-in names the
  two kinds this paragraph MEASURES and drops the fraction, leaving the next paragraph's
  "either" chaining correctly, the `unbuilt` row's round-29 scope unmoved and the remedy table
  authoritative for the rest. NO element is owed and none is added — row 4 and row 29's second
  fixture cover `orphan-output`, rows 3, 6, 42 and 42's round-20 SCOPE TWIN cover
  `stale-declarations` — and a narrowed sentence can never go uncovered. **SITES:** §4's remedy
  lead-in paragraph alone.

**Round 30's numbers:** `contract-fit` **54** obligations enumerated over §0's eleven
requirements and §5's wiring claims, THREE uncovered; `correctness` **86** over §2's exported
signatures and §3-§3.5's comparison rules, ONE uncovered; `operability` **60** over §4's printed
surface, §7's thirteen rows and §5.1, THREE uncovered — SEVEN in all, all dispositioned `fixed`;
split by matrix rather than summed into a number no lens ran (standing rule 4 at the granularity
of matrices). **COUNTS `operability`'s edits CHANGE:** none is a number moved. NO battery row,
NO twin, NO fixture and NO invocation is added, so §1's **49** battery rows and its `~680`
estimate are unmoved (`OP22-3`); §9's round-26 §6 mutant-count census is unread by this apply,
no numeral being written immediately before `mutant`/`mutants`; §4's FOUR exit-table rows, its
four carve-outs, its EIGHT remedy rows and its nine mandated reasons are unmoved and were
re-derived rather than restated; row 27's three invocations, its twin's three and row 31's two
halves are unmoved, these being conjuncts on invocations that already exist; §2's `kind` union,
§3's counts, §5's member figures and §5.1's **five**, **four**, `204` and line **5** are refuted
by nothing here. The ONE figure that stops being a live REMEDY claim is §4's remedy-lead-in
fraction `two of the four`, which after this apply survives as a remedy claim only inside
`OP30-3`'s own quotation of the superseded text and inside round 1's `O-1` entry — §3.3's
occurrence of the same three words is a different subject and is untouched — this section's
convention for a figure a later round must be able to tell from damage. ⚠️ **The clause-2
`unbuilt` line now occurs inside §6 for the first time** — row 26's widened conjunct spells it —
so §6's copy of `is NEWER than the last completed bundle` goes 0 → 1; no live sentence
states that figure, and round 29's sha-pinned `c672565` re-derivation
in row 26's own cell is left as the measurement it is. **No working-tree mutation:**
`scripts/bundle-cli.mjs` was never imported, every probe ran `git`, `stat`, `find`, `grep`,
`sed`, `awk` or `python3` read-only over files as text, and `git status --porcelain` listed
nothing but this design file before and after. **COUNTS THIS EDIT CHANGES, and
their sites:** §5's CI paragraph gains `THIRTEEN of fourteen`, `THREE jobs`, `ten members` for
`meta-checks` and `TEN`/`NINE` for the workflow-file population, and LOSES `in two jobs` as a
live claim — the phrase survives only inside `CF30-1`'s own quotation of the superseded text,
which is this section's convention for a figure a later round must be able to tell from damage;
§0's requirement 10 row gains `TWO of those five` and drops the word `measured` from the FIVE;
§5.1's Honest bound gains `TWO of the four siblings`. **Counts the `contract-fit` fan-out
adds:** §5's consequence-list preamble gains `THREE member-holding jobs` and `TWO of the
three jobs`; §8(c) gains a second live `TEN` for the workflow-file population, the same
figure §5's re-derivation states and not an independent count; §5.1's NAMED-future-element
list and §6's wiring-assertion comment each gain `THREE of the four`. **Still not moved:**
§5's `THIRTEEN of fourteen`, its `6` and `9` censuses, `15th member` and `the other **14**`;
§5.1's **five**, **four**, **204** and line **5**; §6's **49** rows, no row, twin or fixture
being added — and §1's `~680` is an estimate over a file that gains four comment lines and
no assertion (`OP22-3`). **Counts NOT moved:** §5's **6** and
**9**, this design file being excluded from the first BY CONSTRUCTION and filtered out of the
second by `grep -v ^docs/`, so neither census sees a single word added here — re-derived at
`187eb2d`, both unmoved; §5's `15th member`, its `member 12`/`member 13` and its `the other
**14**`, all re-derived unmoved against `package.json`'s fourteen-member chain; §5.1's **five**
and **four**, refuted by nothing here; §5.1's `204` and its line **5**; §1's **49** battery rows
and its `~680` estimate, no row and no twin being added (`OP22-3`); §6's SEVEN
`placedCorrectly` conjuncts and SEVEN mutants, its `SIX of the bundler predicate's EIGHT order
operands` and its THREE census-import mutants; §0's `SIX … THREE … FIVE` and §1's `SIX source
properties … **FIVE**`; §3's **21 of 30**, its `29 PATHS` and §8(l)'s **NINE**; rule 4's live
member count **ZERO**; §3's Units step's **19 of 19**; and §4's exit-table, carve-out, remedy
and reason counts. ⚠️ **`C30-1`'s apply moves NO count either, with its sites:** §1's **49** battery rows and its
`~680` estimate stand, no row and no twin being added (`OP22-3`) — a fixture clause and a mutant
on a twin that already exists; §3's **18** non-`.ts` files across the 19 `src/` trees is
re-derived UNMOVED and gains its `packages/*` distribution beside it rather than a new figure,
and §3's **3** `src/**.json`, its **21 of 30**, its **20**/**9** split, §0's `29 PATHS`, §8(l)'s
**NINE**, rule 4's live member count **ZERO** and §3's Units step's **19 of 19** are untouched,
no config and no unit being added; §0's requirement 8 row's fixture list (rows 6, 17, 22, 23, 29) is CHECKED and NOT moved, `C20-2` not having added row 42 to it either; §6's SEVEN
`placedCorrectly` conjuncts and SEVEN mutants are untouched, `SECOND MUTANT` carrying no
numeral; and §4's exit-table, carve-out, remedy and reason counts are unmoved, no kind, reason
spelling, coverage status or exit code being added. No numeral is written immediately before
`mutant`/`mutants` by this apply,
so §9's round-26 mutant-count census is unread by it. Every earlier round's sha-pinned figure is
left as the measurement it is. ⚠️ **§6's live four-`stale-unit` prediction was re-derived rather
than assumed** — read-only at `187eb2d`, §3's input rules 1-3 and its four-extension output
whitelist run over the 19 root references in a scratchpad module importing nothing from this
repository: exactly FOUR, `packages/contracts`, `packages/engine-claude`, `packages/gates` and
`packages/plugin`, the same four §6 names — so no count resting on it moved either. **No
working-tree mutation:** `scripts/bundle-cli.mjs` was never imported; every probe ran `git`,
`ls`, `grep`, `sed`, `awk` or `node` read-only over files as text; the only writes went to the
scratchpad and to this design file; and `git status --porcelain` listed nothing but this file
before and after.

**Round 31 (2026-08-22) — `contract-fit` and `operability`, each run as the SAME COVERAGE
MATRIX as rounds 18-30 and each enumerated INDEPENDENTLY of round 30's list rather than as a
check that its fixes landed. `contract-fit`: **54** obligations over §0's ELEVEN requirement
rows and §5/§5.1's wiring claims, FOUR uncovered. `operability`: **107** obligations over §4's
printed surface — its FOUR exit-table rows, EIGHT per-kind line shapes, NINE mandated coverage
reasons, the coverage line, the summary, the grouped collapse, the prefix rule, the four
line-order adjacencies, the internal-error surface, its EIGHT remedy rows and its five recipe
steps — plus §7's thirteen rows and §5.1, FOUR failed. EIGHT findings, no two the same defect,
all re-derived read-only at `55666e8` on a clean tree and all dispositioned `fixed`.**
⚠️ **THE ONE THING THIS ROUND DID DIFFERENTLY, and the owner's instruction that set it: before
proposing a scope statement, ask whether the obligation should EXIST.** Rounds 26-30 each found
a claim whose evidence was narrower than the claim, and each fix STATED the scope; a sentence
no requirement traces to is an obligation that goes uncovered again at every widening of the
set it quantifies over, and DELETING it is the other fix. SEVEN of the eight dispositions here
are policy step 1, DELETE; the eighth is policy step 3 and moves BYTES rather than a claim.
**NO battery row, NO twin, NO fixture, NO mutant and NO invocation is added, nothing is
renumbered, and no kind, reason spelling, coverage status or exit code is added or removed** —
so §1's **49** stays correct (`OP22-3`) and §6's sha-pinned outside-citer registry needs no
re-run, renumbering being its only trigger.

**The two classes this round hunted.** First, what an APPLY inserts, at the mechanism the
standing checks provably cannot see: round 10's rule (guard WHAT an apply inserts, not only
WHERE) at a formatter that fires on no edit anyone calls an edit. `CF31-1` and `CF31-2` are
that class. Second, rounds 26-30's class — a claim stated more widely than any evidence in the
document reaches — dispositioned by DELETION instead of by scope: `CF31-3`, `CF31-4`, `OP31-1`,
`OP31-2`, `OP31-3` and `OP31-4`.

- **`CF31-1`, medium, scope SUBSTANCE — §6's wiring-assertion comment says `test:_` and
  `pre_`, which name nothing, and round 30's own entry lists that comment as one of FOUR SITES
  OF ONE PREDICATE while the same apply wrote the other three correctly.** ⚠️ **MECHANISM,
  measured in BOTH directions, read-only at `55666e8` with `git status --porcelain` empty
  before and after:** `node_modules/.bin/prettier --parser markdown` leaves the damaged form
  BYTE-IDENTICAL, and restoring both asterisks on a scratch copy of HEAD makes prettier 3.9.5
  reproduce the damage EXACTLY — the underscore pair, the joined lines and every elided space —
  because round 30 introduced a SECOND asterisk, in a `pre` glob, into a paragraph of the
  un-fenced wiring block, so the two became a left/right-flanking emphasis pair. That is
  verbatim the mechanism the preamble's FOURTH REPRODUCIBLE CHECK records for
  `git show 934c547`. ⚠️ **THE GUARD IS BLIND, AND THE PREAMBLE ALREADY SAYS WHY:** the mangled
  form is the STABLE form, so the prettier plus `norm()` plus `diff` check returns **0** lines
  on the damaged file — re-derived here — while the standing consequence it carries is scoped
  to every REGEX this design asks an implementer to copy, and round 30's two asterisks were
  GLOBS IN PROSE. The standing consequence therefore reads wider than the regexes it names: ANY
  TWO bare asterisks in one paragraph, glob or quantifier alike, and this entry carries none.
  Fixed by DELETE (policy step 1), which removes the asterisk and the site together: restoring
  it is measured stable ONLY while that paragraph holds exactly one, so the next round adding a
  second re-mangles it. NOTHING IS LOST — §6 asserts nothing about the sibling-script reach,
  that comment existing to explain an ABSENT assertion, and §0's requirement 10 row and §5.1
  both carry the statement in full. **SITES:** §6's wiring-assertion comment — one sentence
  deleted, two lines un-joined — and that is the whole fan-out. §0's requirement 10 row,
  §5.1's Honest bound and §5.1's NAMED-future-element list are CHECKED and NOT touched, none
  citing §6; §1's `package.json` row names no sibling; §8 carries no residual on the bypass
  set. The predicate's SITE count falls from four to three, and the two sentences saying
  `four sites` OF THIS predicate are both inside round 30's entry, historical and left as
  written.
- **`CF31-2`, medium, scope SUBSTANCE — §6 row 26 runs words into code spans at SIXTY-SIX
  places, and one of them breaks a printed COMMAND.** The row states
  ``awk '/^## 6\./,/^## 7\./' \| grep -c -F` the same string returns **ZERO**`` — the code span
  closes after `-F`, so the command as printed carries no PATTERN and is not runnable, against
  this change set's own standing rule 3. A second one closes the same way on
  `grep -n 'packages/cli unbuilt'`. ⚠️ **Re-derived read-only, clean tree before and after, and
  the granularity matters (standing rule 4):** the letter-backtick-letter census over row 26
  returns **0** at `187eb2d` and **40** at `55666e8`, but that census is itself narrower than
  the damage — the true figure is **66**, derived by comparing every backtick adjacency in the
  row against the vocabulary the SAME row had at `187eb2d` (an opening backtick preceded only
  by a space, an open paren or a bold marker; a closing one followed only by a space, an
  apostrophe, a double quote, a close paren, a comma, a bold marker or a colon), which returns
  **0** exceptions there and **66** here. **The damage is CONFINED to row 26 and to `CF31-1`'s
  comment:** the same census over the file EXCLUDING §6 row 26 AND `CF31-1`'s comment is **33**
  at both commits, every one of the 33 being this document's own English-suffix idiom. The
  arithmetic is stated so a later round re-derives that exclusion rather than guessing it: the
  whole file is **35** at `187eb2d`, where row 26 contributes **0** and that comment **2**, and
  **76** at `55666e8`, where row 26 contributes **40** and that comment **3**. PROOF IT IS
  DAMAGE AND NOT
  INTENT: round 30's `OP30-1` bullet writes the identical clause WITH the space, so one apply
  wrote it two ways at two sites; and the restored text is prettier-STABLE, `norm()` diff **0**
  and a second run byte-identical, which also proves the elisions were the apply's and not the
  formatter's. Fixed by policy step 3, a corrected VALUE — the row's own bytes. DELETE was
  REFUSED: the damaged text is the mandated `unbuilt` clause-2 wording and the four
  absence-reason spellings, which requirement 8 and §3.3's cli reason table trace to.
  ⚠️ **A policy step 4 was drafted and REFUSED, and the refusal is recorded so a later round
  does not re-propose it:** a standing apply clause re-deriving the letter-backtick-letter
  census would key on a population narrower than the damage it guards — **40 of 66** here,
  measured — which is this round's own class inside this round's own fix. The rule that was
  violated already exists: standing rule 3, the command is the reproducible part. **SITES:**
  §6 row 26 alone. Every cell COUNT is unchanged, which is why round 30's escape-aware row
  census could not see this and why the row-pipe check returns nothing on the damaged and the
  fixed file alike.
- **`CF31-3`, low, scope META — the preamble states its citation guard's blind spots as a
  CLOSED pair and a THIRD was live at EIGHT citations.** A citation carrying one lowercase word
  between `'s ` and the rule name is matched by neither pass: `§4's ordered REMEDY PLAN` six
  times and `§5's rule` plus the bold CI-occurrence name twice. Re-derived read-only at
  `55666e8`: all three passes and the anchor grep return NOTHING and all eight citations
  RESOLVE, so this is blindness and not breakage. MUTANT, in the preamble's own IMPORT RULE
  shape: rename `REMEDY PLAN` inside §4 alone on a scratch copy and pass 3 prints NOTHING while
  six live citations dangle; rename the CI-occurrence name inside §5 alone and pass 2 catches
  only the two bare-bold citations. Fixed by DELETE (policy step 1) at all eight — the word
  goes, the passes are UNCHANGED, and the citations are then in the conformant form the guard's
  own convention states. ⚠️ **The widening was MEASURED before being refused (standing rule 5,
  and round 11's test for whether a check is a check):** inserting `([a-z]+ )?` after `'s `
  takes pass 2 from **66** raw matches to **71** and pass 3 from **22** to **28** at
  `git show 55666e8:<file>`, both still returning nothing — but three of pass 2's five
  additions name no rule (`§0's requirement **1**`, `§3's own **Units**`,
  `§3.2's mandated **minimum**`) and resolve on a bare literal, so it buys three vacuous checks
  for two real ones. The residual is now STATED in the guard rather than closed by regex, and
  it is live again the moment a later round writes the form. **SITES:** the preamble's residual
  sentence; §0's requirement 3 row, §1's `check-stale-dist.test.mjs` row, §2's `formatFindings`
  comment, §3's requirement-3 paragraph, §6's false-negative battery and §7's coverage-line row
  for the six `ordered`; §8(c) and round 25's entry for the two `rule`. §4 is CHECKED and NOT
  touched — it contains `REMEDY PLAN`, which is what the shortened citations resolve against,
  and it keeps the word `ordered` at four sites of its own, so no claim about the plan's order
  is vacated.
- **`CF31-4`, low, scope META — round 30's closing paragraph states a file-wide count that is
  wrong in the commit that writes it.** It reads: §6's copy of
  `is NEWER than the last completed bundle` goes 0 → 1 and the file-wide count 3 → 4.
  Re-derived read-only, clean tree before and after: `grep -c -F` returns **3** at `187eb2d`
  and **7** at `55666e8`; scoped, `awk '/^## 0\./,/^## 9\./'` returns 4 and `awk '/^## 9\./,0'`
  returns **3**, the three extra being round 30's OWN entry quoting the mandated line at three
  places. The §6-scoped half re-derives EXACTLY; only the file-wide half is wrong. `C11-4`'s
  class, and verbatim `CF13-6`'s lesson that a literal written into this section is stale in
  the same apply that writes it. Fixed by DELETE (policy step 1), which is `C11-4`'s own remedy
  and the disposition round 13 took for the 2574 baseline; correcting it to **7** is REFUSED
  for the reason round 13 refused correcting 2574 — a file-wide literal here is stale at the
  next round that quotes the string, and three of the seven occurrences are already inside
  round 30's own entry. ⚠️ **The finding's own verification was narrower than its claim, and is
  corrected here (standing rule 1):** it reported that a grep for the deleted figure returns
  that paragraph alone; it returns TWO lines, the second being round 25's entry on §6's
  plan-first-line prefix enumeration, a different subject that stays. **SITES:** round 30's
  closing paragraph alone.
- **`OP31-1`, medium, scope SUBSTANCE — §4's stated warrant for the prefix RULE was a universal
  over `check:all`'s 14 members, and FOUR of the fourteen falsify it.** Re-derived read-only
  2026-08-22 at `55666e8`, `git status --porcelain` empty before and after:
  `check-claim-scope` writes a bare `<rel>:<line>  quantifier …` finding line (the two lines it
  concatenates after it ARE indented, so §4's continuation exemption reaches those and not it),
  `check-citation-content` writes its `BLOCKING_HEADINGS` literals and its
  `PROSE REFERENCES THAT DO NOT RESOLVE — N` heading, `check-workspace-count` writes
  `err.message` and npm's own stdout alone, and `check-support-window-freshness` writes
  `::warning::check-support-window-freshness: …`, which does not BEGIN with the name. What IS
  true of all fourteen is the VERDICT line, which is not the population §4's rule extends —
  that rule binds the finding lines, the grouped lines, the summary, the plan's first line and
  the coverage line, exactly the detail-line class where the precedent breaks. `CF30-1`'s class
  — a universal over these same fourteen members, false of members — at the site round 30 did
  not sweep: standing rule 9. **Policy step 1, DELETE**: the universal, its fourteen-name
  enumeration and its `zero exceptions` token leave §4, and the two places the same universal
  survived in compressed form go with them — `beside 14 prefixed neighbours` loses `prefixed`,
  and `cannot be grepped by name — a thing every other member supports` loses its trailing
  clause (standing rule 8: one predicate, three sites, and the fix that reaches two of three
  re-opens itself). Nothing traces to any of them: §0 carries no requirement about another
  member's stream, the `check-citation-runs.mjs:290` precedent cited in the same sentence
  already establishes that the convention exists, and the RULE's operative warrant is the
  measured attribution argument below it — member 0 of 15 chained into one stream, and line 5
  of §5.1's measured 204 under `pretest`. §8(o)'s LIVE sentence pointed at the deleted
  measurement and is re-pointed onto §8(o)'s OWN glyph census, re-derived unmoved 2026-08-22
  with the same `grep -nP` over the same fourteen scripts: **7** hits in **4** files, every one
  inside a COMMENT and none reaching a stream. ⚠️ **The re-point is a DELETION of the pointer
  and not a claim that §4 says nothing about glyphs — §4's coverage-line bullet carries a glyph
  census of its own, which this round leaves exactly as written.** Round 14's `OP14-5` entry
  preserves the superseded phrasing and is NOT touched.
- **`OP31-2`, low, scope SUBSTANCE — §4's prefix RULE licensed a stream nothing in this design
  writes to.** `grep -n stderr` over all 9,176 lines returned exactly ONE hit at `55666e8`, the
  rule's own clause. Three sites decide the opposite: §4's opening ("Human text on stdout by
  default"), §2's `formatInternalError` comment (`on **stdout**`, which is the whole `C11-5`
  warrant for that function taking `json`), and §6 rows 27 and 31, which assert
  `stdout contains`, beside row 43's CLI twin, which asserts a line RENDERED on stdout.
  Meanwhile the precedent §4 cites BY LINE for its own `WARN` shape prints to the other stream
  — verified at `55666e8`, `check-citation-runs.mjs` puts its `WARN` line on `console.warn` and
  its `FAIL` lines on `console.error`, with only the `PASS` line on `console.log` — so an
  implementer copying that shape WHOLE, as standing rule 7 tells them to, satisfied §4's rule
  verbatim, FAILED §6 rows 27 and 31, and got a clean `--json` stream by accident at the one
  place `C11-5` exists to defend. **Policy step 1, DELETE** the two words `or stderr`; because
  deleting a domain would LOOSEN the rule, the same edit states the stream positively — every
  line the rule enumerates is on stdout — so nothing this rule bound is released. The strike is
  recorded at the site so a later round cannot restore it.
- **`OP31-3`, low, scope SUBSTANCE — §4's `stale-shipped-declarations` line ended in a clause
  that restated its own head and that no element reads.** `; the shipped copy is not the
current cache` says in plain English what `dist/index.d.ts does not match
.dts-cache/index.d.ts` says in paths, against this section's own rule two lines above that
  "the line states the OBSERVATION and the table states the fix". `grep -c -F` returned exactly
  ONE hit in 9,176 lines at `55666e8` — its own definition — so §6 reads it nowhere: row 29's
  fourth fixture glosses this kind's line as byte-based with no mtime delta while the SAME cell
  demands the fifth fixture's line VERBATIM. Requirement 8 is discharged by §3.3's and §3.5's
  comparisons, never by this sentence. `OP30-1`'s class at the sibling line that round did not
  reach, and here **policy step 1, DELETE** is available where at row 26's clause-2 line it was
  not. ⚠️ **`C13-2` is NOT re-opened:** what that round required is that this line be
  cause-NEUTRAL, and the surviving head is purely observational, so §3.5's sentence resting on
  it stands — and row 22's twin pins the marker-ABSENT BRANCH in which the kind fires, never
  this clause.
- **`OP31-4`, low, scope SUBSTANCE — one token denoted two values inside one section, and §6
  could not tell the readings apart.** §4's internal-error block mandated
  `— <path>: <err.message>` while §4's `--json` paragraph said `<err.message>` already carries
  the `<path>: ` prefix. §3's wrapper re-throws as `<path>: <err.message>`, so at the catch
  there is no separate `path` binding: an implementer taking the block literally manufactures
  one and prints `${err.path}: ${err.message}`, rendering the locus TWICE at exit **2** from
  `check:all` member 0 and at line 5 of §5.1's measured 204 under `pretest`. Rows 27 and 31 and
  row 27's `C18-5` twin are `contains` assertions over the head, the path and the verdict
  clause, and none forbids an inserted locus — row 31 being this design's ONLY no-read fixture.
  And the disambiguating sentence — "where the error comes from no read the prefix is omitted"
  — named a branch an implementer cannot act on, both arms being the SAME expression
  `— ${err.message}`. **Policy step 1, DELETE, applied to the ambiguity rather than to the
  disambiguator:** the block's `<path>: ` slot goes from both lines, the omission sentence goes
  with it, and the statement of where the locus comes from stays, re-anchored on
  `<err.message>`. No asserted literal moves (standing rule 8): row 27 asserts the head plus
  the fixture's path, and `check-stale-dist: ERROR internal error — <that path>: ` stays a
  contiguous substring under the one-slot reading because the wrapper puts the path at the
  front of `err.message`; row 27's own MUTANT description already spells the head as
  `check-stale-dist: <level> internal error — <err.message>`, which after this edit agrees with
  the template rather than contradicting it.

**Round 31's numbers.** `contract-fit` **54** obligations, FOUR uncovered; `operability` **107**
obligations, FOUR failed — ONE falsified and THREE uncovered. All eight dispositioned `fixed`.
**DELETIONS, counted because the owner asked for them explicitly — TEN removals across
seventeen sites, and NOT ONE of the ten traced to a requirement in §0, which is why each is
deleted rather than scoped:** ONE four-line sentence from §6's wiring comment (`CF31-1`); the
word `ordered` at SIX citations and the word `rule` at TWO, EIGHT words in all (`CF31-3`); ONE
clause carrying one figure from round 30's closing paragraph (`CF31-4`); §4's prefix universal
with its fourteen-name enumeration and its `zero exceptions` token, plus the word `prefixed`
and the `a thing every other member supports` clause that restated it, plus §8(o)'s pointer at
it — FOUR removals of one predicate (`OP31-1`); `or stderr` from §4's RULE (`OP31-2`); the
`; the shipped copy is not the current cache` clause (`OP31-3`); and the `<path>: ` slot from
BOTH lines of §4's internal-error block together with that block's omission-branch sentence
(`OP31-4`). ONE disposition is policy step 3 and moves BYTES rather than a claim: §6 row 26's
own text (`CF31-2`). **NO residual is added and none widened;** the preamble's residual
enumeration is CORRECTED from a false closed pair to three, which states a bound rather than
creating one. **COUNTS THIS EDIT CHANGES, and their sites:** round 30's closing paragraph LOSES
`3 → 4`; the preamble's residual enumeration goes `Two` to `THREE` and gains **66**, **71**,
**22** and **28**, all four sha-pinned to `git show 55666e8:<file>` beside the regex that
produced them, per `CF13-6`, and all four are RAW `grep` match counts and not distinct
citations — the distinct figures are 18, 22, 5 and 6, stated here so a later round re-deriving
with `sort -u` does not read a moved count as damage; §4's prefix paragraph LOSES its
`14`-member enumeration and its `zero exceptions` as LIVE claims, both surviving only inside
quotations of the superseded text. **Counts NOT moved, re-derived rather than restated:** §1's
**49** battery rows and its `~680` estimate, no row, twin, fixture or invocation being added
and the prescribed test file losing three COMMENT lines and no assertion (`OP22-3`); §4's
`check:all` MEMBERSHIP measurement, re-derived unmoved 2026-08-22 against the fourteen-member
chain — **14** members, `check:tarball` at **12**, `check:install-smoke` at **13**, this check
as FIRST member making **15**; §4's FOUR exit-table rows, EIGHT kinds, NINE mandated reasons,
EIGHT remedy rows and five recipe steps with four carve-outs; §5's `THIRTEEN of fourteen`,
`THREE jobs` and `ten members`, re-derived independently — thirteen members carry a CI step,
`check:claim-scope` carries none, `engine-pin-lint` one at `:244`, `meta-checks` ten,
`packaging` two; §5's **6** and **9** censuses and its `ten` workflow-file population; §5.1's
**five**, **four**, **204** and line **5**, `package.json:20-23` re-read unchanged; §0's
requirement 10 row's `TWO of those five`; §3's **18**, **21 of 30** and `29 PATHS`; §8(l)'s
**NINE** and §8(o)'s **7** hits in **4** files across **14** member scripts; §6's SEVEN
`placedCorrectly` conjuncts and SEVEN mutants; and §7's thirteen rows. No numeral is written
immediately before `mutant` or `mutants` by this apply, so round 26's mutant-count census is
unread by it. The `see above`/`see below` and `TODO`/`TBD`/`FIXME`/`<insert` sweeps below
`## 0.` were re-run and return only backticked quotations used as history, so round 10's
"guard what an apply INSERTS, not only where" finds no live site. **ALL FOUR standing checks
re-run on the FIXED file:** the anchor grep, passes 1, 2 and 3, the escape-aware row census and
the prettier plus `norm()` plus `diff` check all return nothing, and a second prettier run is
byte-identical. **No working-tree mutation:** `scripts/bundle-cli.mjs` was never imported;
every probe ran `git`, `grep`, `awk`, `sed`, `node -e` over `package.json`, or `python3`
read-only over files as text; prettier ran only over COPIES in the scratchpad; and
`git status --porcelain` was empty before and after.

**Round 32 (2026-08-22) — `contract-fit` / `correctness` / `operability`, each run as the SAME
COVERAGE MATRIX as rounds 18-31 and each enumerated INDEPENDENTLY of round 31's list rather
than as a check that its fixes landed. `contract-fit`: **57** obligations, the distinct
(requirement, cited-section) pairs §0's ELEVEN requirement rows name, re-derived by
deduplicating a section-sign extraction over each row's third cell — ZERO uncovered.
`correctness`: **30** over §3.3's cli surface — its THREE clauses, clause 1's TWO metafile
branches, its ELEVEN artifact-set members, the unit-state table's THREE rows, the
`dist`-contents reason table's FIVE and the cli reason table's SIX — ONE uncovered.
`operability`: **65** — §4's EIGHT exit-code obligations, being its FOUR exit-table rows under
the default and under `--strict`, its EIGHT per-kind line shapes, its NINE mandated coverage
reasons, its EIGHT remedy rows and its FIVE ordered-recipe steps; §7's THIRTEEN rows; and the
preamble's TEN standing checks, the citation guard's THREE stated residuals and its
four-digit-form refusal — ONE failed, FALSIFIED by measurement rather than uncovered. FOUR
findings, all re-derived read-only at `944fc9d` on a clean tree and all dispositioned
`fixed`.**
⚠️ **The owner's instruction round 31 acted on FIRST governs this round too: before proposing
a scope statement, ask whether the obligation should EXIST.** THREE of the four dispositions
are policy step 1, DELETE; the fourth is policy step 3 and names an element for a mandate that
had none. **NO battery row, NO twin, NO fixture, NO mutant and NO invocation is added, nothing
is renumbered, and no kind, reason spelling, coverage status or exit code is added or
removed** — so §1's **49** stays correct (`OP22-3`) and §6's sha-pinned outside-citer registry
needs no re-run, renumbering being its only trigger.

- **`CF32-1`, low, scope SUBSTANCE — §6's wiring block asserted two `check:all` members this
  change set neither adds nor edits, and what it asserted of them was entailed by the line two
  above it.** The comment read "the two dist-reading members §5 orders it before: present, and
  after it" over an `indexOf` of `npm run check:tarball` compared greater than zero, and the
  same for `check:install-smoke`. ⚠️ **VACUOUS BY THIS SECTION'S OWN STANDARD, re-derived
  read-only 2026-08-22 at `944fc9d`** — `node -e` over `package.json`'s `check:all`, split on
  the chain separator: **14** members, `check:tarball` at **12** and `check:install-smoke` at
  **13**, matched exactly by those two literals; and the `members[0]` equality two lines above
  already forces every OTHER member to an index of at least 1, so a greater-than-zero test
  reduces to PRESENCE and the "after it" half of the comment was carried by that line and by
  nothing here. Every implementation of this design passed both — the appended-last, no-flag
  implementation `CF9-3` was filed for fails `members[0]` and nothing else, and so do a CLI
  hardcoding a true `strict`, a `units.mjs` importing `enumerateWorkspaces` and a bundler with
  no metafile write. Their only discriminating power was against `check:tarball` or
  `check:install-smoke` being REMOVED from `check:all`, which is not an implementation of this
  design at all. ⚠️ **AND THEY COULD FAIL A CORRECT IMPLEMENTATION** — `OP16-3`'s and
  `CF20-1`'s class. Both literals pinned member TEXT this change set does not own: let a later
  change set chain `npm run check:tarball` with a flag after it — this repository already
  flags a check inside its own definition, `check:claim-scope` at `package.json:44` — and with
  this design correctly implemented the index is -1 and the assertion fails on a wiring that
  is exactly right. §6 refuses that shape in terms elsewhere: the live smoke test derives both
  sides from the live tree "so no literal is pinned and it still cannot rot as the repository
  grows", and this block already declines to assert a member COUNT for the same reason, one
  line below. Fixed by DELETE (policy step 1) of the comment and both assertions. NOTHING
  TRACED TO THEM: requirement 10 is "wire into `check:all`", §5's ordering claim is discharged
  IN FULL by the `members[0]` equality, and the presence of two other members in `check:all`
  is a repository fact this design does not own. **SITES:** §6's wiring block alone. §0's
  requirement 10 row, §1's `package.json` row, §1's THREE-EXISTING-FILES paragraph, §5's
  `CF9-3` bullet and §9's round-9 `CF9-3` entry are CHECKED and NOT touched: every one of them
  cites `members[0]`, never these two assertions.
- **`C32-1`, high, scope SUBSTANCE — clause 1's chunk member is specified in TWO branches, and
  the branch every tree in this repository takes today had no element.** §3.3 states that
  member as the chunk-pattern-matching entries of the marker's outputs when the marker exists
  and as "at least one file matching the pattern" when there is no PARSED metafile, and §2's
  `cliArtifactGaps` comment states the same split. §6 held an element for the first branch
  alone: rows 25 and 33 both start from row 11's COMPLETE cli, so both carry a marker and both
  read the SET. A `cliArtifactGaps` resolving that requirement as the marker's outputs when
  there is a metafile and as NOTHING when there is not — an EMPTY requirement, which nothing
  can be missing from — passed both of them, the `C10-4` conjunction and §8(p) partial-copy
  bullets, rows 10, 12, 21, 22's twin, 39's negative twin and 46's marker-ABSENT twin, and the
  live smoke test, which asserts the unit SET and the coverage SHAPE and explicitly not the
  finding set. Every element reaching clause 1 with the marker absent holds a chunk-pattern
  file PRESENT, so an empty requirement is missing nothing on every one of them. ⚠️ **MEASURED
  read-only 2026-08-22 at `944fc9d`, `git status --porcelain` empty before and after and
  `scripts/bundle-cli.mjs` never imported:** `ls -d packages/cli/.bundle-meta` reports no such
  file while `packages/cli/dist` holds `bin.js`, `index.js`, `bin/supervisord.js`,
  `index.d.ts`, `plugin/` and FIVE chunk-pattern files, so the fallback is the branch every
  developer's tree takes until the first post-adoption build — the state §6's live-smoke
  paragraph and §7's day-one row both already measure. Apply the ALL-CHUNKS deletion row 25 carried in round 32 —
  `rm packages/cli/dist/chunk-*.js packages/cli/dist/run-dispatcher-*.js`, that row's
  mutation until round 33 narrowed it to the second family alone (`C33-1`) — to it and
  the design's reading is an `unbuilt packages/cli` line with the `bundle:cli did not finish`
  reason and the chunk named, exit **1** under `--strict` at `check:all` member 0; that body
  instead reports `compared`, one advisory and **exit 0 even under `--strict`** on a
  `dist/bin.js` whose first import statement names a chunk that is gone — verbatim `C11-6`'s
  own harm, at the branch `C11-6`'s fix created. **Fixed by policy step 3, a STRENGTHENED
  VALUE, no deletion and no narrowing being available at the mandate:** the false-negative
  battery's marker-absent gap bullet already existed and already held the marker absent, so
  only its UNSPECIFIED gap moves — EVERY chunk-pattern file absent with every other member of
  §3.3's artifact set present. The wrong body then reports `compared` plus the advisory and
  fails on that bullet's own fixture, needing no mutant swap. ⚠️ **Standing rule 8, checked at
  the fix:** the bullet's subject is disjointness, an implementation emitting BOTH kinds still
  fails on the narrowed fixture, and a CORRECT implementation is not failed — clause 1's
  fallback finds no matching file, the unit is not `compared`, and §3.2's gate on a
  `packages/cli` that is not `compared` suppresses `bundle-provenance-missing` exactly as the
  bullet requires. §8(i)'s third cost is CHECKED and NOT touched: it already quotes the "at
  least one file matching the pattern" fallback and works its example at ONE chunk of five, so
  the ZERO case it now excludes was never inside it. **SITES:** §3.3's clause-1
  no-parsed-metafile sentence; §6's false-negative battery's marker-absent gap bullet; §3.2's
  chunkless-agreement sentence, the second site of the same predicate, where the element is
  named; and §2, where the restatement of that sentence is DELETED rather than given a third
  copy of the citation (standing rule 8 — one predicate, three sites). §0, §1, §3, §3.1, §3.4,
  §3.5, §4, §5, §5.1, §7 and §8 are CHECKED and NOT touched, as are §6's rows, its wiring
  assertions and its live smoke test.
- **`CF32-2`, low, scope META — §9's round-31 `CF31-2` bullet predicted a census figure for the
  document it was published in, and the prediction was false on arrival.** It read: because
  this bullet quotes the broken command once above, that census re-runs at **34** after this
  apply and not 33. ⚠️ **Re-derived read-only 2026-08-22 at `944fc9d`, `git status --porcelain`
  empty before and after:** the letter-backtick-letter census returns **33** over the whole
  file, and **33** again scoped below the `## 0.` heading and again with §6 row 26's line
  excluded — all three readings, never 34. That this IS the census the bullet names is
  confirmed by the bullet's own other figure, re-derived exactly: row 26 alone returns **0** at
  `187eb2d` and **40** at `55666e8`. MECHANISM: the quotation the plus-one rested on closes its
  code span before a SPACE, so it is not a letter-backtick-letter adjacency and adds **0**, and
  the whole `CF31-2` bullet region returns **0**. The bullet's own next sentence says why,
  three lines later — that census is "narrower than the damage — **40 of 66**" — and the form
  it quoted is one of the 26 the census provably cannot see. So the bullet predicted a movement
  from a quotation its own named census is BLIND TO, one bullet before `CF31-4` deleted a
  file-wide figure for being wrong in the commit that wrote it. Fixed by DELETE (policy step 1)
  of the whole prediction clause. CORRECTING it to 33 was REFUSED for `CF13-6`'s and
  `CF31-4`'s own reason: a file-wide literal about the CURRENT document is stale at the next
  round that writes an English-suffix code span, and this document adds them routinely. NOTHING
  IS LOST — the quotation-versus-damage convention is stated at its other site, in round 31's
  own numbers paragraph. ⚠️ **SECOND HALF, policy step 2, NARROW: the SURVIVING 33 did not
  re-derive from the words beside it either.** Same census: the whole file is **35** at
  `187eb2d` and **76** at `55666e8`, row 26 is **0** and **40**, and `CF31-1`'s comment is
  **2** and **3**. So "the rest of the file" silently excluded `CF31-1`'s comment as well as
  row 26, and a later round re-deriving it as written gets 35 and 36 and reads a correct figure
  as damage. The exclusion is now NAMED and the arithmetic stated beside it, both sha-pinned
  per `CF13-6`. **SITES:** §9's round-31 `CF31-2` bullet alone.
- **`OP32-1`, low, scope META — the preamble's four-digit-form refusal states this file's own
  line count as an absolute literal, and that literal was already wrong in the commit that
  wrote it.** The clause read: this file is **7306** lines and has grown about 290 a round for
  twelve rounds. Re-derived read-only at `944fc9d`, `git status --porcelain` empty before and
  after and `scripts/bundle-cli.mjs` never imported: `wc -l` over this file returns **9450**.
  The literal was wrong at its own apply — `git show 2c203a0:<file> | wc -l` returns **7678**
  at the commit that WROTE the sentence, and the figure it published is the PREVIOUS commit's
  count, `git show e99b066:<file> | wc -l` — which is `C11-4`'s pattern and verbatim
  `CF13-6`'s 2574-against-3092 one screen above it in this same preamble. ⚠️ **The consequence
  is not cosmetic, because that sentence IS the refusal's headroom argument:** a reader
  re-deriving the refusal from the stated figures gets nine rounds of room before this file's
  own anchors go five-digit, while measured the file is **549** lines short of 9999 and the
  last eleven applies added **3454** lines, a mean of **314** — `git show <sha>:<file> | wc -l`
  over `3095116`, `5fef126`, `53996f5`, `8fdf3ed`, `7837d25`, `e99b066`, `2c203a0`, `e70c9bb`,
  `c672565`, `187eb2d`, `55666e8` and `944fc9d` — so the real headroom is under TWO rounds and
  the clause understates its own residual's urgency by a factor of five at the one sentence
  that says when that residual expires. Fixed by DELETE (policy step 1), which is `C11-4`'s
  own remedy, round 13's disposition of the 2574 baseline (`CF13-6` / `OP13-5`) and round 31's
  disposition of this exact class one round earlier (`CF31-4`): the absolute count and the
  rate-and-span pair go together, and the blindness the sentence states survives with no figure
  in it for a later apply to invalidate. CORRECTING it to **9450** is REFUSED for the reason
  round 13 refused correcting 2574 — a file-wide literal written here is stale in the same
  apply that writes it, and this one provably was. NARROW and STRENGTHEN were checked first and
  refused: narrowing to a sha-stamped figure reintroduces a literal the next apply invalidates,
  and there is no VALUE to strengthen, the clause mandating nothing and pinning nothing. If a
  figure is ever restored it carries its `git show <sha>:<file> | wc -l` command, which is
  `CF13-6`'s standing ruling. ⚠️ **Standing rule 9 in the same pass:** round 31 deleted one
  file-wide literal of this class and left this one standing, though it is both older and
  further out, which is the eleventh consecutive round to find a completion claim only partly
  true. **SITES:** the preamble's four-digit-only refusal sentence, the only LIVE one, and this
  entry. §9's round-26 entry keeps BOTH of its occurrences — one sha-attributed to `e99b066`
  and one stating what round 26 ruled at that sha — because this section preserves superseded
  figures by design.

**Round 32's numbers.** `contract-fit` **57** obligations, ZERO uncovered; `correctness` **30**,
ONE uncovered; `operability` **65**, ONE failed. All four dispositioned `fixed`, and neither
`contract-fit` finding is a coverage gap — both are the opposite class, an element that
discharges NO obligation and a §9 figure that was false in the commit that published it.
**DELETIONS, counted because the owner asked for them explicitly — FOUR removals at FOUR
sites, and not one of them traces to a requirement in §0, which is why each is deleted rather
than scoped:** ONE comment line and TWO assertions from §6's wiring block, requirement 10
being discharged in full by the `members[0]` equality beside them (`CF32-1`); ONE prediction
clause carrying one figure from §9's round-31 `CF31-2` bullet (`CF32-2`); this file's absolute
line count with its per-round growth rate and the span that rate was measured over, THREE
figures in ONE clause of the preamble's citation guard (`OP32-1`); and ONE
twenty-two-word restatement of §3.2's chunkless-agreement provenance from §2, replaced by a
pointer at the section that carries the argument in full, requirement 3's row being about the
metafile-derived INLINED SET and not about clause 1's chunk membership (`C32-1`). **NO
residual is added and none widened.** **COUNTS THIS EDIT CHANGES, and their sites:** above the
`## 0.` heading the literal `7306` goes **1** to **0** and the string `290 a round` goes **1**
to **0**, both by an `awk` cut at that heading piped into `grep -c -F` with the string; inside
§9 they go **2** to **4** and **1** to **3**, this entry naming each on two lines, its
quotation of the deleted clause and this declaration; so file-wide they go **3** to **4** and
**2** to **3**, stated only so a later round re-deriving them does not read a moved count as
damage — the pair SCOPED above the `## 0.` heading is the claim, per `CF31-4`'s own lesson
that a round's own §9 entry re-inflates a file-wide count. §9's round-31
`CF31-2` bullet LOSES **34** as a LIVE figure, its only live site — after this apply no claim
about the current file carries it, the bolded-34 grep returning §6's row-number bullet, where
**34** is a battery ROW NUMBER and stays, plus this round's own quotation of the superseded
figure — and GAINS **35**, **2**, **76**, **40** and **3** as the arithmetic behind the
surviving **33**, all five sha-pinned to `187eb2d` and `55666e8` beside the census that
produced them, per `CF13-6`. Round 31's numbers paragraph carries neither **33** nor **34**
(checked) and is untouched, as is round 30's entry. **Counts NOT moved, re-derived rather than
restated:** §1's **49** battery rows and its `~680` estimate — NO row, twin, fixture, mutant
or invocation is added or removed and no kind, reason spelling, coverage status or exit code
moves, the prescribed test file losing ONE comment line and TWO assertions that assert nothing
(`OP22-3`) and one free value in a false-negative bullet becoming specified — so §6's
sha-pinned outside-citer registry needs no re-run, renumbering being its only trigger; §4's
and §5's `check:all` MEMBERSHIP measurement, RE-DERIVED unmoved 2026-08-22 against the
fourteen-member chain — **14** members, `check:tarball` at **12**, `check:install-smoke` at
**13**, this check as FIRST member making **15**; §3.3's ELEVEN artifact-set members, its
THREE unit-state rows, its FIVE `dist`-contents rows and its SIX cli rows; §4's FOUR
exit-table rows, EIGHT kinds, NINE mandated reasons, EIGHT remedy rows and five recipe steps;
and §7's THIRTEEN rows. Each was re-counted off its own table on THIS document rather than
carried forward from round 31's entry (standing rule 2); `~680` is an estimate of a file this
design has not written and is not a count. No numeral is written immediately before `mutant`
or `mutants` by this apply, so round 26's mutant-count census is unread by it. The
`see above`/`see below` and `TODO`/`TBD`/`FIXME`/`<insert` sweeps below `## 0.` were re-run
and return only backticked quotations used as history, so round 10's "guard what an apply
INSERTS, not only where" finds no live site. **ALL standing checks re-run on the FIXED file:** the
anchor grep, passes 1, 2 and 3, the escape-aware row census, the orphan-row sweep, the
comma-then-blank sentence-split sweep and the prettier plus `norm()` plus `diff` check all
return nothing, and a second prettier run is byte-identical. **No working-tree mutation:**
`scripts/bundle-cli.mjs` was never imported; every probe ran `git`, `grep`, `awk`, `sed`,
`wc`, `ls`, `node -e` over `package.json`, or `python3` read-only over files as text; prettier
ran only over COPIES in the scratchpad; and `git status --porcelain` was empty before and
after.

**Round 33 (2026-08-22) — `contract-fit` / `correctness` / `operability`, each run as the SAME
COVERAGE MATRIX as rounds 18-32 and each enumerated INDEPENDENTLY of round 32's list rather
than as a check that its fixes landed. `contract-fit`: **57** obligations, the distinct
(requirement, cited-section) pairs §0's ELEVEN requirement rows name, re-derived by extracting
every section sign from each row's third cell and deduplicating per row — 8, 7, 9, 1, 2, 3, 7,
7, 6, 3 and 4 across requirements 1-11 — ONE uncovered, the (requirement 9, §3 input rule 4)
pair at clause (i). `correctness`: **32** over §3.3's cli surface — its THREE clauses, clause
1's TWO metafile branches, its ELEVEN artifact-set members, the chunk member's TWO NAMING
FAMILIES (the refinement round 32's **30** did not carry, §3.3's pre-check paragraph having
stated the split in terms while nothing in §6 could tell the two apart), the unit-state
table's THREE rows, the `dist`-contents reason table's FIVE and the cli reason table's SIX —
ONE uncovered. `operability`: **20**, taken per KIND rather than per step because that is the
granularity the gap lives at (standing rule 4) — §4's FIVE ordered-recipe steps, its FOUR
carve-outs, its EIGHT remedy rows and step 1's THREE selector kinds — ONE uncovered. THREE
findings, all re-derived read-only at `2ef2ce5` on a clean tree and all dispositioned
`fixed`.**
⚠️ **The owner's first question was asked BEFORE each fix was written.** All three obligations
survive policy step 1: none is entailed by a neighbour, none is restated in full elsewhere,
each bounds a case this repository can reach, and an implementer provably acts differently on
each. **DELETIONS: ONE, at ONE site** — §6 row 25's `this is the only row that makes the chunk
member non-vacuous`, which traces to no requirement in §0 and had been false since round 12
appended row 33. Policy step 2, NARROW, had nothing to narrow at any of the three; all three
dispositions are policy step 3, STRENGTHEN A VALUE.

- **`CF33-1`, medium, scope SUBSTANCE — clause (i) of §3's input rule 4 was a MANDATED
  CONJUNCT that EVERY element AGREED on, which is `CF25-1`'s formula recorded one clause over
  in §3's own words.** Rounds 24 to 27 gave clauses (ii) and (iii) an element apiece on each
  side; clause (i) had none, and §3 closed that paragraph with `No claim is made here about
clause (i)'s coverage`. The cheapest wrong body spells it as PATH CONTAINMENT —
  `allConfigs.filter((c) => c.startsWith(unitDir + "/"))`, `unitDir` being a PATH and
  `descendantConfigs`' first argument — keeping (ii) and (iii) exactly as §3 states them.
  Every rule-4 config in this design sat INSIDE its unit's directory (row 47's under
  `packages/A`, row 3's under `packages/cli`), so location and chain agreed on all of them and
  that body passed both halves of rows 3 and 47, round 27's two mutants, the false-negative
  battery, the wiring assertions and the live smoke test. ⚠️ **Re-derived read-only 2026-08-22
  at `2ef2ce5`:** of the **30** tracked `tsconfig*.json`, exactly ONE has a chain resolving
  into a unit — `packages/cli/tsconfig.dts.json`, whose chain is `packages/cli/tsconfig.json`
  then `tsconfig.base.json` — and it sits UNDER `packages/cli`, so the live tree cannot
  discriminate either and rule 4 has **0** members under BOTH readings. The harm runs BOTH
  ways. FALSE NEGATIVE, the founding failure's shape: a root-level `tsconfig.gates-emit.json`
  extending `./packages/gates/tsconfig.json` with `"outDir": "./packages/gates/dist/emit"`
  satisfies all three clauses, is in NO input set under that body, and a `touch` of it prints
  PASS, exit **0 even under `--strict`** at `check:all` member 0 and under flagless `pretest`,
  on a `packages/gates/dist` compiled under the old `compilerOptions` — and such a config IS
  enumerated, `isVariantTsconfig` admitting any `tsconfig.*.json` basename
  (`repo-census.mjs:75-79`) while the walk pushes a root-level variant through its
  `rel === ""` branch (`:316-318`), which is why `tsconfig.base.json` is in
  `variantConfigFiles` today. FALSE POSITIVE the other way: a
  `packages/gates/tsconfig.standalone.json` with NO `extends`, `"composite": true` and
  `"outDir": "./dist"` fails (i) correctly, while that body attaches it and prints `stale-unit
packages/gates` with `rm -rf packages/gates/dist` plus a 19-unit rebuild. FIXED at the one
  value every element left free, the rule-4 config's LOCATION: §6 row 47's MAIN half moves OUT
  of the unit directory to `tsconfig.emit-into-A.json` at the FIXTURE ROOT, declaring
  `"extends": "./packages/A/tsconfig.json"` — so `composite` is still INHERITED-TRUE and round
  24's pin is preserved — and `"outDir": "./packages/A/dist/emit"`, which resolves against the
  config's own directory to the SAME `packages/A/dist/emit` the cell resolved before, a STRICT
  subdirectory reached by no minus-bullet, so round 27's accepting-side property is unchanged.
  The verdict is UNCHANGED: `stale-unit` with the reported set EQUAL to the single `Unit.dir`
  `packages/A`. ⚠️ **`OP16-3`'s and `CF20-1`'s disqualifier checked AT the fix:** a CORRECT
  three-clause body accepts the relocated config on all three clauses and the row's verdict
  still holds, so no correct implementation is failed. ⚠️ **Standing rule 8, checked at the
  fix:** round 27's two mutants still fail this row — `descendantConfigs` returning the empty
  array leaves the fixture clean, and the EQUALITY reading declines `packages/A/dist/emit` —
  and the ROUND-25 TWIN is UNTOUCHED at `packages/A/tsconfig.emit-elsewhere.json`, keeping its
  clause-(ii) failing-side job, its clean-in-both-states verdict and its own two mutants;
  `packages/A/dist/emit/` is still never written, so §3's output set, §3.1's stem bijection and
  rows 14 and 44's `eo-*-fixture-*` elements are untouched. ⚠️ **The proposal's own site list
  was wrong on one point and is corrected here (standing rule 2):** it said §0's requirement 9
  row `spells the outDir property, not the file name, so its literals do not move`, but that
  row spells `"./dist/emit"` as row 47's `outDir` `from round 27`, a LIVE reading, so the
  literal moves there too. ⚠️ **A SECOND correction, made in consolidation:** the proposal
  named row 47's main half `clause (i)'s FAILING side`, which contradicts §0's own vocabulary —
  a FAILING side is a config the clause EXCLUDES, as row 3 is for (iii) and the round-25 twin
  is for (ii) — and this config SATISFIES (i). It is the ACCEPTING side against the
  PATH-CONTAINMENT reading, in round 27's own phrasing one clause over, and §0 is written that
  way. **SITES, all moved together per standing rule 9:** §0's requirement 9 row, at the
  element list and at that literal; §2's `descendantConfigs` comment, at the criterion
  sentence, the fifth-body literal and a SIXTH wrong body; §3's Units step inside `C21-1`'s
  **19 of 19** sentence; §3's rule-4 three-clause statement, its rows-3-and-47 pairing, its
  round-27 accepting-side sentence, its round-25 twin sentence and its closing clause-(i)
  paragraph; and §6 row 47 at its fixture cell (twice), its mutation cell, its verdict cell
  (twice) and its twin's three back-references.
- **`C33-1`, medium, scope SUBSTANCE — the chunk pattern's SECOND naming family decides
  clause 1's required set and was exercised by NOTHING.** §3.3's pre-check paragraph states
  the breadth in terms — the pattern matches all five live artifacts, "i.e. **both** naming
  families esbuild produces under `splitting: true`" — and that breadth decides clause 1's
  REQUIRED chunk set. Re-derived read-only at `2ef2ce5`:
  `awk '/^## 6\./,/^## 7\./' <file> | grep -c 'A-Z0-9'` returned **0** — whole file **4**, at
  §2, §3.2, §3.3's over-match bound and §9's round-1 history — and the same slice piped to
  `grep -on 'run-dispatcher'` returned **2** hits, BOTH on ONE line, row 25's mutation cell. An
  `isChunk` keyed on the `chunk-` PREFIX rather than on the `-[A-Z0-9]{8}\.js$` SUFFIX
  therefore passed every element: row 5's two chunk-pattern files are `T_old`/`T_new` with no
  names pinned; rows 11, 33 and the complete-cli-build rule say "5 chunks" or "a chunk-pattern
  file" with no family pinned; row 33 deletes three of the four chunks `dist/bin.js` imports,
  ALL first-family, so its gap set is identical under both readings; row 25 deleted BOTH
  families at once and names its gap descriptively rather than by equality, so a prefix body
  naming four gaps satisfied it exactly as a correct body naming five did; row 46's chunk set
  is empty; the false-negative battery's marker-absent gap bullet removes EVERY chunk-pattern
  file; and the live smoke test asserts the unit SET and the coverage SHAPE and explicitly not
  the finding set. **The wrong result, on this repository's own artifact set:** measured
  read-only at `2ef2ce5`, `packages/cli/dist` holds **five** files matching the pattern and
  **four** carrying the `chunk-` prefix, `run-dispatcher-POLZZ2DH.js` being the fifth. On a
  marker-PRESENT tree whose `outputs` name all five — which the complete-cli-build rule already
  binds every cli-scoped fixture to, row 25 among them — delete that one file: the correct body
  finds a chunk-pattern entry of `outputs` with no file on disk, names the gap and reports
  `unbuilt packages/cli`, exit **1** under `--strict` at `check:all` member 0; the prefix body
  finds it in neither its derived set nor its fallback, clauses 2 and 3 still hold — deleting
  an output LOWERS the newest output mtime, this design's own argument for retaining clause 3
  — and it reports `compared` with ZERO findings, **exit 0 even under `--strict`**, on a
  `packages/cli` short one esbuild output. Verbatim `C11-6`'s harm at the naming family
  `C11-6` did not name, in `C18-1`'s and `C20-1`'s shape: mandated in §2 and §3, required by
  no fixture. **Fixed by a STRENGTHENED VALUE.** DELETE was checked and REFUSED — the breadth
  is load-bearing, deciding clause 1's required set, and dropping it re-opens the founding
  failure; NARROW was refused — §3.2's Rule is correct as written and §3.3's own bound already
  states the over-match side. §6 row **25** is the host, its mutation cell already naming both
  families: its fixture's chunk set is pinned FOUR-plus-ONE by family, its MUTATION VALUE
  becomes the second-family file deleted ALONE, and its verdict takes the prefix-keyed mutant.
  ⚠️ **`OP16-3`'s and `CF20-1`'s disqualifier, checked BEFORE the clause: it cannot fail a
  correct implementation** — the deleted file is a chunk-pattern entry of that fixture's own
  marker `outputs`, so §3.3's clause 1 mandates `unbuilt` there in terms, and the four
  surviving chunks, the three entry outputs, `dist/index.d.ts` and the six plugin entries keep
  every other comparison clean. ⚠️ **Standing rule 8, checked both ways:** row 33's
  three-of-four mutation and its "at least one" mutant are a different row and a different
  mutant and are untouched; row 25's ORIGINAL subject survives — a body with NO chunk member at
  all still reports `compared` on the single deletion and still fails this row — and the fully
  chunkless state stays covered by the false-negative battery's marker-absent gap bullet
  (round 32, `C32-1`) and by row 46's empty chunk set, so nothing row 25 carried alone is lost.
  The alternative host — row 33 with its gap-set EQUALITY widened to the second family — was
  considered and NOT taken: its subject is the metafile-derived SET, and row 25's mutation cell
  already names both families. ⚠️ **The `bundleAt` half is NOT closed, and the row says so
  rather than overclaiming:** re-derived read-only 2026-08-22, all five chunk-pattern files
  carry the identical mtime **1787401912826.4854**, one build having written them, so a
  minimum over four equals the minimum over five on this tree and the live smoke test cannot
  see it either. Row 5 was checked as a second host for that half and NOT taken — its subject
  is the MINIMUM, not the family, and an element for a harm no measurement on this tree reaches
  is exactly the class round 32 deleted. ⚠️ **One claim goes with the rewrite because it was
  ALREADY false:** row 25's "this is the only row that makes the chunk member non-vacuous"
  predates round 12's row 33, on whose fixture a body with no chunk member at all also reports
  `compared` and also fails — `C11-4`'s class at an exclusivity claim rather than at a count.
  **SITES, all moved together per standing rule 9:** §6 row 25's fixture, mutation and verdict
  cells; §3.3's pre-check paragraph, where the two-family measurement now names its element;
  and §9's round-32 `C32-1` bullet, whose "Apply row 25's OWN mutation to it" is re-pointed to
  the literal command it meant — a POINTER this apply re-targets, never a superseded figure §9
  may keep.
- **`OP33-1`, medium, scope SUBSTANCE — §4 step 1 selects on THREE kinds and every
  plan-asserting fixture in §6 exercised ONE.** Round 25's `OP25-1` closed step 1's PRESENCE at
  §6 row 28's PLAN TWIN and left its KIND SET open; round 21's row-21 mutant closes only the
  OVER-broad direction (a trigger keyed on the finding SET) and never the under-broad one.
  Re-derived read-only over this document as it now stands: every §6 element asserting a
  RENDERED remedy plan sits in the battery table — rows 21, 28, 34, 35, 38, 39, 42 and 46 —
  and NOTHING after it, the false-negative battery, the wiring assertions and the live smoke
  test holding no plan clause at all; of those eight, `orphan-output` and `unbuilt` occur in
  FOUR cells and every occurrence is prose about the kind's ABSENCE from that fixture. So
  `stale-unit` was the only kind that ever selected step 1 anywhere in this battery. WRONG
  IMPLEMENTATION: a plan assembler whose step-1 selector is
  `result.findings.filter((f) => f.kind === "stale-unit")` — the reading §4's worked example,
  its live four-`stale-unit` prediction and the one plan fixture that reached step 1 all
  invite. It passes rows 21, 28 and its twins, 29, 30, 34, 35, 36, 38, 39, 42, 43's CLI twin
  and 46, the false-negative battery, the wiring assertions and the live smoke test. WRONG
  RESULT, on §4's own measurements: an `orphan-output` prints `npm run build` then step 5,
  while §4's remedy row for that kind reads "neither `npm run build` NOR `tsc -b --clean`
  clears this" — so the re-run at step 5 reports the identical finding, verbatim §4's "the
  check fires again, identically, forever", at `check:all` member 0 and at line 5 of §5.1's
  measured 204 under flagless `pretest`. That is `CF14-1`'s class at the step round 25 added to
  prevent it, and the same holds for a non-cli `unbuilt`, whose remedy row measures plain
  `npm run build` as re-emitting nothing for the 18 tsc-only units. **FIXED by a fixture clause
  plus a mutant on a twin that already exists, in round 30's `C30-1` shape: NO row, NO twin, NO
  fixture row and NO invocation is added.** **DELETE was checked FIRST and REFUSED:** dropping
  `orphan-output` or `unbuilt` from step 1's selector would ship the defect itself — the first
  traces to §0's requirement 2 through §3.1, the second to §3.3's `unbuilt` verdict, and BOTH
  carry a §4 remedy row measured as unclearable by `npm run build` alone, with the printed plan
  as the only surface by which that remedy reaches an operator (§4's table being "the ordered
  plan's SOURCE, never a printed line"). ⚠️ **The proposal's fixture clause is TIGHTENED in
  consolidation (standing rule 7):** it ordered the added orphan unit's outputs against its
  SOURCES alone, which leaves rule 3's chain free and lets the same fixture fire `stale-unit`
  on that unit and falsify the cell's own `orphan-output` and NOT `stale-unit` claim; the
  clause now orders those outputs against the unit's own `tsconfig.json` and chain as well,
  which is row 42's SCOPE TWIN's wording copied WHOLE. ⚠️ **A SECOND consolidation change:**
  §4's step-1 pointer is APPENDED after that step's "which is why the element had to be a NEW
  twin" sentence rather than spliced into the middle of its ABSENCE clause, whose four-element
  em-dash list the proposed insertion would have re-pointed — `CF21-1`'s splice, at a proposal
  rather than at a live edit. ⚠️ **Standing rule 8, checked in BOTH directions AT the fix:**
  the extension is the PLAN TWIN's alone, so row 28's MAIN half, its count-only mutant and its
  N=1 TWIN run the fixture the clean-fixture column states; neither added unit carries a
  `stale-unit`, so the `×2` equality and the N=1 reduction stay exact; the orphan is written at
  DEPTH 0, so row 4 keeps its standing as this battery's one stem-walk recursion row; exactly
  ONE `unbuilt` is added, so row 36's "Rows 28-30, 32 and 34 each render at most ONE `unbuilt`"
  stays true; row 2's "rows 28 and 34 are the only other multi-unit `stale-unit` fixtures"
  stays true, the extension adding no `stale-unit` unit; and the fixture holds no
  `packages/cli`, so the complete-cli-build rule, `C15-2`'s inlined-set rule and row 4's twin's
  `packages/cli`-beside-an-orphan claim are all out of its reach. **SITES, all eight plus this
  entry, per standing rule 9:** §6 row 28's PLAN TWIN carries four — the fixture clause and the
  KIND-SET conjunct, the round-25 step enumeration SCOPED, the round-25 mutant count SCOPED,
  and the `THIRD MUTANT` with `OP16-3`'s disqualifier re-derived over the extended fixture.
  Then §6 row 21's PLAN TWIN, whose non-subsumption reason read "row 28's finding set is
  `stale-unit` throughout" and would have gone FALSE in this commit, and whose kill list gains
  the new mutant; §6 row 34, which cites row 28's twin as where step 1's per-unit shape is
  read; §6 row 4's TWIN, whose "no other fixture" justification enumerates the rows reaching
  the stem side on trees holding no `packages/cli`; §6's plan-first-line prefix enumeration;
  §4's non-vacuity row-set sentence; §4's step-1 line; and §2's remedy-plan comment.

**COUNTS THIS EDIT CHANGES, and their sites — exactly TWO.** First, §6 row 47's MAIN-half
mutant enumeration, TWO → **THREE**, at that row's verdict cell and at its TWIN's
standing-rule-8 sentence — round 27's own pair of sites for the same count, moved ONE → TWO
there (`C27-1`). Second, the `A-Z0-9` census SCOPED to §6 by round 33's own awk slice, **0** →
**1**: that command is `grep -c`, which counts LINES and not hits, and §6 row 25 is ONE line,
so the THREE occurrences this apply writes into it — two in its fixture cell, one in its
verdict cell — move the line count by one and the hit count `grep -on` returns from **0** to
**3**. ⚠️ **The proposal declared that figure as `0` → `2`, counting sites as lines; it is
corrected here, a measurement at the wrong granularity being as wrong as none (standing rule
4), and §3.3's paragraph sha-pins only the BEFORE value it actually measured.** The file-wide
form is deliberately NOT pinned, counting LINES over paragraphs prettier may re-wrap.
**THREE MUTANTS are added, every one of them on a row or twin that already exists:** the
PATH-CONTAINMENT body at §6 row 47's MAIN half, the prefix-keyed `isChunk` at row 25's verdict
cell, and the proper-subset step-1 selector at row 28's PLAN TWIN. ⚠️ **Round 26's §6
mutant-count census is unread by this apply — re-derived **13** at `2ef2ce5` before and
**13** after:** the one numeral-bearing hit this apply touches changes WORD and not count
(`TWO mutants` → `THREE mutants` at row 47's verdict cell), row 28's `TWO mutants` is
PRESERVED verbatim and merely SCOPED to round 25's append, row 47's twin's `BOTH of its
mutants` becomes `ALL THREE of its mutants` and matches no alternative of that command's
pattern, and this round's two other mutants are spelled `Mutant = ` and `THIRD MUTANT`, in row
41's and row 8's `SECOND MUTANT` shape (standing rule 7). Row 47's twin's sha-pinned **10** at
`e99b066` therefore stays as written, this file's convention for a sha-pinned figure.
**Counts NOT moved, re-derived rather than restated:** §1's **49** battery rows and its `~680`
estimate — NO battery row, NO twin, NO fixture row, NO stem, NO output-set member and NO
invocation is added, nothing is renumbered, and no kind, reason spelling, coverage status or
exit code moves (`OP22-3`) — so §6's sha-pinned outside-citer registry needs no re-run,
renumbering being its only trigger; that registry's **36** hits and **19** distinct rows at
`f3aed16` are left as the measurement they are, and it gains row **25** on its next run, §3.3's
new citation being written UNBOLDED so `grep -onE '§6 rows? [0-9]+'` can see it. §4's
non-vacuity plan-row SET stays the SIX rows 21, 28, 34, 38, 39 and 42 and §2's mirror keeps
"Those **six** rows", the operability element landing on a row already in both — hosting it at
row 29, whose fixture SET already holds both end states, would have falsified four SET
statements in the commit that landed it (`C11-4`'s class, `C28-1`'s present-tense shape). §6's
plan-first-line prefix enumeration keeps its four rows (21, 28, 42, 46) and §6's `C20-2`
two-twin row list keeps 42, 21 and 28, row 28 acquiring no third twin. §3's **21 of 30**, its
**20**/**9** split, §0's **29 PATHS**, §8(l)'s **NINE** and §3's Units step's **19 of 19** are
LIVE-tree measurements a synthetic fixture cannot move — re-derived at `2ef2ce5`, the tracked
`tsconfig*.json` number **30**, and rule 4's live member count **ZERO**,
`packages/cli/tsconfig.dts.json` still setting `"composite": false` explicitly and so failing
clause (iii) ALONE and satisfying clause (i) under BOTH readings. §4's FOUR exit-table rows,
EIGHT kinds, NINE mandated reasons, EIGHT remedy rows, FIVE recipe steps and four carve-outs
are unmoved; §3.3's ELEVEN artifact-set members, THREE unit-state rows, FIVE `dist`-contents
rows and SIX cli rows, §6's SEVEN `placedCorrectly` conjuncts and SEVEN mutants, row 27's three
invocations, row 43's CLI twin's four flag settings and §7's THIRTEEN rows were each re-counted
off their own tables. §5's and §5.1's figures are read by no site this apply writes.
**No working-tree mutation:** `scripts/bundle-cli.mjs` was never imported — the
chain-into-unit derivation PARSED the 30 tracked configs rather than importing
`scripts/repo-census.mjs`; every probe ran `git`, `grep`, `awk`, `sed`, `wc`, `ls`, `cat`,
`node -e` reading `fs.statSync`/`fs.readdirSync` under `packages/cli/dist`, or `python3`
read-only over files as text; every write went to the scratchpad; and
`git status --porcelain` is empty at `2ef2ce5` before and after.

**Round 34 (2026-08-22) — `operability`, run as the SAME COVERAGE MATRIX as rounds 18-33 and
enumerated INDEPENDENTLY of round 33's list rather than as a check that its fixes landed.
**62** obligations over §4 alone, derived at this round from the section as it stands: its
**13** mandated FINDING-LINE shapes (the eight kinds, `unbuilt` carrying four reason
variants, `bundle-provenance-missing` two, and the four grouped kinds each carrying the
`×N` collapse), its exit table's FOUR conditions by TWO `strict` columns (**8**), its **15**
non-finding line rules (the prefix, stdout-only, the FOUR order adjacencies, the `PASS`
slot at `M = 0` and at `M ≥ 1`, the `nothing was checked` shape, the STANDALONE slot at
`M = 0` and at `M ≥ 2`, the summary printing exactly once, the coverage line's suppression,
its renderer and its carrying no command), its **4** `--json` surfaces, its **3**
internal-error line rules, and its **19** remedies — EIGHT table rows, FIVE ordered steps,
FOUR carve-outs, the plan block's shape, the plan being the only remedy surface and
`Finding.remedy`'s per-kind VALUE. **TWO uncovered.** Both re-derived read-only at
`5e4918f` on a clean tree and both dispositioned `fixed`.**

⚠️ **Both are round 25/27/28/33's shape — a fixture built to discriminate P whose
INCIDENTAL property Q correlates with P, so a body keyed on Q passes.** Here Q is the
TRIGGER's location in one case and the KIND a lone pinned value happens to be in the other.
**DELETIONS: ONE, at ONE site** — §4's step-3 paragraph's second consequence, "it would also
make §6 row 34's asserted step 4 unreachable on row 34's own fixture", which is a §6
CITATION rather than a claim and traces to no requirement in §0: row 34's main half asserts
NO STEP, which round 25's own `OP25-1` says of that same verdict cell. **COUNTS THIS EDIT
CHANGES: THREE, and all three are RETIRED rather than moved, at ONE site each — all three
inside §2's `formatFindings` REMEDY PLAN comment.** That comment loses "the other four"
(the `OP21-1` plan-presence list, which this round's append makes five), "Those six / rows"
(wrapped across a line break) and "all five rows named here": each counts a SET this round's
own append extends, and `C11-4`'s remedy for a count an appending round re-invalidates is
the SET, never the figure. Their neighbour, "the other five rows", is KEPT and SCOPED to
round 25's append instead, that clause being a claim about the set as it stood.
⚠️ **Re-derived read-only 2026-08-22 over the working copy, per standing rule 3 — the
command is the reproducible part:** `grep -n -o -E "(six rows|five rows)"` returns THREE
hits, ALL of them `five rows` — §2's two above and one in an earlier §9 entry's prose about
a different subject, and this entry's own quotation of the pattern becomes a further §9 hit
at the commit that lands it, §9 legitimately holding text a LIVE census must exclude — the
`six` being invisible to that pattern because the line breaks
between the two words; `grep -n -o -E "\*\*Those six"` returns exactly ONE; and
`grep -n -o -E "the other four"` returns SIX, of which exactly ONE is this list's, §2's,
retired here — the other five being §4's delta-less-kinds paragraph, two §6 battery cells,
one §8 bullet and one §9 entry, none of them about this set. No battery row, no twin, no fixture, no invocation and no mutant-carrying
row is added; nothing is renumbered; no kind, reason spelling, coverage status or exit code
is added — so §1's **49** stays correct (`OP22-3`), §4's `e70c9bb` four-`--json`-site figure
stays correct, and §6's sha-pinned outside-citer registry needs no re-run. **DELETIONS OF
OBLIGATIONS BEYOND THE ONE ABOVE: ZERO** — the three literals retired here are cardinalities
of a set stated in full beside them, never obligations, and each traces to the same
requirement that set does. Round 26's
DELETE-FIRST rule stayed in force: neither obligation survives deletion — the first bounds a
tree this repository reaches by two published scripts, the second a member §2 declares
REQUIRED and §6 row 43 already reads — and NARROW had nothing to narrow at either, so both
dispositions are policy step 3, STRENGTHEN AN EXISTING ELEMENT, with policy step 2 at the
three sentences resting on the first.

- **`OP34-1`, medium, scope SUBSTANCE — §4's rule that the ordered recipe's step 3 stays
  UNCONDITIONAL had NO element for twenty rounds, because the ONE fixture asserting
  step 3 is triggered by a file that no wrong reading looks at.** Round 14 rejected a
  carve-out conditioning step 3 on `newerInput` and recorded the rejection in the
  `stale-declarations` remedy cell; that cell's own OTHER branch — "`npm run build` also
  clears it whenever the newest input is a `packages/*/src/**.ts`" — is the licence a plan
  assembler takes to skip the generator's measured ~5 minutes. Under it, on a run whose SOLE
  finding is a src-triggered `stale-declarations`, §4 step 2's carve-out suppresses step 2,
  the conditioned step 3 suppresses itself, step 4 never fires, and the printed plan is
  `re-run the check` ALONE — a plan that cannot clear the finding it is printed for,
  `CF14-1`'s class at the step §4 added to prevent it, on the tree §4's own step-3 paragraph
  measures as reachable (edit `packages/perf/src`, outside §3.2's inlined 16 and outside
  `packages/cli`'s closure, then `npm run typecheck`). ⚠️ **Re-derived read-only 2026-08-22
  at `5e4918f`, over §6 as delimited by this file's `## 6.` and `## 7.` headings:** the literal
  `remedy plan:` occurs **8** times there and `bundle:types` **5**, and of the five, THREE
  are
  ABSENCE lists (the rendered-text group bullet, row 18's coverage clause and row 34's
  mutant-(b) clause), one is prose about the step's duration and one is inside row 34's
  MUTANT rendering — so no element asserts step 3's COMMAND at all, and the only element
  asserting the step is row 42's PLAN TWIN, by step NUMBER. The `stale-declarations`
  fixtures in §6 are the SET {row 3 (config trigger), row 6 (src trigger), row 34's main
  (src trigger, TWO kinds), row 42's PLAN TWIN end state (producer trigger, SOLE), row 43's
  end state (src trigger, SOLE)}, and exactly ONE of the five carries a plan assertion — row
  42's, whose trigger is a `scripts/bundle-types.mjs` PRODUCER edit forced by its main half's
  `C15-3` subject. So P is "step 3 prints whenever `stale-declarations` fired" and Q is "the
  trigger lies outside every `packages/*/src`", and Q held on the only fixture that read P.
  §4 step 2's carve-out paragraph naming the producer edit as the state's reachable form is
  what kept the two trigger classes from ever being separated. **Fixed by policy step 3** — a
  PLAN conjunct on §6 row 43's CLI twin's FLAGLESS invocation, which already runs the process
  and already reads its rendered output on exactly the missing fixture: a SOLE
  `stale-declarations` whose newest comparand member IS a `packages/*/src/**.ts`. The mutant
  needs no injection point, failing on that twin's own end state, and cannot fail a correct
  implementation, §4 step 3 carrying no clause on the trigger. Scoped to step 3's PRESENCE
  and silent about step 2, so row 42's PLAN TWIN keeps the sole-`stale-declarations`
  carve-out to itself (standing rule 8). Policy step 2 at the sentences that rested on
  the deleted citation AND at every LIVE list this new element now belongs to, stated as the
  SET of sites rather than as a count (`C11-4`): §4's step-3 paragraph, §4's ordered recipe
  at STEP 3 ITSELF — which named no element for its COMMAND, which is how that command went
  twenty rounds unasserted while step 1 carried its citation on its own line — §4's
  non-vacuity SET for the ordered plan and §4's `OP21-1` plan-presence list, §2's mirror of
  BOTH of those (one list stated twice, as §4's own "both lists here and in §2" says) with
  its three cardinalities retired and its fourth scoped, §6's rendered-text group bullet and §6
  row
  21's PLAN TWIN's pass-list, plus an amendment on §9's round-14 `C14-2` entry, whose "the
  proposed battery row for it was vacuous besides: its fixture is the sole-kind case, where
  the correct plan also emits step 3" is true only of the PRODUCER-triggered sole-kind
  fixture round 14 happened to have. ⚠️ **The first apply reached the §4 remedy cell, the §6
  bullet and the row-21 pass-list and stopped there** — the §4 and §2 lists that ENUMERATE
  the plan's elements were left naming six rows that no longer exhaust them, and §4's mirror
  was ALSO still missing row 28's PLAN TWIN from round 25. Standing rule 9 at this round's
  own fix, and the eleventh consecutive round in which a completion claim was partly true.

- **`OP34-2`, low, scope SUBSTANCE — §4's remedy table defines `Finding.remedy` for EIGHT
  kinds and ONE was read, at the kind a plan-derived value happens to get right.** Re-derived
  read-only 2026-08-22 at `5e4918f` over the same §6 delimitation: the phrase pinning a VALUE — a
  finding's `remedy` asserted EQUAL to a table command — occurs **once**, at row 43's CLI twin's `stale-declarations`, and row 43's own `--json`
  deep-equalities are against the run's OWN return and therefore pin no value at all — the
  shape `OP22-1` closed for the field's PRESENCE and left open for its CONTENT. The plausible
  wrong body derives `remedy` from the ordered PLAN rather than from the table — the FIRST
  step that kind selects, which §4's own "the table below is that plan's SOURCE" invites read
  backwards. It agrees with a correct implementation on `stale-bundle`,
  `stale-plugin-assets` and `bundle-provenance-missing` (step 2), on
  `stale-shipped-declarations` (step 4) and on `stale-declarations` (step 3 — the one pinned
  value), and on `stale-unit`, `orphan-output` and `unbuilt` it emits `rm -rf <unit>/dist`
  ALONE, dropping the rebuild: a `--json` remedy that deletes a unit's outputs and never
  restores them, which is `CF14-1`'s class at the one surface §2 declares this field for and
  is worse than the finding it answers. **Fixed by policy step 3** — a RETURN-VALUE conjunct
  on §6 row 28's PLAN TWIN, whose round-33 extension already carries one unit per one of
  those three kinds and already runs `checkStaleDist`; it asserts NAMING and ORDER rather than
  byte-equality, so a cell's trailing gloss does not break it, and it needs no injection point.
  DELETE was checked first and refused: §2 declares the member REQUIRED, row 43 already reads
  it, and §4 states the serialisation now rather than after a consumer exists. Latent today —
  nothing consumes `--json` — which is why this is `low` and not medium, and why it is stated
  now for the same reason §4 gives. ⚠️ **ONE further LIVE site this conjunct moves, outside
  §6 row 28's own cell and missed by the first apply (standing rule 9):** §2's `Finding`
  note, whose "`remedy` above rides that same element" credited the `--json` DEEP-EQUALITY —
  which pins PRESENCE and no VALUE, this finding's own measurement — with covering the
  field, and which now names this conjunct for the VALUE and states the direction the mutant
  reverses. ⚠️ **§6's rendered-text group bullet was checked and is LEFT AS WRITTEN:** row
  28's PLAN TWIN still READS rendered text, so its "BOTH of which read rendered text" stays
  true and its membership in that group is unchanged — `C20-2`'s rule there is per HALF and
  never per assertion (`OP25-1`'s own "membership per HALF" three lines below), and a
  RETURN-VALUE conjunct riding the same half neither joins that group nor removes the half
  from it.

**No working-tree mutation:** `scripts/bundle-cli.mjs` was never imported; every probe ran
`git`, `grep`, `awk`, `sed`, `wc` or `python3` read-only over files as text; `git show` wrote
the base revision to the scratchpad only; and `git status --porcelain` named this design file
alone, before and after.

**Round 34 (2026-08-22) — `correctness`, run as the SAME COVERAGE MATRIX as rounds 18-33 and
enumerated INDEPENDENTLY of round 33's list rather than as a check that its fixes landed.
**67** obligations over the lens's own scope — §3's **15** input-set rules (rule 1's member,
its RECURSION and its per-unit `srcDir`; rule 2's member and its EXCLUSION side; rule 3's TWO
members and its per-unit walk; rule 4's THREE clauses on BOTH sides; and the configs coming
from `enumerateTsconfigs`), its **12** output-set rules (the `distDir/**` walk, its RECURSION,
the per-unit `distDir`, the four-extension whitelist on BOTH sides, the FOUR minus-bullets
with the scratch one counted at its THREE consumers, and the strict `>` Verdict), the **18**
comparand and derivation obligations of the SIX comparisons in §3.1-§3.5, and the **22** skip
rules (the unit-state table's FOUR named reasons, `packages/cli`'s THREE clauses with clause
1's chunk member counted at its THREE branches and clause 2 at its boundary and its key, the
`skipped`/`unbuilt` suppression pair, the FOUR per-comparison declines, §3.2's inlined-unit
absence rule and its FIVE-rank precedence, and §3.4's and §3.5's precedence lines). **ONE
uncovered.** Re-derived read-only against `git show 5e4918f:<file>` and dispositioned
`fixed`.**

⚠️ **It is round 25/27/28/33's shape — a fixture built to discriminate P whose INCIDENTAL
property Q correlates with P, so a body keyed on Q passes.** Here P is `their bytes differ`
and Q is `their sizes differ`. **DELETIONS: ZERO.** Round 26's DELETE-FIRST rule was applied
and the obligation survives it: §3.5's byte rule traces to §0's requirement 1 through the
`dist/index.d.ts` exclusion, §8(d) makes it this design's ONLY content-correctness claim, and
§4's recipe step 4 exists because of it — dropping any of that ships the hole. **NARROW had
nothing to narrow:** the rule is already scoped to two named files. The disposition is policy
step 3, STRENGTHEN A VALUE — two bytes-values in a fixture cell — and it adds NO row, NO twin,
NO fixture and NO invocation.

- **`C34-1`, medium, scope SUBSTANCE — §3.5's `their bytes differ` was discriminated from
  `their sizes differ` by NOTHING, and the two coincided at every element in this design.**
  §3.5's Rule reads `report stale-shipped-declarations when … their bytes differ — size
first, then content`, and §2 declares the helper as `sameBytes(pathA, pathB) -> boolean //
size, then content — §3.5`. Both sites name size FIRST and content SECOND, so the body that
  performs the first step and not the second — `statSync(a).size === statSync(b).size` — is
  the reading BOTH of them invite, and it is a FALSE NEGATIVE in the comparison round 9 added
  to close `CF-2`'s shape at the second excluded artifact. It passed everything.
  ⚠️ **Re-derived read-only 2026-08-22 against `git show 5e4918f:<file>`, the COMMITTED
  document this round read** (the working copy carried this same round's other lens's
  in-flight edits and was therefore read as text only, never as the subject of a count):
  `awk '/^## 6\./,/^## 7\./' <file> | grep -c size` returns **0** and the same slice piped
  to `grep -c sameBytes` returns **0** — so §6 never names the helper and never names the
  property. Every fixture in this design in which the two files DIFFER is §6 row 22's MAIN
  half's barrel rewrite — that half, row **29**'s FOURTH fixture, which is its end state, and
  row **34**'s SOLE-KIND TWIN, which cites `row 22's own mutation` — and on all three the two
  SIZES are a FREE VARIABLE, no cell pinning the cache's length, so the natural construction
  (a realistic cache against a small barrel; §3.5 measures the live pair at **230,199** bytes
  against a `tsc` barrel three orders of magnitude smaller) makes the sizes DIFFER and the
  size-only body FIRE, agreeing with a correct one. Every remaining cli fixture binds the pair
  byte-identical through the complete-cli-build rule's `CF8-2` cache clause or leaves a side
  absent, so both bodies report the same thing; the false-negative battery's
  `--force`-identical-bytes bullet has EQUAL sizes AND EQUAL bytes, clean under both; rows
  **17**, **23** and **26** and row 23's twin are coverage rows on trees where one side is
  gone. The live tree cannot discriminate either — re-derived read-only 2026-08-22, `cmp`
  reports `packages/cli/.dts-cache/index.d.ts` and `packages/cli/dist/index.d.ts` IDENTICAL at
  **230,199** bytes each — so §6's live smoke test, which asserts the unit SET and the
  coverage shape and explicitly not the finding set, is blind to it.
  ⚠️ **The wrong result is on the tree §3.5 was ADDED for.** §4's recipe step 3
  (`npm run bundle:types -- --force`) then stop is §3.5's own counterexample paragraph, and
  step 4 exists because "step 3 alone leaves the pre-force copy shipped". After any source
  edit whose regenerated declarations keep the byte COUNT — an exported identifier renamed to
  another of the same length is the ordinary case — the size-only body reports PASS, exit
  **0 even under `--strict`** at `check:all` member 0 and at line 5 of §5.1's measured 204
  under flagless `pretest`, with the published `types` entry
  (`packages/cli/package.json` `exports["."].types`, inside `files`) holding the pre-force
  copy. That falsifies §8(d)'s stated exception in terms — `which §3.5 compares BYTE FOR BYTE
… that establishes the shipped copy is the bytes bundle-types.mjs last produced` — which a
  size comparison establishes for the LENGTH alone, and it is `C11-6`'s own harm at a second
  comparison: a mandated conjunct with no element, hardcoded never to fire for a whole class
  of input.
  **The element, policy step 3 — TWO VALUES in §6 row 22's TWIN's fixture cell.** That twin's
  `.dts-cache/index.d.ts` and `dist/index.d.ts` are pinned byte-identical AND exactly **28**
  bytes, `export declare const abc: 1;` — a plain declaration and deliberately NOT itself a
  barrel, so the twin's EXISTING mutant (an implementation suppressing §3.5 whenever a `tsc`
  barrel is present) still sees a barrel at the END state and nowhere else — and the twin's
  rewrite writes `export * from "./errors.js";`, a DIFFERENT `tsc`-shaped barrel of the SAME
  **28** bytes (both counts re-derived with `printf … | wc -c`). At the end state the SIZES
  are then EQUAL and the BYTES differ, so the SECOND MUTANT — a `sameBytes` comparing SIZE
  alone — reports CLEAN and FAILS on the twin's own fixture with no injection point, rows 44,
  47 and 48's discipline. ⚠️ **`OP16-3`'s and `CF20-1`'s disqualifier, checked BEFORE the
  clause was written: it cannot FAIL a correct implementation** — §3.5's Rule is `their bytes
differ`, and a byte comparison reports this kind here whatever the two sizes are.
  ⚠️ **The TWIN and not the MAIN half, deliberately (standing rule 8, checked AT the fix):**
  §6 row 29's FOURTH fixture is the MAIN half's END STATE and renders §4's byte-based
  `(<a> vs <b> bytes)` line, so pinning the MAIN half to one length would have made that line
  render two EQUAL numbers on the one fixture that reads it. Hosting the pin on the twin
  leaves the main half's `mtime UNCHANGED or NEWER` rewrite, its size-UNPINNED cache, row 29's
  fourth fixture and row 34's SOLE-KIND TWIN all exactly as they were, and leaves this twin's
  own verdict — exactly this kind PLUS one advisory `bundle-provenance-missing`, with
  `bundle-freshness` at `not-run (.bundle-meta/metafile.json absent)` and the other three
  `full` — and its existing mutant untouched, a same-length barrel being a `tsc` barrel.
  **SITES: EIGHT, per standing rule 9 — and this entry read `SITES: TWO` until the fan-out
  was walked section by section, which is standing rule 9's own subject.** The ELEMENT is
  one — §6 row 22's TWIN cell — and the CLAIMS resting on it are six more, plus this entry.
  (1) §3.5's COST paragraph read "in the `tsc`-barrel case the sizes differ by three orders
  of magnitude, so the size pre-filter decides it without a read": the sentence that
  licenses the size-only body outright, stated as a property of the CASE, and FALSE of this
  twin's own fixture. It is now scoped to the live pair, with the CONTENT step stated as
  mandatory. (2) §8(d) — the claim at fault — carries the "asserted by NOTHING until round
  34" amendment, in §8(e)'s own round-19 shape. (3) §0's requirement 8 row, whose
  `cache vs the shipped copy` discharge held at SIZE granularity only, in the shape its
  requirement rows 3, 5, 6, 7, 10 and 11 already carry. (4) §6's `CF8-2` cache clause binds
  every cli fixture's pair to the SAME bytes and never to a LENGTH, and now records this
  twin as the one fixture taking that BOUNDARY — row 19's twin's `C19-2`/`CF17-3` shape,
  one fixture rule over. (5) §6 row 29's FOURTH fixture cited "row 22's end state", which
  this pin makes AMBIGUOUS between two halves of opposite byte length; it is narrowed to
  the MAIN half, without which the standing-rule-8 paragraph above is not true as written.
  (6) §2's declaration gains the pointer to the element, so a later round cannot shrink the
  pin and silently restore the hole. §3.5's RULE sentence and §2's `size, then content`
  SPELLING are LEFT AS WRITTEN: the optimisation is correct, and what was missing was the
  element, never the sentence. ⚠️ **The fixture literal 28 is now LIVE at all eight sites
  and is ONE value** — both spellings re-derived at 28 with `printf … | wc -c` — so a round
  moving it must move it at every one of them. No count moves: §6's round-26 mutant census
  re-derives 13 before and after, none of these six edits spelling `N mutants`; round 33's
  `A-Z0-9` census over §6 is unread; and no row, twin, fixture, invocation or kind is added.

**COUNTS THIS EDIT CHANGES: NONE, and the sites checked rather than assumed.** No battery row,
no twin, no fixture, no mutant-carrying row, no stem, no output-set member, no unit and no
invocation is added; nothing is renumbered; and no kind, reason spelling, coverage status or
exit code moves — so §1's **49** battery rows and its `~680` estimate stay correct
(`OP22-3`), and §6's sha-pinned outside-citer registry needs no re-run, renumbering being its
only trigger. ⚠️ **Round 26's §6 mutant-count census is unread by this apply — re-derived
**13** before and **13** after under §9's round-26 command** (`awk '/^## 6\./,/^## 7\./'
<file> | grep -onE '(ONE|TWO|THREE|FOUR|FIVE|[0-9]+) mutants?'`): the added mutant is spelled
`SECOND MUTANT`, in row 8's and row 41's own shape (standing rule 7), which matches no
alternative of that pattern. Row 47's twin's sha-pinned **10** at `e99b066` therefore stays as
written. Round 33's `A-Z0-9` census over §6 is likewise unread, this apply writing no such
literal. §3's **21 of 30**, its **20**/**9** split, §0's **29 PATHS**, §8(l)'s **NINE**, §3's
Units step's **19 of 19** and rule 4's live member count **ZERO** are LIVE-tree measurements a
fixture value cannot move. §3.5's **230,199** is RE-DERIVED and unmoved.

**No working-tree mutation, and one disclosure.** `scripts/bundle-cli.mjs` was never imported;
every probe ran `git`, `grep`, `awk`, `sed`, `wc`, `printf`, `ls`, `cmp` or `node -e` reading
`fs.statSync`/`fs.readdirSync`/`fs.readFileSync`, all read-only, and every scratch write went
to the scratchpad. ⚠️ **The working copy was NOT clean while this lens ran:** this round's
`operability` lens applied its own edits to this same file between this lens's first read and
its apply, so this entry's counts are re-derived against `git show 5e4918f:<file>` — the
committed document — and never against the worktree, and this apply is a read-then-write of
the file AS IT THEN STOOD, so that lens's edits are carried forward rather than clobbered.

**Round 34 (2026-08-22) — `contract-fit`, run as the SAME COVERAGE MATRIX as rounds 18-33 and
enumerated INDEPENDENTLY of round 33's list rather than as a check that its fixes landed.
**57** obligations, the distinct (requirement, cited-section) pairs §0's ELEVEN requirement
rows name, re-derived at `5e4918f` by the extraction round 33 states — every section sign in
each row's third cell, deduplicated per row — and returning that round's vector unchanged: 8,
7, 9, 1, 2, 3, 7, 7, 6, 3 and 4 across requirements 1-11. **ONE uncovered:** the
(requirement 9, §3 input rule 4) pair AGAIN, at clause (i) — round 33 closed its ACCEPTING
side and its own entry says so, leaving the FAILING side unclaimed. Re-derived read-only and
dispositioned `fixed`.**

⚠️ **It is round 25/27/28/33's shape for the FOURTH time in ten rounds, and the incidental
property here is a WHOLE CLAUSE no fixture separates.**

- **`CF34-1`, medium, scope SUBSTANCE — clause (i) was still a MANDATED CONJUNCT EVERY ELEMENT
  AGREES ON, on its EXCLUDING side.** §3 said as much in terms — `NO UNIVERSAL coverage claim
is made for clause (i) even now` — and every rule-4 config in this design SATISFIES (i): row
  47's main half, that row's round-25 twin's `packages/A/tsconfig.emit-elsewhere.json`, and row
  3's `packages/cli/tsconfig.dts.json`. So clause (i) was DELETABLE from `descendantConfigs`
  with every element still passing, which is `CF25-1`'s formula at the last of the three
  clauses still in that state. The SEVENTH wrong body, absent from §2's enumeration of six,
  filters on (ii) AND (iii) ALONE with NO chain test at all — the reading round 33's own
  sentence makes cheapest, the argument list being PATHS and clause (ii) already tying a config
  to ONE unit. It passed §6 row 47's MAIN half (that config satisfies (ii) and (iii), so the
  mutation still renders `stale-unit packages/A`), row 47's ROUND-25 TWIN
  (`tsconfig.emit-elsewhere.json` fails (ii), so both states stay clean), row 3
  (`tsconfig.dts.json` fails (iii)), row 2 and its round-18 twin (cross-attachment blocked by
  (ii) alone), rows 48 and 49 (neither holds a variant config), the census-import predicate
  (which matches the import STATEMENT) and the live smoke test. **The live tree cannot
  discriminate it either, re-derived read-only 2026-08-22 at `5e4918f`** by parsing all **30**
  tracked `tsconfig*.json` as text — no import of `scripts/repo-census.mjs` and none of
  `scripts/bundle-cli.mjs`: the ONLY configs whose RESOLVED `composite` is `true` are the **19**
  units' own `tsconfig.json`, each with `outDir` resolving to its own `distDir`, so this body
  attaches each to the one input set it is already a rule-3 member of and NO unit's input set
  gains a member — rule 4 contributing **ZERO** under the correct reading and **19** REDUNDANT
  members under this body, the same live input sets either way;
  `packages/cli/tsconfig.dts.json`
  sets `"composite": false`, `tsconfig.base.json` declares neither `composite` nor `outDir`, and
  the root `tsconfig.json` carries no `compilerOptions` (`Object.keys` returns
  `files, references`). Its WRONG RESULT is §3's own worked example one clause over —
  `packages/gates/tsconfig.standalone.json`, NO `extends`, `"composite": true`,
  `"outDir": "./dist"` — which fails (i) correctly and which this body ATTACHES, so a `touch`
  of it (a `git checkout`, a branch switch, format-on-save) prints `stale-unit packages/gates`
  at `check:all` member 0, exit **1** under `--strict`, with `rm -rf packages/gates/dist` plus
  a 19-unit rebuild for a config whose output side provably cannot move for it: verbatim the
  false positive this rule's clause-(ii) passages already refuse.

**DELETIONS: ZERO**, and the policy order was walked rather than assumed. Every element
enumerated traces to a requirement in §0 or a wiring claim in §5, so step 1 had nothing to
take; step 2, NARROW, had nothing to scope, §3's closing paragraph keeping clause (i) in terms
and §0's requirement 9 mandating it. **The disposition is policy step 3, STRENGTHEN A VALUE**,
at the one value every rule-4 element left free — whether ANY fixture config FAILS clause (i) —
and it adds no row and no twin: §6 row 47's ROUND-25 TWIN gains a SECOND non-member config,
`packages/A/tsconfig.standalone.json`, declaring NO `extends` so (i) FAILS on the CHAIN while
the FILE sits INSIDE `packages/A`, `"composite": true` directly so (iii) holds, and
`"outDir": "./dist"` resolving to `packages/A/dist` — that unit's `distDir` ITSELF — so (ii)
holds by the containment row 3's config already satisfies. It is set NEWER than the newest
output under `packages/A/dist`, the MIRROR of that twin's first config, which is OLDER, so the
BASELINE is where it discriminates. The twin's VERDICT is unchanged: CLEAN at BASELINE and at
the END state, zero findings, ZERO `orphan-output`.

⚠️ **`OP16-3`'s and `CF20-1`'s disqualifier checked AT the fix:** a CORRECT three-clause body
excludes the new config at (i) — the reading §3 already fixes at its HYPOTHETICAL
`packages/gates/tsconfig.standalone.json` (this entry read live; corrected in round 35,
`CF35-3`, the file not existing on this tree), where a config with NO `extends` sitting
INSIDE a unit fails (i) — so both of the twin's states stay clean and no correct implementation is
failed. ⚠️ **Standing rule 8, checked in BOTH directions:** the twin's mutants (a) and (b)
both carry clause (i) as the CHAIN test, so both DECLINE the new config and both still fire on
`tsconfig.emit-elsewhere.json` at the END state — neither is vacated; the new config writes no
directory and sits outside `srcDir` and outside `distDir`, so §3's output set, §3.1's stem
bijection, rows 14 and 44's `eo-*-fixture-*` elements and row 47's MAIN half are untouched; and
row 47's MAIN-half PATH-CONTAINMENT mutant is ALSO killed by this config, which is a SECOND
element for a body the MAIN half already names and is deliberately NOT counted as a fourth
mutant of the twin.

**SITES, all sixteen plus this entry, per standing rule 9:** §0's requirement 9 row TWICE — the
twin's element description, and its `its config being SYNTHETIC` clause; §2's
`descendantConfigs` comment TWICE — the SEVENTH wrong body, and the EXCLUDING-clause element
count; §3's rule-4 clause-(i) sentence, its `The two EXCLUDING clauses now each name their own
element` sentence and its closing clause-(i) paragraph; §3's `buildUnits` paragraph, whose
`row 47 carries TWO variant configs` would have gone FALSE in this commit — `CF14-4`'s class,
which that paragraph names itself; §6 row 47's TWIN at SEVEN of its own clauses — the fixture,
the mutant list, the END-state sentence, the census sentence, the disqualifier, the
standing-rule-8 sentence and the SYNTHETIC clause; and §8(l)'s round-25 sentence.

**COUNTS THIS EDIT CHANGES, with their sites — exactly FOUR.** §2's wrong-body enumeration SIX
→ **SEVEN**, at §2's `descendantConfigs` comment. The EXCLUDING-clause element count TWO →
**THREE**, at that same comment and at §3's rule-4 paragraph. §6 row 47's TWIN's mutant count
TWO → **THREE**, at that twin's verdict cell and at NO other site. And §3's `buildUnits`
paragraph's variant-config count for row 47, TWO → **THREE**. ⚠️ **Round 26's §6 mutant-count
census is re-derived rather than restated — **13** hits at `5e4918f` before this apply and
**13** after:** the one hit this apply touches changes the WORD before `mutants` and not the
count, and the third mutant is spelled `(c)` inside the same enumeration; the breakdown at that
sha is NINE hits in other rows' own cells (one each at rows 1, 4, 5, 28 and 44, two each at
rows 39 and 40), TWO at row 47 (its MAIN half's and its twin's) and TWO inside the
wiring-assertion comment blocks, and not one an aggregate. The twin's sha-pinned 10 at
`e99b066` is SUPERSEDED rather than preserved, this being the round that moves the count it was
measured to protect. **Counts NOT moved, re-derived rather than restated:** the new config is
SYNTHETIC and adds NO ROW and NO TWIN, so §1's **49** battery rows and its `~680` estimate
stand (`OP22-3`), nothing is renumbered and §6's outside-citer registry needs no re-run; rule
4's live member count is **ZERO** under the correct reading, and the seventh body's **19** are
each already in the input set they land in, so the live input sets are identical either way;
§3's **21 of 30**, its **20**/**9** split, §0's **29 PATHS** and §8(l)'s **NINE** are LIVE-tree
measurements a synthetic fixture cannot move, re-derived at `5e4918f` with the tracked
`tsconfig*.json` number **30**; row 47's MAIN half keeps its THREE mutants and its own fixture;
this round's own matrix is unmoved at **57**, no edit here adding a section sign to any §0
cell; and §4's kinds, reasons, remedy rows, recipe steps and exit rows are unread by this
apply, both halves of row 47 asserting a RETURN value and no rendered text.

**No working-tree mutation:** `scripts/bundle-cli.mjs` was never imported and neither was
`scripts/repo-census.mjs` — its two predicates were re-read as TEXT at `:75-79` and `:316-318`;
every probe ran `git`, `grep`, `awk`, `sed`, `wc`, `fold`, or `node`/`python3` reading files
through `readFileSync`, all read-only, and every scratch write went to the scratchpad.
⚠️ **The working copy was NOT clean while this lens ran:** this round's `operability` and
`correctness` lenses had already applied their own edits to this same file, so every derivation
here was run against the tracked SOURCE tree and the committed document at `5e4918f`, which
those edits do not touch, and this apply is a read-then-write of the design file AS IT THEN
STANDS, carrying their edits forward rather than clobbering them.

**Round 35 (2026-08-22) — `correctness`, TWO findings: `C35-1`, medium, SUBSTANCE, and
`C35-2`, low, META. Entered here in the SAME round that filed them, which is the preamble's
own standing requirement. `C35-1` is round 34's own question — a mandated conjunct whose
elements all AGREE on it — asked of a predicate rather than of a rule, and the answer is the
same shape one section over.**

- **`C35-1`, medium — round 20's whitelist VACATED the element for §3's `dist/plugin/**`
  minus-bullet, and that round did not re-check the bullets it now subsumed.** `C20-1` made
  `isCompilerOutput` a four-extension WHITELIST (`.js`, `.d.ts`, `.js.map`, `.d.ts.map`)
  BEFORE §3's four minus-bullets. §6 row 7's mutation is `touch dist/plugin/.mcp.json`, and
  `.json` is outside that whitelist — so from round 20 that row flipped for the WHITELIST
  and for nothing else, while §3's minus-bullet list, §0's requirement 1 row and §2's
  `isCompilerOutput` comment all name it as the `dist/plugin/**` bullet's element. Re-derived
  read-only 2026-08-22 at `a5605e9` over §6's whole mutation column: NO element in this
  design moves a whitelisted-extension file's mtime beneath `packages/cli/dist/plugin` — row
  20 DELETES a top-level entry, rows 8 and 24 move plugin SOURCES, §8(p)'s bullet empties
  `dist/plugin/skills/` with the marker ABSENT, and no fixture rule mandates any extension
  inside `dist/plugin` at all. So an `isCompilerOutput` carrying the whitelist and the other
  three bullets while OMITTING `dist/plugin/**` passed every battery row, the false-negative
  battery, the wiring assertions and the live smoke test: the bullet was DELETABLE with every
  element still passing, which is round 34's own `CF34-1` at a predicate instead of at a rule.
  Wrong result on the tree the bullet is kept for: `bundle-cli.mjs:178` copies the six
  `PLUGIN_ASSET_ENTRIES` with `cp` and no `preserveTimestamps`, so the moment any file
  beneath them carries one of the four extensions that body reads a COPY TIME as
  `packages/cli`'s newest output and masks every `packages/cli/src` edit landing between the
  esbuild write (`:121`) and the copy loop (`:177-179`) — **PASS, exit 0 even under
  `--strict`** at `check:all` member 0 and under flagless `pretest`, on a `packages/cli`
  compiled from stale sources, which is verbatim requirement 1's own "excluding the
  `bundle:cli` asset copy" and blind spot 1 re-opened. The window is `C18-6`'s one bullet over
  and STRICTLY WIDER: re-derived read-only at `a5605e9`, **36.46 ms** on this working copy
  (newest file beneath `packages/cli/dist/plugin` at mtimeMs **1787410216218.5693** against
  the newest §3-filtered output at **1787410216182.1096**) against `C18-6`'s **11.10 ms** for
  `:121`→`:153`. ⚠️ **NOT live on this working copy, and that is stated rather than
  hidden:** re-derived read-only at `a5605e9`, `packages/cli/dist/plugin` holds **26** files
  over `.json`, `.md`, `.mjs` and `.mts` and **ZERO** with a whitelisted extension, so the
  whitelist subsumes the bullet TODAY and no live assertion can see the gap — while
  `packages/plugin/hooks/` already holds **five** `.mjs` RECURSIVELY — four at its top level and one under `lib/`, re-derived read-only 2026-08-22 with `find packages/plugin/hooks -type f -name '*.mjs'`, the recursive form because a `.js` ANYWHERE beneath an entry is copied by `bundle-cli.mjs:178` (standing rule 1), so the drift is one commit away,
  `CF15-4`'s own argument at the neighbouring set. Fixed by STRENGTHENING A VALUE, never by
  adding a row: row 7's fixture PINS `dist/plugin/hooks/hook.js` and its mutation moves THAT
  file instead of the `.json`, after which the whitelist-carrying bullet-omitting body fails
  BOTH halves of row 7's verdict on row 7's own fixture with no injection point. ⚠️
  **The `.tsbuildinfo` bullet is the SAME observation with the OPPOSITE disposition, and
  carrying the two as one claim is what hid this:** that file is rejected by EXTENSION
  unconditionally, so dropping its bullet flips nothing and there is no wrong body to catch —
  round 23 ruled exactly that at §6 row 5's twin ("a cell claiming one mutant per excluded
  file would be asserting a mutation that cannot fail") and §3's minus-bullet list went on
  crediting it with "row 9 plus the false-negative battery". §3 now states which THREE bullets
  have a DISCRIMINATING element and which ONE — `.tsbuildinfo` — needs none. ⚠️ **This
  sentence and §3's both read `two` when first applied, each dropping `dist/index.d.ts`,
  whose element is row 7's round-18 TWIN and which THIS ENTRY's own coverage matrix below
  lists as covered by exactly that twin — standing rule 9 inside the entry that cites
  standing rule 8.** ⚠️ **Standing rule 8, checked at
  the fix:** the raw-`dist/**` mutant row 7 already killed is killed by the amended mutation
  too, a raw walk taking `hook.js` exactly as it took the `.json`; row 7's round-18 TWIN moves
  `packages/cli/dist/index.d.ts` and is untouched; the added file sits beneath an entry
  clause 1 already requires to EXIST, is outside the filtered comparand under BOTH walked
  sides so the complete-cli-build rule's marker clause is unaffected, and `packages/cli` is
  exempt from §3.1 by `kind`, so no stem, no output-set member and no verdict elsewhere moves.

- **`C35-2`, low — the guard against the apply hazard that once doubled this document
  restated a count and a site list, and both had drifted.** The preamble's
  `String.replace` paragraph read "this document carries **four** live `String.replace`
  special patterns as legitimate content — three dollar-apostrophes and one dollar-backtick,
  in §0's requirement-9 row, twice in §3's reproducible-command block, and in §3.3's
  chunk-collision bound", measured 2026-08-20 and re-derived unchanged by rounds 13 and 20.
  Re-derived read-only 2026-08-22 with `grep -oE` for a dollar followed by an apostrophe or
  by a backtick, across the shas the rounds that moved it are pinned at: **4** at `9c247eb`
  and `3095116`, **6** at `e99b066`, **8** at `2ef2ce5` and **10** at `5e4918f` and
  `a5605e9` — seven dollar-apostrophes and three dollar-backticks. So SIX sites were live
  and unlisted: §5's `pre-commit`/`pre-push` grep and its `ci.yml` header grep, §6 row 25's
  cell — REWRITTEN by round 33 for `C33-1`, which is exactly the "blocks re-derived and
  rewritten most rounds" the same sentence warns about — and three in §9. `C11-4`'s class
  at the count and `CF14-4`'s at the enumeration, in the one paragraph whose whole subject
  is what a later apply may safely pass to a string replacement. The RULE above it is
  unconditional and was never a whitelist, which bounds the harm to a reader taking the
  four-site list as one; both are DELETED rather than corrected, per `C11-4`'s own remedy,
  and replaced by the instruction to re-run the census before any apply and never restate a
  subset of its output. §9's earlier entries keep their sha-pinned **four**, which is this
  file's convention for a figure a later round must be able to tell from damage.

ℹ️ **What round 35 re-derived and could not break, enumerated as the coverage matrix this
lens owns — §3's input-set and output-set rules, the six comparisons of §3.1-§3.5 and each of
their skip rules, **98** obligations, of which this finding is the ONE with no element:**
input rules 1 and 2 and rule 2's exclusion side (rows 1, 48, the false-negative battery's
`.info`/`.snap`/`.mjs` bullet); rule 3's own-config and ancestor members and its
every-unit quantifier (row 2 and its round-18 twin); rule 4's SIX sides, all three clauses
now carrying both (row 47's main half and its round-25 twin's three configs, plus row 3);
the per-unit `srcDir`/`distDir` reading (row 49) and `kind` (row 4's round-23 twin); the
output set's recursion (row 1), its whitelist on both walked sides (row 44's round-20 twin),
its `dist/index.d.ts` bullet (row 7's round-18 twin) and its scratch bullet at the mtime and
stem sides (rows 44 and 14); the strict Verdict and its equal-mtime exclusion side; the three
`unitState` states, their three literal reasons and the `no dist/` fourth (rows 9, 13, 14, 15,
30, 39); `skipped` NOT suppressing the four cli-scoped comparisons (row 43) and `unbuilt`
suppressing them (row 26); §3.1's recursion, its exactly-one count and its bundled-unit
exemption (row 4 and its twin); §3.2's metafile derivation (row 45), its minimum over the
chunk set (row 5), its two naming families (row 25), its three input members (row 5's
round-23 twin, rows 38 and 41), both `not-run` causes (rows 46 and 40) and four of the five
REASON PRECEDENCE pairs, the fifth being §3.2's own stated residual; §3.3's three clauses —
clause 1 excluded at four members, clause 2 at both the `>=` boundary and the marker-FILE
key, clause 3 stated as isolable by no fixture since round 12 (`C11-2`) — and its comparand's
directory and extension halves (row 42's round-20 and round-30 twins), its chain member
(row 3's round-18 twin) and its producer (row 42); §3.4's mixed-type and SCOPE halves (row 24
and row 8's second mutant) and its three ranked not-run reasons (rows 26's twin, 12, 46); and
§3.5's both-exist conjunction at each side (rows 22, 23, 17's second assertion, 23's
both-absent twin) and its size-THEN-content step (row 22's round-34 twin). Round 34's own
live claims re-derive exactly: parsing all **30** tracked `tsconfig*.json` read-only at
`a5605e9`, the configs passing clauses (ii) and (iii) are the **19** units' own
`tsconfig.json` and no other, and the one config whose chain resolves INTO a unit is
`packages/cli/tsconfig.dts.json`.

**COUNTS THIS EDIT CHANGES, with their sites — exactly ONE, and it is DELETED rather than
moved.** The preamble's `String.replace` census count, **four** → DELETED per `C11-4`, at
that paragraph and at NO other live site — and TWICE inside it, the second being the
ORDINAL "the fifth and sixth instances", which is the deleted four's own arithmetic and
which the first apply left standing three sentences from the two it deleted. The figure
itself is now carried ONCE, sha-pinned, by that same paragraph's own re-derivation, and is
deliberately NOT restated here. ⚠️ **AND §9's round-13 entry is NOT sha-pinned:** it names
no seven-hex commit anywhere in its own text and carries a BARE **four**, so
`rounds-13 and -20 entries keep their own sha-pinned four` was true of round 20 alone —
that entry pinning `9c247eb` in its own paragraph. Round 13's figure is datable
nonetheless, re-derived read-only 2026-08-22 at `f3aed16`, the commit recording that
round's findings, where the census returns **4**. ⚠️ **And §3's minus-bullet-attribution
count does NOT move, though this paragraph first recorded it as THREE → TWO:** what moves
is the MEMBERSHIP — `.tsbuildinfo` OUT, having no wrong body to catch, and
`dist/index.d.ts` IN, its element being row 7's round-18 TWIN — while the count stays
**THREE**. Standing rule 4's distinction read at a fan-out: a count and the set it counts
are two different measurements.
⚠️ **Round 26's §6 mutant-count census is re-derived rather than restated — **13** hits at
`a5605e9` before this apply and **13** after** (`awk '/^## 6\./,/^## 7\./' <file> | grep -onE
'(ONE|TWO|THREE|FOUR|FIVE|[0-9]+) mutants?'`): the added element is spelled `SECOND MUTANT`,
in row 8's and row 41's own shape, which that command does not match — round 28's own
precedent, and the reason this fix adds ZERO hits. **Counts NOT moved, re-derived rather than
restated:** §1's **49** battery rows and its `~680` estimate, this fix adding NO ROW and NO
TWIN (`OP22-3`); §6's outside-citer registry, nothing being renumbered; rule 4's live member
count **ZERO**, §3's **21 of 30**, its **20**/**9** split, §0's **29 PATHS** and §8(l)'s
**NINE**, none of them read by an edit to a synthetic fixture's contents; §4's kinds,
reasons, remedy rows, recipe steps and exit rows, row 7 asserting a RETURN value and no
rendered text; and `C18-6`'s **11.10 ms**, which this round quotes rather than moves.

**SITES, all moved together per standing rule 9.** `C35-1`: §0's requirement 1 row; §2's
`isCompilerOutput` comment; §3's `dist/plugin/**` bullet's neighbour, the
minus-bullet-attribution sentence inside the `dist/index.d.ts` bullet; §3.4's opening
paragraph, in §3.5's opening's own shape; and §6 row 7's fixture cell, its verdict cell and
its round-18 twin's `INSTEAD of` clause. `C35-2`: the preamble's `String.replace` paragraph
alone — the rule it states is unchanged — but THREE restatements move inside it, not two:
the count, the site enumeration, and the ORDINAL "the fifth and sixth instances" two
sentences below them, which the first apply left standing. ⚠️ **`no other LIVE site
restates either` was true of the document OUTSIDE that paragraph and FALSE of the paragraph
the sentence was written in — a search narrower than its claim (standing rule 1) at the
verification of this round's own fix, which is what round 9 earned that rule for.** The
census figure is stated ONCE, sha-pinned, in that paragraph's own re-derivation, and no §9
entry restates it.

**No working-tree mutation:** `scripts/bundle-cli.mjs` was never imported and neither was
`scripts/repo-census.mjs` — its two predicates were re-read as TEXT at `:75-79` and
`:316-318`; every probe ran `git`, `grep`, `awk`, `sed`, `find`, `wc` or `node`/`python3`
reading files through `readFileSync`/`statSync`, all read-only, and every scratch write went
to the scratchpad. `git status --porcelain` was empty at `a5605e9` before this apply.

**Round 35 (2026-08-22) — CONSOLIDATED ENTRY for the `contract-fit` and `operability`
lenses, appended after the `correctness` entry above and carrying that entry's own two
corrections as well. The round's question is round 34's, asked of EVERY CONJUNCTIVE RULE in
this document rather than of input rule 4 alone: a clause needs an ACCEPTING element where
it HOLDS and the rule fires, AND an EXCLUDING element where that clause ALONE fails while
the others hold and the rule does not. With the accepting side alone the clause is
DELETABLE and a body omitting it passes everything; with the excluding side alone a body
that always fails it passes too.** The SET walked, stated as the set rather than as a count
(`C11-4`): §3.3's THREE-clause `packages/cli` `unbuilt` rule, §3.2's structural-silence
condition and its reason precedence, `unitState`'s three states and the predicates that
separate them, the marker-freshness rule, the four cli-scoped skip rules,
`placedCorrectly`'s seven conjuncts, §3's input rule 4's three clauses (round 34's), §4's
five ordered steps with their four carve-outs, §4's exit table's four conditions by two
`strict` columns, and §4's `nothing was checked` carve-out.

- **`OP35-1`, high, scope SUBSTANCE — §4's ONE carve-out from A DEGRADED RUN CARRIES A NEXT
  ACTION is a THREE-conjunct conjunction that had exactly ONE element, in which all three
  conjuncts HOLD, so every conjunct was individually DELETABLE.** Reached independently by
  `operability` and by `contract-fit` on the same sentence, and consolidated here as one
  finding. The element is §6 row 39's NEGATIVE twin, where no unit was compared, no
  comparison ran and no finding fired and the plan must NOT print; no element anywhere had
  a single conjunct failing while the other two held, every plan-asserting element having
  units compared greater than zero. §4 states the SAME carve-out ten lines earlier with
  only TWO conjuncts, for the `PASS` line, which is correct there because that line is
  absent from the findings path; a `formatFindings` sharing ONE predicate between the two
  surfaces — the natural body, both firing on the same tree — therefore withholds the plan
  on the tree `npm run build:clean` leaves, whose skipped and `unbuilt` counts §4's own
  `C11-3` paragraph measures, and passes every element in this design. The printed page is
  then an `unbuilt packages/cli` line, a skipped summary and a four-entry coverage line with
  no `rm -rf`, no `npm run build` and no re-run instruction — verbatim `OP14-1`'s defect,
  at `check:all` member 0 and at line 5 of §5.1's measured 204 under `pretest`. **Fixed by
  one STRENGTHEN and two ADDs:** §6 row **36**, its tree pinned to exactly the two `unbuilt`
  units it already named, carries a plan conjunct and a second mutant — the two-conjunct
  carve-out — for "no finding fired"; row **39**'s ALL-NOT-RUN TWIN (its MAIN fixture plus
  `rm -rf packages/cli/.dts-cache`) for "NO unit was compared"; and row **39**'s CACHE-ONLY
  TWIN (its NEGATIVE twin's fixture plus a cache newer than §3.3's comparand) for "no
  comparison ran". DELETE was checked first and refused — the carve-out is research Q5's own
  requirement that a clone nobody built stay quiet — and NARROW had nothing to narrow, all
  three conjuncts being load-bearing on a different tree each. Fan-out at every LIVE site
  the rule governs: §4's carve-out sentence and its `PASS`-line sibling, §4's non-vacuity
  SET and its `OP21-1` plan-presence list, §2's mirror of the carve-out and of BOTH lists,
  §3.2's structural-silence bullet — which leans on the third conjunct in terms and could
  not discriminate it — §6's prefixed-rendering enumeration, §6 row 38's coverage-line
  ABSENCE argument and §6's `C22-1` marker-state enumeration.

- **`OP35-2`, medium, scope SUBSTANCE — §4's ordered recipe step 4 is rendered by no
  element, which is the state round 34 closed at step 3 and left standing at the adjacent
  step (standing rule 9).** Re-derived read-only 2026-08-22 at `a5605e9` over §6 as
  delimited by this file's `## 6.` and `## 7.` headings and BEFORE this apply:
  `npm run bundle:cli` occurred **4** times there, every one an ABSENCE list or prose about
  a mutant's per-kind list. The two elements that select step 4 name it by step NUMBER —
  row 34's SOLE-KIND TWIN and row 42's PLAN TWIN — so a plan assembler whose step-4 line
  copies its neighbour's `npm run bundle:types -- --force` (standing rule 7's
  part-of-a-shape copy) passes every element, and on row 34's SOLE-KIND TWIN's own fixture,
  where §4 says this step is the whole remedy, the entire printed plan regenerates the cache
  and never lifts it into `dist/index.d.ts`: §3.5 is byte-keyed, the finding survives
  verbatim, step 5 reports it identically and the generator's ~5 minutes buy nothing —
  `CF14-1`'s class. **Fixed by policy step 3** — that twin's existing clause is widened from
  a step NUMBER to step 4's COMMAND by LITERAL, with its mutant named; no row, twin, fixture
  or invocation is added, and the mutant cannot fail a correct implementation, §4 stating
  the command at the step and its remedy table's `stale-shipped-declarations` row giving the
  same one (`OP16-3`'s disqualifier).

- **`CF35-3`, low, scope SUBSTANCE — a HYPOTHETICAL config was cited as a LIVE file at two
  round-34 sites (standing rule 2).** §6 row 47's ROUND-25 TWIN and §9's round-34
  `contract-fit` entry both called `packages/gates/tsconfig.standalone.json` §3's live
  example. It does not exist and §3 never says it does — §3 introduces it as a config and
  refers back to it as that paragraph's own, while §2's parallel citation already reads
  "§3's own worked example". Re-derived read-only 2026-08-22 at `a5605e9`:
  `git ls-files | grep -i standalone` returns exactly ONE path,
  `docs/evidence/phase-15/closeout-c7-standalone-entry-point.txt`; `ls packages/gates/`
  returns `dist`, `goldens`, `package.json`, `src` and `tsconfig.json`; and
  `git ls-files | grep -E 'tsconfig.*\.json$' | wc -l` returns **30** with no `standalone`
  member. A live one would be a TWENTIETH config passing clauses (ii) and (iii), which would
  contradict §3's own round-34 re-derivation and move requirement 9's **21 of the 30**.
  **Policy step 2, NARROW** — the citation is scoped to what is verifiable; the disqualifier
  argument both sentences carry is unchanged, clause (i) being a test on the RESOLVED
  `extends` chain and never on where the file sits (round 33, `CF33-1`).

⚠️ **AND ONE CONJUNCT IS UNCOVERED ON ITS EXCLUDING SIDE AND IS NOT FILED, because this
document already ruled on it: §3.3's clause 3.** Every tree failing it fails clause 1 too,
clause 1 requiring the three entry outputs BY NAME, so no excluding element is
constructible and a `unitState` implementing clauses 1 and 2 alone passes every element in
§6. That is not new: round 12's `C11-2` measured it — "The three entry outputs are clause-1
MEMBERS, so clause 1 alone already catches every state clause 3 does" — and that round's
disposition, still standing in §3.3's own `build:clean` paragraph, RETAINS the clause as
deliberate redundancy against clause 2 and against a later edit dropping the entries from
clause 1's set, and records that dropping it is one edit away from re-opening `C-R4-1`.
Deleting it was proposed in this round and REFUSED as re-litigation of a dispositioned
finding; the proposed fan-out also missed that paragraph's FIVE live `clause 3` references
and its "it is not deleted" sentence, so applying it would have left §3.3 stating both
arities two hundred lines apart — commit `c18ebff`'s defect one section over. A later round
should read this note before filing the missing excluding side again.

**COUNTS THIS EDIT CHANGES, and their sites.** TWO cardinalities are RETIRED rather than
moved, at ONE live site each: "all three of row 39's twins" in §6 row 38's coverage-line
ABSENCE argument, and "its two twins" in §6 row 39's own round-21 unchanged-clause — each a
count of a set this round appends to, and `C11-4`'s remedy for a figure an appending round
re-invalidates is the SET or the round-scoped statement, never the number. And §3's
minus-bullet-attribution count moves **TWO → THREE** at its three LIVE sites, all moved in
this pass (standing rule 9): §3's `dist/index.d.ts` bullet, the `C35-1` bullet above and the
COUNTS paragraph above; what moved there is the MEMBERSHIP, `.tsbuildinfo` OUT and
`dist/index.d.ts` IN. The preamble's ORDINAL "the fifth and sixth instances" is DELETED at
its one site. ⚠️ **Re-derived read-only 2026-08-22 — the command is the reproducible part:**
`grep -c -F "all three of row 39's twins"` returns **2**, of which exactly ONE is live (§6
row 38), the other sitting inside §9's round-16 entry and left as written; and
`grep -o -E "row \*{0,2}39\*{0,2}'s [A-Za-z-]+ ?[A-Za-z-]* ?twin"` returns **19** hits, EVERY
one qualified as the NEGATIVE twin, so `C20-2`'s "a citation must say WHICH" rule needs no
site changed even though row 39 now carries FOUR twins.

**NOTHING ELSE MOVES:** §1's **49** battery rows and its `~680` estimate (THREE twins and NO
row are added, `OP22-3`); §6's row numbering and its command-derived outside-citer registry;
§6's mutant-count census, re-derived at **13** at `a5605e9` before this apply, every added
element being spelled `SECOND MUTANT`, which that command does not match; the **30** tracked
configs, the **21 of the 30**, the **20/9** split, §0's `29 PATHS`, §8(l)'s NINE and rule 4's
live member count of **ZERO**; §4's kinds, reasons, remedy table, exit table and five recipe
steps; and §3.3's clause count, which stays at THREE. **No residual is added** — every
obligation closed here is closed by an element rather than bounded.

⚠️ **Disclosed: the working copy was NOT clean while these lenses ran.** Round 35's
`correctness` apply had already written to this file, so `git status --porcelain` named this
design file throughout; every derivation was run read-only against the committed document at
`a5605e9` or against the working copy where stated, and every anchor was re-verified as
occurring exactly once against the document AS IT THEN STOOD.

**No working-tree mutation:** `scripts/bundle-cli.mjs` was never imported and neither was
`scripts/repo-census.mjs`; every probe ran `git`, `ls`, `grep`, `awk`, `sed`, `wc` or
`python3` reading files as text; every scratch write went to the scratchpad.

**Round 36 (2026-08-22) — CONSOLIDATED ENTRY for `contract-fit`, `correctness` and
`operability`, each run as the SAME COVERAGE MATRIX as rounds 18-35 and each re-enumerated
from the document as it now stands rather than as a check that round 35's fixes landed. SEVEN
findings, all `fixed`, one of them ENLARGED at the fan-out and two of them corrected in
consolidation.** `contract-fit` enumerated **7** obligations with **0** uncovered and ONE
UNCOVERED-AND-RULED; `correctness` **30** over §3's five mtime comparisons and their
thresholds, THREE uncovered; `operability` **36** over §4's output surface, FOUR uncovered
(`OP36-1` carrying two, one per conjunct). Every uncovered obligation is closed by an ELEMENT
and none by a bound, so **no residual is added**.

⚠️ **UNCOVERED-AND-RULED, re-checked by all three lenses and filed by none: §3.3's clause 3.**
Every tree failing it fails clause 1 too, so no excluding element is constructible — and
round 12's `C11-2` measured exactly that and RETAINED the clause as deliberate redundancy
against clause 2, which can never subsume it because deleting an output LOWERS the newest
output mtime and makes the marker look MORE current, and against a later edit dropping the
entries from clause 1's set. Round 35 refused a proposed deletion as re-litigation of that
disposition. Round 36 read the RULING's rationale rather than the sentences around it, has
nothing to add to it, and records the clause in the third bucket: its missing excluding
element is a CONSEQUENCE of the redundancy and not evidence against it, so it counts toward
`obligationsUncovered` at no lens. Nothing in this round touches it or the sixteen sites it
governs.

- **`CF36-1`, high, META — a 7,830-character run of spaces inside a code span, driving most
  of the file's growth.** Row 47's `CF35-3` re-derivation names the command
  `git ls-files | grep -i standalone`; between `git ls-files` and the escaped pipe stood
  **7,830** spaces, measured by regex on the raw line read-only at `d0b706e`, and
  `git show a5605e9:<file>` carries the whole command zero times, so all of it is round 35's.
  It made row 47's fourth cell **18,799** characters against the next widest cell's **12,217**
  and padded all **51** lines of the battery to **23,419**. Policy step 3, STRENGTHEN A VALUE
  — the sentence is a live and correct re-derivation and only its literal is damaged. The
  repair is TWO edits and the mechanism is stated because it is unusual: an exact-match anchor
  carrying 7,830 literal spaces cannot be emitted reliably, so the span's CLOSING boundary is
  moved to the LEFT of the run and its opening text removed on the right, which leaves the run
  as ordinary table-cell text. MEASURED on a scratchpad candidate rather than assumed:
  `prettier --parser markdown` 3.9.5 collapses it to ONE space and a second prettier run is
  byte-identical. ⚠️ **The after-width the finding carried, 16,837, is NOT restated and is the
  clearest cross-lens artifact this round produced:** it was measured with THIS lens's edits
  alone, and the `correctness` and `operability` twins widen other cells in the same table, so
  the re-padded width consolidates to a different number. `C11-4`'s class at a figure two other
  lenses move in the same apply — the direction is recorded, the command re-runs it, and no
  after-figure is written down.

- **`CF36-2`, medium, SUBSTANCE — a fixture glob that matches nothing, in the cell that
  discharges standing rule 8.** The sentence naming rows 14 and 44's elements spelled §3's
  fixture glob with UNDERSCORES in place of both asterisks. The `grep -o` census over the
  `eo-` fixture family returns **31** of the asterisk form and ONE of the underscore form at
  `d0b706e`, that one being this site, while the same sentence at `a5605e9` spells it
  correctly and the underscore form occurs **0** times there. It is a citation resolving to
  nothing, `CF13-4`'s class, inside the cell §0's requirement 9 names for clause (i)'s and
  (ii)'s excluding elements. ⚠️ **`CF31-1`'s CONTINGENCY IS NOT TAKEN, AND THAT WAS MEASURED
  RATHER THAN ASSUMED:** prettier 3.9.5 over a candidate carrying the restored asterisks
  leaves both byte-identical and a second run is byte-identical to the first, so the DELETE
  remedy `CF31-1` used is not needed here. Policy step 3, STRENGTHEN A VALUE, at ONE site;
  the other 31 asterisk-form sites are already correct and none moves.

- **`CF36-3`, medium, META — 57 code spans run together with their prose, and FOUR MORE the
  finding's own predicate could not see.** The finding measured 57 letter-side elisions in row
  47's fourth cell and proposed a letter-keyed clause for them. Re-derived at `d0b706e` under
  CommonMark backtick-RUN pairing scoped to paragraphs: **57** abut a letter and **4** more
  abut a comma, a full stop or a colon — after "ONE path", after "untouched", after "MUTATION"
  and after "round 33" — every one of the four new in round 35, the widened sweep returning
  **0** at `a5605e9`. **61** sites are repaired, not 57: standing rule 9 at the fan-out of a
  FINDING rather than of a fix, and standing rule 5 at the finding's own predicate. Policy
  step 3, STRENGTHEN A VALUE, for the 61; then policy step 4, ADD, for the preamble's SEVENTH
  structural clause, whose one new obligation is named in full — an apply is not finished
  until that sweep returns nothing, reported as the command with the sha it was run at.

⚠️ **TWO CLAIMS IN `CF36-3` WERE WRONG AND ARE CORRECTED HERE RATHER THAN CARRIED (standing
rule 2).** The finding called this "the SECOND consecutive round" to ship this damage and
attributed `944fc9d` to ROUND 34. Re-derived read-only: `944fc9d`'s own message body opens
"Round 31, 26 consolidated edits", so that commit is round **31**'s and the two events are
rounds 31 and 35, not consecutive. And round 31 did not merely repair the damage — ⚠️ **it
REFUSED a policy-step-4 clause of exactly this kind and recorded the refusal so a later round
would not re-propose it (`CF31-2`): "a standing apply clause re-deriving the
letter-backtick-letter census would key on a population narrower than the damage it guards —
40 of 66 here, measured".** That ruling is ENGAGED rather than stepped around. The clause
added here is a different predicate — CommonMark backtick-RUN pairing, both sides, punctuation
included — and it was measured against ROUND 31's own damage as well as round 35's: **0** at
`187eb2d`, **60** at `55666e8`, **0** at `944fc9d`, **0** at `a5605e9` and **61** at
`d0b706e`, so it flips in both directions on BOTH events and would have caught both. ⚠️ **It
is nevertheless still NARROWER than the damage — 60 of `CF31-2`'s own 66 — so the clause is
written as a FLOOR and not a certificate, with that bound STATED rather than hidden**, which
is the shape the anchor grep in the same preamble already uses for its own residual and what
makes this an addition to `CF31-2`'s refusal rather than a re-run of it.

- **`C36-1`, medium, SUBSTANCE — the strict threshold of THREE of this design's FIVE mtime
  comparisons was mandated by three sections and evaluated at its BOUNDARY by nothing.** §3.2's
  Rule (`is newer than bundleAt`), §3.3's declaration-cache rule (`exceeds the cache's mtime`)
  and §3.4's Rule (`exceeds the newest beneath packages/cli/dist/plugin`) each state one, while
  §7's `Second-granularity mtimes` cell states the mitigation for all five in ONE cell and its
  own warning names an element for §3.3's clause 2 alone. Re-derived read-only 2026-08-22 at
  `d0b706e` over §6's whole fixture column and classified by hand: §6 holds exactly TWO
  mtime-EQUALITY clauses — row 19's round-19 twin, which is clause 2's mirror, and the
  false-negative bullet's `input mtime exactly equal to output mtime`, which row 19's own cell
  scopes to §3's input-versus-output Verdict. Every other fixture writes its two sides strictly
  apart, so a body spelling ANY of the three comparisons `>=` passed every battery row, the
  false-negative battery, the wiring assertions and the live smoke test. The cheapest of the
  three to write wrong is the declaration cache: `bundle-types.mjs:70`, re-read as TEXT and
  re-derived character for character in consolidation, is
  `if (!force && existsSync(OUTPUT) && statSync(OUTPUT).mtimeMs >= newestSourceMtime())`, so
  the wrong reading is the PRODUCER's own guard read back, and §4's `npm run build` clause
  cannot clear the finding it raises because `:70` declines to regenerate at exactly this
  boundary. **Closed by ADD, after DELETE, NARROW and STRENGTHEN A VALUE were tested and
  refused** — the strict threshold IS what §7's cell claims as its mitigation, nothing entails
  it, each rule is already scoped to one comparand pair, and the values are correct: what is
  missing is an ELEMENT. THREE TWINS therefore, in row 33's shape so nothing is renumbered and
  NO ROW is added (`OP22-3`), each in `C19-2`'s own remedy shape — the host row's OWN clean
  fixture with ONE mtime slot re-pinned to EXACT equality, verdict the ABSENCE of that row's
  own kind at BASELINE, mutant that comparison spelled `>=`, which fails on the twin's own
  fixture and needs no injection point — and none of the three can FAIL a correct
  implementation, equality being clean under the mandated reading (`OP16-3`'s and `CF20-1`'s
  disqualifier). **NEW OBLIGATIONS NAMED, exactly three:** each threshold is STRICT at
  equality — §3.2's over `bundleAt`, §3.3's over the cache mtime, §3.4's over the newest
  beneath `dist/plugin`. Each twin asserts the absence of ONE kind and nothing else,
  `C19-2`'s own consolidation ruling: on all three fixtures the coverage entries and the other
  kinds are free variables, and row 6's twin re-pins a SOURCE forward, which may reach
  `checkUnitFreshness`, so an aggregate assertion would have failed a correct implementation.
  ⚠️ **NONE of the three equalities is LIVE on this working copy, re-derived in consolidation
  with `git status --porcelain` empty before and after and no import of
  `scripts/bundle-cli.mjs`:** newest `packages/*/src/**.ts` **1787171860413.817** against
  `packages/cli/.dts-cache/index.d.ts` at **1787172303152.899**; newest of the six
  `PLUGIN_ASSET_ENTRIES` **1787049783306.418** against the newest beneath
  `packages/cli/dist/plugin` at **1787414591040.448**; and **1787171860413.817** against
  `bundleAt`, the MINIMUM over the five chunk-pattern files, at **1787414591004.189**. That is
  why those twins are the only elements there are.

- **`OP36-1`, medium, SUBSTANCE — §4's `PASS`-line `nothing was checked` carve-out is a
  TWO-conjunct conjunction whose ONLY element holds BOTH conjuncts, so each was individually
  DELETABLE.** Enumerated independently over §6: exactly THREE elements assert a rendered
  `PASS` line — row 18 at `M = 0` by EQUALITY, row 39's MAIN half at `M = 1` by EQUALITY and
  row 39's NEGATIVE twin in the carve-out shape — with conjunct values (false, false),
  (false, false) and (true, true), so no fixture held exactly one. A body spelling the
  carve-out "no unit was compared" ALONE prints the suffix with the clause list suppressed on
  row 39's CACHE-ONLY TWIN, where `declaration-cache` RAN and read `full`; one spelling it "no
  comparison ran" ALONE prints it on row 39's ALL-NOT-RUN TWIN, where every unit but
  `packages/cli` COMPARED. Both passed every element in §6, the false-negative battery, the
  wiring assertions and the live smoke test. **Fixed by policy step 3, STRENGTHEN A VALUE:**
  the two twins round 35 added are WIDENED, each also asserting the rendered `PASS` line in
  §4's ORDINARY shape — parenthetical PRESENT, no `; nothing was checked`, both counts and the
  reason off that twin's OWN fixture (`OP16-3`) — with the mutant for each being the carve-out
  with the OTHER conjunct deleted. DELETE was checked FIRST and refused: neither conjunct
  entails the other, the two trees separating them in opposite directions, and `OP15-5` traces
  the carve-out to research Q5's quiet-clone requirement; NARROW had nothing to narrow. The
  **THE CLAUSE LIST IS SUPPRESSED ON THIS LINE AND ON NOTHING ELSE** rule rides the same
  predicate and takes the same two elements, which is why it is edited in the same sentence
  rather than two hundred lines away.

- **`OP36-2`, medium, SUBSTANCE — §4's rule that the `PASS` line prints on the exit table's
  FIRST TWO rows only has THREE accepting elements and NO excluding one.** Rows 18, 39's MAIN
  half and 39's NEGATIVE twin each assert that the line is THERE; not one element anywhere
  asserts that a run holding a non-advisory finding renders NO `PASS` line. Every §6 site that
  SAYS so — row 18's cell, row 38's fourth-assertion cell, row 35's fixture cell — is FIXTURE
  PROSE explaining which row owns which summary slot. Re-derived read-only in consolidation:
  `check-citation-runs.mjs`'s `console.log` at `:289-291` follows all three of its
  `process.exit(1)` guards, at `:267`, `:278` and `:287`, and carries no condition of its own,
  so "PASS" there means "did not exit 1" — and a `formatFindings` copying that shape WHOLE
  (standing rule 7), printing the line ABOVE the grouped findings so row 18's ORDER conjunct
  passes and leaving the STANDALONE summary in its own slot so row 38's FOURTH assertion
  passes, prints a green verdict on every flagless findings-carrying run: at `check:all`
  member 0, and under `pretest` roughly 199 lines above vitest's summary at exit **0** by
  design. **Fixed by policy step 3, STRENGTHEN A VALUE:** §6 row 38's rendered half gains a
  FIFTH assertion in that row's own negative-assertion shape (`OP16-4`) — no line beginning
  `check-stale-dist: PASS` — with the whole-shape copy as its mutant. DELETE was refused: §4's
  exit table, its prefix rule and its **SKIPPED UNITS ARE NAMED ON BOTH PATHS** rule all rest
  on this line's absence, which is what gives the STANDALONE slot a reason to exist.

- **`OP36-3`, low, SUBSTANCE — §4's plan gate has an element for each DISJUNCT holding and
  NONE for both FALSE.** Row 34 covers findings-with-all-`full`, row 39's MAIN half
  zero-findings-with-a-non-`full`-entry; the only element asserting the plan's ABSENCE is row
  39's NEGATIVE twin, where the SECOND disjunct is TRUE, so it exercises the
  `nothing was checked` carve-out and never the gate. A `formatFindings` whose gate is the
  NEGATION of that carve-out alone — the reading §4's own "and on nothing else" licenses when
  taken as a definition — renders `check-stale-dist: remedy plan:` and an indented
  `re-run the check` on row 38's CLEAN fixture, which is the STEADY STATE this design names:
  every local `npm test` against a built tree, `check:all` member 0, both `test` legs of
  `ci.yml:129`, line 5 of §5.1's measured 204 under `pretest`. **Fixed by policy step 3,
  STRENGTHEN A VALUE:** row 38's CLEAN side gains a rendered plan-ABSENCE assertion — a CLEAN
  side carrying the discriminating half being row 49's own shape, `CF8-2` read the other way —
  and §4's "and on nothing else" is SCOPED to DEGRADED runs so it can no longer be read as the
  gate's definition. Row 38's clean side is the host because its zero-finding all-four-`full`
  state is FORCED by the `C15-2` sibling-fixture rule, the complete-cli-build rule and the
  round-26 plugin-SOURCE clause rather than incidental (`OP16-3`).

**COUNTS THIS EDIT CHANGES, and their sites.** ⚠️ **ONE live figure moves, and it had ALREADY
drifted before this round touched it:** §6's mutant-count census inside row **47**'s
ROUND-25 TWIN cell, **13** to **14**, at that ONE live site. Re-derived read-only at `d0b706e`
with that cell's own quoted command and never restated as a subset (`CF14-4`): TEN hits in
other rows' own cells (rows 1, 4, 5, 28, **34** and 44 once each, rows 39 and 40 twice each),
TWO at row 47 and TWO inside the wiring-assertion comment blocks. The drift is round 35's own
— that round re-derived **13** at `a5605e9` BEFORE its apply and then wrote a standing-rule-8
sentence into row **34** carrying a count word before the token that command greps for, so the
promise that every ADDED ELEMENT would be spelled `SECOND MUTANT` held for the elements and
not for the prose beside them. ⚠️ **The `correctness` lens proposed leaving that figure alone
as sha-pinned history and the `operability` lens proposed correcting it; consolidation TAKES
the correction**, because the sentence sits in a LIVE section and is read there as the current
state, and the entry names the command rather than a bare number. Round 36's own additions are
spelled `Its mutant is` and `Mutant =`, which that command does not match, so **14** is the
figure before and after this apply — re-derived on the applied candidate. **Everything else
holds, re-derived rather than restated:** §1's **49** battery rows and its `~680` estimate,
NO row, NO twin's host row and NO fixture being added; §6's row numbering and its
command-derived outside-citer registry, nothing being renumbered; §3's **21 of the 30**, its
**20**/**9** split, §0's `29 PATHS`, §8(l)'s NINE, rule 4's live member count of **ZERO**,
§3.3's THREE clauses, §5.1's **204** and its line **5**, and §4's kinds, reasons, remedy rows,
recipe steps and exit rows. TWO further things move that are stated as figures NOWHERE: the
battery's padded row width, **23,419** at `d0b706e` and re-padded by `prettier` in this apply
across all **51** of its lines — the after-figure deliberately NOT written down, three lenses
moving cells in that one table (`C11-4`); and the `eo-` glob census, 31 asterisk forms and one
underscore form becoming 32 and none. ⚠️ **THREE twins and ONE clean-side assertion are added, and the
clean-side one adds ONE INVOCATION — the formatter read on row 38's CLEAN state**, which no
half of that row previously performed; it is recorded here rather than hidden inside an
"adds no row" claim.

**SITES, all moved together per standing rule 9.** `CF36-1` and `CF36-3`: §6 row 47's
ROUND-25 TWIN cell in twenty chunks, plus the preamble's structural-clause enumeration and the
preamble's new seventh-clause paragraph and command. `CF36-2`: that same cell, one literal.
`C36-1`: §3's Verdict paragraph, which states the strict convention once for all five; §3.2's
Rule; §3.3's declaration-cache rule; §3.3's clause-2 bullet, whose "the battery's one
equal-mtime element" is scoped to round 19 rather than left in the present tense; §3.4's Rule;
§6 rows **5**, **6** and **24**; §6's false-negative equality bullet, whose GENERIC wording is
what let it read as the boundary element for all five; §6's `C20-2` per-half paragraph; §2's
`isCompilerOutput` comment and §3's minus-bullet-attribution paragraph, the TWO live
UNQUALIFIED "row 5's twin" citers, one of them split across a line break and invisible to a
per-LINE search (standing rule 1), each re-pointed to the ROUND-23 twin; §7's
`Second-granularity mtimes` row; and §8(e). `OP36-1`: §4's `PASS`-line carve-out paragraph,
carrying both the carve-out sentence and the suppression rule that rides its predicate; §2's
mirror of that carve-out; §6 row **39**'s ALL-NOT-RUN and CACHE-ONLY twins; and §6's
prefixed-rendering enumeration. `OP36-2`: §4's **SKIPPED UNITS ARE NAMED ON BOTH PATHS**
paragraph; §4's prefix rule, whose "the `PASS` line when there is one" is the same obligation;
§2's `formatFindings` comment, the one site stating WHEN there is no `PASS` line; §6 row
**38**'s rendered half; and §6's prefixed-rendering enumeration. `OP36-3`: §4's **A DEGRADED
RUN CARRIES A NEXT ACTION** gate; §4's "and on nothing else" sentence, scoped to DEGRADED
runs; §4's non-vacuity SET; §2's mirror of the gate AND of that SET; §1's `report.mjs` cell;
§6 row **38**'s CLEAN side; and §6's prefixed-rendering enumeration. ⚠️ **§1's `report.mjs`
cell was found by the SITE WALK and named by no lens** — its "withheld ONLY on the
`nothing was checked` PASS" reads as "printed everywhere else", which is the counterexample
body verbatim, at the cell an implementer builds that file from.

**Checked and NOT changed, stated rather than omitted:** §0's eleven requirement rows, none of
which states a threshold or traces to §4's line shapes, and requirement 1's citation of row 5
which already reads `Row 5's round-23 TWIN`; §1 apart from the `report.mjs` cell; §2 apart from
the three citations and mirrors named above, its signatures, arity and per-function comments
being untouched; §3.1, which has no mtime comparison at all; §3.3's unit-state reason table and
its cli rules; §3.5, which keys on BYTES and is not one of the five mtime comparisons; §4's
exit table, its eight per-kind line shapes, its eight-row remedy table and its five ordered
recipe steps; §5 and §5.1; §6's false-negative battery, its `C22-1` marker-state enumeration,
its wiring assertions and its live smoke test, which reads no rendered text; §7's other twelve
rows; and §8's residuals, §8(l)'s NINE included.

**Re-run on the FIXED candidate, all read-only and all returning nothing:** the escape-aware
row census, the orphan-row sweep, the comma-then-blank sentence-split sweep, the new
seventh-clause sweep, the editor-directive and placeholder sweeps below the `## 0.` heading,
passes 1, 2 and 3, the anchor grep, and `prettier --parser markdown` plus `norm()`
plus `diff` with a second prettier run byte-identical to the first. The dollar census is
unchanged across the apply, measured before and after rather than restated from a list.

**No working-tree mutation:** `git status --porcelain` was empty at `d0b706e` before and after;
`scripts/bundle-cli.mjs` and `scripts/repo-census.mjs` were never imported;
`scripts/bundle-types.mjs`, `scripts/check-citation-runs.mjs` and
`./mtime-propagation-probe.mjs` were read as TEXT; every probe ran `git`, `ls`, `grep`, `awk`,
`sed`, `wc`, `python3` or `prettier` reading files as text, and every scratch write went to
the scratchpad.
