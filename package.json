{
  "name": "engineering-orchestrator-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Engineering Orchestrator monorepo root (private workspaces root; the published CLI package is packages/cli, name \"engineering-orchestrator\").",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "build:clean": "tsc -b --clean",
    "typecheck": "tsc -b",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "test:live": "vitest run --config vitest.live.config.ts",
    "test:e2e": "vitest run --config e2e/provisioning/vitest.config.ts && vitest run --config e2e/matrix/installation/vitest.config.ts && vitest run --config e2e/matrix/git/vitest.config.ts && vitest run --config e2e/matrix/orchestration/vitest.config.ts && vitest run --config e2e/matrix/connector/vitest.config.ts && vitest run --config e2e/live/vitest.config.ts && vitest run --config e2e/release/vitest.config.ts",
    "check:e2e-types": "tsc -p e2e/provisioning/tsconfig.json --noEmit && tsc -p e2e/matrix/installation/tsconfig.json --noEmit && tsc -p e2e/matrix/git/tsconfig.json --noEmit && tsc -p e2e/matrix/orchestration/tsconfig.json --noEmit && tsc -p e2e/matrix/connector/tsconfig.json --noEmit && tsc -p e2e/live/tsconfig.json --noEmit && tsc -p e2e/release/tsconfig.json --noEmit",
    "lint": "eslint . --ignore-pattern 'spikes/**'",
    "lint:fix": "eslint . --fix --ignore-pattern 'spikes/**'",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish",
    "check:workspace-count": "node scripts/check-workspace-count.mjs",
    "check:hygiene": "node scripts/check-repo-hygiene.mjs",
    "check:release-notes": "node scripts/check-release-notes.mjs",
    "check:engine-pin": "node scripts/check-engine-pin.mjs",
    "check:all": "npm run check:workspace-count && npm run check:package-graph && npm run check:hygiene && npm run check:release-notes && npm run check:engine-pin",
    "check:package-graph": "node scripts/check-package-graph-acyclic.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "2.31.0",
    "@commitlint/cli": "21.2.1",
    "@commitlint/config-conventional": "21.2.0",
    "@eslint/js": "10.0.1",
    "@types/node": "24.13.3",
    "@vitest/coverage-v8": "4.1.10",
    "eslint": "10.7.0",
    "eslint-config-prettier": "10.1.8",
    "globals": "17.7.0",
    "prettier": "3.9.5",
    "typescript": "6.0.3",
    "typescript-eslint": "8.64.0",
    "vitest": "4.1.10"
  }
}
